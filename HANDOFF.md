# 文秘写作当前交接

> 新的开发任务先读本文件一次即可；同一任务后续不重复读取。历史开发日志在 Git，不在这里累积。

## 当前状态（2026-08-23）

- 项目：`D:\wenmixiezuo`，分支 `codex/desktop-entry`。
- V6 核心创作工作台已在本地完成开发、旧实现清理和发布前验收，唯一执行记录为 `docs/NEXT_CORE_WORKFLOW_DEVELOPMENT_CHECKLIST.md`，382/382 项已勾选。
- 本批次没有生产部署授权，也没有执行发布、服务切换、生产迁移或生产数据操作。生产站点仍为 `https://wenmixiezuo.com`；生产 API/Web/迁移版本保持本批次开工前状态，不得把本地 V6 结果误写成已上线。
- 本地主流程固定为：设定 → 故事线 → 分卷 → 事件 → 章节；辅助工具固定为：资料库、取名、团队、任务、灵感、设置。
- 桌面沿用左侧书籍栏，宽屏顶部同排显示 5 个主流程和 6 个工具，中宽工具收进 3×2 弹层；手机保持顶部两排导航，没有底部导航或第三排。
- 本地最新迁移为 `0067_chapter_editor_synthesis_requests.sql`；0065 建立 V6 核心对象/账本/版本底座，0066 建立七岗位 AI 节点批次，0067 把主编综合改为作者显式触发。三次迁移均只向前追加，不改写历史正文、结算或旧迁移。

## V6 产品事实

- 设定页包含开书资料、完整设定库、逐项多成员候选、作者编辑稿、副编整理和主编确认；正式确认前只形成临时计划资料包，不写正史。
- 故事线支持一条核心线+支线、双核心线、多核心线、单元故事；线路生命周期与卷内参与状态分离，故事地图只投影权威计划/实际总账。
- 分卷支持“从零想、有方向、完整卷纲”三种作者入口，两条实质不同路线、严格勾选拼合、表达方案和主编可编辑整理。
- 事件页按因果事件链、角色功能占位、角色绑定和事件节奏推进；白话节奏映射到后台明确作用域模板，单事件卷保留完整对象与结算。
- 章节页统一章链、近期详细章纲、正文、多主笔候选、事实/文学/体验三席、作者主动主编综合和章→事件→卷结算；章纲与不可变正文在后台仍是独立版本对象。
- 规划只记录未来；只有作者最终定稿正文的结算可以写入实际账本。AI 候选、示例、未确认章纲和三席报告不能成为事实。
- 七岗位为主编、副编、编剧、主笔、事实席、文学席、体验席；岗位池和模型绑定后台可配置并版本化。前端不显示模型名、擅长、速度或成功率，只显示真实头像、姓名、岗位、供应公司、消耗等级和状态。
- 同岗位同批次冻结同一 `context_pack_id/hash`、作者输入版本、Skill、模板和模型绑定；同一模型不得伪装成异模型独立复核。ContextCompiler 仍是唯一资料包权威，成员不保存私人全书记忆。

## 已删除与兼容

- 新闭环通过后已删除 `PlanningWorkspace.tsx`、`BrandingDesignDialog.tsx`、`TeamWorkspace.tsx`，源码和 Web 构建产物均为零有效引用。
- 旧“信息、规划、章纲、正文”不再作为独立导航入口；需要的历史能力嵌入新五页，没有第二套影子流程。
- 旧 `view` 深链接只保留显式映射并立即规范化为 `stage`；例如 `view=master` 转为 `stage=volume`。历史任务、版本、正文、来源引用和结算继续读取原权威表。

## 最终验收证据

- `pnpm dlx npm@11.13.0 run verify:full`：成功；188/188 个测试文件、802/802 个测试通过，Contracts/API/Worker/Web 类型检查和生产构建通过。
- 迁移：0065/0066/0067 的空库、旧库升级、重复执行和回滚 3 文件 7/7 通过；账号/多书隔离和历史 owner 迁移测试通过。
- 定向集：核心闭环 16 文件 70/70；V6 UI/工作台 6 文件 36/36；模型失败恢复与安全边界 10 文件 73/73。
- 真实 AI：复用本机已有 Codex 登录态，以只读、临时、禁工具、禁止现金回退模式对七岗位各执行一次真实探针；7/7 成功，`cashCostCny=0`，只保留岗位、Token、耗时、长度和输出哈希，不保存答复正文。
- 浏览器：1366/1440/1536/1672/1920/360/390/430 共 8 个视口 × 设定/故事线/分卷/事件/章节/团队 6 页，共 48 张截图，0 失败。
- 桌面稳定区域相似度：设定 97.05%、故事线 97.78%、分卷 98.10%、事件 98.19%、章节 97.01%、团队 98.64%；人工逐区检查通过，手机两排导航保持。

## 基础安全与后续发布

- Key 只来自环境变量，不能进入数据库、Git、日志、上下文、导出或备份；核心业务读写始终携带已验证会话的 `owner_id + book_id`。
- 正文是不可变完整版本；普通清理不得覆盖正式作者原文、结算或已合并迁移。永久删除作者数据仍需影响预览、`YES` 和二次确认。
- 若老板后续单独授权发布，必须按 `docs/DEPLOY.md` 执行：Web 暂存构建、保留旧哈希、原子切换；API/Worker/迁移向后兼容，在途任务连续 30 秒为零并立即复核，逐服务切换且保留回滚版本。
- 不取消、暂停或改写作者任务制造发布窗口；生产异常优先回滚，不在生产数据上边猜边改。

## 开发入口与文档权力

- 新功能先读本文件一次，再按 `docs/DEVELOPMENT_WORKFLOW.md` 和当次新建的开发/验收清单推进。
- V6 当前合同以 `docs/PRODUCT.md`、`docs/DATA_MODEL.md`、`docs/API.md` 与本次清单为准；旧规格只用于定位兼容范围，不能否决老板的新决定。
- 账号与书籍隔离、作者正式数据不可变、密钥/会话、迁移和生产回滚是长期硬门禁。

## 历史与恢复

2026-08-21 规则精简前的完整原文保留在 Git 基线 `e8517d9`。默认不读；需要追溯旧编号或恢复某条细节时再查对应文件历史。