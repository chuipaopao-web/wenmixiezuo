# 文秘写作当前交接

> V7 已替换旧产品并成为唯一当前产品。不要恢复、引用或重新实现旧版页面、工作流和书籍投影。

## 当前权威入口

- 作者端：`https://wenmixiezuo.com/`
- 独立后台：`https://admin.wenmixiezuo.com/`（后台应用位于该域名的 `/v7/` 路由）
- 本地作者端：`http://127.0.0.1:43110/`
- 本地独立后台：`http://127.0.0.1:43110/v7/`
- 当前代码区：`coauthoring-v7/`
- 当前分支：`codex/light-coauthoring-v7`

生产 API、Worker、Caddy 和 V7 静态发布当前仍运行第 69 批已验收版本。2026-08-31 09:30 公网只读复核显示生产运行包为 `wm-v7-20260831-080200-309bc4c8`，API 与 Worker 发布号一致且 Worker ready；作者端与独立后台为 200，未登录会员接口为 401。第 70、71 批均未切换生产：当前部署私钥被服务器的 `root` 公钥认证拒绝，阿里云控制台也没有有效登录态；自动安全审查禁止继续枚举账号，因此生产部署受外部访问通道阻塞，未做任何生产写入。生产数据库和服务器运行目录始终是线上事实来源，本地数据不能回写或替代生产数据。

## 新任务最小阅读

1. 当前正在执行的唯一工单：`coauthoring-v7/docs/worklists/V7-AUTHOR-WORKFLOW-RELIABILITY-20260831-71.md`
2. 开发规则：`AGENTS.md`
3. V7 代码交接：`coauthoring-v7/HANDOFF.md`
4. 只有涉及数据、接口或部署时，按需读取 `docs/DATA_MODEL.md`、`docs/API.md`、`docs/DEPLOY.md`

历史工作清单和 Git 历史只用于审计，不是当前产品规格。已合并迁移及其历史表名必须保留；它们不代表旧产品仍在运行。

## 当前产品边界

- 只保留 V7 作者端、V7 独立后台和 V7 创作流程。
- 账号、密码登录、会话、会员、反馈、安全、用量、备份和恢复是共享平台底座，由 V7 继续使用。
- 旧版书籍、资料库、任务、正文和工作流不迁移到 V7，也不在作者端展示。
- 正文不可变版本、`owner_id + book_id` 隔离、API Key 环境变量边界和任务审计继续有效。
- 本地数据永久删除仍必须先生成影响预览，并经过 `YES` 与 `CONFIRM:<previewId>` 两次明确确认。

## 当前实现与发布状态

- 旧作者端、旧专用 API、旧 Worker 循环、本地向量运行时及其依赖、测试和文档已退出当前树；已合并迁移和历史表名未改写。
- 本地作者数据已经按双确认收据永久删除；缓存、截图、临时备份和旧发布副本已删除。`artifacts/` 只保留部署密钥、当前生产运行包和当前本地 V7 静态包。
- 当前依赖锁只含 6 个 V7/共享 workspace；当前 105 个已合并迁移全部保留，既有 102 个历史迁移字节未改写，第 71 批新增 0104/0105 只做前向兼容。所有作者生产数据均未删除、覆盖或回填。
- 旧开书团队初始化、编辑租约、旧岗位提示编译和旧成员专业人设运行闭包已删除；V7 成员只保留身份、模型和当前任务责任，题材融合人设仍按书籍任务装配。
- 当前 V7 未使用的 Codex、opencodego 连接器、配置、启动变量和测试已删除；V7 当前六个模型档案与方舟 Coding/Agent Plan 路由已回归通过。
- 71C 对 21 个候选文件（5,345行）完成 `tsc --listFilesOnly` 与静态 import BFS 零运行引用审计；但删除补丁两次被自动安全审查拦截，原因是候选横跨任务、预算、SQLite、ContextPack、设定/资产及作者文本安全面。本批实际没有删除这 21 个文件，不得拆分或绕过审查；后续只有获得针对该影响面的新知情确认后才能另批处理。
- 每个新创作任务固定调用一次资料策划 Agent；题材融合人设只在任务期生成，不绑定岗位。全书、卷、链等可复用同一通用方法，但下层只接收活动父层的结构化方法应用、因果、伏笔、开放问题和当前接口，不接收方法教材或上层全文。
- 作者任务中心已补齐部分失败、耗时、成员详情、失败道歉、保留成果和原位恢复；批量清理只归档失败/中断开书任务，不会移除等待作者确认/决定的任务。独立后台已核验资料包、任务人设、方法、模型、用户和会员页面；会员开通/续费/撤销使用管理员范围幂等键和单事务不可变流水。
- 第 71 批隔离浏览器以测试作者提交 1308 字开书想法并实际入库；测试书“边城沙盘”已访问开书资料、设定、时光机、卷、链、章、资料库、任务、团队和会员/反馈。桌面与 390px 无横向溢出；设定失败保留结果，时光机失败可继续未完成步骤、返回全书方向或设定修改，反馈真实写入隔离库。
- 第 71 批同时把目录外题材的普通异常转换为可修正的 400 领域错误；任务页长想法压缩为 48 字摘要；青铜升级付费从办理日开始，付费档不能直接降为青铜，后台默认白银/98元并显示“升级并记录收入”。
- 本地 `RELEASE_ID` 为 `wm-v7-20260831-183000-83eebb2a`；71A/71B 提交为 `42c5ce1`、`83eebb2`，71C 最终提交和源码归档尚未冻结。终审后全量门禁632项通过、3项既有跳过；最新干净静态候选为 `24717cb9eac7309dd487`（9文件，manifest `4683b6317e5727e7b87f0ddcbccc6342776d30f81cd535a83456d60b609df0e0`）。生产仍为 `wm-v7-20260831-080200-309bc4c8`，上一生产实现提交为 `c347d32`、发布元数据提交为 `47ed28e`。恢复正确 SSH 运维账号或重新授权现有公钥后，从第 71 批在途任务门禁继续，不重做本地改造。
- Git 代码清理基线为 `899ed10`，当前开发基线为 `9a700a9`；不得重置、覆盖或强推。远程仓库推送不属于当前生产发布通道，不能用远程分支状态判断生产版本。
