# 文秘写作当前交接

> V7 已替换旧产品并成为唯一当前产品。不要恢复、引用或重新实现旧版页面、工作流和书籍投影。

## 当前权威入口

- 作者端：`https://wenmixiezuo.com/`
- 独立后台：`https://admin.wenmixiezuo.com/`（后台应用位于该域名的 `/v7/` 路由）
- 本地作者端：`http://127.0.0.1:43110/`
- 本地独立后台：`http://127.0.0.1:43110/v7/`
- 当前代码区：`coauthoring-v7/`
- 当前分支：`codex/light-coauthoring-v7`

生产 API、Worker 和 Caddy 已于 2026-09-02 18:04 CST 切换到第82批 `wm-v7-20260902-174800-e5b21c74`；静态版本保持第80批 `363e8b5c7989d05e8fba`（manifest SHA-256 `7d5e34a71de63bd9ff90606571368b890f91dc359ebeafad29019e1ac72a971d`，本批 `static_switch_required=0`）。106 个迁移、发布号、公网健康、作者端、独立后台和服务状态已独立验证；`/health` 返回 `status=ok`、`worker=ready`、`canStartModelTasks=true`、`releaseId=wm-v7-20260902-174800-e5b21c74`，API/Worker/Caddy 均为 `active`。本批完整备份为 `20260902T100232Z-423695`；第81批应用 `wm-v7-20260902-100400-40c109f8` 继续作为立即回滚保留点，静态 previous 指针为 `37fde3ad807a5b4c87c6`。为满足发布空间门禁，部署前 prune 仅删除不在指针链中的旧发布目录和失败半成品目录，作者数据、数据库、备份和回滚版本未触碰。阿里云 `admin + sudo` 公钥自动发布通道已验证，后续不再需要老板打开 Workbench 手工输入部署命令；生产数据库和服务器运行目录始终是线上事实来源，本地数据不能回写或替代生产数据。

## 新任务最小阅读

1. 当前已完成的最新工单：`coauthoring-v7/docs/worklists/V7-OPENING-CONFIRM-DEADLOCK-20260902-84.md`。**第84批修复真实作者开书确认死锁（账号 zhengxifeng2024@163.com 卡在"页面内容已经修改，请先提交主编复审"，永远无法建书）：根因是确认比对两侧不对称——提交侧经 parseOpeningPackage 重建（省略空 authorInstructions、清空 authorNotes/goal/dilemma/boundary），而"采纳全部建议"未触发模型重做时 revise() 直接把作者修订稿（含这些字段+revisionDirective）存为 active 候选，哈希永远不等。修复：`openingPackageHash` 替换为 `openingPackageUnchanged`（两侧同投影剥内部字段+键序无关稳定比较，候选侧不再重 parse）。测试 `tests/integration/domain/v7-opening-confirm-consistency.test.ts` 5/5、相邻开书域 20/20，apps/api 类型检查 0 错误。同工单含分卷规划西施（glm-5.3 coding）稳定失败的只读诊断 SQL（v7_planning_generation_runs / v7_planning_model_calls），部署后由 codex 在服务器执行定性。`RELEASE_ID` = `wm-v7-20260902-203200-84a1f2c6`，第82批部署流程可复用。**
2. 上一批工单：`coauthoring-v7/docs/worklists/V7-PLANNING-LIBRARY-REF-RECOVERY-20260902-82.md`。**第82批修复真实作者时光机"规划成员引用了本轮未提供的后台资产"整树硬失败（账号 1746495718@qq.com、wanzidoufutang@163.com 受影响，127分钟任务报废）：`validateDesignStrategy` 对 libraryRefs 改为确定性归一（候选包外/缺说明/重复引用丢弃、类型漂移按 key 归一、超限保留前N项），剧情不改写、不调用模型；生成提示词同步声明上层引用会被忽略。**实现提交 `6c090ac`、工单+发布号提交 `2b7fa7c`（`RELEASE_ID` = `wm-v7-20260902-174800-e5b21c74`）。测试证据：候选包专项 10/10、规划+设定集成 49/49、第81批快照回归 19/19，backend dist 重建后 apps/api、apps/worker 类型检查 0 错误。**第82批已部署上线：`DEPLOYMENT_PASSED`，`apps=/opt/wenmi-releases/wm-v7-20260902-174800-e5b21c74/source/apps`，`static=/opt/wenmi/releases/versions/363e8b5c7989d05e8fba`，`backup_run_id=20260902T100232Z-423695`，`completed_at=2026-09-02T10:04:37Z`。部署流程复用第81批 prune→上传→stage→backup→preflight→cutover→postdeploy；首次上传误用第81批源码包被远端 SHA 门禁拒绝，线上未切换，修正第82 wrapper 后重跑成功。**
3. 第83批审计结论（无代码行为变更，新增一个服务器脚本）：`coauthoring-v7/docs/worklists/V7-REPO-STRUCTURE-AUDIT-20260902-83.md`。**V7 在当前分区已是干净独立闭包（201运行源码零孤儿、无真实V7→V6引用、apps/*是共享平台基座而非旧版遗留），不新建分区。** 新增 `artifacts/deploy/server-disk-hygiene.sh`：默认 dry-run，`sudo APPLY=1 bash server-disk-hygiene.sh` 才执行；清理 journal/apt/npm缓存//tmp部署残留/非指针静态旧版本/30天未变动旧暂存发布；**数据库、最新3份备份、回滚链、retained-release-evidence 一律不碰**。建议第82批部署完成后顺手在服务器执行一次（先 dry-run 看输出再 APPLY）。
4. 第81批工单（已上线）：`coauthoring-v7/docs/worklists/V7-CREATION-CHAR-BUDGET-RECOVERY-20260902-81.md`。一次性修复全书字符预算门禁这一类问题：卷/链/章生成门禁与时光机同源（正文超预算即整门禁失败）、外加事实账本 40 条硬上限截断；实现提交 `3239108`、工单证据提交 `42cbb86b`、发布号提交 `1958fabb`（`RELEASE_ID` = `wm-v7-20260902-100400-40c109f8`）。
5. **第81批已部署上线**。最终部署结果：`DEPLOYMENT_PASSED`，`release=wm-v7-20260902-100400-40c109f8`，`apps=/opt/wenmi-releases/wm-v7-20260902-100400-40c109f8/source/apps`，`static=/opt/wenmi/releases/versions/363e8b5c7989d05e8fba`，`backup_run_id=20260902T045012Z-418412`，`started_at=2026-09-02T04:45:24Z`，`switch_started_at=2026-09-02T04:51:49+00:00`，`completed_at=2026-09-02T04:51:54Z`。部署前先执行 prune 清理了未引用旧发布和失败半成品；部署过程中修正了 `artifacts/deploy/deploy-r81-1958fabb.sh` 两个编排问题：运行闭包报告目录需由 `wenmi` 可写、备份锁只做启动探测避免和 `backup.sh` 自锁冲突。冻结物料仍保留在 `artifacts/`：
   - 源码包 `artifacts/deploy/1958fabb-source.tar.gz`（SHA256 `8a0cfa3c0d60b0501573cdea36719ac419b613e16d560fe7a185f6f477a9c8a3`，4204122 字节，`git -c core.autocrlf=false -c core.eol=lf archive 1958fabb` 生成）。
   - 部署编排 `artifacts/deploy/deploy-r81-1958fabb.sh`（阶段顺序 stage→backup→preflight→cutover→postdeploy，flock 防并发，失败自动回滚，期望迁移数 106）。
   - 磁盘清理 `artifacts/deploy/prune-unreferenced-releases-r81.sh`：删除 `/opt/wenmi-releases` 下不在指针链的旧目录（保留第79/78/77/76批四个目录），带 apps 符号链接校验，必须先于部署执行以过磁盘门禁。
   - 一键封装 `artifacts/deploy-batch81.ps1`：本地哈希校验→LF 规范化→上传并执行 prune→上传脚本与源码包→远端 sha256 校验→后台启动部署→每45秒轮询日志至 DEPLOYMENT_PASSED/DEPLOY_R81_FAILED，最后输出 `deployment-result.txt` 与 `RELEASE_ID`。执行方式：`powershell -NoProfile -ExecutionPolicy Bypass -File D:\wenmixiezuo\artifacts\deploy-batch81.ps1`（约165分钟超时上限，通常几十分钟完成）。
   - 静态身份分支：web 静态不嵌入 RELEASE_ID，本批构建应复现第80批静态版本 `363e8b5c7989d05e8fba`；脚本内置校验 manifest sha256 `7d5e34a71de63bd9ff90606571368b890f91dc359ebeafad29019e1ac72a971d` 后跳过静态切换，仅切换应用。
   - 已回写部署证据并同步 `coauthoring-v7/HANDOFF.md`。注意：`artifacts/deploy-batch81.ps1` 的轮询只看日志最后3行，成功结果最后3行不含 `DEPLOYMENT_PASSED` 时可能继续等待；本次通过读取 `deployment-result.txt` 与 `/opt/wenmi/RELEASE_ID` 独立确认上线成功。
6. 开发规则：`AGENTS.md`
7. V7 代码交接：`coauthoring-v7/HANDOFF.md`
8. 只有涉及数据、接口或部署时，按需读取 `docs/DATA_MODEL.md`、`docs/API.md`、`docs/DEPLOY.md`
历史工作清单和 Git 历史只用于审计，不是当前产品规格。已合并迁移及其历史表名必须保留；它们不代表旧产品仍在运行。

## 当前产品边界

- 只保留 V7 作者端、V7 独立后台和 V7 创作流程。
- 账号、密码登录、会话、会员、反馈、安全、用量、备份和恢复是共享平台底座，由 V7 继续使用。
- 旧版书籍、资料库、任务、正文和工作流不迁移到 V7，也不在作者端展示。
- 正文不可变版本、`owner_id + book_id` 隔离、API Key 环境变量边界和任务审计继续有效。
- 本地数据永久删除仍必须先生成影响预览，并经过 `YES` 与 `CONFIRM:<previewId>` 两次明确确认。

## 当前实现与发布状态

- 旧作者端、旧专用 API、旧 Worker 循环、本地向量运行时及其依赖、测试和文档已退出当前树；已合并迁移和历史表名未改写。
- 本地作者数据已经按双确认收据永久删除；已确认无用的缓存、截图、临时备份和旧发布副本已删除。`artifacts/` 仅作当前部署、回滚与验证证据，不是生产数据来源；临时构建不得冒充线上版本。
- 当前依赖锁只含 6 个 V7/共享 workspace；当前 106 个已合并迁移全部保留，既有 102 个历史迁移字节未改写，第 71 批新增 0104/0105 与第74批 0106 只做前向兼容。所有作者生产数据均未删除、覆盖或回填。
- 旧开书团队初始化、编辑租约、旧岗位提示编译和旧成员专业人设运行闭包已删除；V7 成员只保留身份、模型和当前任务责任，题材融合人设仍按书籍任务装配。
- 当前 V7 未使用的 Codex、opencodego 连接器、配置、启动变量和测试已删除；V7 当前六个模型档案与方舟 Coding/Agent Plan 路由已回归通过。
- 第72批在老板再次知情确认后物理删除21个零运行引用旧源码，并删除3个重复43180本地启动器和1个一次性标签脚本；标签约束已迁入正式测试。同步删除3个仅覆盖旧闭包的测试，105个迁移、恢复脚本和作者数据零改动。新运行闭包门禁从真实入口冻结208个运行源码、18个运维源码、14个运维资源、105个迁移和8个静态资源，拒绝不可达源码、退役路径复活、动态加载、`dist`残留和敏感生产环境文件进入发布证据。
- 第73批把开书、设定、规划、人物、创作的恢复绑定统一冻结到当前成员、岗位、供应商、模型和套餐，历史损坏或退役任务只读；移除旧规划配方、旧人物成员、退役模型别名和零引用运行文件。运行闭包v2冻结208个运行源码、13个运维源码、14个运维资源、105个迁移、8个静态资源和24个构建输入，当前14个模块、68项能力均有真实入口证据。
- 第74批修复开书历史任务劫持新建入口、设定部分失败/重新设计卡死、会员门禁误报成员离岗、操作区位置不一致和时光机恢复，并删除12个经零引用证明退出V7运行闭包的旧Canon/知识生命周期源码与纯旧测试。新增0106只为设定任务补充失败阶段和安全重试证据；运行闭包v2冻结201个运行源码、13个运维源码、14个运维资源、106个迁移、8个静态资源和24个构建输入，14个当前模块、68项能力保持真实入口。
- 第74批上线复测又发现任务中心没有读取设定任务账本，导致统一整理真实失败却显示“0项”。现已增加按会话 `owner_id` 隔离的设定任务只读投影，排除内部题材档案和过期任务，不暴露错误码/阶段/模型协议；“继续处理”会打开对应书籍 `section=setting&focus=final-review`，失败统一整理自动展开，刷新仍保留恢复位置。
- 第75批检查最近滚动72小时14个活跃账号，账号、owner和会员关系全部正常；可恢复归档4项确认终止且无候选、调用、未知结果或租约的旧版假供应商失败，保留13项有效待作者确认资料。共同根因已修复：会员未开通、到期或算力不足会保留真实`MEMBERSHIP_*`门禁并立即停止，不再错误换成员或留下`provider_unavailable`残留。最终活动/未归档终止/working/unknown均为0；上线后真实作者开书约2分30秒进入待确认并产出2份候选。
- 第76批修复时光机用历史任务覆盖正式成果、候选读取过宽、开书额度错误被通用失败页隐藏，以及同一规划请求跨实例重复认领风险。指定六章书的全书/卷/链正式树、章纲、6章不可变正文、6份结算和24项正式化记录均未改写；指定账号正常但剩余额度低于AI开书安全预留，现显示真实阻断并保留手工开书恢复。当前单机不具备一万人同时在线证据，扩容与分级压测仍是商业推广门禁。
- 第77批修复规划树资料策划看不到正式设定 `sourceId` 却被要求精确返回该编号的合同断裂；新任务只向资料策划席提供任务所需来源编号，主设计席仍使用去内部字段快照。历史成功输出中的 `itemKey` 只在当前书冻结快照内唯一一对一归一为版本 `sourceId`，未知、重复和跨书值继续拒绝；同一失败任务可复用旧成功调用，不重复扣量。
- 第78批修复时光机恢复时选择最早旧框架任务、失败总审永久锁住少量已确认设定、4xx业务阻断被伪装成网络错误、损坏历史链抹掉已定稿章数和旧任务永久计入待处理。当前只接续最新同路线任务；旧只读名册会按当前路线、配方、资料快照和成员创建幂等替代任务；不删除历史任务、正式规划、正文或模型调用审计。
- 每个新创作任务固定调用一次资料策划 Agent；题材融合人设只在任务期生成，不绑定岗位。规划、创作和人物任务现在会先幂等确保当前书级题材融合档案；同书并发只生成一次，未知结果不盲目重发，明确失败只按冻结资料重试。全书、卷、链等可复用同一通用方法，但下层只接收活动父层的结构化方法应用、因果、伏笔、开放问题和当前接口，不接收方法教材或上层全文。
- 作者任务中心已补齐部分失败、耗时、成员详情、失败道歉、保留成果和原位恢复；批量清理只归档失败/中断开书任务，不会移除等待作者确认/决定的任务。独立后台已核验资料包、任务人设、方法、模型、用户和会员页面；会员开通/续费/撤销使用管理员范围幂等键和单事务不可变流水。
- 第 71 批隔离浏览器以测试作者提交 1308 字开书想法并实际入库；测试书“边城沙盘”已访问开书资料、设定、时光机、卷、链、章、资料库、任务、团队和会员/反馈。桌面与 390px 无横向溢出；设定失败保留结果，时光机失败可继续未完成步骤、返回全书方向或设定修改，反馈真实写入隔离库。
- 第 71 批同时把目录外题材的普通异常转换为可修正的 400 领域错误；任务页长想法压缩为 48 字摘要；青铜升级付费从办理日开始，付费档不能直接降为青铜，后台默认白银/98元并显示“升级并记录收入”。
- 第72批实现提交为 `3eb2d83`，`RELEASE_ID` 为 `wm-v7-20260831-213000-c42451f0`；干净全量门禁共626项通过、3项既有条件跳过，六工作区类型检查和构建通过。生产静态版本为 `4ca84707c13a83435548`（9文件，manifest `d99f2a871458537b6f25a0bbb31c56f88d4b83c5e6062c4f8817001d77ac0735`）。生产发布前后均为105个迁移且本批 `applied=[]`，连续30秒在途任务为零；回滚数据库 SHA-256 为 `e3c182659d907de4896aaea3ee00e54137a17b7ff98eb957bebe775a2ddbbee7`。
- 第73批实现提交为 `3e18702`，生产静态版本为 `c8974bfb34d73176fcd3`（9文件，manifest `0bb3cf17037f6bc08ddcf9f56904bb0cec746c793d38306abbe44a48ca382f60`）。根项目75个测试文件367项、Backend 86项Vitest加12项Node合同、作者端143项、后台43项均通过，后台另有3项既有条件跳过；全部类型检查、API/Worker/双前端构建和静态包校验通过。生产数据库备份 SHA-256 为 `4a0b7b1e304150632180d949e86d984e73024dbd8270d2edc8c08827b5a1aa66`，发布前后迁移均为105且 `applied=[]`，连续30秒在途任务为零。
- 第74批任务中心补丁提交为 `b6b8501`，作者端168项、设定集成32项、任务/设定/导航专项75项、API与作者端类型检查及生产构建通过。生产连续30秒零在途任务后切换；静态9文件 manifest 为 `e003e0ad3441561475a3b39a2379171161e8ec7a80e90a944770245f2beb0d78`，数据库备份 SHA-256 为 `353c5f54cfe27c913e03c7a31b3d547cd165a311b22272cf8a34bdf33f810e3e`。线上任务中心正确显示1项设定失败，桌面一步恢复、刷新恢复、390×844无横向溢出、44px底部按钮和浏览器/服务零错误均通过。
- 第75批实现提交为`8a89c06`；Linux原生全量门禁通过根项目381项、Backend 86+12项、作者端168项和后台43项，后台3项既有条件跳过，六工作区构建与同目录模块解析通过。静态9文件 manifest 为`cfe51abcafcb2a7615d96405747e95656a47d5f394f23ccbc38e20019eb85318`，回滚备份 SHA-256 为`dc3afe40e2aed17be4ee219c544830aaed56e484052b21b8db773db6da9353aa`；发布后服务错误日志0、数据库完整性和作者登录态空白开书页刷新均通过。
- 第76批实现提交为 `0fca168`；Linux隔离 `verify:full`、规划并发专项、时光机与作者入口专项、两次迁移 no-op、隔离确定性 API、完整备份、连续30秒零任务和公网资源门禁通过。登录态浏览器验证新建不白屏、不接管无关任务、实际可输入2000字，390×844无横向溢出且任务页显示准确恢复；未提交真实模型任务。切换窗口后的错误级服务日志为0，不代表未来绝无错误。
- 第77批实现提交为 `e20b071`；生产不可见目录 `verify:full`、本地规划/时光机专项、两次迁移 no-op、隔离确定性 API、完整备份和连续30秒零在途通过。切换后 API/Worker/Caddy active、数据库 quick_check=ok、外键0、错误级日志0；指定账号、钻石会员、两本书、正式路线和15次失败证据未被发布改写。真实模型重试必须由作者在已登录页面点击现有恢复动作，本批未冒充作者提交。
- 第78批产品修复提交为 `780c3d8`，发布门禁稳定性提交为 `038fccc`。最终本地与生产不可见目录完整门禁均通过746项、3项既有条件跳过，六工作区类型检查和构建通过；生产备份、两次迁移no-op、隔离API、连续30秒零在途、公网资源和切换后错误级日志通过。生产验收账号真实创建新路线任务，少量确认设定不再409；额度不足准确显示业务原因且任务中心只保留最新可处理项。390×844主操作位于卡片底部。
- 生产当前保留第78批源码、第77批立即回滚源码和第76批更早回滚源码。为满足发布空间门禁，只删除两个未引用旧发布中的可重建 `node_modules` 及 npm/apt 缓存，共释放952,832,000字节；旧源码、作者数据、迁移、数据库回滚副本、完整备份和审计证据均未删除。精确V6发布目录中的67迁移隔离数据库与回滚证据继续保留在 `/opt/wenmi/retained-release-evidence/`。
- Git 代码清理基线为 `899ed10`，当前已上线实现基线为 `038fccc`；不得重置、覆盖或强推。远程仓库推送不属于当前生产发布通道，不能用远程分支状态判断生产版本。
