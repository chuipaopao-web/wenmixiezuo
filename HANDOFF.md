# 文秘写作当前交接

> 新的开发任务先读本文件一次即可；同一任务后续不重复读取。历史开发日志在 Git，不在这里累积。

## 当前状态（2026-08-22）

- 项目：`D:\wenmixiezuo`，分支 `codex/desktop-entry`，生产站点 `https://wenmixiezuo.com`。
- 产品仍处于初始完善期，工作流已具备工程闭环，但 UI/交互尚不满意，后续以手机端逐页重设计为主，不受旧卡片布局和旧视觉细节约束。
- 当前主流程：开书 → 非剧情设定 → 卷方向 → 事件链 → 事件大纲 → 完整章链/近期细纲 → 正文 → 三级结算。
- 当前生产版本：`wm-longform-r2-20260822-020120-b2c0ec09`（代码提交 `61cb87b`），已上线非剧情宏观设定折叠流程、逐席重新设计与临时资料包更新、Coding Plan/Agent Plan Kimi K3 模型路线、独立平台后台和 Worker 失败恢复；主站保持 `https://wenmixiezuo.com` 不变。
- 独立后台地址为 `https://admin.wenmixiezuo.com`，作者创作台不再挂载管理入口；包含用户、算力、真实API成本、模型、问题、模板、提示词和会员经营数据。服务器已就绪，阿里云 DNS 仍需添加 `admin` A 记录指向 `47.243.152.159` 后才能公网访问并自动签发证书。
- 最新迁移 `0064_setting_member_resilience.sql`：记录设定编剧逐席运行状态、失败原因和最近尝试时间；历史迁移不改写。全量回归数字以最近一次 `npm run verify:full` 的最终结果为准。

## 下一阶段重点

1. 先做整体信息架构和手机端创作路径，不急着逐个美化旧卡片。
2. 优先页面顺序：应用壳/导航 → 开书信息 → 设定 → 分卷 → 事件与章纲 → 正文；每页先解决状态、主操作和失败恢复，再统一视觉。
3. 交互目标：少填少选、逐层展开、AI在正确时机介入、进行中有明确反馈、完成后自动收起、失败能直接恢复。
4. 写作工作台参考模式记录在 `.agents/skills/wenmi-ui-ux/references/writing-workspace-patterns.md`。

## 当前产品事实

- 当前专项：按 `docs/PROJECT_SLIMMING_IMPLEMENTATION_AND_ACCEPTANCE.md` 清理零引用代码、重复文档和旧测试，并建立日常快速门禁。

- 核心设定为四项：世界舞台、社会运行与秩序、规矩与代价、边界与留白；设定只收录脱离具体人物和剧情仍成立的宏观世界规则。人物、配角、反派与人物关系跟随分卷、事件、章纲、正文和结算逐层形成。
- 设定页采用“完整设定库 → 确认勾选并直接开始 → 逐项设计 → 主编审查 → 自动进入分卷”的折叠流程，不再有二次确认页。逐项确认的宏观内容进入非正史临时资料包，修改/清空后下一任务自动使用新指纹；每份编剧方案可单席重新设计并保留其他成功方案。已下架的早期人物/剧情类内容只在兼容区显示，可从当前设定单项移除，历史版本与正文保留。
- 分卷普通作者只看具体故事路线，不显示三幕式、拯救猫咪等内部术语；路线可整选、片选、融合、编辑或重做。
- 第一卷包含前500字兴趣职责、黄金三章、持续追读变化、防疲劳和约10万字内或卷末重大高潮。
- 普通作者接口使用自然语言洁净投影；管理员审计与普通页面隔离。
- 当前创作团队15名；除高级编剧外的活动AI岗位统一走火山方舟 Coding Plan，高级编剧清照单独走 Agent Plan Kimi K3；具体岗位与模型绑定以运行配置和 `apps/api/src/contracts/agent-team-v2.ts` 为准。
- 本次设定页与模型路线修改已完成本地定向验证，并在发布前完成全量门禁：182个测试文件、775项用例全部通过，共享契约/API/Web/Worker构建与各包类型检查通过；浏览器多尺寸验收因宿主Windows沙箱故障未验证。`wm-longform-r2-20260822-020120-b2c0ec09` 已于 2026-08-22 完成生产发布：迁移 0064 应用成功，API/Worker 均正常，公网首页、健康、鉴权和全部21个新旧哈希资源验收通过。

## 基础安全与部署

- Key 只在服务器环境变量，不能进入 Git、文档、日志或上下文。
- 核心数据按 `owner_id`、`book_id` 隔离；正文不可变；永久删除作者数据需双重确认。
- Web-only：暂存构建、保留旧哈希资源、原子切换入口、验证后回滚可用，不需要停 API/Worker。
- API/Worker/迁移：向后兼容，重启前在途任务连续30秒为零并立即复核，逐服务切换。
- Windows 发布归档仍使用 `git -c core.autocrlf=false -c core.eol=lf archive`，避免迁移校验和被 CRLF 改写。

## 开发入口与文档权力

- 每次新功能先读本交接一次，然后以 `docs/DEVELOPMENT_WORKFLOW.md` 和本次新建的开发/验收清单推进。
- 只有账号与书籍隔离、作者正式数据不可变、密钥/会话、迁移和生产回滚属于长期硬门禁；对应资料是 `docs/SECURITY_AND_OPERATIONS.md` 与受影响的 `docs/DATA_MODEL.md` 小节。
- `docs/PRODUCT.md` 及工作流、岗位、UI、提示词说明都可随老板新功能同步修改或删除，不得否决新需求，也不要求开工前全量阅读。
- UI/UX 与长篇质量 Skill 只在任务实际涉及相应问题时按需使用，不是固定开发审批链。
## 历史与恢复

2026-08-21规则精简前的完整原文保留在 Git 基线 `e8517d9`。默认不读；需要追溯旧编号或恢复某条细节时再查对应文件历史。