# 文秘写作当前交接

> 新的开发任务先读本文件一次即可；同一任务后续不重复读取。历史开发日志在 Git，不在这里累积。

## 当前状态（2026-08-23）

- 项目：`D:\wenmixiezuo`，分支 `codex/desktop-entry`。
- V6 核心创作工作台、滚动故事线/平台治理和功能资产守恒批次均已完成开发、验收和生产发布；功能台账批次的唯一执行记录为 `docs/FEATURE_CAPABILITY_GOVERNANCE_DEVELOPMENT_CHECKLIST.md`，未勾选项为 0。
- 生产站点 https://wenmixiezuo.com 与独立后台 https://admin.wenmixiezuo.com 已上线发布 `wm-feature-registry-r1-20260823-142555-fa301aca`；上一回滚版本为 `wm-rolling-storyline-r2-20260823-114755-f324dfeb`。本批无数据库迁移，生产数据库仍为 `0068_rolling_storyline_growth.sql`，API、Worker、Caddy 与新版 Web 入口均已验证健康。
- 最新 Web-only 修复版本为 `wm-archive-visibility-r1-20260823-151339-388e94a`：修复浅色书架“已归档书籍/归档当前书籍”白字白底，以及手机书籍抽屉被遮罩覆盖的问题；真实浏览器验证桌面/手机均可见、可点，对比度 5.35:1。该修复只原子切换 Web 入口，API/Worker 未重启。
- 主流程继续保持原功能板块与顺序：设定 → 故事线 → 分卷 → 事件 → 章节；滚动故事线支持零线开写、阶段终点、卷末提炼、主编下一至两卷推荐、作者决定与继续观察。
- UI 只统一视觉效果，不改原功能板块、顺序、壳层和交互骨架；桌面恢复 144px 浅色书架与原导航质感，页面流程栏全部删除，手机保持顶部两排导航。
- 本地和生产最新迁移为 0068_rolling_storyline_growth.sql；它只向前新增滚动故事线、作者边界、候选/决策和创作模板版本对象，不改写历史正文、结算或旧迁移。

## 本批次已完成并上线

- 独立后台在原九个模块之外新增第十个模块“功能台账”，完整展示 33 个功能模块、130 项能力和历史状态；原有后台导航、作者端功能布局和手机顶部两排导航未被重排或删除。
- 机器台账登记 122 项当前可用能力、8 项历史决策和 2 项疑似遗失；稳定 ID、所属端/模块、入口、作用、代码/API 证据与当前状态均可核查。
- 对照基线固定为上一生产源码 `d98dc81` 和早期稳定版本 `61cb87b`；删除、迁移、替代、明确下线和疑似遗失使用显式决策账本，不再凭页面印象判断功能是否丢失。
- 管理员接口为 `GET /api/v1/admin/feature-capabilities`，支持基线、状态、模块和关键词筛选；只向管理员开放，不返回密钥、内部思维链、模型私密配置或作者正文。
- `npm run verify:capabilities` 已接入日常 `verify` 和 `verify:full`，会校验台账唯一性、证据存在、历史去向和锁定的基线快照，防止后续升级静默删除功能。
- 当前确认的疑似遗失只有两项：主编设计书名、主编设计书籍简介。后端路由、服务、数据和测试仍存在，但作者端入口与调用缺失，且没有明确下线决定；后台持续显示，等待恢复、迁移或明确下线的产品决定。
- 唯一执行清单为 docs/NEXT_PLATFORM_MODULE_AND_VISUAL_DEVELOPMENT_CHECKLIST.md；第 0–15 项已全部实现、核查、测试并勾选，生产证据在对应 EVIDENCE 文档和 artifacts/next-platform-20260823。
- 本批次只重构功能模块和交互闭环，不推翻原来成熟的视觉语言。V6 重构前生产界面的字体、排版密度、留白、色彩、按钮质感和阅读舒适度是质量下限；新页面可以更高级、更饱满，但不得更差。
- UI 执行双基准：未改稳定区域相对旧版视觉基线达到 95%，改造区域相对登记的新设计图达到 95%，并通过旧版/设计图/新版三联图和人工视觉评分；功能通过但视觉退步仍判失败。
- 全站删除页内流程栏、圆点连线和阶段柱，但手机保留现有顶部两排功能导航，不增加第三排。
- 设定页只调整当前设定工作台，不恢复旧信息页；主角性格在设定页轻量显示并进入版本化角色卡。
- AI 团队准确口径为 7 类岗位、初始 25 名成员：主编 3、副编 3、编剧 5、主笔 5、事实审查席 3、文学审查席 3、体验审查席 3；25 名不写死，后台可以继续增加。
- 每名成员后台分别绑定供应商和模型。作者只选择成员：每个节点默认一名，可更换或追加同岗位多人；闲置与未选成员不调用、不消耗。
- 作者端成员只显示头像、姓名、岗位、供应公司、消耗等级和状态；不显示模型名、擅长、速度或成功率。
- 同岗位同批次成员收到完全相同的 ContextPack、作者输入、岗位/节点 Skill 和模板；副编是可见的局部整理/融合成员，ContextCompiler 是不可见的资料包编译系统。

## V6 产品事实

- 设定页包含开书资料、完整设定库、逐项多成员候选、作者编辑稿、副编整理和主编确认；正式确认前只形成临时计划资料包，不写正史。
- 故事线支持一条核心线+支线、双核心线、多核心线、单元故事；线路生命周期与卷内参与状态分离，故事地图只投影权威计划/实际总账。
- 分卷支持“从零想、有方向、完整卷纲”三种作者入口，两条实质不同路线、严格勾选拼合、表达方案和主编可编辑整理。
- 事件页按因果事件链、角色功能占位、角色绑定和事件节奏推进；白话节奏映射到后台明确作用域模板，单事件卷保留完整对象与结算。
- 章节页统一章链、近期详细章纲、正文、多主笔候选、事实/文学/体验三席、作者主动主编综合和章→事件→卷结算；章纲与不可变正文在后台仍是独立版本对象。
- 规划只记录未来；只有作者最终定稿正文的结算可以写入实际账本。AI 候选、示例、未确认章纲和三席报告不能成为事实。
- 七岗位为主编、副编、编剧、主笔、事实席、文学席、体验席；岗位池和模型绑定后台可配置并版本化。前端不显示模型名、擅长、速度或成功率，只显示真实头像、姓名、岗位、供应公司、消耗等级和状态。
- 同岗位同批次冻结同一 `context_pack_id/hash`、作者输入版本、岗位/节点 Skill 和模板；每名成员另行冻结自己的供应商/模型绑定。同一模型不得伪装成异模型独立复核。ContextCompiler 仍是唯一资料包权威，成员不保存私人全书记忆。

## 已删除与兼容

- 新闭环通过后已删除 `PlanningWorkspace.tsx` 和 `TeamWorkspace.tsx`；对应规划与团队能力已有明确替代，源码和 Web 构建产物均为零有效引用。
- 旧 `BrandingDesignDialog.tsx` 组件已经删除，但历史对照确认其“主编设计书名/简介”作者入口没有被当前页面完整替代；现有后端能力继续保留，两项已登记为疑似遗失，不再误报为“已经嵌入”。
- 旧“信息、规划、章纲、正文”不再作为独立导航入口；除上述两项疑似遗失外，需要保留的历史能力已经迁入新五页，没有第二套影子流程。
- 旧 `view` 深链接只保留显式映射并立即规范化为 `stage`；例如 `view=master` 转为 `stage=volume`。历史任务、版本、正文、来源引用和结算继续读取原权威表。

## 最终验收证据

- 功能资产守恒批次：目标测试 3 个文件、17/17 用例通过；功能守恒脚本、API/Web/测试类型检查和 Contracts/API/Web 生产构建通过。1440/360/390/430px 精确视口均无横向溢出，33 个模块、2 项疑似遗失和后台第十个入口均可见。
- 该批次没有数据库迁移、权限/账号隔离、作者数据不可变或核心创作流程结构变化，按风险门禁未额外触发 `verify:full`；守恒脚本已接入 `verify:full`，后续满足全量条件时自动执行。
- 最终 npm run verify:full：成功；189/189 个测试文件、814/814 个测试通过，Contracts/API/Worker/Web 类型检查和生产构建通过。
- 迁移：0065/0066/0067/0068 的空库、旧库升级、重复执行和生产副本预演均通过；账号/多书隔离、历史 owner 迁移与正式库不可变表哈希门禁通过。
- 最终发布目录隔离冒烟：9 个核心文件、44/44 用例通过；覆盖滚动故事线、结算投影、25 人团队、ContextPack、失败席重试、独立后台经营与治理。
- 真实 AI：复用本机已有 Codex 登录态，以只读、临时、禁工具、禁止现金回退模式对七岗位各执行一次真实探针；7/7 成功，`cashCostCny=0`，只保留岗位、Token、耗时、长度和输出哈希，不保存答复正文。
- 浏览器：本地完整页面与生产域名真实静态资源均完成 1366/1440/1536/1672/1920/360/390/430 八个视口复核；生产域名认证页面 10 页共 80 张，登录壳另 16 张，流程栏/溢出/资源失败/浏览器异常均为 0。
- 生产域名 50 个桌面稳定区域相对旧版最低相似度 96.66%、平均 97.25%，通过 95% 门槛；人工逐区检查通过，手机两排导航保持。

## 基础安全与生产发布

- Key 只来自环境变量，不能进入数据库、Git、日志、上下文、导出或备份；核心业务读写始终携带已验证会话的 `owner_id + book_id`。
- 正文是不可变完整版本；普通清理不得覆盖正式作者原文、结算或已合并迁移。永久删除作者数据仍需影响预览、`YES` 和二次确认。
- 最新生产发布已按 docs/DEPLOY.md 执行：在途任务连续 30 秒为零并立即复核；API、Worker 与 Web 逐项切换，保留数据库备份、旧运行时、旧入口和旧 Web 哈希资源回滚资产。
- 最新生产验收：活动任务为 0，数据库完整性为 `ok`，API/Worker/Caddy 均为 active 且 API/Worker 重启计数为 0；主站、后台、两端 `/health` 和新版静态资源为 200，真实未登录接口为 401，线上入口与发布包一致，发布后 API/Worker/Caddy 错误日志均为 0。
- 不取消、暂停或改写作者任务制造发布窗口；生产异常优先回滚，不在生产数据上边猜边改。

## 开发入口与文档权力

- 新功能先读本文件一次，再按 `docs/DEVELOPMENT_WORKFLOW.md` 和当次新建的开发/验收清单推进。
- V6 当前合同以 `docs/PRODUCT.md`、`docs/DATA_MODEL.md`、`docs/API.md` 与本次清单为准；旧规格只用于定位兼容范围，不能否决老板的新决定。
- 账号与书籍隔离、作者正式数据不可变、密钥/会话、迁移和生产回滚是长期硬门禁。

## 历史与恢复

2026-08-21 规则精简前的完整原文保留在 Git 基线 `e8517d9`。默认不读；需要追溯旧编号或恢复某条细节时再查对应文件历史。
