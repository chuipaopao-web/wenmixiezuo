# 文秘写作当前交接

> 新任务先读本文件一次。同一任务后续不重复读取；历史开发记录在 Git 和历史清单中按需追溯。

## 当前唯一目标（2026-08-23）

项目准备进入 V7：在当前仓库中建立独立分支和模块，重新开发作者工作流与前端 UI，按资产清单复用可靠后台能力，完成切换后删除被替代的旧代码。

V7 尚未开始功能编码、尚未创建分支、尚未部署。当前只完成了产品交接、资产分类和开发验收清单整理。

## V7 唯一必读文件

按顺序读取：

1. `docs/V7_PRODUCT_SPEC.md`：最新产品目标、轻量共创流程、AI介入、UI和工程边界。
2. `docs/V7_ASSET_INVENTORY.md`：后台与前端的保留、适配、重做、禁止删除和待实证清单。
3. `docs/V7_ACCEPTANCE.md`：逐项开发、验收、旧实现清理、测试和部署清单。
4. `docs/DEVELOPMENT_WORKFLOW.md`：通用开发闭环。

除非定位成熟实现或兼容问题，新任务不要读取 V6/NEXT 历史规格、旧视觉图或长篇聊天记录。

## 权威与失效声明

- 老板最新明确决定最高。
- V7 讨论形成之前的工作流、页面结构、UI图、步骤名称、卡片数量和前置门槛全部失效，不能作为 V7 开发依据。
- `docs/PRODUCT.md`、`docs/NEXT_CORE_WORKFLOW_DEVELOPMENT_CHECKLIST.md`、`docs/NEXT_PLATFORM_MODULE_AND_VISUAL_DEVELOPMENT_CHECKLIST.md` 记录 V6 当前生产实现与历史验收；它们只用于识别复用资产、读取历史数据和确认删除范围，不得否决 V7。
- 旧 V6 设计图不再是 V7 的 95% 视觉基准。V7 必须先出新的信息架构和逐页桌面/手机原型，由老板确认后再编码。

## 已锁定的 V7 方向

- 核心体验：AI 给少量方向，作者选择/补充/编辑，AI 扩展，作者确认，正文结算，系统更新记忆，再进入下一步。
- 作者只选题材也能开书；一句话方向和已有内容均为可选。
- 故事线改为正文实际驱动的故事雷达，不作为开书和第一卷前置门槛。
- 后台保留轻量事件骨架和完整章链；前端默认收起，只详细处理当前事件与当前章，未来最多详细到后2章。
- 每个 AI 介入节点都有可留空的作者补充输入。
- 7 类岗位、初始 25 名成员继续作为后台资源池；作者选成员，不选模型。
- 桌面保留左侧书籍栏和顶部页面切换；手机保留顶部两排导航。页面内部和工作流重新设计。
- V7 在当前仓库建立 `coauthoring-v7` 模块，不复制第二套项目、数据库、登录或部署系统。

## 必须保留的底座

- 登录、会话、`owner_id + book_id` 隔离；
- 作者正式数据和正文不可变完整版本；
- ContextPack、来源、哈希和任务资料隔离；
- AI成员/模型快照、任务、预算、调用、失败重试和审计；
- 章节、事件、卷结算与计划/事实分离；
- 已合并迁移、历史版本、来源引用和生产回滚能力；
- API Key 只来自环境变量，不进入数据库、Git、日志、上下文或导出。

## 当前仓库状态

- 项目目录：`D:\wenmixiezuo`。
- 当前分支：`codex/desktop-entry`；建立文档前相对远端领先 50 个提交。
- 当前 HEAD（建立文档前）：`19c69e7`。
- 当前未跟踪目录：`.claude/`、`.codex-backups/`、`.visual-qa-data-20260823/`、`artifacts/`，视为老板现有内容，不得删除、提交或混入 V7。
- 当前生产仍运行 V6。生产迁移最新为 `0068_rolling_storyline_growth.sql`。
- V6 已有 AI资源管理、ContextPack、任务恢复、版本和结算能力；V7 先通过网关复用，不整体搬迁或复制。

## 新任务第一步

1. 按 `docs/V7_ACCEPTANCE.md` 第0项发出开工工单。
2. 检查工作区和当前 Git 状态，保护老板已有内容。
3. 从当前 HEAD 创建 `codex/light-coauthoring-v7`。
4. 只做资产复核、信息架构、状态流和新 UI 原型；老板确认前不实现页面。
5. 原型确认后，先打通“题材→开书方向→卷方向→当前事件→章纲→正文→结算→下一章资料包”的第一条纵向闭环。

## 测试与部署

- 每个小批只跑受影响测试、对应包类型检查/构建和相关浏览器场景。
- V7 属于核心创作工作流结构变化，最终必须运行 `npm run verify:full`；不在每个小项后重复全量。
- 文档整理任务不部署。V7 未通过清单、真实浏览器验证、最终门禁和回滚准备前不得切换生产入口。
