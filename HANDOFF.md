# 文秘写作当前交接

> V7 已替换旧产品并成为唯一当前产品。不要恢复、引用或重新实现旧版页面、工作流和书籍投影。

## 当前权威入口

- 作者端：`https://wenmixiezuo.com/`
- 独立后台：`https://admin.wenmixiezuo.com/`（后台应用位于该域名的 `/v7/` 路由）
- 本地作者端：`http://127.0.0.1:43110/`
- 本地独立后台：`http://127.0.0.1:43110/v7/`
- 当前代码区：`coauthoring-v7/`
- 当前分支：`codex/light-coauthoring-v7`

生产 API、Worker、Caddy 和 V7 静态发布已于 2026-09-01 切换到第77批 `wm-v7-20260901-203016-69bf9d6d`，静态版本 `e33dd243118bc63140c5`（9文件，manifest `0b062fcddc2a04df6f728fa4ab5d757e5d69b404e352c1eaa4d017095a504559`）；106 个迁移、API/Worker 发布号、作者端、独立后台、静态哈希资源、未登录权限门禁和公网健康均已独立验证。第76批应用 `wm-v7-20260901-153007-bb16a6ec` 与静态版本 `6fba50a787f23722fcaf` 现为立即回滚点，更早服务 `wm-v7-20260901-093501-f8c6c4e4` 继续保留；本批完整备份为 `20260901T123453Z-r77`。阿里云 `admin + sudo` 公钥自动发布通道已验证，后续不再需要老板打开 Workbench 手工输入部署命令；生产数据库和服务器运行目录始终是线上事实来源，本地数据不能回写或替代生产数据。

## 新任务最小阅读

1. 当前已完成的最新工单：`coauthoring-v7/docs/worklists/V7-TIME-MACHINE-ACCOUNT-INCIDENT-20260901-77.md`
2. 开发规则：`AGENTS.md`
3. V7 代码交接：`coauthoring-v7/HANDOFF.md`
4. 只有涉及数据、接口或部署时，按需读取 `docs/DATA_MODEL.md`、`docs/API.md`、`docs/DEPLOY.md`

历史工作清单和 Git 历史只用于审计，不是当前产品规格。已合并迁移及其历史表名必须保留；它们不代表旧产品仍在运行。

## 当前产品边界

- 只保留 V7 作者端、V7 独立后台和 V7 创作流程。
- 账号、密码登录、会话、会员、反馈、安全、用量、备份和恢复是共享平台底座，由 V7 继续使用。
- 旧版书籍、资料库、任务、正文和工作流不迁移到 V7，也不在作者端展示。
- 正文不可变版本、`owner_id + book_id` 隔离、API Key 环境变量边界和任务审计继续有效。
- 本地数据永久删除仍必须先生成影响预览，并经过 `YES` 与 `CONFIRM:<previewId>` 两次明确确认。

## 当前实现与发布状态

- 旧作者端、旧专用 API、旧 Worker 循环、本地向量运行时及其依赖、测试和文档已退出当前树；已合并迁移和历史表名未改写。
- 本地作者数据已经按双确认收据永久删除；已确认无用的缓存、截图、临时备份和旧发布副本已删除。`artifacts/` 仅作当前部署、回滚与验证证据，不是生产数据来源；临时构建不得冒充线上版本。
- 当前依赖锁只含 6 个 V7/共享 workspace；当前 106 个已合并迁移全部保留，既有 102 个历史迁移字节未改写，第 71 批新增 0104/0105 与第74批 0106 只做前向兼容。所有作者生产数据均未删除、覆盖或回填。
- 旧开书团队初始化、编辑租约、旧岗位提示编译和旧成员专业人设运行闭包已删除；V7 成员只保留身份、模型和当前任务责任，题材融合人设仍按书籍任务装配。
- 当前 V7 未使用的 Codex、opencodego 连接器、配置、启动变量和测试已删除；V7 当前六个模型档案与方舟 Coding/Agent Plan 路由已回归通过。
- 第72批在老板再次知情确认后物理删除21个零运行引用旧源码，并删除3个重复43180本地启动器和1个一次性标签脚本；标签约束已迁入正式测试。同步删除3个仅覆盖旧闭包的测试，105个迁移、恢复脚本和作者数据零改动。新运行闭包门禁从真实入口冻结208个运行源码、18个运维源码、14个运维资源、105个迁移和8个静态资源，拒绝不可达源码、退役路径复活、动态加载、`dist`残留和敏感生产环境文件进入发布证据。
- 第73批把开书、设定、规划、人物、创作的恢复绑定统一冻结到当前成员、岗位、供应商、模型和套餐，历史损坏或退役任务只读；移除旧规划配方、旧人物成员、退役模型别名和零引用运行文件。运行闭包v2冻结208个运行源码、13个运维源码、14个运维资源、105个迁移、8个静态资源和24个构建输入，当前14个模块、68项能力均有真实入口证据。
- 第74批修复开书历史任务劫持新建入口、设定部分失败/重新设计卡死、会员门禁误报成员离岗、操作区位置不一致和时光机恢复，并删除12个经零引用证明退出V7运行闭包的旧Canon/知识生命周期源码与纯旧测试。新增0106只为设定任务补充失败阶段和安全重试证据；运行闭包v2冻结201个运行源码、13个运维源码、14个运维资源、106个迁移、8个静态资源和24个构建输入，14个当前模块、68项能力保持真实入口。
- 第74批上线复测又发现任务中心没有读取设定任务账本，导致统一整理真实失败却显示“0项”。现已增加按会话 `owner_id` 隔离的设定任务只读投影，排除内部题材档案和过期任务，不暴露错误码/阶段/模型协议；“继续处理”会打开对应书籍 `section=setting&focus=final-review`，失败统一整理自动展开，刷新仍保留恢复位置。
- 第75批检查最近滚动72小时14个活跃账号，账号、owner和会员关系全部正常；可恢复归档4项确认终止且无候选、调用、未知结果或租约的旧版假供应商失败，保留13项有效待作者确认资料。共同根因已修复：会员未开通、到期或算力不足会保留真实`MEMBERSHIP_*`门禁并立即停止，不再错误换成员或留下`provider_unavailable`残留。最终活动/未归档终止/working/unknown均为0；上线后真实作者开书约2分30秒进入待确认并产出2份候选。
- 第76批修复时光机用历史任务覆盖正式成果、候选读取过宽、开书额度错误被通用失败页隐藏，以及同一规划请求跨实例重复认领风险。指定六章书的全书/卷/链正式树、章纲、6章不可变正文、6份结算和24项正式化记录均未改写；指定账号正常但剩余额度低于AI开书安全预留，现显示真实阻断并保留手工开书恢复。当前单机不具备一万人同时在线证据，扩容与分级压测仍是商业推广门禁。
- 第77批修复规划树资料策划看不到正式设定 `sourceId` 却被要求精确返回该编号的合同断裂；新任务只向资料策划席提供任务所需来源编号，主设计席仍使用去内部字段快照。历史成功输出中的 `itemKey` 只在当前书冻结快照内唯一一对一归一为版本 `sourceId`，未知、重复和跨书值继续拒绝；同一失败任务可复用旧成功调用，不重复扣量。
- 每个新创作任务固定调用一次资料策划 Agent；题材融合人设只在任务期生成，不绑定岗位。规划、创作和人物任务现在会先幂等确保当前书级题材融合档案；同书并发只生成一次，未知结果不盲目重发，明确失败只按冻结资料重试。全书、卷、链等可复用同一通用方法，但下层只接收活动父层的结构化方法应用、因果、伏笔、开放问题和当前接口，不接收方法教材或上层全文。
- 作者任务中心已补齐部分失败、耗时、成员详情、失败道歉、保留成果和原位恢复；批量清理只归档失败/中断开书任务，不会移除等待作者确认/决定的任务。独立后台已核验资料包、任务人设、方法、模型、用户和会员页面；会员开通/续费/撤销使用管理员范围幂等键和单事务不可变流水。
- 第 71 批隔离浏览器以测试作者提交 1308 字开书想法并实际入库；测试书“边城沙盘”已访问开书资料、设定、时光机、卷、链、章、资料库、任务、团队和会员/反馈。桌面与 390px 无横向溢出；设定失败保留结果，时光机失败可继续未完成步骤、返回全书方向或设定修改，反馈真实写入隔离库。
- 第 71 批同时把目录外题材的普通异常转换为可修正的 400 领域错误；任务页长想法压缩为 48 字摘要；青铜升级付费从办理日开始，付费档不能直接降为青铜，后台默认白银/98元并显示“升级并记录收入”。
- 第72批实现提交为 `3eb2d83`，`RELEASE_ID` 为 `wm-v7-20260831-213000-c42451f0`；干净全量门禁共626项通过、3项既有条件跳过，六工作区类型检查和构建通过。生产静态版本为 `4ca84707c13a83435548`（9文件，manifest `d99f2a871458537b6f25a0bbb31c56f88d4b83c5e6062c4f8817001d77ac0735`）。生产发布前后均为105个迁移且本批 `applied=[]`，连续30秒在途任务为零；回滚数据库 SHA-256 为 `e3c182659d907de4896aaea3ee00e54137a17b7ff98eb957bebe775a2ddbbee7`。
- 第73批实现提交为 `3e18702`，生产静态版本为 `c8974bfb34d73176fcd3`（9文件，manifest `0bb3cf17037f6bc08ddcf9f56904bb0cec746c793d38306abbe44a48ca382f60`）。根项目75个测试文件367项、Backend 86项Vitest加12项Node合同、作者端143项、后台43项均通过，后台另有3项既有条件跳过；全部类型检查、API/Worker/双前端构建和静态包校验通过。生产数据库备份 SHA-256 为 `4a0b7b1e304150632180d949e86d984e73024dbd8270d2edc8c08827b5a1aa66`，发布前后迁移均为105且 `applied=[]`，连续30秒在途任务为零。
- 第74批任务中心补丁提交为 `b6b8501`，作者端168项、设定集成32项、任务/设定/导航专项75项、API与作者端类型检查及生产构建通过。生产连续30秒零在途任务后切换；静态9文件 manifest 为 `e003e0ad3441561475a3b39a2379171161e8ec7a80e90a944770245f2beb0d78`，数据库备份 SHA-256 为 `353c5f54cfe27c913e03c7a31b3d547cd165a311b22272cf8a34bdf33f810e3e`。线上任务中心正确显示1项设定失败，桌面一步恢复、刷新恢复、390×844无横向溢出、44px底部按钮和浏览器/服务零错误均通过。
- 第75批实现提交为`8a89c06`；Linux原生全量门禁通过根项目381项、Backend 86+12项、作者端168项和后台43项，后台3项既有条件跳过，六工作区构建与同目录模块解析通过。静态9文件 manifest 为`cfe51abcafcb2a7615d96405747e95656a47d5f394f23ccbc38e20019eb85318`，回滚备份 SHA-256 为`dc3afe40e2aed17be4ee219c544830aaed56e484052b21b8db773db6da9353aa`；发布后服务错误日志0、数据库完整性和作者登录态空白开书页刷新均通过。
- 第76批实现提交为 `0fca168`；Linux隔离 `verify:full`、规划并发专项、时光机与作者入口专项、两次迁移 no-op、隔离确定性 API、完整备份、连续30秒零任务和公网资源门禁通过。登录态浏览器验证新建不白屏、不接管无关任务、实际可输入2000字，390×844无横向溢出且任务页显示准确恢复；未提交真实模型任务。切换窗口后的错误级服务日志为0，不代表未来绝无错误。
- 第77批实现提交为 `e20b071`；生产不可见目录 `verify:full`、本地规划/时光机专项、两次迁移 no-op、隔离确定性 API、完整备份和连续30秒零在途通过。切换后 API/Worker/Caddy active、数据库 quick_check=ok、外键0、错误级日志0；指定账号、钻石会员、两本书、正式路线和15次失败证据未被发布改写。真实模型重试必须由作者在已登录页面点击现有恢复动作，本批未冒充作者提交。
- 生产当前保留第77批源码、第76批立即回滚源码和更早第75批回滚源码。为满足发布空间门禁，已删除未被 apps/current/previous/RELEASE_ID 或回滚链引用的旧构建目录 `wm-v7-20260901-015500-d14dc64c`；Git 历史可重建其源码，作者数据、迁移、数据库备份和审计证据未删除。精确V6发布目录中的67迁移隔离数据库与回滚证据继续保留在 `/opt/wenmi/retained-release-evidence/`。
- Git 代码清理基线为 `899ed10`，当前已上线实现基线为 `e20b071`；不得重置、覆盖或强推。远程仓库推送不属于当前生产发布通道，不能用远程分支状态判断生产版本。
