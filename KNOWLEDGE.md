# 项目长期知识

## 已确认事实

- 项目目录：`D:\wenmixiezuo`。
- 本项目与 `D:\AI智囊团` 完全独立；AI智囊团只是讨论来源，不是小说平台运行时依赖。
- 当前只面向老板个人本机使用，但核心领域预留 `owner_id`、版本和未来租户扩展边界。
- 产品的主要操作方式是自然语言对话；按钮和菜单作为快捷方式和故障兜底。
- 每本书按 `book_id` 隔离消息、任务、正文、知识、记忆、索引、预算、主编租约和界面状态。
- 当前release按DEC-021运行11名成员：主编/副编、两名异模型编剧、设定、主笔/副笔、审校、体验、研究和版权；历史9人书籍在没有未终结任务时幂等升级，旧实例仅停用留痕。
- 11名成员不等于11个不同模型；岗位与模型通过不可变修订解耦。默认剧情席为DeepSeek＋GLM，Kimi可替换一席，豆包不讨论剧情；主笔为Codex、副笔为GLM。正文固定事实/文学/体验三职责、三异模型点评，默认GLM/Kimi/豆包，GLM写手时事实席用DeepSeek。
- DEC-027在11名创作成员之外增加小文秘书本地工具角色；它负责确定性小操作、低风险资料、实体/别名候选、检索准备和透明分流，不计创作/点评模型，不直接写正史或回答剧情。
- 消息路由优先级是确定性命令→显式点名直达→活动会话续接→低风险本地处理→专项岗位→剧情会话→主编升级；老板原话不可变，秘书摘要只能附加。任务大小按风险/权限/可逆性/创作影响，不按消息长度。
- 小文秘书只保存工作状态、可重建书籍缓存、明确操作/UI偏好和版本化工具经验；创作选择、采纳率和原始聊天不自动沉淀为迎合规则。当前本地语义资产固定为 `Xenova/bge-small-zh-v1.5` 指定revision、512维量化ONNX并按清单哈希校验；运行时禁止远程下载和现金fallback，模型只产受Schema约束的候选。
- 公开检索入口已运行结构化、FTS、LanceDB向量和SQLite关系四通道；向量使用归一化BGE查询指令和 `bge-normalized-l2-v1` 保守无答案门槛，同源跨车道候选聚为一簇，任何候选仍需回查SQLite块和来源。
- Codex GPT-5.6 Sol、DeepSeek V4 Pro、GLM 5.2、Kimi K2.6和豆包Seed 2.0 Pro已分别完成最小真实连通；只允许Codex登录态与方舟Coding/Agent Plan，现金fallback为false。Kimi通道显式关闭隐藏思考以保证最终文本不被输出Token吃空。
- 成员栏必须直接显示一句话职责，点击后显示公开岗位合同；公开职责与内部系统提示分离。道韫研究员保留为按需现实事实专家，纯虚构设定、普通聊天和固定三评不激活，未触发时零调用。
- 首版业务代码默认由Codex单独开发；小说中的Agent成员是产品功能，不参与本项目代码协作。
- 长期记忆依赖本地结构化事实、故事圣经、正文索引和不可变上下文包，不能只依赖模型聊天长度。
- 正文、正史和重大决定不能被模型静默覆盖。
- 老板已于2026-07-16正式授权首版连续开发，唯一实现目录为 `D:\wenmixiezuo`。
- 老板已于2026-07-19正式授权长篇终局release连续开发，活动ID为 `wm-longform-r1-20260719-003435-e4d7b8b7`；按冻结八阶段施工，普通阶段不重复请示，完成前不得伪造E3/E4或Agent状态。
- 产品显示名称为“文秘写作”；Web/API默认端口为 `43110/43111`；数据根目录为 `D:\wenmixiezuo\data`。
- 未获单次付费授权前现金费用上限为0；真实凭证缺失时使用确定性假模型。
- 第二物理数据备份暂未配置；远程Git已配置私有仓库 `chuipaopao-web/wenmixiezuo` 并使用SSH推送 `main`。远程Git只备份代码和文档，不等于小说数据第二副本。
- Schema 7已实现五类叙事投影、版权隔离/干净室、研究来源与候选证据；联网不可用时必须明确说明，不得声称近期热点。
- Web工作台采用桌面三栏、移动抽屉和沉浸阅读；草稿与已加载快照进入IndexedDB，缓存键携带 `book_id` 并按 `canon_revision` 失效。
- Agent界面状态只由任务状态、分配Agent和Worker当前任务共同决定；相同模型来源会如实重复显示。
- Worker主循环定时器必须保持Node事件循环；心跳定时器可不单独保持进程，生产驻留由主循环负责。
- 首版release `wm-v1-20260716-220959-d5dd704d` 已完成阶段0至8；实现提交为 `fdfcb8a`，最终验收矩阵位于release目录。
- 普通消息创建活动主编真实回复任务；自然创作意图会自动启动主编加相关岗位讨论，`讨论 <问题>` 只是显式快捷方式。
- 创作规划必须经过真实岗位意见、主编真实汇总和完整 `确认方案 <ID>`；确认后生成可追溯规划成果，未齐备时任何入口都不得启动主笔。
- 最近聊天仅作为有界任务上下文，不自动进入长期记忆或正史；按需专家在任务结束后回到待命。
- 模型调用必须同时匹配配置快照、实际适配器和返回来源；工具调用使用受限HTTP或无shell子进程适配器，取消会真实下传。
- 长对话API单页最多500条，工作台DOM最多挂载最近200条；更早记录仍通过稳定游标保存在本地数据库中。
- 24小时Worker稳定性证据为1440个一分钟确定性逻辑周期，另有真实进程驻留和心跳测试；不得将其描述为24小时墙钟等待。
- 最终产品规模目标为单书300至500万字、1000章以上；工程容量按500万规范化中文字符、1500章验收，首版5章和旧100万字符测试不能外推为该能力。
- 十一名成员的长期陪伴通过稳定身份、岗位连续性日志、当前关注、工作证据和岗位专用连续性包实现；不让模型全天运行，也不默认加载全部聊天。
- 超长篇采用场景、章节、故事弧、卷、全书主脊五级增量连续性，滚动规划保留30至50章轮廓、5至10章细化和1至3章场景契约；详细规格见 `docs/ULTRA_LONGFORM_CONTINUITY.md`。
- 知识生命周期采用同一SQLite权威下的临时、候选、正史、派生四层；聊天与完整稿件默认归档，拒绝候选保留但排除，任务临时向量索引7天后可清理，作者可见原始内容不自动永久删除。
- 资料切片采用不可变原文、结构节点、场景节拍父块/检索子块和原子事实；默认不复制重叠，通过邻接有界扩展，重切片使用新快照原子切换。完整合同见 `docs/CHUNKING_DESIGN.md`。
- 已结束阶段默认使用阶段结算，不把早期全部正文放入当前上下文；旧实体、开放承诺/伏笔、规则、知情/关系/声音证据、历史因果、摘要冲突/缺项或老板明确回顾会触发卷→故事弧→章节/场景→正史原文的有界下钻。硬事实和开放线程不按章节距离失效，摘要不是事实权威。
- 重要方案必须在形成推荐前执行适用Skill并保存 `design_review_id`、Skill版本/哈希、事实/推断/偏好/未知、替代、两轮审查、证据和回滚；Skill不默认进入产品岗位上下文。完整规则见 `docs/DESIGN_GOVERNANCE_AUDIT.md`。
- 安全/费用/确认硬锚与作者偏好分层；自动事实抽取不得高于确认原文；事实按世界有效、人物知情、系统修订三轴查询；候选会休眠而不删除；长会话使用带模型/提示/来源快照与轮换探针的锚定压缩。
- 三级下钻唯一计数为活动集0、卷1、故事弧2、章节/场景及结构化事实3，最小原文证据解引用单独记预算；上下文先预留输出，实体同名未消歧时不得静默选择。
- 四路混合检索不直接加分：先做正史/三轴时间/实体硬门禁，再按查询意图运行结构化事实、FTS、向量和关系/Wiki；结果分H硬约束、E证据、I灵感，H不参加RRF，E/I分车道融合。同源父子块/摘要/Wiki只算一个证据簇，关系只从已消歧种子有界扩展，确定性结论必须闭环到正式事实或最小原文。完整合同见 `docs/HYBRID_RETRIEVAL_ORCHESTRATION.md`。
- 长篇终局增量的开工设计已用 `DR-20260719-01` 一次冻结：总合同、运行状态机、本机安全/运维、独立评测和八阶段TDD计划分别位于 `docs/PRE_DEVELOPMENT_DESIGN_FREEZE.md`、`docs/RUNTIME_WORKFLOWS.md`、`docs/SECURITY_AND_OPERATIONS.md`、`docs/EVALUATION_PROTOCOL.md` 和 `docs/superpowers/plans/2026-07-19-final-longform-platform-implementation.md`。
- 历史首版release继续是只读基线 `wm-v1-20260716-220959-d5dd704d`；长篇增量 `wm-longform-r1-20260719-003435-e4d7b8b7` 已激活并进入连续开发。
- 长篇增量使用Schema 0010至0016向前新增、outbox、非活动快照、影子读和逐书策略指针切换；失败切回旧策略/快照，不修改旧正史含义。旧事实缺三轴时间时标记部分完整，补全只形成候选。
- 容量、检索正确性、运行可靠性、创造性非劣效和真实纵向文学质量分别按E0至E4声明；确定性500万字符/1500章回放不能冒充真实模型1000章文学质量。
- 长篇终局范围基线保存在 `docs/OWNER_DEVELOPMENT_SCOPE_REVIEW.md`；老板已确认并激活当前release，实施与证据以活动release目录为准。
- DEC-023指出的旧Web缺口已在当前长篇release实现：提供设定框架、总纲、卷纲、章纲、章节列表、真实卷章树、版本/来源/候选确认和1500章有界导航。
- DEC-024不安装第三方通用去AI味Skill作为产品运行时依赖；妲己/Kimi执行带位置证据的小说专用检测，主编生成定点修改单。外部规则只有经过来源、许可证、安全和小说质量非劣效审查后才能写入内部版本。
- DEC-033替代DEC-025的开书必填范围：界面只要求书名与频道；一句话想法、主要标签和作品边界可选，目标读者、规模、表达基线、世界观、力量体系、主线、结局和具体技法在书内讨论。主要标签是软方向并固定说明“其他自由发挥”，只有老板主动选择的额外边界才是硬意图；当前先完成UI预览，不能冒充软/硬/候选运行时治理已激活。
- “情绪引擎”不是开书字段；情绪图谱分开计划曲线与正式正文形成的实际曲线，只做带证据/置信度的分析投影，不自动决定剧情或成为正史。
- 首页取消写1章/连续写3章/连续写5章选择。两名异模型编剧在剧情稳定后各自估算最小/推荐/最大章节跨度和前提，主编向老板推荐；正式正文始终逐章点评、确认、结算后继续。
- 模型配置可随时创建新修订，但只影响未来任务；运行中的讨论、工单、稿件和点评继续引用冻结快照。无兼容三点评组合时受阻，不临场偷换或按量付费回退。

## 来源索引

- 原始需求卡：`384ef0a8-a27a-4494-94b2-f48ad7137f97`。
- 专题讨论：`be9efd1d-a832-430a-8ac9-236dff4ea34a`。
- 最终方案卡：`0e33dc63-51fb-4319-934e-a1349d33cb51`。
- 24条完整共识：`docs/CONSENSUS_LEDGER.md`。
- 讨论后的老板决定：`docs/DECISIONS.md`。

## 使用方式

日常开发优先读取精简规格。只有遇到规格冲突、需要了解否决理由或验收细节时，才按条目读取完整共识，避免上下文过载。
