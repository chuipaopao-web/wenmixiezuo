# 连续开发路线图

## 1. 推进规则

- 正式开发使用唯一 `release_id`。
- 一次正式授权后，在范围、预算、架构和安全边界不变化时，阶段通过即可进入下一阶段，不重复询问普通继续。
- 每阶段必须汇报完成内容、验收证据、风险和下一阶段范围。
- 改变产品边界、扩大范围、增加未批准现金费用、修改重大安全/版权/正史门禁、执行生产恢复或遇到外部阻塞时必须请老板决定。
- 同一根因初次失败后最多自动进行两次修复尝试；仍未解决时重新分析任务结构，禁止无限转派。

## 2. 阶段1：项目与契约底座

### 目标

建立独立仓库、锁定运行时与依赖、目录、配置、文档发布、迁移框架、API契约、测试框架和质量门禁。

### 计划文件范围

- 根目录配置和启动脚本；
- `apps/web`、`apps/api`、`apps/worker` 空骨架；
- `apps/api/src/contracts`；
- `apps/api/src/infrastructure/db/migrations`；
- `tests` 与确定性假模型；
- 当前文档。

### 验收

- 三个应用可以构建和统一启动；
- 只监听本机；
- 空库迁移、重复执行和失败回滚通过；
- 文档、API和Schema版本可追溯；
- 未创建任何智囊团运行依赖。

## 3. 阶段2：数据安全底座

### 目标

完成Repository、跨书隔离、不可变正文、文件登记、操作恢复日志、书籍生命周期、归档、备份、恢复和密钥边界。

### 实施顺序

1. Repository隔离契约；
2. 文件目录和 `file_registry`；
3. 暂存、哈希、同卷提升和数据库登记；
4. 操作日志与故障恢复；
5. 书籍归档、导入、恢复和删除墓碑；
6. 一致性备份与真实恢复验证。

### 门禁

在Worker接入前，必须用模拟写入验证中途崩溃、已提升未登记、已登记文件缺失、跨书访问和恢复。不能先生成真实正文再补安全机制。

## 4. 阶段3：运行与管理底座

### 目标

完成任务状态机、独立Worker、模型与工具适配、预算冻结、检查点、真实状态、11个创作Agent实例、小文秘书按书会话、双主编租约和接管。历史首版9实例仅作为显式升级输入，不在新书中继续创建。

### 门禁

- Worker崩溃后能区分未调用、成功可复用、结果不明、暂存待校验、可恢复和确实失败。
- `interrupted` 不自动重试。
- 旧 `editor_epoch` 指令拒绝。
- 同书严格串行，任务状态来自真实事件。
- 11个创作岗位、模型绑定修订、点评选择策略与小文秘书按书会话原子创建；按需专家不会默认全部激活，小文秘书不计入创作Agent人数。

## 5. 阶段4：小说领域核心

### 目标

完成DEC-078当前开书卡、版本化分类、定位卡、版本化标签定义/别名、主编自然语言标签治理与主动开场、题材适配、11人创作团队与独立小文秘书会话、创作方案、故事圣经、总纲、章纲和写作契约。当前开书卡包含1—8位初始主角和20—800字故事方向；故事方向贯穿设定与总纲规划但不成为正史或主笔重复上下文。DEC-070退役独立卷纲，正文仍可按物理卷组织。

开书卡按DEC-048增加无状态剧情梗概识别：最多5000字符、本地确定性候选、只填空字段、作者可修改、不保存原文且不写正史；布局把该入口与分类卡组成左列连续工作区，消除与右侧主角卡高度不一致造成的大块空白。

### 门禁

- 自然语言和标签能够形成可编辑定位卡；
- 明确、推断、未指定和冲突状态可见；
- 定位版本变化精确失效下游成果；
- 计划、候选设定和正史不混淆。
- 新书缺少故事方向不能提交；旧书缺少该字段仍可读且不改写历史快照；主编开场只校准方向并询问1—3个设定问题。
- 主编只在当前书籍、当前租约和可撤销范围内配置标签；其他Agent的标注建议不会冒充老板确认。

## 6. 阶段5：记忆与正史核心

### 目标

完成分层记忆、知识实体、事实断言、A/B/C/D门禁、章节事实、正史版本、FTS、上下文包、人物状态、标签赋值、可读语义标注、资料缺口和确定性一致性检查。

### 门禁

- 500万规范化中文字符、1500章满规模测试的分布式硬锚点召回100%；
- 两书记忆和检索零串线；
- D级事实未确认不能结算；
- 旧正史上下文不能继续写入；
- FTS损坏可重建且不影响正文和正史。
- 标签、语义标注、关系/情绪和地图投影均带来源、故事时间与状态；资料缺口不会把有意未知和自由创作区误判为阻断。

## 7. 阶段6：创作闭环

### 目标

从自由聊天和双编剧跨度评估进入滚动规划，再逐章跑通：写作、硬检查、三职责异模型点评、最多两次定点重写、最终复核、事实提取、结算和断点续跑。取消1/3/5章批次选择，但保留跨章串行续跑能力。

### 实施顺序

1. 主笔选择和真实模型能力验证；
2. 单章上下文包与写作契约；
3. 完整初稿与不可变版本；
4. 审校、问题结构和定点重写；
5. 章节结算与章末状态；
6. 自然“继续写”触发下一已规划章节，验证五章逐章串行续跑；
7. 中断恢复和跨章质量预警。

### 门禁

前章未结算后章不启动；半章不能进入正式正文；已完成章节不重复生成；质量失败不能当技术故障无限重试。

## 8. 阶段7：专业增强与完整界面

### 目标

完成完整开书卡、自由聊天首页、本书资料、设定大纲、剧情总纲和章纲组成的规划工作台；正文工作台独占章节列表和真实卷章树，并支持作者不可变修订、重写和定稿链；作者可见主角状态资料库、计划/实际情绪图谱、关系/主支线/钩子/信息差图谱、地点地图和标签中心；完成模型绑定预览/激活/历史页、拆书和版权门禁、联网研究以及桌面/手机/PWA完整体验。1500章按物理卷加载和虚拟滚动，不能用通用JSON、批次数按钮或硬编码“第一卷”代替正式界面。

### 门禁

- 图谱可从正式成果重建，不成为第二套正史；
- 作者可以从中间全屏资料库查看实体、版本、地图、标签和任务相关缺口；“来源与证据”按资料对象去重显示中文事实、来源章、证据等级、权威状态和简短依据，不提供重复原始证据卡，也不暴露内部键、ID、JSON或原始向量；
- 主笔不能访问原文隔离区；
- 换名仿写和标志性结构照搬被阻断；
- 网络资料只形成候选；
- 多书页面状态、草稿和离线缓存零串线；
- 页面不跳动，Agent状态真实。

## 9. 阶段8：全量验收与本地发布

### 目标

完成故障注入、确定性模型全链路创作、24小时Worker逻辑稳定性测试、两书隔离、备份恢复演练、文档同步和本地Git发布；真实模型只有在环境变量凭证和费用授权齐备后另行启用。

### 门禁

以 `ACCEPTANCE.md` 为准。阶段8只做全量回归和交叉故障验证，不能把阶段2至7的关键风险拖到最后首次测试。

## 10. 并行规则

- 每阶段开始前建立任务DAG、唯一负责人、允许/禁止文件和合并顺序。
- 只有文件范围完全互斥时才并行。
- 两个任务需要同一文件时，先建立公共前置任务并串行验收。
- 数据库迁移、公共API、共享类型、核心编排和安全机制始终串行。
- 单项测试通过不等于集成通过，合并后重新执行全量验证。

## 11. 阶段验收包

每阶段保存：

- `release_id`、阶段和任务DAG；
- 已完成、受阻和明确排除项；
- 主分支提交哈希；
- 迁移版本与实际执行结果；
- API、共享类型和文档版本；
- 类型检查、测试、构建和本地运行结果；
- Repository、正文、备份和恢复状态；
- 风险和下一阶段允许范围。

## 12. 超长篇连续性后续路线

首版八阶段历史不回写。后续按“权威数据结构→层级连续性/阶段结算→检索活跃度与触发下钻→岗位连续性→滚动规划→前端可见→满规模E2/E3→真实E4”串行推进。公共Schema、Repository、投影水位、检索和上下文属于核心共享边界，必须串行实现和验收。

容量门禁为500万规范化中文字符、1500章，不再使用单行100万字符作为超长篇证明。长期陪伴门禁为DEC-021的11名成员跨重启、跨卷、跨100章间隔的正确接续和未激活零调用。文学质量仍按真实模型纵向证据单独报告，不把容量测试冒充创作质量。

DEC-015门禁为：旧阶段默认摘要化、全局硬事实/开放线程不衰减、后期关键触发可逐级回查早期正史、无关旧正文不注入、摘要错误可从权威源恢复、每次下钻和上下文预算有界。任何一项未通过，不得启用为正式生产默认策略。

DEC-019在检索活跃度之后串行实现：不可变查询计划→四路通道运行→同源证据簇/冲突组→H/E/I三车道融合→正式来源闭环→11名成员差异化组包。先取得Schema/Repository和E2隔离恢复，再用独立金标校准Top-K、关系预算、RRF和无答案阈值；不得先凭经验固定权重后用同源夹具自证。

## 13. 长篇终局增量冻结路线

DEC-020把上述“后续路线”冻结为新release的八个可验收阶段，历史首版阶段编号和证据保持不变：

1. release基线、安全入口与能力探针；
2. Repository边界、Schema 10—11和知识生命周期；
3. 切片快照、投影outbox与本地语义通道；
4. 四路检索、证据闭环与上下文编译；
5. 长篇连续性、11名成员连续性与滚动规划；
6. 自由聊天、双编剧方案/跨度评估、单活动写手、三职责/三异模型全文点评与创造性保护；
7. 规划/资料工作台、检索诊断、可移植和本机运维；
8. 独立评测、全规模回放、恢复与发布。

唯一逐任务施工清单为 `docs/superpowers/plans/2026-07-19-final-longform-platform-implementation.md`。老板已于2026-07-19明确开工并激活release `wm-longform-r1-20260719-003435-e4d7b8b7`。每书必须按回填、探针、影子读和原子指针切换；任何阶段不得用全库一次切换替代。

DEC-021/DEC-026作为本冻结路线的岗位与点评修订：阶段2增加11人模板、模型绑定修订和三点评职责数据合同；阶段5实现主编/副编、默认DeepSeek＋GLM双编剧、可替换剧情席、主笔/副笔连续性与租约；阶段6让每个完整稿由三个异于活动写手的模型并行点评，GLM写手时事实席用DeepSeek，并验收AI腔风险、标记段落占比、政治/情色风险、三份全文Token和最多两轮修订；阶段7展示11人团队、模型配置和三份报告；阶段8完成真实模型独立性、误报/漏报、非劣效和故障阻断证据。

DEC-023/024/025定义的阶段7界面由DEC-046和DEC-070进一步修订：阶段7必须展示完整开书、自由聊天、本书资料、设定大纲、剧情总纲、章纲，以及只在正文页出现的真实物理分卷章节列表；不显示卷纲或1/3/5章批次入口；阶段6/8验证去AI味规则默认检测、定点修订和创造性非劣效，不安装第三方通用Skill作为运行时黑盒改写器。

DEC-027进一步修订阶段1、3至8：阶段1探测本地通用工具模型硬件/许可/哈希但不下载；阶段3/4实现工具模型适配、冻结路由金标、消息路由决定和允许降级；阶段5保存按书会话与可回滚工具经验；阶段6实现点名直达、低风险本地处理、主编主持的持续剧情会话和原话保真；阶段7展示小文秘书受理身份与11名创作成员；阶段8同时验收路由正确性、云Token、本地资源、创作非劣效和纵向漂移。DEC-043进一步规定窄右栏只显示11名创作成员，小文秘书仅在聊天和本地回执中出现，避免与已合并的系统身份重复。

DEC-041/Schema 0023补充正文作者修订、重写/定稿生产入口、主角追加式状态账本和安全属性公式；当时顶层图谱统一人物关系、情绪、主线、支线、钩子/伏笔和信息差。当前作者界面按DEC-072只保留人物关系、情绪、钩子/伏笔和信息差，主线回到剧情总纲阶段，支线回到会话、章纲和开放线程；后台历史投影继续兼容。该增量不改变八阶段架构、十一岗或正式三席，完成证据按 `DR-20260720-04` 和release目录记录。

DEC-043把主角页面从固定八分类改为按书动态分组：只消费已结算、带来源且明确使用主角关系键的事实，无法可靠归类时建立资料缺口并由作者确认分类；作者重分类只追加修订。默认字体档提高约10%，右栏不再重复显示小文秘书卡。

DEC-073把任务中心从单书工作台移至书架首页：使用轻量只读聚合接口按活动书返回任务、相关章节/成员、预算和待确认，书内导航不再重复任务入口。后台任务、Worker和书籍隔离语义不变，所有取消、重试与确认仍携带所属 `book_id`。

## 14. DEC-049增量交付

持续创作会话增量沿用活动长篇release，不另开服务或第二数据库：Schema 0026与Repository→会话/讨论路由→锁定后滚动规划→试写/正式分流→主笔最小上下文→阶段结算选择→质量快照与回退→工作区投影→迁移、运行、恢复、规模和验收证据。

该增量最高只能用确定性测试取得E2工程证据。E3独立金标和E4真实模型纵向创作继续作为后续质量门禁，不因代码、测试数量或50章以前的试写而自动通过。

## 卷驱动 V2 实施顺序（DEC-107）

当前release追加P0至P17施工合同，完整任务见docs/superpowers/plans/2026-08-08-creation-workflow-v2-simplification.md。

顺序固定为：规格冻结→可达基线→前端无行为拆分→共享合同/模板→作者协作→开书→设定→卷纲→事件→章纲→上下文→正文审查→事件/卷结算→任务状态→新创作台→条件式团队任务→旧代码精简→发布验收。

卷纲、事件、上下文和结算先在测试书/影子策略运行；完整单章与恢复通过后才切活动书；至少一个完整卷到下一卷、旧书和来源门禁通过后才删除旧页面装配。
### P7当前交付记录（DEC-108）

已完成Schema 0038、卷规划Repository/Service/REST、开书与设定/上一卷结算依赖、不可变候选、模板实例、当前卷作者原话隔离、CAS确认、历史切换、影响预览、跨书门禁和当前卷原页面交互；旧前端剧情模式库与阶段总纲组件已删除。DEC-109进一步完成真实`volume_plan_generation`任务、两位编剧并行且互不可见的独立ContextPack/ModelCall、A/B检查点、主编后置融合、幂等复用、取消、失败重试、结果不明停止和页面真实状态。

P7记录保留为历史阶段证据；后续DEC-110已经完成P8—P14、P16和P17的E2工程收口。真实套餐模型的跨题材盲评、创造性非劣效和长篇纵向效果仍未取得E3/E4。

### P8—P17当前交付记录（DEC-110）

Schema 0039—0041、事件序列与双编剧事件候选、完整事件章链与近期1—3章冻结、卷—事件—章纲事实审查边界、单章正文工作流推进、事件/卷计划—实际结算、下一卷门禁、标准SSE游标恢复、统一创作台和旧前端页面精简均已完成E2工程实现。

P8、P9、P10、P11、P12、P13、P14和P16完成。P15是必须另行确认成员拓扑的条件任务，当前继续使用DEC-021/099确定的11名创作成员与独立小文秘书。P17已完成类型、测试、构建、迁移、隔离、恢复、运行和本机UI冒烟；真实套餐模型跨题材盲评、独立金标与20/50/100—200章纵向文学质量仍属于E3/E4门禁，不能由本次绿测替代。