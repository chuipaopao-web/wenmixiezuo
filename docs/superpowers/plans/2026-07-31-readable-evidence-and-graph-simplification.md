# 作者可读证据中心与叙事图谱精简实施计划

> **执行合同：** 由当前 Codex 在 `D:\wenmixiezuo` 内单独完成；不调用开发子 Agent，不修改 `D:\AI智囊团`。

**目标：** 恢复资料库中的证据入口，但只显示作者能理解、可追溯且已经去重的信息；从叙事图谱前端移除与剧情总纲重复的“主线、支线”入口。

**不做什么：** 不删除事实、证据、主线或支线投影数据；不修改正史、RAG、知识投影生成、数据库迁移或模型上下文；不恢复原始 JSON 和内部字段展示。

**技术边界：** 继续使用现有 `/library` 数据契约。证据中心复用事实中文化、权威状态和来源章转换逻辑；图谱只收窄作者可见标签，后台投影继续保留。

## 任务 1：建立展示回归基线

**文件：**
- 修改：`tests/integration/experience/workspace-ui.test.tsx`

**步骤：**
1. 把“证据入口不存在”的旧断言改为“来源与证据入口存在”。
2. 断言证据页显示中文事实、来源章节、证据等级，不显示内部关系键和原始结构。
3. 为相同事实加入重复测试数据，断言作者界面只出现一次。
4. 将图谱标签改为人物关系、情绪、钩子与伏笔、信息差，并断言主线、支线不存在。
5. 运行体验测试，保存红灯证据。

## 任务 2：实现作者可读证据中心

**文件：**
- 修改：`apps/web/src/app/App.tsx`
- 修改：`apps/web/src/app/app.css`

**步骤：**
1. 恢复资料库的“来源与证据”标签。
2. 新增专用 `EvidenceCenter`，按资料对象归组并复用既有事实去重逻辑。
3. 每项仅显示中文事实名称、事实值、来源章、证据等级和权威状态。
4. 对可用的证据摘录只做安全、短文本展示；过滤内部 ID、键名、JSON 和重复内容。
5. 使用横向全宽列表布局，避免窄卡片造成竖排。

## 任务 3：精简叙事图谱

**文件：**
- 修改：`apps/web/src/app/App.tsx`

**步骤：**
1. 从 `GraphTab` 和作者可见标签中移除 `mainline`、`subplot`。
2. 更新页面说明为四类叙事视图。
3. 保留主线、支线投影读取与兼容代码，避免影响历史数据、RAG 和恢复。

## 任务 4：同步当前规格与任务账本

**文件：**
- 修改：`docs/DECISIONS.md`
- 修改：`docs/PRODUCT.md`
- 修改：`docs/ACCEPTANCE.md`
- 修改：`docs/USER_GUIDE.md`
- 修改：`docs/DEVELOPMENT_ROADMAP.md`
- 修改：`docs/COVERAGE_MATRIX.md`
- 修改：`TASKS.md`

**步骤：**
1. 记录新决定：证据页恢复为作者可读证据中心，取代“完全不设证据页”的旧展示决定。
2. 明确原始证据结构仍不得出现在作者界面。
3. 明确主线、支线归剧情规划，不再作为独立图谱标签。
4. 记录目标、边界、验收、测试、停止条件与回滚方法。

## 任务 5：验证与提交

**验证命令：**
- 定向 UI 集成测试
- 类型检查
- 自动测试
- 构建
- 空库迁移与升级验证
- 本地运行探测
- 验收脚本

**停止条件：**
- 作者界面仍出现内部键、原始 JSON 或重复证据；
- 图谱仍显示主线、支线；
- 事实或投影后台数据被删除；
- 现有资料库、图谱或规划回归失败。

**回滚方法：** 回退本次 UI、测试和当前规格提交；本次没有数据库迁移或数据删除。
