# 单阶段剧情总纲与剧情模式库实施计划

1. 扩展 `stage_master_v2` 可选字段并保持旧数据兼容。
2. 调整主编落库提示词和反向拆解约束，一次只处理当前完整阶段。
3. 建立本地版本化剧情模式目录，按分类、题材和标签推荐。
4. 在规划页提供搜索、分组、主辅选择和一键发起讨论；模式不直接写入正史。
5. 更新总纲阅读与编辑界面，展示结束验收、防偏移边界及创作自由区。
6. 对《少女的实验笔记》现有正文与章纲生成兼容的新候选总纲版本，不创建新书。
7. 执行类型检查、自动测试、构建、迁移与本地运行验证。
