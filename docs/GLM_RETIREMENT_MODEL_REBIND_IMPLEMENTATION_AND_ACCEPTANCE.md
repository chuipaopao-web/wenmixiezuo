# GLM 下架与模型换绑开发、验收清单

状态：已完成并上线（2026-08-22）

## 一、老板最新决定

- GLM-5.2、GLM-5.3 从当前可用模型中直接下架，不保留存量书兼容分支。
- 红玉（第二编剧）改绑 Coding Plan 的 `doubao-seed-2.1-turbo`。
- 西施（副编）改绑 Coding Plan 的 `deepseek-v4-flash`；班昭（事实审核）改绑 Coding Plan 的 `minimax-m2.7`，避免与文学审核使用同一模型伪装独立复核。
- API 发布重启时，全部测试书的当前 AI 成员绑定统一收敛到新方案；历史调用记录只作为审计证据保留，不再参与当前选模。

## 二、开发清单

- [x] 修改 15 人团队岗位默认模型绑定，确保当前岗位零 GLM-5.2/5.3 引用。
- [x] 修改运行时角色模型配置，使资料压缩/连续性岗位使用豆包，编辑岗位使用 DeepSeek Flash，事实审核使用 MiniMax M2.7。
- [x] 平台模型方案过滤 GLM-5.2/5.3，旧自定义方案含下架模型时自动回退到最新默认方案。
- [x] 模型适配器在执行前明确拒绝 GLM-5.2/5.3，避免旧任务快照继续调用下架模型。
- [x] 更新受影响测试、当前规格与交接状态；历史调用记录和历史发布证据不篡改。
- [x] 发布 API，并在 API 切换后恢复 Worker；现有测试书当前绑定已统一换绑，不做数据迁移、不删除作者数据。

## 三、逐项验收清单

- [x] 默认团队：红玉=`doubao-seed-2.1-turbo`，西施=`deepseek-v4-flash`，班昭=`minimax-m2.7`。
- [x] 运行时配置与团队合同一致，四位普通编剧仍保持不同模型方案来源。
- [x] 平台管理端允许模型列表中没有 GLM-5.2/5.3，保存下架模型会被拒绝。
- [x] 旧平台方案或旧任务试图调用 GLM-5.2/5.3 时，分别回退新默认或返回“模型已下架”的明确错误。
- [x] 定向测试通过：运行时配置、团队合同、平台方案、模型绑定与启动收敛。
- [x] API 类型检查与生产构建通过；本次不运行全量测试，原因是仅修改模型配置/绑定和执行门禁，无数据库结构、权限或跨服务恢复变化。
- [x] 部署前在途任务连续 30 秒为零并立即复核；API 切换后 Worker 已恢复，健康检查通过。
- [x] 生产数据库当前有效绑定中 GLM-5.2/5.3 数量为 0，新模型绑定数量与岗位/测试书数量一致。
- [x] 公网首页、健康接口、未登录书籍权限检查正常，服务日志无新错误。

## 四、数据与安全边界

- API Key 仍只从服务器环境变量读取，不进入代码、数据库、日志或本文档。
- 不删除正文、设定、任务或历史模型调用记录；只替换当前有效 AI 成员模型绑定。
- 不引入迁移。若发布验证失败，回滚到上一 API/Worker 发布版本。

## 五、完工证据

- 本地定向验证：9 个直接测试文件、35 项用例全部通过；API 类型检查、API/Worker 生产构建通过；未运行全量测试。
- 新模型最小真实探针：Coding Plan `minimax-m2.7` 成功，输出 1 token，套餐现金成本记录为 0。
- 生产发布：`wm-model-rebind-r1-20260822-155800-b658d38a`，代码提交 `b658d38a`；发布前备份成功，任务连续 30 秒及切换前立即复核均为 0。
- 生产当前绑定：红玉/豆包 17，西施/DeepSeek Flash 17，班昭/MiniMax M2.7 17；GLM-5.2/5.3 当前有效绑定 0。
- 线上验收：7 个关键 API 构建文件与暂存产物一致；API/Worker/Caddy active，Worker ready；公网首页 200、健康 200、未登录书籍 401，新启动日志无错误。
