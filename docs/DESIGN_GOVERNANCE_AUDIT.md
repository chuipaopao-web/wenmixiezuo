# 当前设计治理与审计规范

## 1. 适用范围

架构、数据、上下文、检索、Agent、生成、审查、结算或长篇质量的重要变更使用本规范。纯文案、低风险样式和明确机械修复不启动完整审查。

## 2. 权威顺序

作者最新决定 → 项目章程与开发规则 → 当前生效决定 → 当前规格 → 代码和证据。Git只提供追溯，不能覆盖当前规格。

## 3. 设计记录

每项重大设计创建唯一 `design_review_id`，记录：

- 问题、可观察症状和真实根因；
- 已确认事实、推断、作者偏好和未知；
- 适用Skill名称与文件哈希；
- 至少一个最强替代方案及取舍；
- 机制、反例、指标、证据等级和停止条件；
- 影响文件、迁移、回滚和复核人。

## 4. 两轮审查

第一轮检查正确性、安全、恢复、跨书隔离、版本和来源。第二轮检查创造性、人物生命力、合理惊喜、情绪力度、整体阅读质量和作者控制权。第一轮提升不能抵消第二轮下降。

## 5. 上下文和RAG变更

必须验证任务矩阵、其他书排除、活动版本、作者原话、硬约束位置、来源回查、预算、降级和投影可重建。不能用“增加向量、多Agent或更大上下文”代替根因证据。

## 6. 创作机制变更

相对现行流程或弱治理基线做非劣效对照。局部修改使用多样本对照；全链路修改使用跨题材、跨阶段和纵向盲评。评分表不得直接成为正文提示词。

## 7. 证据等级

E0规格，E1机制存在，E2确定性工程，E3独立金标/真实模型盲评，E4真实长篇纵向效果。不得用短流程或固定同义词测试声明E3/E4。

## 8. 精简审计

精简前检查启动入口、动态引用、路由、Worker任务、迁移、恢复、测试和作者数据。只有当前产品不需要且运行时零引用的代码或资料才能移出工作树。已合并迁移、作者原件和不可变正文不参与普通精简。

## 9. 当前审计基线

`DR-20260810-UNIFIED-DESK-20`：当前工作流为开书—设定—卷—事件链—事件大纲—章纲—正文—三级结算；统一创作台和独立灵感只围绕这些对象运行。开发结算必须通过运行时可达性、类型、全量测试、四端构建、真实本地服务和文档同步，并以双事件20章确定性纵向流程作为E2基线。真实套餐模型20章文学质量仍需独立E3证据，不能由自动测试冒充。

## 10. DR-20260810-SEMANTIC-CLEANUP

- 问题与根因：当前对象化工作流已经替代对话式入口，但Worker能力声明、少量展示工具、旧评测脚本、上下文策略默认值和附件物理表名仍携带旧语义；根因是功能迁移后缺少一次跨入口、路由、数据、测试和恢复边界的统一收口。
- 已确认事实：当前主流程是对象化协作；作者附件仍是有效能力；已合并迁移不可改；历史任务必须被明确拒绝而不是重新执行；旧书升级适配必须保留；Git需要承担后期恢复点。
- 作者偏好：清理代码语义残留，仓库只表达当前功能，同时保留Git结算便于恢复。
- 未知：真实存量库中的旧附件数量与真实模型长篇效果，不用推断替代迁移测试和E3/E4验收。
- 适用Skill：`wenmi-longform-quality`；SHA-256 `5bfe9c079103ba048eddd7c257abd35800e8f26f9461ad0bd84f3424b562b7c8`。
- 选择机制：删除零引用旧运行时和旧评测入口；当前展示只保留通用净化；上下文策略升级到对象化版本；新增0042前向迁移，将旧附件表和字段原位改为作者附件语义，并升级规划输入链接类型；保留迁移、历史任务拒绝和旧书升级适配。
- 最强替代方案：新建附件表、复制数据、删除旧表。该方案命名更直接，但复制与删除扩大数据风险，且不优于SQLite原位重命名，因此放弃。
- 第一轮审查：验证迁移保留原件与来源、旧表退出、链接同步升级、跨书/任务/版本边界不变；运行迁移、类型、全量测试、构建、可达性与文档校验。
- 第二轮审查：不改11名成员、创作提示词、卷—事件—章纲约束和软模板；清理只减少错误上下文与错误能力暗示，不增加创作公式。
- 证据与停止条件：E2工程证据全部通过且Git工作树完成结算才可结束；出现数据丢失、旧任务被重新执行、跨书污染、当前附件无法进入卷纲上下文或任一门禁失败立即停止。
- 回滚：代码和文档可回退到本次结算前提交；0042只做保数据重命名，已升级数据库不倒退迁移，必要时由后续前向迁移修正。

## 11. DR-20260810-WORKFLOW-VISIBILITY

- 问题与根因：实际十章书籍具备卷纲、事件链、事件大纲和事件章纲正式记录，但结算后活动事件指针被清空，前端又只读取活动事件并只显示待处理章纲；卷计划列表还只选择活动状态，因此作者看到“规划、章纲为空”。服务端工作流同时把活动事件引用固定返回为空，放大了问题。旧数据审计只统计正文数量，不能证明逐级规划链存在。 进一步从新书复测发现，旧十章脚本仍调用已退役的对话消息接口，本地确定性模型也缺少设定对象融合落库合同；两者都会让“测试已通过”的结论失真。
- 已确认事实：目标书籍存在1个已确认卷纲、1个已结算事件、1个已完成章链、10份已结算事件章纲和10章定稿正文；每章流水线均能通过正式制品版本追溯到对应事件章纲。缺陷属于工作流引用与历史可见性，不是规划数据丢失。
- 作者偏好：从头核对流程；规划和章纲不得在正文完成后消失；项目资料需要可整页复制。
- 未知：真实套餐模型的十章文学质量仍需独立E3证据，不由本次确定性数据与界面修复代替。
- 适用Skill：wenmi-longform-quality；SHA-256 5bfe9c079103ba048eddd7c257abd35800e8f26f9461ad0bd84f3424b562b7c8。
- 选择机制：工作流返回真实活动事件版本；活动对象不存在时按当前卷最新已完成事件加载只读历史；事件页和章纲页区分可编辑活动态与只读完成态；重写当前数据审计，逐级检查开书、设定、卷纲、事件链、事件大纲、章纲、正文及结算绑定；文档同步额外生成单页合订版。
- 最强替代方案：结算时继续保留活动指针。该方案会混淆“已完成”与“仍可修改”，使误操作和重复结算风险上升，因此选择清空活动指针但保留只读历史。
- 第一轮审查：验证活动引用、完成态加载、禁止历史修改、每章版本绑定、数据库完整性、跨书作用域、类型、测试、构建和文档同步。
- 第二轮审查：不改变11名成员、提示词、模板或创作自由；只恢复作者对真实规划链的可见性和验证能力，不把审计字段注入正文生成。
- 证据与停止条件：目标书逐级数据审计、组件回归、全量工程门禁和真实启动均通过才可结算；任一规划层缺失、正文无法追溯章纲、历史页可误修改或其他书数据混入立即停止。
- 回滚：前后端和文档可通过本次Git结算提交回退；不修改或删除正式作者数据和既有迁移。

## 12. DR-20260810-SETTLED-HISTORY-VALIDITY

- 问题与根因：刚完成的测试书没有任何作者修改，但已结算事件的章纲页显示“上层已变化，需重建”。章纲有效性查询只接受状态为活动的事件、卷纲及其版本；事件结算和卷结算正常推进状态后，活动快照查询返回空，于是把业务状态推进误判为内容换版。
- 已确认事实：测试书的卷纲版本、事件版本、事件章链、10份章纲和10章正文绑定均存在；事件章链状态为已完成，来源版本未改变且没有被标记过期。缺陷只在有效性解释与界面文案，不是作者修改或数据丢失。
- 作者偏好：没有修改就不得提示重建；提示必须让作者能区分正常结算、真实上游换版和历史来源异常。
- 未知：永久删除或未来归档策略是否允许物理移除来源版本；当前权威规则不允许普通流程删除正式来源，因此本次按来源存在性校验。
- 适用Skill：`wenmi-longform-quality`；SHA-256 `5bfe9c079103ba048eddd7c257abd35800e8f26f9461ad0bd84f3424b562b7c8`。
- 选择机制：活动章纲继续对比当前活动快照；已完成或已归档章纲改为按自身锁定的事件版本和卷纲版本查询历史快照。锁定来源存在且对象未明确过期时返回有效，并显示“结算版本已锁定”。
- 最强替代方案：只在前端隐藏警告。该方案会掩盖服务端仍返回错误有效性，其他客户端和后续验收仍会误判，因此否决。
- 第一轮审查：覆盖事件结算后、卷结算后、活动态和真实过期态；历史查询继续强制 `owner_id + book_id + event_id + version_id`，不放宽跨书隔离。
- 第二轮审查：不改变创作提示词、模型、Agent、模板或正文，只修复状态解释和作者反馈，不限制创造性。
- 证据与停止条件：后端结算集成测试与前端历史页面测试通过；全量类型、测试、构建、迁移、启动和文档同步通过后结算。若真实上游换版不再使活动候选失效、历史跨书串线或锁定来源缺失仍显示正常，立即停止。
- 回滚：回退本次代码和文档提交即可；本次不修改数据库迁移和作者数据。

## 13. DR-20260810-UNIFIED-DESK-20

- 问题与根因：旧界面把书架、横向步骤、常驻团队栏和对象页面分散展示，设定方案在真实任务开始前占满页面；旧确定性验收又可能在上下文缺失时生成无关固定正文，无法证明卷—事件—章纲确实约束正文。
- 已确认事实：当前产品保持11名创作成员；灵感需要保留但不能修改正式对象；作者要求统一创作台、成员与任务透明、模板大白话、图谱资料库合并，并用多角色双事件修仙新书跑通至少20章。
- 作者偏好：界面采用高分辨率iOS式玻璃层次，输入按需展开；书籍常驻左侧，团队、任务、灵感和设置位于顶栏；测试数据保留。
- 未知：真实套餐模型的具体文风和20章文学质量，需要E3盲评，不用确定性模型结果冒充。
- 适用Skill：`wenmi-longform-quality`；工作流质量边界保持软模板、人物主动性、因果代价和来源约束，不向正文注入评分公式。
- 选择机制：统一创作台壳层；11名成员卡片显示真实任务/上下文/输出状态；灵感使用独立数据与接口且仅允许作者摘录；卷和事件模板支持无模板、组合、重排与自定义；确定性写手读取同一正式ContextPack，并新增当前书角色、事件和术语泄漏回归。
- 最强替代方案：保留旧页面，仅更换配色和隐藏部分控件。该方案不能解决对象分散、任务不透明、讨论污染和假正文验收，因此否决。
- 第一轮审查：验证跨书隔离、活动版本、无章纲不得生成正文、事件/卷结算、错误重试、不可变正文、真实状态、迁移/启动、类型、全量测试、构建和文档同步。
- 第二轮审查：验证修仙测试书至少6名主动角色、2个因果连续事件、20个不同章纲、可见选择与代价、当前术语覆盖和无旧夹具泄漏；不把确定性文本质量上升为E3声明。
- 证据与停止条件：9项必要设定、1卷、2事件、20份章纲、20章正文、60份审查、20次章节结算、2次事件结算和1次卷结算全部成功且数据保留；出现无章纲正文、跨书污染、固定旧角色泄漏、错误状态或任一工程门禁失败立即停止。
- 回滚：代码、规格、Skill和前端可通过本次Git结算提交回退；新增讨论表与作者测试书数据保留，数据库只用前向迁移处理。

## 14. DR-20260810-IOS-TOP-NAV

- 问题与根因：上一版虽然去掉书架首页和右侧栏，却把书籍切换与对象功能继续混在左栏，并仅在顶栏放四个工具；视觉只追加一层覆盖，旧绿色变量和旧页面组件仍占主导，因此布局和风格都没有实现作者原意。
- 已确认事实：在线页面加载的是最新构建，不是浏览器缓存；问题来自规格与实现偏差。现行数据、工作流、11名成员和20章测试书不需要修改。
- 作者偏好：左边只放书籍并负责换书；上方放全部功能并负责换页；整体使用iOS高级浅色玻璃风格。
- 未知：不同Windows缩放比例和较窄屏幕下最合适的功能密度，需要响应式测试确定，不靠横向滚动掩盖。
- 适用Skill：`wenmi-longform-quality`只检查页面入口与对象链不丢失，不限制视觉方案或创作方式。
- 选择机制：采用“纯书籍栏＋单一顶部功能栏＋中心页面”的三段式信息架构；旧主题ID保留以兼容本机偏好，但重新映射到iOS浅色/阅读白/冰蓝/深色四套材质。
- 最强替代方案：保留旧结构，只把绿色改成蓝色。该方案不能修复书籍与功能混杂，也会继续产生两套导航，因此否决。
- 第一轮审查：验证换书、全部功能切换、归档/恢复、无重复导航、无横向滚动、窄屏抽屉、现有工作流状态与数据不变。
- 第二轮审查：只改变信息层级和视觉，不修改提示词、上下文、模型、规划或正文；确保作者操作路径更短且页面内容不被装饰挤压。
- 证据与停止条件：组件交互、无障碍、类型、全量测试、构建、真实启动和文档同步全部通过；任一功能入口消失、换书串页、窄屏不可操作或工作流回归失败立即停止。
- 回滚：通过本次Git结算提交回退前端与规格；不修改数据库迁移和作者数据。

## DR-20260810-WORKSPACE-LABELS 功能名称统一

- 已确认事实：作者要求12个功能入口使用信息、设定、分卷、规划、章纲、正文、资料库、取名、团队、任务、灵感、设置；数据库已有稳定surface、对象ID、任务来源和不可变审计文字。
- 作者偏好：前端、后端提示、数据库关联和当前文档必须一致，每次开发结算自动更新文档中心。
- 推断：把显示名直接改成数据库枚举会造成无收益迁移并威胁旧书恢复；显示层与稳定键分离更安全。
- 方案比较：方案一批量改表名、枚举和历史文字，风险高且会破坏来源证据；方案二由Contracts提供单一显示名合同，保持稳定键并在展示层转换旧文字。选择方案二。
- 第一轮审查：检查12个功能顺序、Web/API共享依赖、author input surface映射和界面点击测试。
- 第二轮审查：检查旧数据只读展示、全量类型/测试/构建、真实启动和文档中心同步；任一失败即停止结算。
- 回滚：回退本次Git提交即可恢复旧显示；数据库无结构和数据回写，因此无需数据回滚。

## 15. DR-20260810-TEMPLATE-CAUSAL-RECOMMENDATION

- 问题与根因：外部建议希望补足题材×风格推荐、事件间因果、下一卷重选和动态规则，但其中固定爽点频率、永久悬浮状态和规则原地修改会与当前软模板、统一创作台和不可变版本原则冲突。当前实现已有后端模板注册表和事件因果字段，但推荐只读取开书标签，前端把所有模板混排并硬限制最多三种，事件页也没有把主角状态承接完整展示给作者。
- 已确认事实：卷级与事件级模板已分开；后端模板具有版本和内容哈希；规划版本保存模板快照；卷结算包含真实结果、人物关系和开放问题；事件对象已有进入状态、触发、选择与代价、结果、结束条件、人物变化和下一事件接口。
- 作者偏好：参考有效建议并马上进入手工测试，同时保护AI创造性，不重新增加重复入口和固定创作公式。
- 未知：真实套餐模型在多卷纵向创作中对推荐排序的收益，需要E3/E4证据，不由本次确定性排序与界面测试冒充。
- 适用Skill：`wenmi-longform-quality`；模板和节奏只能作为软参考，正式事实仍以当前书活动版本和结算来源为准。
- 选择机制：题材留在信息页，卷推进方式留在分卷页；推荐区显示2—3种，其他方案按需展开；取消三种硬上限但保留0—3种建议；推荐信号加入当前活动卷和最近真实卷结算；事件页显式展示“上一结果—主角状态—触发—选择代价—结果—下一接口”。
- 最强替代方案：照搬固定频率参数并允许数据库直接修改活动映射。该方案短期可配置，但会把软倾向变成公式、破坏旧卷复现与回滚，因此否决。
- 第一轮审查：验证推荐只使用当前书资料、上一卷真实结算、模板版本哈希和快照不变；验证无跨书输入、旧卷可复现、事件编辑字段完整、接口与测试合同一致。
- 第二轮审查：验证作者可以不选、混合或自定义；不存在固定爽点、反转或章数频率；推荐只排序不限制选择，人物主动性和合理惊喜不因模板增加而下降。
- 证据与停止条件：类型、定向测试、全量测试、四端构建和文档同步全部通过后才结算；出现跨书推荐、旧规划模板快照变化、第四种选择仍被禁用、结算未参与下一卷推荐或任一门禁失败立即停止。

## 16. DR-20260810-UI-INFORMATION-HYGIENE

- 问题与根因：设定页同时显示功能副标题、完成横幅和重复计数，分卷页常驻大块教学卡，资料库在顶栏、总入口和资料卡片区连续重复名称与说明；任务和规划卡还直接渲染检查点、任务ID、内部阶段和原始模型标识。根因是各组件独立补充“帮助信息”，缺少跨页面信息唯一性和作者/审计展示分层。
- 已确认事实：顶部功能栏已经提供12个功能定位；后台任务、模型调用、检查点、版本和来源仍必须完整保存；作者要求界面只显示一次同义信息，并用自然语言反馈真实进度。
- 作者偏好：删除前端功能使用介绍，页面始终干净；隐藏技术执行细节，但不隐藏真实进度、错误和恢复操作。
- 未知：真实作者对首次空状态帮助密度的最佳偏好，需要后续使用反馈；本次不以猜测保留所有常驻说明。
- 适用Skill：`wenmi-longform-quality`；SHA-256 `e113b85833e4c961723f36deeb93c00163736a2ecf573c1c2232b054109f4440`。
- 选择机制：顶栏作为唯一常驻功能定位；中心页只保留对象、进度和操作；静态帮助改为首次空状态或决策点按需出现；普通创作页统一把内部状态映射为自然语言并隐藏技术标识，后台审计记录不变。
- 最强替代方案：只用CSS隐藏截图中的三段文字。该方案会保留其他页面的同义重复和机器键回退，也无法形成长期审查门禁，因此否决。
- 第一轮审查：验证主操作、真实状态、错误、取消/继续/重试、版本影响和无障碍名称仍可用；后台任务与调用证据不删除，数据、API、迁移和跨书隔离不变。
- 第二轮审查：减少作者认知负担，不修改创作提示词、模板、上下文或正文；确认清理不会把必要的剧情约束、候选差异和人物信息一并隐藏。
- 证据与停止条件：可见文本去重测试、技术词泄漏测试、相关组件测试、类型、全量测试、构建、真实启动和文档同步全部通过后结算；出现主操作消失、未知状态原样泄漏、错误不可恢复或任务审计数据被删除立即停止。
- 回滚：回退本次前端、规格与Skill改动；不涉及数据库迁移、作者数据、正文或任务记录。

### 明确结论与置信度

高置信度采用“顶栏唯一定位、中心页对象与操作优先、技术证据后台保留、作者进度自然语言化”。本次只改变展示与审查门禁，不删除任务、模型调用、上下文包、来源或版本证据。

### 当前事实基线

- 顶部功能栏已经提供十二个功能的持续定位。
- 设定、分卷、资料库、规划、章纲、正文、团队、任务与灵感仍由真实业务对象和任务状态驱动。
- 普通页面此前存在重复标题、固定教学说明、任务编号、检查点、模型内部编号和测试配置外露。
- 数据库、API、任务状态机、作者数据、上下文编译和检索来源不在本次改动范围。

### 能力追踪矩阵

| 能力 | 改动前问题 | 修正机制 | E2测试 |
|---|---|---|---|
| 页面定位 | 顶栏与中心标题重复 | 顶栏唯一常驻定位，中心标题改无障碍名称 | 可见文本去重 |
| 真实进度 | 机器阶段与检查点外露 | 映射为自然语言，未知值不原样回退 | 规划、章纲、任务状态测试 |
| 恢复操作 | 技术词压过作者动作 | 统一为继续、停止、重试与影响说明 | 故障恢复交互测试 |
| 团队透明 | 原始供应商路径和模型ID外露 | 普通页面只显示成员与自然状态，团队管理显示整理后的来源 | 团队页面测试 |
| 后台审计 | 误把隐藏等同删除 | 数据、API和调用记录保持不变 | 全量回归与构建 |

### 最强替代方案与取舍

最强替代方案是保留现有DOM，只用CSS隐藏截图中的几段文字。它修改最少，但无法阻止其他页面继续显示同义说明、原始机器键和内部编号，也不能形成回归门禁，因此否决。选择展示层语义清理与测试约束，代价是需要同步更新多个组件和既有测试。

### 预先验尸与反例

- 反例一：把所有说明全部删除，首次空状态没有下一步。修正为只删除常驻功能介绍，首次空状态、危险操作和关键决策仍可按需说明。
- 反例二：为“界面干净”隐藏错误和恢复按钮。修正为错误、影响、取消、继续和重试始终可见。
- 反例三：未知状态继续直接显示内部键。修正为未知值显示“正在处理”或“需要处理”，后台保留原值。
- 反例四：团队页完全隐藏模型独立性。修正为普通创作页隐藏内部ID，设置与团队管理只显示整理后的模型来源。

### 修正后的最终设计

1. 顶栏承担功能定位，中心页不重复同名功能标题、宣传副标题和固定教学横幅。
2. 同一完成数、状态或说明在一个页面只展示一次，并放在最接近操作的位置。
3. 设定页只保留真实确认进度与对象列表；分卷和规划页只保留对象、流程、方案与操作；资料库只保留视图切换和资料内容。
4. 普通创作页不得渲染任务ID、内部状态/阶段键、检查点、ContextPack、哈希、JSON、SQL、Worker、SSE、原始供应商路径或模型内部ID。
5. 所有进度来自真实任务状态；展示层只做自然语言映射，不伪造百分比、成员工作或模型结果。

### 创造性与输出质量保护

自由创作区保持不变：本次不修改卷与事件软模板、上下文矩阵、提示词、检索或正文生产约束。以当前功能和创作结果为基线做非劣效回归；E2只证明页面与状态机制，不冒充文学质量提升。后续真实模型盲评仍用于E3，真实长篇纵向阅读效果仍属于E4。

### 测试与证据等级

E0为本次规格和决定；E1为组件映射与审查机制；E2为类型、定向交互、全量测试、构建、启动和可见文本检查；E3为不同真实模型与作者盲评；E4为真实长篇纵向使用。本次最多声明E2，不用工程测试冒充E3或E4。

### 剩余风险与未知项

剩余风险是首次空状态帮助可能过少、整理后的模型来源粒度可能需要作者反馈、窄屏中动态状态可能拥挤。未知项包括真实作者对帮助密度的偏好和真实模型长时间任务的进度措辞，需要实际使用反馈，不在本次确定性测试中猜测。

### 停止、回滚和升级条件

任何主操作、真实错误、取消/继续/重试、影响预览或无障碍名称丢失，任何机器键原样回退，或任一类型、测试、构建、启动、Skill与文档同步失败，都停止结算。代码与文档通过本次Git改动回滚；数据库、作者数据和迁移没有改写。若需要改变任务状态机、模型独立性合同或上下文编译，则升级为新的架构审查，不在本次界面清理中顺带修改。

## 17. DR-20260810-AUTHOR-PLAIN-LANGUAGE-GATE

- `design_review_id`: `DR-20260810-AUTHOR-PLAIN-LANGUAGE-GATE`
- 问题与根因：信息页作者想法使用透明、绝对定位的单选框；点击标签后浏览器会聚焦隐藏控件并把创作台内部滚动到错误位置，表现为白屏和乱跳。与此同时，动态状态、错误和结构化内容虽有零散翻译，却没有语义分类门禁，既可能漏出任务编号和技术路径，也可能用全局词语替换误伤小说正文。
- 已确认事实：作者在真实运行书籍中点击“必须遵守”稳定复现；源代码存在透明绝对定位单选框；后台原始任务、上下文、版本和作者数据必须保留；作者要求所有可见信息先转成大白话，并保护小说梗概与正文语义。
- 作者决定与偏好：立即修复白屏；普通页面不显示技术执行细节；进度和错误用自然语言；大白话转换必须符合小说梗概、章纲、正文和作者原话的语义。
- 推断与未知：高置信度判断滚动跳转来自隐藏单选框聚焦；不同浏览器的具体滚动距离未知，但改成可见按钮可以从机制上消除该路径。真实模型输出中尚未出现的全部术语无法穷举，因此保留安全兜底和后续扩展表。
- 适用Skill：`wenmi-longform-quality`；SHA-256 `aa26c9a8e07a4b4b9cb62673cf5a834689386bcd3580890ebb32038095a41899`。
- 选择机制：关键选择改为可见、可键盘操作的按钮；统一作者展示门禁区分 `interface/progress/error/story` 四种语义。前三类翻译术语、过滤机器键/编号/路径并提供自然恢复动作；故事文本只清机器协议，绝不全局替词。门禁只处理展示副本，不写回业务对象。
- 最强替代方案：在页面根节点使用DOM观察器自动替换全部文本。该方案覆盖面看似最大，但会绕过React状态、误改正文与作者原话，并可能造成二次重排和新跳转，因此否决。
- 第一轮审查：按钮切换不得提交、导航、重载或丢失后续输入；未知状态、任务编号、哈希、技术路径和机器载荷不得穿透；API与数据库原件、任务恢复和跨书隔离保持不变。
- 第二轮审查：以带有“JSON、边界”等自然词的小说句子作为反例，确认故事文本逐字保留；界面术语转换不进入模型上下文、规划、正文或作者原话，不压缩创造性和人物语言。
- 证据与停止条件：交互测试覆盖四种作者意图强度；门禁测试覆盖技术状态、路径、故事文本和原始数据不变；类型、全量测试、四端构建、Skill验证、审计验证、真实启动和文档同步全部通过后结算。任一白屏、滚动跳转、正文词语变化或技术词泄漏立即停止。
- 回滚：回退作者想法控件、作者展示门禁和本节规格；不回滚数据库迁移，不删除作者数据、正文、任务或审计记录。

### 明确结论与置信度

高置信度采用“可见稳定控件 + 按语义分类的展示门禁”。不采用全页面DOM替换，也不让展示翻译写回业务数据。

### 当前事实基线

- 作者意见强度有必须、偏好、灵感和问题四类，业务值保持不变。
- 普通页面已经有结构化内容和状态映射，但此前没有统一处理目的参数。
- 小说梗概、正文和作者原话必须逐字保护，机器协议与技术执行信息必须隔离。

### 能力追踪矩阵

| 能力 | 改动前问题 | 修正机制 | E2测试 |
|---|---|---|---|
| 意图选择 | 隐藏单选框聚焦导致滚动乱跳 | 可见按钮、`role=radio`、真实 `aria-checked` | 四项切换与后续输入可见 |
| 动态状态 | 页面可能原样回退机器值 | progress门禁映射与安全兜底 | 技术词与UUID泄漏测试 |
| 错误反馈 | 技术路径或服务错误可能外露 | error门禁隐藏路径并给恢复动作 | 本地路径/SQL错误测试 |
| 小说语义 | 全局替词可能改坏正文 | story通道只清机器协议 | 自然词逐字保留测试 |
| 后台追溯 | 前端隐藏可能误删证据 | 展示副本单向转换，原件不变 | 全量回归与恢复测试 |

### 最强替代方案与取舍

DOM观察器可以在不改组件的情况下替换所有页面文本，但无法可靠区分按钮说明、模型状态、作者原话和小说正文，也会与React重渲染互相覆盖。选择类型化展示门禁需要逐步接入通用展示边界，但能保护语义并形成可测试合同。

### 预先验尸与反例

- 反例一：把小说中的“边界”机械改成“范围”。修正为story通道在术语替换前返回原文。
- 反例二：已知枚举先被改成“待整理内容”。修正为保留已知枚举，再由作者格式器映射中文。
- 反例三：错误字符串携带本地文件路径。修正为error通道直接给自然语言恢复提示。
- 反例四：按钮看起来可选但键盘不可用。修正为真实button和radiogroup语义，不用伪装div。

### 修正后的最终设计

1. 作者意见强度使用四个可见按钮，点击只改变本地选择状态。
2. `AuthorPresentationGate` 只作用于即将显示的副本。
3. interface/progress/error执行大白话翻译、技术字段过滤和安全兜底。
4. story保护梗概、章纲叙述、正文和作者原话，只清除机器协议与异常载荷。
5. 任何无法安全显示的动态内容不回退原值，显示自然语言占位或恢复动作。

### 创造性与输出质量保护

门禁不进入模型提示词、ContextCompiler或正式对象，不改变AI创作自由区。故事文本逐字保护；技术信息只在作者展示副本中隐藏，后台事实与审计原件完整保留。

### 测试与证据等级

E0为作者截图与决定；E1为控件和门禁代码；E2为交互、技术泄漏、语义保护、类型、全量测试、构建和运行证据；E3/E4仍需真实作者持续使用，不由本次工程测试冒充。

- 本轮E2证据：门禁与体验16个测试文件、71项测试通过；项目全量148个测试文件、555项测试通过；Contracts、API、Worker、Web类型检查与生产构建通过；当前文档中心同步和一致性检查通过。

### 剩余风险与未知项

剩余风险是未来新增页面绕过统一展示函数，或真实模型产生未收录的新技术术语。通过通用结构化展示边界、源代码泄漏扫描和持续补充回归样本降低风险。

### 停止、回滚和升级条件

再次出现选择跳页、白屏、滚动错位、原始机器值、路径、任务编号外露，或梗概/正文/作者原话被展示门禁改写时立即停止发布。若需要修改业务数据或模型输出合同，升级为新的架构审查，不在展示门禁中顺带改动。

## 18. DR-20260810-KIMI-K3-RETIREMENT-AND-200-CHAPTER-AUDIT

- `design_review_id`: `DR-20260810-KIMI-K3-RETIREMENT-AND-200-CHAPTER-AUDIT`
- 问题与根因：老板确认Kimi K3消耗量过大，要求立即暂停并全部替换为Kimi 2.6或2.7。代码审计发现K3同时存在于运行时主编默认、十一人团队主编与备用写手、写手允许名单、旧K2.6别名升级和测试断言；只改一个默认值会留下可恢复或可重新激活的K3路径。
- 已确认事实：火山方舟当前官方套餐页列出Kimi K2.7；本项目已有`kimi-k2.7-code`请求兼容与响应测试。当前本机运行在确定性模式，Agent Plan凭证未配置；活动书及其活动绑定均不含K3。数据库保留东方仙侠和游戏电竞数据流两本新测试书各100章，以及更早的确定性测试书；历史证据不回写。
- 作者决定与偏好：暂停全部K3调用；用Kimi 2.6或2.7替换；随后保留两本各100章、不同题材和多标签的完整流程测试数据，并逐项检查按钮、自然语言展示、规划链、资料库和跨书准确性。
- 推断与未知：根据当前官方支持与本项目既有适配证据，优先使用Kimi K2.7 Code而不是重新启用已退役别名中的K2.6 ModelHub。没有套餐凭证时无法形成真实Kimi文学质量证据；确定性200章只能证明工程链、对象绑定、隔离和展示，不得冒充E3/E4真实模型质量。
- 适用Skill：`wenmi-longform-quality`。模板、审查和纵向检查保护创造性，不把100章目标变成每章固定爽点公式。
- 选择机制：默认和团队K3统一迁移为Kimi K2.7 Code；旧`kimi-k3`与`kimi-k2-6-modelhub`配置值只允许向K2.7升级解析；设定/事实席使用GLM 5.2，保持主笔或备用写手与事实、文学、体验四异模型；历史快照和完成调用不回写。
- 最强替代方案：把主编改为K2.7，但保留备用写手K3或允许从历史修订恢复K3。该方案不能实现“暂停全部使用”，也会把高消耗路径藏进故障接管，因此否决。
- 第一轮审查：验证默认配置、团队模板、环境别名、允许名单、公开候选和所有活动绑定均不含K3；验证两位编剧异模型、单一活动写手和三席独立审查；无凭证时不得真调用或按量回退。
- 第二轮审查：用东方仙侠与游戏电竞数据流两本书做跨题材纵向验收，逐级验证开书、设定、分卷、事件链、事件大纲、章纲、正文、章节/事件/卷结算、资料库、检索来源与作者展示。检查人物可辨识度、因果自然度、信息漂移和模板化重复，但不把软节奏变成硬门禁。
- 证据与停止条件：先通过K2.7小样本连接与输出验证才允许扩大真实调用；任何凭证缺失、模型名不受套餐支持、K3仍可激活、异模型冲突、跨书污染、无章纲正文、技术信息外露、乱码或工程门禁失败，立即停止扩大并修复。没有凭证时保留确定性验收结果并明确证据上限。
- 回滚：代码与规格可通过Git结算回退；新书和正文数据按老板要求保留。回滚不得重新启用K3，也不得删除历史模型调用证据。

### 当前E2执行证据

两本测试书分别完成1卷、10事件、100章纲、100章正文、300份审查、100次章节结算、10次事件结算和1次卷结算。审计发现初版确定性事实提取仍硬编码旧样稿“林澈/铜钥匙”，导致两本书虽有全文切片和情绪/钩子投影，但实体、事实、关系和时间线均为0；停止验收后已改为题材无关的正文证据提取，并在数据库副本验证幂等后，以正史纠错修订补建保留数据，正文不改写。

该轮复核逐条检查218条正式事实原句，跨书污染、内容偏移、乱码和技术词泄漏均为0；两书分别形成6个核心人物实体，仙侠书110条事实/5条关系/110条时间投影，电竞书108条事实/3条关系/108条时间投影。全量148个测试文件、555项测试、类型、四端生产构建、42版幂等迁移、真实本地启动、17项隔离与恢复专项测试以及含200个不可变正文文件的隔离恢复备份均通过。但该轮只证明了资料总量，没有证明势力、地点、道具资源和事件时间线逐分类可用，相关分类完成结论已撤销并由第19节重新验收。真实Kimi K2.7仍因未配置Agent Plan凭证停在连接门禁，不以确定性结果冒充E3。

## 19. DR-20260811-LIBRARY-CATEGORY-COVERAGE

- `design_review_id`: `DR-20260811-LIBRARY-CATEGORY-COVERAGE`
- 问题与根因：老板在保留测试书中发现势力、地点地图、道具资源和事件时间线为空。复核确认此前验收只统计实体、事实和时间线总量；两书实体实际全部为人物。前端又把事件时间线错误实现为筛选`event`实体，接口没有返回已经存在的`timeline_projection`，因此总数通过仍会出现多个空页。
- 事实、推断与未知：两书均有已确认的世界、规则、资源设定和100章定稿正文；当前正史各有100条以上时间投影，均可回查来源。未知的是未来真实模型对所有新题材的分类召回率，不能由两本确定性样稿外推为E3/E4。
- 作者决定与偏好：检查并修复全部分类功能；信息必须来自本书已确认设定或定稿正文，页面使用自然语言，不显示技术字段，也不能用占位资料掩盖空白。
- 适用Skill：`wenmi-longform-quality`；SHA-256 `e886eaed0d2f0c8a19f4d197c368fe28173c6e977d7b126e027aba0d22b1e217`。修复保护创造性：事实席只提取正文实际存在且影响后续的资料，不要求每章或每本机械凑齐分类。
- 选择机制：分类页合并两类权威来源——已确认设定用于说明世界、势力、地点、资源和规则边界；正文结算实体与事实用于说明已经实际出现的对象；事件时间线直接读取当前正史时间投影。两类来源明确标注，规划不进入正史。
- 最强替代方案：自动为每个空页生成一批看似完整的势力、地图点、道具和事件。该方案会把AI猜测或规划冒充正史并污染后续检索，因此否决。
- 第一轮审查：接口按owner/book/current canon revision隔离返回时间线；分类设定映射使用稳定项目键与中文语义，不修改设定原文；正文实体必须绑定原文证据与不可变稿件版本。
- 第二轮审查：用东方仙侠与游戏电竞两本100章书逐项点击人物、势力、地点、道具资源、事件时间线、规则和来源，分别核对类别非假空白、正文证据、跨书污染、自然语言和无技术字段；总数不再替代逐分类验收。
- 证据与停止条件：任一有来源分类仍为空、时间线不是当前修订、证据无法逐字回查、规划冒充正史、跨书结果、乱码/技术字段或幂等失败均立即停止。类型、全量测试、构建、迁移、运行、隔离/恢复和文档同步全部通过后才能结算。
- 回滚：回退接口字段、前端分类适配、事实提示与纠错脚本；不回滚已合并迁移，不删除书籍、正文、原设定、旧事实或审计记录。纠错修订保留可追溯来源。

### 本轮E2执行证据

- 《阵骨问天》正史修订102：人物6、势力1、地点28、道具1、资源1、正式事实141、关系5、事件时间线105；141条事实逐字回查正文通过。
- 《零帧登顶》正史修订102：人物6、势力1、地点10、道具1、资源1、正式事实121、关系3、事件时间线105；121条事实逐字回查正文通过。
- 两书跨书污染为0，纠错脚本重复执行不产生新修订；组件测试逐项点击资料库12个页签，真实接口返回对应分类与自然语言来源。
- 全量148个测试文件、555项测试、类型检查、四端生产构建、42版幂等迁移、真实本地运行和文档中心检查通过。
- 修复后备份`backup-2026-08-10T17-31-13-453Z-96a92ce0`包含200份不可变正文文件，正式库完整性、外键和隔离恢复验证全部通过。
- 当前证据等级仍为E2；未来真实模型对新题材的分类召回率和长期资料质量仍需E3/E4验证。

## 20. DR-20260811-LIBRARY-SEMANTIC-VIEWS

- `design_review_id`: `DR-20260811-LIBRARY-SEMANTIC-VIEWS`
- 问题与根因：资料库把作者确认的宽泛设定按关键词重复铺进势力、地点、道具和规则页；人物页一次展开全部逐章行动造成高空白；事件时间线把每个人物每章参与记录当成事件；主角面板只显示当前值而不展示获得、失去和历史。这些不是单个CSS缺陷，而是把设定来源、正式事实、人物状态和实际事件四类语义混成一个通用卡片集合。
- 已确认事实：保留两本百章书具有正式人物、势力、地点、道具/资源、章节结算、10个已结算规划事件和可回查正文；现有`character`稳定类型同时包含主角与配角；主角状态表已支持追加版本、状态、发生章节和故事时间，但公开面板没有完整使用。正文事实中的`event.chapter_N`是人物出场事实，不是事件摘要。
- 作者决定与偏好：角色改为配角；配角默认只显示姓名和一条出场，其余按需展开；分类页去掉不明所以的重复设定；时间线表达正文事件；主角面板记录境界、属性、道具装备以及获得/失去章节。
- 推断与未知：事件蓝图适合作为事件归属和计划—实际对照，但不能充当实际结果。若正文没有明确日期，显示“书内时间未注明”比虚构年月更可靠。未来真实模型能提取多少细粒度人物状态仍需E3/E4验证，不能用本次界面重组冒充。
- 适用Skill：`wenmi-longform-quality`；本轮同步增加资料库语义分层、人物状态史与实际时间线门禁。最终SHA-256在本轮验证证据中登记。
- 选择机制：`settings`只在设定来源页；API确定性返回排除主角的`supportingCharacters`；正式分类只读实体/事实；生效规则只读`world_rule`；配角使用折叠详情；主角看板返回当前、待确认和完整历史；时间线按已结算`story_events + event_chapter_outlines + stage_settlements`聚合，规划标题只标注所属规划，结算状态与章节范围证明实际发生。
- 最强替代方案：继续使用通用实体卡，只缩短高度并把“已确认设定”改名。该方案仍会混淆来源和事实，人物出场仍冒充事件，检索与作者理解继续偏移，因此否决。
- 第一轮审查：验证同书主角排除、配角展开、四个分类无重复设定、规则来源、事件与结算一一覆盖、正文时间不编造、状态历史追加、owner/book隔离和来源回查。
- 第二轮审查：不强迫每个角色拥有固定属性模板，也不要求每章产生状态或爽点；只在正文明确时沉淀，保护题材自由、人物留白和合理惊喜。规划与正文差异保持可见，不自动改写规划或正文。
- 证据与停止条件：组件交互、API合同、两书真实数据审计、跨书污染、类型、全量测试、构建、幂等迁移、备份恢复、真实运行和文档同步全部通过后结算。若主角仍混入配角、分类仍重复设定、时间线仍按人物刷条数、虚构日期、状态历史丢失或任一来源不可回查，立即停止。
- 回滚：前后端、规格与Skill通过本次Git结算回退；不修改已合并迁移，不删除旧事实、正文、书籍、备份或历史审计。旧`character`键与事实来源保留兼容。

### 本轮E2执行证据

- 当前长篇质量Skill SHA-256：`ff9a5edd90f296f5c948dd289c26603f3c43ba30905647f17944d7fced83b021`；官方Skill结构校验和本项目审查记录校验均通过。
- 组件逐项打开12个资料分类，验证配角折叠、主角排除、势力/地点/道具无重复设定、生效规则、正文事件时间线、内容来源与全部相关操作；接口验证主配角隔离及完整状态历史。
- 真实事件结算测试验证`eventTitle`、章节范围、实际结算摘要和未知故事时间；不再读取人物`event.chapter_N`出场投影充当事件。
- 《阵骨问天》：6个人物实体、5名配角、1个势力、28个地点、2个道具/资源、10个已结算事件、4条生效规则；主角混入0，人物行动冒充事件0。
- 《零帧登顶》：6个人物实体、5名配角、1个势力、10个地点、2个道具/资源、10个已结算事件、6条生效规则；主角混入0，人物行动冒充事件0。
- 全量148个测试文件、557项测试、类型检查、Contracts/API/Worker/Web生产构建通过；五个验收测试文件7项通过。
- 42版迁移连续执行两次均无新增迁移；备份`backup-2026-08-10T19-20-48-149Z-3f581430`含200份不可变正文文件，正式库完整性、外键和隔离恢复验证通过。
- 本机服务健康且两本保留书的新资料库接口可用；当前证据等级为E2，真实模型长期人物状态提取质量仍由E3/E4持续使用验证。

## 21. DR-20260811-LIBRARY-SEMANTIC-PROFILES

- `design_review_id`: `DR-20260811-LIBRARY-SEMANTIC-PROFILES`
- 问题与根因：上一轮已经分离设定来源、主角、配角和实际事件，但配角仍只有少量通用事实，势力/地点/道具继续共享通用实体卡；地点与地图在同一页；时间线把结算摘要、规划标题和来源同时铺开。根因是接口只返回原始实体/事实，页面自行猜分类，没有类型化公开视图和最小作者展示合同。
- 已确认事实：现有实体、事实、人物状态、事件结算、来源和开书初始地图都按书保存且可回查；两本保留书有100章不可变正文。现有正文并不保证写明每个配角年龄、每个势力人数或每个道具属性，缺失必须保持未知。
- 作者决定与偏好：配角应查看年龄、性格、门派、装备等有依据资料；势力查看人数、实力、等级、场地和地位；地点与地图分开，地图从出生点按故事行进连接大地点；道具查看属性、归属和流转；事件时间线只保留事件与时间，没日期就用章节；项目文档逐项解释前端、后台、AI、检索和上下文。
- 推断与未知：类型化字段能提高查询和阅读清晰度，但真实模型对不同题材字段的召回率仍需长期E3/E4验证。地图节点能证明故事到达先后，不能自动证明现实距离或方向。
- 适用Skill：`wenmi-longform-quality`；本轮同步更新主文件、上下文/RAG、验收和工作流参考，明确档案是可选语义和动态检索入口，不是创作公式或固定岗位包。
- 选择机制：API从本书权威实体/事实重建配角、势力、地点、道具资源档案，保留字段来源并对空字段返回空数组；明确所属/持有关系可反向形成成员或归属记录。地图只取正文已出现的大范围地点，优先明确出生地，否则以首个正式场景为起点，按章节先后连线；方位无证据为空。时间线后台保留结算证据，作者层只显示`display_time + event_title`。
- 最强替代方案：让AI按固定人物卡、势力卡和道具面板自动补齐所有空字段，并生成完整坐标地图。该方案会把推断冒充正史、扩大上下文并限制题材创造性，因此否决。
- 第一轮审查：检查类型字段与事实键语义、来源章节、反向成员/归属、主角排除、地点层级、出生地判定、章节路线、故事时间回退、owner/book隔离和无来源空值。聚焦测试已发现并修复“成员数误入成员名单”和“空出生地字段误判所有地点”两项边界错误。
- 第二轮审查：检查字段模板是否迫使AI凑齐、路线是否制造地理关系、时间线精简是否丢失后台回查、页面是否重复出场来源、检索是否把全档案固定塞给岗位。最终方案保留全部审计详情，只收敛作者默认视图。
- 证据与停止条件：类型/组件/API/两书真实数据/跨书隔离/全量测试/构建/迁移/运行/备份恢复/Skill校验/文档同步全部通过后才结算。任一虚构字段、错书值、地图假方向、主角混入配角、时间线重新显示技术长文、上游版本失效或来源不可回查都立即停止。
- 回滚：通过本轮Git提交回退代码、规格、Skill和公开视图；不回滚已合并迁移，不删除书籍、正文、事实、状态、来源、备份或旧审计证据。
- 执行证据：
  - 类型化聚合、资料库13个分类页、配角展开、世界路线和简洁时间线的组件/API/单元针对性测试共3个文件10项通过；长篇质量Skill结构校验与本设计审计校验通过。
  - 全量149个测试文件、559项测试、全项目类型检查、Contracts/API/Worker/Web生产构建和35份当前文档一致性检查通过。
  - 两次42版迁移均无新增迁移；备份`backup-2026-08-10T21-48-26-543Z-20effb57`包含200份不可变正文文件，正式库完整性、外键和隔离恢复通过。
  - 《阵骨问天》返回5名配角、1个势力、28个地点、2个道具/资源、8个路线节点和10个事件；《零帧登顶》返回5名配角、1个势力、10个地点、2个道具/资源、8个路线节点和10个事件。
  - 旧测试正文没有明确写明的年龄、性格、势力人数和道具属性保持未知；没有用AI猜测补齐。地图方位无来源时为空，事件时间缺失时只回退为章节范围。

## 22. DR-20260811-STRUCTURED-GENRE-200-CHAPTER-AUDIT

- `design_review_id`: `DR-20260811-STRUCTURED-GENRE-200-CHAPTER-AUDIT`
- 问题与可观察症状：现有两本百章书证明了通用卷—事件—章链和类型化资料页，但没有覆盖“人物与灵宠双属性面板”以及“领地—资源比例—建筑升级—武将面板”这两类强结构题材。只复用旧仙侠或电竞文本会让资料库看似有总量、却无法证明数值、归属、获得、消耗与升级机制能被正文、结算、检索和作者界面共同承载。
- 已确认事实：当前Schema允许`character/location/organization/item/resource/skill/stat_panel/world_rule/event`正式实体；事实席支持题材生成的关系键和主角动态状态；资料库类型化视图按来源字段聚合，未知字段保持空。当前本机没有真实Agent Plan凭证，因此本轮只能形成确定性E2工程证据。
- 作者决定与偏好：保留一部游戏仙侠异界和一部领主经营新书各100章；逐项测试全部功能与按钮；资料库必须能供AI成员检索；前端只显示自然语言；完成后保留数据并关机。
- 推断与未知：高置信度认为两类题材能暴露现有通用事实提取、主角状态和资料档案的边界。真实Kimi K2.7对百章文学质量、数值提取召回率和作者长期满意度仍未知，不用确定性夹具冒充E3/E4。
- 适用Skill：`wenmi-longform-quality`。面板字段只在正文确有需要时出现，不把固定属性表注入全部创作任务，也不把数值一致性审计变成限制合理惊喜的写作公式。
- 选择机制：扩展当前可恢复百章验收工具和题材无关事实合同，为两类场景提供独立人物、地点、势力、面板、资源和十事件因果；正文显式写出关键值和变动，事实席只从逐字证据沉淀。再用类型化资料接口、ContextCompiler来源、全文/关系/时间检索与作者展示逐项复核。
- 最强替代方案：测试结束后直接向正式事实表批量插入完整面板和资源卡。该方案速度快，但会绕过正文、审查、章节结算和来源门禁，无法证明产品真实流程，因此否决。
- 第一轮审查：检查全部上游版本绑定、逐章不可变正文、事实原句、资源守恒、升级代价、主配角/灵宠区分、规划与正史分离、跨书隔离、检索来源、取消重试恢复、迁移和备份。
- 第二轮审查：检查两书题材差异、人物与灵宠/武将主动性、十事件因果、章节钩子、机械重复、面板是否挤占叙事、作者修改成本和自然语言展示；一致性提升不得以人物工具化或每章强制报表为代价。
- 证据与停止条件：任一无章纲正文、错版提交、跨书事实、无法逐字回查的数值、资源账不平、规划冒充正史、灵宠/武将工具化、技术信息外露、乱码或工程门禁失败，立即停止并修复后续跑。两书数据、全量测试、构建、迁移、运行、隔离、检索、备份恢复、文档同步和Git结算全部通过后才结束。
- 回滚：代码、验收规格和夹具通过本轮Git提交回退；已合并迁移不回滚，两本测试书、不可变正文、正式事实、调用和审计证据按作者要求保留，发现错误使用前向纠错修订而非覆盖历史。


### 两本结构化题材测试书的当前执行证据

- 保留《灵契天墟》6ea9d46d-2d60-40fe-8e1a-261d1db1f17b与《灰烬领主》1d3246b9-8e25-4829-b50f-b507d3681c5c，每本均完成1卷、10事件、100章纲、100章正文、300份三席审查和111次三级结算，当前均为正史101版。
- 审查中先后发现并修复：测试题材标签不在当前目录、任务租约短于百章步骤、投影长事务阻塞、只读分卷接口无条件开启写事务、启动冷启动误报、段内跨句人物关系漏提取，以及同一人物多个同类关系被误判冲突。所有修复均有回归测试。
- 《灵契天墟》形成23个实体、1105条正式事实、5条关系；《灰烬领主》形成28个实体、1422条正式事实、5条关系。2527条事实逐字回查不可变正文，1280个上下文包跨书污染为0。
- 游戏仙侠100章人物和灵宠面板连续，技能与装备无提前获得；领主经营100章资源账连续、无负数，产出损耗、武将面板、建筑面板和6次升级均有正文来源。
- 全量150个测试文件、563项测试、类型检查、四端构建、42版幂等迁移、真实本地健康、混合检索金标与隔离恢复备份通过。最终审计保存在data/verification/structured-genre-two-books-200-chapters/final-audit.json。
- 浏览器控制因本机Windows隔离组件无法建立连接；按停止条件没有虚构点击证据，改用组件、接口和全量交互测试并明确E2边界。真实Kimi K2.7百章文学质量、人工视觉和长期作者体验仍为E3/E4未知。

## 23. DR-20260811-UNIFIED-ACCOUNT-ACCESS

- `design_review_id`: `DR-20260811-UNIFIED-ACCOUNT-ACCESS`
- 问题与根因：旧实现以固定本机owner和每次启动重建的临时页面会话保护回环服务，无法让老板和其他用户拥有稳定账号，也无法形成用户管理后台。若另外开发“本机免登录版”，会造成两套身份、两套owner来源和不同测试路径。
- 已确认事实：当前书籍和核心对象已经携带 `owner_id`；Fastify、SQLite和React可承载初始账号体系；老板明确要求本机自己使用时也要登录，旧测试书不重要；手机号和微信需要外部供应商、资质与真实凭证。
- 作者决定与偏好：采用最快可用的邮箱密码注册登录；老板也作为普通产品用户登录；不保留单用户旁路；需要用户管理后台。
- 推断与未知：邮箱密码可以最快形成一致身份边界，但公开运营仍需要验证、找回、限流、政策和生产部署。真实用户规模、邮件供应商、短信渠道和微信开放平台资质尚未知。
- 方案A（选中）：统一邮箱密码账号，首个注册者为管理员，所有owner从会话派生。优点是只有一条产品路径、可本机立即测试；代价是公开前还需补运营能力。
- 方案B（否决）：保留本机免登录并另建公网账号。短期界面简单，但会制造权限旁路、测试盲区和数据迁移歧义。
- 方案C（暂缓）：直接以手机号或微信为首发。用户体验更熟悉，但依赖费用、资质、回调域名和外部凭证，无法成为当前最快路径。
- 第一轮审查：核对注册并发、邮箱唯一、密码摘要、会话明文边界、Host/Origin/跨站写入、重启持久、退出撤销、暂停撤销、管理员自锁和API错误语义。
- 第二轮审查：核对所有领域路由不再使用固定owner、双用户书籍零串线、管理员普通业务仍只见自己的书、登录前不渲染创作数据、前端只显示自然中文、手机号/微信以后不会建立第二套身份。
- 创作质量影响：账号层只改变访问范围，不改变11名成员、ContextCompiler、检索内容、创作模式或质量门禁；更严格的owner来源减少跨用户内容污染。
- 证据与完成边界：类型、迁移、账号安全、请求策略、双用户隔离、工作台组件、全量测试、构建、文档同步和Git结算全部通过后，统一账号核心可完成。邮箱验证/找回、限流、HTTPS与合规未完成前，不宣称公开运营完成。
- 停止与回滚：出现免登录旁路、明文密码/令牌落库、跨用户可见、普通用户进入管理员接口、最后管理员可被停用或业务路由仍取固定owner时立即停止。代码可通过本次Git提交回滚；已合并迁移不删除，使用向前迁移修正，不删除用户书籍。

## 24. DR-20260811-FIRST-ADMIN-LOCAL-DATA-ADOPTION

- `design_review_id`: `DR-20260811-FIRST-ADMIN-LOCAL-DATA-ADOPTION`
- 问题与根因：账号系统上线后，首位管理员被创建到新的空owner，而8本既有本机书仍留在历史 `owner-local-boss` 下，造成管理统计看见书但管理员书架为空。最新作者决定明确推翻“旧书不接管”的旧偏好。
- 已确认事实：当前数据库只有1个活动管理员；该账号owner除账号和owner自身记录外没有创作数据；历史本机owner有8本书及完整关联对象，且未绑定任何账号。会话每次通过 `user_accounts` 读取owner，因此切换账号关联后现有会话自然使用新归属。
- 作者决定与偏好：老板就是首位注册者、管理员和本机测试者；全部本机数据归该账号；其他用户数据完全独立。
- 推断与未知：复用owner比跨数百张表搬运数据更可靠。未来自定义 `WENMI_OWNER_ID` 可由首次注册事务识别；历史已升级库的44版迁移只处理默认本机owner和唯一空管理员。
- 适用Skill：`wenmi-longform-quality`，指纹 `91dc6d1a715de8cad741fe8affbe914872f4a514ad094f3d8e21d9beaf5ff2c5`。本变更只修正owner访问边界，不改变11名成员、创作流程、上下文内容或质量公式。
- 方案A（选中）：首次注册事务复用未认领且含书的历史owner；已有唯一空管理员由带严格条件的向前迁移重新绑定。
- 最强替代方案（否决）：逐表复制或批量改写所有 `owner_id`。这容易遗漏附件、不可变正文、任务、上下文、检索和未来新表，并破坏来源与恢复链。
- 第一轮审查：核对唯一账号、管理员角色、历史owner存在且未认领、当前owner无书、会话继续有效、管理员可见8本、第二用户owner不同且书架为0。
- 第二轮审查：确认不改写书籍、正文、资料库、任务、附件、上下文、检索、版本或AI成员绑定；管理后台仍只统计已绑定账号数据，管理员普通业务不能看其他用户书。
- 证据与停止条件：出现多账号迁移、重复owner、旧书减少、跨用户命中、会话失效、迁移非幂等或完整性/外键失败时立即停止。类型、定向测试、全量测试、构建、44版迁移、真实库审计、启动和文档同步全部通过后完成。
- 回滚：代码通过Git恢复；已合并迁移不删除或逆向执行，如需修正只新增前向迁移。任何情况下都不删除或覆盖作者数据。
## 25. DR-20260811-STORY-FIRST-EVENT-PLANNING

- design_review_id: DR-20260811-STORY-FIRST-EVENT-PLANNING
- 问题与根因：事件领域模型已经包含进入状态、触发、阻力、选择代价、结果、人物变化和下一接口，但作者界面直接展示字段表和专业模板来源，像工作流配置单而不是故事设计工具；只做术语过滤也无法让摘要形成情绪和悬念。
- 已确认事实：StoryEventContent已经覆盖故事预告需要的全部权威字段；模板选择支持不用、自定义和多选；活动事件版本与上一事件结算引用能够区分计划和实际；本次不需要数据库迁移。
- 作者决定与偏好：参考外部故事化建议并立即修改，完成后由作者亲自测试；界面要让作者感觉在设计故事，同时保留完整编辑能力并保护AI创造性。
- 推断与未知：故事视图预计降低理解成本，但真实作者效率和真实模型文学质量需要E3/E4观察；当前组件与确定性流程只能形成E2证据。
- 适用Skill：wenmi-longform-quality，SHA-256 91dc6d1a715de8cad741fe8affbe914872f4a514ad094f3d8e21d9beaf5ff2c5。
- 选择机制：前端从当前正式字段即时构建故事预告，默认故事视图并保留细节视图；事件链显示本卷开场、已发生或预计承接，并用“因为……所以……”连接。事件模板在作者层映射为可选阅读感受，普通界面隐藏专业来源。生成端增加具体、主动、有困境的表达要求，同时明确禁止固定悬念句式。
- 最强替代方案：新增持久event_blurb和数值张力曲线。它能减少前端即时组合，但会制造第二份可能过期的事件真相，并用虚假精度把软节奏变成硬指标，因此否决。
- 第一轮审查：验证无Schema变化、正式结构字段和作者编辑无损、模板键/版本/哈希继续保存、故事与细节读取同一版本、计划/实际标记来自真实结算引用、活动和完成态都可查看。
- 第二轮审查：验证阅读感受可不选/混合/自定义，不把模板变成配方；生成提示不要求固定“但是/没想到”；故事投影不补造设定、不限制场景对白和局部解法。
- 证据与停止条件：故事/细节切换、因果连接、实际/预计承接、专业来源隐藏、作者编辑和生成提示均有测试；类型、全量测试、四端构建和文档同步全部通过后才结算。出现字段不可编辑、第二套真相、计划冒充正史、固定公式、技术信息外露或任一门禁失败时立即停止并回滚表现层。
- 回滚：通过本轮Git差异回退组件、样式、提示和规格；不回滚迁移，不删除或改写书籍、事件版本、正文、结算、上下文、模型调用和历史证据。
- 执行证据：定向2个文件4项测试通过；整仓四部分类型检查、151个测试文件/566项测试、Contracts/API/Worker/Web生产构建和35份当前文档同步检查通过。未新增数据库迁移，既有事件、正文和结算数据未改写。


## 26. DR-20260811-CHAPTER-CHALLENGER-AND-PIPELINE-BOUNDARIES

- `design_review_id`: `DR-20260811-CHAPTER-CHALLENGER-AND-PIPELINE-BOUNDARIES`
- 问题与根因：卷纲和事件大纲有双编剧独立方案，但事件章链和单章章纲只有一个指定编剧，作者在关键细化点缺少第二视角；同时副编与文姬的资料/判断边界、正文硬检查归属、技术失败接管和字数目标在手册与代码中存在模糊或旧值残留。
- 已确认事实：章链与单章版本、任务检查点、ContextPack和模型调用已经可追溯；任务类型是开放文本，不需要新增Schema。正文流水线已有同一模型2次技术尝试、结果未知不盲重试、副笔最多接管1次和最多2轮硬检查修复，但`targetMet`仍使用旧2500—3500目标，通用提示也有旧值。
- 作者决定与偏好：采纳DeepSeek建议并立即修改；保留主方案效率，只在作者需要时让另一编剧挑战关键节点；规则不能压制AI创造性，也不能把挑战变成自动公式或第二套正式方案。
- 事实/推断/偏好/未知：事实为现有版本、调用与恢复边界；推断为轻量挑战能以较小成本提升关键节点选择质量；偏好为默认不触发、最多3条；真实作者采用率和文学收益仍是E3/E4未知。
- 方案A（否决）：章链与每章都生成两份完整方案再由主编融合。覆盖最全，但成本、等待和比较负担翻倍，并容易把细化工作公式化。
- 方案B（选中）：一份正式主方案＋作者主动触发的一次单向挑战。挑战者只看冻结上游、精确主方案版本和作者关注点，只提关键替代意见，不自动融合。
- 方案C（否决）：维持单方案且没有第二视角。实现最简单，但不能解决作者在关键转折和钩子处无法比较的问题。
- 第一轮审查：核对owner/book隔离、活动卷/事件/章链版本、目标候选指纹、主编剧与挑战者异模型、幂等键、取消/重试、结果未知、换版失效、任务检查点审计和无Schema迁移。挑战结果不得调用正式章纲保存或确认接口。
- 第二轮审查：核对默认不触发、最多3条、只挑战关键冲突/选择/转折/钩子、每条说明收益/代价/后续影响、无自动融合、无固定叙事公式、作者可忽略、前端自然语言且不暴露内部任务字段。
- 正文边界审查：硬检查必须早于三审并由代码执行；目标2700—3200、硬范围2350—3650；最多2轮定点修复。主笔最多2次明确技术尝试，供应商结果未知不盲重试；副笔在两次明确失败后最多接管1次，文风不满意不触发技术接管。
- 证据与停止条件：合同、服务、管线、HTTP、Worker、前端、确定性模型和针对性测试完成后，再通过类型、全量测试、构建、迁移、运行、隔离和文档同步。出现挑战自动写正式对象、同模型冒充第二意见、错版建议继续显示、挑战进入正文资料包、硬检查由模型代替、字数边界失效或风格问题触发副笔时立即停止。
- 回滚：通过本轮Git差异回退挑战表现与任务类型，恢复单主方案；不回滚已合并迁移，不删除书籍、章纲、正文、任务、上下文、调用或审计。硬检查与接管边界修正可独立保留。
- 执行证据：Contracts/API/Worker/Web/测试类型检查通过；定向5个文件28项测试和整仓151个测试文件/566项测试通过；四端生产构建、正式库44版幂等迁移、本机网页200响应和API健康/Worker就绪通过。挑战任务的书籍隔离、异模型成员、精确版本指纹、最多3条建议、不新增正式版本和不混入讨论来源均有自动化证据。浏览器人工视觉连接受桌面沙箱刷新故障阻断，本轮只声明组件级E2，不声明人工视觉E3。

## 27. DR-20260812-GAME-LORD-DOULUO-200

- `design_review_id`: `DR-20260812-GAME-LORD-DOULUO-200`
- 问题与范围：用两本全新、机制差异明显的小说重新验证当前全流程和全部功能。一本是带资源、建筑和武将面板的游戏领主文，一本是斗罗大陆世界观下不复述原作主线的原创支线同人；每本100章并保留全部数据。
- 已确认事实：当前工作流、不可变正文、三审、ContextCompiler、资料投影和账号隔离已存在；本机没有真实外部模型凭证；确定性题材路由此前主要从检索片段判断，标题未稳定进入路由；长流程中投影Worker可能与上下文包落库短暂争用SQLite写锁。
- 作者决定与偏好：重新开始测试，完整覆盖200章和所有功能，发现BUG直接修复；保留两本书、章纲、正文、审查、资料库和检索数据。
- 事实/推断/偏好/未知：事实为对象计数、来源原句、任务和数据库证据；推断为两类差异题材能揭示题材路由与资料抽取偏置；偏好为斗罗同人保持原创支线和角色独立；真实外部模型文学质量、版权风险判断和长期读者体验仍未知，属于E3/E4。
- 方案A（选中）：通过正式API与Worker逐层创建两本书，逐章执行到100章，并用只读审计器检查正式数据库、不可变正文、事实证据、上下文隔离、资料类别和题材专项面板。
- 最强替代方案（否决）：直接向数据库批量插入200章、面板和事实。该方案更快，但会绕过卷—事件—章纲约束、三审、确认、结算、ContextPack和资料提取，无法证明产品真实流程。
- 第一轮审查：核对两书开书分类、11名成员、9项必要设定、卷双方案/融合、10事件双方案/融合、章链、章纲、逐章硬检查、三审、不可变正文、事件/卷结算和任务恢复；正文按当前书标题、人物和题材路由，不允许通用样稿回退。
- 第二轮审查：逐条回查事实原句，检查主角/配角/势力/地点/地图/道具资源/事件时间线/生效规则/来源、关系投影、面板连续性、相邻重复、乱码/技术信息、上下文串书、账号归属、检索金标、备份恢复和前端12入口/资料库13页签交互。
- 缺陷与修正一：游戏领主首轮100章误用通用人物。根因是正文路由只看被压缩的检索来源，未读取当前提示标题与上一状态。修正为当前标题、上一状态和来源共同参与题材路由，并加入空来源题材回归测试；旧失败书保留为失败证据，不计入通过结果。
- 缺陷与修正二：斗罗自定义词“武魂魂技树”被放入正式辅助分类。修正为正式分类键不变，自定义词进入`customTags`，并增加分类库合同回归。
- 缺陷与修正三：第90章附近上下文包落库遇到`database is locked`。根因是本地投影提交恰好占用SQLite写锁。修正为只对明确锁冲突做最多3次短退避，上下文包和同任务附件绑定使用同一有限策略；其他SQL错误立即失败，重试耗尽后保留任务恢复路径。单元测试覆盖瞬时恢复、非锁错误不重试和耗尽停止。
- 创作质量影响：题材路由修复避免错书人物和机制进入正文；正式分类/自定义词分离避免标签污染；有限锁重试只改变本地持久化时序，不改写提示、候选、正文或审查，也不扩大AI创作约束。
- 停止条件：任一书缺少上游活动版本、三审或三级结算，任何事实无原句、跨书/跨owner引用、面板缺章、唐三/小舞混入、资源/魂力状态不可解释、任务残留、测试/构建/迁移/健康/备份/文档失败，立即停止并修复后续跑。
- 执行证据：有效书《界域领主日志》`8dd69a57-bb5f-4370-8bca-8524ab66660b`与《斗罗星轮行》`efe3f031-db23-4353-aa18-d77104684d3c`均完成100章和完整三级结算。双书共51个实体、2522条可逐字回查事实、5类证据型关系、1274个上下文包、975条检索记录，跨书污染为0。前端重点10个文件37项、全量152个文件573项、四部分类型、四端构建、44版双次迁移、本机健康、检索金标和980文件隔离恢复备份通过。隔离验收后按老板要求将两本有效书完整转入首位管理员书架：迁移108类当前业务表439258条记录及4个向量表8384条向量记录，迁移前备份可恢复，迁移后完整性、外键、管理员归属与双书专项审计再次通过；失败样本不转入管理员书架。
- 证据等级与局限：当前为E2。真实外部模型凭证缺失，浏览器控制又因Windows隔离组件故障无法连接；没有虚构人工视觉或真实模型点击/文学质量证据。后续真实模型盲评与作者长期使用属于E3/E4。
- 回滚：代码可通过本轮Git提交回滚；没有新增或修改已合并迁移。测试书、不可变正文、事实、上下文和备份不删除；有限重试若需调整，只新增代码与测试，不逆向改写作者数据。