# 顶部导航聚焦复原开发验收清单

> 建立日期：2026-08-23
> 当前状态：已完成并上线
> 本批只修改顶部导航栏排版与开发规则；其他页面、功能模块、数据、接口和交互一律不动。

## 0. 范围锁定

- [x] 0.1 以老板截图和“恢复旧版均匀间距”为唯一 UI 目标，只处理顶部导航栏的宽度分配、间距与对齐。
- [x] 0.2 保持现有 11 个导航入口的名称、图标、顺序、显隐、点击行为和红点状态不变。
- [x] 0.3 保持作者页面内容、左侧书架、归档入口、对话框、后台、API、Worker、数据库和 AI 流程零改动。
- [x] 0.4 手机继续保持顶部两排导航，不增加、删除或重排入口。

## 1. 旧版本与远程仓库对照

- [x] 1.1 获取 GitHub 远程分支最新引用，只读对照远程历史，不用记忆重写已有实现。
- [x] 1.2 对照当前版本、上一生产版本和旧版稳定实现的顶部导航 DOM/CSS，找出均匀排版的最小可复用规则。
- [x] 1.3 记录采用的旧版本来源、原规则和当前冲突规则；不复制无关页面或整段旧样式。

## 2. 开发规则与流程

- [x] 2.1 在 `AGENTS.md` 增加：老板提出明确修改意见时，只聚焦明确问题；未经要求不得连带调整无关功能模块、页面或视觉区域。
- [x] 2.2 在 `docs/DEVELOPMENT_WORKFLOW.md` 增加同一范围守恒门禁，并要求清单明确列出“不改范围”。
- [x] 2.3 两处规则同时增加：写代码前先思考最小实现，能少写不多写，不为局部修改引入额外抽象或重构。
- [x] 2.4 两处规则同时增加：修改已有功能前先查当前代码、本地 Git 历史和远程仓库；旧版已有成熟实现时优先最小复原或复用，不重新发明，不影响其他功能。

## 3. 顶部导航最小复原

- [x] 3.1 只修改顶部导航直接相关 CSS；优先采用旧版稳定规则，删除或覆盖造成主流程过宽、工具区拥挤的当前冲突声明。
- [x] 3.2 桌面端导航在可用宽度内均匀分布，首尾留白与入口间距稳定，不出现前松后挤。
- [x] 3.3 当前激活态、禁用态、悬停态、任务红点、键盘焦点和点击行为保持不变。
- [x] 3.4 360/390/430px 手机仍为原有两排，全部入口可见、可点且无横向溢出。

## 4. 回归门禁

- [x] 4.1 增加顶部导航范围回归测试，锁定 11 个入口、原顺序、桌面均匀布局规则和手机两排规则。
- [x] 4.2 运行相关测试、Web 类型检查和 Web 生产构建。
- [x] 4.3 真实浏览器检查 1366/1440/1920px 桌面和 360/390/430px 手机；只评估导航间距、两排结构、可见性、点击命中和横向溢出。
- [x] 4.4 `git diff` 证明产品代码只涉及顶部导航 CSS和对应测试；规则文档只增加本次要求，不夹带其他修改。

## 5. 提交与发布

- [x] 5.1 清单逐项核查通过，更新 `HANDOFF.md` 并提交；不纳入备份、视觉产物和临时数据。
- [x] 5.2 按 Web-only 流程在公网不可见目录预检、保留旧哈希资源并原子切换入口，不重启 API/Worker。
- [x] 5.3 生产核查首页、新哈希资源、目标导航、手机两排、API/Worker/Caddy 状态和错误日志后报备。

## 6. 对照与验收证据

- GitHub 远程来源：`origin/codex/desktop-entry` 的旧版导航为 8 个主入口、4 个工具入口，容器比例 `2:1`，每个按钮实际等宽。
- 当前冲突来源：提交 `f324dfe` 把入口改为 5+6 后仍覆盖为 `2:1`，造成前五项过宽、后六项拥挤。
- 最小复原：保留旧版“两组按入口数量分配宽度”的结构，仅把两个 `flex-grow` 数值改为当前入口数量 `5:6`；没有修改导航 DOM、入口、图标、顺序或行为。
- 自动验证：顶部导航回归测试 2/2 通过，Web 类型检查与生产构建通过。
- 浏览器验证：1366/1440/1920px 的按钮宽度最大误差 0.156px、中心间距最大误差 0.141px；360/390/430px 均为 5+6 两排，11 个入口全部可见、可命中且无横向溢出。
- 代码提交：`30eb06b`（`fix: restore even top navigation spacing`）；只提交顶部导航 CSS、回归测试、当次清单和三份规则/交接文档，未纳入备份、截图或临时数据。
- Web-only 发布：`wm-top-nav-spacing-r1-20260823-153525-30eb06b`；服务器暂存目录的归档 SHA-256、Contracts 构建、Web 类型检查、Web 生产构建和两个目标 CSS 声明全部通过后，保留旧哈希资源并原子切换入口。
- 生产验证：主站、独立后台、两个 `/health` 和 6 个新哈希资源均通过；公网 CSS 包含 `flex:5 1 0` 与 `flex:6 1 0`；线上入口与发布包一致，API/Worker/Caddy 为 active，API/Worker 重启计数为 0，近 10 分钟三服务错误日志均为 0。
- 本批是单处 CSS 排版与规则文档变更，不涉及数据库、权限、账号隔离、作者数据不可变、跨服务恢复或核心工作流结构；按风险门禁未触发 `verify:full`。
