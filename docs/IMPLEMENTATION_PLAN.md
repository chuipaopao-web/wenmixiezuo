# 首版实施计划

本计划用于正式开工后的连续开发。老板已于2026-07-16通过 `DEC-002` 正式授权执行全部八个阶段。

首版默认由Codex单独完成开发、测试、文档和验收，不调用其他开发Agent。任务模板中的“负责人”和“复核人”由同一Codex在不同阶段执行证据化自检；高风险操作仍由老板确认。只有老板以后明确授权，才增加其他开发成员。

## 开工准备

1. 已确认：显示名称“文秘写作”、Web/API端口 `43110/43111`、数据目录 `D:\wenmixiezuo\data`、未授权付费时现金保护线为0、第二物理备份未配置、远程Git未配置。
2. 初始化本地Git仓库和唯一 `release_id`。
3. 将当前文档作为首个基线提交。
4. 为阶段1建立SQLite任务账本和验收矩阵。

## 计划目录

```text
apps/web/
  src/app/
  src/features/books/
  src/features/chat/
  src/features/agents/
  src/features/artifacts/
  src/features/chapters/
  src/features/tasks/
  src/features/knowledge/
  src/features/settings/
  src/lib/api/
  src/lib/sse/
  src/lib/offline/

apps/api/
  src/domain/
  src/application/
  src/contracts/
  src/infrastructure/db/
  src/infrastructure/files/
  src/infrastructure/models/
  src/infrastructure/tools/
  src/infrastructure/retrieval/
  src/infrastructure/recovery/
  src/http/

apps/worker/
  src/scheduler/
  src/runtime/
  src/executors/
  src/adapters/
  src/health/

tests/
  contract/
  integration/
  e2e/
  fixtures/
  fault-injection/
```

## 阶段1任务清单

1. 建立workspace、锁定Node稳定LTS和包管理器版本。
2. 创建Web、API、Worker最小可构建入口。
3. 定义统一错误、ID、版本、时间、事件信封和API响应类型。
4. 建立SQLite连接、WAL、安全Pragma和向前迁移执行器。
5. 建立确定性假模型、测试数据库和临时数据目录。
6. 建立统一启动器、健康检查和本机监听门禁。
7. 建立文档哈希、Schema版本和发布基线。
8. 运行类型检查、测试、构建、空库迁移和本地启动验收。

## 阶段2任务清单

1. 定义Repository契约和自动隔离上下文。
2. 实现owners、books、file registry和生命周期迁移。
3. 实现暂存、内容哈希、同卷原子提升和正式登记。
4. 实现稳定operation ID和独立恢复日志。
5. 实现归档、导入隔离区、恢复隔离区和删除墓碑。
6. 实现一致性快照、文件清单、哈希和临时恢复验证。
7. 编写跨书、崩溃、孤立文件和恢复故障注入测试。

## 阶段3任务清单

1. 实现任务、阶段、事件、依赖、幂等、租约和心跳。
2. 实现Worker领取、检查点、暂停、继续、取消和恢复。
3. 实现模型、工具、联网统一适配接口和调用账本。
4. 实现预算冻结、结算、滚动窗口和保护线。
5. 实现9个岗位模板、Agent实例和运行时能力验证。
6. 实现真实presence和SSE回放。
7. 实现活动主编租约、epoch、接管包和旧指令拒绝。
8. 编写结果不明、重复调用、预算竞争、失联和接管测试。

## 阶段4任务清单

1. 实现定位草稿、分类、动态标签和来源状态。
2. 实现确认后原子建书和9岗位初始化。
3. 实现题材适配规则、快照和失效传播。
4. 实现创作方案、故事圣经、总纲、卷纲、章纲和写作契约Schema。
5. 实现成果版本、比较、选择和从旧版创建新版。
6. 实现有范围、参与者、预算和收口状态的讨论流程。

## 阶段5任务清单

1. 实现实体、事实断言、故事时间和证据。
2. 实现A/B/C/D事实门禁、确认和熔断。
3. 实现正史版本、绑定、撤回、冲突和章节原子结算。
4. 实现分层记忆、FTS、检索记录和不可变上下文包。
5. 实现人物状态、时间线、关系和章末状态投影。
6. 实现确定性一致性检查和失效传播。
7. 完成百万字硬锚点和跨书检索测试。

## 阶段6任务清单

1. 实现主笔选择、盲测和异模型独立评审。
2. 实现单章预检、上下文包和写作契约。
3. 实现完整初稿暂存、版本登记和硬规则检查。
4. 实现结构化审校、严重度和最多两次定点重写。
5. 实现事实提取、正史结算和后续章纲更新。
6. 实现3至5章串行批次、检查点和断点续跑。
7. 实现跨章质量趋势和换笔建议。

## 阶段7任务清单

1. 实现情绪、主线、支线、钩子和信息差投影。
2. 实现原文隔离、抽象结构卡、干净室上下文和版权审查。
3. 实现研究区、来源证据、缓存和候选推荐。
4. 实现桌面三栏、全屏阅读、手机抽屉和真实状态。
5. 实现PWA、IndexedDB草稿、离线阅读和缓存失效。
6. 完成长文性能、无障碍和页面无闪跳测试。

## 阶段8任务清单

1. 执行全部故障注入和跨阶段回归。
2. 使用确定性适配器运行24小时Worker测试。
3. 完成两本测试书隔离和主测试书连续5章真实创作。
4. 执行中断、接管、预算、正史变化、版权和恢复演练。
5. 在干净目录恢复数据库、正文和正史并核对哈希。
6. 同步文档、覆盖矩阵、老板说明和发布证据。
7. 创建本地发布提交；远程存在并获授权时再推送。

## 每项代码任务模板

正式派工前必须填写：

- 任务ID与 `release_id`；
- 目标；
- 不做什么；
- 唯一写负责人；
- 允许和禁止文件；
- 依赖和当前版本；
- 实现约束；
- 验收标准；
- 测试命令；
- 失败停止条件；
- 回滚方法；
- 独立复核人。
