# 历史首版实施计划

> 本文记录 `wm-v1-20260716-220959-d5dd704d` 已执行的首版八阶段，不再作为长篇终局增量施工清单，也不改写历史任务。当前长篇终局增量仍处于E0设计冻结；老板已确认包含DEC-027小文秘书与本地分层路由的开发方向。收到老板明确开工指令后，唯一执行计划为 `docs/superpowers/plans/2026-07-19-final-longform-platform-implementation.md`。

本计划用于正式开工后的连续开发。老板已于2026-07-16通过 `DEC-002` 正式授权执行全部八个阶段。

首版默认由Codex单独完成开发、测试、文档和验收，不调用其他开发Agent。任务模板中的“负责人”和“复核人”由同一Codex在不同阶段执行证据化自检；高风险操作仍由老板确认。只有老板以后明确授权，才增加其他开发成员。

## 开工准备

1. 历史开工时确认：显示名称“文秘写作”、Web/API端口 `43110/43111`、数据目录 `D:\wenmixiezuo\data`、未授权付费时现金保护线为0、第二物理备份未配置。远程Git后来已配置为私有仓库 `chuipaopao-web/wenmixiezuo`；这一后续事实不改写首版开始时的历史状态。
2. 初始化本地Git仓库和唯一 `release_id`。
3. 将当前文档作为首个基线提交。
4. 为阶段1建立SQLite任务账本和验收矩阵。

## 计划目录

```text
apps/web/
  src/app/
  src/features/books/
  src/features/chat/
  src/features/agents/
  src/features/artifacts/
  src/features/chapters/
  src/features/tasks/
  src/features/knowledge/
  src/features/settings/
  src/lib/api/
  src/lib/sse/
  src/lib/offline/

apps/api/
  src/domain/
  src/application/
  src/contracts/
  src/infrastructure/db/
  src/infrastructure/files/
  src/infrastructure/models/
  src/infrastructure/tools/
  src/infrastructure/retrieval/
  src/infrastructure/recovery/
  src/http/

apps/worker/
  src/scheduler/
  src/runtime/
  src/executors/
  src/adapters/
  src/health/

tests/
  contract/
  integration/
  e2e/
  fixtures/
  fault-injection/
```

## 阶段1任务清单

1. 建立workspace、锁定Node稳定LTS和包管理器版本。
2. 创建Web、API、Worker最小可构建入口。
3. 定义统一错误、ID、版本、时间、事件信封和API响应类型。
4. 建立SQLite连接、WAL、安全Pragma和向前迁移执行器。
5. 建立确定性假模型、测试数据库和临时数据目录。
6. 建立统一启动器、健康检查和本机监听门禁。
7. 建立文档哈希、Schema版本和发布基线。
8. 运行类型检查、测试、构建、空库迁移和本地启动验收。

## 阶段2任务清单

1. 定义Repository契约和自动隔离上下文。
2. 实现owners、books、file registry和生命周期迁移。
3. 实现暂存、内容哈希、同卷原子提升和正式登记。
4. 实现稳定operation ID和独立恢复日志。
5. 实现归档、导入隔离区、恢复隔离区和删除墓碑。
6. 实现一致性快照、文件清单、哈希和临时恢复验证。
7. 编写跨书、崩溃、孤立文件和恢复故障注入测试。

## 阶段3任务清单

1. 实现任务、阶段、事件、依赖、幂等、租约和心跳。
2. 实现Worker领取、检查点、暂停、继续、取消和恢复。
3. 实现模型、工具、联网统一适配接口和调用账本。
4. 实现预算冻结、结算、滚动窗口和保护线。
5. 实现9个岗位模板、Agent实例和运行时能力验证。
6. 实现真实presence和SSE回放。
7. 实现活动主编租约、epoch、接管包和旧指令拒绝。
8. 编写结果不明、重复调用、预算竞争、失联和接管测试。

## 阶段4任务清单

1. 实现定位草稿、分类、动态标签和来源状态，并建立版本化标签定义、命名空间、别名、适用对象和归档治理。
2. 实现确认后原子建书和9岗位初始化。
3. 实现题材适配规则、快照和失效传播。
4. 实现创作方案、故事圣经、总纲、卷纲、章纲和写作契约Schema。
5. 实现成果版本、比较、选择和从旧版创建新版。
6. 实现有范围、参与者、预算和收口状态的讨论流程。
7. 实现主编自然语言资料治理意图与受限工具契约；明确、单书、可撤销的标签配置直接执行，其他Agent建议只进入候选。

## 阶段5任务清单

1. 实现实体、事实断言、故事时间和证据。
2. 实现A/B/C/D事实门禁、确认和熔断。
3. 实现正史版本、绑定、撤回、冲突和章节原子结算。
4. 实现分层记忆、FTS、检索记录和不可变上下文包。
5. 实现人物状态、时间线、关系和章末状态投影。
6. 实现确定性一致性检查和失效传播。
7. 实现标签赋值、语义标注、任务相关资料缺口、地点空间关系、关系/情绪时间投影和作者可见资料库查询API。
8. 完成百万字硬锚点、标签治理、候选/确认边界和跨书检索测试。

## 阶段6任务清单

1. 实现主笔选择、盲测和异模型独立评审。
2. 实现单章预检、上下文包和写作契约。
3. 实现完整初稿暂存、版本登记和硬规则检查。
4. 实现结构化审校、严重度和最多两次定点重写。
5. 实现事实提取、正史结算和后续章纲更新。
6. 实现3至5章串行批次、检查点和断点续跑。
7. 实现跨章质量趋势和换笔建议。

## 阶段7任务清单

1. 实现情绪、主线、支线、钩子和信息差投影。
2. 实现原文隔离、抽象结构卡、干净室上下文和版权审查。
3. 实现研究区、来源证据、缓存和候选推荐。
4. 实现桌面三栏、全屏阅读、手机抽屉和真实状态。
5. 实现中间全屏资料库：总览/缺口、角色、势力、地点/地图、道具/资源/技能、事件/时间线、规则、伏笔/钩子、关系/情绪图谱和标签中心。
6. 实现PWA、IndexedDB草稿、离线阅读和缓存失效。
7. 完成长文性能、图谱/地图交互、无障碍和页面无闪跳测试。

## 阶段8任务清单

1. 执行全部故障注入和跨阶段回归。
2. 使用确定性适配器运行24小时Worker测试。
3. 完成两本测试书隔离和主测试书连续5章真实创作。
4. 执行中断、接管、预算、正史变化、版权和恢复演练。
5. 在干净目录恢复数据库、正文和正史并核对哈希。
6. 同步文档、覆盖矩阵、老板说明和发布证据。
7. 创建本地发布提交；远程存在并获授权时再推送。

## 长篇质量增量：混合RAG运行时

本增量按DEC-012、DEC-016、DEC-018和 `docs/HYBRID_RAG_DESIGN.md` 实施，不改写阶段1至8历史验收：

1. 建立 `content_chunks`、嵌入模型快照、投影任务/水位和检索通道契约的向前迁移；事实/关系/状态明确世界有效、人物知情、系统记录/正史修订三轴。
2. 接入LanceDB OSS TypeScript本地嵌入模式，安装/显式升级时把模型、许可和哈希清单固定到 `data/cache/models`；正常运行禁用远程模型加载，验证Windows/Node离线冷启动、空缓存/错哈希、取消和目录隔离。
3. 按DEC-017实现正文场景根/场景节拍父块/检索子块，以及设定、规划、事实、Wiki、人物声音、特殊结构块和任务临时资料的独立确定性切片器；证据原文与索引/嵌入文本分离。
4. 实现本地Node嵌入适配器，以独立中文小说查询集选择并锁定模型、量化、维度和查询指令；切换模型必须全量重建。
5. 按DEC-019实现不可变查询计划、同名/别名消歧和受Schema/操作白名单限制的AST；实现结构化、FTS5、LanceDB向量、关系/Wiki四路通道记录，先做权威/三轴时间/实体硬过滤，未消歧实体不得成为关系种子。
6. 实现H硬约束、E证据、I灵感三车道；按事实ID/来源范围/父子与摘要血缘聚成证据簇，建立冲突组，E/I分别RRF和岗位重排，H不参加融合且不得被删除；选中确定性结论必须闭环到正式事实或最小原文。
7. 将已确认标签和语义标注作为结构化过滤/岗位重排元数据，不把普通标签拼入正文嵌入文本；实现相关投影增量更新和版本水位。
8. 实现按书/任务索引隔离、不可变切片快照、来源覆盖/范围/父子邻接/哈希探针、SQLite原子清单切换、投影水位、幂等补建、损坏重建、备份恢复校验和旧正史失效。
9. 为九岗位实现不同查询意图、通道、粒度、下钻、证据闭环和灵感预算；查询计划和通道运行分别记录本地候选、P50/P95、CPU/内存与实际模型注入Token。
10. 使用500万规范化中文字符、1500章的满规模语料和与实现分离的分层抽样金标，完成各通道与三车道消融、同源重复、标签过滤、时间/多跳/否定/冲突/无答案、延迟、磁盘、内存和Token评测。
11. 使用真实模型完成岗位上下文和创造性非劣效盲评；只有E3＋E4范围证据成立时才更新长篇质量声明。
12. 建立临时、候选、正史、派生四层生命周期字段、提升记录和保留策略；候选使用 `active/dormant/promoted/rejected/superseded`，任务/阶段关闭时未确认项休眠；提升必须创建新正史版本并在同一权威事务登记投影任务。
13. 实现聊天/稿件归档、每章20个热版本、任务临时向量索引7天宽限清理、拒绝候选默认排除，以及所有作者原始资产的恢复与永久删除门禁。
14. 故障注入覆盖提升事务中断、归档校验失败、清理任务重复执行、投影重建失败、陈旧水位降级、关系扩展爆炸、错误实体种子和无答案填空；任何失败不得丢失原始资料或让候选驱动正式生产。
15. 实现“确认原文高于自动抽取”的证据门禁：抽取先候选，B级硬事实使用前回查最小来源，冲突时隔离绑定；上下文组包按实际Tokenizer先预留输出/工具/格式/安全余量，硬资料不得静默截断。

## 超长篇连续性增量：1000章以上Agent陪伴

本增量按DEC-014、DEC-015、DEC-018和 `docs/ULTRA_LONGFORM_CONTINUITY.md` 实施；在当前文档授权边界改变前只保留为任务计划，不创建业务代码、迁移或运行数据：

1. 新增承诺/伏笔、五级连续性节点、岗位连续性日志、关注快照、压缩快照/探针和质量窗口的向前迁移与Repository契约。
2. 实现场景→章节→故事弧→卷→全书主脊的增量编译、阶段结算、来源回链、事实/状态/承诺/因果/来源探针、精确失效和按范围重建。
3. 实现滚动规划：30至50章轮廓、5至10章细化、1至3章场景契约，以及故事弧/卷检查点。
4. 为九岗位实现差异化连续性包，以及 `always_active/open_loop/stage_active/dormant_triggered/closed_local/superseded` 检索活跃度；验证跨重启、跨卷、跨100章间隔和正史升级接续，同时保持未激活零调用。
5. 实现锚定式聊天压缩、独立硬锚、来源逐段哈希、模型/适配器/提示词/Tokenizer快照、轮换事实/决定/继续工作/来源/冲突/未决探针、旧消息回读率和失败回滚；原始聊天不进入正式向量索引。
6. 实现每章、每10章、故事弧、卷和20/50/100/200章窗口的分层治理，不让软趋势自动改写正文。
7. 实现阶段摘要优先和实体/开放线程/规则/因果/冲突等触发下钻，限制为一次补充周期；统一活动集0、卷1、故事弧2、章节/场景及结构化事实3，原文证据解引用单独记预算，并分别审计本地候选与实际注入Token。
8. 增加超长篇状态、承诺、连续性、阶段结算、下钻预览、成员关注和质量窗口API及前端视图。
9. 运行500万字符/1500章E2满规模回放、E3独立金标、故障/恢复/重建/跨书测试；覆盖后期章节回查早期正史、摘要故障原文兜底、误触发/漏触发和上下文有界，并记录目标电脑资源基线。
10. 真实模型按20/50/100—200章评测，并对阶段摘要＋触发下钻、摘要永不回查、任何命中都回查和整书上下文做创造性非劣效消融；只有实际跑到1000章以上和目标字数范围，才允许声明完整范围的Agent长期陪伴或文学质量。

## 每项代码任务模板

正式派工前必须填写：

- 任务ID与 `release_id`；
- 目标；
- 不做什么；
- 唯一写负责人；
- 允许和禁止文件；
- 依赖和当前版本；
- 实现约束；
- 验收标准；
- 测试命令；
- 失败停止条件；
- 回滚方法；
- 独立复核人。
