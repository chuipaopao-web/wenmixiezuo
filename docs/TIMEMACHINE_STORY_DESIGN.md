# 时光机与故事线库完整设计方案

## 当前新增决定：按节点实测选择模型（2026-09-16，待实施）

老板要求所有已登记模型在实际调用节点上测试速度、稳定性与质量，后台展示证据，并按节点选合格前三派工；不再固定GLM等模型反复试错。唯一实施合同为主区`.local/dispatch/inbox/task-rebuild-closeout-01.md`顶部“MODEL-NODE-EVAL”，含现有代码入口、分批真实测试/预算、存储字段、后台UI、资格排名/熔断/恢复与验收。先质量准入再比较速度，样本不足不称稳定，少于三名合格不凑数；生成/审查异模型、真实成员身份、冻结历史不改。当前未测出排名、未上线新后台，不将计划写成已完成。该范围包含先修C的反馈调度缺口，不能把系统漏传反馈算成模型质量差。

## 当前新增决定：按节点实测选择模型（2026-09-16，待实施）

老板要求所有已登记模型在实际调用节点上测试速度、稳定性与质量，后台展示证据，并按节点选合格前三派工；不再固定GLM等模型反复试错。唯一实施合同为主区`.local/dispatch/inbox/task-rebuild-closeout-01.md`顶部“MODEL-NODE-EVAL”，含现有代码入口、分批真实测试/预算、存储字段、后台UI、资格排名/熔断/恢复与验收。先质量准入再比较速度，样本不足不称稳定，少于三名合格不凑数；生成/审查异模型、真实成员身份、冻结历史不改。当前未测出排名、未上线新后台，不将计划写成已完成。该范围包含先修C的反馈调度缺口，不能把系统漏传反馈算成模型质量差。

> 2026-09-13最新分卷增量见第24节：默认6—10卷，每卷常规30万—50万字、预算上限50万字，500万字必须10卷。替代13卷及阶段数固定绑定卷数建议。方案已定、运行待开发；本轮只维护文档和后台方案记录。

> 2026-09-11：第23节更新为分层编号、叙事锚点、自主记忆维护与代码差异审计。卷A/链A1/章1、主线1/支线1覆盖旧编号建议；新增能力待开发，未部署。

版本：开发规格 v2.1及VP-01增量，2026-09-13。分卷数量与容量以第24节最新决定为准；其余编号、分层设计和自主维护以第23节为准，第22节为工程基础。P0基础和P1部分代码已有历史测试，完整真实闭环仍待验收；VP-01业务未实施。旧业务只作参考，不拼接。执行状态见[本批记录](../coauthoring-v7/docs/worklists/TIMEMACHINE-192.md)，规格存在不代表功能已实现。

方案范围：故事线资产、推荐、全书方案、分卷容量、上下文、成员执行、事实/结算、树与轨迹、编辑和后台、失败恢复与发布。按最新实施授权与第22节分批执行；旧版功能与静态原型不能代替新运行链验收。

## 1. 本轮确定的方向

- 作者页面继续使用“分卷”，不新增“大故事单元”导航或替换现有卷、链、章名称。
- 主要故事指有完整目标、过程、结果的一段故事。多个主要故事围绕主线展开，为人物提供连续的能力、认知、资源、关系或处境变化；不要求每次都升级或获胜。
- 故事数量只是篇幅容量参考，不是最低合格线。取消此前“低于34/50/84个必须补足”的提议；更不能靠拆步骤、重复冲突凑数。
- 故事线根据题材、融合题材、人物和作者偏好推荐，不把同一套模板套给所有作品。
- 首次进入自动启动主编推荐流程，作者选择具体故事线后，再设计三套完整全书方案。
- 同时显示可选的故事组织偏好，默认“由主编推荐”；作者可选择集中讲一个核心故事或几个重要故事交织，不必先填写主线数量。群像意愿独立，不与主线结构互斥。
- 开篇尽早建立期待：约300字抓住注意、首章形成明确期待、前三章有推进或小兑现。具体手段服从题材，不强制爆炸、打脸、死亡。
- 主角历程、全书情绪方向、长期期待、分卷故事和完结落点共同构成全书骨架。最后一卷收束，不再显示“接下一卷”。

## 2. 页面目标与边界

作者看完应能回答：这本书讲什么、为什么值得追、谁会经历什么、哪些故事交织、各卷大致发生什么、最后如何完结。

时光机负责全书方向和分卷粗规划，不提前完成所有人物人设、事件链和章纲。采用后给下层提供清楚的任务与边界，也保留后续创作调整空间。尚未发生的规划不成为正文事实。

## 3. 故事线库：按用途组织，按作品推荐

### 3.1 初始目录

以下36项为本次提议的基础目录，不是“全网最全”的声明。实施先逐项核查旧资产，保留独有含义，建立旧新映射；不能直接清空原库。相近条目可能搭配，不等于重复。

| 分类（浏览入口） | 类型（作者选择） | 简介与使用区别 |
|---|---|---|
| 个人命运 | 实力成长 | 掌握新能力、解决更复杂的问题；不限于修炼等级 |
| 个人命运 | 逆袭立足 | 从受限处境取得尊严、机会或地位，不等于只加战力 |
| 个人命运 | 自我认同 | 认识并选择自己想成为的人，不强制人格蜕变 |
| 个人命运 | 身份转变 | 身份改变带来责任、归属与选择，区别于单纯升级 |
| 个人命运 | 救赎重生 | 面对过去的错误或伤害，尝试弥补、重新开始 |
| 个人命运 | 生存脱困 | 应对持续威胁，争取活下去或脱离困境 |
| 人物关系 | 爱情 | 感情产生、发展与选择，可相守也可分离 |
| 人物关系 | 友情 | 从相识走向信任、分歧、互助或疏离 |
| 人物关系 | 亲情 | 血缘或家庭关系中的陪伴、责任、冲突与修复 |
| 人物关系 | 师徒传承 | 教授、学习、独立与传承，可以认同也可以超越师门 |
| 人物关系 | 伙伴羁绊 | 长期共同承担行动与命运，适用于搭档、团队及非人伙伴 |
| 人物关系 | 宿敌关系 | 对抗中相互影响，可敌对到底，也可转为尊重或合作 |
| 事业与组织 | 职业事业 | 专业实践、职业选择、责任与成就，不限升职 |
| 事业与组织 | 经营积累 | 资源投入、经营取舍与成果积累，避免账目代替故事 |
| 事业与组织 | 势力建设 | 建立组织、规则、人才与共同目标，区别于个人赚钱 |
| 事业与组织 | 团队磨合 | 能力互补、协作分歧与共同承担，不要求所有人服从主角 |
| 事业与组织 | 竞争夺魁 | 围绕比赛、资格或稀缺机会竞争，结果不只“夺冠” |
| 事业与组织 | 权力争夺 | 权力取得、使用、制衡与代价，不局限朝堂 |
| 行动目标 | 复仇讨还 | 追索伤害、责任与补偿，允许改变复仇方式 |
| 行动目标 | 守护家园 | 守护人、地方或生活方式，不强制扩大成拯救世界 |
| 行动目标 | 寻找营救 | 寻找并带回人或重要对象，重点是行程与选择 |
| 行动目标 | 归乡重聚 | 回到故乡或重逢旧人，处理归属及物是人非 |
| 行动目标 | 旅途冒险 | 跨越未知环境，遭遇新局面并产生持续后果 |
| 行动目标 | 使命抉择 | 接受、拒绝或重新解释被交付的责任 |
| 探索真相 | 身世追寻 | 追索出身及关联关系，区别于所有世界秘密 |
| 探索真相 | 案件调查 | 围绕具体事件查证并形成答案，不强制犯罪题材 |
| 探索真相 | 阴谋揭露 | 发现有意隐藏的行动与目的，区别于自然未知 |
| 探索真相 | 历史追索 | 重建往事及其对当下的影响，不能只堆背景资料 |
| 探索真相 | 未知探索 | 探索规律、空间或现象，适用于科学、奇幻与怪谈 |
| 探索真相 | 秘密身份 | 隐瞒、误认、揭露身份改变行动与关系 |
| 生活与时代 | 日常陪伴 | 通过持续相处形成趣味、温暖与关系积累 |
| 生活与时代 | 家庭变迁 | 家庭成员及生活结构随时间变化，侧重集体历程 |
| 生活与时代 | 疗愈重建 | 从伤痛或失序中重建生活，不强制完全治愈 |
| 生活与时代 | 制度变革 | 改变群体运行方式及利益关系，不强制革命式结局 |
| 生活与时代 | 战争和平 | 集体冲突、战争经历与和平选择，兼顾个人命运 |
| 生活与时代 | 文明兴衰 | 长期社会、技术与文化变迁，避免只有宏大名词 |

“信任与背叛”“误会与和解”等作为关系变化方式关联多种故事线，不再为了同义表达重复建类型；确有独立用途的新类型可以扩充。作者可自由输入，不受目录封闭限制。

### 3.2 群像与双主角

人物组织与主线结构分开保存：单主角、双主角、群像描述人物组织；单主线、多主线描述核心追求的组织。一位主角可以推动多条主线，多位主角也可以共同推动一条主线，不能根据人数或总字数机械确定。根据已有开书信息推荐，作者可调整；修改已确认主角结构要说明影响，不能默默替换主角。

群像中，每个主要人物或群体有自己的目标、行动及落点，多条线会交汇、相互促成或阻碍。禁止全员只是帮助主角，禁止仅增加视角数量冒充群像。双主角明确双方推动故事的职责，不强制戏份平均。群像不必多视角，单主角也可以有丰满配角；故事组织和叙述视角分别处理。

### 3.2.1 主线、支线与完整故事的区别

| 概念 | 职责与判定 |
|---|---|
| 全书主线 | 承担核心追求，有持续行动、重要转折及终局职责；一条或多条由具体方案决定 |
| 贯穿支线 | 重要关系、秘密或独立追求，跨多个卷发展并影响全书 |
| 阶段支线 | 某阶段展开并收束的独立追求，可与主线汇合 |
| 本卷核心故事 | 本卷重点解决什么、推进哪些主支线，不因此新建一条全书主线 |
| 主要故事 | 有目标、过程、结果的完整故事段落，可直接服务主线，也可服务支线；其数量不等于支线数 |

成长、爱情、经营等是内容类型，不预设主支线身份。恋爱作品的爱情可以是主线；同一类型也可以形成多个人物的不同具体线。主编推荐时提出主支线建议，三套方案各自确定结构与数量；作者可以调整。多主线不等于平分戏份，每卷说明当前重点推进哪些、哪些暂隐及重新承接位置。

### 3.3 题材与融合题材推荐

主编同时读取题材组合、主辅风格、创意尺度、核心卖点、人物与世界。题材用于解释推荐，不作为排他过滤器。

示例：仙侠+经营可以推荐技艺成长、工坊经营、伙伴和势力建设；悬疑+恋爱可以推荐调查与信任变化，但不能因融合恋爱而强制破坏推理；体育+群像可以推荐竞技成长、团队磨合、宿敌与职业选择；日常+奇幻可以围绕非人伙伴与共同生活，不必拯救世界。

推荐必须指出本书具体人物、目标及可能变化。缺少配角时可提出候选身份，明确尚未设定，不能编造为已存在角色。融合题材不能简单拼接各题材固定套餐；主编判断哪些形成同一个故事，哪些独立展开。作者明确不要的内容不得自动加入。

### 3.4 每条资产的后台结构

稳定ID、名称、短简介、适用情形、设计要点、变体、可搭配类型、语义重复或区别说明、题材化示例、可参考方法ID、各层用法、版本与状态。默认展示名称和短简介，展开查看其他字段。

故事线资产回答“写什么关系或追求”；方法资产回答“怎样展开”。关联方法是参考，不是必须引用。初始通用简介建议一两句话，题材化例子按需读取，不整库注入。

## 4. 作者流程与手机交互

### 4.1 首次进入

进入后立即建立或恢复本书推荐任务，显示主编正在准备推荐。系统先检查当前正式版本对应的短卡；没有或过期则由资料成员整理，之后主编推荐。资料成员实际工作时显示其真实身份，不能伪装主编已在设计。

进入即显示“你希望故事怎样展开？”选择区，不等待推荐完成、不设单独必答步骤。三项单选，手机纵向卡片：

| 作者选项 | 页面介绍 | 后台偏好 |
|---|---|---|
| 由主编推荐（默认） | 根据本书人物、题材和想写的故事，推荐合适的安排。 | 自动判断，不预设主线数 |
| 集中讲一个核心故事 | 围绕一个主要追求展开，其他故事共同推动它。 | 单主线 |
| 几个重要故事交织推进 | 几个重要目标分别发展，相互影响，共同走向结局。 | 多主线，数量由主编建议 |

作者明确的既有同项偏好应恢复，不用默认值覆盖。下方独立选项“也希望配角拥有自己的完整故事”，介绍“让重要人物有自己的目标和选择，他们的行动会影响全书走向”。它表达人物展开意愿，不自动把作品改为多主线或增加叙述视角。已有群像、双主角信息直接继承并可见，不要求重新作答。

主编推荐同时返回建议组织方式及一句本书理由。“由主编推荐”仍保持自动偏好，不因收到建议就替作者锁定。作者在推荐中途修改选择，系统保留其最新版本；旧推荐不得覆盖新选择。改组织方式不清空已选故事线，开始设计时按最新版本重新判断主支线。若建议与明确选择冲突，提出具体建议由作者决定，不能暗改。

推荐完成后显示主编大头像及引导语：“老板，我们来设计全书骨架。这是我为本书推荐的故事线，您看看，还想加入哪些？”

每张卡为“具体类型＋本书一句话介绍＋选中状态”。推荐可预选，但作者点击开始设计前可自由调整。没有固定选择数量，不铺满36项；推荐多少由作品需要决定。至少需要表达一个可推进的核心故事追求，不要求一定选中名为“成长”的标签。

### 4.2 添加与选择

作者选择小类型，不选择“个人命运”等整个分类。点整张卡可切换，选中不仅依赖颜色。点“添加其他故事”打开独立面板：搜索、按用途分类浏览、简介、选择；提供“我想加入……”文本输入。新增通用项需转化为本书建议后再进入正式设计，不伪装已有具体剧情。

手机底部显示已选数量和开始设计操作，已选项可展开移除；不得遮住正文、输入法或设备安全区。分类导航可以换行或纵向选择，不把入口藏在不可察觉的横向滚动里。桌面使用同一数据和流程。保留输入、滚动与选择，失败不清空。

### 4.3 合并与主次建议

首次推荐前，主编先处理明显重叠；作者调整后，在开始设计时统一检查，不每次勾选就调用模型。

没有需要决定的问题直接设计；有问题则就地展示具体建议：例如两条都围绕同一工坊经营，建议合成一条并保留两方面意图。操作“接受建议／分别保留”。多条不重叠但篇幅有限时建议哪些贯穿、哪些阶段展开，作者可修改。

不因数量多自动删减，不静默改变作者主次。一次汇总相关建议，避免连续弹窗。作者分别保留后不反复要求合并；后续设计通过不同目标、关系或阶段明确区别。

## 5. 结果页：三套完整方案

三套回应同一作者选择和正式上游，核心行动路线、冲突组织、人物关系或结局实现路径有实质差异，不能擅改既有结局约束。切换卡固定标方案A/B/C并显示成员；结果内部的故事标题体现差异，不仅换文风。

明确选择单主线时三套均采用单主线，明确多主线时三套均采用多主线，具体条数可不同；自动推荐时三套可以结构相同或不同，不强制单线、多线、群像各出一套凑差异。群像及配角偏好独立继承。采用后若改结构，生成新候选并识别关联影响，原采用版本保留。

每套顶部可显示“全书主线1条 · 贯穿支线3条 · 阶段支线暂拟8条”一类概览，数字来自该方案实际记录，不是预设目标。未具体设计的数量明确为预计；同一条线跨卷不重复计数。展开显示名称、人物、目标、展开与收束卷。主要故事数量另作容量参考，不混进支线计数。

切换时整个方案内容一起变化，作者已填意见按方案保存，状态不串。每套页面如下：

| 顺序 | 默认展示 | 展开或编辑 |
|---|---|---|
| 全书概览 | 谁从何处出发、争取什么、如何走向结局 | 主要看点、整体味道、创作边界 |
| 开篇抓人 | 开头抓眼、首章期待、前三章推进 | 各项怎样承接本书卖点 |
| 长期期待 | 开篇惦记什么、想看到的变化、结尾怎样回应 | 推进到哪些卷、阶段兑现 |
| 故事线 | 主支线身份、谁的线、起点、主要变化、最终落点 | 群像归属、交汇关系、展开及收束卷 |
| 分卷 | 卷名、主要故事、转折高潮、结束变化 | 篇幅参考、主要故事清单、关系变化、期待职责和衔接 |
| 完结 | 最终冲突结果、人物落点、期待兑现 | 保留有意开放的问题及理由 |
| 操作 | 调整故事线、修改意见、采用本方案 | 新旧候选比较与版本 |

去掉无关说明、技术字段、方法编号和“设计依据”入口。上下文短卡、版本和调用记录放后台。必要失败提示与候选身份仍须明确，不能为了简洁误导作者。

### 5.1 全书基线

#### v1.6 全书方案页的故事线与交织

本次对象是全书方案页“故事线怎样交织”，不是时光树。首次设计产出主线和支撑全书方向的重要支线，不固定四条，也不把重要支线全部强制贯穿全书；局部支线与链以后按卷细化。用紧凑纵向编号目录显示：编号、主线/重要支线身份、名称、一句从起点到落点的走向。手机不挤成多栏。

目录下展示关键交织：003→001表示有向影响，001＋002表示汇合；每项有一句本书具体作用，不只写“相互推动”。点击编号查看走向及相关关系。多线时按编号定位或聚焦直接关系，不截断目录；细小碰面留给卷链。同线跨卷延续不等于两线交织。

分卷引用当前方案目录编号；切换候选时目录、关系、分卷整体更新。候选编号是方案内标识，采用时由系统映射到稳定书内ID，不能与另一候选或独立时光树演示样例强行等同。

基线是采用方案共同遵循的创作方向，包括主要看点、整体情绪、主角出发点与最终走向、作者保留与排除的内容。前端用“主要看点／整体味道／最终走向”等直白文字，不另外堆术语表。

情绪基线承接已有风格，可有局部低谷、紧张与反差；不是全书保持同一情绪。角色成长可表现为能力、资源、关系、认知或选择，不强制性格改变。全书基线在方案采用时形成版本，三套中允许的差异明确显示，不以一个隐藏基线限制三个方案。

### 5.2 开篇约束

快节奏网文默认要求：开头约300字出现可感知的冲突、异常、欲望或趣味；第一章形成明确继续阅读的期待；前三章有实际推进或小兑现，并引出后续期待。300字是设计定位，不能硬截文字或机械计数判定文学质量。

时光机给简短开篇安排，首卷继承，前三章规划落实，实际正文再检查。惊险、反差、关系、谜团、机会均可抓人，不强制固定打脸或爆炸。作者明确选择其他节奏时采用其偏好并可见记录。剧本未有运行链时不得承诺该功能已支持剧本，按本期网文实施。

### 5.3 长期钩子与阶段看点

长期期待须尽早建立可感知的苗头，贯穿推进并在结尾回应；不必首章公布谜底。不是“下次出现什么装备”等循环新鲜感，也不是后期突然出现的终局设定。

前端用三列表达“开篇让读者惦记什么／读者想看到的变化／结尾如何回应”。同义期待合并，不强制固定三条。允许一条核心期待及必要的辅助期待。阶段钩子归相关卷链，全书中只记录其对长期期待的贡献。

例如无灵根者能否立足，是处境期待；机甲能否作为伙伴被承认，是关系与世界期待。展示必须结合具体方案，不能三套照抄同一兑现路径。这里不向作者展示模型推理过程。

## 6. 主要故事数量与分卷容量

### 6.1 口径与参考

“主要故事”有相对完整的目标、发展过程与结果，围绕主线或有作用的支线展开。报名、抽签、进场不算三个主要故事；一个完整故事也可能跨卷，跨卷只算一次。多视角重复呈现同一故事不重复计数。

按平均约3万—6万字承载一段主要故事，可给出以下初始估算。它不是行业统计，也不是全题材统一标准：

| 目标篇幅 | 主要故事参考数量 |
|---|---|
| 200万字 | 约34—67个 |
| 300万字 | 约50—100个 |
| 500万字 | 约84—167个 |

数量只用于讨论容量，不强制最低数量、不因超出或低于范围阻断生成。主编结合题材、群像交织和故事复杂度解释明显偏差，必要时建议丰富故事或调整篇幅。实际字数是所有故事交织后的总叙述量，不能按故事线各算一遍再相加。

### 6.2 如何放进设计

时光机先规划全书路线与各卷，再按卷分批补齐主要故事的一两句概览；每段简述目标、过程关键变化和结果。不要一轮生成数百个细节事件。系统提供去重后的数量和篇幅估算，主编判断是否重复、平直或容量不足。

人物的稳定成长指长期有累计：经验、能力、资源、关系、地位、认知或目标逐步变化，允许失败、停顿、损失与恢复，不要求每段升一级。不要因为“成长”要求所有日常、群像或感情故事成为实力升级工具。

前端每卷默认展示最重要的故事概括；展开看该卷主要故事清单和预计篇幅。数量作为辅助信息，不做作者必须凑满的进度条。

### 6.3 分卷输出要求

每卷保存：核心故事、承接局面、目标、主要阻碍、关键推进或转折、高潮或重要选择、结果、人物及关系变化、故事线与长期期待贡献、后续衔接、主要故事清单、篇幅估算。

v1.6卷方向卡：默认卷名与一句主要故事；展开显示“这一卷写什么、主要故事推进、本卷关联编号及具体交织、关键转折、高潮与本卷兑现、人物变化与得失、本卷结束与后续”。主要故事概括目标、关键过程及结果，不提前拆对白、场景、章节，不固定每卷四段。转折、高潮和兑现重合时可合并，无关维度省略。最后一卷末项改为全书收束。

本卷须有自己的阶段兑现，不能只引出下一场危机；日常、情感、经营可用关系或生活成果兑现，不强制战斗与灾变。不为每卷凑齐全部编号。卷取方向卡及直接相关线卡，不注入全书所有关系。以上均为规划，不能提前写成正文事实。原型默认展开“给这片大陆搭个脚手架”的详细卡，其余卷可以自行展开。

明确本卷重点推进的全书主线、其他主线的推进或暂隐、支线新增与收束；前端展开显示“推进哪些线／新增哪些线／收束哪些线”。不把每卷核心故事都计算为新增全书主线，也不因一卷暂隐就把该线标记完结。

这是判断维度，不是每卷都必须有爱情、背叛、伤亡或强反转；无关项省略。最后一卷只显示全书收束与人物落点，不统一生成下一卷任务。未兑现期待要么安排回应，要么明确是有意开放，不能遗漏后称开放式结局。

## 7. 向卷、链、章传递

### 7.1 v1.7 职责与结束条件（待本轮作者确认）

全书故事线管长期追求，卷管阶段局面，链管相对完整的具体故事。不能硬性规定一卷/一链等于一条支线；链可推进主线、支线或多线。卷的链数由阶段故事需要决定，不能按全书线数分配固定链数，也不要求每条链结尾都引出新悬念。保持既有稳定ID，不为呈现同一关系重复创建故事。

全书线卡补充四项：追求什么、关键变化、怎样才算核心期待得到回应、预计哪些卷推进与收束。每卷对每条线单独指定职责及一句具体目标：推进、转折、阶段兑现、最终收束。它们是本卷计划动作，不替代该线实际生命周期；同一卷可承担多项职责。未列出的卷不自动承担推进任务。例：003匠人合作、004历史误判在第7卷完成核心回应，第8卷重点完成001工程与002自主选择；已获得的联盟和知识作为成果继承，不因被使用就重开旧线。

卷方向卡必须有阶段目标、阻力、主要故事推进、线职责、阶段结果和结束条件。结束条件描述足够明确的故事结果，而不是“写够字数/完成固定链数”。例如第7卷结束须联合工程完成局部验证、各方合作职责落定、误判原因获得证据解释；完整天劫可留到终卷，不阻塞本卷结束。可按题材合并表达，不强制成功、战斗、牺牲、性格改变。

链在后续卷设计中生成，包含当前局面、本链目标、关键阻力与变化、结束条件、后续保留项及所影响线ID。订单链在验收及责任落定时可以结束，即使旧零件谜团仍未解。目标失败、明确放弃或改变亦可形成完整阶段结果；但须由成员依据正文说明结果，不把任务技术完成当作叙事完成。若原计划要求成功而正文实际失败，标记偏离并定向修订依赖计划，不能悄悄宣布原计划成功。

正式正文接受后，记录成员结合正文证据判断阶段结果及尚欠事项；系统保存不可变来源、版本、权限、幂等和状态，不能只靠关键词/字数决定完结。证据不足保留待核对；章节数量到达计划值但关键结果未发生，不能自动收束。作者已接受正文不因计划不符而自动覆盖。

下一条链上下文为“本卷阶段目标与职责＋当前链卡＋直接相关线卡＋必需前文实际结果”。结果卡记实际事件、资源/关系等变化、未解事项、证据引用、版本和对计划的偏离；不要重复搬运已完成链全文。必要原文通过限定书籍和版本的工具补读，重要事实缺失不静默截断。计划终点与当前事实分栏；前端显示“本卷怎样结束／留给后续什么”，内部来源引用留在详情或后台。

审查重点：全书期待是否有去向、每卷是否有阶段兑现、链是否有可辨认结果、未解伏笔是否有归属、提前收束线是否被终卷机械重开。审查不给所有作品强加相同节奏，不因某卷没有爱情或强反转而失败。资料供给按已有token预算动态编译，不把全书关系图送给每一章。

前端保留故事目录与关键交织，点编号查看线目标和卷安排；分卷把“关联编号”替换为“编号＋本卷职责＋具体结果”，然后显示结束条件及交接。原型首卷提供“链交接示例”帮助核对边界，它不是提前生成的真实链。全部状态标规划候选，不冒充实际正文。正式开发需覆盖未设计、准备中、已形成候选、部分失败与续做、已采用、来源改动待核对；本轮静态原型不伪装真实任务流程。

| 层级 | 继承什么 | 本层补充什么 |
|---|---|---|
| 卷 | 采用的全书方向、相关故事线、当前卷主要故事、关键结果、人物起止变化与期待职责 | 具体矛盾过程、人物安排、转折高潮与链分配 |
| 链 | 本卷目标、当前主要故事及相邻衔接、相关人物事实与规则 | 行动、阻碍、选择、事件结果与章分配 |
| 章 | 当前链进度、章职责、实际已发生事实、相关关系状态和局部规则 | 场景、叙述、对白、节奏与章末期待 |

同一主要故事可包含多条链，也可跨卷；使用稳定ID保持归属，不能强制主要故事等于链。下层可补全实现方式，若改变采用的关键结果、人物落点或世界规则，提出上游修订并标记受影响范围，不静默改写。

规划终点不是当前人物状态。后续生成同时区分“现在实际发生到哪里”和“计划最终走向哪里”，避免角色提前知道真相或提前获得能力。

## 8. 信息短卡与上下文规则

### 8.1 六栏模板

1. 创作要求：题材及融合方式、主辅风格、创意尺度、目标篇幅、明确偏好。
2. 主角与起点：已确认的核心人物、处境、当前关系；未知项不补造。
3. 核心卖点：关键能力、反差、主要吸引力及必要边界。
4. 世界背景：足以理解宏观故事的环境、资源与冲突来源。
5. 影响全书的规则：会改变路线可行性、人物身份、死亡或能力结果的条件。
6. 已有全书意图：前序已采纳的开局、结局、主题方向，明确是规划。

资料成员整理而不创作新事实。原始资料保留，卡附来源及版本；不是整套设定的无损替代品。不重复装入同一事实的正文、规则字段和审查评价。方法资产不是作品事实，单独提供。

### 8.2 注入分工

系统：鉴权、版本选择、源读取、缓存、token预算、工具授权、防重、状态与保存。资料成员：判断相关性、合并重复表达、填卡、发现关键缺项。主编／设计成员：理解创作意图、推荐与设计、按需要申请补读和方法。

推荐阶段注入短卡、当前故事组织与人物偏好及故事线轻目录，需要时查具体资产；方案阶段注入短卡、作者最新选择及意见、当前方案任务、已选方法摘要；分卷细化阶段只注入该方案必要基线、主支线职责、当前卷及相邻衔接，不重复携带另外两套方案全文。作者偏好与主编建议分别保存，不能把建议冒充指令。

成员可以按用途搜索全库，但不要求查询或读完全部方法。工具只返回本次相关结果；方法选材后清理无用工具输出。系统不靠关键词硬判文学相关性。找不到方法可原创设计，方法是参考，不能以“未引用某方法”直接否决作品。

### 8.3 预算与分批

老板红线（2026-09-12）：设计成员单次收到的上下文（提示词）不得超过1.5万字，取代本节早前"不设通用字符安全值"的原则；模型路由的token预算继续按实际计量，为输出和工具补读预留空间。后台显示每次调用的字符数与每轮耗时。没有可用预算配置不得盲发超长请求。

开发时为短卡整理、推荐、单套方案、分卷补全分别配置并测试预算。预计超限时先去重复表示、缩小当前工作范围，再让资料成员重整相关卡或拆批；不能截断关键条件、静默删原文、反复用同一超限输入重试。短卡长度以必要信息完整为准，R191的594字符只是一个人工样例，不作为目标上限。实现：卷卡批次只带涉及线与全卷概要；主编审校与编剧自检拆为"全书结构层（紧凑候选+补读工具）＋按设计批次的锚点与细节层"，每层都有界；网关在1.5万字处硬拒绝并记录prompt_chars（迁移0119）。

资料过多时分源整理再合并，保留关键事实出处。每轮传入任务所需事实与近期产物，不携带无限历史会话。增加批次不固定为一次只设计一个微小条目，按实际输出大小和速度测试调整。

## 9. 成员执行节点

### v1.8 三名编剧与真实归属

最新显示修订：三个切换卡片固定标为“方案A、方案B、方案C”，同时保留编剧头像、姓名与职责。故事名称保留在对应结果内容，不卡在切换标签中代替方案字母；字母表示当前候选位置，不代替内部稳定ID或真实成员归属。

默认三套由三个不同的已启用策划编剧成员各负责一套，主编负责推荐、发现明显重复与定向复核，不把主编头像当作三套设计者。三位成员读取同版上游短卡和作者要求，各自规划路线并按用途查方法；初稿不互相传阅，完成后由主编比较差异，不预先固定为三种模板。负责一套的成员继续补全该套分卷，除非明确换人。

每个方案标签显示成员头像、姓名、职责与方案名。数据来自该候选任务冻结的成员快照，不按第几个标签硬编码；模型绑定、工具、预算及真实调用来源留在后台，成员身份不同不代表底层模型必定不同。重试保留同一候选身份，换人须记录原成员与接续成员，不伪造原作者。历史配置更新不能倒改旧结果署名。

三名成员不足时明确缺席与原因，可先完成可用成员的方案；不能把同一成员改名凑三人。并发数由供应商限流与预算决定，不要求同时发请求。每套先完成骨架，再按实际token预算分批补卷，分批持久化；任何一套失败只续该套未完成部分。进度由实际完成的结构单元计算，不按计时假装完成；未完整方案可查看但不可采用。完整且核查通过的一套不被其他失败方案阻止采用。

原型使用真实成员目录的红玉、幼薇、苏映棠与头像，明确“席位演示，样例非成员实际生成”，不能将手写候选冒充模型实测。正式UI成员由任务返回，数量状态与头像失败回退（姓名首字）也须覆盖。

岗位职责固定，显示姓名、头像和模型来自统一成员注册表，不在前后端各写一套。主编是本流程负责人，设计成员执行方案，资料成员提供短卡；具体可用成员需实施核查，不假称新岗位已上线。

| 节点 | 执行者 | 输入／产物 | 是否启动AI |
|---|---|---|---|
| 进入与恢复 | 系统 | 会话、书籍、版本／恢复已有状态 | 否 |
| 短卡缺失或过期 | 资料成员 | 正式源／六栏短卡及来源 | 是；有效缓存不重复调用 |
| 首次推荐或明确更新 | 主编 | 卡与资产目录／本书具体故事线 | 是 |
| 组织偏好、勾选、输入、保存 | 系统 | 作者选择／含主线与人物偏好的选择版本 | 否 |
| 开始设计检查 | 主编 | 最新偏好、选择与推荐／通过或具体结构、合并主次建议 | 是；相同输入复用结果 |
| 作者处理建议 | 系统 | 接受或分别保留／确定要求 | 否 |
| 三套路线 | 设计成员 | 同一要求／三套独立候选 | 是，分别保存与恢复 |
| 各方案分卷补全 | 设计成员 | 本方案／主要故事与卷概览 | 是，按预算分批 |
| 语义复核 | 主编 | 三套摘要及必要补读／差异、遗漏与修改意见 | 是 |
| 定向修订 | 设计成员 | 具体问题与相关部分／修订候选 | 仅有问题时启动 |
| 采用与版本传递 | 系统 | 完整候选与作者操作／正式规划版本 | 否 |
| 作者提出修改 | 主编后接设计成员 | 意见、当前版本／影响范围与新候选 | 是；原版不覆盖 |

格式、权限、关联ID、缺字段和预算由系统检验；故事是否有趣、重复、贴合题材与期待由成员判断。不要求展示或存储思维链，只保留简短决策结论、依据引用与实际工具记录。若复核与设计使用同一模型，后台如实显示，不宣称独立异模型审查。

## 10. 状态、失败、更新与采用

推荐准备中、待选择、待处理建议、方案设计中、部分成功、待采用、已采用、更新可用、失败可重试明确区分。结果成功且已采用时不再出现初始“请确认设计”引导。

刷新与重复点击以owner、book、来源版本、选择版本和任务类型防重。任务离页继续，保存到已完成批次；三套中成功的可先查看，失败的单独续做。采用必须是该套内容完整且通过必要核查，不因其他候选失败阻止已完成候选采用。

选择版本包含故事组织偏好、人物展开意愿及故事线。推荐响应保留其输入版本；晚到的旧响应仅作历史结果，不自动替换当前选择或据此发起设计。仅改变选择不会触发每次重生成，点击开始设计统一检查；运行中方案冻结输入，后续改选保留为新草稿，不串入旧任务。

暂时网络或服务错误可有界自动重试；超限先重整或拆批；缺关键来源先补读；格式错误局部修复。达到预算／时限后真实显示停止与恢复按钮，不无限循环。开发测试后确定各类重试次数并在后台登记，不能只写“自动重试”而没有上限。

任务冻结来源、资产、成员配置版本。配置更新只影响新任务；旧页面与接口兼容，不能因静态发布打断作者任务。上游改动标记候选过期并提供更新，不直接清空已采用内容。选择改变后必须更新相关故事、分卷与期待，不能仅勾选UI变化。

采用保留方案、故事线、卷和主要故事的稳定ID及版本关系；已写正文保持完整。后续修改显示受影响卷链，具体重设计在作者授权范围内执行。

## 11. 后台展示与同步

资产区新增明确“故事线”入口，按用途浏览36项及后续扩展，群像等组织方式单独管理。保留方法库原有用途分类，故事线引用方法，不复制同一套资产。旧资产核查结果和合并映射可追溯。

“功能与AI流程→时光机”按真实节点展示介绍、作者操作、岗位、资料来源、输入输出、预算、工具、失败恢复、状态及版本。实际运行记录能查短卡、方法查询、主要故事数、批次、失败原因；作者页不展示这些内部信息。

本文作为完整方案唯一正文，随新后台接入阅读；后台对未实现、已实现未验证、已测试和已上线明确标记。开发路线、功能介绍与配置必须随每批更新，历史任务保留当时版本。不能因文档存在就显示已完成。后台入口尚未发布，实际实现进度以R192记录为准。

## 12. 建议开发顺序与验收范围

### v1.8 实施顺序与本轮核查

额度有限时不跳过验证或把静态原型直接上线。本次先把方案与原型收尾；得到实施确认后按下面三批推进，每批可独立检查与接管：

1. 先跑通一套真实闭环：新对象协议与存储、短卡来源版本、主编推荐及作者选择、一个编剧的工具查询/骨架/分卷分批生成、保存恢复、主编定向复核、采用与卷资料交接。只有真实模型返回且持久化读回才算通过，不用手写样例替代。作者UI暂不发布半成品。
2. 扩展到三成员：冻结成员/模型快照，受限并行，独立候选及卷分批，部分失败续做、切换、意见保存和修订版本，保证编号、成员及状态不串。完整一套可单独采用。前后端完成权限与跨书隔离、刷新/双击/晚到响应、缺成员、超限、模型不可用、源变更测试；真实探针记录实际输入/输出token和工具调用，不保存思维链。
3. 对接后续卷与正文：验证实际卷资料包，再开发章节接受后的结果事件、线与伏笔状态维护、树和轨迹；需采用正文证据验证收束、重修失效、重放与防重。后台同步正式方案/功能状态/测试记录，已验证版本再按安全发布约定上线。前两批不宣称正文树完成。

本轮检查明确：手写三套不等于真实三成员实测；候选局部编号不等于采用后的书内编号；原型时光树是另一份独立演示，尚未与选中方案连通；链示例不是已经生成的链；静态成功页不覆盖运行中/部分失败/恢复体验。正式开发必须补齐这些连接与状态。终卷不机械挂所有线，提前收束的成果以结果卡继承；不能按线数固定链数。

原型的方案编辑入口只演示局部选择保存，未真实重生成；正式版改选须产生新的输入版本。方法库逐条语义正确性、预算在真实模型上的效果、百万字长期效果仍需后续验证，不能因为文档完整就标全部通过。

| 批次 | 工作 | 交付检查 |
|---|---|---|
| 第一批 | 审计旧故事线资产、建设目录与题材化规则、后台方案入口 | 逐项简介和适用情形、旧资产保留映射、前后台同源与状态准确 |
| 第二批 | 短卡缓存、主编推荐、组织偏好与手机选择及建议合并 | 默认自动、显式单线多线、群像独立、融合题材、晚响应不覆盖、改结构保留勾选、分别保留、不重复调用 |
| 第三批 | 三套方案、主支线概览、分批分卷、开篇和长期期待展示 | 三套遵守偏好、全页切换、区别故事数与支线数、跨卷去重、最后一卷收束、容量只作参考、超限恢复 |
| 第四批 | 调整、版本采用、卷链传递及完整流程 | 关联更新、实际事实与未来规划区分、隔离防重、部分成功和真实模型验证 |

各批先验证再发布；生产发布遵守现有静态原子切换、保留旧资源、后端在途任务安全要求。R192已获实施授权，具体进度在本批记录中更新，未验证部分不发布。

测试具体矩阵在开发时维护到项目验收文档，不在此复制第二份矩阵。应覆盖纯题材与融合题材、群像、日常无战力升级、复杂调查、长篇容量、旧书恢复与移动端。工程正确性、真实模型效果、长篇实际阅读效果分别报告，不互相冒充。

## 13. 授权与实现范围

本轮授权涵盖产品流程、页面内容、故事线管理方式、主要故事容量参考和开发顺序。数据库字段、调用参数、模型预算和批量大小由开发者根据现状与实测落实，不能把技术选择转交给作者。最新“不合并旧业务代码”要求覆盖最初的增量实现路径。

R192已获实施授权，按批次实施。不沿用R191两个方案、固定八卷、原型本地保存作为新方案完成证据。

## 14. 旧代码核查与能力去向

以下为2026-09-10本地源码核查，不代表本次已访问生产验证：

| 证据 | 已有能力 | 新方案处理 |
|---|---|---|
| author-app/src/TimeMachinePage.tsx | 全书方案、分卷树、正文进度、故事动态、伏笔及未解问题 | 保留结果和恢复能力，重排为全书方案、时光树、正文轨迹三个页内入口 |
| 同页StoryDynamics | 按story_line、foreshadowing、open_question分类；intersected作为交汇项 | 不将最新交汇状态当完整交汇历史；树中保留关系，轨迹中保存发生记录 |
| application/creation/v7-creation-formalization-service.ts | 正文定稿生成结算，再维护人物、故事状态与规划 | 仅参考“采用正文后触发”的职责，新建受控正文事件入口，不复制旧执行链 |
| repositories/v7-creation-runtime-repository.ts | stableKey、状态版本、来源结算、防重写入 | 新建独立模型和存储，不沿用旧状态表作为新树权威；旧记录仅按显式迁移流程处理 |
| application/planning/v7-planning-maintenance-service.ts | 依据正式结算更新规划实际、提出规划调整 | 实际变化与未来调整分开展示，不自动修改已采用结局 |
| backend/creation-runtime/creation-runtime.ts | 故事线、伏笔、问题与树实际的提取协议及状态枚举 | 重新设计提取合同与状态机，不照搬旧枚举；停止与兑现明确分开 |

旧代码中的“正文进度”只有章数和最近确认链，不是完整人物成长轨迹；已有情绪、体验、结果字段也不能冒充实际读者反馈。新增轨迹必须来自实际结算记录，不能由规划曲线补画。旧记录缺关联时列为待归属，不按名称相似强制连线。

旧实现只提供“用户是否还需要这项能力”的核查证据。不得复制旧时光机页面、结算服务、状态仓储、提示词或工具循环到新模块，也不得运行时导入它们。R192最初对旧页面和结算的增量试写已按用户纠正撤回，未部署。新架构的范围和安全边界见第21节。

## 15. 时光树的结果与视觉

### 15.1 大树结构与故事关系分开

粗壮树干代表作品，大枝代表卷、小枝代表链。章节在链详情中展开，不默认再画一层。主线与支线是贯穿这些结构的故事关系，不等于卷或链；一条线可以跨卷推进，一条链可推进多条线。多主线不复制树干，用主线筛选与路径高亮表达，共享链只保留一个结构节点。

时光机只确定全书主线、重要贯穿线和各卷方向，不预先生成全部链与支线。具体故事随卷、链设计逐步展开；未知数量显示尚待展开，不能按字数机械补齐空故事。原型8卷、32链、8条故事线是交互样例，不是生产数量限制或实际全书规划。

全树缩略图保留所有卷轮廓，链多时折叠；点击卷展开链与线引用，缩放和分页不截断目录。按第23.2节显示卷A、链A1和主线/支线数字代码，画布省略长名称但图例明确类型；内部ID、显示代码和阅读排序分离，变更保留编号版本和别名。

每条线拥有永久内部ID和带类型的书内显示代码，如主线1、支线3；按第23.2节分别分配、维护版本及别名。候选临时身份与采用后的正式身份分开，合并方案保存映射，不让模型自行重排全书编号。

| 符号 | 语义 | 禁止误判 |
|---|---|---|
| 红色果实＋已收束文字 | 正式正文已完成该故事线的当前目标 | 规划写了结局、任务已完成、主角胜利一次不等于整线收束 |
| 绿色叶片＋伏笔编号 | 正文已埋设但尚未充分兑现的伏笔 | 所有未结束支线不能自动变成伏笔 |
| 枝梢＋推进中 | 线仍在发展 | 没有更新不等于遗忘或停滞 |
| 淡色枝干＋规划中 | 已采用但正文尚未开展 | 不计入正文已发生数量 |
| 灰色标记＋停止/撤销 | 明确弃置或不再采用 | 不显示红果实冒充兑现 |
| 待核对标记 | 证据不足、来源改动或归属未确认 | 不假装确定、也不删除原记录 |

支线收束与伏笔兑现是独立状态，一枝可以同时有果实和绿叶。一个伏笔可涉及多条线，拥有唯一ID并引用多个关联，树上引用同一伏笔，统计只计一次。若一个目标明确结束后开启新目标，优先建立关联的新线；若原收束判断被纠正，则同一线记重开事件并保留原因。

### 15.2 规划与正文

规划与正文使用独立切换视图。有正文时默认正文，没有正文时默认规划并明确尚无正文；原型初始为规划演示。规划只显示预期经过与依赖，不显示红果绿叶冒充已兑现。正文只用有效正式正文事件投影，尚未写到的卷可保留淡色轮廓，不编造链进展。画布内只显示数字与符号，不放标题、简介或长状态文字，名称、来源和状态文字放在列表与详情。

电脑采用“左侧树／右侧支线序列表”，手机采用“上方树／下方可展开列表”。列表显示编号、支线名称、状态与一句话进展。树上点击编号定位并选中列表项，列表点击定位并突出枝干；手机点编号可直接打开底部详情。点按热区至少44px，不能要求精准点叶尖。

一条线的红果与绿叶可以共存，伏笔详情按唯一ID去重。故事线编号可在不同卷链中重复显示，表示同一实体的多个推进位置，不重复计数；红果与叶片正式实现必须挂到对应收束、埋设或推进事件位置，不在每个历史位置复制当前最终状态。原型符号只演示选中范围内的当前状态，尚未实现逐章位置投影。

### 15.3 路径与交织

点击故事线高亮它经过的卷链；“交织关系”按需展示选中线的路径与直接相关连接，默认不铺满全树。结构父子线表示包含；故事推进路径表示同一线的出现顺序，不自动代表因果。承接、影响、汇合分别记录关系类型、具体卷链端点及一句原因；只因同卷出现不能建立因果连接。原型使用树外路径条与关系卡，避免手机画布交叉遮挡，后续可在选中节点之间增加连接。

规划关系由设计成员提出并随规划版本保存；正文关系由记录成员依据正式文本确认，两者不能自动互转。选择视图决定关系的数据来源。序列表可按卷、主线、状态筛选，并保留全局数量与回到全树入口；手机在当前卷展开链列表与底部详情，全树可随时返回。未归属故事有独立列表，不强行挂到任意卷。

支持缩放、拖动、回到全树、搜索编号或名称、未收束与待兑现筛选；筛选后保留必要主干和选中关系。手机默认聚焦当前卷相关枝干。详情展示目标、归属、当前状态、最新变化、关联伏笔、来源章节和历史轨迹。停止、待核对、规划中等也有独立形状与画布外文字图例，不只靠红绿颜色。

不得在官网公开暴露作者树或正文证据。分享、导出若后续增加须沿用书籍权限，本期不顺手开发公开分享。

## 16. 正文轨迹及其他旧内容怎样展示

时光机页内分为“全书方案／时光树／正文轨迹”。首次推荐和设计仍是主流程，未有正文时后两项显示真实空态，不复制规划冒充轨迹。

正文轨迹按正文阅读顺序展示，不按AI任务完成时间排序。可按人物、故事线、卷筛选，记录本次发生什么、关系/资源/认知/目标如何变化、伏笔推进、线的交汇或收束及来源章。默认一章简短摘要，展开看变化与证据，不铺满全部结构化字段。

“交汇”是可重复发生的事件，不应永久替代故事线的生命周期。线可以在交汇后继续推进、完成或暂停。人物轨迹展示正文支持的实际变化，不画凭空数值成长曲线；情绪结果是成员对文本的判断，标注为故事情绪，不声称真实读者情绪。

未解问题与伏笔分别保存：明确埋设的叙事承诺是伏笔；信息暂缺、人物疑问或事实不确定不自动升级为伏笔。正文回答问题才标回答，不能以规划答案结案。长期间隔只作可查看信息，不按“若干章未出现”直接判遗忘。

前端展示“正文已保存至第N章／记录已核对至第M章”，存在中间缺口时显示缺口及待处理数量，不用最大章号冒充连续覆盖。部分更新失败保留最后成功结果并标注不是最新状态。

## 17. 正文更新、成员判断与事务边界

1. 正文正式采用：系统保存不可变版本及正式事件，正文保存成功不依赖后续AI维护成功。未采用草稿不更新正式树。
2. 资料准备：系统取本章正式正文、有效的既有故事线/伏笔目录、相关上一状态与当前规划职责；资料成员在资料超出预算或需要相关性判断时整理补读，不能用摘要替代唯一必要正文证据。
3. 记录成员判断：识别推进、交汇、收束、埋设、强化、部分兑现、完整兑现、停止及未知，引用正文位置；“本章没出现”不等于结束。只产出变化提议，不自行改正文、编号或规划。
4. 系统验证：身份与书籍、源版本、可引用ID、证据出处、状态枚举、幂等和并发版本。结构错误可修复；有效引用不自动证明语义判断正确。
5. 需要复核的变化：与现有状态矛盾、收束缺关键结果、关联有歧义等交给记录复核成员定向核对；普通清晰推进不每次额外叫主编。仍不确定保留待核对，不强行选一个。
6. 成功应用：以一个结算版本写入增量事件和投影，更新树与轨迹；实际内容与未来建议分开。后续生成只在其依赖的必要状态可靠时继续，非相关维护失败不全书阻断。

成员判断依据：目标是否已得到明确结果，结果可以成功、失败、放弃或改变；“失败”不代表故事不完整。“弃置叙事线”与“角色作出放弃选择并完成其故事”不同，前者不是兑现，后者可以是有据的收束。

伏笔完整兑现要求文本足以回应该伏笔的核心承诺，提到一次或揭示一半只标部分兑现。人物的错误解释与真实答案区分，不因人物自信就记为世界事实。悬念故意保留可继续为叶片，不自动认定缺陷。

## 18. 版本、编号、重修与异常恢复

规划采用时系统分配稳定线ID/编号及关系；正文新出现未规划的线，成员提出新建与归属，由系统分配。无法确定归属进入“待归属”，不凭相同姓名或相似标题自动合并。合并保留旧编号别名与历史证据；拆分分配新编号并记录来自哪条旧线，不修改过去事件。

并行或乱序结算以章节顺序和当前正式版本处理。晚到旧结算不能覆盖新状态，重复事件不能增加编号或果实。涉及不同人物的同时变化可保留并行记录，但最终投影必须可重放。

正文重修建立新正式版本，系统找出依赖旧版本的变化，标记相关投影待重核，再按有效正文顺序重算受影响部分；保留旧状态历史，不直接覆盖证据。后续章仍引用旧事实时先提示冲突，不假装单章重算解决了全部下游影响。

撤回采用、改变主线、线合并拆分、角色改名、跨卷迁移等均有版本映射。改名不生成新人物或新线；计划删除不删除已发生正文。作者可以提出纠错，成员核对来源；不能直接点击果实把未发生事件写成事实。若作者明确改正文，走正文版本流程。

暂时网络错误有界重试，模型结果未知先查调用记录避免重复计费；输入超限拆批，缺关键证据补读；无权限、源消失和不可恢复格式问题明确停止相关节点。重试不会重写已经成功的正文或其他线。前端刷新只读取，不触发全书重新结算。

## 19. 时光树输入后续成员的方式

不把树的图片作为权威上下文。成员接收结构化相关线卡：稳定ID与显示号、目标、生命周期、最近进展、未解决事项、相关伏笔、实际证据及计划职责。按编号可查历史和原文，工具始终限定owner/book。

卷设计查本卷推进线及即将需要回应的期待；链设计查当前故事关联线、伏笔与上一结果；章设计查当前事件所需事实。是否需要回应由成员结合计划与正文判断，不能按编号、颜色或固定章数自动下结论。全书未收线目录可以分页搜索，不每次注入全量轨迹。

交织短卡只含“承接什么—本次推进什么—影响或留给谁”，每条关系包含稳定端点ID、计划/正文身份、版本与来源引用。卷取本卷的入边、主要汇合与输出；链取直接依赖和预期影响；章取当前链职责及必需事实，不递归展开全书关系。资料成员选择相关项、合并重复事实，系统按模型实际token预算预留输出与工具空间；超预算定向补查或分批，不能静默截断必要因果。语义待核实关系明确标记，不作为已发生事实注入。不保存思维链，不以树上视觉距离判断相关性。

## 20. 结果验收与责任补充

除第12节外，完整交付须证明：正文采用后树自动变化、支线果实与伏笔叶片可以共存、多主线正确挂接、编号不会因排序刷新而变化、轨迹按章顺序、跨卷伏笔不重复统计、重修后旧结算不覆盖、失败保留正文及可恢复、权限隔离、旧数据不被编造关联。

后台每个节点明确是否启动AI、岗位和实际模型、输入来源、工具、输出、预算、重试与状态；不把主编、资料和记录岗位混为一个“AI处理中”。界面与真实运行同源，并同步本方案、开发路线和验证证据。自动更新机制、真实模型语义效果与长期小说质量分别验收，不承诺零遗漏或永久不崩。

## 21. 全新实现结构与切换边界

工程落地与已核实缺口详见第22节。以下对象与模块仍是设计边界，不是已具备的接口。

### 21.1 新模块划分

新领域核心位于`rebuild/packages/time-machine-core/`，独立定义对象、状态、输入输出、工具权限与验证，不导入coauthoring-v7的规划、结算或页面模块。应用编排、持久化和作者界面分别围绕新核心实现，旧代码作为查证资料而非依赖。

| 新模块 | 职责 | 不承担 |
|---|---|---|
| 创作意图与推荐 | 资料卡、题材融合、作者偏好、主编推荐与建议 | 决定正文已发生什么 |
| 全书方案 | 三套候选、主支线、主要故事、分卷、开篇、期待与采用 | 提前写完章纲或冒充正史 |
| 故事关系 | 稳定编号、线的归属交汇、伏笔与问题关联 | 用树形外观强迫关系只有一个父节点 |
| 正文记录 | 正式正文变化提案、证据校验、语义复核与版本事件 | 改正文、替作者采纳草稿 |
| 状态投影 | 从有效事件构建时光树、轨迹和资料查询 | 模型文学判断、根据颜色推断状态 |
| 工作编排 | 任务状态、预算、批次、续作、重试和配置冻结 | 自行猜测未完成结果 |
| 作者与后台界面 | 同源状态、清晰选择、结果与记录查看 | 自己重新推算收束或把缓存当最新 |

账号、会话、正文保存、模型凭据与现有账本是平台外部能力，不在本次重复实现。新模块通过明确定义的适配接口使用，禁止导入旧时光机业务。模型调用统一计费入口只提供传输和计量，提示词、调用顺序和上下文选择重新设计。

### 21.2 新对象与状态语义

作者意图版本记录题材融合、主辅风格、结构偏好、人物偏好、选择和自定义。推荐版本绑定意图及源资料版本；主编建议与作者指令分别存储。方案有draft/reviewable/adopted/superseded身份，不和模型任务状态混用。

故事线ID与编号由系统分配，包含主线/贯穿支线/阶段支线职责、目标、人物和关系。线的正文生命周期为尚未展开、推进、暂缓、收束、明确停止；交汇是事件，待核对是可信状态而非生命周期。胜败是收束结果，不是生命周期。伏笔为计划、已埋、发展、部分回应、已回应、明确撤销；计划不算已埋，撤销不算回应。

轨迹事件包含事件ID、书籍、相关对象ID、正文版本及位置、前置状态版本、变化类型、结论、执行成员、判断依据及核对状态。事件追加保存，投影可重建；不以最后一次写入的整块JSON作为不可追溯真相。

### 21.3 正文事件入口

新模块接收平台正式采用事件，必须再次查证当前正文版本和内容哈希，不能信任浏览器声明。消费凭据按书籍和正文版本防重。已接管书籍仅一个记录执行所有者，旧维护不得同时写新模块对象；切换不通过修改作者任务制造窗口。

支持正文新采用、替换和撤回的独立事件。重修使直接或间接依赖失效，新投影标记待核对，按正文顺序补核；已经保存的正文不回滚。节点失败只阻断确实依赖该状态的生成。未知结论不写为确定事实，资料供给必须显露未知。

### 21.4 历史与发布

先在独立测试环境完成新流程。已有作者书籍不自动删除、不强制重新设计；旧方案可只读保留，若用户选择迁入新树，由新成员基于正式正文及选定规划重新识别，并保存迁移来源，不能直接把旧状态算作新系统已核对。

发布开关按书籍控制新业务所有者，新旧界面与执行器不共同管理同一新任务。开关变更需检查在途任务并保留回退路径；已产生新版本不能回退成旧代码误读新结构。完整依赖检查确认新模块没有旧业务导入后，才允许接入和上线。

### 21.5 结果优先的验收案例

用全新合同运行至少这些场景：单主线与多主线、群像与单主角、无战力成长的日常、融合题材、三套部分失败、作者中途改选、主要故事跨卷、支线收束但伏笔未回收、伏笔关联多线、交汇后继续推进、来源正文改写、晚到旧结果、重复采用事件、未知归属、历史迁入、跨账号/书隔离。

各项验收关注作者看到的结果及后续成员实际收到的资料，不以旧测试数量或复用比例作为成功标准。没有完成或没有实测的部分在后台和交接里明确保留未完成，不能承诺今晚必然全部完成而省略验证。

## 22. v2.0可实施合同与全面核查结论

### 22.1 核查证据与纠正

本轮核对本文、详细开发规格入口、交接记录、新核心`rebuild/packages/time-machine-core/src/domain.ts`及新API的`rebuild/apps/api/src/create-server.ts`，未访问生产、未读新的作者资料、未运行真实模型。既有原型交互测试与7项核心单测是历史证据，不能冒充本轮端到端测试。

| 问题 | 修订决定 |
|---|---|
| 原型三套与树为手写独立样例 | 正式版所有页面按同一采用版本/书籍查询；候选不得生成“已发生”树 |
| 新核心仅有线、伏笔与事件，缺分卷/链职责和应用层 | 先补协议和存储事务，再接成员；现有核心不是可以直接上线的后端 |
| `parentIds`无环校验容易被当作故事交织规则 | 卷/链包含关系无环；相互影响允许双向，汇合可多端点；有时序的具体依赖事件才检查不可能的时间循环 |
| 原文位置吻合被误当作事实正确 | 位置校验只是结构验证，另做语义支持、否定、传闻、人物认知与世界事实区分 |
| 当前投影依赖单章版本且默认依赖输入完整 | 增加事件所依赖的多章来源、对象状态版本、正式章顺序；持久化维护完整依赖和连续核对水位 |
| 原型职责、终卷安排仍是样例 | 实际输出必须引用本套目录并覆盖每条核心期待，不能把样例数量和阶段当模板 |
| 新API仅见账号/书架/开书路由，且独立PG配置 | 不宣称新时光机已接入。使用新领域包＋当前生产宿主的薄适配层；不要求先切整个数据库和身份系统 |
| 缺修改入口、脱敏细则、任务事件协议 | 以下给出明确入口、对象、状态与错误恢复；实现前补测试，不靠页面推断业务状态 |

### 22.2 上游来源、可信身份与输入清单

每个设计任务冻结`SourceManifest`：owner/book、正式开书版本、已确认设定版本集合、作者偏好版本、采用规划版本（如有）、正文基线水位（如有）、资产/模板版本。版本和摘要hash由系统生成，模型不得自报“最新”。只选已确认条目；草稿、审查意见、示例、工具说明和其他候选分别标身份，不能混成事实。

上游短卡仍用六栏，另附机器可用的引用索引：每个关键断言映射源对象/版本/字段或正文范围。不可遗漏主角身份、明确禁止项、核心能力的故事相关限制、既定开局与结局、创意尺度和作者主辅偏好。与全书方向无关的价格、日常细则不预载；原件可定向补读。短卡不是无损摘要，不能据短卡没写就断言设定不存在。

关键源相互冲突时建立具体待确认问题，展示冲突项与各自出处；不让资料成员私自决定作者意图。已接受正文与设定冲突不静默覆盖任一原件，标出计划修订或正文纠错路径；创意夸张不等于事实错误。未知配角可以设计为候选，新名字绑定候选实体ID，不能冒充已确认人物。

上游修改先使依赖卡标记过期；成员运行中继续使用冻结快照，完成结果标“基于旧资料”。采用前重检manifest，关键变化必须重新核对相应范围；无关改动经影响核对可复用，不能只凭时间戳全书重做。缓存键至少含owner/book、manifest、任务层级、模板版本与脱敏策略版本。

### 22.3 对象、事务和后端接入

在新领域包定义运行时schema与TypeScript类型，入库、工具返回和模型输出均按schema验证，不能只依靠TS编译。下列是逻辑对象，物理表/索引随宿主存储适配落地，但事务边界不可拆掉：

| 对象 | 必需信息与约束 |
|---|---|
| SourceManifest / ContextCard | 来源版本清单、断言引用、有效/过期/待核对；原件不覆盖 |
| IntentRevision / Recommendation | 意图版本、推荐版本、作者选择与主编建议分离 |
| CandidateRevision | 候选ID、不可变修订、来源、成员快照、基线、线目录、期待、卷卡、完整性及核查结果 |
| StoryLine / StoryRelation | 稳定ID、书内编号、角色/范围/目标/回应条件；有类型的关系及端点；编号不当数据库主键 |
| VolumePlan / ChainPlan | 稳定ID、显示顺序、起始/目标/职责/结束条件/交接、规划版本；链在后续生成 |
| Adoption | 当前采用指针、候选修订、编号映射、作者操作；旧采用不可变 |
| Task / Step / Attempt | 任务与批次ID、输入版本、实际成员/模型、租约、预算、状态、已保存产物与错误分类 |
| FormalEvent / Assessment / Projection | 正式来源版本集合、证据、依赖、语义判断、可信状态、结算批次与可重建投影 |
| Outbox / Consumption | 提交后事件、消费键、重试、连续水位；业务写入和outbox同事务 |

必要唯一约束：`(owner,book,displayNumber)`分别约束线与伏笔编号空间；`(owner,book,operation,idempotencyKey)`防重；`(task,step,batch,inputRevision)`防同批重复产物；正式消费键包含事件类型、正文稳定ID与版本。ID和owner/book都从会话及服务端目标绑定，拒绝前端跨书ID拼接。采用事务校验完整性、来源和expectedRevision，分配映射并更新指针、写outbox一次提交。并发冲突返回409和当前版本，不覆盖作者改动。

新应用层只依赖端口：Identity/BookAccess、SourceRead、TaskRepository、PlanRepository、FormalTextRead、ModelGateway/Billing、AssetRead、EventOutbox。凭据仅网关持有。生产适配可以共用当前账号、任务传输与账本，但不可导入旧时光机提示词、上下文编排、生成与结算逻辑。独立API宿主保留适配能力，不能连接另一套PG后假定与生产作者身份相同。一个书籍一个执行所有者，不双写新旧业务对象。

拟定接口均为新增模块合同，尚未实现：
- `GET /api/time-machine/books/:bookId/state`：统一页面快照、revision、可操作能力及来源新鲜度，纯读取不触发AI。
- `POST .../recommendation-runs`：首次入页显式建立或恢复任务；相同输入幂等返回同一run。
- `PUT .../intent`：expectedRevision保存作者选择；不自动按每次勾选调用模型。
- `POST .../design-runs`、`POST .../runs/:id/retry`：开始或只续失败步骤，不能借retry更换输入版本。
- `GET .../candidates/:id/revisions/:revision`、`POST .../candidates/:id/revisions`：读取及保存人工修改/AI修订请求；原候选保留。
- `POST .../adoptions`：原子采用完整修订；前端仅传ID与expectedRevision，不上传“已核查通过”。
- `GET .../events?after=sequence`：鉴权事件流，断线按游标续读；超过保留窗口返回重新取快照指令。
- `GET .../projection`、`GET .../history`、`POST .../correction-requests`：按采用/正文基线读树轨迹及提交纠错。

内部上下文供给不开放给浏览器任意拼接：`compile(scope,level,targetId,planRevision,formalBaseline)`返回卡、引用与预算清单；内部正式正文事件由平台outbox进入，不能以浏览器POST代替正式采用事实。

### 22.4 成员执行与上下文预算

执行顺序：来源检查→必要的资料成员填卡→主编推荐→作者选择→三个独立编剧各自产出骨架→各自分批补卷→逐套必要复核→作者采用。主编比较三套差异是质量建议，不让另外两套未完成成为完整一套采用的硬阻断。骨架先冻结线ID与大致卷职责，分卷批次可提修订但不能自行漂移全书结局。

每个成员接收：岗位任务、来源身份标签、作者明确要求、本层产物合同、预算和可用只读工具。工具允许按用途查方法目录、读方法卡、按实体/来源读作品资料；禁止任意SQL、文件、网络或跨书读取。模型可原创，不需要引用所有方法，不允许借检索结果中的自然语言扩大权限。工具返回是资料而非系统指令；模型产物用文本安全渲染，不执行HTML/脚本。

资料整理、推荐、单套骨架、卷补全分别维护可配置预算。请求前验证 `系统+输入卡+保留工具返回+最大输出+安全余量 ≤ 路由可用窗口`；按模型tokenizer计量，未知tokenizer用保守估算并标识，不能把字符数当精确token。缺少经验证的路由预算时任务等待配置，不能盲发。后台显示每段token、输出上限、补查预算与实际消耗。

首轮工程默认：每步骤最多6次只读工具调用、同一暂时性错误最多2次自动重试、输出格式局部修复最多1次；达到限额保存进度并明确暂停原因。以上是可调初值，真实探针后逐节点定稿，不是文学规则。429/暂时网络按限流退避；输出截断应拆当前批，不能原样反复重试；401/403不重试；模型结果未知先核对网关attempt与账本，不重复收费重发。任务时间、token与费用上限共同控制，并在派发前计入预算。

卷批次大小由输出估算和既有样例决定，不固定一次一卷或一次全书。每批只携本方案基线、当前卷职责、直接前后承接和必要事实；不附其他两套方案、不无限累积检索历史。检索结束把选中的短卡和引用编译为下一步骤输入，丢弃无用工具输出，但保留服务端审计。必要依据容纳不下就缩小工作范围或补读，不能静默截断。

### 22.5 幻觉、事实审查与记录结算

创作允许提出新的候选情节；幻觉控制针对把不存在的来源、人物关系、已发生事件或工具调用说成既成事实。每条关键判断标为源事实/规划候选/人物认知/推测/未知。设计复核重点是姓名身份、作者要求、源事实冲突、故事职责遗漏；不因抽象脑洞违背现实常识就拒绝，也不保证“绝无幻觉”。

正文审查与结算分开：正文接受前可审查事实连续性与文本质量；正文接受后记录成员只识别已接受文本的变化，不能趁结算改正文。来自梦境、传言、谎言、否定、条件句或人物猜测的内容，按其认知主体和可信身份记录，不提升为世界事实。倒叙同时保留阅读顺序和故事内时间；正文后文揭露旧事实不直接抹去当时人物认知。

关键结算（死亡、核心身份揭露、线收束、伏笔完全兑现、逆转既有状态）须有定向语义复核或明确人工核准记录；普通有据的进展不强制重复全章审查。引用切片吻合不是语义通过；多来源冲突或证据不足保留待核对。抽取遗漏亦是风险：对本章当前链目标、已知相关线与明确新承诺做覆盖核对，不要求扫描全书每条线。

结算管线：接受正文并写outbox→读取当前正式版本→抽取候选变化→核验范围/证据/语义→原子写评估事件、依赖与投影revision→写通知outbox→更新连续核对水位。每步可恢复；正文保存不依赖结算成功。读者界面可看到正文已保存，而记录处理中/部分失败，不能把任务结束误标全部核对完成。

水位是按当前正式章节序列连续完成的位置，不是最大章号；删除、重排、番外和倒叙均使用稳定章ID及顺序版本。同一结算对重复事件幂等，乱序到达先等待依赖，独立分支可完成但不跳过缺口。新发现线先分配书内身份再记录事件；未知归属进入待归属，不硬挂到任意卷。

正文重修/撤回使依赖其旧版本的事件失效，再传递至下游依赖；先显示最后有效投影并标待核对，不能把无效果实仍显示为确认。重算产生新评估，旧证据保留。受影响的后续生成等待必要事实可靠，其余阅读、保存和无关设计可继续。引用跨度约定为原始文本UTF-16索引与hash，不做去空白后比对；跨章结论持有多个sourceRef。模块只消费已验证正式版本，不接受外部自报“supported”。

评估修正以`supersedesAssessmentId`连接旧评估，只取当前有效评估构建投影；旧的待核对标记在新评估明确解决后解除，不能因曾经失败永久卡住。对于明确失效但未重算的事实，保留历史展示、从当前供给中排除。安全校验拒绝未知change.kind、非法状态转换和缺失依赖；模型返回枚举正确不等于允许从已收束直接恢复推进，重开必须有原因与来源。

### 22.6 脱敏与最小数据供给

模型只接收当前书创作必需内容。账户密码、会话、API密钥、支付资料、手机、邮箱、IP、内部部署路径和其他书内容不进入提示词；任务身份用内部不透明引用，不能直接把账户整行序列化。作品角色姓名、虚构地址不能一律脱敏破坏故事；发现作者输入中的真实联系方式或外部敏感身份且与创作无关时，以稳定占位符替换，映射只留服务端受控存储。无法判断且会影响内容含义时提示局部处理，不擅改原文。

工具和模型出入口共用脱敏层，检查输入文本、工具参数、错误和日志；新鲜私密键即使混在作品文本也不允许传给模型。原始作品保留在原权限域，脱敏副本可带安全span映射，但不可把模型基于替换文本的索引当原文证据，必须还原核对。后台默认只显示脱敏摘要和长度，查看必要原文需明确权限与审计，不把管理员权限等同可任意浏览所有作品。

普通运行日志不落完整上下文/正文/工具原文，不保存思维链。敏感诊断临时包须有访问控制、过期与审计，实施默认7天自动清理非业务调试包，正式正文和结算历史不受影响。浏览器不缓存其他作者资料；登出清理本地私有缓存。无新增公开分享、第三方遥测或作品外传目标。

### 22.7 状态、实时更新与作者编辑入口

任务状态：queued/running/waiting_input/retryable_failed/failed/succeeded/cancelled；步骤同样有独立状态、lease与attempt。候选状态另为draft/partial/ready/stale/adopted/superseded。正文投影为current/updating/needs_review/partial。不得用一个status同时表达三者。租约过期先核对真实调用状态再接管，旧attempt无权覆盖新产物。

UI只依统一快照与递增sequence更新。推送失败以带版本的定时读取降级；隐藏页降低频率，返回即补读。过期响应不能覆盖新revision；不同方案各自有进度，不伪装共享百分比。总批次变化显示已完成项与当前工作，而不是进度倒退的假百分比。失败保留已完成内容及明确续做入口；首次入页只幂等建立推荐，不因刷新重生成。

| 修改内容 | 作者入口 | 写入及后果 |
|---|---|---|
| 开书与设定源 | 返回资料/设定，保留当前意见 | 原模块新版本，相关候选标旧来源，不清空原结果 |
| 故事选择/主次偏好 | 调整故事线 | 保存意图草稿，点击开始设计才新任务 |
| 候选文字、线目标、卷安排 | 当前卡的“修改”与整体“提出调整” | 人工修改原样留修订；AI修改先展示建议版本，不自动覆盖 |
| 线的增删合并/拆分/排序 | 故事目录编辑 | 临时ID映射、关系引用校验；采用后删除为归档，不删除正文关联 |
| 分卷顺序、职责、结束条件 | 卷卡修改与顺序调整 | 更新计划修订，列出受影响卷链；不重排已接受正文 |
| 改用另一套/重新设计 | 方案级操作 | 有正文时展示未来影响并建新规划版本，不改变历史事实 |
| 记录不准确 | 线/轨迹详情“反馈记录有误” | 提交纠错，核对来源后新评估；不能点红果直接改事实 |

作者修改保存与采用分开；关键结构/引用未通过时可以保存草稿但不能采用。只对影响已采用内容的重要操作展示具体差异，不每次改一句话都弹确认。手机保持输入与滚动、可到达主要操作，三套用ABC＋成员，标题在正文。多线列表可搜索/分页，不能只截取前几条冒充全部。等待、空态、部分成功、过期和恢复必须在真实API联调时验证。

### 22.8 后台必要信息与可调项

后台同源显示：方案版本及实施状态；各步骤真实负责成员/模型；来源manifest及新鲜度；工具实际调用与所选方法；输入/输出token、预算、时延、重试原因；候选完成度/采用版本；结算缺口、待核对项、重放状态。默认展示结论与定位入口，不把模型内部字段倾倒给作者。

可配置：启用成员与模型路由、各节点预算/并发/超时/重试、短卡模板、方法资产名称简介与版本、提示词版本、发布开关。修改需要权限和审计，新配置只影响新任务，历史冻结快照保留。后台不可把“未实测”切成“通过”、不可无证据修改正文事实、不可取消作者任务制造发布窗口。方案/路线/功能介绍按已验证交付同步，不把本规格所有功能一键标完成。

### 22.9 分批开发与放行证据

统一采用`docs/ACCEPTANCE.md`测试矩阵；以下是本模块具体交付和证据，不另建重复验收制度。

| 批次 | 交付与依赖 | 必须提供的结果证据 |
|---|---|---|
| P0 协议与平台端口 | schemas、仓储迁移、权限、manifest、采用事务、outbox；清理核心parentIds语义 | 空库/升级/回滚兼容；跨书拒绝；并发采用和编号；重复事件；无旧业务依赖 |
| P1 一套真实设计闭环 | 资料短卡、主编推荐、作者选择、一个编剧工具循环、分批卷、必要复核和采用 | 真实模型产物落库读回；来源/预算/引用准确；超限拆批、断点恢复；真实卷供给包可读 |
| P2 三成员与作者后台UI | 三套独立状态、编辑修订、完整方案切换、同源后台记录 | 部分失败可续、成员归属真实、晚响应不串、改选不覆盖；手机/电脑与空/忙/失败/过期可用 |
| P3 卷链交接 | 卷卡→链卡→章任务供给与计划/事实分离 | 用真实一卷和至少两条链验证目标、结束条件、实际结果接续；不把只读资料包当下层已可运行 |
| P4 正文结算与树轨迹 | 正文outbox、抽取复核、版本依赖、事件投影、纠错和展示 | 正式章接受、重复/乱序、传言否定、收束带伏笔、重修失效、恢复与连续水位；真实章样例人工核对 |
| P5 发布与运营收尾 | 安全开关、只读兼容、后台功能状态、追溯和发布物料 | 发布前测试及生产冒烟；静态与API分开，按既有在途任务安全规则；代码回滚不恢复作者数据 |

P0/P1完成前不发布只接手写样例的页面；P2可作为全书方向独立版本发布，但只有真实后续接口可用时显示下一步按钮。P4未完成的树显示未开放或只读旧入口，不伪装新记录已维护。完整时光机交付须P0—P5均有证据；每批用相同文档原位更新状态。

首轮真实质量集：本书管理员样例（仅获准范围）、单主线/多主线、群像、日常无战力增长、悬疑含误导、融合荒诞题材。重点检查事实准确、创意不被模板压平、故事差异、各层可执行与等待成本。故事线资产与方法库逐项语义审计单独记已核对/待核对，未逐条读完不得声称全库完全正确；长期百万字一致性不能用几章样例验收代替。

## 23. 分层编号、叙事锚点与自主记忆维护（2026-09-11修订，待开发）

本节落实最新用户流程：卷A→链A1→章1，主线1/支线1；每层按来源自主查询方法、设计、检查，正文后主动维护事实与关系。覆盖此前第23节“卷1/链全书连续”的建议。只有产品方案与技术合同已记录，新增功能未实现、未部署；不得把历史测试当本节验收。第22节其他安全/版本要求继续有效。

### 23.1 职责、权威与记录起点

AI负责语义：识别同一实体、新线与旧线、目标/开场/收束、关系、事实、方法选择和偏移判断。系统负责事务：授权、分配号码、验证引用、保存版本、防重、任务及状态投影。无需为数字计数调用小模型。设计者可主动补查、自检修订；资料成员整理短卡；记录成员提取实际变化；独立核对成员验证证据；主编处理跨层方向与重大调整。岗位来自真实成员目录，不为此强制固定人数或模型品牌。

书籍建立即记录ID和作者意图版本；开书/设定采用保存正式来源；全书候选保存候选关系，作者采用后分配卷/主线/支线编号；卷方案采用后分配链号；链内章方案采用后建立章号与归属。正文接受后才记录实际进度。新增支线按后续故事需要建立，不在时光机穷举所有小剧情。

“正史库”如果保留为产品总入口，必须分正式规划与正文事实两个区。作者认可“卷A结尾当县长”只使其成为已采用规划，不证明现在已任职。规则是设定约束，作者指令是创作要求，正文是已接受叙事证据；若正文与规则冲突，保留双方并调查，不自动改规则粉饰错误。摘要、向量索引、Wiki与图谱都是有来源的派生视图，不成为另一份事实权威。

### 23.2 编号、排序与变更

| 对象 | 正式显示 | 分配时点/作用域 |
| --- | --- | --- |
| 卷 | 卷A · 风云再起 | 全书方案采用；书内A…Z、AA、AB，统一大写 |
| 链 | 链A1 · 活下来 | 卷A方案采用；卷内1起，卷B为B1 |
| 章 | 章115 · 好看吗 | 链内章节规划采用；书内连续阅读编号，不每卷重置 |
| 主线 | 主线1 · 张三修机甲 | 全书方案采用；书内独立序列 |
| 支线 | 支线3 · 天台上的交易 | 采用规划或正文有证据新登记；书内独立序列 |
| 伏笔及其他实体 | 伏笔7、人物/道具标题 | 稳定内部ID，必要编号；不把每句话创建为对象 |

内部entity_id永不变。显示代码、标题、阅读顺序独立保存且有版本；链的复合代码包含所属卷，不把字符串前缀当数据库父子关系。主线/支线的作用域分类与“贯穿/阶段”持续范围分开；归档号码不复用。候选ID在candidate_id+revision内解析，方案A与卷A是不同对象类型，界面不可省略类型造成混淆。

采用事务分配映射并同时写关系与outbox，幂等键返回原结果；唯一约束至少覆盖owner/book、type、namespace、display_code及有效编号版本。分配在事务锁/计数器内完成，不无锁max+1。现有SQLite BEGIN IMMEDIATE中的max+1并非无锁竞态，但现有作用域不足，适配其他数据库时不能照搬锁假设。

未写章节插入/重排随新的规划版本原子重新计算显示顺序并保存别名，不要求作者维护映射。已接受正文的章节显示号默认冻结；中间插入已写章节使用“章115-1”等插入标识，除非作者批准整书重编号，避免旧阅读引用悄悄改义。链跨卷移动时由新规划归属计算新显示号并保留旧别名及有效版本；卷字母默认不随排序变化，已形成内容的卷重排保存order_key。所有旧引用使用ID或代码+编号版本，别名有歧义时工具返回候选供成员补查，不取第一个匹配。

合并保留旧ID并建立merged_into，拆分建立新ID和derived_from，历史证据仍归原对象；不得用删除重建抹去记录。已采用线改主支线身份保留ID，分配新类型代码并记录别名。作者只决定有实质故事影响的改动，不负责手工更新号码或关联。

### 23.3 每层输入、输出与字数预算

| 节点 | 首次注入 | 设计产物 | 留到下一层 |
| --- | --- | --- | --- |
| 时光机 | 已核对书籍短卡、作者意图/体量、主角开局与相关世界边界 | 全书基线、长期期待、主支线、各卷开场/过程方向/关键转折/高潮兑现/人物变化/收束/交接、分卷字数 | 具体链、场景、逐章事件 |
| 卷设计 | 本卷卡、相关线职责、上卷实际结果/未决项、必要角色与规则 | 链列表、每链开始/过程/结束、卷内作用、依赖与预计字数 | 逐章叙述与对白 |
| 链内章设计 | 当前链卡、本卷职责、实际前情、相关线/角色/道具/规则 | 若干章及每章任务、剧情事件细节、衔接、预计字数和预期变化 | 正文表达 |
| 正文 | 当前章方案、有效事实、邻章承接、必要细节 | 候选正文、自检修订和相关引用 | 正文未接受不得写实际账本 |

每层执行成员先判断任务和资料缺口，使用方法工具查询/读取合适参考，自主搭配或不选方法。已经检索且版本有效的方法可复用，无需为“显得自主”重复查询。查库不只限生成前，设计、自检、审查中发现疑点都可补查；当前一次性选材逻辑需要调整。

字数统一使用与作者正文统计同一word_count_policy版本，不能把token当字数。规划保存target/min/max与单位和是否作者硬要求；常规“约200万字”为软目标。卷数与链数由成员根据故事容量和题材判断，不默认6—8卷、不强制等长。系统检验每层数字合法、子层target总和与父层target一致；差异由成员调整分配或提出父层新预算版本，不靠系统裁正文凑数。实际字数单独累计，超出软预算触发复估，不擅自收束或截稿。

开场/收束使用后述锚点，不只是标题；人物弧光是有故事依据的变化，可以稳定、复杂或未定，不强制每卷性格逆转或牺牲。高潮/转折可重合，非战斗题材不必战斗。gain/loss等字段允许不适用并给简短说明，不强迫编造损失。

### 23.4 叙事锚点：短语可检索，条件可核对

每个卷、链、章任务和已规划故事线都有一句话开场与收束；中间保留本层必要过程和关键变化，不一开始拆完百万字细节。设计成员同时生成最小可核对锚点：
- anchor_id、owner_entity_id、plan_revision、kind（entry/exit/milestone）、summary；
- subject_ids、目标状态/事件、适用时段与范围、关键限定条件、判定逻辑（all/any/ordered）、相关anchor引用；
- importance（required/flexible）、未完成如何承接、允许的等效达成方式（如有），来源与作者明确约束；
- 索引keywords/aliases由资料或设计成员补充，只辅助发现。自然语言条件仍由成员理解，不写关键词正则充当结算。

默认把独立承诺拆成必要的原子条件，再按逻辑组合；不强制固定锚点数量。规划条件简短，不把细节、成本、限制到处重复；必要限定只存一次并按ID引用。正文核对保存anchor_id、plan_revision、状态（not_observed/partial/satisfied/contradicted/uncertain）、evidence_refs、有效时间、检查成员与修订。未出现不是违反；“未知”不能伪装完成。

例：卷A“张三正式成为县长”不能凭一句“以后你就是县长”满足。需核对叙事是否明确任命/就任已生效、不是假扮/梦想/传言、主体身份正确。若作者设定的完成条件只是获得任命，则不擅自增加必须到任办公；条件必须忠于本卷目标。达到主要目标后进入ready_to_close，核对其他required承诺与必要后果，自动提出/安排剩余收尾章；不得因命中目标删掉待写结果，也不得无限追加新冲突。

卷A实际结束形成交接卡，卷B开场规划与该卡对照。缺前置则补设计承接或提出调整；终卷没有“接下一卷”。按已接受正文满足退出条件并完成收尾后，记录成员提出closed，核对通过由系统自动更新；有未兑现伏笔不必阻止本卷结束，伏笔继续单独跟踪。

### 23.5 主支线跨卷：关键落点与弹性区间

采用“重要节点预先安排、具体路径随下层细化”，不强制每条线穿过固定卷数，也不只靠成员顺便想起。时光机为每条重要线设计start/process/end与关键里程碑，记录首次建立、重要转折、预期收束的建议卷或卷区间；只有作者明确要求、因果依赖和终局必要承诺标required，其余flexible。

每卷职责包含line_id、action（start/advance/pause/close）、本卷预期结果、关联anchor、约束强度与理由。允许该卷无某条线职责，不必把全库塞入每卷；同线跨卷延续不是新增交织关系。全书层只有重要主支线，后续卷/链可增加局部支线；新线需查目录避免重复，并说明是否影响上层。

后续设计时读取本卷直接职责与必要一跳关系，实际前情变化时成员可提出移动flexible里程碑；局部调整不改变作者核心目标，可自动形成可追溯规划修订。跨卷重大移位、改变全书主线/结局/已明确承诺需作者决定。记录修改前后、原因及受影响对象。显示“计划经过卷A—C；实际在章1、章4、章9推进”，不把计划跨度当实际参与。

### 23.6 自主工作循环、反省与工具

所有层共用受控执行器，任务包含目标、当前对象/版本、来源清单、成员权限、预算与完成判据。循环为读取短卡→判断缺口→查询资料/方法→观察工具返回→设计→自检→必要补查/局部修订→独立核对→交付。重用既有成员网关/计费/租约；不复制旧时光机业务，不把新循环伪装成固定模板执行。

只读工具合同（待实现或扩展）：resolve_reference(type,code,numberingRevision)；search_entities(type,query,cursor)；read_entity(id,revision/asOf)；read_relations(id,plan|actual,范围,depth,cursor)；read_evidence(sourceId,version,span)；read_ledger(entityIds,asOf)；方法search/read。全库可分页检索，不能一次注入全书。工具返回明确范围、来源版本、是否截断/下一页、记录是否已核对。

写工具只接受propose_changes与request_review等结构化提议，由系统权限/版本校验和核对后自动提交；AI不能任意SQL或自行认定审核通过。候选局部修订可自动保存；实际账本不依赖作者逐条批准。核对成员可查证，区分真实冲突/证据不足/文学建议；自检和独立核对不是同一项。不同模型可配置，同模型会话不能冒称异模型独立。

各角色有调用/费用/时间/输入输出预算；重复同查询无新证据时反馈并终止无效循环，首次核对有问题优先局部修订，超过修订预算保存待处理结果。不因需要更多思考而无限调用；事实缺口未解决不强行输出“通过”。只保留行动、结果、简短依据和版本，不存思维链。

### 23.7 正文结算、自动维护与纠错

正式接受正文的事务同时写不可变版本和outbox。同书按有效前情依赖执行：
1. 读取本章有效版本、当前章/链/卷职责与必要锚点、相关账本及连续水位；超预算由资料成员切片，保留否定、传言和转折语境。
2. 记录成员识别新增事实、状态变化、线推进/交织、锚点满足、伏笔与人物/关系/能力/道具持有及位置等当前作品需要的内容；不预先要求固定16账本或所有对象全量提取。
3. 输出typed提议：对象/临时新对象、变化类型、前后状态、叙事时间、事实模态、证据位置、依赖版本与未知项。新人物/新线先解析身份和检索既有目录，不能名字相似即合并。
4. 系统验证会话书籍范围、引用、证据原文位置与版本；核对成员同时检查“提议是否有支持”和“是否漏掉重要变化”，不能只验证已提取列表。多段正文需覆盖清单，不凭一个可信度数值自动放行。
5. 核对通过的提议自动提交结算版本和变化事件、更新投影，再由outbox更新Wiki/关系/搜索索引与时光树。未解决争议只标记受影响对象，其他无依赖的可靠记录可提交；全章仍标partial。
6. 对账本提取错漏，成员查原文后自动提出纠正并经核对追加更正事件，旧记录保留。作者不手工同步人物、道具、线进度或树；后台提供必要的查看与人工干预入口。
7. 正文自身矛盾：保留现有文本和冲突，生成局部修改候选及影响说明；不能替作者静默改正式正文。作者采用修订后自动重算。作者要求改变核心设定同理，不能把记录更新当作擅改故事授权。

同章同版本的结算幂等键包含owner/book/chapter/textVersion/contractVersion；重放不会重复事实或资源增减。正文修订沿依赖图失效旧派生结果并重算，保留旧证据。新版本提交有expectedRevision；旧任务晚到不覆盖。乱序接受允许正文保存，依赖事实的结算等前情补齐；正文和结算水位分开展示。

记录narrative_time与written_chapter，回忆在章115出现不等于事件发生在章115时点。人物信念/猜测不进入客观事实；道具转移、消耗与失而复得按事件处理；能力使用不等于永久获得；叙事不可靠处保持待核对。事实矛盾可能长期无确定答案，系统保存范围和未知，而不是为“账本必须完整”编造解释。

### 23.8 结束、偏移与持续检查

采用规划时即保存计划关系；实际关系只由有效正文证据产生。章节“涉及该线”与“推进该线”分开记录；仅提名字不算推进。查询返回涉及章集合、实质推进章集合、跨越卷/链、最近变化、已满足/未满足锚点与未决项，不用最大章号减最小章号冒充出现次数。

每章结算检查本章直接影响；累计5个新核对章节默认触发局部推进审视（可配置，只是检查窗口），关键锚点满足、链/卷收尾、依赖冲突立即触发，任务合并去重。模型判断合理铺垫、实际停滞、偏移或记录缺口；不能强制五章推进每条线或每链不可逆增长。主编在卷终等节点扩大检查范围，不每章扫描整本书。

实质达成→ready_to_close→必要收尾→closed；正式记录自动更新；下一层生成使用已核对的实际结束卡与下一开场卡。新发展改变计划时，局部flexible计划自动版本化调整并告知摘要，重大目标变化请求作者决定。章节预算耗尽但故事未结束只触发重新估量；不得为满足预算谎报完成。伏笔兑现和支线结束为两种独立状态。

### 23.9 数据、混合检索与上下文技术路线

关系型存储为事务权威；正文与规划版本、实体、编号/别名、锚点、计划关系、实际事件/关系、证据/依赖、结算/核对记录、投影/水位和outbox形成新模块。复用平台身份、正文存储及模型端口，不同时建立两套作者事实库。现有schema逐项扩展，通过新迁移，不重写已合并0114—0117；跨协议使用显式v1→v2适配，缺字段标legacy_needs_enrichment，不能填空冒充完整。

检索路线：中文编号/内部ID先精确解析；问题描述采用文本检索和向量检索取候选，再结合相关关系及当前时间版本过滤/排序，成员读取必要原文。向量相似度不决定事实或身份。SQL关系表先承载图谱，只有真实查询规模证明需要才引入专用图数据库；Wiki是事件与证据生成的可读页面，不允许模型更新Wiki后反向覆盖原文。向量存储由接口封装，先接与当前生产数据库部署兼容的索引，不为此立即迁移整库。

索引文档包括owner/book/entity/sourceVersion、sourceRole、有效区间、实体别名和正文位置；必须在检索端过滤权限、候选/正式/事实类别与版本，不能把他书候选返回模型后才删。旧索引命中读回权威库复核；索引延迟或缺失时精确/文本回源并标明未完成，不把“没检索到”当“不存在”。向量不可用不能假装已执行混合检索，相关功能状态降级显示。删除/失效通过outbox传播，重复消费安全。

预算按任务实际模型窗口计算：系统指令+短卡+证据+方法+工具返回+输出/推理预留+安全余量，总和调用前验证。只注入父级职责、当前实际状态和直接相关关系；祖先用短卡而非全部全文。工具查询结果有页数/字节/token上限，选中证据常驻，过时工具页移出工作上下文；关键证据缺失先补读或拆任务，不静默截断。规划锚点与实际满足证据分层标识。

### 23.10 API、状态与后台交付

在现有/api/time-machine/books/:bookId命名空间内扩展：GET entities、resolve-reference、entities/:id/relations、entities/:id/history、entities/:id/anchors、settlement-status、reviews；响应含版本/来源角色/下一页及水位。POST proposals、reviews/:id/resolutions、settlements/:id/retry与reconcile；写入有幂等键/expectedRevision/权限，reconcile只处理已接受来源，不允许客户端注入任意正式事实。内部port订阅正文accepted/revised，避免作者每章点“同步”。

任务状态queued/preparing/running/checking/revising/partial/succeeded/failed/cancelled，语义review_pending独立记录；succeeded不能仅是HTTP200或模型返回JSON。进度记录对象阶段/完成项、实际成员、调用用量，不捏造百分比。未知调用先核对持久化结果；普通网络失败有限退避，手动续做不重复已成功调用。取消任务保留已保存结果，租约过期按记录恢复，不盲目重发模型。

作者界面：中文编号+标题、卷链章位置、相关主支线、本层预计/实际字数、开场与收束、关键变化和实际状态。树显示A/A1等代码与数字符号，不加长标题；旁边序列表完整显示名称/状态，点击可查章节证据。布局与原型需更新，旧“纯数字”已由本次层级代码要求替代。自动维护默认进行，仅展示“已更新哪些对象”和需要决定的重大问题。

后台：开发路线RB-22/23/24卡、唯一规格阅读、每批状态和证据；运行页提供对象/锚点/关系、源版本、检索与工具摘要、用量预算、结算与索引水位、失败/待核对、自动修订差异、重算影响。提示模板/预算/工具/审查配置版本化并支持预览，运行任务冻结配置。正文与原始证据访问受管理员权限和审计保护，默认脱敏摘要，不放密钥/会话/思维链。

核查结果：现有路线服务解析第3/5/6节，不自动渲染文档开头所有补充；现有AgentWorkflowPage只内嵌AGENT_WORKFLOW_SPEC，未直接展示本规格。文档交付必须同步真实解析卡和工作流入口说明；下一UI批次要接正式规格阅读入口并验证链接可读，不能把Markdown链接写进仓库就声称线上后台已有完整阅读器。

### 23.11 当前代码差异审计与开发批次

2026-09-11只读核对，未重新运行应用测试或访问生产：
| 实现证据 | 现状 | 本次需改 |
| --- | --- | --- |
| core/contracts.ts | schemaVersion=1；卷start/goal/conflict/turningPoint/gain/loss/ending/handoff/duties | v2增加字数、锚点、可选弧光/兑现与required/flexible；loss不再强制非空；线补过程与推荐区间 |
| core/store.ts adopt | 只为line/expectation建整数映射；主支线共用line序列；事务/幂等已有 | 增加卷A、卷内链A1、章和独立主支线作用域、版本别名/排序，保留旧映射可读 |
| core/volume-context.ts | 本卷职责与一跳线关系供给，标明未来规划；超限拒绝 | 补本卷稳定身份/预算/锚点、上卷实际结束卡、所需事实水位与灵活职责 |
| books/time-machine-sources.ts | 读取开书targetWords | 增加统计口径与硬/软要求；不再只在prompt提总字数 |
| books/time-machine-design-service.ts | 设计前最多6轮方法/源补读、每批2卷、主编复查与一次修订 | 循环覆盖设计与自检阶段；核对者能补读原文；引入新字段及局部修订任务 |
| core/domain.ts | line/seed/intersection事件与投影，证据位置校验 | 扩展锚点、卷链章、人物/关系/能力/道具所需记录；语义核对、持久化结算与正文事件接入 |
| admin/rebuild-control-service.ts、AgentWorkflowPage.tsx | 路线解析与旧工作流正文入口已有 | 新规格可读入口、真实执行/结算/检索状态页待接入 |

core路径为rebuild/packages/time-machine-core/src；books路径为apps/api/src/application/books；admin为apps/api/src/application/admin。原始存储、会话、模型网关等平台端口可复用，新业务不复制旧时光机。历史116项测试只证明当时范围，不证明本节能力。

按R192既有阶段新增可独立验收的小批次：
| 批次 | 具体交付 | 依赖/完成依据 |
| --- | --- | --- |
| P0-N | 新版本实体/编号/锚点/字数/关系协议、兼容读、采用事务 | 先于新生成；并发/重放/候选隔离/旧记录读回 |
| P1-R | 一位编剧自主方法及事实查询→设计→自检→核对→采用 | P0-N；真实模型成功与错误修订样例，预算/账务记录 |
| P2-U | 方案A/B/C成员与新标签、详情编辑、后台规格同源阅读 | P1-R；三套状态不串、手机交互、失败恢复 |
| P3-H | 卷A链列表→链A1章列表→按实际前情供给 | P0-N/P1-R；至少两链真实交接、计划与事实分开 |
| P4-S | 正文accepted→提取→独立核对→自动记账→对象状态 | P3-H；先一章贯通再多章乱序/修订/恢复，不依赖完整树 |
| P4-I | 混合检索索引、Wiki、图谱及主动查询接通 | P4-S事件/权限；精确/文本回源先可用，向量路径单独验证 |
| P4-T | 锚点收束、偏移检查、自动局部规划维护与时光树 | P4-S；多章证据、里程碑、未解伏笔与重大调整 |
| P5-O | 后台运行追溯、配置、生产兼容/发布回滚 | 已验批次；正文安全/不双写/任务安全发布 |

本轮仅文档与差异审计，不启动上述实现或生产变更。后续按授权实施时，逐批维护同一worklist与路线，记录文件/迁移/命令/结果/真实模型成本/待验证项。验收矩阵统一见ACCEPTANCE第10节。无需其他AI重新设计产品，但仍须先核对当前Git和schema、处理增量迁移与平台端口；不能把本设计当已运行代码。

### 23.12 GLM 5.3接手顺序：先收束时光机，再向下开发

最新用户明确：Codex本轮只整理方案，实施交给Claude中的GLM 5.3。以下顺序覆盖第23.11节可能被理解为同时展开所有层的安排。业务基线为6ef7990（含此前P0/P1实现和v2.1文档）；误启动的本轮产品试写已完全撤回。其他AI不得把撤回代码当已完成成果，不改变产品目标或搬入旧时光机业务。

**阶段一：先调整已开发的时光机，未完成前不继续铺新功能。**

1. 核对第23.11节差异表，检查core/contracts.ts、store.ts、volume-context.ts、API sources/design-service/routes和既有测试。建立与当前冻结任务兼容的新协议：旧候选按旧版本读，新任务明确版本；不能给旧结果补空字段并称满足新设计。
2. 全书和每卷增加预计字数/范围，开场与收束锚点、人物变化/高潮兑现，主支线开始/过程/结束及关键落点/弹性区间。保留已有全书基线、长期期待、三方案方向、交织关系、终卷结束规则；不强制loss或每卷推进所有线。
3. 采用时分配卷A和独立主线1/支线1，正式规划与正文事实分开。模型提出语义关系，系统事务编号与保存；重试/修订/候选隔离有效。此时不创建不存在的链A1或章1，只确定未来编号和父级接口合同。
4. 调整时光机成员执行：主编推荐→作者选择→编剧主动查方法/必要来源→分批卷→自检修订→独立核对与必要补查→候选保存。沿用已验证的账户/费用/任务恢复，修正失败或重启后的行为；同一模型不伪装多个模型。
5. 按卷供给输出采用的卷ID/显示号、预计字数、锚点、本卷线职责和来源版本；正文实际前情尚未有新接口时明确未提供，不用规划伪造。为后续卷设计保留可消费的版本化接口。

阶段一验收：协议、仓储/API及隔离测试通过；真实模型至少一套从资料到设计、自检/核对、采用与按卷读回通过，并人工核对事实、条件与字数；失败/续做不重复成功调用。未通过不能用历史116项测试替代，也不能直接接页面后宣告完成。

**阶段二：完成时光机全书方向功能，作为独立交付。**

1. 接入三个真实编剧独立候选，方案A/B/C显示成员头像姓名；切换时基线、长期期待、线、交织和分卷全部切换。独立状态及部分失败可恢复，至少一套完整可采用，不要求失败方案拖住其他结果。
2. 实现主编推荐及作者增选/自定义故事线、单多主线建议、开始设计、方案对比/修改/重设计/采用、刷新恢复、来源变更核对、真实任务与失败状态。保留已批准绿色视觉与手机优先，采用动作防双击，修改内容不得静默丢失。
3. 结果展示卷A及其字数/开场/转折/人物变化/收束/交接，主支线编号与具体交织；后台展示规格、阶段状态、成员/版本/工具与用量、失败恢复。后台路线与规格阅读入口必须实际可读，不能仅改文档头部。
4. 作者入口切换前检查新旧任务所有者和兼容策略，保留历史结果只读与回退。卷设计尚未开发时明确“卷设计待开放”，不放点击无效的“进入卷设计”。实际正文时光树尚未实现，不以原型假树代替；规划展示可用但必须标明规划。

阶段二验收：三套真实归属、完整切换/部分失败、作者调整与采用、来源过期/刷新恢复、手机与桌面、权限/版本/预算及后台入口有证据；可独立发布“新时光机全书方向”。不能宣布正文结算、动态时光树或后续创作已经完成。部署按DEPLOY执行，先验证再发布，不打断现有作者任务。

**阶段三：今天讨论的后续能力依次实现。**

1. 卷页：本卷信息和有效前情→方法与资料查询→链规划→自检核对→采用，生成链A1—A13及关系/预算/锚点。
2. 链及章规划：链卡→具体剧情事件与章任务→采用，建立全书章号和章到链归属。先证明真实卷→两条链→章节规划的交接。
3. 正文与自动记录：正式正文接受事件→变化提取→证据核对→自动事实/人物/道具/关系更新；增加修订失效、重算、乱序及连续水位。作者不手工维护账本。
4. 混合检索/Wiki/图谱：在前述有效记录基础上增加索引和关系查询；精确定位与文本回源先可用，向量路径独立验收。按需检索而非全书注入。
5. 锚点达成/收尾、主支线偏移巡检、局部规划自动修订、轨迹和实际时光树；记录与正文可追溯，重大方向/正式正文修改保留作者决定。

上述阶段三不回头重做已通过的时光机；若发现确需改上游协议，增加版本和兼容测试，只改受影响接口。按照第23节能力与ACCEPTANCE第10节验收，不再按固定九节点、固定故事数或固定情绪模板实施。

**交接执行要求。** GLM首先读取根AGENTS/HANDOFF、本文23.12→23.1—23.11→22、R192 worklist与开发路线，检查Git后从阶段一开始。每个逻辑批次更新原位状态和测试证据，再提交；保留其他修改，不私自清理作者数据，不编造已上线。遇到工程细节自行决策，实际费用/新凭据/不可逆数据操作按已有授权边界处理。将本节转交即可继续开发，不要求用户再次口述全部历史。

### 23.13 R195完整输入与书籍资料模板

故事线推荐、全书基线设计每次模型调用的完整输入均不超过15000字符，不是仅资料正文，不要求填满。统计适配器实际发送的system/messages序列化文本（包括岗位、任务、JSON字段、来源标记、选定故事线、方法笔记和补查结果）；目前工具动作及返回在业务提示内。今后增加原生工具定义或独立历史消息时，也必须进入同一输入计数。当前使用JS字符串长度作保守字符计数；模型名/温度/输出上限等非模型可见传输参数不作为资料字符。输出与推理另按窗口预留，字符上限不代表注意力保证。超限发送前拒绝并保留进度，不截断关键条件、不多次盲目重发。

复用核心资料六栏：①题材融合、核心构想、方向与矛盾；②主角及核心人物的身份处境、目标、能力条件、关系，兼顾群像；③影响故事的世界秩序、力量限制、势力及资源矛盾；④作者既定开局/结局/关键节点，未知留空且未来要求不当事实；⑤篇幅、节奏、情绪、语言与感情偏好；⑥明确禁止项和前提及其适用范围。每条text与sourceKeys同存；保留主体、条件、因果、例外，不用八条/八十字机械压缩，不搬普通价格、重复原文，不填充尚未设计的故事。格式安全校验的最大条数/长度仍保留，不能当内容凑数目标。

资料含JSON/来源建议约6000字符，属于软预算，简单书可更少。全书设计额外使用作者选择的故事线及补充（现有intent保存标题与描述，作为storylineIntent独立区）；不得注入未选择的全部候选，不能伪造尚未确定的起止过程。方法与补查记录作为methodReference独立区。两类任务的bookMaterial保持同版本、同内容，来源变化或模板升级才重新整理；推荐时不重复追加作者要求。新模板tm2-card-2不覆盖旧卡；已有运行仍可读取其冻结来源和旧模板缓存。

资料页整理、合并仍由资料成员，语义核对由主编承担。本批不新增“设定完成后提前整理”调度，也不代表主编事实审查必然准确。验收须区分工程边界通过与真实语义/注意力实测。

**CTX-01实测标注（2026-09-16，K3执行，证据见`.local/dispatch/outbox/ctx01-evidence/`与s1.result.md的CTX-01小节）**：deepseek-v4-pro单题材三长度（15000/20000/30000总输入字符）×三位置×双任务实测14次有效调用：提取任务在15k与20k档均出现关键事实整句遗漏（3/9，"记忆燃料永久失去"核心设定句整句未提取，遗漏与长度/位置无单调关系）；方案任务7/7保留因果链、相认时序、别名与否定条件（1份终因稀释瑕疵）。结论：本15000字符规范未通过实测验证为"关键事实必然被利用"的安全条件；按合同不以上调长度为默认推荐，优先关键事实排序前置单独成块、去重、按对象分包、生成后事实对照与独立异模型审查。多题材/多模型/重复运行因套餐到期受阻，未测试项不形成任何长度承诺。

### 23.14 R196故事线资料装载优化

不为小资料制造多轮压缩：按模型消息转义后的来源JSON字符预算5500装页，整份资料能放下则不切碎；只有超长单份来源无损分片，保留原key且不切断Unicode字符。相较旧1800字符/6500字节分片，中文资料能以较少页整理与核查。总模型输入仍受15000字符和窗口预算检查，不因页预算放宽总上限。

资料成员在同次输出前自查，不输出思维链或额外自评分；不扩写理论、方法、故事建议或尚不存在的经历，没有最低长度要求。单页正常路径为整理一次、主编核查一次，然后推荐；失败修正仍保留，不为速度跳过核查。新模板tm2-card-3按来源版本缓存，推荐和后续基线复用。

推荐和骨架的bookMaterial仅携带六栏文字数组，去掉每条重复的sourceKeys；数据库短卡和主编原文核查仍保留完整来源引用，未删事实或改变权威。其他需要来源工具的步骤继续保留索引。合成3500字资料验证3次调用、再次推荐1次、后续设计不再整理；这是工程证据，不是模型实际响应秒数或语义质量结论。

## 24. 分卷数量与容量开发规格（VP-01，2026-09-13）

### 24.1 决定、范围与状态

老板明确最多10卷，不采用500万字13卷。沿用默认最低6卷、常规每卷30万—50万字；50万字为单卷规划预算上限，不要求每卷恰好50万字。500万字只能分10卷且每卷50万字。阶段是叙事职责，卷是故事容器，两者不固定一一对应。

本轮合同：只更新本节、直接引用文档及后台RB-22方案档案并同步服务器文档；验证Markdown解析与线上文档一致。保留现有时光机、模型调用、提示词、数据库、作者方案与正文，不实施下述业务代码，不启动GLM。后续开发按VP-01A/B/C执行；文档上架不是运行规则生效。

### 24.2 确定性计算

输入W为作者已确定的目标总字数，单位字，正整数。缺失时沿用既有篇幅输入入口，标记待确定，不猜300万字。有效计算范围W≤5000000；超过时返回“当前最多10卷、每卷最多50万字，请调整总字数目标”，保留输入，不截断、不增加第11卷、不发模型请求。目标区间尚未选定具体值时先保留区间，由既有作者入口确定预算后再执行本策略。

- W≥1800000：可行最少卷数L=max(6,ceil(W/500000))；可行最多卷数U=min(10,floor(W/300000))。
- 默认N=clamp(floor(W/400000+0.5),L,U)。系统计算，不占用模型推理。
- 0<W<1800000：N=6，标记short_volume，允许低于30万字；不为满足下限增添剧情。极短作品主编提醒分卷可能过碎，但不擅自改为少于6卷。
- 作者指定卷数仅接受6—10范围；W/N>500000时返回容量冲突并给出可行范围，不能违背10卷/50万字上限。W≥1800000且W/N<300000可保留作者明确选择，但显示短卷说明。
- 系统输出初始均分预算，整数余数依卷序分配，精确合计为W。成员可以在上限内重新分配，各卷target为正整数且总和仍等于W，不允许用显示取整掩盖预算差异。
- 主编原则上沿用N；确需拆合时仅在可行范围内提出一次调整及具体原因。不同候选可有不同的可行卷数，无须为三方案故意改变数量。数学校验不判断文学质量。

| W | 默认N | 平均容量 |
| --- | --- | --- |
| 150万 | 6 | 25万，短卷 |
| 180万 | 6 | 30万 |
| 200万 | 6 | 约33.33万 |
| 300万 | 8 | 37.5万 |
| 400万 | 10 | 40万 |
| 450万 | 10 | 45万 |
| 500万 | 10 | 50万 |
| 超过500万 | 不生成 | 目标与容量上限冲突 |

### 24.3 成员工作流程与检索

前置仍为设定完成→作者确认→设定总清单有效→资料整理核查→故事线推荐及选择→全书方案设计，不借本批提前生成故事线。

系统把policyVersion=vp-01、W、L/U/N、均分预算及例外标记作为简短结构化区加入全书设计上下文。复用同版本书籍短卡、已选故事线、作者修改意见；所有岗位、提示词、工具结果和资料合计仍受15000字符上限，不要求填满。数据源变更后新任务重算，运行任务使用冻结版本。

每位设计成员先判断本书需要解决的结构问题，再通过R209的正式工具按用途查询少量相关方法；重点可包括宏观节奏、阶段结构、故事交织、群像、人物变化、承接和长期兑现，不要求全部调用。不注入全部方法库。D/E工具尚未完成时，记录依赖未就绪，不假装已使用新库。

成员先确定全书开局、关键变化与最终回应，再将阶段职责安排到N卷；一阶段可跨多卷、一卷可跨阶段转折。四阶段六卷可用1+2+2+1，但不是强制比例，不保存“1.5卷”。每卷须有不同的核心目标、过程与收束；不以阶段名称代替剧情，不统一强制失败、牺牲或打脸。

系统格式/预算检查→设计成员自查修正→与设计成员不同底座的独立审查→作者选择。审查同时接收完整候选与必要上游资料，不能只看压缩后的分卷摘要。检查相邻卷重复、容量空洞、因果断裂、故事线遗漏和结局兑现；缺少证据不得自评为满分。修正使用任务现有有界重试预算，只修失败候选，失败保留已完成结果；记录简短依据、方法编号与证据引用，不保存思维链。

### 24.4 输出、采用与后续交接

复用BlueprintV2/VolumeV2的words、start、goal、conflict、turningPoint、arc、ending、handoff和duties，不新建第二套卷表。每卷显示名称、预计字数、开始、核心故事/阻力、关键发展转折、人物关系变化、主支线编号职责、可核查收束结果、后续影响。当前单字符串不足以表达关键发展时先明确最小增量并升级合同；不得顺手扩成全卷章纲。最后一卷handoff展示为全书收束，不强制接下一卷。

采用前卷ID为候选内部引用；采用后沿用numbering分配卷A—卷J及既有主支线号。采用在同一事务保存选定方案、预算策略版本、卷预算和引用，expectedVersion/幂等键保护重复确认与并发修改。正式规划仍是未来安排，不写成正文事实。

后续卷设计只接收本卷预算/职责、相邻卷必要锚点、关联故事线及有效事实，再自行规划链；不提前把所有章灌入时光机。正文接受后的事实结算与实际字数为另一类记录：预算到数不自动收卷、不截断正文、不拒绝保存；收束达成须引用正式正文证据并判断必要后果是否交代。预计超50万字时提示调整剩余规划，而不是修改已写事实或偷偷增加第11卷。全文目标修改导致当前规则不可行时保留原方案并显示待调整。

### 24.5 技术路线与开发批次

| 批次 | 范围与实际接入点 | 完成条件 |
| --- | --- | --- |
| VP-01A | 在rebuild/packages/time-machine-core增加纯预算策略模块并从index导出；contracts/context/execution接策略版本及校验；采用现有WordBudget，避免另建卷库 | 边界及预算性质测试通过；不改鉴权和运行入口 |
| VP-01B | apps/api/src/application/books/time-machine-design-service.ts及实际调用的上下文/模型适配，冻结策略、供给短卡和R209工具、自查独立审查、采用事务；接实际端口不复制旧业务 | 新书真实三候选流程可用，错误恢复和模型用量记录准确，D/E依赖明确 |
| VP-01C | 作者时光机预算/卷数/例外提示与方案切换；后台规则版本、目标、计算结果、模型调整原因、校验和实测记录；更新RB-22完成范围 | 手机与桌面交互、采用后卷上下文一致，经过安全发布与线上核对 |

实施前先核查这几个现有文件的实际调用关系和已有编号预算测试；V7命名不代表重新使用旧业务。新后端模块负责策略，现有API仅作受控适配。若保存policyVersion需增量字段，执行器选择最小向后兼容方案；不做不可逆迁移，不重算旧书、不清除作者数据。新任务启用版本；部署前任务保持原快照，不能同一任务走两套策略。回滚停用新任务策略但继续读取既存版本，不能删除已保存预算。

### 24.6 验收与后台状态

单元覆盖：缺失/零/负/小数目标；1799999、1800000、2000000、3000000、4000000、4500000、5000000、5000001边界；500万固定10卷；指定11卷/单卷50万以上拒绝；余数分配精确合计；每个合法生成结果6—10卷且每卷不超过50万。主编重分预算若总和错误、卷ID重复或职责悬空，给出可修正反馈，不静默改文案。

集成覆盖：设定未确认不启动；15000字符超限保留进度；三候选分别校验；失败后不重复扣量/不重跑已成功候选；采用幂等与并发冲突；跨书隔离；目标修改使旧候选待重核；最终卷不显示下一卷。用200万、300万、500万及一个短卷样本进行真实成员测试，分别记录耗时、检索选择、结构质量和审查修正，不把模拟响应当文学验收。沿用ACCEPTANCE对应风险门禁，不另建平行测试矩阵。

后台RB-22必须标明：方案已定、VP-01A/B/C均待开发；记录制定人Codex及日期、规则/短卷例外/超限处理、详细文档位置、真实实现与实测缺口。已有时光机上线状态保留，不能把本增量标成已实现。未执行任何真实模型测试，不承诺零BUG。

本轮文档验收：2026-09-13本地及生产现有parseRebuildPlan均能解析82单元，RB-22包含分卷规则、成员流程、三批计划及缺口记录；六份直接关联文档已同步生产来源。仅文档更新，API/Worker保持原版本且健康，无模型调用或数据库修改；不将服务端记录可读宣称为业务分卷已实现。

## 25. 故事线资料：结果展示、作者编辑与失效（2026-09-15，S1-A阶段二）

### 25.1 导航与对象

时光机二级导航为**全书｜时光树｜轨迹｜资料**。故事线是"全书"的子步骤（推荐→确认→基线方案都在全书内完成）；时光树与轨迹保留入口、如实显示尚未创建。**资料**页展示并允许编辑本书的"故事线资料"。

故事线资料是作者确认故事线后形成的正式版本对象，与推荐前输入资料包（开书+已确认设定短卡）是两个对象，不混为同一编辑对象：

- **内容（可编辑）**：勾选的主编推荐故事线（72c3a62f复核后纠正：**材料自含**每条线的稳定ID/角色/标题/描述正文，作者在资料页可直接修改标题与描述；角色由服务端按原推荐裁定，前端只读展示；推荐原件保留在推荐记录中不变，不依赖推荐仍在最新12轮内）、作者自添故事线（标题/描述可编辑）、故事展开方式、群像偏好、作者补充要求原文。旧版本材料行无自含正文的由服务端从原推荐回填；推荐已不可解析时如实提示，不伪造内容。
- **来源引用（只读）**：推荐运行ID与规范哈希、开书/已确认设定版本签名、资料版本号与形成方式（作者确认/作者修改）。不提供开书或设定的编辑字段；开书/设定资料按来源引用展示。
- **全书工作摘要**（主编推荐语等）可折叠；资料页主要内容默认展开、中文标签、结果正文直接可读。未确认过故事线的书显示尚未创建，不编造内容。

### 25.2 版本、失效与事务边界（已纠正版）

- 新表`tm2_storyline_materials`（owner/book/id/revision/content_json/content_hash/created_by/idempotency_key/created_at）：内容哈希为`materialContentHash`（规范覆盖：来源三要素+勾选线ID与正文+自添线+展开方式+群像+补充要求），与设计轮快照`selection.requestHash`同口径；当前版本=该书最大revision；旧版本全部保持可读。草稿单行表`tm2_storyline_material_drafts`（owner/book主键）：保存草稿不失效任何后续，不每次击键失效。
- `tm2_design_runs`增量列`needs_redesign`（默认0）：作者编辑保存生效的同一事务内，把所有选择哈希≠新内容哈希的设计轮标记为需重新设计；已采用基线通过其候选轮的标记联动，在state投影显示"基于旧版故事线资料，需重新设计"。被标记轮的结果保留可读，但采用接口拒绝（409）并提示重新设计；候选人工修订同样拒绝；在途旧任务不强行销毁，晚到结果保留为旧版本且不可采用。**失效基线不再供下游读取**：卷设计规划上下文（planning-context）读取时命中needs_redesign=1的已采用基线→409"已采用的全书方案基于旧版故事线资料，需重新设计；旧规划保留可查看，正文不受影响"。
- 作者确认选择创建设计轮时（startDesignRound既有事务内，72c3a62f复核后纠正）：**当前无材料**→按客户端选择校验并写入v1（created_by='selection-confirm'；客户端若带expectedMaterialRevision>0→409零写入）；**已有材料**→客户端必须带expectedMaterialRevision且等于当前revision（不符→409"故事线资料版本已变化"带当前版本号），再比对客户端选择哈希与材料哈希（不符→409"内容已变化"，**旧选择不得静默盖过新资料**），一致时用**服务端存储的快照**构造设计输入建轮。内容相同不新建版本、不触发失效。失效标记只由**作者编辑保存**产生；正常重新确认流程行为不变。
- 编辑保存接口：严格校验（复用选择合同上限与推荐来源校验，勾选线覆盖正文必须在本次勾选内、缺省由推荐补齐），按owner/book/expectedRevision做CAS；**影响预览返回签名**（覆盖待保存内容哈希/材料revision/来源版本/下游采用revision与规划版本/受影响轮集合），确认保存必须原样带回，事务内重算不符→409"影响预览已变化"并返回新预览，不按旧预览直接写；幂等键唯一，重复提交返回原版本。保存只建立新版本与失效事实：不触发推荐任务、不启动任何重生成、不额外收费；下次创建设计轮自然读取新材料内容（哈希一致，新轮needs_redesign=0）。刷新/重启后失效状态来自数据库，不靠前端内存。
- 卷、链、章规划与正文在本阶段尚未创建：影响预览如实显示已采用基线的真实卷概要数（volumeOutlines，区分"全书方案附带卷概要"与"独立卷设计"），卷/链/章三级标注"尚未实现独立卷设计"，不编造数量；既有正文/已接受内容永远不因资料编辑被删除或自动重写（后续阶段接入时按本节合同扩展失效范围）。

### 25.3 接口与页面

- GET state扩展：`storylineMaterial`（当前版本内容含自含勾选线正文/来源/版本列表/草稿）、`runs[].needsRedesign`、`adopted.needsRedesign`。
- POST `…/storyline-material/preview`：返回依赖旧资料的全书基线、各设计轮（含在途数量与状态）、已采用基线的真实卷概要数（volumeOutlines）及卷/链/章（未实现如实标注）的影响清单，以及绑定下游版本的预览签名`signature`。
- PUT `…/storyline-material/draft`：保存草稿（覆盖式，不失效）。
- POST `…/storyline-material`：确认保存（CAS+幂等+预览签名事务内重算+同事务失效标记）；确认文案逐字为"保存此修改后，基于旧版故事线资料的全书基线，以及后续卷、链、章规划将需要重新设计。已有正文会保留，不会自动覆盖。"，按钮"取消""保存修改并标记重设"。
- POST `…/design-runs`：选择快照可带勾选线正文`selectedLines`（覆盖须在勾选内，role服务端裁定）；已有材料时必须带`expectedMaterialRevision`（不符或内容哈希不符→409零写入）。
- 资料页：默认展开的内容区（自含故事线清单/作者要求/来源引用），编辑态每条勾选线直接编辑标题/描述（稳定ID、角色只读）→取消/保存草稿/保存修改→影响预览→确认弹窗；保存后全书页旧方案显示需重新设计标记且不可采用；409预览变化时页面刷新后重新生成预览并请作者再次确认。全书页确认与正式资料不一致时先保存材料新版本，再用新版本自动开始设计。

### 25.4 验收范围

反例与集成：确认选择建v1且同选择不重复建版；编辑保存后旧轮needs_redesign=1、采用旧候选409、旧结果可读；保存不触发推荐任务；下次开轮读取新材料且新轮无标记；expectedRevision不符409+新预览；同幂等键重复保存无重复版本；草稿保存/恢复且不失效；跨owner/book隔离；事务失败回滚无半版本；预览卷/链/章如实显示尚未创建。请求恢复：未决设计请求冻结完整请求（含expectedMaterialRevision）持久化，已有资料v1与"保存v2后接力"两条首发未到达服务器反例，刷新前后请求正文与幂等键完全相等；恢复期间资料已变更则按原冻结版本重试、服务端409零新轮，不自动改取最新版本；已创建仅丢响应回放同轮不重复开任务；材料版本/快照/内容冲突为确定性拒绝retryable=false（真实HTTP断言），页面据此清未决且清除后再次刷新不重发。页面：三宽度真实点击（导航/默认展开/编辑/取消/草稿恢复/确认文案逐字/保存后标记）。验证后更新后台RB-22本节规则与开发/验收/发布状态。

### 25.5 验收状态（2026-09-15，K3执行，未发布）

- **工程验收已通过**：集成测试 `tests/integration/domain/s1a-storyline-material.test.ts` 9/9（真实HTTP链路：确认建v1→编辑保存→基线/旧轮同事务标记→adopt/候选修订409→不推荐任务→新轮无标记→CAS 409带新预览→幂等→草稿不失效）；阶段一回归 s1a-storyline-selection+s1a-k3-fixes+s1a-fixes+s1a-30a6-fixes 28/28；页面测试 17/17（含资料页4个新用例）；author-app tsc+vite build 与 API tsc 通过。
- **浏览器验收已通过**：真实 Edge headless+CDP，390/800/1440 三宽度点击——二级导航四项、资料页未创建态、确认→材料v1、编辑→预览→逐字确认文案、保存v2、全书页失效标记+采用禁用，8张截图核对无误（`.local/dispatch/outbox/s1a-browser-evidence/stage2/`）。
- **真实模型语义未验**：本节功能为作者编辑与失效机制，不依赖模型生成；故事线资料的真实文学效果随第23节整体链路验收，不在本节范围。
- **发布状态**：未发布。本批与 S1-A 其他改动一起按既有部署授权另行发布。

### 25.6 复核修复与验收状态（2026-09-16，72c3a62f复核后K3离线修复，未发布）

Codex在1dbed5cd复核发现：已有材料的未决设计恢复缺少expectedMaterialRevision，本节暂未整体验收。未决记录必须冻结完整设计请求与原资料版本，刷新原样重试；恢复时不得改取最新版本。具体修复与反例在原REBUILD-CLOSEOUT-01任务顶部。以下保留K3交付证据，不表示新增缺口已关闭。Coding Plan到期不再构成统一调用阻塞（Agent Plan文字模型已连通），真实长任务质量、CTX扩展和生产切换仍待完成。

Codex复核72c3a62f提出六项缺口，本批全部离线修复（未调用真实模型、未发布、未消耗已到期套餐）：

1. **结果正文可编辑**：材料自含勾选线正文（`selectedLines`），资料页每条已确认故事线标题/描述直接可编辑，稳定ID、role服务端裁定、推荐原件不变；旧版本材料服务端回填正文，不依赖最新12轮推荐。
2. **版本权威**：design-runs带`expectedMaterialRevision`——已有材料时版本号或内容哈希不符均409零写入，旧选择不能静默盖过新资料；初次确认建v1，服务端存储快照构造设计输入。
3. **影响预览绑定**：预览返回签名（待保存哈希/材料revision/来源版本/下游采用revision/受影响轮集合），确认保存事务内重算，预览后下游变化→409带新预览、零写入。
4. **失效全链**：planning-context读取命中needs_redesign已采用基线→409，失效基线不再供卷设计读取；预览区分全书方案卷概要（真实计数）与独立卷设计（未实现如实标注）。
5. **工作UI**：审查相位member=实际reviewer、卷卡相位=writer、进行中统一"正在工作"（无真实分母不给百分比）；已持久化任务显示可离开说明。
6. **验证收尾**：材料/选择集成19/19（新增正文编辑/版本权威/预览签名绑定/旧版本回填/工作投影用例）；阶段一回归30/30；解析回归12/12；页面测试21/21（新增正文编辑保存断言、版本一致直达设计、不一致先存资料再自动设计、409重预览四用例）；author-app tsc+vite build、API tsc通过；浏览器三宽度（390/800/1440）真实截图含工作态（"正在工作"+成员+可离开）与编辑态（推荐线正文直接编辑），8张核对无误（`.local/dispatch/outbox/s1a-browser-evidence/stage3/`）。
- **仍未完成（如实标注）**：真实模型语义验收与CTX阶段二尚未完成；Coding Plan到期为此前失败原因，当前Agent Plan文字模型已连通，应在切换后的独立证据目录继续评测，不能混用旧通道结果。生产尚未切换。verify:full旧债逐项现状记录在s1.result.md（rebuild-control已修12/12，其余台账/迁移期望/创作链夹具债仍待收束），本轮另有上述未决请求恢复缺口待修。

### 25.7 未决设计请求的版本持久化与刷新恢复（2026-09-16，1dbed5cd复核后K3定点修复，未发布）

**当前验收（覆盖下方历史待修记录）**：2026-09-16 Codex复核10bcf7e5通过，四处材料确定性拒绝改为retryable:false，真实HTTP与页面合同一致。独立实跑材料/选择20/20、页面24/24通过；版本冻结、过期拒绝、未决终结及再次刷新不重发闭环，本项关闭。整体S1-A真实质量、CTX、Agent Plan生产切换及发布状态不由此项验收替代。

Codex复核422a48c7：冻结版本传递已确认、页面24/24独立通过；版本冲突响应实际retryable:true与测试false不一致，换真实值后清未决断言失败。此项尚待统一确定性冲突合同：需要作者重新核对的资料版本/内容拒绝不能原请求自动重试；用真实HTTP断言不可重试及零新轮，页面验证再次刷新不重发。执行范围见原任务顶部“422a48c7复核后K3接续”。

**该合同已统一（2026-09-16，K3定点修复，未发布）**：`startDesignRound`的材料版本冲突、材料快照损坏与内容不一致四处DomainError改为retryable=false（仅此组需作者重新核对的确定性拒绝，不动其他409/429/服务故障合同；幂等回放优先与原版本冻结保持）。真实app.inject反例：保存v2后旧标签页冻结v1+新幂等键→HTTP409、error.retryable=false、details.currentRevision=2、该roundKey零运行；同键再次回放仍409零新轮（幂等回放仅适用于已成功建轮的键）。页面第三用例mock与真实合同逐字对齐（VALIDATION_ERROR/message/details/retryable=false），并补清除未决后再次刷新不重发断言。验证：材料集成14/14（含新真实HTTP用例）、选择集成6/6、页面24/24、API tsc通过。此项恢复缺口关闭。

Codex在1dbed5cd复核发现：已有故事线资料时，设计首发未到达服务器便断网，刷新重试漏传expectedMaterialRevision，被服务端版本门禁409拒绝；原测试只覆盖无材料首次创建。本批定点修复（未调用真实模型、未发布）：

1. **冻结完整请求持久化**：`PendingDesignRecord`增加`expectedMaterialRevision`；两个落盘入口（全书页直接确认`sendDesignRequest`、保存资料后接力`confirmMaterialSave`）均写入发送时冻结的版本号；无材料首发不落该字段，保持缺省语义。恢复时只使用记录中的冻结值，绝不回填当前最新版本冒充原请求；账号/书籍切换清理同步重置。
2. **反例先行**：新增三用例——①已有资料v1首发未到达服务器→刷新→重试正文与幂等键完全相等（含expectedMaterialRevision=1）→成功回执后清除未决；②保存资料v2后接力设计首发未到达→刷新→重试冻结v2原样带回；③恢复期间资料已被改为v2→重试仍用冻结v1→服务端409明确拒绝、终结未决、零新轮，不自动改成v2绕过作者确认。已创建仅丢响应回放同轮由既有6ad621dd用例覆盖。
3. **验证证据**：页面套件24/24（连跑4次全绿）；材料/选择集成19/19；author-app tsc通过。按任务要求未重复真实模型探针与五轮全套回归，未出现新失败故未扩大定位。
- **仍未完成（如实标注）**：同25.6——真实长任务质量/自然过审采用、CTX跨题材重复实测及生产通道切换仍待完成；本批不涉及模型调用。整包不发布。

### 25.8 ab8464c4端到端定点修复与局部恢复实跑（2026-09-16，K3执行，未发布）

Codex端到端验证三套方案无可采用后定点处理：A卷卡3遇HTTP400、B卷卡0截断、C审查12条issues。离线修复（09aa19e4）：供应商400白名单诊断捕获（code/param/requestId，自由文本不落库）、volume-card可见预算默认3000→6000。C的12条issues经逐条核对锚点合同与作者冻结要求全部真实、无审查加码；当时定性"修订轮已尽需作者决策"经1f831c6a复核纠正——见25.9，12条从未进入修订输入，属工程反馈路由问题，不由作者承担、不以轮次用完推给老板。

**6000修正被真实恢复实跑证伪（同日复跑，09aa19e4隔离副本+探针库只读副本）**：A新轮未复现400（诊断捕获就位；A新轮只跑到卷0，原卷3的400故障不能认定已消失，根因保持如实未知），但A/B双双截断于volume-card:0——6000>5000使DeepSeek从省略thinking直出翻转为显式enabled+4k预算，上报output_tokens=10000触顶；GLM-5.3可见路由+动态余量，6000+8000=14000触顶。证据边界（1f831c6a复核纠正）：截断分支先抛异常不提取正文，数据库无output_text不证明零可见，可见/思考分项供应商未上报，不能断言全部token用于思考；同次skeleton以8000+综合余量成功只证明骨架节点自身，不外推卷卡。卷卡并入8000综合节点组属**待真实复验的配置**（理由：可见预算对齐v2卷卡历史8k设计并触发既有综合余量；节点级调整，不全局调大；6000字符校验是生成后校验，不限制供应商生成成本）。离线反例已同步改为断言8000，不认定截断修复成功。

**预算纪律**：端到端累计上限100调用/600000token/3600秒，本批恢复实跑用5调用/55722保守token/约1155秒，累计32/289298/约3507，墙钟余量不足支撑再次整轮恢复，8000修正未做真实模型复验，标注待下一预算窗口验证。采用验证未执行（无自然过审候选），不部署。

### 25.9 1f831c6a复核后离线收束（2026-09-16，K3执行，未发布）

按原任务"1f831c6a复核"三项离线收束，零真实模型调用。

**项1 节点请求预算参数表（回归断言到mock fetch请求体，非仅入参）**：volume-card可见预算×成员模型的实际max_tokens/thinking字段（与服务timeMachineSynthesisHeadroom同源配对，prompt 5字符时GLM动态余量保底8000）——GLM-5.3：3000→11000/省略thinking、6000→14000/省略、8000→32000/省略；DeepSeek-v4-pro：3000→3000/disabled、6000→10000/enabled(4000)、8000→20000/enabled(4000)；Kimi-K3：3000→3000/disabled、8000→12000/enabled(4000)。如实记录不匹配：DeepSeek显式enabled声明预算恒为默认折算4000，与max_tokens中实际余量（8000时12000）不等，端点不保证遵守声明预算。网关预留一致性经真实适配器+mock fetch验证：reserved_tokens=max_tokens+输入UTF8字节+2048（两模型实测逐字节相等），4500字提示词下预留约47k<64000窗口，不越限；未为测试绿调整任何推理参数。

**项2 截断安全统计**：适配器截断分支新增TruncationDiagnostic（stopReason白名单字符、可见文本字符数、推理块有无、供应商明确分项reasoning/text tokens，未上报为null不按差值猜），只存数值/枚举；部分正文、思维链不落库不进诊断，known/unknown计量不变、截断仍是失败。双fixture区分"有部分正文也截断"（textchars=20/thinking-yes/rsntok-null）与"无正文截断"（textchars-0/stop-length/rsntok-900）；网关并入diagnosticCode，作者消息保持通用文案不泄漏。

**项3 C修订闭环离线追踪（探针库只读）**：C实际只有一轮修订——round0自检失败（payoff为null+4条锚点判定标准模糊，共5条）触发自检修订轮（design-service.ts:528），审查按设计只在revisionRound=1运行，其12条issues产出时自动修订轮已不可用（:534仅round0）。逐条定性：**11/12从未进入任何修订输入**（卷1收束/条件/fallback、卷2分流/代价/fallback、卷3强度冲突/收束/fallback、卷4强度冲突/收束/fallback两轮产物逐字段相同或改动与本问题无关），属反馈路由未送达，不是"模型收到未改"，也不由作者承担；**1/12（卷四"三项核心制度"未点明）的相关自检反馈确实送达，模型把"落地运行"改为"三项核心制度"但仍无名目**——收到后未改到位，自检revision-1与审查双重再报。机制事实（供Codex决策，不擅自加轮不改审查标准）：自检与审查共用同一个唯一自动修订轮，自检先用掉后审查问题无轮可送。

**根因与最小复验方案（待Codex核定，本批不执行）**：①B截断根因=节点预算分类缺失导致的可见预算与思考消耗叠加（精确分项待项2统计上线后由真实响应给出）；最小复验=B同轮retry仅重做volume-card:0单节点（GLM max_tokens=32000，预留约40k<窗口），1次调用至多+2次合同内自动重试，约5-10分钟，用新截断统计读取stop/textchars/thinking/rsntok实证；已成功步骤缓存（request_hash不变）复用，节点预算变更只影响失败步骤重发，缓存适用。②A的400根因仍未知（A新轮只到卷0，原卷3故障不能认定消失），复验须等B通过后另评估，不与B合并扩大。③C的12条属未送达反馈，修复方向=反馈路由（自检/审查修订轮关系）而非作者决策或加轮。

**C反馈调度修复（d7fc67f5复核通过，2026-09-16，离线实施）**：design()改为——初稿完成后自检与独立审查在**同一候选版本**上先后执行（审查不再被自检失败跳过），两类阻塞问题带来源汇总（完全相同文本去重并合并来源，语义相近不机械合并；suggestions不升级必改），统一进入唯一一次自动修订；修订版照常自检+审查复检，任一阻塞仍存在即诚实revise，不强制pass、不存在revisionRound=2；全通过不重修。版本绑定：初稿审查verdict绑初稿候选revision、复核verdict绑修订版revision（仓储1起）；已冻结候选/审查历史不回写；旧快照恢复的run缺初稿审查步骤时该步骤真实执行，步骤缓存按step id+输入hash存取，不用不同输入旧缓存冒充新汇总。反例六场景（真实服务+fixture）：双源同入一次修订/仅自检/仅审查/全通过零修订/修订仍阻塞诚实revise/审查技术失败走原边界（暂时性错误耗尽合同内2次自动重试后如实失败）/重入零重复调用。

**单节点真实调用许可闸门（d7fc67f5复核项4）**：`single-dispatch-permit.ts`仅隔离探针使用（生产装配不引用、生产执行器不变）。绑定run+step登记attempt+快照冻结模型（ModelRequest无modelId字段，模型在resolver装配侧核对）+输入hash（消耗时从tm2_model_calls录入许可文件）；先消耗再dispatch（宁可未知不重复），消耗状态持久化persistPath，重启不重置；其余一切dispatch（后续卷卡/其他run/未登记attempt/模型不符/许可已消耗）在网络前以不可重试拒绝阻止。fake fetch四反例：目标恰好一次且hash入档、目标成功后卷1零触网、重启新实例仍阻止、三类非法dispatch零消耗。

**下轮真实验证的强制门禁（替代"约一次/约5-10分钟"口径）**：节点=B方案run（f07a2919-ffe1-462e-aafa-282e88e94c6e）同轮retry的volume-card:0单步骤；输入=该步骤登记attempt（服务claim时落tm2_attempts）+快照模型glm-5.3；请求上限=max_tokens 32000（可见8000+综合余量24000）、网关预留约40k<64000窗口；许可文件=`.local/dispatch/recovery/single-call-permit.json`；停止条件=许可消耗后一切dispatch被闸门阻止（run如实失败invalid属预期，已成功步骤缓存保留）；调用硬上限=1次真实dispatch（无合同内自动重试——截断/invalid不自动重发，temporary由闸门第二击阻止）；墙钟硬上限=单请求适配器超时（90秒级）+轮询余量，总计≤600秒。预算记账沿用累计口径不清零。观察项=新截断统计（stop/textchars/thinking/rsntok）与可见输出是否形成可提交卷卡。

### 25.10 MODEL-NODE-EVAL首批基础设施与初筛通道（2026-09-16，K3执行，未发布）

按原任务顶部MODEL-NODE-EVAL合同连续实施的第一阶段成果。当前状态：**初筛真实批次进行中，尚无成绩榜/名册切换上线，不称已完成**。

**已完成的离线基础设施（验收前置项，全部有反例测试）**：
- 迁移0127（纯增量）：tm2_eval_run/case/budget/ranking/node_policy五表；预算预留/实耗/未知分列持久化，重启不归零；密钥与思维链不入库。
- 节点登记表`node-registry.ts`：22个真实调用点nodeKey（用途/岗位/预算档位/合同摘要/提示版本/源文件行号），首批四类堵点8节点（card-extract/card-merge/card-finalize/skeleton/volume-card/volumes-batch/review-source/review-anchors），推荐/检索/自检/修订第二批，其余已实现节点第三批；未实现功能不编造评测。
- 执行器：发送前原子预留（超限即budget-stopped不多发一次）、case幂等断点续传（终态跳过不重复计费）、全局并发2同模型1、429指数退避（用尽记终态不无限重试）、unknown单独计量（供应商未上报用量按预留额转unknown列，不按0）、重启对账reconcileReservedOnBoot（悬空预留归零，实耗/未知绝不清零——冒烟实跑暴露的kill后悬空预留）。
- 排名准入：Wilson下界排序、n≥10、一次技术交付≥90%、质量通过≥90%、关键约束零漏失（critical:前缀入库）、审查零漏报+误报≤10%、同底层modelId去重取前三、n<20的p95标小样本、不足三名显示实际数量不凑数、排名同输入可复算、应用/回滚只影响新任务快照。
- 离线测试：执行器/仓储/排名22项+样本提示13项+后台服务7项；既有domain套件317/319（time-machine-schemes 2项失败经git历史核对为3b87663d前资料版本门禁与旧断言顺序既有旧债，非本批引入，已报Codex）。

**合成样本与提示层**：三题材（历史融合/玄幻成长/都市感情）×三长度档×两时间段；生成节点screen2/holdout10正例，审查节点正反各半（植入3类已知错误测漏报、干净候选测误报）；调参screen与保留holdout样本hash分离不混用。提示镜像生产模板：cardContractFor/planningMaterial/prepareCardMerge/timeMachineReviewChecks直接import复用，骨架/卷卡/审查内联模板按生产动态分支（目标体量/作者故事线承接/紧凑规则/短卡注入）复现，漂移防护测试断言关键语句仍在生产源码。已知偏差诚实标注：review-source的read_source补查动作循环属流程编排，列入第二批methods-select评测，本批评估已提供回查片段后的verdict决策回合，不谎称覆盖补查能力。

**真实通道与初筛（56/56已完成，小样本不称稳定结论）**：ProductionEvalAdapter桥接ModelAdapterFactory（structured_planning与生产时间机器网关一致），错误分型429/unknown/鉴权/截断（output_length_limit）/供应商白名单code；运行器scripts/evaluation/node-model-eval.ts（同批次id重跑自动断点续传）。名册枚举7个文字模型（agent-catalog TEXT_MODELS除已停用glm-5.2；MiniMax仅后缀表未登记名册不枚举；缺权限/装配失败明确标未测不静默漏项）。冒烟真实验证：skeleton×DeepSeek Pro合同通过（1337入/12713出token，176秒，用量known）。**工具修正①**：初筛首发发现card-extract评测提示的来源key为两段式而生产parseCard要求三段式kind:id:revision（time-machine-sources.ts格式）——属评测工具bug非模型问题，已修正并对齐生产分页结构；修正前5次真实请求的证据行已删除（预算账本保留真实消耗5请求/43078token，不蒸发烧掉的事实），修正后card-extract×DeepSeek Pro/Flash均ok=2/2。

**初筛全批结果**（批次model-node-eval-b1，56 run全succeeded；每格n=2共同样本=小样本初筛，仅用于筛结构/协议可工作候选，不作排名依据；实耗117实际请求+9未知/96.3万+19.3万token，上限400/1200万内）：
- card-extract：ds-pro/ds-flash/glm-flash/k3均2/2；k2.7 1/2（1次幻觉引用）；doubao 0/2（2次"短卡引用不存在"=模型幻觉来源key）；glm-5.3 0/2（1截断225s+1次JSON不可解析）。
- card-finalize：仅glm-5.3 0/2（2次max_tokens截断，各约250s），其余6模型2/2。
- card-merge / review-anchors：7模型全2/2（glm-5.3在review-anchors最慢325s但结构通过）。
- skeleton：glm-flash 1/2（1次供应商400 SensitiveContentDetected=供应商内容审查，非模型质量问题），其余6模型2/2。
- volume-card：ds-flash 1/2（1次供应商technical_failure），其余6模型2/2。
- volumes-batch：ds-flash 0/2（2次供应商technical_failure）、glm-5.3 0/2（2次max_tokens截断，543s/626s）；其余5模型2/2（**工具修正②后**，见下）。
- review-source：ds-pro/glm-flash/k3/doubao 2/2；glm-5.3 1/2；ds-flash/k2.7 0/2——失败均为"已提供回查片段仍要求补查"，即模型选择继续read_source而非下verdict；该口径属本批已知偏差（补查动作循环列入第二批methods-select评测），记合同错误并注明，不等同审查质量结论。
- **失败分类汇总**：模型合同问题——glm-5.3共5次截断（card-extract1/card-finalize2/volumes-batch2，与生产历史上报一致，按节点处理不一票否决全岗位）+1次JSON不可解析、doubao card-extract幻觉引用2次、k2.7 card-extract幻觉引用1次；供应商/通道——ds-flash 3次technical_failure（volume-card1/volumes-batch2）、glm-flash 1次SensitiveContentDetected；流程口径——review-source补查循环5例（上注）。无429/限流；排队时间另列未混入生成耗时。

**工具修正②（9c5cfb46）**：volumes-batch初筛曾7模型"全灭"，其中5模型9案例失败原因同为"卷卡字段超60字"——核查发现评测校验器误把逐卷（volume-card）路径的逐字段≤60字+6000字符上限套到批量路径，而生产批路径（design-service批分支）只验批次完整/编号顺序+锚点数组形态，且批提示从未声明60字规则：双重失真（校验比生产严+提示未告知规则），属工具bug非模型集体不行。已对齐生产批合同并补漂移防护反例（批路径超60字通过/缺卷与锚点非数组拒绝/逐卷路径保持强制，43/43+tsc过）；9条失真案例作废删除（操作前整库备份.local/eval/node-model-eval.pre-volumes-batch-fix.bak.sqlite，预算账本保留真实消耗），5个受影响run以修正校验器重跑全部2/2（glm-5.3截断与ds-flash供应商错误证据维持有效未重跑）。**诚实边界**：重跑通过只证明结构合同，质量未评；输出工件此前未落盘（artifact_path全空），验证阶段盲评所需工件保存为下一批前置。

**后台"节点评测"视图（已建未上线）**：V7NodeEvaluationService（视图状态分层：未测/进行中/小样本/合格/不达标/暂停/待复测，名册7模型全列不静默漏项；排名计算只接受保留验证样本，初筛样本拒绝生成排名；应用/回滚写node_policy含rankingRevision+policyVersion单调递增；自动暂停规则=连续3次技术失败或5次内2次截断→暂停派工待复测）+6条admin路由（requireAdministrator）+admin-console新页签（与"模型速度与准入"静态历史报告同页分离展示，不冒充实时成绩；前三/样本数/成功率/median/p95/截断超时/token/准入原因/应用回滚/手工暂停复测）。

**节点策略派工接线（6a81418d，默认关闭）**：`node-policy-dispatch.ts`（nodeFamilyFor节点家族映射与registry口径一致/resolveNodeMember/nodeFallbackOrder纯函数）；快照构建按节点家族冻结承担成员进可选`nodeDispatch`字段——无策略无排名不写入（旧快照无此字段=现状逐字节一致）、全部候选暂停冻结null受阻标记、pending_retest不算暂停；review家族候选保持排除全部编剧模型（生成/审查异模型约束不因派工破坏）；设计服务`call()`集中解析覆盖（structured/repair/author-N/review全部调用点一处生效），遇null诚实失败报"暂无合格成员"不静默回退到被暂停成员。15项离线测试（映射/纯函数/快照集成/设计服务生效含默认关闭一致性与受阻不发出模型调用），tsc过。

**未完成项（如实）**：初筛批次未跑完；验证阶段（保留样本≥10次、异模型盲评质量评审、分歧复核）未开始；排名应用与"不稳定模型被排除由候补接替"的真实证明未做；隔离新书端到端未做；GLM5.3卷卡暂停派工状态沿用，待复测数据出来后按合同处理。

### 25.10.1 验证第一波与老板两次执行补充（2026-09-17凌晨，K3执行，未发布）

**验证第一波批次model-node-eval-b1-validation完成**：8节点×4候选×10保留holdout样本（计划320案例，实际307请求+1未知/240.6万token，上限400/1200万内；断点续跑未重复任何已完成案例）。技术层成绩（一次技术交付=结构且输出合同通过，质量盲评另计）：
- 全ok 10/10：card-extract四候选、skeleton四候选、volume-card四候选、volumes-batch四候选；card-finalize（ds-flash/ds-pro/k3）、card-merge（ds-flash/ds-pro/doubao）、review-anchors（doubao/k2.7）。
- 部分通过：card-finalize doubao 7ok+3合同错（短卡引用不存在/JSON不可解析=模型幻觉）→技术交付70%<90%门槛；card-merge glm-flash 9+1；review-anchors ds-flash 9+1供应商错误、glm-flash 9+1；review-source doubao 9+1。
- **提前淘汰（真实触发3例）**：review-source ds-pro 2/4、glm-flash 5/7、k3 4/6——各2次模型合同失败后判定剩余全过也<90%，停止剩余资格测试并标记early-eliminated（失败归因模型，按节点处理不跨节点）；review-source仅doubao进入质量机检环节，若质量不达标该节点如实标"暂无合格模型"不凑前三。
- 失败归因汇总：模型合同问题13例（doubao幻觉引用3、review-source淘汰6、glm-flash 2、ds-flash 1、doubao review-source 1）；供应商2例（ds-flash review-anchors 1次、usage未知1例单独计unknown口径）。无429/限流。

**老板三点补充落地（87d48c9b，离线测试10项全过）**：①技术成功与质量合格分开——排名准入的技术交付率改为"结构且输出合同通过"，堵住合同错误被排除盲评后仍算技术成功的漏洞（doubao card-finalize 7/10按此定义不达90%门槛，不进合格前三）；②盲评加防误放——主判通过案例按序号确定性每3抽1异模型复核，抽查不过=分歧null=未定不自动算过；评审可靠性校准（已知正确cleanPlan应判过/已知缺陷flawedPlan应判不过），校准不过的评审其结论quality_note标注"仅供参考"，校准结果落.local/eval/judge-calibration.json（幂等）；③合并核算——仓储budgetTotals全批次分账+合计一并展示，不以独立账本绕合同总上限；9条volumes-batch失真案例的作废原因及实际消耗（11.0万token）落档.local/eval/invalidations.json（untracked工件，源备份库可查）。

**老板六点补充落地（ac18b999，离线测试41项全过+tsc过）**：①不重启整批——续跑跳过全部已完成run/case；②进度行——每组完成打印完成数/计划数、当前节点×模型、最近结果时间、账本累计、按实测均值ETA；③失败归因分类器eval-failure-attribution（模型/供应商/评测工具/未定；评测工具问题走作废流程不归咎模型）；提前淘汰如上述真实触发；④入围标准不降——准入仍n≥10+双90%+零关键漏失+审查零漏报误报≤10%；⑤并发核查——合同授权全局2同模型1，原运行器串行未用上授权并发，已改worker池2（执行器信号量保证同模型1），超时600s/429退避[5s,20s]维持合同原值未提高；⑥自动接续——验证完成后直接启动评审批次（见25.10.2），排名与隔离端到端随后连续执行，不等待逐步确认。

**迁移0128（纯增量，数据全保留）**：tm2_eval_run重建扩展status CHECK增加early-eliminated（SQLite不能改CHECK，按原列定义重建+INSERT全量拷贝，操作前整库备份.local/eval/node-model-eval.pre-migration-0128.bak.sqlite）。

### 25.10.2 盲评批次与三处评审侧工具失真修正（2026-09-17凌晨，K3执行，未发布）

**教训复用**：volumes-batch"全灭先查工具"的原则在盲评阶段连续三次生效——凡失败高度集中于同一原因/同一样本/同一节点，先怀疑评测工具，不先判评审或候选模型。

**失真①校准探针cleanPlan未承接对抗线（26770737）**：v1校准6/6评审一致把"已知正确"cleanPlan判不过。核查：作者明确要求对抗线全书贯穿，而fixture的cleanPlan两卷duties只有主线——按"作者已确认故事线是否被真实承接"量规，评审合法判不过，属探针自身不干净。修正volumeOf加对抗线职责+钉住测试；CALIBRATION_CONFIG_ID升v2；失真期间3条评审结论作废重评。

**失真②校准探针cleanPlan为占位符方案（4e6fd372）**：v2首轮ds-pro仍判clean不过。一次性诊断探针（judge-calib-diag.ts，消耗3次调用入judging账本）取得完整issues：卷级字段全是"本卷目标/关键转折事件"占位符、四幕结构与两卷矛盾、里程碑/期待/关系单薄——评审意见文学上成立，cleanPlan只是结构合法而非文学合格。充实为结构一致、里程碑分步（主线3+对抗线3）、期待有推进回应（2条）、关系具体（2条）、卷级内容无占位符的真正合格方案；ds-pro复测clean=pass、两条flawed=fail（并精确指出v2未来承诺锚点与duties空缺）。

**失真③评审资料口径与候选所见错配（9b37dae5，blind-v2）**：校准通过后正式评审card-extract出现14评0过、跨评审对同一原因（"遗漏作者明确要求40万字/轻松向/不虐主"）——核查：card-extract候选按生产流程（design-service.ts:317过滤intent页）**从未见过作者意图页**，评审却按含intent的全量资料判"遗漏"；card-finalize生产任务明确禁止把故事方向放偏好栏，评审反向要求preferences收录故事线标题；fixture的prohibitions来源标注intent页但意图页文本无该句。修正：buildJudgePrompt按节点对齐候选实际所见资料+节点任务说明（card-extract排除意图页与故事线标题、card-merge给分页短卡、card-finalize写明任务禁令）；intent文本补"不得让对抗线中途消失"使卡字段来源可核对；JUDGE_CONFIG_ID升blind-v2；47例（card-extract35+card-finalize12）作废重评；钉住测试覆盖。修正后同一批card-extract输出重评10/10通过——再次证明是资料错配不是候选质量问题。

**评审校准最终结果（calibration-v2）**：通过=kimi-k3/doubao-seed-2.1-turbo/glm-5.3-flash（其结论作正式证据）；未过=deepseek-v4-pro（flawed-skeleton漏检一次，单次校准存在随机性，如实标注）、deepseek-v4-flash（clean误判）、glm-5.3与kimi-k2.7-code（探针调用失败）——四者结论quality_note标注"仅供参考"不作准入依据。

**作废与消耗（全部落档.local/eval/invalidations.json，预算账本绝不清零）**：v1校准约21次、失真期评审3例、47例错配评审、诊断3次，消耗均保留在judging账本；judging账本从400上限中已耗约158+未知21，剩余约242——全量236案例评审（含复核/抽查）预计不足，预算硬停时未评案例保持未评审如实标注，不隐形追加、不降低抽查/复核标准凑数。

**失真④审查干净样本为占位符方案（机检误报统计失真）**：评审运行中发现全部模型对审查正例（干净样本）"误报"2-10条/例——核查：审查正例用的就是失真②前的占位符cleanPlan，模型指出的问题（占位符/对抗线无去向）真实存在，机器校验把一切报告计为误报→误报率统计系统性失真。处置：正例机检质量信号32条作废（负例召回证据保留，记invalidations.json）；审查样本sampleHash原只含seededErrors导致夹具修正后重测被断点跳过（fdeda1dc修正：hash含方案内容长度，夹具变=新样本新case，不算重复已完成案例）；5个succeeded审查run重开，以充实后夹具重测（3个early-eliminated的review-source run保留——其合同失败为结构性补查循环问题与夹具无关，证据有效）。

**评审批次预算硬停（按合同停手）**：blind-v2正式评审完成139/236案例（通过123/不过1/分歧15），judging账本实耗346+未知54/400达上限硬停，97案例（skeleton尾部、volume-card大部、volumes-batch全部）保持未评审不隐形追加；评审调用截断失败26次（评审模型对skeleton/volume大方案的结论超出max2000，属评审配置观察项，后续批次如实报告）。预算消耗构成如实归因：v1校准21+失真期3例（约6次）+错配47例（约90次）+诊断3次+v2校准21+正式评审约225次——工具失真烧掉约三成评审预算，教训已转化为钉住测试。

**排名预览（隔离副本proof.sqlite，rank-preview.ts，草稿不写正式库）**：card-extract前三ds-flash/k3/glm-flash（ds-pro第4）；card-finalize前三ds-flash/k3/ds-pro、**doubao below_threshold（技术交付70%<90%）**；card-merge ds-pro质量85.7%<90%不达标（glm-flash/ds-flash/doubao前三）；skeleton前三ds-pro/ds-flash/k3；volume-card仅doubao/glm-flash合格（k2.7/k3质量未评完）；volumes-batch全部质量未评完；审查节点按旧负例召回数据暂无合格模型（重测进行中，结论以重测后为准）。所有n=10为小样本，p95标小样本估计。

**card-finalize排除接替真实证明（ranking-dispatch-proof.ts，证据.local/eval/ranking-dispatch-proof.json）**：隔离副本全链路真实数据——computeRanking（doubao below_threshold）→applyRanking（node_policy active=前三）→setNodePolicy暂停doubao（证据：n=10中3次合同失败=模型幻觉引用）→生产派工纯函数resolveNodeMember/nodeFallbackOrder：承担=排名第1ds-flash、doubao从候补序列消失；第1名也暂停后由候补k3接替、doubao仍不可达。正式库未改、生产快照未触碰、派工策略默认仍关闭。已知措辞瑕疵如实记录：resolveNodeMember在排名第1暂停由第2接替时reason仍写"按已应用节点排名第1名承担"（行为正确、文案易误读，留后续小修）。
