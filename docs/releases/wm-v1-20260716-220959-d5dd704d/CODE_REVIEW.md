# 首版最终代码复核

- 复核基线：`b41d603`
- 被复核实现提交：`fdfcb8a`
- 复核人：当前Codex，以独立阅读、故障注入和全量命令复核；未调用其他开发Agent。
- 结论：未发现未解决的 Critical、Important 或会阻止首版交付的 Minor 问题。

## 复核中发现并已修复

| 级别 | 问题 | 修复与证据 |
|---|---|---|
| Important | 版权检查只能覆盖单来源中文近写，跨语种事件链和多来源拼接可绕过 | 加入规范化事件链LCS、跨语言风险和来源并集覆盖；`copyright-cleanroom.test.ts` 覆盖换名、翻译和拼接 |
| Important | 版权服务存在但章节流水线没有强制生成前/生成后门禁 | 预检要求有效干净室包，硬检查阶段对全部非授权来源执行聚合检查；门禁在任何模型调用前可阻断 |
| Important | 接管包缺少章节和真实待确认详情 | 接管包加入章节、正文指针和确认对象/范围/影响；全链路在第3章前完成接管并拒绝旧epoch |
| Important | 模型能取消，但HTTP/子进程工具调用没有真实取消适配层 | 新增受限HTTP、无shell子进程和工具调用账本；真实请求Abort、真实进程终止、任务取消向下传播均有测试 |
| Important | 预算达到70%只发事件，没有保存重新预测 | 阈值事件现在保存剩余Token/现金和缩减非必要岗位/检索或停止新调用的确定性预测 |
| Important | 讨论领域表可用，但老板对话无法形成可运行闭环 | `讨论 <问题>` 现在创建持久任务，由Worker完成两岗位模型调用、上下文、预算、意见、汇总和 `确认方案 <ID>` |
| Important | 长对话会全量传输和全量挂载DOM | API按游标最多返回500条，界面最多挂载最近200条，同时显示数据库全量历史计数 |
| Important | API文档列出尚未注册的接口，归档/恢复/备份缺正式入口 | 文档收敛到已注册契约；补齐归档、恢复、严格删除、任务审计、预算用量、备份和隔离验证接口 |
| Important | 桌面启动器可能把同端口的其他健康服务误认为文脉写作 | 启动就绪同时校验release_id、Web 200和品牌HTML；停止器仍只处理已登记且整棵进程树均属于本项目的进程 |
| Minor | 新增版权拒绝测试使用了Vitest不支持的显式泛型 | 类型检查发现后移除泛型，目标测试和全量类型检查重新通过 |
| Minor | 失联恢复夹具使用与模型快照不一致的伪来源 | 模型调用新增适配器/快照/返回来源三重一致校验，夹具改为真实配置来源后继续验证结果未知恢复 |

## 已接受的交付边界

- 真实模型和联网研究凭证未配置；默认明确使用0现金确定性适配器，不冒充外部供应商质量。
- 24小时Worker证据使用1440个一分钟逻辑调度周期；另有独立真实子进程驻留和心跳测试。没有把逻辑时钟描述为24小时墙钟运行。
- 未配置第二物理备份位置，只承诺本机一致性备份和干净隔离目录恢复。
- 未配置Git远程，发布仅存在本地 `main` 分支。
