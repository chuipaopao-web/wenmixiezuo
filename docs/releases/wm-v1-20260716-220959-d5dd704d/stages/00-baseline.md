# 开工基线验收记录

- `release_id`：`wm-v1-20260716-220959-d5dd704d`
- 目标：登记老板开工决定，锁定参数，初始化本地Git与后续任务账本边界。
- 不做什么：不触碰 `D:\AI智囊团`；不调用真实付费模型；不创建远程仓库；不声称第二物理备份能力。
- 唯一写负责人：当前Codex。
- 允许文件：当前项目文档、Git元数据、发布记录、后续 `data/control` 任务账本。
- 禁止文件：`D:\AI智囊团` 全部内容；项目外文件；来源快照 `docs/SOURCE_REQUIREMENTS.md`、`docs/FINAL_SOLUTION.md`、`docs/CONSENSUS_LEDGER.md`。
- 依赖：Node `24.16.0`、npm `11.13.0`、Git `2.54.0.windows.1`。
- 实现约束：只在 `D:\wenmixiezuo`；唯一 `release_id`；本地端口只监听 `127.0.0.1`。
- 验收标准：开工决定可追溯；Git `main` 初始化；发布标识唯一；运行数据被Git忽略；任务账本可查询。
- 测试命令：`git status --short`、`git log -1 --oneline`、后续 `npm run ledger:status`。
- 失败停止条件：检测到项目外写入、密钥落盘、需要现金费用或无法建立一致任务账本。
- 回滚方法：仅在尚无业务数据时删除本项目内新增的未提交文件；已提交内容使用后续修复提交，不重写历史。
- 独立复核人：当前Codex在提交后使用只读命令复核。

## 证据

- 2026-07-16：检测 `43110` 与 `43111` 未被监听。
- 2026-07-16：确认本机 Node、npm 与Git版本。
- 2026-07-16：`DEC-002` 已记录开工授权与参数。
- 基线提交：`ce514d7518ed070b974f1609fe07ec60390705e5`。
