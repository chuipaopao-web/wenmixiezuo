# 长篇终局增量设计冻结记录

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`（预留，尚未激活）
- `design_review_id`：`DR-20260719-01`
- 后续修订：`DR-20260719-02`至`DR-20260719-08`
- 日期：2026-07-19
- 基线commit：设计轮开始时 `4f145e4`
- 历史正式release：`wm-v1-20260716-220959-d5dd704d`
- 当前证据等级：E0

## 授权和范围

本轮只授权整理文档和项目规则。由当前Codex单独完成，不调用其他开发Agent；未修改业务源码、既有迁移、运行数据和活动 `RELEASE_ID`，未安装依赖、下载模型或启动服务，未修改、停止或重启 `D:\AI智囊团`。

## 冻结成果

- 总设计：`docs/PRE_DEVELOPMENT_DESIGN_FREEZE.md`
- 运行状态机：`docs/RUNTIME_WORKFLOWS.md`
- 安全与运维：`docs/SECURITY_AND_OPERATIONS.md`
- 独立评测：`docs/EVALUATION_PROTOCOL.md`
- 八阶段施工：`docs/superpowers/plans/2026-07-19-final-longform-platform-implementation.md`
- 决定：`DEC-020`
- Skill审计：`docs/DESIGN_GOVERNANCE_AUDIT.md#11-dr-20260719-01-开工设计冻结复核`

## 已冻结的实施边界

1. SQLite和不可变文件保持唯一权威；FTS、结构化事实、Wiki/关系、阶段摘要和LanceDB是可重建投影。
2. 新Schema使用0010至0016向前新增；迁移不运行模型、嵌入或全量回填。
3. 每书按outbox回填、非活动快照验证、影子读和原子策略/快照指针切换；失败切回旧指针。
4. 四路检索采用硬门禁、结构/FTS/向量/关系意图路由、H/E/I三车道、同源簇和最小原文闭环。
5. 四种模式、五级输入、先预留输出、岗位最小包、完整生成后软审校保护创造性。
6. 本机短会话、精确Host/Origin、隔离导入、SSRF、秘密脱敏、epoch取消栅栏和真实状态进入release阻断门禁。
7. 工作台保持浅绿可调、176/190px窄栏、中心优先、右栏仅成员；任务/资料/连续性/检索/设置为二级页面。
8. 容量、正确性、运行、创造性和纵向文学质量按E0—E4分开声明。
9. 小文秘书是11名创作成员之外的本地工具角色；确定性代码优先，本地模型只产候选，点名直达，剧情会话保留原话并由主编/双编剧直接参与。

## 开工条件

老板发出明确开工指令后：

1. 将本预留ID写入活动 `RELEASE_ID`；
2. 在 `TASKS.md` 创建阶段1任务；
3. 保存开工时Git/环境/旧首版全量回归证据；
4. 严格按最终实施计划连续八阶段开发；
5. 每阶段通过门禁并提交证据后自动进入下一阶段。

在此之前，本目录不能被描述为活动release，本文不能被描述为运行验收。

## 设计轮验证结果

- 两份长篇Skill审计均通过：`PRE_DEVELOPMENT_DESIGN_FREEZE.md` 和 `DESIGN_GOVERNANCE_AUDIT.md`。
- 文档引用检查通过；全部当前Markdown未发现UTF-8替换字符；活动 `RELEASE_ID` 仍是历史首版。
- `git diff --check` 通过，只有仓库既有的Windows CRLF提示。
- 首次在受限执行层运行 `npm run verify` 时，类型检查通过，但测试进程连接自身 `127.0.0.1` 临时端口被环境以 `EACCES` 拒绝，连锁产生SSE、HTTP取消和Worker等待3项失败；没有修改测试或业务代码。
- 允许本机回环后用同一命令重跑通过：63个测试文件、132项测试全部通过；API/Web/Worker类型检查和生产构建通过，Web产物约295.65 kB、gzip约89.04 kB。
- 在 `C:\Users\momo\AppData\Local\Temp\wenmi-design-freeze-migrate-20260719-0106` 隔离目录运行迁移：第一次从空库应用0001至0009并到Schema 9，第二次 `applied: []`；正式 `data` 未修改。
- 本轮只修改文档和账本；没有业务源码、依赖锁、既有迁移或运行数据变更。
- 设计冻结首个提交：`95d23e0`。该提交后的干净工作树发布验收3/3通过，发布审计包含阶段包、Schema 9、依赖锁、AI智囊团独立、无密钥/占位、运行数据忽略、产品名称、桌面入口和使用说明，`failures: []`。

## DEC-021修订：十一人团队与三异模型点评

- `design_review_id`：`DR-20260719-02`
- 状态：E0修订；不改变“预留、尚未激活”的release状态。
- 团队：貂蝉/西施主副编、婉儿/红玉两名异模型编剧、文姬设定、秋香/湘君主副笔、妲己审校、昭君体验、道韫研究、弄玉版权，共11人。历史清照文编仅保留旧书兼容，职责并入妲己。
- 点评（历史冻结，具体模型已由DEC-026修订）：每个完整稿保持事实/文学/体验三职责异模型独立报告；主编合并一张修改单，最多两轮。三席缺失、重复、错版本、与活动写手同模型或需要按量付费回退时明确受阻。
- 质量：Kimi输出可解释AI腔风险与实际标记段落占比，不能称AI作者概率；豆包输出带位置和策略版本的政治/情色风险，不能称法律或平台最终保证；硬证据不按多数票，文学软建议通过创造性非劣效门禁。
- 专项审计：`docs/AGENT_TEAM_REVIEW_AUDIT.md`；施工和验收已同步到最终八阶段计划与 `docs/ACCEPTANCE.md`。

## DEC-022修订：职责可见性与按需研究员

- `design_review_id`：`DR-20260719-03`
- 成员卡直接显示一句话职责，点击进入公开岗位合同；公开职责、边界、激活条件和交付物与内部系统提示及隐藏规则分离。
- 道韫只在明确现实事实问题下激活；纯虚构设定、普通聊天、一般脑暴和固定三评不触发。未激活时零检索、零模型调用、零全文输入，研究卡未经确认不进入正史。

## DEC-025/026修订：最小开书、自然规划与可配置模型

- `design_review_id`：`DR-20260719-06`、`DR-20260719-07`
- 开书只收书名、题材、分类、目标读者、预计规模和初始表达基线；进入书籍即自由聊天。世界观、力量体系、主线和结局按需讨论，叙事视角在首个正式工单前确认。
- 首页移除1/3/5章批次选择。两名异模型编剧分别估算剧情最小/推荐/最大章节跨度，主编向老板推荐；正文始终逐章点评、确认、结算后再继续。
- 默认剧情席为DeepSeek＋GLM，Kimi可替换一席，豆包禁止剧情讨论；副笔改为GLM。点评默认GLM/Kimi/豆包，GLM写手时事实席改由DeepSeek承担。
- 模型绑定按书保存不可变修订；预览和校验通过后只影响未来任务，运行中任务不换模型。专项审计：`docs/ONBOARDING_MODEL_FLOW_AUDIT.md`。

## DEC-027修订：小文秘书、本地分层与透明路由

- `design_review_id`：`DR-20260719-08`
- 小文秘书是独立工具角色，不写入11个创作Agent，不计双编剧、写手或三点评模型；右栏分组显示但保持190px中心优先。
- 消息原样持久化并哈希，按确定性命令→显式点名→活动会话→低风险本地→专项岗位→剧情会话→主编升级路由；任务大小按风险/权限/可逆性/创作影响，不按长度。
- 剧情讨论由小文秘书准备资料后转为主编主持的持续会话，两名异模型编剧和主编直接面向老板；秘书摘要不能替代老板原话。
- 秘书记忆只含工作状态、可重建书籍缓存、明确操作偏好和版本化工具经验；不得沉淀创作喜好、另建私人正史或自改提示/权重。
- 本地通用工具模型在阶段1/3/4完成硬件、许可、哈希、路由金标、内存和延迟验证后选择；不可用时确定性/结构化路径降级，不远程下载或现金fallback。专项审计：`docs/LOCAL_SECRETARY_ROUTING_AUDIT.md`。
- DEC-027核心设计提交为 `316a1d8`；提交后干净工作树发布验收3/3、全部审计项通过且 `failures: []`。全量回归为63个测试文件/132项测试、三应用类型检查和生产构建通过；隔离迁移到Schema 9后重复执行为 `applied: []`。本证据仍是E0设计与历史首版回归，不代表新运行时已经实现。
