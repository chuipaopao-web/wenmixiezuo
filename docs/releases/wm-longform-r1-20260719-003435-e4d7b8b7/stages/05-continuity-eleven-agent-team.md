# 阶段5：超长篇连续性与十一人团队

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- `design_review_id`：`DR-20260719-09`
- 唯一开发者：当前Codex；未调用其他开发Agent
- 结果：通过

## 已实现合同

- Schema 14—15新增开放承诺、五级连续性节点、版本化阶段结算与探针、滚动计划、双编剧章节跨度估算、质量窗口、团队/模型/提示词快照、岗位连续日志、主笔租约、三点评面板和小文秘书路由/经验候选。
- 新书原子创建11名女性创作成员：貂蝉主编、西施副编、婉儿与红玉双编剧、文姬设定、秋香主笔、湘君副笔、妲己审校、昭君体验、道韫研究、弄玉版权；短岗位与公开职责绑定版本快照。旧书九人历史记录不改写。
- 小文秘书不写入 `agent_instances`，不冒充第12名创作成员；点名直达、剧情讨论交主编与双异模型编剧，确定性小操作不调用远程模型，付费/密钥/永久删除等受保护操作停下。
- DeepSeek与GLM双编剧先独立提交、使用相同简报且真实模型来源不同；豆包不进入剧情席。GPT/Codex或GLM可承担写手；点评兼容器要求事实、文学、体验三席彼此异模型且都不同于活动写手。
- 主编/副编沿用 `editor_epoch` 接管；新增 `writer_epoch` 防止主笔故障接管后旧写手迟到提交。岗位日志只保存步骤、依据、异议、结论与交接，拒绝内部思维链字段。
- 旧阶段先用结算导航；实体、规则、开放承诺或因果线程在第500、1000、1500章仍触发正史原文回查，不采用固定章数遗忘。结算探针失败时保留上一活动版本。

## 验证证据

- `npm run verify`：API/Web/Worker及测试类型检查通过；91个测试文件、184项测试全部通过；三应用生产构建通过。
- 阶段目标集覆盖：十一人身份与短岗位、小文秘书非Agent、双编剧异模型与豆包拒绝、三点评兼容、11份岗位提示快照、连续日志去思维链、写手epoch接管、开放承诺、阶段结算失败保旧、滚动计划、500/1000/1500章触发、自然讨论三成员真实调用。
- 空库迁移应用0001—0015；生产Schema 13迁移前以SQLite `VACUUM INTO` 创建 `data/backups/pre-stage5-20260719-082117.sqlite`，完整性为`ok`；生产仅应用0014—0015，再次迁移`applied: []`，`integrity_check=ok`、`foreign_key_check=0`。
- 恢复验证：把迁移前副本恢复至隔离目录 `data/verification/stage5-restore-20260719-0824`，应用0014—0015后完整性为`ok`且外键错误为0；未覆盖生产数据。
- 真实三进程构建产物smoke通过：HTTP-only会话、Worker ready、Node v24.16.0、SQLite FTS5可用、Web ready。缺少本地嵌入资产时向量通道如实降级为false。
- 未调用真实付费模型、未读取或写入API Key、现金费用为0，未永久删除作者数据，未触碰 `D:\AI智囊团`。

本阶段证明E1机制和E2构造场景成立；不冒充独立金标E3或真实长篇文学质量E4。
