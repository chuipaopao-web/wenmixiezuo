# 阶段8：全规模回放、耐久、恢复与发布

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- `design_review_id`：`DR-20260719-09`
- 适用Skill快照：`wenmi-longform-quality` SHA-256 `dfbb5275c6521caf248dbfb89229aceaa34ada4aaec279ce2a272e8d4d11bf07`
- 唯一开发者/复核人：当前Codex，按实现轮与两轮自审分离；未调用其他开发Agent
- 现金费用：0；未把API Key写入代码、数据库、日志、上下文、备份或Git
- AI智囊团：未修改、停止、重启或作为运行时依赖

## 最终缺陷复核

阶段8在规模与旧数据复核中定位并修正五项会阻断发布的问题：多来源被单来源活动切片快照覆盖；过期Worker仍可续租；历史9人书不会安全升级到11人；停用旧实例仍被未来模型策略重绑；桌面启动器用未认证接口探测Worker。修复合同见DEC-030，均有回归测试或真实启动证据。

## 自动门禁

- `npm run verify`：API/Web/Worker及测试类型检查通过；97个测试文件、199项测试全部通过；三端生产构建通过。
- Repository、跨书隔离、知识生命周期、切片/投影、四路检索、连续性、11人治理、创作闭环、三点评、可移植、恢复、安全会话和桌面组件均包含在全量回归。
- 新增旧团队升级测试覆盖9→11、历史实例停用保留、提示/绑定/资料回填、幂等重启，以及存在未终态任务时延后切换。
- 新增租约逻辑耐久覆盖1440个一分钟周期、24个任务、27次零现金模型调用、3次模拟过期崩溃回收、无僵尸任务、完整性ok和外键0。该证据明确不是24小时墙钟等待。

## E2满规模回放

可复现命令：`npm run evaluate:scale`。机器实测36秒完成；结构化原始报告保存在 `../evidence/final-scale-e2.json`。

- 5本书并存；主书正好5,000,000个NFC规范化字符、1500章、15卷。
- 单一活动快照覆盖1500个不可变来源和7500个检索块；工作区窗口限制80章。
- 122个跨全书分布锚点 Recall@5 = 1.0；P50 0.26ms、P95 0.34ms；跨书泄漏0。
- 故意删除7500条FTS后全部重建，重建及隔离恢复后的 Recall@5 仍为1.0。
- 回放数据库114,270,208字节；报告时RSS 369,299,456字节。该RSS是报告时点值，不伪称峰值。

## 正式数据、备份与运行

- 正式库连续两次执行迁移均为Schema 18、`applied: []`；`integrity_check=ok`、外键违规0。
- 升级后最终一致性备份：`backup-2026-07-19T03-30-38-134Z-0cb09b33`；数据库SHA-256 `9df554c2442c75ed4111152ebff291205af14dee9a2593ef596ebab074aaee72`；清单SHA-256 `b46d193fd30ad5bdf7252607b958e54ff195dd31235c666a9ba14906df859d06`。隔离恢复路径为 `quarantine/restore-backup-2026-07-19T03-30-38-134Z-0cb09b33-dae02f18`，完整性和外键检查通过，未替换正式库。
- 三个历史书籍均保留9个停用历史实例并具备11个当前实例、11个活动绑定和11份活动岗位提示快照；升级幂等。
- 桌面入口真实启动API、Worker和Web；release匹配、HttpOnly会话建立、Worker=`ready`、Web=200。停止入口执行后43110/43111监听数0且PID文件清除。
- 桌面快捷方式 `C:\Users\momo\Desktop\文秘写作.lnk` 指向 `D:\wenmixiezuo\文秘写作-启动.cmd`，工作目录为项目根。
- 桌面与移动端人工截图保存在 `data/verification/workspace-desktop-final.png` 和 `data/verification/workspace-mobile-final.png`；右栏真实显示11名成员，移动端没有被桌面三栏压扁。

## 发布边界

阶段8通过E2工程验收。确定性回放证明500万字符/1500章的数据、索引、隔离、重建和恢复容量，不证明真实模型能连续写出1000章高质量小说。当前没有用假模型伪造E3独立金标或E4真实纵向文学质量；这些证据只能随真实凭证、冻结金标和实际创作跨度追加。第二物理小说数据副本仍未配置，私有远程Git只备份代码和文档，不冒充小说运行数据备份。

结论：八阶段适用工程门禁全部通过；本提交经最终发布审计后推送私有 `origin/main`，并以远端分支与本地HEAD一致作为远程代码备份证据。
