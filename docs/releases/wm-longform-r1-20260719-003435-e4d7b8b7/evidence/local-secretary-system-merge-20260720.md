# 小文秘书与系统可见身份合并证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- `design_review_id`：`DR-20260720-01`
- 决定：DEC-038
- 功能提交：`e83b264`
- 证据等级：E2工程实现与构造验证；不声称E3/E4文学质量提升

## 交付结果

- 普通聊天不再显示独立“系统”姓名或齿轮头像，新旧内部系统来源统一显示“小文秘书”。历史消息数据库原文未改写，已知旧机械通知只在显示层转换为自然说明。
- 新回执写入 `message_type=local_assistant_notice`；`sender_type=system` 仅作既有Schema内部来源和历史兼容值。
- “你好”“小文秘书，你是做什么的”“查看任务”“打开资料库”由确定性本地路径处理；任务概览读取真实按书任务，前端按返回动作打开对应页面，模型调用为0。
- 点名、剧情、写作和点评仍由真实创作成员直接处理；点名成员回执不再落入默认机械文案。
- 通用未处理错误使用自然且不泄漏堆栈、SQL、绝对路径或秘密的说明；请求ID仍在API元数据和本地日志中追踪。

## 红绿与缺陷证据

首次目标测试在旧实现上得到4个测试文件、5项失败，分别锁定问候升级主编、任务查看未本地执行、历史系统身份仍可见和通用“内部错误”。实现后相同目标集27/27通过。

全量第一次回归发现应用层边界测试把新增正则 `.exec(text)` 误识别为数据库 `.exec()`；根因确认后改用 `text.match(...)`，没有把新文件加入SQL白名单。边界与对话目标集10/10通过。实机第一次截图发现旧消息正文仍显示“确定性离线适配器”，随后增加无损显示兼容并完成红绿UI测试。

## 最终自动验证

- `npm.cmd run verify`：退出0；API/Web/Worker与测试工程类型检查通过；102个测试文件、225项测试全部通过；API、Web、Worker生产构建通过。
- `node .agents/skills/wenmi-longform-quality/scripts/validate-audit.mjs docs/LOCAL_SECRETARY_SYSTEM_MERGE_AUDIT.md`：`PASS`。
- `npm.cmd run migrate`：退出0；正式数据库Schema 19，`applied: []`，没有改写既有迁移。
- 迁移/升级/备份恢复/Repository隔离/消息分页专项：5个测试文件、10项测试全部通过。
- `git diff --check` 与暂存差异检查：退出0。
- 正式发布功能用例：3/3通过；首次审计只报告新截图尚未纳入Git，证据提交后在干净工作树复跑。

## 运行与视觉验证

只重启文秘写作自身进程。真实本机探针返回：API health=`ok`、release ID匹配、Worker=`ready`、Web HTTP 200且根节点存在；没有修改、停止或重启 `D:\AI智囊团`。

实机1600×900截图：[local-secretary-system-merge-20260720.png](./local-secretary-system-merge-20260720.png)。截图显示历史通知的可见身份与头像已统一为“小文秘书”，旧离线适配器文案已转换为自然说明，右栏显示“小文秘书（本地秘书）”。

## 数据、安全与回滚

- 没有新增Schema迁移，没有批量改写历史消息，没有修改正文、正史、模型绑定或书籍业务数据。
- 没有调用付费模型，没有新增或打印API Key；本地秘书确定性路径模型Token为0。
- 回滚使用非破坏性Git revert功能提交与证据提交；数据无需恢复。发生误路由、虚假完成、成员被代言或内部错误详情泄漏时按DEC-038停止对应能力并向前修复。
