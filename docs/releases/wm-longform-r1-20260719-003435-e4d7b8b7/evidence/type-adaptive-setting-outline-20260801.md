# 类型自适应设定大纲验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 决定：DEC-083
- `design_review_id`：`DR-20260801-type-adaptive-setting-outline-v1`
- 日期：2026-08-01
- 证据等级：E2工程证据；不据此声称跨题材文学质量提升

## 实现结论

设定大纲不再把完整资料库冒充每本书的必谈清单。后端以作者显式选择的频道、单一分类、融合题材和标签解析唯一类型画像，返回“当前必须确定”和“建议补充”；故事方向只作为讨论内容和软锚点，不因“经营、游戏、领地、调查”等普通词激活整套模板。前端使用同一readiness响应，只初始化活动条目，保留已有内容、自定义项和可展开的完整目录。

商业经营与领主经营已拆分：商业经营只建议生产方式、货币/收入、阶层和信息边界，不再要求领地、人口、城池或军队。

## 自动验证

- 类型检查：`npm.cmd run typecheck`通过。
- 专项测试：设定画像与工作区UI共53项通过。
- 全量测试：134个测试文件、507项测试通过。
- 构建：API、Web、Worker全部成功；Web仅保留既有大于500 kB的分包建议警告。
- 迁移：连续执行两次，均为Schema 34、`applied: []`，证明本次无迁移且幂等。
- 运行：`http://127.0.0.1:43111/health`返回`ok`和正确release；Web `http://127.0.0.1:43110`返回HTTP 200；Worker通过桌面启动脚本readiness。
- 备份恢复：`backup-2026-08-01T09-09-37-872Z-afb61da6`验证通过；生产库完整性`ok`、外键违规0；隔离恢复完整性`ok`、外键违规0并在验证后丢弃。
- 验收测试：3个文件、3项全部通过。
- 验收审计：产品名、桌面入口、迁移、密钥、AI智囊团隔离等检查通过；唯一失败项为当前实现尚未提交导致的“工作树干净”，不是运行或功能失败。
- 差异检查：`git diff --check`通过，仅有Windows行尾提示。

## 真实书籍核验

对当前书籍《少女的实验笔记》调用正式作者API：

- 画像键：`romance+urban+business+mystery`
- 作者显示：`言情关系＋都市现实＋商业经营＋悬疑调查`
- 必谈：通用最小骨架、关系前提、关系阻力、案件规则、证据链、真相层次
- 建议：关系成长、情感边界、生活圈、阶层、信息边界、商业生产与收入、调查等
- 未出现：领地、人口、城池产出、军队

该探针只读取当前书籍和readiness，未改写生产书籍、设定、规划、正文或正史。

## 恢复与停止条件

本次无Schema和生产数据回滚。若后续发现已有设定不可见、融合题材依赖丢失、故事梗概普通词再次误激活类型、前后端门禁漂移或完整目录被注入单次模型上下文，应回滚画像解析、门禁和前端分层，同时保留所有历史内容。
