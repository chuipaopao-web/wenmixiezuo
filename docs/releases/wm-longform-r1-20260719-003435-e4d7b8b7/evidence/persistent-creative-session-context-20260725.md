# DEC-049持续创作会话与最小上下文工程证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- `design_review_id`：`DR-20260725-01`
- 日期：2026-07-25
- 证据上限：E2工程证据；未调用真实模型，不声称E3独立金标或E4文学质量

## 实现范围

- 单书单活动持续创作会话、不可变黑板和事件序列；
- 首次/重大改向双编剧独立推演、一次交叉质疑、主编综合；
- 方向锁定后才估算跨度，并只细化未来1—3章；
- 剧情预演绑定正史/黑板修订和来源指纹；
- 试写临时链与正式三席生产链分离；
- 主笔初稿4200字符、章目标/章纲1500字符、重写9000字符的版本化最小上下文；
- 卷/故事弧阶段结算选择和关键触发下钻边界；
- 分职责稿件质量快照、唯一最佳稿和多维退化回退；
- 工作区显示当前会话、下一步、候选分支和自然语言快捷动作。

## 验证记录

| 门禁 | 命令/结果 |
|---|---|
| 目标回归 | 17个迁移、Repository、跨书、会话、阶段选择、质量回退和故障恢复测试文件，35项全部通过 |
| 类型/全测/构建 | `npm.cmd run verify`：API/Web/Worker类型检查通过；122个测试文件、377项全部通过；三端生产构建通过 |
| 迁移 | `npm.cmd run migrate`连续执行：正式库Schema 26，`applied: []`；空库和旧库逐级升级测试通过 |
| 本地运行 | 确定性模式从生产构建启动；`127.0.0.1:43110/43111`、HTTP-only会话、API、Worker ready、Web、FTS5、本地向量能力冒烟通过；测试进程随后安全退出 |
| 满规模E2 | 500万NFC字符、1500章、15卷、5本书、7500块；122个分布锚点Recall@5=1，跨书泄漏0，P50 0.39ms、P95 0.67ms |
| 故障恢复 | 删除7500条FTS记录后重建7500条，Recall@5恢复为1；隔离恢复库 `integrity_check=ok`、外键违规0 |
| 正式备份 | `backup-2026-07-25T09-49-51-601Z-b676ab69`；74个文件；数据库哈希 `b2341ed65af6ec7e3da182234b3159b1e3deecb44ae90fa262b6fe82bf70a3d5`；清单哈希 `8955494e9e00eb1b309e7924ead5fa6fdbefa75a25ed00850d1cb3e447099b0b`；隔离恢复验证后销毁副本 |
| 正式验收测试 | 实现提交 `8a5f089` 后以干净工作树运行；`tests/acceptance` 3/3通过，Schema 26、密钥/TODO/旧名称/智囊团依赖、运行数据、桌面入口和文档等全部审计项通过，`failures: []` |

## 边界与回滚

- 未调用真实Codex、DeepSeek、GLM、Kimi或豆包，不消耗套餐Token，也没有继续生成测试小说正文。
- 4200/9000字符是当前策略版本，不是所有模型的注意力保证；后续必须用真实Tokenizer、消融和纵向盲评校准。
- 会话黑板、预演和质量快照均不成为正史；旧稿、旧预演和失败证据保留审计。
- 业务/UI可以非破坏性revert；Schema 0026已进入向前迁移，只能通过后续迁移修正，不允许改写历史迁移。
