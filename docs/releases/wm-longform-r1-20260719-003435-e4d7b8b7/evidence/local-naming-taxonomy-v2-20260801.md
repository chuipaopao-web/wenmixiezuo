# 取名助手独立细分类验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 决定：`DEC-082`
- `design_review_id`：`DR-20260801-local-naming-taxonomy-v2`
- 实现提交：`1ffe8e6`
- 日期：2026-08-01

## 问题与修复

旧目录只有46项，曾把命名规律明显不同的对象合并处理，例如“人工智能/机器人/仿生人”和“器灵/精怪/妖族”。这会让作者无法准确表达命名对象，也会让候选名称混用语感与后缀。

本次把目录扩充为122个可见、可选择、可生成的独立细类：人物15、地点24、势力22、物品22、生灵15、能力24。人工智能、机器人、仿生人、器灵、精怪、妖族、异族、神明、亡灵等均拥有独立ID、说明和生成规则；山脉/山峰/山谷、江河/溪流、药剂/丹药/毒物、法宝/神器/圣物等原合并项也已拆开。候选只在本地生成，不调用模型或付费API，不自动写入书籍、正史或正文。

## 回归与门禁

| 检查 | 结果 |
|---|---|
| RED基线 | 旧实现仅46类，人工智能、器灵、精怪独立目标为空，测试按预期失败 |
| 生成器专项测试 | 7/7通过；校验122类、无合并禁用项、ID唯一、每个可见目标均生成4个候选 |
| 生成器＋工作区UI | 43/43通过；开书和书内取名入口可显示并选择人工智能、机器人、仿生人、器灵、精怪 |
| 全量验证 `npm.cmd run verify` | API/Web/Worker/Tests类型检查通过；134个测试文件、498项测试通过；生产构建通过 |
| 迁移 | Schema 34；连续两次执行均为 `applied: []` |
| 本地运行 | Web `127.0.0.1:43110` 返回200；API `/health` 返回 `status=ok` 且release_id正确 |
| 备份恢复 | `backup-2026-08-01T04-17-53-519Z-66193f1d`；隔离恢复完整性通过，验证副本已销毁 |
| 备份哈希 | 数据库 `34963859f59081812e752f4ad68618b77fd203bca11e2405c16a8979c8068813`；清单 `2d0e1b072fd62d5fa6e4a682a770ac40925bcc49cdcb55f57936490b7cb7551a` |
| Git检查 | `git diff --check`通过 |
| 正式验收 | 3/3通过；验收审计全部通过，`failures: []` |

## 证据边界与回滚

- 这证明目录覆盖、分类独立、确定性候选生成和双入口交互可用，不宣称本地词库候选优于人工命名或真实模型创意。
- 本次没有模型调用、现金支出、密钥读取、数据库迁移或业务数据写入。
- 如需回滚，只需回滚目录、生成映射、UI样式与对应测试；无需回滚Schema或生产数据。
