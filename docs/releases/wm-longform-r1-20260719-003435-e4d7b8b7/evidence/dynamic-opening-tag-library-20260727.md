# 动态开书标签库验收证据

- release_id：`wm-longform-r1-20260719-003435-e4d7b8b7`
- decision：`DEC-056`
- 日期：2026-07-27

## 验收范围

- 一个主分类与最多3个同频道辅助分类；
- 通用标签包、题材扩展包和自定义标签；
- 默认只显示主辅分类相关标签；
- 完整库按分类切换并支持搜索，不一次展示全部词条；
- 标签保持软方向，不自动成为正史或逐章硬约束。

## 验证结果

- `npm.cmd run typecheck`：通过；
- `npm.cmd test`：123个测试文件、391项测试全部通过；
- `npm.cmd run build`：API、Web、Worker全部构建通过；
- `npm.cmd run migrate`：数据库当前Schema 28，无待应用迁移；
- `npm.cmd run acceptance`：3项验收测试通过；提交前审计仅因工作树尚未提交而失败，提交后复跑。

## 回滚

回滚本次Git提交即可恢复旧的单分类与平铺标签目录；未新增数据库迁移，不涉及生产数据恢复。
