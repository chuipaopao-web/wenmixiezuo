# 作者可读展示层与聊天去技术化证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 任务：`UX-20260725-02`
- 决定：DEC-050
- `design_review_id`：`DR-20260725-02`
- 日期：2026-07-25

## 修复事实

1. 服务端有效输出可从前后混杂文本中用字符串/转义感知的平衡括号扫描提取版本化白名单答复；规划落库JSON、模型协议和岗位原文不会进入作者消息。
2. 非结构化fallback不再生成可展开原始引用；独立岗位意见仍完整保存在讨论意见与模型调用审计中，没有删除或覆盖。
3. 讨论消息改为自然确认“锁定当前方向”“确认当前规划/方案”，决定ID只留在内部引用；旧带ID命令继续兼容。
4. Web新增确定性作者展示层：递归解析安全JSON字符串、翻译字段/枚举、隐藏内部ID/哈希/模型协议/来源UUID；规划编辑继续操作原业务值。
5. 图谱只显示章节、自然状态和有效内容；未提取记录不再展开点评协议。资料库实体不显示结构版本，版权研究不显示存储字段。聊天不显示供应商/模型ID。
6. 历史fallback若含合法结构化主编答复，可只读恢复自然结论；无法可靠恢复时保持安全提示，不展示原始杂乱内容。

## 创造性与审计保护

- 未做字符截断，白名单继续保留关键依据、结构不同的备选方向、风险、未知、需要确认项和补充来源。
- 原始聊天、岗位意见、规划版本、图谱投影、版权研究、正文和正史均未改写；无Schema迁移。
- 未新增模型调用，未使用API Key，未触发按量付费；现金回退仍为关闭。
- 未修改、停止或重启 `D:\AI智囊团`，也未停止或重启文秘写作常驻服务。

## 验证结果

| 门禁 | 结果 |
|---|---|
| 目标回归 | 5个文件53项通过，覆盖双重转义/损坏载荷、嵌入JSON、历史fallback、自然确认、规划/图谱/版权作者视图 |
| 全量自动测试 | 123个测试文件、383项全部通过 |
| 类型检查 | API、Web、Worker与测试类型检查通过 |
| 生产构建 | API/Worker TypeScript构建、Web Vite构建通过；4562模块完成转换 |
| 空库迁移 | 0001—0026全部应用，Schema版本26 |
| 现有数据升级 | `D:\wenmixiezuo\data\database\wenmi.sqlite`重复迁移无新增，版本26 |
| 备份与隔离恢复 | `backup-2026-07-25T12-29-09-516Z-0fb58008`；74个文件；生产与隔离副本完整性均为ok；外键违规0；隔离副本验证后销毁 |
| 本地运行 | Web 43110返回200；API health=ok且release匹配；Worker=ready；43110/43111仅监听127.0.0.1 |
| 费用边界 | `cashFallbackAllowed=false`，活动模式为subscription-plan；本任务没有模型调用 |
| 验收 | 验收测试3/3通过；提交前审计除“工作树尚未提交”这一预期项外均通过，提交后执行干净工作树复核 |

## 回滚

本次只有应用、Web、测试和当前规格变更，可通过非破坏性Git revert回滚；不得删除历史消息、讨论意见、规划、投影、研究、正文、正史或本次已验证的生产备份。
