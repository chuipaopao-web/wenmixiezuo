# 当前卷双编剧候选与主编融合证据

- release_id：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 决定：DEC-109
- design_review_id：`DR-20260808-volume-planning-ai-orchestration-v1`
- Skill快照：`wenmi-longform-quality` SHA-256 `DB5CC99164C27174E90A04C23469DCA739DB1A294EC31F89DC2334BBDB8F4E78`
- 证据等级：E2工程机制；未取得E3真实模型盲评或E4长篇纵向质量证据。

## 已实现

1. `volume_plan_generation`持久任务冻结卷/工作流CAS、开书、设定、上一卷确认版/结算、当前卷作者原话/作用范围/已解析附件摘要、模板、三席模型、预算和来源指纹。
2. 两位编剧并行使用独立ContextPack和ModelCall，候选A/B互不可见；主编只在两份结构合法候选已保存后读取二者并产生独立融合版。
3. A/B/融合均追加不可变`volume_plan_versions`并带`source_task_id`；任务成功不切换活动版，作者仍经影响预览显式确认。
4. 同一幂等请求只返回原任务；安全重试复用成功模型结果与已保存候选；取消、失败/中断重试和结果不明停止沿用任务租约/提交栅栏。
5. 作者意见严格匹配当前卷对象。页面显示真实任务、成员/模型来源、候选数量和停止/恢复/重试；本地确定性配置明确显示为流程测试，不冒充异模型证据。
6. 当前卷父级重复意见入口已停用，只保留绑定具体卷ID的对象内入口。

## 自动证据

- API、Worker、Web、测试TypeScript检查：全部通过。
- 卷规划定向回归：4个文件、45项全部通过。
- 全仓Vitest：154个文件、635项全部通过。
- 新集成测试验证：3次成功模型调用；3份不同内容哈希的不可变候选；A/B包不含`planning:independent_volume_candidates`；融合包包含两份完整候选；作者原话进入A/B；重复幂等任务ID不变；活动卷版本仍为空直到作者确认。
- 生产构建：contracts、API、Worker、Web全部通过。Web保留既有大包体提示，不是本轮新增失败。
- 隔离迁移：空库应用0001—0038，第二次无迁移，当前版本38。
- 隔离运行：API健康`ok`，Worker`ready`，readiness允许模型任务；Worker能力包含`volume-plan-generation`。验证后已停止隔离进程并清理临时数据。
- `git diff --check`：通过。

## 尚未证明与下一步

- 真实套餐模型跨题材规划质量、候选结构差异和创造性非劣效仍需E3盲评；完整卷与下一卷纵向效果需E4。
- 当前页面使用有界轮询读取持久任务；P13继续统一SSE游标恢复、全局状态与Worker重启体验。
- P8开始事件领域前，必须只读取作者已确认的活动卷版本，不能把任一未确认候选当作活动卷纲。

## 回滚

关闭`generate`入口和Worker任务分支，保留任务、调用、ContextPack、候选与Schema 0038数据只读；作者候选、当前活动卷、正文和正史不删除、不改写。