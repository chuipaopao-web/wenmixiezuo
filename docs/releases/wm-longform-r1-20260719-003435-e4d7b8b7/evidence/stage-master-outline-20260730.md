# 阶段式剧情总纲验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- `design_review_id`：`DR-20260730-03`
- 决定：`DEC-069`
- 日期：2026-07-30
- 证据等级：E2（工程闭环；不外推为长篇文学质量证明）

## 实现范围

- 两名异模型编剧在独立阶段各自提交完整 `stage_master_v2` 阶段总纲。
- 两份方案分别通过结构校验后才允许一次交叉质疑。
- 活动主编只能基于真实编剧产物整合，缺任一份有效阶段总纲即中止。
- 每阶段包含连续章节范围、主线遭遇/解决/结果、阶段级起承转合、阶段总结、待回收信息与伏笔、后续方向。
- 作者界面使用中文阶段卡片，并可编辑嵌套字段生成候选版本；不显示机器协议字段。
- 叙事图谱按阶段生成一条简洁主线投影。
- 历史浅层剧情总纲保持只读兼容；新确认或重新编辑的总纲采用新合同。

## 红绿回归

- 修改前专项测试：7项失败、66项通过，覆盖服务端结构、双编剧产物、投影和前端阶段卡片。
- 实现后专项测试：5个文件、73项测试全部通过。
- 代码复核发现并移除一处重复主线字段校验；修复后重新执行完整门禁。

## 工程门禁

- `npm.cmd run typecheck`：通过。
- `npm.cmd run verify`：通过；130个测试文件、461项测试全部通过；API、Web、Worker构建通过。
- 空库迁移：完整应用 `0001`—`0032`。
- 同库二次迁移：`applied: []`，当前版本32。
- 升级兼容：9个文件、13项测试全部通过。
- 独立运行探针：通过；测试服务使用临时数据目录和独立端口。
- 创建书籍端到端：通过；2本书、2份开书快照、2份主角资料、22个成员、2个主编启动任务，外键异常0。
- 恢复链：6个文件、13项任务/晋升/章节/备份/便携恢复测试全部通过。
- 正式库备份前完整性：`integrity=ok`，外键异常0。
- 隔离恢复：通过；备份 `backup-2026-07-30T13-04-25-186Z-f16f4e10`，49个文件，恢复验证后已丢弃隔离副本。
- `git diff --check`：通过，仅有工作区行尾转换提示。
- 提交前 `npm.cmd run acceptance`：3项功能验收通过；审计仅因本次尚未提交的工作树不干净而按预期失败，提交后重跑。

## 创造性非劣化边界

- 起承转合只作为阶段总结视角，不强制映射到单章。
- 章节范围是可版本化预计值，不是正史或填充篇幅指标。
- 待回收项允许为空，不为满足模板虚构伏笔。
- 总纲不保存逐章事件，不替代卷纲、章纲或正文。
- 本次没有真实长篇盲评，因此不声称E3/E4文学质量证据。

## 回滚

- 应用与文档使用Git revert回滚。
- 不删除历史规划版本，不改写正文、正史或开书资料。
- 新结构不需要数据库迁移；历史只读路径持续可用。
