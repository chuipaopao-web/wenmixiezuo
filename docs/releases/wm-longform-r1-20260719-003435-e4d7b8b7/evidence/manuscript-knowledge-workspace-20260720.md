# 正文、图谱与主角资料工作台验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 决定：DEC-041
- `design_review_id`：`DR-20260720-04`
- 功能提交：`5c0dfc9 feat: rebuild manuscript and protagonist workspaces`
- 日期：2026-07-20
- 证据等级：E2（工程、迁移、恢复和本机运行）；不外推真实模型纵向文学质量E3/E4

## 1. 交付范围

- 规划页仅保留全书框架、基本设定、总纲、卷纲和章纲；章节目录只在正文页出现。
- 正文页固定左侧卷章目录和右侧编辑区。未结算正文可保存不可变新版本、提交真实重写任务或进入完整定稿审校；已结算正文只读。
- 图谱统一显示人物关系、情绪、主线、支线、钩子/伏笔和信息差；资料库不再重复展示图谱页。
- 资料库增加主角面板，覆盖身份、属性、资源、装备、技能、城池、将领和兵力；条目追加修订、可归档、历史不物理删除。
- 作者只填主角姓名时，只在同书内与完全相同的活动角色标准名自动关联；结构化正史事实可更新面板，格式无效的派生事实不回滚正文正史。
- 基本设定增加版本化属性公式；解析器仅支持声明变量、数字、括号和 `+ - * / %`，不使用 `eval`、函数或网络。

## 2. 类型、自动测试和构建

最终命令：`npm run verify`

- API、Web、Worker和测试TypeScript检查：通过；
- Vitest：108个测试文件、264项测试全部通过，失败0；
- Web生产构建：4562个模块，CSS 76.09 kB、JS 395.30 kB；
- API和Worker生产构建：通过。

关键专项包括：

- 作者正文保存CAS、旧稿不变、陈旧基准/已结算拒绝；
- 作者修改稿从硬检查进入三席审校，不重复生成初稿，不直接增加正史修订；
- 重写与定稿绑定当前不可变版本并创建真实任务；
- 主角候选/正史/归档历史、跨书隔离、同名实体自动关联和无效派生事实安全跳过；
- 公式正常计算、未知变量、非法字符、除零和代码注入拒绝；
- 规划/正文/图谱/资料库UI去重及可访问交互；
- 应用层数据库边界测试确认新增业务服务只经Repository访问SQLite。

## 3. Schema 23与正式库

命令：连续两次 `npm run migrate`

- 第一次：仅应用 `0023_manuscript_protagonist_workspace.sql`，当前版本23；
- 第二次：`applied: []`，当前版本23；
- 正式库 `PRAGMA integrity_check`：`ok`；
- 正式库 `PRAGMA foreign_key_check`：0项；
- 空库迁移和Schema 9→23升级、重复运行均在全量测试中通过；0001—0022未改写。

## 4. 备份与恢复

命令：`npm run verify:backup`

- 备份ID：`backup-2026-07-20T04-02-23-591Z-cb052082`；
- 数据库哈希：`8775dc064f175e1bc888cc3de8c96657431e01fdd7d77e03e7a363ab62b54c5c`；
- 清单哈希：`1b782086e08cf29ac880c231922f355bc7565073b62fdc74e750c626ae509469`；
- 生产库检查：完整性`ok`、外键违规0；
- 隔离恢复：验证通过，完整性`ok`、外键违规0，验证副本随后销毁；
- 未执行生产数据恢复或真实书籍永久删除。

## 5. 本机运行与视觉证据

- 仅停止/启动文秘写作自身服务；未修改、停止或重启 `D:\AI智囊团`；
- 构建产物运行探针：HttpOnly本机会话、Worker `ready`、Web `ready`、SQLite FTS5可用、本地向量可用；
- 最新构建恢复常驻后：`127.0.0.1:43110` Web返回200，`127.0.0.1:43111` health为`ok`，Worker为`ready`；
- 主角和公式只读新接口在正式运行库返回作用域内结果；
- 1600×900实机截图：[manuscript-workspace-runtime-20260720.png](manuscript-workspace-runtime-20260720.png)。截图证明固定一屏、窄左栏、中部内容区和仅成员右栏的真实构建；正文/规划/图谱/资料专项状态由18项UI集成测试覆盖。

## 6. 正式验收与边界

功能提交后在干净工作树运行 `npm run acceptance`：

- 验收测试3个文件、3项全部通过；
- Schema锁定指向0023；
- release、阶段0—8证据、依赖锁、Node版本、无AI智囊团运行时依赖、无硬编码密钥、无代码占位、数据忽略、桌面入口和使用说明全部通过；
- `failures: []`。

本次没有调用付费模型，没有写入API Key，没有声称自动状态抽取或长篇创造性达到E3/E4。真实模型跨题材的状态抽取准确率、数值连续性和文学非劣效仍按既有纵向评测门禁积累证据。
