# 首页多书任务中心验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 决定：DEC-073
- `design_review_id`：`DR-20260731-global-task-center-v1`
- 日期：2026-07-31

## 交付结果

- 书架首页左栏提供书架、任务、团队和设置。
- 书内左栏移除任务，只保留对话、规划、正文、图谱、资料库和版权。
- 首页任务中心按活动书分组显示进行中任务、每书最近任务、预算和待确认。
- 任务详情显示所属书；取消、重试和确认显式使用所属 `book_id`。
- 首页只调用轻量 `/api/v1/task-center`，不为每本书下载完整工作区、全部章节、消息或卷树。
- 返回书架和切换书籍不暂停、不取消后台任务。

## 自动验证

- 工作区API专项：4/4通过；验证轻量聚合响应包含书、可见任务、任务相关章节、预算和待确认，且不包含卷树、消息数和创作会话。
- 工作区UI专项：35/35通过；验证首页入口、书内无任务、多书分组、跨书取消/重试/确认和零完整workspace请求。
- `npm.cmd run verify`：通过。
  - API、Web、Worker和测试类型检查通过。
  - 130个测试文件、469项测试全部通过。
  - API、Web和Worker生产构建通过。
- `npm.cmd run migrate` 连续执行两次：均为 `applied: []`，Schema 33，证明本任务无迁移且现有迁移幂等。
- `npm.cmd run acceptance`：3/3验收测试通过；提交前审计唯一失败为预期的“工作树不干净”，提交后重新执行。
- `git diff --check`：通过。

## 本地运行验证

对重新构建并安全重启后的文秘写作执行真实HTTP探针：

- `GET http://127.0.0.1:43111/health`：`status=ok`，release匹配。
- `GET http://127.0.0.1:43110`：HTTP 200。
- 建立HttpOnly本地会话后调用 `GET /api/v1/task-center`：HTTP成功，当前真实库返回1本活动书。

运行验证只重启 `D:\wenmixiezuo` 的文秘写作服务，没有停止、重启或修改 `D:\AI智囊团`。

## 数据与回滚

本任务没有数据库迁移，没有修改任务状态机、Worker写路径、正文、正史、岗位提示词或模型上下文。回滚只需Git revert本任务的只读接口、首页视图和文档，不需要生产数据恢复。
