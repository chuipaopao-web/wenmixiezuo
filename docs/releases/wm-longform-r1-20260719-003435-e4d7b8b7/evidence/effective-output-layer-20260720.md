# 有效输出层实现与验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- `design_review_id`：`DR-20260720-02`
- 决定：DEC-039
- 日期：2026-07-20（Asia/Shanghai）

## 交付结果

- 普通小文秘书/岗位模型回复在原有一次调用内按结构化合同返回，服务端只做白名单解析、确定性套话清理和完全重复段落去重，没有增加第二次模型调用。
- 默认消息保留直接结论、关键依据、风险/未知、少数替代和必要动作；同次调用允许公开的完整最终回复带SHA-256保存在 `effective_output` 引用中。
- 剧情讨论首屏显示主编收口和独立岗位意见数量，展开后可见每名参与者与主编的完整允许公开回复；讨论决定继续使用完整主编结果。
- 结构解析失败、合同越界、长单段或引用损坏均安全回退；不按字符数截断。正式正文、临时试写和三席完整点评不进入该展示层。
- 前端提供“查看完整回复/收起完整回复”，保留附件解析，使用原生按钮和 `aria-expanded`。

## 设计与自检

- `node .agents/skills/wenmi-longform-quality/scripts/validate-audit.mjs docs/EFFECTIVE_OUTPUT_LAYER_AUDIT.md`
  - 结果：`PASS: docs/EFFECTIVE_OUTPUT_LAYER_AUDIT.md`
- 依据项目“业务代码由当前Codex单独完成”约束，本次没有调用其他开发Agent，也不声称人员独立审查；代码复核由当前Codex按差异、数据生命周期、失败回退、Token调用数和创造性非劣效边界逐项自检。
- 自检发现并修复两项问题：正则 `.exec` 触发应用层SQL边界扫描；fallback展开内容曾错误复用清理文本。最终分别改为无歧义匹配写法和“默认清理、展开原始最终回复”。

## 自动验证

1. 有效输出、开放回复、讨论与UI专项：4个测试文件，31项全部通过。
2. `npm run verify`：
   - API、Web、Worker和测试工程类型检查通过；
   - 103个测试文件、232项全部通过；
   - API、Web、Worker生产构建通过。
3. `npm run migrate` 连续执行两次：`currentVersion: 19`、`applied: []`。
4. 消息分页、任务恢复、备份恢复、Repository跨书隔离专项：4个测试文件、7项全部通过。
5. `npm run verify:backup`：
   - 备份：`backup-2026-07-19T21-17-18-119Z-cea32ffc`；
   - 生产库完整性：`ok`，外键错误0；
   - 隔离恢复：通过，恢复副本验证后已销毁；
   - 数据库哈希：`710dcffc24d0fc9e42fc14e10ea53a4b31e63013f0b26e65ffd3284f376e3d20`。
6. 文秘写作自身三进程按受校验控制记录安全重启：`127.0.0.1:43110` Web可访问，`127.0.0.1:43111` API健康，Worker readiness为 `ready`；未修改、停止或重启 `D:\AI智囊团`。
7. `git diff --check` 通过。

## 提交后验收

- 核心功能提交：`6ce2080`（`feat: add effective agent output layer`）。
- 核心提交后在干净工作树执行 `npm run acceptance`：3个验收文件、3项全部通过；发布审计全部检查通过，`failures: []`。
- 提交前的同一命令也曾运行；当时功能验收3/3通过，审计仅按设计报告工作树尚有待提交变更。核心提交后的干净工作树结果是最终正式验收依据。

## 证据边界与回滚

- 当前证据达到E2工程闭环；没有真实跨模型大样本盲评，因此不声称已经取得E3/E4文学质量提升。
- 真实模型JSON遵循率、老板对精简度的长期满意度和不同岗位的展开率仍需运行数据验证；失败时优先原样展示而不是强制压缩。
- 回滚使用非破坏性 `git revert`；Schema未改变，不恢复或改写历史消息、正文、正史和模型调用记录。
