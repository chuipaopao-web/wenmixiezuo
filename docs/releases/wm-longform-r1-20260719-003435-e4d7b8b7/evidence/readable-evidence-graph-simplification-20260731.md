# 作者可读证据中心与叙事图谱精简验收

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 日期：2026-07-31
- 任务：`UX-20260731-03`
- 决定：`DEC-072`
- 实现提交：`73ed56b08d74c6c9c6908c55f5d45f1bd5b3894d`

## 交付结果

1. 资料库恢复“来源与证据”入口，按资料对象归类事实。
2. 同一对象下关系和值相同的事实只显示一次。
3. 页面只显示中文事实名、事实值、来源章、证据等级、权威状态和安全摘录。
4. 序列化JSON只解析白名单摘录，不显示内部ID、英文关系键、原始JSON或来源数组。
5. 叙事图谱作者导航移除“主线”“支线”，保留人物关系、情绪、钩子与伏笔、信息差。
6. 后台事实、证据和 `mainline` / `subplot` 历史投影全部保留；本次无数据库迁移、无正史修改、无生产数据删除。

## TDD证据

- 红灯：工作台回归测试在“来源与证据”按钮不存在时失败。
- 绿灯：新增可读证据中心、去重与JSON防泄露后，工作台专项 `35/35` 通过。
- 回归断言：证据入口存在；重复事实只出现一次；内部ID、英文关系键、原始JSON不出现；图谱“主线”“支线”按钮不存在；其余四类仍可进入。

## 完整验证

| 门禁 | 结果 |
| --- | --- |
| `npm.cmd run typecheck` | 通过，API/Web/Worker/测试类型检查无错误 |
| `npm.cmd test -- --reporter=dot` | 130个测试文件、469项测试全部通过 |
| `npm.cmd run build` | 通过；Web生产构建转换4562个模块 |
| `npm.cmd run migrate` 连续两次 | 通过；Schema版本33，均无待应用迁移 |
| 本地Web探针 `127.0.0.1:43110` | HTTP 200，根节点存在 |
| 本地API探针 `127.0.0.1:43111/health` | `wenmi-api` 状态 `ok` |
| `npm.cmd run verify:backup` | 通过；49个文件，SQLite完整性正常、外键违规0、隔离恢复后丢弃 |
| `git diff --cached --check` | 通过 |
| 实现提交后 `npm.cmd run acceptance` | 3个验收测试全部通过，审计无失败，工作树干净 |

## 风险与回滚

- 风险：部分作者曾从图谱进入主线或支线，移除后需要转到剧情总纲、创作会话、章纲或开放线程。
- 停止条件：作者查找主支线的成本明显上升、证据来源丢失、内部字段重新泄露或事实重复。
- 回滚：回退实现提交即可恢复作者入口；后台数据未删除，因此无需生产数据恢复。
