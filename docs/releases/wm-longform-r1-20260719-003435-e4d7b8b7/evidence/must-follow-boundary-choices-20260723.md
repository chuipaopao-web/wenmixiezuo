# “必须遵守”快捷标签恢复证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 决定：`DEC-047`
- 功能提交：`d6e955c`

## 根因与修复

- 根因：DEC-046完整开书界面替换旧弹窗时，只迁移了 `mustFollow: string[]` 提交字段，遗漏旧版四组标签目录和选择控件，导致界面退化为纯文本框。
- 修复：服务端分类目录恢复感情与关系、主角体验、内容尺度、结构与结局四组24项；前端恢复多选按钮，保留自定义文本，并增加“无额外限制”互斥入口。
- 数据边界：快捷选择与自定义内容合并、去重后仍由服务端执行1—12条校验；未改迁移、正史或正文。

## 验收

| 门禁 | 结果 |
|---|---|
| 红测 | 分类合同缺 `boundaryGroups`、UI缺快捷标签，两项均按预期失败 |
| 目标回归 | 2个测试文件、23项通过 |
| `npm.cmd run verify` | 117个测试文件、357项通过；API/Web/Worker类型和生产构建通过 |
| 隔离运行 | Schema 25、38分类、2频道、4组边界、24项快捷标签 |
| `npm.cmd run acceptance` | 3/3通过；静态审计 `failures: []` |

未调用付费模型，未触碰 `D:\AI智囊团`。
