# 固定工作台与单一左侧导航验收证据

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`
- 增量任务：`UI-20260719-01`
- 决定：`DEC-032`
- 验证日期：2026-07-19
- 负责人：当前Codex，未调用其他开发Agent

## 需求覆盖

| 要求 | 实现与证据 |
| --- | --- |
| 首页固定一屏 | `html`、`body`、`#root` 和 `.app-shell` 使用有界动态视口高度并禁止根页面滚动；源码回归测试固定检查这些契约。 |
| 内容仍可回看 | 左栏、右栏、聊天流、正文、资料和任务区保留独立 `overflow: auto`；1600×900真实Chrome截图可见聊天区和成员栏各自滚动条。 |
| 功能统一移到左侧 | 左栏唯一“创作功能”导航包含对话、规划、正文、图谱、资料库、版权六项；自动测试逐项按无障碍名称查找。 |
| 消除重复入口 | 中部 `.workspace-tabs` 和左栏旧“规划成果/知识与正史/版权研究”入口被删除；自动测试断言不存在。 |
| 原位置显示书籍信息 | 43px中部信息栏显示真实书名、中文状态、卷数、章数和正史修订；活动/归档状态均有回归测试。 |
| 保持窄侧栏 | 桌面仍为176px左栏和190px右栏；移动端继续使用既有抽屉，未增加UI依赖。 |

## 新鲜验证

- Web专项：`npm test -- --run tests/integration/experience/workspace-ui.test.tsx`，1个测试文件、12项测试通过。
- 全量门禁：`npm run verify`，API/Web/Worker类型检查通过，100个测试文件、211项测试通过，三端生产构建通过。
- 迁移：`npm run migrate`，活动数据库Schema 18，`applied: []`，无迁移漂移。
- 运行：安全停止并重新启动文秘写作自身服务；`http://127.0.0.1:43111/health` 返回 `ok` 和正确release，Web入口返回HTTP 200。
- 视觉：Chrome headless 1600×900截图位于本机忽略目录 `data/verification/workspace-fixed-shell-final.png`；人工检查确认左侧六入口、书籍信息栏、内部滚动和固定输入区。截图不进入Git，不包含密钥。
- 差异：`git diff --check` 通过。

## 边界与回滚

- 未修改数据库、API、Worker、RAG、Agent岗位、模型绑定或运行数据；未触碰 `D:\AI智囊团`。
- 本证据只证明本次UI行为和既有工程门禁，不升级任何长篇文学质量E3/E4结论。
- 回滚使用本增量提交的非破坏性 `git revert`；运行数据不需要恢复。
