# 运行时创作链修复记录（2026-07-27）

## 修复范围

- 章节跨度与规划落库解析支持Markdown标题、代码围栏、多行JSON和前后说明文字。
- 锁定规划保留主编原始结构化落库证据，作者界面继续使用精简有效回复。
- 故事弧采用建议章节跨度；未来章纲仍只细化1—3章，避免把滚动章纲数量误当整段剧情长度。
- 任务完成、失败、取消均同步终结任务阶段，前端不再显示假“工作中”。
- 自动接管只允许初始主编单向切换一次；副编故障不会自动切回形成乒乓接管。
- 通知显示当前真实活动主编；自然锁定语句可携带补充条件；相邻重复秘书通知去重。
- 有效输出中的非空字符串列表不再被误判为空。
- 没有可锁定结论时返回409业务说明，不再显示“内部错误”。
- 桌面启动兼容开发宿主同时注入 `Path`/`PATH` 的情况。

## 验证证据

- `npm.cmd run verify`
  - 类型检查通过；
  - 124个测试文件、403项测试全部通过；
  - API、Web、Worker生产构建通过。
- 空库迁移：从0001至0029全部成功，schema version 29。
- 现有库升级：无待执行迁移，schema version 29。
- 独立API运行：`/health` 返回 `status=ok`。
- 文秘写作桌面服务重新启动成功，Web `43110`、API `43111`、Worker均就绪。

## 现有书籍状态核验

活动书《这游戏上线就给钱》目前只有一份草稿设定，没有活动总纲、卷纲、章纲或正文。原因是旧版本的锁定规划任务在解析章节跨度时失败，数据库并不存在可供前端显示的正式产物，不是前端把产物隐藏了。

修复后已执行一次真实副编受理调用并成功结束，但该调用只是持续会话回复，不是“双编剧讨论后的主编方向结论”，因此系统按正史门禁拒绝直接锁定，未伪造规划资料。继续生成正式规划前，必须先完成一次主编主持的双编剧方向讨论，再由老板锁定和确认。

## 预算说明

活动书仍有46,000 Token预留，对应三次供应商结果未知的中断调用（30,000 + 8,000 + 8,000）。根据不可重复扣费与结果未知保护规则，未在没有供应商终态证据时擅自释放。它们不是当前仍在运行的任务。
