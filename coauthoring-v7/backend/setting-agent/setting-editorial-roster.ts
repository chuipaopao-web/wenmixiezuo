/** Compatibility entry: one implementation lives in the independent creation runtime. */
export { V7_SETTING_MEMBERS, settingMembersForRole, validateSettingEditorialRoster } from '@wenmi/opening-runtime';
