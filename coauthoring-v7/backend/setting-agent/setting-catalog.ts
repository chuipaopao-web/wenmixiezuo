/** Compatibility entry: one implementation lives in the independent creation runtime. */
export { V7_SETTING_CATALOG, activeSettingCatalog, settingItemByKey, settingTopicKey, deputyNeeded } from '@wenmi/opening-runtime';
