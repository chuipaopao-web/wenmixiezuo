# 文秘写作 V7 交接

> V7 已完成生产替换，是当前唯一产品。旧版页面、工作流、作者数据和接口不得恢复到当前入口。

## 当前运行事实

- 生产作者端：`https://wenmixiezuo.com/`
- 生产独立后台：`https://admin.wenmixiezuo.com/`
- 本地组合入口：`http://127.0.0.1:43110/`，后台位于 `/v7/`
- 生产 API、Worker、Caddy 均运行 V7；最近一次独立后台静态发布版本为 `da36f2ab404b080fc9f7`
- 当前分支：`codex/light-coauthoring-v7`
- 当前唯一清理工单：`docs/worklists/V7-ONLY-CUTOVER-CLEANUP-20260830-67.md`

## 已上线能力

- V7作者端：首页、开书、设定、时光机、卷、链、章、V7资料库、任务、团队、会员与反馈。
- V7独立后台：创作资产、平台运营、成员治理、模型与岗位、提示词与上下文、任务追溯、功能台账。
- 共享平台底座：账号密码登录、会话、会员、安全、用量、备份、恢复和管理员权限。
- Agent执行底座：TaskContract、ContextPack、PromptManifest、模型快照、用量记账、失败恢复和状态审计。
- 创作可信边界：作者确认规划与正文实际分离；正文保留不可变完整版本；正式更新携带来源、版本和书籍范围。

## 当前唯一产品边界

- V7不读取或展示旧版书籍、资料库、任务、正文和工作流。
- 旧作者端及旧专用API入口已经退出workspace、构建、启动与生产路由。
- 账号、会员、反馈、安全和用量属于平台能力，不因旧版源码清理而删除。
- 已合并迁移和带历史后缀的数据库表名必须保持兼容；它们只是存储历史，不是旧产品入口。
- 本地数据库、备份、浏览器档案和发布副本不是生产权威。永久删除其中的作者数据仍需精确预览与双确认。

## 当前 Agent 与工作流

- 重要规划默认使用 Kimi K3、DeepSeek V4 Pro、GLM 5.3。
- 开书、设定、时光机、卷、链、章纲、正文、审查与结算均使用节点任务合同和分层资料包；不再默认让整支团队读取同一大包。
- 作者可以在允许的节点选择一至三名成员；失败只重试失败席位，不清空已完成方案。
- 时光机展示全书方向、卷级简述和真实进度仪表；卷、链、章各自承担不同信息密度，不共享同一产品页面。
- 任务、团队和当前页面展示相同的真实成员状态，失败必须道歉并给出恢复入口。

## 新任务最小阅读

1. 根目录 `AGENTS.md` 和 `HANDOFF.md`。
2. 当前唯一工单。
3. 只读取受影响代码与对应Skill；不要把历史工单当规格。
4. 涉及数据、接口或部署时，按需读取根目录 `docs/DATA_MODEL.md`、`docs/API.md`、`docs/DEPLOY.md`。

## 清理进行中

- 仍被V7使用但带旧版本文件名的共享合同、Skill、模板和核心服务正迁移到中性命名。
- 过期交接文档已改成生产V7事实。
- 本地约30GB数据库、备份、浏览器与发布副本待生成精确影响预览；未收到双确认前不会永久删除。
- Git工作树包含完整V7开发成果，禁止重置和强推；通过全量门禁后建立V7当前基线提交。

