# 文秘写作 V7 交接

> V7 已完成生产替换，是当前唯一产品。旧版页面、工作流、作者数据和接口不得恢复到当前入口。

## 当前运行事实

- 生产作者端：`https://wenmixiezuo.com/`
- 生产独立后台：`https://admin.wenmixiezuo.com/`
- 本地组合入口：`http://127.0.0.1:43110/`，后台位于 `/v7/`
- 生产 API、Worker、Caddy 均运行第81批 V7 `wm-v7-20260902-100400-40c109f8`、Worker ready，静态版本保持第80批 `363e8b5c7989d05e8fba`
- 静态发布本批未切换（`static_switch_required=0`），manifest SHA-256 为 `7d5e34a71de63bd9ff90606571368b890f91dc359ebeafad29019e1ac72a971d`；第78批服务 `wm-v7-20260901-230200-78a11ce0` 保留为部署脚本声明的回滚点，静态 previous 指针为 `37fde3ad807a5b4c87c6`
- 当前分支：`codex/light-coauthoring-v7`
- 当前最新工单：`docs/worklists/V7-CREATION-CHAR-BUDGET-RECOVERY-20260902-81.md`

## 已上线能力

- V7作者端：首页、开书、设定、时光机、卷、链、章、V7资料库、任务、团队、会员与反馈。
- V7独立后台：创作资产、平台运营、成员治理、模型与岗位、提示词与上下文、任务追溯、功能台账。
- 共享平台底座：账号密码登录、会话、会员、安全、用量、备份、恢复和管理员权限。
- Agent执行底座：TaskContract、ContextPack、PromptManifest、模型快照、用量记账、失败恢复和状态审计。
- 创作可信边界：作者确认规划与正文实际分离；正文保留不可变完整版本；正式更新携带来源、版本和书籍范围。

## 当前唯一产品边界

- V7不读取或展示旧版书籍、资料库、任务、正文和工作流。
- 旧作者端及旧专用API入口已经退出workspace、构建、启动与生产路由。
- 账号、会员、反馈、安全和用量属于平台能力，不因旧版源码清理而删除。
- 已合并迁移和带历史后缀的数据库表名必须保持兼容；它们只是存储历史，不是旧产品入口。
- 本地数据库、备份、浏览器档案和发布副本不是生产权威。永久删除其中的作者数据仍需精确预览与双确认。

## 当前 Agent 与工作流

- 重要规划默认使用 Kimi K3、DeepSeek V4 Pro、GLM 5.3。
- 开书、设定、时光机、卷、链、章纲、正文、审查与结算均使用节点任务合同和分层资料包；不再默认让整支团队读取同一大包。
- 作者可以在允许的节点选择一至三名成员；失败只重试失败席位，不清空已完成方案。
- 时光机展示全书方向、卷级简述和真实进度仪表；卷、链、章各自承担不同信息密度，不共享同一产品页面。
- 任务、团队和当前页面展示相同的真实成员状态，失败必须道歉并给出恢复入口。

## 新任务最小阅读

1. 根目录 `AGENTS.md` 和 `HANDOFF.md`。
2. 当前最新工单。
3. 只读取受影响代码与对应Skill；不要把历史工单当规格。
4. 涉及数据、接口或部署时，按需读取根目录 `docs/DATA_MODEL.md`、`docs/API.md`、`docs/DEPLOY.md`。

## 当前清理状态

- 旧产品页面、专用 API、Worker 循环、本地向量运行时、旧依赖、旧测试和重复文档已从当前树删除。
- 第 71 批只使用隔离测试数据，未删除、覆盖或回填任何作者生产数据；此前有收据的历史本地清理不得重复执行。
- 当前 106 个已合并迁移全部保留，既有 102 个历史迁移字节未改写；第 71 批新增 0104/0105 和第74批0106只做前向兼容。第74批全量门禁及任务中心补丁的受影响门禁通过；六个工作区类型检查、API/Worker/双前端构建和静态组合包校验通过。
- 老板已确认删除旧开书／团队／岗位提示运行闭包，以及 V7 未使用的 Codex、opencodego 模型通道；对应当前源码、配置、启动变量和测试已经移除。
- 成员没有固定专业人设；V7 治理只保存成员身份、可用模型和任务责任，书籍题材融合人设按具体任务装配。
- 第72批在新知情确认后删除21个零运行引用旧源码、3个重复43180本地启动器、1个一次性标签脚本及3个旧闭包测试；105个迁移和全部作者生产数据继续保留。运行闭包门禁冻结208个运行源码、18个运维源码、14个运维资源、105个迁移和8个静态资源。
- 第73批继续删除旧规划配方、旧人物成员、退役模型别名和零引用运行文件；开书、设定、规划、人物、创作任务都以冻结的完整成员绑定恢复，损坏或退役历史任务只读。运行闭包v2冻结208个运行源码、13个运维源码、14个运维资源、105个迁移、8个静态资源和24个构建输入，14个当前模块、68项当前能力均有真实入口证据。
- 第74批修复开书返回/重进/新建白屏、设定部分失败和重新设计卡死、会员门禁错误成员事件、失败恢复与页面操作区；删除12个零V7引用的旧Canon/知识生命周期运行与纯旧测试文件。运行闭包v2现为201个运行源码、13个运维源码、14个运维资源、106个迁移、8个静态资源和24个构建输入，14个当前模块、68项当前能力保持不变。
- 第75批核对最近滚动72小时14个活跃账号，账号、owner和会员关系全部正常；4项确认终止且无候选、调用、未知结果或租约的旧版假供应商失败已可恢复归档，13项有效待作者确认资料保留。会员未开通、到期或算力不足现在保留真实门禁并立即停止，不再错误换成员或残留假供应商故障；最终活动、未归档终止及working/unknown调用均为0。
- 第76批修复时光机历史任务覆盖正式成果、候选读取过宽、开书额度错误被通用失败页隐藏和同一规划请求跨实例重复认领。六章书的正式规划、章纲、正文、结算与正式化记录均未改写；指定账号正常但额度低于AI开书安全预留，现有准确阻断和手工恢复。当前2核4GiB单机、SQLite、单Worker不具备一万人同时在线证据。
- 第77批补齐规划树资料策划任务中的正式设定 `sourceId/sourceKind/sourceVersion`，并把历史成功输出中的 `itemKey` 仅在当前冻结快照内唯一归一为版本 `sourceId`；严格跨书与未知引用校验不放宽，旧成功调用可复用且不重复扣量。
- 第78批统一了时光机与任务中心的当前任务选择：只接续同路线最新框架任务，旧只读名册按当前正式来源幂等创建替代任务；少量已确认设定不再因历史失败总审永久锁死，4xx保留真实业务处理办法，历史链损坏也不会抹掉已定稿章数。历史任务和作者成果只读保留。
- 统一任务中心现在读取按会话作者隔离的设定任务投影，失败统一整理计入待处理数；题材档案等内部批次、过期方案和内部错误字段不进入作者端。“继续处理”直达对应书籍的设定失败位置，刷新后仍能继续。
- 规划、创作和人物任务现在会先幂等确保当前书级题材融合档案；同书并发只生成一次，未知结果不盲目重发，明确失败复用冻结资料。任务失败卡片统一先说“对不起，这次没有完成”，再说明结果已保存。
- 实现提交 `3eb2d83` 的干净全量门禁626项通过、3项既有条件跳过，六工作区类型检查和构建通过。第72批已经通过生产副本预检、历史会员异常筛查、正式备份、连续30秒零在途任务、逐服务切换和公网资源验证；生产迁移保持105个且本批无新增应用。
- 实现提交 `3e18702` 的第73批门禁通过：根项目75个测试文件367项、Backend 86项Vitest加12项Node合同、作者端143项、后台43项通过且3项既有条件跳过；类型检查、API/Worker/双前端构建和静态包校验全部通过。生产副本预检、数据库备份、连续30秒零任务、逐服务切换、作者端/后台/静态资源/权限/会员异常/服务日志公网复核均通过，迁移保持105个且 `applied=[]`。
- 第74批任务中心补丁实现提交 `b6b8501`，作者端168项、设定集成32项、作者专项75项和API/作者类型检查通过。生产静态9文件 manifest 为 `e003e0ad3441561475a3b39a2379171161e8ec7a80e90a944770245f2beb0d78`，回滚备份 SHA-256 为 `353c5f54cfe27c913e03c7a31b3d547cd165a311b22272cf8a34bdf33f810e3e`；连续30秒零任务、逐服务切换、权限/资源/日志与真实作者桌面和390px恢复复测通过。
- 第75批实现提交`8a89c06`，Linux原生全量门禁通过根项目381项、Backend 86+12项、作者端168项和后台43项，静态manifest为`cfe51abcafcb2a7615d96405747e95656a47d5f394f23ccbc38e20019eb85318`，回滚备份SHA-256为`dc3afe40e2aed17be4ee219c544830aaed56e484052b21b8db773db6da9353aa`。登录态真实浏览器进入空白2000字开书页并刷新不恢复旧任务；上线后真实开书约2分30秒生成2份候选并进入待确认，未知调用为0。
- 第76批实现提交 `0fca168`，Linux隔离全量门禁、专项并发与时光机测试、106项迁移与两次no-op、隔离API、完整备份、连续30秒零任务和公网资源验证通过。登录态浏览器验证新建不白屏、不接管无关任务、实际可输入2000字，390×844无横向溢出且任务页恢复准确；未提交真实模型任务。完整备份为 `20260901T064600Z-345664`，当前仍缺异地副本、告警和正式恢复演练。
- 第77批实现提交 `e20b071`，生产隔离 `verify:full`、规划/时光机专项、两次迁移no-op、隔离API、完整备份 `20260901T123453Z-r77`、连续30秒零在途和公网资源验证通过。发布后服务错误日志为0；指定账号、会员、书籍、正式路线和15次失败记录保持不变，真实模型恢复仍由作者在已登录页面触发。
- 第78批产品提交 `780c3d8`、发布稳定性提交 `038fccc`；本地和生产隔离完整门禁最终均为746项通过、3项既有条件跳过，六工作区类型检查和构建通过。完整备份 `20260901T153310Z-r78`、两次迁移no-op、隔离API、连续30秒零任务、公网资源和切换后日志均通过。生产验收账号真实创建路线任务，旧409已消失；额度不足准确显示且任务中心只有最新任务可继续，390×844操作可达。
- 生产当前保留第78批当前源码、第77批立即回滚源码和第76批更早回滚源码。发布前只清理两个未引用旧发布的可重建依赖缓存及npm/apt缓存，释放952,832,000字节；旧V6隔离数据库、作者生产数据、106个迁移、数据库回滚副本和完整备份均未删除。
- 第81批 `wm-v7-20260902-100400-40c109f8` 已于 2026-09-02 12:51 CST 部署上线；源码包 SHA-256 为 `8a0cfa3c0d60b0501573cdea36719ac419b613e16d560fe7a185f6f477a9c8a3`，完整备份 `20260902T045012Z-418412`，`/health` 返回 `status=ok`、`worker=ready`、`canStartModelTasks=true` 且 releaseId 匹配。部署前 prune 只删除未引用旧发布和失败半成品目录，作者数据、数据库、备份和回滚版本未触碰；部署物料脚本修正了闭包报告目录权限和备份锁自锁问题。
- Git工作树包含完整V7开发成果和本轮清理，禁止重置和强推。

## 第 71 批真实回归

- 隔离测试作者提交 1308 字开书想法并实际入库；测试书“边城沙盘”已访问开书资料、设定、时光机、卷、链、章、资料库、任务、团队和会员/反馈。
- 桌面与 390px 均无横向溢出；设定失败保留结果，时光机失败可继续未完成步骤、返回全书方向或设定修改；反馈真实写入隔离库。
- 目录外题材现在返回可修正的 400 领域错误；任务页只显示 48 字开书想法摘要；青铜升级付费从办理日开始，后台默认白银/98元并显示“升级并记录收入”。
- 最终隔离运行探针使用新数据库完整执行105个迁移；API健康返回本地 `RELEASE_ID`，新作者先获得到期为2099年的青铜体验，再升级白银，周期精确为办理日起一年、98元流水记为 `grant`；同一幂等编号重放不延长到期日，后台活跃付费用户统计为1。探针结束后只删除本轮生成的临时构建与隔离数据库，未触碰作者数据。
- 上述创作流程仍是本地隔离的深度交互证据；其同源第72批构建已上线，并另行通过生产版本、静态资源、健康、权限、迁移、任务与服务日志核验，不能把两类证据混为一次线上作者数据测试。
