# 文秘写作 V7 交接

> V7 已完成生产替换，是当前唯一产品。旧版页面、工作流、作者数据和接口不得恢复到当前入口。

## 当前运行事实

- 生产作者端：`https://wenmixiezuo.com/`
- 生产独立后台：`https://admin.wenmixiezuo.com/`
- 本地组合入口：`http://127.0.0.1:43110/`，后台位于 `/v7/`
- 生产 API、Worker、Caddy 均运行第72批 V7 `wm-v7-20260831-213000-c42451f0`、Worker ready，静态版本为 `4ca84707c13a83435548`
- 静态发布共9个文件，manifest SHA-256 为 `d99f2a871458537b6f25a0bbb31c56f88d4b83c5e6062c4f8817001d77ac0735`；上一线上服务 `wm-v7-20260831-183000-83eebb2a` 与静态版本 `24717cb9eac7309dd487` 保留为回滚点
- 当前分支：`codex/light-coauthoring-v7`
- 当前唯一工单：`docs/worklists/V7-FULL-AUTHOR-RUNTIME-CLOSURE-20260831-73.md`

## 已上线能力

- V7作者端：首页、开书、设定、时光机、卷、链、章、V7资料库、任务、团队、会员与反馈。
- V7独立后台：创作资产、平台运营、成员治理、模型与岗位、提示词与上下文、任务追溯、功能台账。
- 共享平台底座：账号密码登录、会话、会员、安全、用量、备份、恢复和管理员权限。
- Agent执行底座：TaskContract、ContextPack、PromptManifest、模型快照、用量记账、失败恢复和状态审计。
- 创作可信边界：作者确认规划与正文实际分离；正文保留不可变完整版本；正式更新携带来源、版本和书籍范围。

## 当前唯一产品边界

- V7不读取或展示旧版书籍、资料库、任务、正文和工作流。
- 旧作者端及旧专用API入口已经退出workspace、构建、启动与生产路由。
- 账号、会员、反馈、安全和用量属于平台能力，不因旧版源码清理而删除。
- 已合并迁移和带历史后缀的数据库表名必须保持兼容；它们只是存储历史，不是旧产品入口。
- 本地数据库、备份、浏览器档案和发布副本不是生产权威。永久删除其中的作者数据仍需精确预览与双确认。

## 当前 Agent 与工作流

- 重要规划默认使用 Kimi K3、DeepSeek V4 Pro、GLM 5.3。
- 开书、设定、时光机、卷、链、章纲、正文、审查与结算均使用节点任务合同和分层资料包；不再默认让整支团队读取同一大包。
- 作者可以在允许的节点选择一至三名成员；失败只重试失败席位，不清空已完成方案。
- 时光机展示全书方向、卷级简述和真实进度仪表；卷、链、章各自承担不同信息密度，不共享同一产品页面。
- 任务、团队和当前页面展示相同的真实成员状态，失败必须道歉并给出恢复入口。

## 新任务最小阅读

1. 根目录 `AGENTS.md` 和 `HANDOFF.md`。
2. 当前唯一工单。
3. 只读取受影响代码与对应Skill；不要把历史工单当规格。
4. 涉及数据、接口或部署时，按需读取根目录 `docs/DATA_MODEL.md`、`docs/API.md`、`docs/DEPLOY.md`。

## 当前清理状态

- 旧产品页面、专用 API、Worker 循环、本地向量运行时、旧依赖、旧测试和重复文档已从当前树删除。
- 第 71 批只使用隔离测试数据，未删除、覆盖或回填任何作者生产数据；此前有收据的历史本地清理不得重复执行。
- 当前 105 个已合并迁移全部保留，既有 102 个历史迁移字节未改写；第 71 批新增 0104/0105 只做前向兼容。最终全量门禁632项通过、3项既有跳过；六个工作区类型检查、API/Worker/双前端构建和静态组合包校验通过。
- 老板已确认删除旧开书／团队／岗位提示运行闭包，以及 V7 未使用的 Codex、opencodego 模型通道；对应当前源码、配置、启动变量和测试已经移除。
- 成员没有固定专业人设；V7 治理只保存成员身份、可用模型和任务责任，书籍题材融合人设按具体任务装配。
- 第72批在新知情确认后删除21个零运行引用旧源码、3个重复43180本地启动器、1个一次性标签脚本及3个旧闭包测试；105个迁移和全部作者生产数据继续保留。运行闭包门禁冻结208个运行源码、18个运维源码、14个运维资源、105个迁移和8个静态资源。
- 规划、创作和人物任务现在会先幂等确保当前书级题材融合档案；同书并发只生成一次，未知结果不盲目重发，明确失败复用冻结资料。任务失败卡片统一先说“对不起，这次没有完成”，再说明结果已保存。
- 实现提交 `3eb2d83` 的干净全量门禁626项通过、3项既有条件跳过，六工作区类型检查和构建通过。第72批已经通过生产副本预检、历史会员异常筛查、正式备份、连续30秒零在途任务、逐服务切换和公网资源验证；生产迁移保持105个且本批无新增应用。
- Git工作树包含完整V7开发成果和本轮清理，禁止重置和强推。

## 第 71 批真实回归

- 隔离测试作者提交 1308 字开书想法并实际入库；测试书“边城沙盘”已访问开书资料、设定、时光机、卷、链、章、资料库、任务、团队和会员/反馈。
- 桌面与 390px 均无横向溢出；设定失败保留结果，时光机失败可继续未完成步骤、返回全书方向或设定修改；反馈真实写入隔离库。
- 目录外题材现在返回可修正的 400 领域错误；任务页只显示 48 字开书想法摘要；青铜升级付费从办理日开始，后台默认白银/98元并显示“升级并记录收入”。
- 最终隔离运行探针使用新数据库完整执行105个迁移；API健康返回本地 `RELEASE_ID`，新作者先获得到期为2099年的青铜体验，再升级白银，周期精确为办理日起一年、98元流水记为 `grant`；同一幂等编号重放不延长到期日，后台活跃付费用户统计为1。探针结束后只删除本轮生成的临时构建与隔离数据库，未触碰作者数据。
- 上述创作流程仍是本地隔离的深度交互证据；其同源第72批构建已上线，并另行通过生产版本、静态资源、健康、权限、迁移、任务与服务日志核验，不能把两类证据混为一次线上作者数据测试。
