# R190 方法库与成员查询执行闭环

授权：用户要求由负责人安排并完善整个方法供给功能。依R188规范推进：校正库版本化接入、可分页查询/读取、成员主动判断与工具执行、最终上下文清理、上下层引用与后台实际执行记录。不是只更新说明。旧任务/作者作品/账号/账务保留，不重跑作者任务。

分批顺序：1 校正方法版本与查询合同；2 规划/创作共用有界方法工具循环与检查点；3 后台版本/查询/选择/请求体积展示和继承验收；4 工程、真实模型与部署、同源文档状态同步。资料事实仍由已存在的资料准备服务提供，本次不假装实现尚无来源对象的新工具。每批检查后继续，不逐批请求确认。

保护：owner/book服务端绑定、版本冻结、已成功请求复用、未知调用禁止盲目重试、取消后不继续派发、方法允许不用/原创。查询最多4轮8次，不能整库注入。可缓存方法定义，不跨书缓存作品用法。真实模型采用合成书籍样例，不外传作者作品。

验收：原338来源/333校正卡与别名覆盖，旧v3任务可读，新版本重复启动不追加；分页/跨层查询/恶意参数/未知ID/无引用原创；真实网关直出、查工具后设计、预算截止、重放、未知/取消；卷链章参考只含导航或选中卡，不全目录；后台成功空态和移动导航；相应构建、数据库与全量门禁按核心流程风险执行。API发布等待自然零任务30秒及即时复核，留回滚，不取消作者任务制造窗口。

状态：已完成并发布。本批方法闭环完成，专用事实补读工具、新时光机交互不在本批实现范围。

证据：verify-full-final.log各测试套件通过（核心605、方法102、作者231、后台81、运行16）；构建在更新管理来源核对值后build-final.log通过。真实创作网关补测工具循环/最终请求/重复回放通过。real-model.json三种合成故事均通过，方法动作3/1/3次，选材后无查询页；这不是长篇文学质量验收。390/1440真实浏览器通过。

部署：实现635a6ed3；API/Worker wm-v7-20260910-011000-635a6ed3；后台静态c25b6a3fa0efcf59ce23。生产副本仅新增0113，账户/书籍/任务计数不变、外键与完整性检查通过；旧/新方法配置可被兼容回退版本读取。任务自然连续30秒为零后切换，未取消作者任务；静态保留旧哈希资源，作者端字节不变。

回退：wm-v7-r190-compat-635a6ed3基于R189执行逻辑，仅接入v4方法读取和0113迁移，关闭新查询循环；已测试历史和新版本读取。不能直接回退未经兼容的旧二进制。两份早期迁移预检副本无损压缩，解压哈希一致，生产数据库未因此改动。

旧基线修正：两份迁移测试补齐0110–0113预期；4个R189已存在的应用SQL文件记录为明确旧基线，新MethodAgentRuntime仍通过repository访问；后台6处原有节点补代码行号；来源闭包识别?raw资源。未将新业务SQL加入豁免。
