# 第102批：产品决策原则合并与重复Skill清理

## 变更合同

- 目标：将用户明确要求的事实核查、明确建议、授权区分、设计自由四条原则统一到开发流程；删除已无独立职责的产品决策Skill。
- 范围：docs/DEVELOPMENT_WORKFLOW.md、根AGENTS阅读路由、开发流程Skill引用、两份直接相关场景核查、两份HANDOFF及本清单；删除独立产品决策Skill与已合并的旧场景文件。
- 保留：数据安全、授权与费用边界、用户既有偏好、讨论不自动开发、已授权工作不重复审批、按风险验证和真实证据责任；不新建替代Skill，不改变当前产品设计或逐页讨论顺序。
- 不改：产品代码、模型/Agent运行配置、作者数据、数据库、发布号、现有.claude目录；不部署、不推送。
- 验证：直接文件结构/链接、删除项有效引用归零、开发Skill格式校验、四条原则及旧场景覆盖、完整Git差异。只做静态规则核查，不运行产品测试、模型或浏览器，不声称已验证未来Agent行为。

## 合并依据

产品决策Skill仅包含讨论方法与路由，没有脚本、运行资产或独立产品功能。开发流程第1节已有事实核查与结果责任，合并到同一小节；讨论由根AGENTS引向该小节，不扩大开发Skill的触发范围。旧产品场景P01—P09按语义并入开发场景，已有等价场景不重复维护。历史工单中的四个Skill/清理记录保持历史原貌，Git保留被删文件。

## 进度

- [x] 核对当前源文件、Git历史/远程和直接引用；工作区只有既有.claude。
- [x] 合并、删除与静态验证。
- [x] 交接更新；本地提交随本批记录完成。

## 结果与证据

- 开发流程第1节将四条原则合并为唯一正文，同时保留反例、实际结果验收与纠错责任；开发Skill只链接该节，删除重复说明。根AGENTS明确普通产品讨论直接读第1节，开发Skill仍不因讨论而启动。
- 已删除`.agents/skills/wenmi-product-decision/SKILL.md`和旧产品决策场景文件；旧场景映射为P01→D21、P02→D14/D19/D22、P03/P04→D23、P05→D04、P06→D24、P07→D15、P08→D25、P09→D02。现存开发场景共25项，重复规则不再维护两份。
- 静态核查通过：四条原则及旧场景均有对应规则；20个直接本地链接/锚点有效，两个删除目标不存在，活动规则没有旧Skill调用或名称引用。历史工单和Git记录保留，不把历史描述当活动路由。
- 自带quick_validate.py未能运行，原因是当前Python缺少PyYAML；未安装新依赖。使用Node内置文件读取对实际两项简单字符串frontmatter、命名、描述长度/字符及未完成占位做针对性检查，通过；这不是通用YAML解析器或真实Agent行为评测。
- 两份HANDOFF已记录统一入口。完整差异仅含本批9个文档/规则文件，无产品代码、数据、模型配置、RELEASE_ID和其他任务修改；未跟踪.claude保持不动。
- 未运行应用测试/构建、浏览器、模型或生产操作，不推送、不部署。规则合并不能保证开发者永不犯错，后续仍以实际结果与证据验收。
