# R180 后台功能地图读取恢复

根因：RB-19顺序表名称已经改为“单页创意开书与结果采用”，详情标题仍为旧名，线上parseRebuildPlan严格校验拒绝整张地图。此前静态发布只检查功能管理字段和代码指纹，漏掉顺序表与详情标题一致性。

处理：原位统一文档名称，先原子同步线上文档恢复；发布校验加入表/卡片标题唯一性和完整性检查，回归真实文档解析及权限接口。保留现有功能、已确认方案、账号与作品数据，不放宽线上校验或伪造正常状态。

已恢复：线上parseRebuildPlan及readRebuildControl成功读取82单元和实际任务信号，无服务重启。继续补发布前验证。

完成：11项rebuild-control真实文档/管理员与普通账号权限接口回归通过；4项node:test发布检查通过，覆盖旧标题、缺失/重复卡片和CRLF。新增检查接入既有verifyFunctionManagement入口，静态发布脚本已调用此入口；后续发布须携带新版scripts/verify-function-management.mjs。线上文档原子同步后重跑实际readRebuildControl，返回82单元，不模拟空地图。API和静态版本不变，作者数据不变。
