# 第137批：换成员重做选择器样式修复

目标：换成员重做与初次开书使用同样的紧凑头像选择卡片，显示头像、姓名、岗位和已选状态。

交付：2026-09-07 03:31:40已上线，代码65d7d33b，静态8eb2920cb02c0392b813。上线390/1440四场景、头像资源与实际尺寸、radio隐藏、横排与手机布局、选择提交/模拟失败保留通过。作者构建通过（已有大chunk提示保留）。静态资源哈希与受保护接口401通过，后台字节未变，API/Worker PID576175/576194未重启；保留R136静态回滚。无模型调用或数据库修改。

根因：共用DesignerMemberPicker已经接入两个入口，但NewNovelPage.css将头像尺寸、隐藏radio、网格卡片限定在novel-create-surface；确认资料页面使用package-create-surface，导致规则未命中。此前浏览器只核验DOM和点击提交，没有断言头像尺寸与卡片布局，漏检该样式缺陷。

范围：只扩展该选择器样式到opening-redesign-choice，同步rebuild副本，移除重做选择器嵌套边框与额外上边距。保留初次开书选择器、成员名册与选中提交、作者意见、任务和后端。不开新模型调用、不迁移数据库。按持续授权静态独立上线，不重启API或Worker。

验证：390/1440初次/重做四场景浏览器通过；头像由0高恢复42×42，卡片约61像素高，radio隐藏；桌面重做选择器由337降至136像素，手机约275像素，未溢出。图片加载、选择第二成员、原始想法提交和模拟失败后选择保留通过；初次选择器尺寸与改前一致。证据artifacts/rebuild/137。状态：已发布并完成上线验证。
