# 第96批：重构功能地图与配置中心

## 变更合同

- 用户目标：先在独立后台看清全部功能、详细说明、技术路线、开发顺序、当前开发工作、已知问题和运行证据；用新的产品管理入口替换当前资产首页。核实本地源码边界和旧实现退出方式。
- 授权范围：后台首页/导航和功能地图、配置入口；必要只读管理API、直接测试、执行文档与交接。产品管理页面在当前后台入口生效，已有运营、成员、提示、资产、用户、会员和问题处理仍可访问。
- 不改：作者端、模型调用、身份/权限规则、既有数据库语义、支付/费用、生产配置；不删除源码、数据、迁移、回滚资料；不部署、不推送。
- 状态权威：执行文档继续唯一维护重构顺序/进度，后台直接读取其表格和单元卡片；代码存在、开发完成、验收、上线、问题和近期任务信号分别展示。无证据显示未验证，不显示“无BUG/全部畅通”。配置中心复用真正生效的配置入口，不伪造总开关。
- 实现方式：复用现有管理员认证和14项后台能力，以功能地图及配置中心替换默认入口并保留能力对照。新增只读投影，不新建账号体系或数据库。先开发RB-00.1，不改变其他单元ID，不把本批授权扩展到全站重构。
- 验收：全部原66单元和85来源项可定位；新增本批单元纳入顺序；搜索/阶段筛选/详情/依赖导航/刷新可用；进度与源文档一致；文档缺失或格式错误明确失败；普通账号和未登录不能读取；配置导航指向真实功能；任务失败不伪装BUG定论，无记录不判正常。
- 回归与测试：受影响管理API/文档解析/组件测试，API和后台类型检查/构建，源码依赖闭包，后台桌面与390px浏览器走查；保留原导航与认证、问题处理入口。仅新增受现有门禁保护的只读接口，不改变权限规则/数据/任务，未触发全量验证。
- 当前状态：本地实现与验收通过，未部署、未推送；最终证据与剩余问题见下方。

## 核查与执行记录

- 当前应用源码分布在coauthoring-v7（作者端、独立后台、业务引擎）和apps（API、Worker、合同/共享底座），还有scripts/deploy运行与发布支持。当前后台已有功能台账，不能作为新重构状态表。
- 发布文档说明生产使用本地提交归档，但尚未对服务器逐文件比对；不能声称线上全部文件与本地完全一致。生产数据库/文件/环境配置不等同于本地源码。
- 不按V6/V7文件夹名删除代码；后续按功能入口、静态/动态引用、任务所有权、数据兼容、迁移与回滚验收后再退出旧实现。

## 验证与交付

- 数据来源：执行文档1.2直接读取，67行/67卡片/85个来源ID；RB范围引用按顺序展开，不遗漏区间内后台功能。文档丢卡片、状态非法、重复ID、依赖无效、未开发却验收通过和缺失来源章节均拒绝；缺文件503且不泄漏路径。
- 接口与安全：`node node_modules/vitest/vitest.mjs run --configLoader native tests/integration/security/rebuild-control.test.ts tests/integration/security/v7-admin-console.test.ts`通过13项，覆盖未登录401、普通账号403、管理员200、空任务/心跳不伪装正常、失败不被后续成功覆盖及既有管理接口。
- 前端：后台全包47项通过、3项既有条件跳过；随后新增原14入口保留测试并复跑受影响3文件8项通过。最终合计48项通过、3项既有跳过；类型检查通过。
- 构建：backend类型合同构建、API类型检查与隔离输出构建、后台Vite构建通过。主包约596.5kB（gzip约185.4kB），仍有超过500kB提示，未隐藏。机器未提供npm命令，使用项目既有node_modules内对应CLI；不安装新依赖。旧API dist写入受本机ACL限制，改在`.tmp/rebuild96-api-dist`构建，不改权限、不清理现有构建。
- 源码：运行依赖闭包检查通过；本地含生产提交cd7daedb对象。由本地构建/依赖检查可证明构建所需源码齐备；未逐文件比对服务器，生产配置、数据、上传文件不因此认作已有本地副本。
- 浏览器：真实管理员测试会话进入新默认首页；67单元、85来源、搜索注册页、技术路线、依赖跳转、刷新后选中项保留；模型/成员真实管理入口可达。桌面与390×844检查，手机scrollWidth375<=innerWidth390，输入16px。修复详情被顶栏遮挡，复查detailTop约100px。正常使用期间浏览器error/warn为0；刻意断线产生的连接错误是故障注入证据。
- 失败恢复：仅核对并停止本次`rebuild96-server.mjs`进程，点击刷新显示旧数据与过期提示；恢复同一隔离数据库服务后刷新成功、alert清除。测试服务无生产/模型密钥、无Worker，因此无心跳/0任务为真实结果。未调用模型、未修改线上账号/权益/作者数据。
- 本地演示：`http://127.0.0.1:43170/v7/`（Vite），专用API43196，数据仅`.tmp/rebuild96-data`。测试启动脚本/数据库不提交，不作为正式运行或生产启动依赖。
- 限制：这是功能地图与配置入口的交付，不是全后台重构完成；尚未给每项功能接通实时业务探针，故障/畅通显示证据与未验证，不能声明无BUG。后续状态由维护者随实际开发/验收更新源文档；线上读其发布版本的文档。作者端、既有迁移与业务事务无变更。
- 最终文档检查：6份受影响文档的133个本地文件链接及67个单元锚点通过；新旧文件完整差异已按合同复核。只有前端/后端之一已实现时显示“部分实现”，不能显示已开发完等待验收。
