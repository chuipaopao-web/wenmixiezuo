# 第125批：先核对现有开书，再复用模型连接

用户要求开发前检查现有已跑通功能，兼容部分直接接入新后端，避免重写。主代理直接执行。

已核对：现有OpeningAgentEngine有设计、独立审查、修订、成员替补、结构补交与未知调用恢复；OpeningAgentToolGateway已提供6个存取接口。V7OpeningAgentService负责请求/成员快照和恢复；V7OpeningBookService负责确认落书；当前V7OpeningAgentModelGateway直接依赖SQLite会员、调用记录和提示资产。底层ArkPlanModelAdapter与数据库无关，现有套餐请求、思考/输出参数、超时和错误处理可直接保留。HANDOFF第75批有上线后约2分30秒产出开书候选的历史运行记录；本批尚未重新调用生产模型，不能据历史记录承诺当前每个模型正常。

实施范围：先将数据库无关的原模型适配器及配置原样复制到rebuild，复用原测试；用薄执行适配接入第124批PG任务/用量/租约。不另造模型请求协议或重写开书提示词，不改UI、不修改生产任务/作者数据。现有开书执行器与确认流程继续作为待接存储接口的复用来源，不以第124批简化状态机替代它们。

验证：复制文件内容比对、原有模型适配回归、PG与模型执行边界的相关测试（请求范围、结果落档、未知不重发、取消/租约）。涉及跨服务恢复，稳定后集中一次verify:full，不额外委派复验。仅测试替身网络不算真实模型质量通过。

部署：本批不切生产作者入口。需完成原执行器的PG工具接口、完整提示治理快照、原确认建书与真实身份/权益兼容后再切换，不通过保留旧SQLite写入或假owner映射冒充迁移完成。

状态：模型连接复用与PG薄适配本地验收通过；完整开书迁移尚未完成，未部署。

交付：`infrastructure/models/{ark-plan-model,model-adapter,model-runtime-config}.ts`与`domain/roles.ts`复制自现有apps/api，忽略换行/末尾空行后4文件逐字一致；原526行模型测试仅改导入路径，28项通过。依赖undici沿用项目已有7.29.0，不使用安装脚本；新增ExistingOpeningModelExecutor仅适配注册模型、原prompt透传、总预算、发送前落档、租约心跳、可见输出/实际用量存入PG和取消/未知恢复，不生成新提示词，也不把测试模型当生产回退。

验证：新增6项真实PG+替身网络测试通过，覆盖原请求协议/参数、非批准模型和owner范围拒绝、返回结果/用量持久化、思考内容不落档、重复步骤不再次发送、网络未知、明确拒绝、预取消及生成中取消后的结果保留和结算。4文件与原件内容对比通过。稳定后仅一次`run verify:full`通过，日志`artifacts/rebuild/125/verify.log`，125源码边界、类型、141后端/基础及222作者、双Web构建通过；其中新增测试仅6项，另28项为原测试复用。沿用隔离合成test_review124库，无新迁移；没有生产库、真实作者或真实模型调用。

复用边界：原引擎依赖闭包36文件约9373行，接口已经隔离数据库；不因复用盲目搬整份SQLite API。优先适配6个工具接口并保留候选原子提交、源版本、成员快照，再接原SQLite网关中的提示治理快照/用量语义以及原确认建书。现有请求成员字段、作者修订/建议决策、历史任务兼容和确认一致性修复均须保留；第124批简化字段不能替代现有能力。当前新增执行适配器只可由可信后端调用，尚未连接原OpeningAgentEngine、作者HTTP或常驻Worker；不得宣布完整开书或新模型调用已生产上线。

项目路线1.23与HANDOFF已更新，线上路线仍123、生产服务仍R122。下一批复用现有开书引擎和确认逻辑并换PG存取，避免另写设计/审查/修订流程。本批测试属于机制证据，真实模型效果仍引用历史记录且未重新验证。
