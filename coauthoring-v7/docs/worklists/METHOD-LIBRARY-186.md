# R186：完整方法库归层与导航修复

状态：本批方法库与导航已上线；完整全书方向生成探针超时单独保留，不列为通过。用户明确纠正R185只提供25项的错误，要求核查原库、按真实适用层完整提供、去除同义重复并保留不同用途，取消勾选开关，修复短卡模板/创意库无法打开。

范围：原146方法、156模式、36配方逐项核对。建立可追溯同义合并映射，原定义/历史引用不删除；通用结构按不同叙事规模跨层。后台完整分层查阅、搜索、简短介绍和编辑；导航离开编辑有明确反馈。运行供给与后台归层同源，预算限制不得变成静默截取少数方法。保留短卡、作者界面、身份、历史版本及在途任务。不新增时光机故事交互。

验证：原库覆盖与合并去向、宏观结构跨层、运行引用/预算、旧配置冻结、后台栏目切换（含未发布修改）、手机/桌面前后截图、类型与构建、必要真实模型合成样例。按已授权生产流程安全部署，更新功能说明/路线和交接；不为发布取消作者任务。

实现：原338项均有主卡或别名去向，8项同义合并为330项；全书/阶段306、卷313、链325、章322。默认各层方法文本约17179—18467字符，30000字符是发布片段上限，不是总请求。宏观结构可跨层，小说模式与配方按本层规模选择，不能强制写成完整全书。完整目录不受界面24条分页影响；名称与简介支持版本发布，取消勾选。

已验证：32项版本/权限/规划运行回归、14项层级编译与方向依据测试、4项后台组件测试、7项地图校验通过；13个功能执行来源核对通过。后端/API/后台类型与双Web构建通过。390/1440真实组件浏览器：两个栏目、无勾选、搜索六阶段跨四层、分页、真实编辑取消离开保留/确认离开成功、无横向溢出和页面错误。证据artifacts/rebuild/186/browser.json及截图。全书菜单306项17178字符，合成短卡168字符，实际方向业务提示19428字符。

真实生成限制：DeepSeek Pro完整全书方向合成探针分别在60秒、180秒测试时限内未返回完成结果，记为超时，不能记作完整生成通过。K3首个目录探针混用全书工位与链需求，返回其他结构，未通过指定方法检查；后续使用层级一致的读取探针单独验证，不冒充全书生成或长期质量验收。生产模型参数未因测试而调整，无作者资料外传或作者任务启动。

K3层级一致的完整目录读取探针通过：306方法，编译后19613字符，输入9513Token、输出109Token，7320毫秒；准确选择six-act，保留姓名、无灵根、真实材料、核心毁损不可复活/尚完整可修复，并说明六阶段不等于六卷。仅验证读取/应用，不是完整方向生成。证据artifacts/rebuild/186/method-probe-final.log。

首次隔离发布在地图实际解析门禁中失败：表格“待发布”不属于API状态枚举；已改为“未发布”，本地使用实际parseRebuildPlan复验。发生在隔离目录，未切换线上。后续发布必须保留该门禁。

部署：实现8abee67f，发布包ff0a6608，SHA256 a14d3921fa5bca2ce1cadbae7852c92a492d1cf5440211d37bec9c26074293da。隔离源/opt/wenmi-releases/wm-v7-20260909-213500-ff0a6608/source完成API/Worker/双Web构建、地图13来源+82解析、闭包与运行模块检查。连续30秒在途为0并即时复核后逐服务切换；静态ed9f63778829b06a20fe单独原子切换，保留旧哈希，不强制浏览器刷新。线上14资源、作者/后台根页、401、API/Worker健康及只读方法v3/330项、地图1.81/82项通过，无新增迁移，无作者任务取消/数据清理。回滚API与静态均R185，服务切换和验证日志在发布目录。最终开发路线原位同步，不重启服务。
