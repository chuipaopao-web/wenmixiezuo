# R170 设定详情紧凑展示与输出截断核查

范围：设定主题共用一个默认关闭的文字详情入口，移除继承的大面板样式；显式目录引用按实际目录显示中文。必要条件、代价、例外保持可读，存储、上下文、任务与模型额度不修改。

核查：失败组25744692fd816006于2026-09-09 03:51:07收到max_tokens；任务输出额度10000，structured_planning追加4000推理余量。失败路径未保存输出字符和用量，不能报告精确失败字数。前一成功组输出8313字符（包括结构化字段），11748 output tokens。静态R169在03:55:20发布，API/Worker未重启，不能归因于这次网页更新。

验证计划：引用转换单测、设定组件回归、Web类型和构建；390/1440模拟数据浏览器检查单入口、紧凑高度、中文引用、键盘开合、无横向溢出。静态发布保留旧资源并核对API/Worker不重启。无真实模型额外调用、无作品写入。

状态：26项引用/设定组件测试、Web类型检查通过；390/1440浏览器验证主题仅一个入口、折叠高度不超过32px、中文引用、条件可见、键盘开合、无横向溢出/异常/写入通过。等待服务器独立构建与静态发布。
