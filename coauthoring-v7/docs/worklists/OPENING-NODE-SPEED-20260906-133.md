# 第133批：按岗位独立测速与开书前三成员

用户纠正：先测试每个模型的完整开书信息返回速度，在后台查看并让有效速度前三位显示在开书前端；之后独立测试审查速度。GLM 5.2已停用，GLM 5.3、GLM 5.3 Flash和DeepSeek V4 Flash分别存在，不能混淆。上一批虽有独立计时，但名单过期、最后突出串联合计且没有前端前三，需纠正。

本批范围：当前七种文字模型（DeepSeek Pro/Flash、GLM 5.3/5.3 Flash、Kimi 2.7/K3、豆包Turbo）逐一设计，再逐一审查；不调用GLM 5.2。相同合成作者想法、相同岗位输入/参数，完整响应后校验并计时；设计与审查分榜，失败也显示耗时与原因，不拿首字/空结果当完成速度。一轮样本明确标注，不能宣称长期平均；必要的集成/复测单独列证据。每调用180秒、顺序执行、既有订阅通道、禁止现金回退和未知自动重发。GLM Flash先核对通道真实支持，无法调用则明确不可用，不伪造型号或成绩。

实现：保留固定身份/头像、作者页面视觉、旧任务冻结名单和全部作品。当前模型目录退役5.2，新增Flash；历史绑定可解码但不能用于新任务。后台设计/审查分榜；仅可用有效结果参与对应节点排序，开书展示前三且后端校验同一名单，换模型不继承成绩，开书准入不解禁其他节点。必要的候选执行适配仅覆盖开书，停用/改绑可追溯。新结果与旧样本分别说明，不删除历史证据。

验收：模型实际返回及人工内容判断、前三排序/失败排除/改绑隔离/异模型审查与旧任务兼容、开书候选读取确认、后台与作者390/1440、相关类型构建；不跑无关全量。通过后按现有持续授权安全部署、更新路线与交接。

已完成：7设计+7审查顺序首测；3个设计格式失败做一次修复，Pro113.545秒、Kimi2.7 120.560秒、DeepSeek Flash132.261秒（均含首次失败和修复），豆包首轮173.028秒。其余设计180秒未交付。审查首轮旧目录压力样本暴露错误字段/目录修改建议；目录本应由系统先校验，再做语义审查。因此对3个格式合格成员以共同、目录合法且明确违反无系统要求的候选复核：Pro8.267秒、Flash8.800秒、KimiK3 17.939秒，均识别冲突并提供正确字段的修改卡。初测与复核分开保存、后台说明，不能包装为稳定平均或目录纠错能力。

代码接入：前后端同一开书前三名单、审查独立排序且排除设计模型。候选成员仅开放对应开书节点，不改变全局岗位名单；修正旧代码仅识别23成员导致新候选任务被当成历史任务的问题。停用5.2从新模型选项移除，保留历史解码；发布时经配置仓库逐一解绑旧5.2候选并记录审计，不删除历史和作者内容。Flash新品型在现有通道实测无可用返回，继续未准入，不能声称已可用。

证据：artifacts/rebuild/133-design.json、133-review.json、133-repair.json、133-review-recheck.json；相关排序/改绑隔离6例、原治理6例、开书接口/恢复21例、后台3例通过；API/前端类型与核心构建通过。误跑核心含历史测试的类型配置发现两处既有测试类型错误，生产构建配置通过，不扩大范围修复。完整服务预检、真实开书及安全发布正在执行。

最终：2026-09-07 00:31:52上线`wm-v7-20260907-003000-b1bdc357`，静态`aea1d66bee81baffd28e`；主提交b1bdc357，开书新成员头像补丁82278e65。390/1440前端前三顺序、点击选择及各自头像图/裁切通过；后台两榜各7条且无溢出/页面错误。隔离现网副本启动、67份旧提示哈希不变、6项GLM5.2候选解绑预演、合成设计/修订/审查/防重确认通过。真实完整开书140.779秒（Pro78.204秒+格式修复57.457秒、Flash独立审查4.420秒），确认防重/资料读取/扣量一致。运行源码闭包255、108迁移文件与旧版逐一相同，无新迁移。连续30秒在途为零并立即复核后发布，真实配置6项退役留审计，API/Worker新心跳、公网健康/401/双站资源通过。原源码和旧哈希资源保留，作者内容未清理。初次Worker心跳检查未就绪，随后同一循环通过，未触发回滚。隔离副本复制受在线写入影响约数分钟后自然完成；没有停止线上任务，后续可改固定只读快照。
