# 第122批：保留功能接入与分批发布

状态：进行中。用户要求继续安排GPT-5.5复用旧功能接入新架构，测试通过后部署，并同步后台开发路线。

## 目标与范围

- 延续121的真实手动建书/资料闭环，下一条依赖为团队开书的成员、任务、模型与用量。先按代码核对复制边界，复用成熟纯逻辑；权限、持久化和恢复必须适配独立PG，不跨工程运行引用。
- 作者首页、手动表单、信息/设定/取名及创作页视觉保持；120头像选择和进度精简已明确。不得因迁移重新设计保留功能。
- 后台路线使用唯一源docs/REBUILD_EXECUTION_PLAN.md；更新已完成、当前执行、剩余依赖和发布状态，不能以局部通过冒充全单元上线。核查并发布可独立使用的现有功能地图，保持其他管理能力。
- 本批允许必要后端、部署工具、对应管理界面与正式文档修改；只处理本范围及必要依赖，不删除旧代码或作者数据。

## 发布与安全边界

- 用户已授权分批上线，替代121仅本地的后续安排；通过相称测试和生产兼容核查的独立批次自动发布，不另行请求常规上线批准。
- 现网只读核查：R119 API/Worker（wm-v7-20260906-105500-58aa0e75）和R118静态（4f6d09ded6793ff2f998），三服务active，服务器约7.7GiB可用；未发现psql命令。未更改生产配置、作品或任务。
- rebuild当前固定本地PG/Host/Origin，API依赖开发代理移除/api；旧账号、权益和书籍未迁移。不得将全站/API直接指向rebuild；测试通过必须包括该发布范围的真实身份/数据/路由兼容，而非只有独立测试账号。
- 同服务器隔离运行与真实作者切换分别记录。身份/权益单一权威、旧书兼容、任务归属、写入后的回退未验证前，暂停作者入口切换，继续独立接入和可发布功能。不通过双写或伪造会员绕过依赖。
- API/Worker发布按DEPLOY安全窗口与就绪顺序；静态保留旧资源并原子切入口。发布仅所需冻结物料，不夹带其他未验收代码。数据恢复与不可逆迁移仍不在普通上线范围。

## 验收与分工

- GPT-5.5核查/实施明确子范围；主代理定方案、审查差异、独立真实PG/API/浏览器验证并部署。分别记录确定性测试与真实AI验证，模拟模型不冒充可商用效果。
- 后端新增权限/任务/用量核心路径采用rebuild全量加隔离、并发、防重、失败恢复证据；迁移先合成或隔离副本，不使用作者作品做模型测试。
- 后台包含现有管理权限保护、真实源文档解析（81单元/85来源）、正常/错误/恢复与390/1440浏览器；部署核对线上版本、资源、健康、权限和回滚目标。
- 当前只读核查已分配：account_readiness_gpt55查AI开书依赖；foundation_gpt55查生产化与迁移缺口；author_shell_gpt55查地图和最小发布闭包。主代理核对现网与统一发布计划。后续具体编码边界和证据原位追加。

## 已完成实施与验证

- 新后端新增受会话保护的GET /v1/v7/editorial-department，按原前端合同返回7部门23位公开成员，身份/姓名/岗位经主代理与原V7_GLOBAL_MEMBERS逐项比较一致。生成尚未接入，所以ready=0、无当前任务/虚构完成记录，状态明确未接单；不复制模型密钥或私有配置，不建设只能失败的AI任务。该目录不代表团队AI开书已完成。
- 后台当前批次/工作从源文档第3节明确字段读取；缺失显示未登记，拒绝重复字段。保留parseRebuildPlan顶层version兼容；累计聚合改为已开始待完成，入口文案不暗示是真实当前批次。前后端/验收/上线仍分别展示。
- 主代理新工程全量303项通过（81基础/真实PG＋222作者），110源码边界、类型与双Web构建通过；成员源对照证据roster-parity.json。新工程没有迁移或模型/生产数据调用。
- 生产R119归档SHA256校验后独立装配地图闭包，主代理对应API/后台类型与构建通过；25接口/安全测试、49后台测试通过（3项既有条件跳过）。首次新增测试有夹具插入缺多行标记和重复文本选择器问题，修正测试定位后复验；没有削弱业务校验。
- 主代理真实HTTP传输→完整Fastify→隔离SQLite与浏览器390/1440：登录、81项地图、明确当前批次、筛选、详情五列、刷新选中保留、注入503后旧数据保留与恢复均通过，pageerror=0、页面无横向溢出。正常业务未mock，HTTP测试桥仅向真实Fastify inject转送请求，未读取生产账号。证据artifacts/rebuild/122下browser.json及截图。
- 本次拟生产发布仅现有V7只读管理API＋后台地图＋计划源；作者静态、所有生成逻辑和既有SQLite迁移不变。新PG成员目录仅本地接入，不混入该生产包。生产实际结果待后文核实后补记。
