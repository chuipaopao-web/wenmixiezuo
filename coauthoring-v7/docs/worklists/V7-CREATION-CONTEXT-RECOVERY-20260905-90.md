# 第90批：创作资料来源合同与格式恢复

任务编号：V7-CREATION-CONTEXT-RECOVERY-20260905-90
本次唯一清单：coauthoring-v7/docs/worklists/V7-CREATION-CONTEXT-RECOVERY-20260905-90.md
任务类型：已核实线上故障的定向修复
当前问题：2026-09-04 22:13、2026-09-05 07:24（北京时间）卷资料整理失败。压缩目录未提供设定 sourceId，输出却按 sourceId 校验；真实返回使用目录内 sourceKey 被拒绝。末位成员还出现截断 JSON；当前创作资料环节没有同成员格式补交。
复现步骤：以对应已存档候选和模型输出只读重放；另以隔离测试覆盖压缩目录、来源别名、损坏 JSON、未知结果与重复调用。
期望结果：合法来源可准确绑定正式版本；可用历史结果优先复用；格式错误有限补交；越界来源和未知结果仍阻断；作者得到可理解的失败说明。
允许修改：backend/creation-runtime/creation-runtime.ts；apps/api/src/application/creation/v7-creation-context-compiler.ts；相关单元/创作流水线测试；本清单和两级 HANDOFF 的本批状态。老板追加部署授权后，加入 RELEASE_ID 与本批独立发布产物。
明确不改：UI与导航、卷链章产品职责、正文、作者正式资料、历史模型记录、数据库结构、模型名册与计费规则；不重跑生产作者任务或人工改写作者任务数据。
必须保留：按当前候选目录精确校验来源；活动版本与正式内容权威不变；硬预算；成功资料包复用；未知调用不自动重发；现有模型网关的权限、额度、任务血缘与记账。
新功能验收：REQ-01 至 REQ-04。
原功能回归：资料预算、正式来源、全链路创作、任务血缘、未知结果与幂等。
测试范围：backend/API 类型检查及构建；资料编译/创作提示合同/创作流水线相关测试；只读线上样本脱敏重放。无 UI 修改，不做视觉通过声明。
部署要求：老板于 2026-09-05 明确追加“部署上线”，授权本批已提交修复的 API/Worker 发布及必要源码传输、独立构建、备份、健康检查和发布元数据更新；覆盖此前不部署限制。不重跑生产作者任务、不导出作者资料，不修改 UI。冻结产品提交 cd7daedb；发布前完成相称测试、迁移清单一致性、备份和连续30秒零在途检查，保留原版回滚点，逐服务安全切换。

## 复用与职责审计

- 当前实现：目录分四级压缩，最小目录丢失 sourceId；parseContextSelection 只认正式 sourceId；恢复只看最后一份成功调用；JSON.parse 抛错进入下一成员。
- 当前合格实现：第88批规划路线/树已有同成员一次格式补交、repair 血缘、失败保留机制；复用其模式，适配创作资料硬预算和现有网关，不引入新执行器。
- Git：检查 3239108c（资料预算降级）、6237567d（资产菜单）、3882e3c2（规划 JSON 恢复）及现有共享/历史上下文路径。历史产品不恢复为运行依赖。
- 远程：origin HEAD 为本地已有 439a5dd6（数据删除回执文档），当前本地 HEAD f6e308c4；本次不合并、不推送。
- 标准库和现有解析器足够；不引入新依赖或外部源码。
- 系统只处理目录编号、格式、版本绑定与恢复；资料相关性和修正输出仍由 Agent 判断。精确 sourceKey → sourceId 映射仅限本次目录中的唯一设定，不做名称猜测或跨书查询。
- 当前工作区仅有既存未跟踪 .claude/，本批不触碰。

## 风险与验证范围

- 本次不改变 ContextCompiler 权威来源、数据库语义、权限、计费规则或创作阶段结构，不触发全量 verify:full。
- 补交只针对模型已明确返回但合同不合格的结果，每位成员最多一次；使用现有网关独立请求与 basedOnTaskId，未知结果仍停止。
- 保留原始失败证据；不以程序补造缺失 JSON 字段或丢弃非法来源来制造成功。

| 需求 | 实现 | 验收 | 证据 | 状态 |
| --- | --- | --- | --- | --- |
| REQ-01 压缩目录的来源合同一致 | IMP-01 保留编号；唯一精确别名归一 | ACC-01 四级目录与合法/非法/歧义来源 | EVD-01 单元测试；真实存档见 EVD-05 | 通过 |
| REQ-02 损坏格式有限恢复 | IMP-02 同成员一次补交，维持预算与血缘 | ACC-02 补交成功/失败/未知/额度保护 | EVD-02 集成测试及既有会员门禁 | 通过 |
| REQ-03 失败后复用已有可用结果 | IMP-03 按新到旧校验当前包历史返回 | ACC-03 尾部无效时复用较早有效结果，不调用模型 | EVD-03 隔离历史返回顺序回归 | 通过 |
| REQ-04 安全反馈及原流程保留 | IMP-04 本环节失败文案与回归 | ACC-04 无解析错误泄漏、原流程和隔离通过 | EVD-04 原创作流水线、正式化执行器 | 通过 |

## 验证证据

- EVD-01：`tests/unit/v7-creation-context-compiler.test.ts` 新增四层目录编号保留、正式 ID/精确键归一与去重、陌生/旧版/非设定/歧义来源拒绝、含转义字符的补交硬预算。生产代码不补造缺失字段，不静默删除非法引用。
- EVD-02：`tests/integration/domain/v7-creation-pipeline.test.ts` 新增同成员补交成功、每位成员最多两次结果提交、补交未知时停止且再次 compile 不下单；核验 repair 的原任务、用户、书籍和成员血缘。仍走原模型网关做预留与结算；原会员门禁 5 项通过，未修改计费规则。
- EVD-03：构造已完成包、较早有效返回与末尾损坏返回；包重新进入失败后，compile 恢复同一个包，模型调用次数保持 2；原始调用记录保留。生产旧任务是否同时满足后续包预算仍待 EVD-05。
- EVD-04：资料编译、创作流水线、创作提示合同、正式化执行器合计 40 项通过；会员门禁 5 项通过，共 45 项。最初新增测试的仓储方法名/参数错误已修正，最终测试与 tests 类型检查通过。
- EVD-05：**未验证**。自动审批先拒绝将本地修复代码送入生产内存进程重放（理由：未明确授权源码外传），随后拒绝从生产导出两次任务的资料和模型结果到本地（理由：可能含敏感作者资料，未明确授权导出）。两次操作均未执行，没有生产文件/数据变更，也没有取得样本文件。已请求精确范围的本地验证授权；获得前不重试或绕过限制。
- API 类型检查通过；backend 产品构建通过；API 产品构建通过（原 dist 目录写入权限不足，改在 `D:/wenmixiezuo/artifacts/verification/context90/api` 独立输出）；`tsconfig.tests.json` 类型检查通过。
- backend 全包类型检查：**未通过，原版既有错误**。`planning-methods/progressive-planning-briefs.test.ts:60` 的 selectedStrategies 联合类型赋值报 TS2322。本次未修改该文件或对应类型；已用 HEAD f6e308c4 的独立 Git 归档在 `artifacts/verification/context90/baseline` 复现完全相同的错误。不借本批扩项修复。
- 未调用真实模型；未运行全量 verify:full；未进行浏览器验证（无 UI 改动）。

## 差异与作者结果检查

- 产品代码仅修改 2 个合同内文件；测试仅修改 2 个相关文件。无 API 路由、表结构、名册、权限、正文内容、UI 或部署脚本改动。
- 当前目录每一项仍保留选择键；精确设定编号归一只在当前候选集中完成。资料语义选择仍由 Agent 承担。
- 首次资料目录为补交说明预留 1200 字符，四层压缩保留候选；补交仍受原步骤总预算约束，原任务不被补交草稿截断。
- 已完成结果优先复用，不需要新增作者操作；不可恢复时明确保留资料并提供继续/联系管理员的说明。模型原始返回保留供后台核查，作者失败消息不拼接解析器错误。
- 源码检查与自动测试证明机制修复；不能据此声称两次真实任务已恢复、耗时已改善或线上已修复。

## 最终报告

修改文件：2 个产品文件、2 个测试文件、本清单与两级 HANDOFF 本批状态；部署阶段另更新 RELEASE_ID。
明确未修改的模块：UI、产品层级、账号权限、数据库结构、模型名册、计费规则；没有人工改写作者内容或重跑作者任务。老板追加授权后执行本批 API/Worker 部署。
新功能测试：通过，EVD-01/02/03。
原功能回归：通过，EVD-04，合计 45 项。
类型检查/构建：API、tests 类型检查与两产品构建通过；backend 全包类型检查存在已在原版复现的既有错误。
浏览器验证：不适用，无 UI 改动。
未验证项目：两次生产存档的修复后重放、新真实模型生成、真实任务恢复与耗时改善。
提交版本：产品修复 cd7daedb；后续发布元数据提交仅更新 RELEASE_ID、本清单与两级 HANDOFF，不推送。
是否部署：是，老板追加“部署上线”后执行。
部署版本：wm-v7-20260905-090100-cd7daedb，北京时间 2026-09-05 09:09:33 开始切换。
线上验证：通过下方 EVD-06；不能据此认定两次作者任务已经恢复。
回滚点：生产 wm-v7-20260904-200553-648b57f6；新鲜完整备份 20260905T010341Z-508382。

## EVD-06 部署与线上验证（老板追加授权）

- 发布源码为提交 cd7daedb 的 LF Git 归档，SHA-256：`4a6694db36d19b42fd98b1c91198a1e829db5f55840e90bbe0d5205022b90a5c`。服务器只生成本批 RELEASE_ID，不在生产修改产品源码。发布目录 `/opt/wenmi-releases/wm-v7-20260905-090100-cd7daedb/source`。
- 复用上一批已验证的独立发布流程：依赖安装、contracts/backend/API/Worker 构建、5 个相关测试文件共45项、运行源码闭包和实际模块解析全部通过；没有运行全量 verify:full、没有调用真实模型。
- 106 个迁移文件与生产 schema_migrations 的校验和完全一致；生产机私有隔离副本迁移两次均 no-op，隔离 API 健康且模型调用记录增量为0。没有把作者数据库或资料导出到本地，也没有用本地数据替代生产。
- 新鲜备份 `20260905T010341Z-508382` 完成验证。等待既有规划调用结束后，连续30秒在途为0，停止每个服务前立即复核，切换 apps 指针和发布号，逐服务启动。没有取消、暂停或改写作者任务制造窗口。
- 发布脚本返回 `DEPLOYMENT_PASSED`。API/Worker/Caddy 均 active；公网 `/health` 返回本批 releaseId、status=ok、worker=ready、canStartModelTasks=true；数据库 quick_check/外键检查/迁移清单、切换后错误日志检查通过。
- 作者端首页与独立后台 `/v7/` 均 HTTP 200，两端各2个引用的 JS/CSS 资源正常；静态发布清单完整性检查通过，仍为 `c7402f64fd0a9818ef4f`。未登录访问 auth/me 和 admin/overview 均401。本次是 HTTP 与文件验证，不冒充浏览器视觉验收或真实登录后创作验收。
- 发布回执保存在生产发布根目录的 stage-result.txt、backup-result.txt、preflight-result.txt、cutover-result.txt、postdeploy-result.txt；旧版目录和备份保留。两次历史失败任务未重跑，未宣称恢复成功。
