# R172 四项批次与规则辅助字段折叠

老板明确决定每次4项，覆盖R171逐主题方案。不超过4项，按相关主题分组；尾批不足4项按实际数，不将尾项并为5/6项。保留同成员连续接续、完成即保存、上下文来源/版本、独立复核和失败恢复；预算随批次数重新估计，不改模型额度和正式规则。

作者端只直接显示规则正文；条件、适用、涉及、代价、限制与例外全部进入每主题一个默认关闭的“规则详情”。仅有代价或例外时仍能展开，无辅助字段不显示空入口。保留查看详情的整体展开/收起交互。

读取边界：折叠只影响React展示。后端结构化rules和renderSettingRule仍完整保存并渲染所有辅助字段；资料包由后台按来源和任务编译，不抓取作者页面，不受详情开合影响。无需打开详情、无需重新生成即可继续使用原资料。

验证：4+1边界和上下文接续、恢复/隔离集成回归，规则字段投影验证，前端及390/1440浏览器默认隐藏/展开完整/无写入，类型与独立构建。按在途连续30秒零窗口发布；无数据迁移或作者数据清理。

状态：实现完成，待发布。59项后端完整回归（artifacts/r172-backend.log）、26项作者组件、完整类型通过；390/1440浏览器证明辅助字段默认隐藏、展开完整显示，正文仍可读，键盘开合正常、无数据写入/页面异常/横向溢出。5项同类主题实际分为4+1，完整规则投影检查通过。无额外真实模型调用。
