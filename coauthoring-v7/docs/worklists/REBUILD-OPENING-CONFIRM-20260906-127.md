# 第127批：原开书确认接入新后端

授权：用户继续推进；主代理直接开发，先核对现有功能，兼容代码复制复用。保留原页面及业务规则。

范围：复用原开书包严格校验、公开字段一致性比较和蓝图转换；PG确认事务验证最新候选、通过的同源异模型审查、作者原话和会话归属，正式书籍/资料版本/空目录/确认回执/审计一起保存。原opening-books路径支持AI确认，手动入口保持；确认后的AI书能使用原资料读写接口。修订命令、任务作者视图和调度仍继续接入，不能绕过复审而宣布整项迁移完成。

验证：针对性PG与HTTP成功、重复/并发、跨账号、旧候选/改稿/待决定拒绝、保存故障回滚、资料版本与原话保护。复用原校验代码；仅最终按核心确认事务/来源隔离风险做一次集中verify:full，不另派代理或重复验收。

部署：暂不切换生产作者入口，真实身份/权益/旧书兼容和完整开书交互仍未完成；本批不调用真实模型、不操作作者数据。项目路线更新真实阶段，线上未发布状态不得混淆。

状态：本批PG与HTTP路径验收通过，未发布；完整AI开书仍待修订/任务接口/调度与生产兼容。

实现：原v7-opening-package-contract整体复制，严格校验/公开投影比较/蓝图转换运行逻辑不变（比对只差两条导入路径）。新增OpeningConfirmationService和0012不可变AI来源表；鉴权会话锁→任务锁→书籍写入，确认键与任务防重、候选和审查引用/异模型回执复核。复用已有书籍/资料/空目录/审计仓储；空目录表历史名称含manual但没有来源类型字段，AI来源不写manual_book_opening_sources。资料更新复用既有版本服务，只增加读取AI原话来源，保留手动路径。

验证：6项新增测试通过，覆盖实际Fastify HTTP确认、并发只建一本且不追加用量、重复/归档处理、原话和AI来源保护、跨账号/未登录/跨站/伪造字段/旧候选/页面改稿拒绝、审查来源与待决定拒绝、最后审计故障全事务回滚和恢复、原比较器兼容作者可见字段与内部指令。集中verify:full一次通过：154后端/基础+222作者=376项（新增6项，其余复用），173源码边界、类型、双Web构建通过；日志`.local/rebuild/test127.log`、`.local/rebuild/verify127.log`。没有另派代理或附加重复复验脚本。

运行边界：0012仅合成测试库；模型响应使用合成夹具验证原执行器到确认接口，不代表真实模型文学效果。没有更新本地dev服务或部署生产，现网页面和用户数据不变。项目执行路线1.25、详细规格1.10与HANDOFF已同步；线上后台路线仍123，不能标已发布。

下一步：按原revise语义迁移作者改稿/决定及复审，正确衔接每轮预算；接作者任务创建/读取/恢复与Worker调度，复用原页面后再完成真实身份/权益/旧书兼容。不能为快速上线将待决定或改稿直接标审查通过。
