# 第138批：资料与设定复用接入

目标：按老板确认顺序先RB-20资料、再RB-21设定；复用已运行页面与业务能力，核验资料保存、当前设定与历史任务输入。保留全部现有UI及作者确认步骤。

代码核查：开书资料已经有不可变版本、预期版本并发保护和书架标题更新；设定已具备目录推荐、分组设计、复审、重做/融合、确认、租约和未知调用恢复，不能因未接独立核心就重写。当前设定纯合同/目录/编译/解析仍来自旧核心，资料读取与设定事务是生产SQLite适配器。采用第128批已确认方案：把可复用设定纯核心接入rebuild独立运行包，保留唯一生产身份、事务、任务执行器与账本；PG整站切换不在本批。

实施：原样迁入设定目录、提示编译/结果解析、岗位名册及确认设定投影至独立运行核心，旧模块只作兼容导出，避免双份实现。核验资料v1→v2后新任务读取v2、旧任务快照不变；设定修改/确认后下游读当前版本，未确认候选不得冒充正式源。发现实际缺陷再修，不以猜测改变已运行语义。

保留：现有模型/准入、提示文本与预算、全部历史资料与正文、计费和失败恢复；不新建第二执行器，不迁移数据，不改UI，不启动全模型测速。独立核心不得反向导入旧工作区或数据库。源码复用需与原件核对一致。

验证：运行设定合同、资料版本与设定工作流相关测试，补资料修改→新设定调用与历史冻结的定向用例；核心/API构建、运行边界、服务器副本启动、必要真实HTTP链路和页面读取检查。不以模拟模型通过宣称模型文学质量；原样迁移提示不重复真实模型测速。上线需连续30秒零任务并复核，保留R136API/R137静态回滚，发布后同步路线。

状态：2026-09-07 03:49:51北京时间已发布`wm-v7-20260907-035500-d91d470b`，静态`dc1b90160cab1aa117c6`。4个迁入模块与旧实现逐一比对一致，旧路径改为兼容导出；独立核心与兼容核心构建通过。43项相关用例通过（42项首轮通过；新增用例的测试SQL表名笔误纠正后单独复验通过），覆盖资料并发版本、设定工作流与上下文合同。新用例通过真实HTTP处理器验证资料v1→v2、新任务读v2、旧任务冻结v1与提示哈希、已确认设定来源记录。模型为测试替身，不宣称文学质量或真实模型测速完成。

发布证据：服务器核心/API/Worker/静态构建、源码闭包和模块解析通过；隔离数据库再次执行版本链路用例、现网副本完整服务启动通过，74份历史提示哈希保持。R137作者样式包含在发布包，保留旧哈希资源；无新增模型调用、数据迁移或作者数据修改。连续30秒零任务并立即复核，切换后API/Worker新PID578516/578535与心跳正确、公网健康/受保护401/双站入口与资源通过，错误日志无条目。Worker首次新心跳未到时检查重试后通过，未回滚。保留R136后端、R137静态和已复核完整备份。证据位于`artifacts/rebuild/138`及本次服务器release根目录。

范围结论：本批资料/设定复用接入交付完成；RB-20/21整体继续标记验收中，精确依赖失效、全节点真实模型质量/速度及PG整站切换不在本次完成范围。下一步核查时光机/库与全书→卷输入边界，待讨论部分不自动开发。
