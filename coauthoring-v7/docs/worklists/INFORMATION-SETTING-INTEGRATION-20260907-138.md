# 第138批：资料与设定复用接入

目标：按老板确认顺序先RB-20资料、再RB-21设定；复用已运行页面与业务能力，核验资料保存、当前设定与历史任务输入。保留全部现有UI及作者确认步骤。

代码核查：开书资料已经有不可变版本、预期版本并发保护和书架标题更新；设定已具备目录推荐、分组设计、复审、重做/融合、确认、租约和未知调用恢复，不能因未接独立核心就重写。当前设定纯合同/目录/编译/解析仍来自旧核心，资料读取与设定事务是生产SQLite适配器。采用第128批已确认方案：把可复用设定纯核心接入rebuild独立运行包，保留唯一生产身份、事务、任务执行器与账本；PG整站切换不在本批。

实施：原样迁入设定目录、提示编译/结果解析、岗位名册及确认设定投影至独立运行核心，旧模块只作兼容导出，避免双份实现。核验资料v1→v2后新任务读取v2、旧任务快照不变；设定修改/确认后下游读当前版本，未确认候选不得冒充正式源。发现实际缺陷再修，不以猜测改变已运行语义。

保留：现有模型/准入、提示文本与预算、全部历史资料与正文、计费和失败恢复；不新建第二执行器，不迁移数据，不改UI，不启动全模型测速。独立核心不得反向导入旧工作区或数据库。源码复用需与原件核对一致。

验证：运行设定合同、资料版本与设定工作流相关测试，补资料修改→新设定调用与历史冻结的定向用例；核心/API构建、运行边界、服务器副本启动、必要真实HTTP链路和页面读取检查。不以模拟模型通过宣称模型文学质量；原样迁移提示不重复真实模型测速。上线需连续30秒零任务并复核，保留R136API/R137静态回滚，发布后同步路线。

状态：本地核心接入与验证通过，准备服务器预检。4个迁入模块与旧实现逐一比对一致，旧路径改为兼容导出；独立核心与兼容核心构建通过。43项相关用例通过（42项首轮通过；新增用例的测试SQL表名笔误纠正后单独复验通过），覆盖资料并发版本、设定工作流与上下文合同。新用例通过真实HTTP处理器验证资料v1→v2、新任务读v2、旧任务冻结v1与提示哈希、已确认设定来源记录。模型为测试替身，不宣称文学质量或真实模型测速完成。
