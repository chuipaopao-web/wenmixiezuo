# V7 本地恢复、平台底座承接与生产替换工单

## 变更合同

- 任务编号：`V7-CUTOVER-20260830-66`
- 本次唯一清单：`coauthoring-v7/docs/worklists/V7-CUTOVER-20260830-66.md`
- 任务类型：本地运行修复、复用审计、兼容补齐、生产替换、旧代码清理
- 当前问题：本地 V7 作者端与后台均无法打开；生产仍由 V6 静态入口和旧运行目录提供服务；V6 商业必需能力是否全部由 V7 承接尚缺一份基于真实路由、代码和浏览器的零遗漏证据。
- 复现步骤：访问本地作者端 `http://127.0.0.1:43180/`、本地后台 `http://127.0.0.1:43181/` 或本地 API；当前服务端口未监听。
- 期望结果：本地 V7 可稳定访问；只由 V7 承接账号、密码登录、会员、安全、后台运营与必要计量底座；V6 书籍、资料库、任务、正文和创作工作流不迁移；V7 可回滚替换生产；确认零有效运行引用后删除 V6 产品代码。
- 允许修改：V7 作者端、V7 独立后台、V7 后端适配、共享账号/会员/用量/API/部署边界、启动与部署脚本、相关测试和本工单；经零引用证明后删除被 V7 明确替代的 V6 产品代码。
- 明确不改：生产 API Key、密码哈希、账号身份、会员状态和会话秘密；未经永久数据删除门禁不清除生产书籍或历史用量。V6 作者内容只用于清理影响预览和可恢复备份，不进入 V7 产品资料。
- 必须保留：账号密码登录、会话安全、owner/book 隔离、会员等级/有效期/额度规则、独立后台、备份恢复、失败可追溯、生产回滚点。历史算力消费不带入 V7 作者余额；V7 新消费从零开始记录。
- 新功能验收：本地 V7 作者端/后台/API 真机可访问；V6→V7 能力矩阵无未归属商业必需项；V7 线上入口、登录、会员、空书架/书籍隔离和管理后台通过；V6 产品入口不可再达；旧代码删除前零有效引用。
- 原功能回归：账号与会员数据不变；既有 V7 开书、设定、时光机、卷、链、章、任务、团队页面可访问；任务和成员状态来自后端事实；失败不伪装成功。
- 测试范围：批次 A 运行/浏览器；批次 B 受影响测试、类型检查和构建；批次 C 因账号兼容、迁移、核心工作流和生产切换触发完整 `npm run verify:full`、迁移副本预检与线上浏览器；批次 D 引用扫描、构建、启动、路由和回滚检查。
- 部署要求：先恢复本地并补齐缺口；生产只在私有版本目录构建和预检，连续 30 秒无在途任务、备份验证后切换；清理作者数据需另行生产预览、`YES`、`CONFIRM:<previewId>`；旧 V6 代码只在 V7 线上验收且零引用后删除。

## 信息职责审计

| 节点 | 负责者 | 结论 | 状态 |
| --- | --- | --- | --- |
| 本地端口、进程、健康、构建、路由 | 系统 | 确定性诊断和恢复，不调用模型 | 已完成 |
| V6/V7 功能归属、引用和兼容 | 系统审计＋人工工程判断 | 以真实代码、路由、数据模型和浏览器证据判断，不用关键词冒充产品能力 | 已完成 |
| 创作语义节点 | 既有 V7 Agent | 本批不改变已跑通的 Agent 工作合同，只验证入口和底座没有退化 | 已完成 |
| 账号、会员、用量 | 系统 | 共享平台确定性能力，不交给模型 | 已完成 |
| 作者数据永久清理 | 受保护执行器 | 强预览、备份、双确认和写锁复核；本工单不能绕过 | 待处理 |

## 复用审计

| 检查对象 | 预期复用 | 决定 | 状态 |
| --- | --- | --- | --- |
| `scripts/start.mjs` 与 V7 本地入口 | 复用现有启动编排 | 先运行诊断，只有真实缺陷才最小修改 | 已完成 |
| V6 账号/会话/会员/用量实现 | 通过共享 API/数据库平台边界复用 | 不复制第二套账号体系 | 已完成 |
| V7 作者端、后台、后端现有闭环 | 直接保留并补齐适配缺口 | 不重写已验收页面和 Agent 流程 | 已完成 |
| 现有生产发布与备份脚本 | 复用安全步骤，修正与真实服务器目录不一致部分 | 新版本目录构建、备份、迁移预检、原子切换、可回滚 | 已完成 |

## 分批清单

### 批次 A：恢复本地 V7

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| REQ-A1 | 作者能打开本地 V7 作者端、后台并连通 API | 已完成 |
| IMP-A1 | 查明端口未监听原因，复用现有启动器恢复服务；必要时最小修复 | 已完成 |
| ACC-A1 | 作者端、后台、`/health` 返回成功，页面控制台无启动错误 | 已完成 |
| ACC-A2 | 登录、书籍列表、时光机、卷、链、章、任务、团队入口可达 | 已完成 |
| EVD-A1 | 端口、健康响应、进程、浏览器与日志证据 | 已完成 |

### 批次 B：共享商业底座由 V7 承接

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| REQ-B1 | 查清 V6 仍有哪些共享平台能力必须由 V7 承接，明确旧创作数据与流程不迁移 | 已完成 |
| IMP-B1 | 建立真实入口/路由/API/数据/后台/运维能力矩阵并标记保留、已承接、待补、淘汰 | 已完成 |
| IMP-B2 | 对待补项做最小纵向闭环，不改已跑通 Agent 创作规则 | 已完成 |
| ACC-B1 | 账号、登录、会员、用量、后台、安全、备份和用户隔离均有 V7 运行证据 | 已完成 |
| ACC-B2 | V7 不读取、展示或复用 V6 书籍、资料库、任务、正文和创作工作流 | 已完成 |
| EVD-B1 | 能力矩阵、定向测试、类型检查、构建与浏览器证据 | 已完成 |

### 批次 C：生产替换

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| REQ-C1 | V7 在生产替换 V6，保留账号会员与回滚能力 | 已完成 |
| IMP-C1 | 冻结同源发布、生产副本预检、备份、迁移、API/Worker/静态切换 | 已完成 |
| ACC-C1 | 主站只进入 V7；独立后台进入 V7；登录、会员、API、核心页面和手机端通过 | 已完成 |
| ACC-C2 | 在途任务安全、旧版回滚包和数据库备份验证通过 | 已完成 |
| EVD-C1 | 发布版本、健康、日志、浏览器、备份与回滚证据 | 已完成 |

### 批次 D：删除 V6 代码

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| REQ-D1 | 本地与生产删除已被替代的 V6 产品代码 | 进行中 |
| IMP-D1 | 扫描启动入口、路由、动态注册、服务、任务、测试、文档与恢复逻辑，生成零引用证据 | 进行中 |
| IMP-D2 | 删除 V6 产品页面、工作流和仅验证旧行为的测试；保留共享平台底座、历史迁移与回滚归档 | 进行中 |
| ACC-D1 | V7 构建、启动、登录、核心路由和生产运行不依赖已删 V6 代码 | 已通过（已删批次） |
| ACC-D2 | 本地工作区与生产在线目录不存在可执行 V6 产品入口 | 已通过；共享账号、会员、反馈与后台所在的 API 单体继续按能力边界保留，不能冒充 V6 产品入口删除 |
| EVD-D1 | 删除清单、引用扫描、差异审计、构建/运行和线上验证 | 进行中 |

### 批次 E：V7 独立后台能力台账补齐

变更合同补充：本批只把已经保留在共享后台 API 中的功能能力台账接入 V7 独立后台，不复制旧后台外壳，不迁移 V6 书籍、资料库、任务或工作流，不改变 Agent 岗位、模型、提示词、ContextPack 和创作节点。当前用户要完成的是管理员从独立后台直接确认“现有哪些功能、旧能力去了哪里、是否疑似遗漏”；当前阻碍是 `/api/v1/admin/feature-capabilities` 已存在但 V7 后台无入口；现在只显示台账概览、筛选和模块详情，技术证据按需折叠。

信息职责审计：功能 ID、版本状态、入口、证据路径、数量与筛选都是确定性数据，由系统读取；本批不包含创作语义，不调用 Agent。复用审计：接口、类型和旧版台账展示语义直接复用；V7 使用现有 `AssetAdminApp` 导航、`platform-api` 请求封装、后台卡片/表格样式与原位详情，不导入已删除的 V6 React 组件，不新增依赖或弹窗。

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| REQ-E1 | 独立后台完整承接功能台账与版本能力对照入口 | 已完成 |
| IMP-E1 | 复用现有管理员接口，在 V7 后台增加功能台账导航、概览、筛选和模块详情 | 已完成 |
| ACC-E1 | 管理员可查看当前功能、已迁移/替代/退役能力和疑似遗漏，不展示空白成功态 | 已通过 |
| ACC-E2 | 现有 13 个后台栏目、登录和写操作不退化；390px 页面无横向溢出 | 已通过（现为 14 栏，原 13 栏全部保留） |
| EVD-E1 | 定向测试、后台类型检查/构建、真实本地与线上页面证据、最终差异审计 | 已完成 |

## 最小交互合同

- 当前页面唯一任务：先让作者稳定进入 V7；不借恢复入口改动已验收页面结构。
- 唯一主操作：现有各页主操作保持；本批不新增多按钮或弹窗。
- 必须保留：成员头像、真实任务状态、折叠内容、移动端无横向溢出、失败时道歉和恢复入口。
- 技术词投影：启动、部署和迁移错误只留在后台/工单，作者端不显示堆栈、迁移名、模型协议或哈希。
- 手机端首要视口：390×844；作者端主要内容和导航可达，管理后台操作不溢出。

## 阶段证据

- 2026-08-30：生产发布在切流前暂停；生产仍运行 V6，数据库未清理。服务器私有暂存目录不属于在线入口。
- 2026-08-30：本地 `43110`、`43111` 均未监听，初步表明是服务被主动停止或启动器未运行，而不是已证明的页面代码崩溃。
- 2026-08-30：沙箱内启动探针因本地 SQLite 写保护边界失败；按已授权本地研发范围在沙箱外运行原有启动器后，迁移版本 102、API/Worker/Web 构建及启动全部通过。`/health` 返回 `status=ok`、`worker=ready`，作者端 `43110` 与独立后台 `43110/v7/` 均经真实浏览器打开，未登录门禁返回 401。
- 2026-08-30：真实登录态逐页验证信息、时光机、卷、链、章、V7资料库、任务、团队和独立后台；各层页面职责独立，个人中心会员与反馈入口可用，未发现启动告警或把 V6 书籍投影到 V7 书架。
- 2026-08-30：生产切换前完整门禁通过：功能资产检查通过；类型检查通过；根测试 `217` 个文件、`998` 项通过；V7 后端独立测试 `12` 项通过；V7 作者端 `125` 项通过；V7 后台 `36` 项通过、`3` 项环境条件跳过；API、Worker、V7 后端与双前端构建完成。静态发布号 `d210e741dd1144ab314d`，清单摘要 `560e861db3d7e97b795c50e7a36980a72b3712ee779a77be8cf2d1c38418bf8c`。
- 2026-08-30：依赖安全门禁复核通过：生产依赖和全部依赖 `npm audit` 均为 `0`；封面/PDF/Ark 适配器 `32` 项通过；本地向量模型与 LanceDB 混合检索真实探针通过。`sharp@0.35.4` 通过根覆盖修补传递依赖声明滞后，实际原生模块导入、构建和运行探针均通过。
- 2026-08-30：生产发布包 `wm-v7-20260830-144927-d210e741` 在私有目录完成文件摘要、迁移副本、二次迁移幂等、原生依赖和隔离 API 健康预检；连续 `30` 秒确认无在途任务后切换。回滚目录包含完整数据库备份、旧静态文件、环境、Caddy 与 systemd 配置，数据库完整性为 `ok`。
- 2026-08-30：生产切换完成。`wenmi-api`、`wenmi-worker`、`caddy` 均为 `active`，内外网 `/health` 返回 `status=ok`、`worker=ready`、`canStartModelTasks=true` 和正确发布号；服务稳定启动后的日志错误扫描为空。主站真实浏览器标题为“文秘写作 V7”，既有登录会话有效，书架为 `0` 本；个人中心可见会员开通方式和内联“意见与问题反馈”；独立后台登录页真实可达。账号 `68`、会员 `67`、会话 `211` 与切换前一致，V6 书籍仍只保留在待确认清理区，未投影到 V7。
- 2026-08-30：生产数据永久清理只完成只读影响预览，`previewId=7a81b329fa0b6f06dd3c6a133e8603302de11ddedd2daa60516aad2802e6836b`；预览包含旧书 `20` 本、历史模型调用 `485` 次和历史消耗 `4,777,002` Token。尚未收到双确认，未执行任何永久数据删除或用量清零。
- 2026-08-30：生产旧执行目录 `/opt/wenmi/apps.v6-pre-wm-v7-20260830-144927` 经绝对路径、在线符号链接和回滚归档三重检查后删除；可从版本库和 `rollback-backup/v6-author-files-before-v7.tar.gz` 恢复。删除后 API、Worker、Caddy 均为 `active`，公开健康检查继续通过。当前在线 `/opt/wenmi/apps` 只指向 V7 发布包。
- 2026-08-30：本地零引用审计尚未通过：`apps/web` 已退出根构建、启动和生产发布，但仍被根 workspace、锁文件、功能台账及一组仅验证 V6 页面行为的旧测试引用；共享 API 中也仍有 V7 复用的账号、会员、反馈、归档和资料接口，不能把整个 `apps/api` 当旧版粗暴删除。后续只删除 V6 产品页面与旧工作流入口，并把共享底座证据改指向 V7 实现。
- 2026-08-30：上述本地前端引用缺口已处理：根 workspace 与锁文件移除 `@wenmi/web`，`apps/web` 源码和 33 个只验证 V6 页面行为的测试已删除；删除前归档为 `artifacts/retired-v6-source/v6-web-and-ui-tests-before-removal-20260830.zip`，SHA-256 为 `c34c28b8f66827a017d7aa6c6e823674815d0264e272832d768bf7a4c8429e78`。当前 `apps/web` 只存在于历史文档或“不得重新引用”的发布门禁中，不再是 workspace、构建、启动或在线入口。
- 2026-08-30：V6 专用 API 入口 `core-workflow-v6-routes.ts`、`ai-node-v6-routes.ts` 和旧书升级服务/仓储/测试已删除；删除前归档为 `artifacts/retired-v6-source/v6-api-entrypoints-before-removal-20260830.zip`，SHA-256 为 `69a4533342b6dd5726acf14352e1bc1ff26cb17c7384863ff790f82558cb9a6e`。V7 书籍资料修改、归档、恢复和开书标签目录均迁到独立 `/api/v1/v7/*` 路径，集成测试覆盖修改、归档和恢复。
- 2026-08-30：共享 `domain-routes.ts` 仍同时承载后台和历史业务接口，不能以删除一个注册调用的方式粗暴下线，否则会同时中断账号外的管理、反馈、备份和恢复接口。当前安全边界是：V7 作者端除账号、会员、反馈外全部调用 `/api/v1/v7/*`；旧总路由继续留在服务器内部，等待按路由清单拆出共享能力后再做独立删除批次。该保留不恢复 V6 页面、V6 书架或 V6 工作流入口。
- 2026-08-30：删除后完整门禁通过：功能台账 `33` 个模块、`130` 项能力；根测试 `183` 个文件、`835` 项通过；V7 后端 `12` 项通过；V7 作者端 `125` 项通过；V7 后台 `36` 项通过、`3` 项环境条件跳过；全部类型检查和 API、Worker、V7 双前端构建通过。真实浏览器重启后作者端与后台标题正确、账号书架与“提示词与上下文”入口可见，控制台错误均为空。
- 2026-08-30：生产静态资源已从借用的 `/opt/wenmi/apps/web/dist` 无停机迁移到独立 `/opt/wenmi/releases/current`。候选版本 `50f5ce6ab47534104a9f` 的静态清单 SHA-256 为 `d09c87a995bfaf7b69822c247c8de7e14c5171b2d7126d8ce3b0e7886c5f124e`；上线复核发现该构建已调用本批新增的 V7 书籍接口，而线上 API 尚未同步，因此没有把不匹配版本留给用户。`current` 已原子回切到从生产归档恢复的 V7 兼容版本 `d210e741dd1144ab314d-compat`，主站、独立后台、作者资源、健康检查和三项服务全部通过。待新 API/Worker 安全发布后再激活候选静态版本。
- 2026-08-30：生产运行包内最后一个旧 `apps/web` 目录在 Caddy/systemd 零引用确认后删除。删除前可恢复归档为 `/opt/wenmi-releases/wm-v7-20260830-144927-d210e741/rollback-backup/retired-apps-web-before-removal-20260830.tar.gz`，SHA-256 为 `eb475cb640bc50f445b7a66f66acd124caceba930efa5fcce67ea46ce1643560`；该归档同时提供了本次 V7 静态兼容回切来源。删除后主站、后台、API、Worker、Caddy 均正常。
- 2026-08-30：新运行版本 `wm-v7-20260830-164603-50f5ce6a` 的隔离预检先发现本地 `0064` 迁移字节与生产登记版本不同并中止；只在新发布暂存副本恢复登记 SHA-256 `6ef44e72f99ce1f4cd426e92cd29e01c0c51057016276ef30516095c4d015e6e` 后，迁移副本连续两次幂等、完整性和独立端口 API 均通过。生产数据库未因第一次预检发生任何写入。
- 2026-08-30：新 API/Worker/静态版本完成同源切换。在途任务连续 `30` 秒为 `0`，停服务复核仍为 `0`；数据库备份完整性通过，迁移仍为 `102` 且无新增应用。主站静态已正式激活 `50f5ce6ab47534104a9f`，新 `/api/v1/v7/opening-taxonomy` 路由存在并按预期对未登录请求返回 `401`；API、Worker、Caddy 均为 `active`，错误日志扫描为空。生产保留账号 `68`、会员记录 `67`，旧书 `20` 本仍只在待双确认清理区，未被本次发布删除或迁入 V7。
- 2026-08-30：V7 独立后台补齐“功能台账”第 14 个栏目，直接复用 `/api/v1/admin/feature-capabilities`。管理员可按基线、状态、模块和关键词核对 `33` 个模块、`130` 项登记能力、`117` 项当前可用能力及 `2` 项疑似遗漏；模块、判定与代码证据默认折叠，不新增弹窗。定向测试 `13` 项通过，后台完整回归 `39` 项通过、`3` 项环境条件跳过，类型检查和独立暂存构建通过。真实本地登录态桌面页无控制台错误；`390px` 视口 `scrollWidth=clientWidth=375`，当前“功能台账”底部导航自动进入可见区。生产静态版本原子切换为 `da36f2ab404b080fc9f7`，线上 `/v7/?section=features` 返回 `200` 并命中新资源 `assets/index-BzN_m2hL.js`；API、Worker、Caddy 未重启且保持 `active`。线上登录态浏览器目测因跨站连接超时未形成证据，不据此声称已通过；公开入口、资源命中和服务健康已有只读证据。

## V6 → V7 商业底座能力矩阵（代码与真实页面审计）

| 能力 | V7 当前结论 | 本批决定 |
| --- | --- | --- |
| 账号注册、密码登录、会话、退出 | 已承接 | 保留共享 API 与账号数据，不复制第二套账号体系 |
| 会员等级、有效期、算力余额与服务端门禁 | 已承接 | 保留并回归验证 |
| 独立后台：运营、用户与书籍、算力成本、问题、会员收入 | 已承接 | V7 后台入口已真实打开，继续做登录态回归 |
| Agent、模型、温度、提示词、Skill、ContextPack 与运行追溯 | 已承接 | 保留 V7 后台治理中心 |
| 书架切换 | 已承接 | 保留 |
| 书籍归档与恢复 | 后端已有，V7 作者端缺入口 | 本批补齐；归档书与活跃书分区展示 |
| 永久删除单书 | 旧接口只有一次确认，不满足当前双确认硬门禁 | 不把不安全旧入口迁入；后续先补影响预览与二次确认再开放 |
| 作者反馈与后台追踪 | 后端和后台已有，V7 作者端缺入口 | 本批在个人中心内联补齐，不新增弹窗 |
| 会员开通联系方式 | V6 有、V7 缺 | 本批在个人中心补齐简洁开通方式 |
| V7 正式资料库与正文查看 | V7 API 已有，作者端仍是占位页 | 接入 V7 自身形成的正式卷/链/章/正文；首次上线为空，不读取或迁移 V6 资料库 |
| 书籍导出/导入 | 旧版只读写服务器本地目录，不是生产浏览器可用闭环 | 不伪装迁移完成；保留后端备份能力，浏览器安全下载/上传另列后续闭环 |
| 取名 | 已迁入信息页 | 保留 |
| 书名与封面设计 | V7 已有新工作台 | 保留，不回迁 V6 旧实现 |
| 灵感便签、旧设置偏好 | 不属于账号/会员或 V7 核心创作闭环 | 明确不阻塞切换，不回迁旧页面 |
| V6 已有正文与书籍接续 | V6/V7 创作结构不同 | 不迁移、不接续、不展示；只在执行永久清理前保留受控备份与影响预览 |
| V6 旧设定、故事线、分卷、事件、章节工作流 | 已由 V7 信息、时光机、卷、链、章替代 | 删除旧产品入口前做零运行引用证明，历史迁移保留 |
