# 第140批：节奏资产与分层短卡

- 状态：已上线。2026-09-07 06:54:59北京时间，API/Worker `wm-v7-20260907-070000-d0b778a4`，静态 `8cd783a1d32ddba8afa5`。
- 目标：后台能管理四类短资产及各层候选，预览运行时实际供给的资产文本、字符数和版本；全书/分卷/卷/链/章按层少量供给，不再全量菜单或强制章层宏观框架。
- 复用：既有资产标识、运行时菜单合同、管理员认证、任务存档；详细方法库保留为查阅。新增版本化共用节奏配置，历史任务继续原菜单，新任务冻结配置，不改作者作品或旧提示版本。
- 范围：后台叙事方法页增加节奏管理入口；短卡、分层候选、同编译器预览、配置版本、运行绑定；路线新增RB-51.1。全书故事线编辑与时光机流程重构列为下一批，不混入本批。
- 保留：绿色后台外壳、作者页面、已确认资料/正文/规划、身份隔离、模型计费与失败恢复；不新增模型或改准入。
- 验证：短卡和预算、配置非法值/并发/权限/版本冻结、调用集成及旧快照兼容；受影响包构建；后台390/1440浏览器；一份合成故事样例评估，工程与文学质量分开报告。
- 发布：加法配置表，不回填作者数据；现网副本启动、备份/回滚准备，连续30秒零在途任务并即刻复核后API/Worker切换，静态保留旧哈希，线上健康与后台入口验证。
- 风险：运行提示改变；仅新任务使用新版供给，历史任务/已有菜单继续原语义。预算是资产片段预算而非整份上下文；必要正式资料优先。候选不是必用模板，不以关键词裁定文学质量。

## 证据

本地：新增5项核心配置/冻结测试、1项真实HTTP权限发布测试、2项页面交互测试通过；菜单兼容、框架数据、创作上下文受影响用例通过。路线新增单元后旧81项断言更新为82项并核对RB-51.1，相关11项测试通过。组件所在API/核心/后台类型检查通过；后台构建和390/1440浏览器通过，截图与交互证据`artifacts/rebuild/140`。最后补齐规划路线按已冻结候选校验引用的遗漏，重建核心并复验通过；最终服务器核心/API/Worker/静态构建、6项配置与HTTP测试、源码闭包、模块解析、现网副本完整启动通过，74份历史提示哈希不变。

默认全书资产片段584字符，其他层同编译器输出并受1400字符上限约束。完整旧目录保留只读；新配置启用时不再注入整库名册。发布开关WENMI_V7_ASSET_MENU保留，后台显示真实开关状态。冻结发生在首次资产编译，不能声称任务创建瞬间绑定。新表仅存配置和任务版本对应关系。

真实模型只调用一次DeepSeek V4 Flash，使用合成架空三国故事，无作者资料。21.5秒，输入444/输出1539 token，资产片段502字符；6卷合计60万字结构检查通过，人工阅读见成长、治理和关系的因果承接。但仍全部均分10万字，末卷新危机强于终局收束，部分制度变化过于概括；这不是完整时光机流程或长篇文学质量通过，留待下一批阶段/分卷方案优化。最终输出保存在服务器release的`source/.local/r140-real-result.json`，未保存模型思维链。

生产先因磁盘余量被备份预检阻止；无损压缩3份旧隔离预检副本并逐个验证原始SHA256后，完整备份`20260906T225245Z-586002`成功，未删除任何正式备份或作者作品。切换前备份时间算术语法修复并通过bash检查；连续30秒零在途任务并立即复核后切换。Worker首个心跳检查尚未到位，随后有界轮询通过，未触发回滚；最终API/Worker健康、双站HTML/后台脚本/头像哈希、保护接口401全部通过，无新增服务错误日志。保留R139代码与旧哈希资源用于回滚。
