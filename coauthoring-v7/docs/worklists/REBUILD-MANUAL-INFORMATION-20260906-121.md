# 第121批：手动开书与资料接入独立后端

状态：本批手动建书与资料编辑闭环已实现，主代理验收通过，本地预览已更新；未发布生产。用户明确要求先接新后端；由GPT-5.5实现、主代理审查和真实数据库/浏览器验收。

## 范围

- 本批完成保留的自己设计表单→建书→资料读取→编辑保存→刷新重新打开。使用rebuild独立账号、PostgreSQL、API和现有复制页面。复用兼容的字段映射与分类数据，禁止运行依赖旧工程或连接生产数据。
- 保留所有手动输入和当前UI；不新增产品必填字段，不截断作者原文。创建来源不可变；当前资料以完整版本保存，原始来源不随编辑改变。
- 核实保留页面的接口兼容时发现保存资料后书架未刷新，纳入必要适配：InformationPage成功保存通知外壳重读书架，失败不通知；不修改布局、表单或样式。界面验收按UI/UX Skill与当前390/1440视口执行。
- 提供现有前端兼容的opening-taxonomy、opening-books手动分支与book-profile GET/PUT。AI任务/candidate参数不得冒充手动来源，团队真实任务、设定AI和取名AI后续接入，不能宣称本批完成。
- 本人身份由真实会话验证。新增版本读取/保存限定owner/book，expectedVersion防并发覆盖；更新、书架标题、审计同事务。重试创建不复制书籍，失效会话、跨用户、归档、审计失败不能绕过限制。正文、旧迁移和原始资料不得覆盖。
- HTTP容量与现有合法字段容量相容，仅相关路由放宽；Host/Origin/JSON检查保持。新增PUT支持仅允许原本受信本地来源。
- 后台状态是整个重构单元的验收进度，不等于正在执行的开发任务。总计划按实际完成范围更新，不把局部后端验收改为整个功能已发布。

## 验证与发布

涉及新建书核心链路、权限与资料版本，执行rebuild全量验证及fresh PostgreSQL迁移、隔离/并发/失败回滚检查；主代理使用真实本地API和合成账号，手机390与桌面1440浏览器检查创建、保存、刷新、失败输入保留。浏览器不得用模拟业务接口冒充接入。

所有修改在D:/wenmixiezuo/rebuild及对应文档/证据；本批仅独立本地接入，不切换生产、不迁移真实作者、不调用真实模型/邮件、不新增费用。已运行的生产服务和用户任务保持独立。后续正式切换需对应兼容与分批发布验收。

## 结果

- 新增0007完整资料版本表，应用角色仅能SELECT/INSERT。初始资料、原始输入、书籍、空目录与审计同事务；资料版本独立于归档/恢复版本。没有资料投影的旧0006手动书GET只读，首次真实修改保存原始v1和新v2；无变化保存不追加版本。
- 公开手动建书和原页面资料GET/PUT、分类目录已接通；公开必填规则与当前表单一致，内部原始输入容量保留。AI任务参数拒绝冒充手动来源，已归档请求重试提示恢复，不伪装active。局部写入路由2MiB，原始来源256000 Unicode字符总限额保持；作者原话由来源锁定，更新不能改写。错误兼容旧页面，格式错误/请求超限安全返回400/413。
- 分类与标签源文件从当前旧工程完整复制至rebuild，两文件SHA256与来源逐字节一致；运行时不跨旧工程引用。旧映射会按频道推断主角类型，主代理改为优先尊重五种明确角色选择，非枚举的历史身份仍采用原有回退。没有截断作者长字段。
- 主代理最终运行rebuild `verify:full`：107源码边界、严格类型、79基础/真实PG测试、222作者测试和双Web构建通过，共301项。首次新增公开接口测试用了内部宽松输入而失败，已将测试样本改为真实表单合法输入，未放宽公开校验。最终日志：artifacts/rebuild/121/verify-full-final.log。
- 主代理另10组编译后Fastify/真实PG探针通过：5次并发创建防重、资料并发409、Unicode原样、来源不可变、跨owner/伪造AI/Origin拒绝、资料历史应用角色不能改删、归档后拒绝修改且恢复不改资料版本、真实资料插入和审计故障全事务回滚、无投影旧来源GET零写与首次编辑保留v1、合法大请求与400/413错误、注销会话拒绝。证据：root-review.json/log；使用隔离wenmi_rebuild_test_review121_fdbda09153，未接作者真实作品。
- 390/1440真实浏览器、HTTP API及PG：正常填写原表单→创建→资料→编辑→保存→刷新均通过；明确注入503后输入和数据库版本保留、重试只增加一版，刷新前书架名称已同步。JS异常0、非预期横向溢出0。正常业务接口未mock；只改测试代理Host/Origin以访问严格本地入口。独立读回使用同一真实API的inject接口。截图和browser-manual121.json位于artifacts/rebuild/121。
- 浏览器如实记录未迁移的membership/me与editorial-department 404；不影响这条手动路径，但不据此宣称会员/团队AI已接通。设定、取名、团队AI、正文及生产数据迁移仍未完成；RB-08/RB-20整体仍未验证，不能把本批子范围当全单元通过。
- 本地wenmi_rebuild_dev应用0007，原0001—0006未修改；核实后仅更新43282本地API，PID14980。直连及43280代理健康200，新接口未登录401；未重启生产API/Worker、未部署或迁移生产数据。本地预览http://127.0.0.1:43280。后台“开发中”含义在总计划4.1解释，总表记录本批证据。
