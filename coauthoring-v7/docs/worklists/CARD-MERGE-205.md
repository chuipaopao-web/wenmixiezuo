# R205 资料合并膨胀修复

目标：解决《三国送外卖：曹操催单了》资料短卡合并失败，保持设定确认与主编整理前置顺序。

证据：本书19页card已成功；merge:2:0连续两次输出计数5000后失败。37与26条分卡待合并，上一级另有10663字符卡。旧提示要求保留所有已有条目，合并近似累加。

范围：短卡合并提示、来源短编号传输及还原、结果大小校验、测试。保留原始设定、已有分卡、来源校验与逐页语义审查；不删除作者资料、不修改成员绑定、不放宽15000完整输入上限。新合并步骤版本隔离旧检查点，模板缓存版本升级。大小检测不截断内容，由资料成员归纳。

验证：别名跨卡一致/未知来源拒绝/原数据不变/过长输出拒绝；设计流程与资料审查回归；真实模型探针另记，不以测试替代模型质量。通过后按连续30秒无任务窗口发布API，无迁移。只发布代码，不同步内部历史文档。

实现：新增prepareCardMerge，仅调用时将原来源改为本次s1等别名，保存前还原并继续parseCard来源验证；未知别名拒绝，重复引用去重。每次归纳结果含原始引用JSON不得超过6000字符，超限由模型有限修正，不做文本截断。提示目标1500—2200正文字符，不设最低量；保留关键条件而不汇编局部细节。merge:v2步骤与旧步骤分开，旧分卡复用，超长分卡先归纳；新模板tm2-card-4避免新任务复用旧模板缓存。

验证：首轮28项、第二轮27项（共37项不同测试）通过；包含真实数据库的多页合并、来源还原、重复process不重发、模型网关预算与路由权限。API类型与生产隔离构建通过，功能管理校验13单元通过。没有新迁移、没有变更静态UI或成员配置。

部署：2026-09-13 API/Worker wm-v7-20260913-023000-a2050001，静态仍82f5df5bb15ba35e4a38。连续30秒无在途任务并立即复核后切换，API及Worker健康、未登录接口401通过，旧版本保留。只上传3个代码文件与发布脚本，未上传内部文档。

未完成验证：一次真实分卡合并探针被自动审批拒绝，原因是当前书籍私人资料发送既有外部模型仍需用户具体授权；未执行、未绕过、未重排作者任务。用户需明确允许本书资料向当前DeepSeek V4 Pro进行一次合并验证后方可继续该步骤。不能声称本书已生成或无BUG。探针脚本.local/r205-probe.mjs仅本地/服务器暂存，尚未运行。
