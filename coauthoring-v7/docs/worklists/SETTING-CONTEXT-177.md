# R177 设定资料容量治理

目标：解决已完成设定累积后整理资料自身过大，覆盖过长开书资料，保留每批最多4项与已有版本。优先由现有副编承担资料选择；无可用副编时由设计成员接替，有真实调用记录，不新增虚假岗位。

实现：资料页收紧到18000字符；开书资料超过4000字符时与设定完整事实一起分页选择，作者本轮要求和任务始终保留。只选择原文事实、不改写正式源，不按字符截掉条件。最终资料继续受12000字符校验，有限重新选择，无法容纳则明确停止而非发送超限请求。

验证：分页完整性、开书资料边界、非法引用、有限重试、现有设计链路与原版本保护、API类型；生产真实任务结果核查。该实现不能证明语义选取永不遗漏，也不声称无限资料均可容纳。

交付：同步功能管理和路线；安全部署，保持在途任务；完成验证上线后按用户要求关机。

验证：53项回归通过（.local/r177-tests.log），API类型检查通过。副编按planning_context合同调用，长资料集成测试核对实际执行者为deputy-deepseek-v4-pro。原R176真实5项已完成，原版本未变。R177尚待服务器构建、真实选材小样与部署。
