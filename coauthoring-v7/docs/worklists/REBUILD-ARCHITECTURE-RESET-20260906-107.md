# 第107批 全量业务重写的架构与执行安排

## 变更合同

- 用户决定：不复用旧业务代码，全部业务实现重写；开发者负责正确技术安排，不把工程选择交回用户。
- 本批目标：把目标架构、代码隔离、数据保护、迁移边界和下一开发批次落实到现有正式文档。
- 范围：REBUILD_DEVELOPMENT_SPEC第2/3/42/43/57/58章，REBUILD_EXECUTION_PLAN的总原则/当前位置/RB-00及相关旧复用措辞、卡片字段兼容，HANDOFF；本清单只保存合同和核查证据，不复制完整规格。
- 不改：本批不改产品代码、依赖/锁文件、数据库、部署脚本、生产配置、规则或Skill；不删除旧代码、作者数据或历史迁移。不把文档已定当新架构已建成。
- 保留：现有生产服务、账号/权益/正文/版本与审计证据；认可的产品需求、绿色视觉与可用静态素材；成熟第三方框架和密码学库不自行重造。
- 审查编号：ARCH-107。涉及身份、数据库、恢复和用量边界的是设计审查，本批不执行这些生产变更。
- 验收：现有代码事实与目标区别明确；不再同时要求新业务复用旧实现；下一批有范围/出口；受影响链接、RB状态和80个单元编号完整；只有文档变更，不运行产品测试或上线。
- 分工：Astra确定并写入架构；GPT-5.5只读核对迁移保护点及本地运行条件。后续由GPT-5.5编码、Astra关键审查，单次一个编码任务。
- 状态：架构与开发安排文档已完成、核查通过；新工程尚未创建、未部署。下一实施批次108范围已排定在执行文档3.1.1节。

## 核查与交付

- 当前生产仍为R104 API/Worker与R106静态首页；本批未连接生产。新rebuild目录尚不存在。
- 当前根工作区是React/Vite、Fastify和SQLite；独立API/Worker进程不表示所有创作任务已脱离API执行。
- 详细架构的唯一正文是开发规格第42—43章，下一动作唯一入口是执行文档第3节及RB-00卡片。
- GPT-5.5只读核查：user_id/owner_id与密码/会话哈希、会员不可变流水、用量及未知预留、正文/规划全部版本、模型未知调用、Worker正式化范围与已合并迁移均须保护。源证据为账号服务、0043/0045/0076/0084/0101/0103/0105迁移及Worker入口，未读取真实数据；主代理另核对scrypt参数与会话摘要代码。
- 环境事实：docker、psql、pg_isready、pg_ctl、postgres、initdb未发现可用命令，Windows未发现相应服务；不能据此宣称电脑任何位置绝无二进制，但当前没有可用的独立测试库。下一批先准备隔离数据库环境。
- 新决策：重写业务、不复用旧业务运行代码；Node24/TypeScript、React/Vite、Fastify5、PostgreSQL18与pg；独立rebuild工程。真实作者上线前单一身份/账务权威，默认不引入跨库旧服务调用；数据迁移与生产切换仍按专项门禁执行。
- 文档验证：80个RB单元及ID集合保持不变，全部RB锚点存在；新增9处本地文档链接目标存在，关键章节不重复，git diff --check通过。旧站后台复用语句标为历史证据，旧“业务可复用”与“直接下一注册”原则已更新；未运行与纯文档任务无关的构建或全量测试。
- 实际解析器验证：只读调用parseRebuildPlan通过，版本1.5、80单元、85来源；RB-00设计已定、前后端未开始、验收未验证。RB-00与旧RB-00.2卡片保持后台要求的固定字段名；修正105卡片标签缺失不改变已发布产品或其状态。tsx在沙箱中读取系统用户信息失败，获准在沙箱外执行同一只读验证后通过，未运行服务器或数据库操作。
- 交付范围只有HANDOFF、执行文档、详细规格及本清单四份文档；未改AGENTS/Skill、产品代码、依赖、数据库、模型配置或生产。没有真实容量/迁移/新架构运行证据，不宣称架构已经实现或零BUG。
