# 第143批：登录排查与版本文案清理

目标：前后台产品显示去掉V7，核查595341366@qq.com管理员登录。
范围：用户可见品牌、导航、提示；后台身份/登录请求超时恢复。内部接口、路由、版本和数据编号保持兼容，不改密码、权限、作者数据。
证据：生产只读账号为admin/active，最近审计无新增失败；匿名真实浏览器登录表单与输入框可见，auth/me返回预期401。用户具体失败提示待补充，不能称登录问题已定位。
验收：相关请求测试、双Web构建；390/1440登录及超时恢复浏览器检查。静态独立发布，保留旧资源/回滚，不重启API和Worker。
状态：静态修复待发布。账号admin/active；实际生产密码验证被自动安全审查拒绝（明文凭据进入工具调用记录），未执行验证或重置，不能判定密码错误。

相关请求与认证壳16项测试、后台类型检查、双Web构建通过。390/1440本地隔离管理员正常登录、网络超时返回表单并重新核验通过；移动端退出按钮在抽屉，测试改为检查后台已挂载而非误要求按钮常显。无真实模型调用。静态目标141ac7a1eaefd1e7a20b，保留R142资源；API/Worker保持R142。
