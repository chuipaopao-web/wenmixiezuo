# 第153批：模型输出可信边界与交付校验

## 最终交付

2026-09-08 03:14发布wm-v7-20260908-032000-fba2a3b8；静态89c64e8e60892efe6a9d保持，回滚R151。独立构建/闭包/服务账号文件归属校验、备份哈希与新鲜度、连续30秒在途零及即时复核通过；初次Worker心跳校验暂未通过，健康轮询后就绪，公网健康/401/后台资源全部通过。线上无网络模拟确认截断拒绝，数据库只读断言温度与成员和R152 restored.json完全一致。十二次合成调用报告2657输入/1497输出token。下文保留实验过程；未通过的设计提示没有发布，未改变所有模型的温度，不声称普适最优、零幻觉或整体提速。

用户授权落实准确、创作能力和性能兼顾的模型配置。不承诺零幻觉或全局最优。前批温度审计与两次小样未证明降温有效，保持现有温度、默认DeepSeek Pro和成员准入；官方参数来源沿用第152批已核验文档，未验证的代理能力不强制套用原厂参数。

范围：通用创作/审查系统提示明确正式事实、候选、未知、开放创作边界；适配器不把供应商明确截断的输出当成功。兼容现有输出结构、任务重试合同与自定义systemPrompt；不增加审查轮数、不修改作者资料和历史版本、不新增权限或模型。不改UI和图像参数。使用开发流程及长篇质量Skill。

验证：适配器定向测试含Messages与GLM Chat截断、未知响应、正常输出、真实思考内容不外泄；API类型/构建；短合成材料旧/新提示对比，默认Pro设计与已准入GF审查各两个案例、每个案例前后各一次，共最多8次调用，单次可见输出最多1000token，沿用既有思考余量和90秒时限，不遇未知结果自动重发。内容人工核对约束、证据、合理新意、误报，JSON成功不冒充质量通过。短样本不推断长期文学质量。

发布：验收通过按现有授权安全部署API/Worker；无迁移，保留静态、成员、配置及回滚代码，切换前连续30秒无在途且即时复核。若对比无改善或有退化，收窄修改，不能强行宣布最优。

状态：进行中。初版8次对比完成，GF把合理暴雨遮灯情节列为待核实，未通过；不发布初版。收窄提示为具体矛盾、普通观察/新增情节可成立、不臆断超能力。新增4次对应案例复核，总调用上限12次；不是扩展全模型大测。

最终范围收窄：只保留novel_reviewer事实核对说明，并明确不替代文学审查。设计侧第二版仍有围栏JSON及不自然情节，没有稳定改善，所以全部撤去；设计、正文、汇总提示和所有温度/思考参数保持原样。自定义systemPrompt不被改写。GF复核两例分别3.410/2.039秒，发现无依据功效保证、官职矛盾和未来计划冒充现状，放行A普通观察与C暴雨灯号。仅代表本次小样，没有统计显著速度结论，没有全模型质量结论。

代码验证：45项适配器定向测试、2项既有设定已知失败恢复/未知结果不盲目重发集成测试通过；API独立输出目录构建通过。默认dist目录写入权限受限，改用.local/api153构建，未覆盖旧构建。新增截断检查只拒绝供应商明确max_tokens/length终止的结果，保持已知可恢复失败，不把半成品标成功。不新增自动调用轮数。待上线。
