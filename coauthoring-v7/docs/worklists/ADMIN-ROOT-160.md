# R160 后台根入口与新管理员

范围：后台 https://admin.wenmixiezuo.com/ 直接加载管理页面，兼容历史路径及资源；新增用户指定邮箱的管理员，密码仅散列存储。保持作者站、既有管理员及作品不变。

验证：Caddy配置验证、根地址无跳转且内容与后台入口一致、资源及旧入口、真实登录/管理权限、健康检查。只重载代理，不重启API或Worker。账号若已存在先停止创建避免覆盖；操作留审计，不记录明文密码。

状态：已发布。生产Caddy校验和热重载成功，根地址及带section参数均200且无跳转，与历史管理入口内容一致。真实指定密码登录返回admin，管理接口读取成功，测试会话退出；首次探针缺少Sec-Fetch-Site被正确拒绝，按正常同源请求补全后通过。API/Worker进程不变。邮箱820003938@qq.com已新增为active管理员，独立owner，无会员赠送或作者数据变更；创建留auth审计。配置回滚文件在/opt/wenmi-releases/admin-root-160/Caddyfile.before。密码不写入正式文档或Git。
