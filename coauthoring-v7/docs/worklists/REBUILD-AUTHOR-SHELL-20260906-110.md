# 第110批：作者端外壳、导航与兼容页面迁移

状态：本批布局合同已通过，2026-09-06。新工程未发布；整个重构、RB-06完整系统状态/认证联调仍未完成。用户最新授权：Codex按后台路线自主安排GPT-5.5逐项实施；需要新授权/用户测试/密钥的项登记跳过，继续不依赖它的工作。技术决定不反问用户。用户醒来后继续讨论。

## 变更合同

- 优先交付：独立rebuild作者端全局外壳与已定导航；允许复制经审查兼容的旧源码/素材，再在新工程验收；不兼容部分才重写。此决定覆盖107批全量禁止旧代码复用。新工程不得运行时导入旧工作区或共享旧数据库/密钥/生产启动。
- 顶部默认仅两个收起的菜单按钮，左打开书架，右展开发现功能导航；桌面和手机沿用当前色彩、排版与内容宽度规律。书架仍位于左侧，菜单支持关闭、Esc、点击外部、焦点返回、路由切换收起和无横向溢出。
- 一级导航：信息、时光机、规划、状态、福利。信息保留开书资料/设定；库在时光机中作为二级入口；卷/链/章属于规划二级入口；团队/任务归入同一状态页的清晰分区或二级切换；福利为展示页，不虚构奖励、金额或推广结算。
- 保留：公开首页、登录后首页、手动新建小说、信息/设定/时光机/卷/链/章/库的现有内容UI；只改变外围导航和容器，不重新设计页面正文。保留已选书和卷链章定位、深链接、浏览器前后退、刷新、开书草稿/返回路径。
- 允许修改：rebuild/apps/author-web及所需复制前端文件/素材、独立依赖/测试配置与边界检查的必要兼容；本合同、HANDOFF、执行计划、规格与开发流程中与最新授权冲突部分。复制来源与必要差异留清单。
- 不改：旧业务源文件/旧部署与服务/根依赖/生产数据；本批不实现或宣称完成所有后端API，不连接生产做写入，不部署重构工程。旧页面API未迁移时保留明确的加载/失败状态；模拟数据只用于显式本地预览/测试，不能假装真实作者数据或功能已联调。
- 验收：新工程独立构建、组件/路由受影响测试；390/800/1440真实浏览器导航、书架、返回/刷新、规划二级、时光机库入口、状态/福利、长书名与错误态。受保护页面源码/截图对比，记录真实API与夹具测试区别。
- 回归：108/109基础配置、导入边界和任务底座不受影响；本批不改任务/数据语义，不运行旧全量verify:full。只运行独立工程门禁和受影响作者端测试。

## 后续自主执行安排

布局验收后依执行计划继续账号闭环及后续单元。每批新合同、GPT-5.5实施、根代理审查、验收后才标完成；需要用户介入的条目记录具体原因、未完成验证及依赖影响，可继续独立部分。持续维护执行文档为唯一进度源，不能静默跳过阻断依赖或把跳过项标已完成。

## 证据与跳过项

公开/作者页面的当前源码为coauthoring-v7/author-app；开工时新作者端仅108基础占位页，109没有真实会话或书籍API。本批布局证据不能替代账号和书籍后端验收。

- 主审已启动只读旧作者端预览127.0.0.1:43380，使用原测试数据经Playwright拦截全部API；没有真实账号或作者写入。已记录公开/登录后首页、手动新建、信息、设定、时光机、卷链章共9页×390/1440共18张基线，均无JS错误、未匹配API或横向溢出；证据artifacts/rebuild/110/baseline.json及before-*.png。
- 布局完成后下一批账号核查：仓库代码、依赖与配置样板尚无邮件通道接入证据；真实发送、真实邮箱收信、生产旧账号迁移与启用暂跳过，保留待办。账号服务/单次验证凭证/会话/找回改密可在隔离PG与测试MailerPort先做确定性验证，不能以测试模式取消验证冒充真实闭环。
- 本任务已设置每30分钟接续检查（automationId: automation），运行中不重复派工，无变化保持安静；有新用户指令以最新为准，需用户介入或无独立可执行工作时暂停并汇总。

## 主审进行记录

- 新工程9页面×390/800/1440共27次真实Edge页面检查无JS错误、未匹配API或横向溢出，全部使用显式测试夹具，未验证真实账号或创作API。证据`artifacts/rebuild/110/after-pages.json`。
- 迁入的9个受保护内容测试文件共130项通过；整套作者测试首次215项中205通过，旧导航测试需适配新入口，业务断言不得删除。日志`artifacts/rebuild/110/content-tests.log`与`all-author-tests.log`。
- 已退回修正：初稿顶部第三个品牌按钮、二级入口位置、福利研发术语、团队分区刷新丢失；随后发现收起菜单仅透明但仍可键盘访问、跨一级菜单和状态分区丢当前书/卷链章定位、手机展开菜单继承旧网格导致挤压裁切。自动断言通过不能代替截图主审；上述问题全部修复并重验前，本批不标通过。
- API客户端改为同源`/api`，仅由新Vite代理至43282，不读旧通用`VITE_API_ORIGIN`；生产启用另按发布合同配置。

## 最终验收

- GPT-5.5修复上述主审问题后交付，主审验证通过：216项作者端测试（16文件）、17项基础测试（5文件）、严格类型检查、83源码边界扫描、作者端与后台构建。旧导航测试只调整访问路径，任务/草稿/归档/返回等业务断言保留。
- 主审干净副本`npm ci`和`npm run verify`均通过，依赖审计0漏洞；未停止原预览进程。首次离线安装因缓存缺xmlchars失败，改为按锁文件联网安装后通过。构建仍提示作者端666.64kB单chunk（gzip188.44kB），后续页面模块化时优化，不以改高警告阈值掩盖。
- 27页面截图与30项菜单/路径检查通过，视口390/800/1440。二级入口、真实隐藏与焦点返回、菜单完整可见、状态分区刷新/后退、跨功能保留书/卷链章定位、换书清旧定位均已实测。全部业务数据是浏览器显式拦截夹具，不能计为真实账号/创作端到端通过。
- 未拦截API的实际检查：只请求43280同源`/api/v1/auth/me`，新API尚未实现时显示服务暂不可用与重新连接；没有自动登录、模拟作者或生产请求。
- 复制来源：`coauthoring-v7/author-app/src`和`public`。52个前端文件中49个有旧来源，41个与来源相同；8个受保护内容组件（公开首页、手动表单、新建、信息、设定、时光机、规划工作台、库）内容逐字相同（仅忽略换行编码）；原CSS未删改，在新工程内追加外壳规则。新增入口/测试/API基址和必要导航兼容，不运行时引用旧目录。
- 本地证据：`artifacts/rebuild/110/after-pages.json`、`interactions.json`、`unmocked.json`、`source-audit.json`、`clean-review.json`与对应PNG/日志。证据与临时浏览器夹具是本机审查产物；提交的`npm run test:author`与`npm run verify`可从新检出重现工程验证。
- 未验证/未交付：真实身份、邮箱收信、书籍API、真实模型/任务联调、生产迁移和发布。RB-06仅外壳子合同通过，单元整体保留开发中；RB-06.1纯展示范围通过，不代表邀请奖励/会员结算完成。

## 下一批接手

依执行计划进入第111批RB-01/02/03/04账号闭环，先建立权限/账号隔离设计合同与相称验收，再交GPT-5.5实施。沿用新PostgreSQL事务/仓储边界，不原样复制SQLite身份服务；开发端使用独立会话cookie避免同一localhost下与旧服务混淆，不开放首次注册自动成为管理员。邮件通道缺失时先验证本地服务合同与测试MailerPort，禁止取消邮箱验证冒充成功、禁止伪造送达；真实发送、新服务密钥/登录、生产旧账号迁移列为跳过待办。用户新指令优先，当前30分钟接续任务继续有效。
