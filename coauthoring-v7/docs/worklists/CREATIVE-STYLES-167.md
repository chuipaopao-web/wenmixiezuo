# R167 主辅风格偏向

最终状态：已上线wm-v7-20260909-033000-d5567614，静态a90ded41112f65bb0f71；首次错误头像校验触发回退，修正后上线成功。回退/再次切换均等待连续30秒在途零；健康就绪和未就绪重试路径单独验证。发布目录保存public-checks.json、deployment-passed、r167-cutover.sh与r167-precheck.py。最终开书接口22项通过，确认五项随建书保存；规则3项、首页58项及390/1440隔离真实组件验证通过。公网HTML/脚本/头像字节校验、健康和未登录401通过，额外线上浏览器流程被审批拦截未执行。无本批真实模型文学效果证据，未增加模型调用。

授权：用户确定一个主偏向、最多四个辅助偏向并要求实施更新。
范围：沿用有序styles数组，首项为主、其余至多四项为辅助。前端明确分两组；可不选由成员判断；换主项保留其他辅助但去除重复，清空主项同时清空辅助。后端允许至多五个合法风格；旧三项草稿/任务保持兼容，既有作品和任务快照不回填。提示词明确主偏向持续主导，辅助按情节需要使用，不强行每章全部体现。更新后台同源规则、功能介绍和路线。
保留：首页风格、头像、尺度、成员、输入与自动保存、任务恢复及计费。无数据库迁移、无账户或书籍修改。
验证：风格上限/去重/主辅上下文、前端选择换主取消与提交恢复、手机/桌面显示，受影响包构建。无需重复真实模型收费探针；本批验证输入规则和传递，不声称文学效果提升。
部署：验证后安全发布前后端，连续30秒在途零后切换服务，保留旧代码和静态资源；状态说明发布后同步。
状态：实现完成，准备发布。3项创意合同与58项首页交互通过，390/1440浏览器选择五项并真实提交请求、失败后保留输入、后台128卡与说明、头像加载及无溢出通过。Web类型与后端/API/组合静态构建通过；文档来源核对13页通过。仅源文件/文案变更，无迁移；发布保留已验证完整备份和旧版本，不写数据库或放宽磁盘预留。
