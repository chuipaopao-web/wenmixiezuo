# R173 设定完整短句

目标：去掉规则详情，把条件、适用范围、代价与例外融入规则正文；减少重复而非截断事实。

范围：生成/既有主编审查指令、完整规则展示、后台功能及路线。保持每批最多4项、现有模型参数、版本和来源隔离、失败恢复；不新增固定精简岗位，不删除作者内容，不自动在读取页面时调用模型。按用户追加决定，旧辅助小字不再显示，用户会重新设计，现有统一整理按原版本产生候选修订，不静默覆盖正式设定。

验证：新旧完整规则、条件例外和范围保留、主编局部修订、组件展开/编辑、390/1440浏览器、相关后端回归、类型和发布构建。模型语义效果与工程通过分别报告。

部署：相称验证通过后发布，静态保留旧资源；后端只在在途连续30秒为零后切换，保留代码回滚。无迁移、作者数据清理。

状态：本批已上线。现有资料选择提示本身超20k预算导致的独立失败尚未修复，不将本批文字优化等同解决该故障。

发布：2026-09-09 06:54:56，wm-v7-20260909-070000-5a79d5c2，静态e97fe5ace776fee3d4dd。最终独立构建、模块、13功能来源校验通过；在途连续30秒零后切换，公网资源/401、API与新Worker健康检查通过，无近期服务错误日志。未修改或清理作者数据。发布证据/opt/wenmi-releases/wm-v7-20260909-070000-5a79d5c2，回滚为R172代码与0cdeb165a28f33c833c0静态。早期71869c98仅构建，从未上线。最新用户要求为直接隐藏旧小字而非兼容展示，最终浏览器证据已覆盖。

实现：设计及现有主编指令统一为完整短句；主编发现旧辅助字段时按完整规则原文定向合并，无事实冲突也不会跳过。主编合同版本3使新整理不复用旧版完成标记；旧任务来源及历史版本保护不变。前端取消详情入口，旧辅助字段按用户要求不再显示，底层原值保留，不自动读取即调用AI。

2026-09-09验证：61项完整相关后端回归通过（artifacts/r173-complete.log），27项作者组件通过（artifacts/r173-author-final.log），全工作区类型通过（artifacts/r173-type-complete.log），390/1440隔离浏览器无溢出、页面错误或写入，早期旧字段同段展示验证已被用户新要求替代，最终仅显示正文的27项组件/类型/390与1440浏览器重验见artifacts/r173-author-final2.log、r173-author-types2.log、r173-browser-final2.log（artifacts/rebuild/173/browser.json）。新增主编合并路径、旧版本不变、投影同源检查通过。DeepSeek四条合成规则约5.3秒交付，必要条件/代价/数值及不可逆后果保留，但仍有一项scope重复；Kimi该次调用失败，不报告全模型通过。初版独立构建已通过但未上线，追加UI修改重新构建后再发布。
