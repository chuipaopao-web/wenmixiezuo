# R173 设定完整短句

目标：去掉规则详情，把条件、适用范围、代价与例外融入规则正文；减少重复而非截断事实。

范围：生成/既有主编审查指令、完整规则展示、后台功能及路线。保持每批最多4项、现有模型参数、版本和来源隔离、失败恢复；不新增固定精简岗位，不删除作者内容，不自动在读取页面时调用模型。旧辅助事实在合并前完整展示，现有统一整理按原版本产生候选修订，不静默覆盖正式设定。

验证：新旧完整规则、条件例外和范围保留、主编局部修订、组件展开/编辑、390/1440浏览器、相关后端回归、类型和发布构建。模型语义效果与工程通过分别报告。

部署：相称验证通过后发布，静态保留旧资源；后端只在在途连续30秒为零后切换，保留代码回滚。无迁移、作者数据清理。

状态：进行中。现有资料选择提示本身超20k预算导致的失败尚未修复，不将本批文字优化等同解决该故障。

实现：设计及现有主编指令统一为完整短句；主编发现旧辅助字段时按完整规则原文定向合并，无事实冲突也不会跳过。主编合同版本3使新整理不复用旧版完成标记；旧任务来源及历史版本保护不变。前端取消详情入口，旧字段未合并前同段保留，不自动读取即调用AI。

2026-09-09验证：61项完整相关后端回归通过（artifacts/r173-complete.log），27项作者组件通过（artifacts/r173-author-final.log），全工作区类型通过（artifacts/r173-type-complete.log），390/1440隔离浏览器无溢出、页面错误或写入，完整旧条件仍可读（artifacts/rebuild/173/browser.json）。新增主编合并路径、旧版本不变、投影同源检查通过。真实模型样例、独立发布构建和线上校验待完成。
