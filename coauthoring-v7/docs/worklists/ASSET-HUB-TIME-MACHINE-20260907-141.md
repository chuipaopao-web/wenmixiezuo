# 第141批：统一资产中心与时光机阶段路线

- 状态：预发布验证中。用户授权自主完成资产整合、设计逻辑入路线、时光机调整与安全部署。
- 已核实：生产后台管理员登录后节奏接口200/v1/9卡；本地43170登录后同路径404，开发路线200。此前未对本地已登录接口验收，导致新页面与旧服务不同步；须修复本地入口并增加真实端到端验收。
- 后台：所有方法资产共用一个侧栏入口，顶部切换总览、节奏短卡、叙事方法、剧情模式、剧情配方、分层应用；保留已有查询和详情，去掉重复侧栏入口及过期固定三主编说明。功能路线展示每个子功能设计目的、输入输出、处理与版本逻辑、完成范围。
- 时光机：承接已确认开书/设定，设计开局到终局的全书走向，主要故事线、阶段成果与代价、因果承接、阶段到卷的分配和字数；阶段不强等于卷数。同一节奏可以跨阶段复用，下层展开上层责任；保留真实正文进度和候选/确认区别。优先扩展现有版本化规划树与任务底座，不建立第二套正式剧情事实。
- 保留：作者绿色风格、开书与设定功能、正式资料/正文/历史规划、会话权限、计费、任务防重和恢复。新规划须经作者确认，已有书籍不会自动重写。
- 验证：本地和生产已登录后台真实读取/预览；统一资产顶部导航及390/1440展示；时光机受影响解析、版本/权限/候选确认/上下文传递与恢复；一次有界合成模型样例和独立阅读。按改动范围构建，不重跑无关测试。
- 发布：后台可先发布；时光机闭环验证后继续部署。后端向后兼容，备份与回滚、连续30秒零在途任务及立即复核；不取消作者任务制造窗口。
- 后续清单：1定位本地404；2资产统一页面与路线说明；3时光机输入和现有树合同核对；4实现阶段/故事线/字数及显示与确认；5验收上线并同步进度。

## 已完成与证据

- 404根因：隔离API43196旧PID2748仍运行第136批加载的代码；43170前端已更新但API未重启。生产登录读取正常。隔离API重建后节奏GET200、预览POST200、路线82项，未接生产或模型密钥。后续本批API本地PID32228。
- 资产六类导航与路线设计说明已实现。实际管理员登录的390/1440页面均通过，见`artifacts/rebuild/141/local-browser.json`及截图；不是mock后台。只读预览未发布配置。
- 全书可选目标篇幅/卷数、阶段与故事线、版本化保存确认、卷级精简投影已实现；独立工作区同步界面与类型，保留其原API适配器。没有新迁移，也没有改旧规划/正文语义或任务步骤，不触发无关全量验证。
- 受影响规划运行/恢复与树持久化26项、蓝图和文档解析14项、作者39项、后台9项通过；API、运行核心、作者/后台类型及独立工作区专属编译器通过。类型检查曾误用根TS7检查独立TS5配置，已改用该工作区自己的编译器；运行核心全部测试文件历史类型问题不属于正式构建，正式构建通过。
- 恢复复测捕获并修复合同标记被岗位快照投影丢弃的问题，明确失败续跑保留bookBlueprintVersion；旧任务无此标记则沿用旧合同。
- 时光机浏览器为合成书的真实组件与API响应夹具，验证手机/桌面阶段、故事线与调整入口；持久化/确认/权限由真实隔离数据库HTTP测试覆盖，不冒充生产作者作品验收。
- 待完成：真实合成模型交付与独立阅读；静态/API预发布与生产安全切换；线上已登录复核，补充最终版本。
