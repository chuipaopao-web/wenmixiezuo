# 第147批：成员候选接入与设定审查实测

目标：核对56位成员中40位暂离的真实原因，接通所有已配置文本模型的固定岗位候选与管理入口；区分可测试与已通过具体节点验证。只测试设定审查，不测试设定设计；设定设计优先DeepSeek V4 Pro、V4 Flash、Kimi 2.7。停用GLM5.2，保留GLM5.3及Flash；未绑定席位不虚构可用状态。

范围：共用成员名册/节点准入，设定审查正确调用策略与失败接替，后台设定审查耗时/用量/结果，团队真实状态。保留原头像、姓名、固定岗位、开书已有评测与用户页面视觉；不扩大为全站各任务模型实测，不启用未经验证的自动正文工作。

模型验证：既有套餐通道，每个当前文本型号一次同一合成设定样本，最多180秒，最多2并发，无自动重试，记录真实可用结果耗时、Token、结构及人工语义判断；失败不入榜。合成资料不含作者作品；不读写生产密钥。修复旧设定任务时保留输入版本、候选与历史，接替生成新尝试审计，不改旧模型快照或覆盖作者原文。

验证部署：受影响名册/审查/恢复/管理与团队页面测试，相关类型和构建，390/1440界面；不跑全产品测试。代码与模型实测通过后安全部署，API/Worker切换遵循在途连续30秒为零并立即复核；旧版本回滚，静态保留旧哈希。开发路线与HANDOFF记录实际完成边界。

状态：2026-09-07 23:39已发布wm-v7-20260907-233000-6df36911，静态c4ba0e045624eb165403。实际生产原有6个GLM Flash空绑定，已通过管理配置事务补齐并留before/after及slot_events审计。56身份中43绑定、13未绑定预留；绑定不等于所有业务节点通过。

生产：在途连续30秒为零后切换，API/Worker最终健康、版本及心跳一致；启动时首轮心跳尚未更新，后续就绪检查通过。公网资源、管理员资源/头像哈希与未登录401通过。原始数据只读核对确认审查三型号和设计三型号已在实际名单。未执行生产已登录浏览器验收，未自动重跑作者旧失败任务。源码闭包与实际模块解析通过，代码06c35cb2及后续9f656a1c/6df36911。回滚代码R142、静态R146，模型绑定可按审计反向配置，不恢复作者数据库。

实测：每型号一次，无设计测试、无补修。Kimi K3 33.8秒、豆包88.1秒、GLM5.3 Flash131.7秒通过本样本；DeepSeek Flash9.5秒漏1处冲突、Pro20.2秒修订引入新能力、Kimi2.7 66.9秒字段不合格、GLM5.3 180秒未交付，不进入本节点自动审查。原始合成证据在服务器/opt/wenmi-releases/setting-review-147/evidence.json，本地.local/r147-review-evidence.json；共用目录setting-evaluations.js记录人工判断。已知输出Token合计15394，超时用量未知，不冒充完整账单。

根因与修复：原统一整理把审查按structured_planning执行，Kimi K3消耗10000输出额度却没有可用正文；冻结候选排除设计Pro后仅剩K3，无法交接。改用novel_reviewer，按本节点合格模型接替，审查时限180秒；未知结果继续保护。已知失败且无成功输出时生成关联旧任务的新恢复尝试，保留原内容/调用；新增名册成员不使旧冻结模型失效，变更绑定仍拒绝继续旧任务。设计按指定三模型优先，不追加昂贵设计探针。

验证：49项设定/治理集成、24项作者界面、3项后台测试通过；API/作者/后台/rebuild类型通过，独立输出API构建及作者/后台静态构建通过，390/1440真实本地API登录页面检查通过。新测试覆盖恢复防重、原设定/历史不变、候选节点准入和解绑、冻结名册扩容兼容、正式审查策略。生产新备份20260907T152410Z-602979完成；只清理8份早期可重装依赖缓存，未清理作者数据或备份。
