# 第116批：已完成导航的现网兼容发布

状态：进行中。用户要求将已经完成的功能部署上线；既有持续生产发布授权有效。本批先将110批已经验收的作者外壳接入现网，不发布未完成rebuild账号/PG/建书接口。

- GPT-5.5在D:/wenmixiezuo/.local/release116/worktree独立工作区适配；复用rebuild的AuthorApp/navigation和外围CSS，保留当前生产作者API、账号/会话、书架请求合同及构建配置。源码差异最终提交回同仓库，保留可追溯来源，不运行时依赖rebuild。
- 发布已定双折叠菜单、信息/时光机/规划/状态/福利一级导航、库归时光机、卷链章归规划、任务团队归状态，以及不承诺奖励的福利展示。旧深链接仍能进入对应内容页，选中书/卷/链/章、返回/刷新、手动草稿不丢失。
- 保留公开首页及编辑部头像、手动新建、所有现有内容页和模型/任务行为。不复制新PG分页包络或本地限定认证到线上。前端测试覆盖真实现网响应合同，隔离夹具仅用于页面状态/交互，不冒充生产作者读写。
- 按前端共享外壳范围做作者测试、类型/构建、390/800/1440关键页面和菜单/焦点/前后退/失败恢复检查；主代理独立差异审查及浏览器验证。冻结生产API/静态基线，核对源文件兼容。
- Web-only生产部署：准备独立已校验静态版本，保留线上后台与旧哈希资源，原子切换、失败回退，验证公网资源/首页/登录和导航。无API/Worker重启、无数据库迁移、无作者任务重跑/取消。不把新工程整体覆盖现网。
- 后台功能地图发布单独核查：96—98批仅本地，依赖受管理员保护的只读后端投影。不能在接口缺失时只发后台页面，也不混入本批作者静态发布。核查后明确是否可发布及必要验证。

部署证据、剩余项与提交号在收尾时原位补充。本批不声明整个重构已完成。

## 发布前验收

- GPT-5.5交付5个作者端文件，主代理独立重跑210项作者测试、类型检查、构建通过；额外菜单互斥测试通过（总计211项）。测试保留旧账号、归档恢复、任务与手动建书行为断言，只适配新入口。
- 主代理浏览器首次验收发现书架遮罩挡住右上菜单，已修正topbar定位/层级和书架top=58px；同样修正rebuild副本，防止后续复用带回问题。没有更改内容页。CSS由110批原样复用加上述三行修复，保持绿色视觉。
- 修正后390/800/1440共27个内容页面检查及菜单真实点击、遮罩/关闭、Esc焦点恢复、路由参数、刷新与后退通过；390/1440公开首页和登录注册切换通过。作者状态来自明确的合成夹具，不冒充生产用户写入或模型任务验证。无横向页面溢出、脚本异常或未覆盖API请求。
- 证据：artifacts/rebuild/116/root-tests.log、root-menu-test.log、root-typecheck.log、root-build.log、candidate-browser.json及截图。构建保留原有单块JS大于500kB提示，本批不扩大为拆包重构。

## 已核实的发布条件

- SSH只读核对生产API为wm-v7-20260906-003247-ce17325，静态为e37239512b0e6f314e72；API/Worker/Caddy正常，健康允许模型任务。生产作者源码由R104与105/106首页补丁组成，57个文件与本地当前作者代码规范化哈希一致，二进制图片按原字节校验。
- 生产/opt/wenmi/releases为root所有，/opt/wenmi/current/deploy/activate-v7-static.sh实际不存在；不能直接执行DEPLOY中的通用示例。本批复用R106已验证的root部署锁、入口原子替换、previous指针及错误回退方式，通过生产实际存在的R104静态校验工具预检，不改目录权限。
- 后台地图不是本批Web-only包的一部分：它还需要部署/api/v1/admin/rebuild-control后端，保持原后台全文件不变。后续发布需刷新旧测试中79单元的过时断言为当前81单元，核对生产组合与管理员权限，等待安全后端发布窗口；这些是开发和验证工作，不需要老板另行提供技术选择。
