# 第101批：指定用户卷设计资料整理失败

状态：实现及本地验证通过，正在准备生产组合包；尚未部署。

## 变更合同

- 目标：核实1746495718@qq.com截图中的卷设计阻断，修复已证实根因和直接相关恢复入口，保留完成的步骤。用户最新更正：卷最终资料包为15000字符，不是36000；仅在证据确认模型Token不足时，才把相关成员Token预算扩大3倍。当前两次失败的资料调用输出仅76—700/2500 Token、选择调用1906—1997/3000 Token，无Token耗尽证据，不无差别扩大所有调用。
- 范围：卷设计资料读取/预算/校验/检查点与恢复状态、相关作者界面和受影响测试；按证据进一步限定，不混入整体重构。
- 保留：作者原文、冻结输入、正式设定/规划、不可变正文、已完成结果、调用和计量证据、当前视觉风格；不更换供应商、不删除数据、不自动重跑或停止真实作者任务。
- 证据：景阳长歌、第一卷；历史任务86a1f1d1于20:05失败，最新b751b00b于21:35失败，均为context_selection。生产机内只读复现既有返回：8页中第5/第4页失败，11597/11492字符超过8916/8914可用额度；后一次补交仍10424。第3页把72项全部标为不可删除，先前Agent判断被永久锁定，后续重复资料挤占空间，且尚未读到末尾全书方向。没有模型重放或原文导出。
- 修复闭环：卷最终完整发送包严格<=15000字符（其他步骤保留不高于15000的既有额度）；反馈精确包装成本/入选成本，Agent有限自动重整。先前Agent的硬约束标签允许经专门覆盖核对后纠正（作者原文和正式来源不可更改），不得静默丢失真正硬要求；全页通过与完整包长度核验后才激活。未知不重发；每页初次最多3次提交（含首次）；手动恢复先复用既有结果，只为耗尽次数的页增加最多3次补交。恢复键绑定本次请求，避免永久读取同一坏返回。没有无限重启。
- design_review_id：R101-EVIDENCE-COVERAGE。只修正Agent衍生的essential标签，正式源、版本、作者原文和实际状态权威均不变；覆盖审核仍由同一配置成员执行，不能称为独立复核。编号只能指向本页冻结原文；删除旧标签需逐项批准，缺失、拒绝、未知均不能交付。反例验收：重复可合并、否定/条件不能消失、拒绝后保留旧证据、审查网络未知不得重发。无既有受保护数据变形或迁移、无工作流结构重构，运行受影响门禁。
- 恢复：既有资料选择及有效分页检查点复用；context_selection明确失败提供继续整理入口，恢复同一workflow，不要求停止重建。工作中重复恢复防重，未知调用不重发；对超出自动次数的失败显示真实原因类别及可执行恢复。
- 验收：复现真实失败机制的合成回归，成功步骤复用，已知失败可恢复、未知不盲目重发、字符预算和正式来源边界；相关API/Web类型检查与构建，涉及页面时核对手机/桌面真实状态。不能以合成测试证明该作者已恢复。
- 风险：若触及ContextCompiler权威来源、既有受保护数据语义或核心流程结构，先补设计影响与相应门禁；局部已知故障按受影响范围测试，不默认全量。
- 发布：第100批生产组合加本批；排除本地后台96—98批。本地构建，生产轻量检查、备份、保留旧资源与应用回滚点；API/Worker切换前连续30秒零在途并即时复核，不取消作者任务制造窗口。
- 限制：此前“失败返回再次发给模型”的自动补交受审批限制，未经明确授权不实施或回放；可采用不发送失败原文的合适恢复方式。

## 进度

- [x] 读取交接、相关Skill，检查工作区（仅既有未跟踪.claude）。
- [x] 指定账号的线上任务与失败详情，生产机内只读重建两次预算失败；最新失败是新任务，不能归咎于旧页面或用户未刷新。
- [x] 复现、限定修复、验收。
- [ ] 安全发布、公网复验与交接。

## 验证证据

- 单元与集成：资料阅读17项、ContextCompiler16项、创作总线21项；作者工作台28项，全部通过。覆盖预算、来源/版本、全页硬要求、未知不重发、覆盖审核通过/拒绝/网络未知、恢复键与有效页复用、原任务继续、卷链章至定稿维护既有回归。
- Backend构建、API类型检查/隔离构建、作者端类型检查/隔离构建通过。API第一次隔离构建的CLI相对输出路径解析到了D:/.tmp，权限拒绝且未写入；改用项目内绝对路径后通过。没有操作线上进程。
- 真实配置模型、完全虚构材料：5次分批读取，3375/15000字符；保留公开测量结果时机、不能用超能力、先检测木材、检测前不得宣称旧桥安全及当前卷责任。记录：artifacts/diagnostics/r101-synthetic-probe/pack.json。不能代替真实作者恢复或长篇文学验收。
- 新覆盖审核真实探针2次：重复片段获准移除；真实安全约束移除被拒绝，补交保留原证据。记录：artifacts/diagnostics/r101-coverage-probe/summary.json；未发送用户作品，未保存思维链。
- 真实浏览器使用产品组件+隔离虚构接口，390x844手机与1440x900桌面：恢复主按钮可见、不挤压失败说明；继续后刷新保持工作中、提交计数1；未知核对后仍为1，未重复生成。视口已恢复、验收标签页已关闭。此为本地交互证据。
- 已检查完整10个产品/测试文件差异；未跟踪.claude保持不动。无迁移/作者数据改写/新依赖；未运行全量门禁。
