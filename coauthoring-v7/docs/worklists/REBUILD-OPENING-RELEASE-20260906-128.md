# 第128批：开书模块完整发布

目标与授权：用户要求继续直到本项上线、可以进入下一项。主代理直接推进，不以内部接口完成结束。复用现有跑通功能，保留资料/设定/取名及页面风格；已批准的头像选择和状态精简一并交付。

技术纠偏：此前把单功能发布与PostgreSQL整库切换耦合，造成确认后书内功能、身份和账务仍不可用。采取模块与存储分开切换：独立rebuild中的开书核心作为单独编译的共享运行包上线，当前API使用经过验证的SQLite存取/身份/账务/确认/修订适配器。生产继续一套身份、一套账本和一份作者数据；不双写、不导入或重置用户。PG适配保留测试状态，后续专项数据迁移，不标已切库。这属于已授权重构的可逆工程安排，未改变作者功能和权益目标。

实施：独立核心包及构建/发布闭包；现网API依赖新核心并复用原完整任务/修订/审查/确认路径，修复候选提交前中断可能重复生成；发布已批准的成员头像选择和状态简化。核实领取/租约/恢复和已确认书兼容；必要修复限制在此路径。

验收：原完整开书/修订/决定/模型账务及新核心身份一致性；真实事务中断恢复、不重复生成/扣量；原页面手机/电脑合成流程；构建与发布闭包；有限真实模型探针和旧账号/旧书只读证据。最高相关风险按验收标准集中全量一次，修正期只跑相关测试，不另派代理。

发布：常规安全部署授权持续有效。冻结准确源码与编译产物，先保留回滚版本，在途连续30秒零并立即复核后逐服务切换；不取消作者任务制造窗口。公网接口、资源、版本和新开书全流程验证后更新后台状态。任何尚未实际部署的PG功能明确保留未发布。

状态：已发布，完成本项后可进入RB-20。服务`wm-v7-20260906-183000-467a62b9`，静态`5f895e8eb5b64a93c6d6`，北京时间2026-09-06 18:36:57。

本地验收（2026-09-06）：集中根回归488通过、2处失败为已有后台心跳SQL边界遗漏及过期构建输入数量断言；将同一心跳查询移入现有审计仓储、更新真实清单后，相关16项复验通过。其余未执行的工作区测试继续完成：核心99项及16项Node测试、作者211项、后台49项通过（原3项跳过保持）。未重跑全部已通过测试。最终类型/构建/源码闭包通过。SQLite候选事务真实中断后自动恢复，2次模型调用、2个候选，会员扣量与实际回执一致；未知结果不盲目重试。390/1440选择、设计、审查六个浏览器状态通过，选择持久化及键盘提交正确；图片见artifacts/rebuild/128。编译产物隔离探针完成改稿复审、确认防重、书架/资料读取与账务核对。

发布范围增加后台心跳查询归位（相同SQL移至仓储，行为不变），用于满足已有分层验收；没有借此重构其他后台逻辑。工具本地使用现有npm12进行离线workspace链接，第三方依赖未变化；生产构建仍使用服务器已安装工具。部署前核实R122与在途0；上线前必须重新检查连续30秒零。

生产证据：服务器重新构建与本地Web字节一致，3个workspace模块解析到同一新release。隔离真实模型调用仅两次：deepseek-v4-pro设计约76.8秒、kimi-k3审查约3.9秒，总81秒，成功成书/读取资料/扣量核对，未用作者原文。原编译流程合成探针另覆盖改稿、复审和确认防重。生产无迁移、无数据导入；完整备份及预检数据库检查通过，连续30秒在途0后API就绪再启动Worker，公网资源哈希、入口及受保护接口401正确。

空间处理：初次stage因完整备份保留5GiB要求未满足而退出，未切换。对`wm-v7-20260901-153007-bb16a6ec`、`wm-v7-20260902-032349-499e3819`中无占用的preflight-data/database/wenmi.sqlite副本无损压缩；原文件单链接、WAL空、非在线库，压缩前/解压后SHA一致才释放未压缩副本，归档及元数据留在原目录。释放约1.2GB后完成备份`20260906T103530Z-550138`，未降低保留空间要求，未清理作者数据、日常备份或回滚源码。
