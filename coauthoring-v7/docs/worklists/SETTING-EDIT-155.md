# R155 设定编辑按钮可见性

目标：修复点击修改内容后保存按钮白底白字。根因是结果卡片后代按钮规则覆盖内嵌面板主按钮背景。
范围：将普通按钮样式限定为卡片直接操作栏；保留设定内容、保存复审、取消、重新设计及任务逻辑，不修改模型和数据库。
验证：相关页面测试、构建及桌面/手机浏览器可见性检查。通过后仅发布静态资源，保留回滚版本，不重启API/Worker。
状态：修复已写入，验证与部署进行中。
