# 第132批：开书节点模型评测与准入

授权：用户要求按已讨论顺序开工，复用跑通功能，完成测试后上线。本批聚焦开书设计（策划编剧）和现有开书审查（主编），保留页面、作者原话、已确认书籍、任务快照及扣量恢复。先核实当前实现，再以合成资料测试实际7种文字模型；不按56身份重复调用，不动图片节点。

目标：可追溯地展示各模型在开书节点的测试时间、耗时、结构结果及质量判断；只对通过完整节点验证的组合调整准入和默认顺序，不把通道连通或短探针等同上岗。更换模型不能继承另一模型成绩，开书成绩不自动开放全书/卷链章。现有可用组合继续服务。

验证与预算：现有订阅通道，禁止现金回退；同一合成资料，每模型设计与审查各一轮，共14次，最多2路并发、每次180秒，不自动重试未知结果。先观察再决定是否需要少量针对性复测。使用现有开书提示编译和输出解析，结构与人工内容审查分列；胜出组合再跑完整开书/审查/确认与失败恢复验证。工程检查仅覆盖实际修改的准入、名册、快照、后台展示及相应构建，不运行无关全量测试。

部署：完成的后台评测展示与已验证准入按安全窗口发布，保留代码回滚；不删除作者数据、不更换整库、不让新候选未经验证自动接单。当前状态：核查与准备评测中。

实施结果（待发布）：14次首轮与2次针对性复测完成。DeepSeek Pro设计64秒、Kimi K3审查23秒；Flash审查8秒，但设计目录校验失败；Kimi 2.7设计68秒，审查先400，兼容修正后仍无可见文字；GLM 5.2设计目录错误，审查修正400后仍空结果；GLM 5.3设计180秒未返回、审查105秒空结果；豆包设计132秒、审查18秒但建议含目录外标签。人工阅读发现Pro审查误要求填写废弃空位，不推荐作审查首选。已在共享目录保留每节点独立证据，后台总表和当前绑定成员卡展示；不继承另一模型或另一节点成绩，不自动开放33位新增成员。

必要修复：短结构审查不能统一发送thinking.disabled，GLM 5.2/Kimi 2.7省略该不支持字段；复测400已消除，但空结果仍明确未通过，不扩大预算反复烧量。另发现适配器在收到响应头时就取消计时器及上游Abort监听，已改为响应正文读取完毕才清理，保留结果未知/禁止盲目重发语义。相关适配器32项、后台3项、成员绑定4项、依赖闭包4项通过；API编译和后台类型/构建、390/1440真实浏览器通过。未调用图片模型、未修改全局岗位名册或作者数据。下一步以现有Pro设计/Kimi审查验证独立整条开书及确认，再发布展示与适配修复；候选逐节点完整准入仍不能以本次单题成绩代替。
