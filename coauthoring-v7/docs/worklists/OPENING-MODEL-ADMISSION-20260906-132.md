# 第132批：开书节点模型评测与准入

授权：用户要求按已讨论顺序开工，复用跑通功能，完成测试后上线。本批聚焦开书设计（策划编剧）和现有开书审查（主编），保留页面、作者原话、已确认书籍、任务快照及扣量恢复。先核实当前实现，再以合成资料测试实际7种文字模型；不按56身份重复调用，不动图片节点。

目标：可追溯地展示各模型在开书节点的测试时间、耗时、结构结果及质量判断；只对通过完整节点验证的组合调整准入和默认顺序，不把通道连通或短探针等同上岗。更换模型不能继承另一模型成绩，开书成绩不自动开放全书/卷链章。现有可用组合继续服务。

验证与预算：现有订阅通道，禁止现金回退；同一合成资料，每模型设计与审查各一轮，共14次，最多2路并发、每次180秒，不自动重试未知结果。先观察再决定是否需要少量针对性复测。使用现有开书提示编译和输出解析，结构与人工内容审查分列；胜出组合再跑完整开书/审查/确认与失败恢复验证。工程检查仅覆盖实际修改的准入、名册、快照、后台展示及相应构建，不运行无关全量测试。

部署：完成的后台评测展示与已验证准入按安全窗口发布，保留代码回滚；不删除作者数据、不更换整库、不让新候选未经验证自动接单。当前状态：2026-09-06 23:25:41北京时间已发布；候选准入仍待完整节点接入验证。

实施结果（已发布）：14次首轮与2次针对性复测完成。DeepSeek Pro设计64秒、Kimi K3审查23秒；Flash审查8秒，但设计目录校验失败；Kimi 2.7设计68秒，审查先400，兼容修正后仍无可见文字；GLM 5.2设计目录错误，审查修正400后仍空结果；GLM 5.3设计180秒未返回、审查105秒空结果；豆包设计132秒、审查18秒但建议含目录外标签。人工阅读发现Pro审查误要求填写废弃空位，不推荐作审查首选。已在共享目录保留每节点独立证据，后台总表和当前绑定成员卡展示；不继承另一模型或另一节点成绩，不自动开放33位新增成员。

必要修复：短结构审查不能统一发送thinking.disabled，GLM 5.2/Kimi 2.7省略该不支持字段；复测400已消除，但空结果仍明确未通过，不扩大预算反复烧量。另发现适配器在收到响应头时就取消计时器及上游Abort监听，已改为响应正文读取完毕才清理，保留结果未知/禁止盲目重发语义。相关适配器32项、后台3项、成员绑定4项、依赖闭包4项通过；API编译和后台类型/构建、390/1440真实浏览器通过。未调用图片模型、未修改全局岗位名册或作者数据。现有Pro设计/Kimi审查完整流程实测89秒通过；候选逐节点完整准入仍不能以本次单题成绩代替。

最终交付：代码9c08baef，API/Worker wm-v7-20260906-232000-9c08baef，静态2c452232f3966bcaceac。完整真实开书调用Pro 84332ms、Kimi K3 4285ms，任务总89038ms；确认幂等、资料读取和真实token扣量核对通过。合成修改/审查/确认通过；初次预检因旧测试脚本改成4字书名违反现行6字最小要求失败，修正为合法测试书名后通过，没有放宽业务校验。全量生产副本以真实服务账号启动通过，岗位历史提示未变；无迁移。发布前新包严格资源校验、保留旧资源分别比对哈希、22:11完整备份三项SHA复核与5GiB空间门槛通过；在途连续30秒零并立即复核后切换API再Worker。新进程心跳、公网健康/401、双站入口及后台JS/头像哈希、390/1440线上登录入口通过；错误级服务日志无新增。保留R131代码回滚，不恢复或修改作者数据库。

证据：本地artifacts/rebuild/132（浏览器、编译、源码闭包）、artifacts/rebuild/132-results.json与132-review-retest.json（合成模型可见输出，不含思维链）；生产本次release根目录startup.json、synthetic-fixed.log、real-opening.log、public-checks.json、deployment-passed。下一批先完成Flash审查/Kimi 2.7设计的完整节点接入及准入，再推进全书和卷节点；本批不宣称所有岗位成员已正常接单或PG整站迁移完成。
