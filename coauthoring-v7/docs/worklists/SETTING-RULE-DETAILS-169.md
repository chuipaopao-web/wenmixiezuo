# R169 设定辅助信息收起

最终状态：静态55c1974579de4a36c5b3已上线。服务器独立构建同哈希，公网12资源/后台根入口/健康/401通过，API和Worker进程未重启。发布证据/opt/wenmi-releases/wm-web-r169-c4756e86/public-checks.json、deployment-passed，完整源码在source。后台最终状态文档单独原子发布。

授权：按已确认方案，将作者不必常看的适用范围及关联对象收进规则详情；影响理解的条件、代价、例外继续可见。当前statement不保证包含这些字段，因此不直接隐藏必要限制，也不以字符串匹配充当语义去重。无AI数据、存储、生成或任务接口变更。
实施：每条规则正文后保留条件/代价/例外，适用/涉及放入默认关闭的原生details；无辅助信息不显示空入口。保持键盘操作、手机/桌面布局、编辑和任务恢复。
验证：受影响组件、类型/静态构建及390/1440展开收起。仅发布静态页面及后台说明，不重启服务、不调用模型。
状态：检查通过，准备静态上线。24项设定组件、Web类型/组合静态构建通过；390/1440浏览器验证默认关闭、展开适用/涉及、键盘Enter收起、条件例外可见、无空详情入口/横向溢出或页面异常，未提交写请求。静态55c1974579de4a36c5b3；截图与证据artifacts/rebuild/169/。
