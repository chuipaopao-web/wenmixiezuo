# 第129批：成员直接展示与实际模型核验

授权：用户要求选择成员不折叠，直接显示职位、姓名、头像；核查各实际模型，慢或有问题的成员停止工作。主代理直接实施，当前RB-20暂缓。

范围：生产与rebuild的新建小说成员选择保持同样展示，不改任务语义。先读取线上实际治理与近期调用统计，再按实际provider/model去重做合成任务探针，核查结果结构、来源约束和耗时。同模型的岗位需要确认映射与可用性，但不重复相同连通测试。上一批81秒实测仅覆盖DeepSeek Pro与Kimi K3，不能代表全部成员。

策略：真实探针使用既有严格套餐、现有凭据、合成输入，不发送作者正文、不新增现金回退。最多两并发，单次有时限；明确故障、无效输出或显著慢于同任务其他型号者复测，结合历史证据决定是否从新任务派单停用。保留每个必要岗位的可用替代与异模型审查，使用现有治理服务进行版本化更新，保留当前任务冻结快照，不强制停止作者在途任务。没有证据不宣称全部岗位文学质量通过，也不按品牌名称推断模型数量。

验收：受影响组件测试、类型/构建、390/1440浏览器、实际模型记录与治理变更前后证据。页面单独静态发布；下述实际发现要求补充后端兼容发布，配置变更用既有治理入口和审计。状态：进行中。

实现：选择区由details改为常显section，头像/姓名/岗位同时显示，保留单选、自动安排和原有任务提交。已停岗的草稿选择在成功读取名册后归零，不影响想法或已有任务。生产与独立工作区同步。

实测（2026-09-06，生产同一套餐与适配器，合成输入，最大两并发）：

| 实际模型 | 短设计 | 短审查 | 补充验证与决定 |
| --- | --- | --- | --- |
| DeepSeek V4 Pro | 6.1秒 | 2.6秒 | 保留；128批完整开书设计已实测 |
| DeepSeek V4 Flash | 7.1秒 | 3.3秒 | 正文实测5.8秒；保留写作岗位 |
| GLM 5.3 | 76.1秒 | 119.3秒，无可用文字 | 更简单的审查12.5秒成功；近3日规划22次中位539秒、创作8次中位490秒，先停6位成员新分配 |
| Kimi K2.7 Code | 400参数错误 | 15.4秒 | 实际novel_writer调用24.8秒正常；短规划错误来自disabled思考参数不兼容，当前仅写作岗位，保留，不扩展到短规划 |
| Kimi K3 | 8.8秒 | 5.2秒 | 保留；128批完整开书独立审查已实测 |
| 豆包 Seed 2.1 Turbo | 6.6秒 | 50.6秒 | 保留目前策划/写作岗位，不新增审查岗位 |

共12次基础探针+3次针对性复测，无现金回退。当前23位成员绑定6个文本模型及1个绘图模型；GLM 5.2是退役映射，不是当前第二个GLM。绘图模型未纳入文本探针。

证据边界：基础探针检查结构、来源编号和关键约束，不等于文学质量全过。Flash首次规划将未知结余推算为140石，属于无依据推断；补充明确未知量的正文复测未复现，保留写作且不提升为审查/资料岗位。部分模型的修订建议引入额外情节，不能直接当正式事实。历史任务输入/阶段不同，仅作为慢与失败风险佐证；每个岗位所有操作、完整长篇连续性未在本批逐一重测。

验证：原AuthorApp页面测试55项未改场景通过；原选择测试调整后，常显选择与停岗草稿恢复2例通过；独立工程选择/刷新/提交测试通过；生产类型与构建通过，390/1440浏览器无横向溢出及控制台错误。治理变更先在仅复制治理表结构/配置/索引的内存库演练；先将默认副编转给妙玉，再逐个停用GLM，保持每岗唯一默认及异模型审查替代。部署及配置最终结果待回填。

本地合成证据：`artifacts/rebuild/129/`；生产模型结果`/tmp/r129-model-audit.json`、`/tmp/r129-retest.json`，未记录模型思维链或作者正文。配置通过现有V7AgentGovernanceRepository事务与审计，操作人标识codex-authorized-r129，不冒充管理员登录。

发布前发现并纳入必要依赖：规划名册强制三名主编，名册校验强制每岗三人；直接停GLM会阻断规划。调整为至少两名可交接成员，已有三个候选槽按可用成员轮换，不把同成员不同候选冒充异模型复核。独立审查仍排除写作者底模，旧任务冻结不改。停岗后的各运行名册、候选槽交接、停岗成员拒选、异模型审查和不足两主编拒绝测试通过；相关18项测试及后端编译通过。

页面代码9aee49cd已独立发布静态43cb9592b6ee8f46ee56，API550589/Worker550609未重启，公网资源与身份401检查通过。后端将以独立新release发布兼容修复；不迁移数据库，保留旧代码回滚，按连续30秒在途归零后逐服务切换，再完成治理停岗与最终状态。
