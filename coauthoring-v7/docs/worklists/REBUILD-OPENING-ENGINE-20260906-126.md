# 第126批：原开书执行器接入PG

授权：用户要求继续，沿用先查现有功能、兼容则复制复用、主代理直接实施的决定。

范围：原OpeningAgentEngine及必要依赖、原提示编译器保留；六个PG工具接口保存原始输入、任务状态、候选及源版本；通过125批连接模型，冻结成员/提示/调用参数，恢复不重复生成。126批不重写设计/审查语义，不改作者UI，不把原确认建书的审查/修订一致性约束省略。

验证：复用原引擎场景，新增PG原子候选提交、恢复/未知、成员与提示冻结、身份隔离及原模型协议贯通测试。修正期只跑相关测试；稳定后按跨服务恢复风险集中verify:full，不另派重复审查。真实模型/页面及生产迁移单列，替身网络不算真实文学验收。

发布边界：不切生产作者入口；原执行器可运行只是必要步骤，确认建书/旧账号权益和现有书兼容仍需闭环。先交付内部可运行路径并明确剩余项，不改生产作者数据或任务，不迁移生产数据库。

状态：内部路径本地验收通过，未部署。设计→审查→待作者确认/决定已贯通；作者确认建书、修订服务、HTTP和调度另需接入，整项RB-19仍开发中。

实现与差异：原执行器依赖闭包36文件中34文件与源内容一致（忽略换行）；planning-story-routes/progressive-planning-briefs仅抽取两项共享类型到planning-route-contracts，消除类型循环，运行实现不变。原总提示编译器仅调整导出入口。新增legacy-workflow六个PG工具接口和原模型执行连接、0011候选/提示表及任务状态列；成员/提示/温度/版本在创建时冻结。既有任务与模型服务增加同一有效租约内连续执行支持，默认调用行为保持。

发现并修正：原引擎在commitCandidate前saveTask记录解析成功，中断会重新生成。PG适配将尚无候选的成功中间态持久化为待调和，完整模型回执已保存；候选与下一阶段同事务提交。注入真实数据库提交异常后恢复，只完成原设计提交和后续审查，没有第二次设计请求。供应商结果未知时保留候选和用量预留，可信晚到回执完成后继续；没有可信回执不自动重发。

验证证据：原引擎场景原样复用（仅测试导入路径调整）；6项新增PG联测覆盖冻结成员与异模型审查、未知与晚到回执、候选事务中断、格式修复、作者决定和身份/失效租约。目标组合13项通过。首次集中验证在测试前发现旧依赖的类型循环，抽取类型后集中执行通过：148后端/基础+222作者=370项（新增7个测试条目，其余复用既有），169源码边界、类型和双Web构建通过。没有另派代理审查或另造独立复验脚本。测试日志：`.local/rebuild/test126.log`、`.local/rebuild/verify126.log`；后者测试约87秒。仅合成测试库应用0011；本地dev与生产未迁移，没有真实模型花费或作者数据操作。工程测试不代表真实模型文学效果或页面上线验收。

交接：项目执行路线1.24/规格1.9/HANDOFF已更新，线上后台路线仍123（未发布文档）。复用代码位于rebuild独立目录，不运行时导入旧工作区。下一步沿用原确认/修订和建书来源规则，完成作者接口与调度；真实身份/权益/旧书兼容验证前保持生产入口不切换。
