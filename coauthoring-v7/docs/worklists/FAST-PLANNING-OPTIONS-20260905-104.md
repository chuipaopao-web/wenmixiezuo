# 第104批：方案速度、数量与成员状态

状态：速度策略、计数与状态修复通过并已上线；真实作者三套重跑未验证，来源语义质量未通过。唯一任务清单。

## 变更合同

- 目标：查明长等待与方案数量；交互式卷/链方案使用经真实探针验证的快速模型，真实展示交接和交付。
- 事实：生产任务 fed73c17-8f6f-40ba-b95d-74ed8332c83a 总耗时24分11秒，保存 requestedCandidateCount=1、方案1份；Kimi K3方案调用878秒，DeepSeek Pro原调用281秒加修复95秒。截图不能证明本次提交了3份。页面数量仅内存保存、重新挂载默认1。
- 范围：方案模型路由与直接输出参数、相关成员注册/兼容、数量选择与按钮、基于实际产物的状态；对应测试、发布和接管记录。模型替换以真实虚构资料探针结果为依据。
- 不改：作者内容、正式规划/正文/结算、15,000字资料上限、资料取舍策略、其他岗位模型、全产品重构和独立后台。保留现有风格、旧候选与任务读取/确认、成功检查点、选择1/2/3套及独立结果、幂等和结果未知保护。
- 验收：快速路线能产出通过现有合同且遵守资料边界的完整方案；选择3套提交3套，刷新保留并明确显示数量；未交付成员不能声称已交付；旧成员历史候选仍可确认，新增成员不误判旧绑定停用。失败只有限修复/交接，不盲目重发未知结果。
- 验证：受影响模型适配、运行名单、创作服务、作者页面测试，API/Web类型检查与构建；虚构资料真实模型探针；390px/1440px真实浏览器。无数据迁移或核心流程重构，不触发全量。
- 部署：沿用用户长期授权自动安全发布；以生产R103组合为基线，仅加入本合同文件，不夹带本地后台重构。API/Worker切换前在途30秒为零，保留回滚，不操作作者任务制造窗口。

## 证据与待办

- 已查：生产调用时间、成员、计数；静态和治理名单；当前选项结构、后端并行席位及不足不完成判断。
- 实现、结构失败定位、模型探针、相关回归及安全上线已完成；真实作者重新生成结果不冒充已验证。
- 已确认选型：Flash 两次生成及一次修复均违背短链容量，排除。补清合同后 DeepSeek Pro 直接输出147.8秒、豆包153.4秒均通过18万字/64章虚构卷结构校验；Kimi K3/GLM不进入卷链方案队列。三套仍独立并行请求，可由同一真实成员承担多套，不冒充不同成员。取消旧“方案数必须等于不同成员数”的内部限制。
- 兼容直接依赖：新增成员不应使全部旧任务失效，绑定检查允许名单增项/优先级变化，仍拒绝旧成员删除、改岗或实际模型改变；并行结果未知立即保留检查点，不自动更换模型重发。
- design_review_id：R104-routing-budget。业务目的和旧数据库枚举保持不变；方案网关选择独立直接输出路由，冻结额度与真实适配器均不再追加思考额度；卷/链可见上限12,000。卷原8,000+4,000思考；链原5,000已在两种模型真实探针中发生截断，增加至12,000（小于用户允许的三倍）保证可见结果空间。结构修复最多一次，不追加思考烧穿重试。失败/未知、实际用量记账沿用原实现，不改历史账单，不引入新付费服务。
- 本地验证：API/Web类型检查及构建、22项创作集成（含旧绑定兼容、无效成功响应交接、三套/两成员及失败保留）、28项适配器、5项治理、29项后端合同、29项作者页通过。浏览器390/1440宽度真实执行选择三套、刷新、提交、停用模型隐藏；无横向溢出或页面错误，修复无头像时标签被28px头像列挤压。证据 artifacts/diagnostics/r104-browser。
- 真实模型证据仅覆盖虚构单卷结构与延迟，不声称实际作者任务恢复、全程固定耗时或长篇文学质量通过；阅读仍发现桥梁通行等语义细节需要进一步创作审查。作者确认源、原始正文、历史账单均未更改。生产发布验证已通过，具体作者三套生成仍未验证。
- 暂存验证追加发现：链5,000输出截断，因此未切换首个暂存包。12,000额度下同一虚构链DeepSeek 70.333秒/4,013 Token、豆包145.145秒/7,234 Token完整返回。豆包将emotion.intensity写为数字7，按原值转换为字符串7后现有合同通过，缺失仍失败；新增数值0/7及缺失回归，避免此类格式问题触发额外AI修复。来源语义阅读还发现DeepSeek把木梁承诺主体写反，明确不将本批作为文学质量通过证明。
- 服务器首个暂存包113测试、闭包/模块、107迁移一致、备份与隔离启动通过；测试脚本经PowerShell管道附加的末尾CR造成包装退出码1，三组实际日志均通过，最终发布改用文件上传执行。首包未上线，最终包需包含链额度与数字强度修复。

## 最终发布与验收

- 通过：最终生产组合113测试（API55、后端29、作者页29）、闭包与模块解析、107迁移哈希一致及备份副本两次no-op、隔离启动；最终包包含链12,000额度与数字强度修复。产品提交`a2203a3`、`ce17325`，生产R103加本批18文件，不含本地后台96—98批。
- 已上线：北京时间2026-09-06 00:42:16—00:42:18，`wm-v7-20260906-003247-ce17325`，静态`232b47f9388f3bbcb262`；最终归档SHA256 `af592061506bdb9190d51839e06329e72c590081eca748a9782b33e19fa821aa`。连续30秒零在途及停止前复核，不取消作者任务。备份`20260905T163407Z-531320`。
- 发布后通过：14项公开资源状态与哈希、健康和Worker就绪、未登录作者/后台401、107迁移、SQLite完整性/外键、三个服务均active；真实原任务仍为请求1套/保存1套，历史绑定有效，当前快速名单为DeepSeek V4 Pro和Doubao Seed2.1 Turbo。没有代作者提交新任务。
- 回滚：预制`/opt/wenmi-releases/wm-v7-20260905-231552-0a69443-r104-rollback/source`及旧静态`361502891fa73cbb45eb`；仅让R103未知成员检查忽略本批新增`planner-doubao-turbo`，保留该行且旧版仍只读取22个旧成员，其他未知成员仍拒绝。新旧仓储在备份副本兼容验证通过，旧治理5测试通过。补丁见[回滚兼容补丁](../../../artifacts/deploy/r104-rollback-compat.patch)。回滚归档SHA256 `4b2dfc6ab7041dea5dd335291bb439503eaf889f0766e254bbc62c45d5f81bca`；不恢复生产数据库。
- 未验证：真实作者再次选择3套后的实际交付和全流程耗时。未通过：虚构探针的全部来源语义质量（桥梁通行、木梁承诺主体），不能将本批结构通过解释为文学质量通过；此问题保留为后续生成审查/重构输入。
