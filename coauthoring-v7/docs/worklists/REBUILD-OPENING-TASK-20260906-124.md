# 第124批：开书任务与恢复

用户要求本批由主代理直接开发，对比实际成本，不再委派和重复独立验收。

目标：将真实账号的开书请求和总用量预留同事务保存，提供领取、进度保存、有限恢复、取消及未知调用防重发。复用账号、用量及JSON校验，不重写UI或模型适配器。原始想法/成员偏好保留；开书前不伪造book_id。

范围：rebuild新增任务服务与加法迁移，相关测试、执行计划和交接。仅内部服务，不开放未接执行器的作者入口；模型调用、方案确认建书、旧身份/权益迁移随后接入。当前生产保持原服务。本批未满足作者入口发布依赖，不部署独立PG；不得称团队开书已上线。

验证：开发中只跑新增任务测试及类型检查；涉及租约恢复和用量事务，按ACCEPTANCE高风险行，稳定后统一跑一次verify:full。已有套件直接复用，不另造重复的9组验收脚本。使用独立合成测试库，不操作生产及作者资料。

状态：内部服务本地验收通过；完整AI开书仍未接通，未部署。

实现：新增0010两张表，OpeningTaskService复用已有账号/用量/JSON服务。全任务一次预留；调用回执留档、成功步骤形成检查点，跨步骤/重试共用总预算与deadline，旧租约不再发起调用；可能发出的调用等待核对，迟到回执可保存，暂停账号不能发起新调用但允许记录已执行证据。原始输入、预算与已存回执受数据库触发器保护。

验证：`.local/rebuild/run124.mjs exec -- vitest run tests/postgres/opening-tasks.test.ts`最终11项通过。稳定后仅一次`run verify:full`通过，日志`artifacts/rebuild/124/verify.log`：107后端/基础+222作者（原318+新增11）；测试运行48.41+23.02秒；类型、118源码边界和双Web构建通过，作者既有500KB块警告保留。空白合成库`wenmi_rebuild_test_review124_1ad84ad82b`安装及重复迁移通过，没有修改已合并迁移/迁移历史。没有另建9组复验脚本，也未委派其他模型。

本轮修正：规范化JSON返回无原型对象，不能再次传入要求普通对象的校验器；改为原请求只规范化一次用于摘要，传原始普通对象给用量服务。测试重复执行触发共用IP限流，改用合成独立地址隔离测试，不降低产品限流。检查补上暂停账号迟到回执保存、执行但零token仍按结算而非未执行释放。

证据覆盖：重复点击/不同内容冲突、账号隔离、保存设计后新服务实例接续、同调用重复回执不重复计量、有效租约排他与过期接管、未知不重发及迟到核对、总预算不足、有限退避、排队取消/调用中取消、超额证据保留、明确未执行证据、撤销会话/停用账号、任务插入及结算事件故障的真实事务回滚、原文及回执不可覆盖。

限制与下一步：这是供执行器使用的内部服务，未接HTTP/调度器/供应商适配器、语义审查、方案确认建书；结果来自合成测试，不能作为真实模型质量或作者界面证据。内部提供方回执与预算参数不得直接接受作者HTTP输入。未知且供应商无法核对、超总预算的处理仍需后续核对入口。保持现有生产开书；下一批先复用现有开书执行与提示合同适配这些事务边界，不重写成熟UI。不因内部服务验收通过就迁移作者账号、权益或旧书。

交付：项目路线1.22已更新；线上地图仍为第123批。本轮没有生产或本地dev迁移、没有调用真实模型，不能声称已上线或已比较得到固定额度节省率。
