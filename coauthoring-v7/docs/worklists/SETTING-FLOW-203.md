# R203 设定确认到故事线的持久衔接

已授权修复：设计完成不自动越过作者确认；“确认设定并请主编整理”持久创建版本绑定的统一整理任务。主编修改后的候选仍由作者保存，不能静默批准变更。正式保存与后续资料/故事线派工意图同一事务记录；后台消费，页面关闭或重启不丢。沿用资料短卡、自校、独立核对、故事线推荐执行器，不修改作者现有书。

版本变化作废未启动旧派工；归档书不执行；同版本重复消费或页面启动仅一轮；失败保留记录，不无限调用。保持异模型审查排除。验证未确认不启动、保存冲突无派工、成功保存有派工、重复消费防重、版本改变不发旧资料、跨书隔离和页面入口。

R203已上线：wm-v7-20260913-013000-a2030001，静态82f5df5bb15ba35e4a38。增量0121新增派工表和索引，数据库quick_check正常。线上本书24项candidate，没有自动认可作者设定或重新生成。前端40项通过，后台54项分批回归通过（47设定既有场景、2旧长草案改为明确确认、3派工、2路由安全）；API类型及服务端构建通过。测试使用模拟模型，不等于真实文学质量、速度验收。

R204追加分支：先逐项确认、再统一整理且无改动时，也在整理提交事务内记录派工；7项定向回归通过。版本状态见HANDOFF。已存旧任务保留旧顺序快照，不取消、不改写作者历史。

发布偏差如实记录：R203切换脚本未内置迁移前备份，临时备份命令因PowerShell引号失败；最终保留的是迁移后的受限权限一致性备份post-deploy.sqlite，不能称为迁移前备份。0121仅新增表/索引，没有覆盖已有表数据。后续含迁移切换必须把备份成功作为脚本内部前置条件，不能临时并行补做。R204没有新增迁移。
