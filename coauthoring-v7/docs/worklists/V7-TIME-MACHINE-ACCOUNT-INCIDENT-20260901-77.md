# V7 指定账号时光机线上故障修复（第77批）

任务编号：V7-TIME-MACHINE-ACCOUNT-INCIDENT-20260901-77

本次唯一清单：`coauthoring-v7/docs/worklists/V7-TIME-MACHINE-ACCOUNT-INCIDENT-20260901-77.md`

任务类型：生产事故诊断、指定账号恢复、公共根因修复、回归与部署

当前问题：生产账号 `1248307030@qq.com` 反馈时光机仍不可用；第76批已经部署，但该账号真实链路尚未闭环验证，不能以发布成功代替故障修复。

复现步骤：按已验证账号身份定位该作者的活动书籍；对账全书、卷、链、章纲、不可变正文、结算、时光机进度、规划任务与模型调用；结合生产 API/Worker 日志和真实页面确定失败位置；在隔离环境复现同一状态组合。

期望结果：该账号可进入每本活动书籍的时光机并看到与正式内容一致的真实进度；历史失败或损坏任务不能覆盖正式成果；暂时性读取失败可刷新恢复，确有未完成工作时提供一个准确继续动作；不重复生成或扣量。

允许修改：V7 时光机作者端页面及直接 API 客户端；时光机进度、正式规划和规划任务的读取投影、仓储与路由；直接相关测试、发布号、交接和本工单。若生产存在无有效调用、无候选、无活动租约且阻断页面的残留任务，只允许在完整备份、精确影响预览和事务后做可恢复归档或状态调和。

明确不改：不修改该作者或其他作者的账号密码、角色、会员套餐、额度、金额和扣量账本；不改写或删除书籍、作者原话、确认规划、章纲、不可变正文、结算、正式化记录、模型调用审计或已合并迁移；不恢复 V6 页面、工作流或旧任务语义；不处理会员后台入口等相邻问题。

必须保留：会话身份和 `owner_id + book_id` 隔离；正式内容、不可变正文和历史版本；候选与正式内容边界；未知模型结果不重发；活动租约和有效待作者确认任务不清理；当前第76批生产版本、静态版本和数据库回滚点。

新功能验收：

- 指定账号所有活动书籍的时光机状态与正式规划、正文实际和任务事实一致。
- 已有正式成果时不显示整书未完成或让历史失败任务覆盖成果。
- 读取失败、未完成、完成和恢复动作均来自真实后端状态；刷新或重复进入不创建任务、不扣量。
- 生产只允许精确、可逆的状态调和，前后对账证明作者正式数据字节与版本不变。

原功能回归：其他账号与书籍隔离；全书方向、设定、卷、链、章、正文和任务中心读取；时光机桌面与 390×844；规划失败/未知/成功恢复语义保持正确。

测试范围：生产只读账号、书籍、任务、调用与日志对账；受影响仓储/API/作者端测试；API与作者端类型检查及构建；隔离数据复现；指定账号线上页面或同身份 API 验证。若根因改变核心工作流状态解释、账号隔离或跨服务恢复，则在清单记录触发事实后运行 `npm run verify:full`。

部署要求：修复与合同内回归通过后直接部署。API/Worker/迁移发布必须先备份，连续30秒确认在途任务为零并即时复核后逐服务切换；纯静态修复原子切换入口且保留旧哈希。部署后核对版本、健康、指定账号数据和真实页面；失败立即回滚。

design_review_id：DR-V7-TIME-MACHINE-ACCOUNT-77（正式内容事实投影、账号隔离与生产状态调和）

## 用户端三句话

1. 作者此刻要看清自己的书已经完成到哪里，并从正确位置继续。
2. 当前最大障碍是时光机读取或投影失败，让真实成果被错误状态遮住。
3. 页面必须显示正式成果、准确进度和一个可执行动作；任务码、模型名、堆栈和内部键只留在后台。

## 最小交互合同

- 页面唯一主任务是展示真实创作进度并允许从正确节点继续。
- 有正式成果时先展示成果；后台局部读取失败不能清空上次有效页面或降级为整书未完成。
- 真实未完成时只突出一个“继续未完成步骤”；暂时性服务失败只突出“重新读取”。
- 不新增弹窗，不移动无关导航；桌面和手机都保证错误、成果和主操作可达。

## 信息职责与正式来源

| 节点 | 负责者 | 本批合同 |
| --- | --- | --- |
| 身份、书籍归属、版本、任务状态、排序与恢复位置 | 系统 | 以 `owner_id + book_id` 和数据库事实确定，不调用模型猜测 |
| 全书、卷、链、章纲和正文内容 | 当前 V7 Agent 与作者正式化流程 | 本批只读并保护，不生成、不改写 |
| 作者可见失败与恢复动作 | 系统投影 | 将确定性状态转换为准确大白话，不泄漏技术字段 |
| 模型未知或失败调和 | 现有任务恢复机制 | 未知结果只核对，明确失败才允许安全恢复，不盲目重发 |

正式来源优先级：不可变正文和作者确认内容 > 当前活动正式规划 > 当前有效候选 > 历史任务与失败日志。历史任务只能解释运行过程，不能覆盖已存在的正式事实。

## 需求—实现—验收—证据

| 编号 | 对应 | 内容 | 状态 |
| --- | --- | --- | --- |
| REQ-01 | 老板原话 | 查明 `1248307030@qq.com` 时光机仍不可用的真实原因 | 通过 |
| REQ-02 | 老板原话 | 修复该账号及同根因公共路径并重新部署 | 通过 |
| IMP-01 | REQ-01 | 生产账号、书籍、正式内容、任务、调用与日志对账 | 通过 |
| IMP-02 | REQ-02 | 在现有 V7 时光机闭环做最小代码或状态修复 | 通过 |
| IMP-03 | REQ-02 | 相称测试、差异审计、提交、部署和线上复验 | 通过 |
| ACC-01 | REQ-01 | 根因有生产证据且不把业务状态误判为账号损坏 | 通过 |
| ACC-02 | REQ-02 | 指定账号时光机可读，正式成果与版本零改写 | 部分通过：修复已上线，待作者登录后重试真实模型 |
| ACC-03 | REQ-02 | 其他账号隔离、失败/未知恢复和任务幂等回归通过 | 通过 |
| EVD-01 | ACC-01 | 去敏生产查询、服务日志和发布版本证据 | 通过 |
| EVD-02 | ACC-02 | 隔离复现、专项测试和指定账号线上验证 | 部分通过：数据与恢复入口已验，未冒充作者发任务 |
| EVD-03 | ACC-03 | 类型检查、构建、差异审计、部署与回滚证据 | 通过 |

## 复用审计

- 优先复用第76批现有时光机进度端点、正式规划读取、规划任务恢复服务和作者端页面，不建立第二套时光机或状态机。
- 检查当前生产发布包与提交 `0fca168` / `f25740c`，确认不是静态或服务版本未切换。
- 检查相关 Git 历史与现有专项测试，找出未覆盖的真实数据组合；只有当前投影不能表达该组合时才做最小修改。
- 不引入第三方依赖，不读取或恢复 V6 产品代码为运行依赖。

## 阶段证据

- 生产应用与静态资源确为第76批：应用 `wm-v7-20260901-153007-bb16a6ec`、静态 `6fba50a787f23722fcaf`；API、Worker、Caddy active，数据库106项迁移、完整性和外键检查正常。故障不是部署遗漏。
- 指定账号 active，包含一本当前 V7 活动书和一本未进入 V7 规划表的历史书；本批只处理当前 V7 活动书，不恢复旧书工作流。
- 当前 V7 活动书的全书路线任务已 `completed / route_confirmed`，但没有正式规划树头；15次全书树生成均失败，最近失败仍无候选树。
- 生产模型输出证明两个资料策划成员都选择了本书真实设定的稳定条目键，例如 `world-layer`、`history`；当前数据库合法引用却是对应不可变设定版本 UUID。第三席多次因输出预算耗尽未形成有效 JSON。
- 代码根因：`planningMethodSearchPrompt` 明确要求返回逐项事实源的 `sourceId`，但全书树路径使用 `publicSnapshot()` 删除了 `sourceKind/sourceId/sourceVersion`，Agent不可能返回系统要求的精确引用；路线任务传入完整快照，因此路线成功而树连续失败。
- 复用决定：保留现有资料策划 Agent、严格来源验证、任务冻结与重试；只为资料策划输入增加任务所需的精确来源引用，主设计成员仍使用去内部字段的公开快照。不新增状态机、数据库迁移或第三方依赖。
- 兼容决定：旧任务已经成功返回的 `itemKey` 只在当前冻结快照的正式逐项设定事实内做唯一一对一 `itemKey → sourceId` 归一；重复键、未知值和跨书值仍由原严格校验拒绝，不做语义猜测。
- 生产形状回归主动删除已冻结任务中的 `contextPlan`，保留成功的资料策划模型调用，再续跑同一任务；服务复用旧输出、恢复规范化资料计划，资料策划模型调用数没有增加，证明不会因本次兼容重复扣量。
- 本地专项结果：规划编辑部集成 15/15、规划方法检索 7/7、时光机/规划树领域与体验 34/34、作者端时光机 30/30；API、测试和作者端 TypeScript 检查通过；API 在独立输出目录构建通过并核到新 helper。既有 `apps/api/dist` 由本机 ACL 保护导致原位写入被拒，未改权限、未覆盖旧产物，改用隔离构建目录完成同一编译门禁。
- 本批没有数据库迁移、数据形状、权限、账号隔离或不可变正文语义变化，本地不触发 `verify:full`；生产统一发布编排仍会在不可见暂存目录执行冻结的完整验证。
- 新发布号冻结为 `wm-v7-20260901-203016-69bf9d6d`；前端源码未变，部署以冻结源码真实构建的静态哈希做原子切换，不能为了制造静态编号改动页面。
- 实际构建证明 `RELEASE_ID` 会进入前端产物，因此静态版本真实变化为 `e33dd243118bc63140c5`，manifest 为 `0b062fcddc2a04df6f728fa4ab5d757e5d69b404e352c1eaa4d017095a504559`；部署改为新静态原子切换，没有为制造哈希修改前端源码。
- 部署前为满足10GiB暂存门禁，精确确认并删除未被 apps/current/previous/RELEASE_ID 或回滚点引用的旧构建目录 `/opt/wenmi-releases/wm-v7-20260901-015500-d14dc64c`，释放约1.6GiB；目录可由Git历史重建，作者数据和备份未触碰。
- 三次预切换停止均发生在生产指针变化前：首次为上传包仅admin可读，第二次为静态哈希预判错误，第三次为真实作者任务在途。每次都先确认原服务和指针不变，再精确清除未启用暂存；最终脚本改为最长等待30分钟并要求连续30秒零在途，不取消作者任务。
- 完整备份 `20260901T123453Z-r77` 已发布，数据库与3个登记作者文件校验通过；最终于 `2026-09-01T13:21:43Z` 部署成功。生产应用为 `wm-v7-20260901-203016-69bf9d6d`，静态为 `e33dd243118bc63140c5`，previous为 `6fba50a787f23722fcaf`，106项迁移保持不变。
- 线上复验：API/Worker/Caddy active，公网health为新发布且Worker ready，数据库 quick_check=ok、外键0、错误级日志0，未登录时光机/设定/会员接口均401；编译产物同时含精确来源快照和唯一标识归一逻辑。
- 指定账号仍为active、diamond有效至2026-09-15、额度200,000,000；两本书、已确认全书路线、15次历史失败任务和最新任务 `e3637679-22b0-4fa4-a875-fbd010bf303e` 均未被部署改写，working规划调用为0。当前仍没有正式树头；作者下一次在登录页面点击现有恢复动作时才会复用旧成功资料策划输出并继续生成，本批没有绕过会话冒充作者或自动扣量。
