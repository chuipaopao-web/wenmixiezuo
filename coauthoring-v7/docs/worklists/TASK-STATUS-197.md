# R197 新时光机任务状态接入

范围：作者任务状态漏掉tm2任务、书内状态混入其他书。新增只读任务投影到现有规划任务列表，真实阶段与执行成员、最新方案槽位、采用/待决定/失败状态；不提供未实现的停止操作、不伪造百分比。归档书排除；作者书内按bookId筛选，全局仍可看本账号。保持原页面风格及其他任务操作。

验证：任务投影隔离/归档/状态转换、书内筛选/跳转/显示、API及作者类型构建、手机桌面走查。仅代码/静态资源部署，不上传此前被拒绝的完整路线文档；连续零在途窗口后API发布，静态独立切换。

状态：已上线（实现提交6d7d507a）。API/Worker：wm-v7-20260912-224500-6d7d507a；静态：88cb4b0c78479876f3d3。

验证结果：领域18项、状态组件10项、规划运行定向回归1项通过；API/作者类型及构建通过，13项管理档案核对通过。390px无横向溢出，任务卡片与跳回时光机交互通过；1440px DOM宽度检查通过。线上14个资源哈希、4处未登录鉴权、后台入口和API/Worker健康通过。真实《仙门售后我无敌》返回本书故事线任务；归档旧书排除。未冒充完成真实模型质量验收。

发布事故及处理：首次脚本误用r197作版本尾码，不满足运行时7—8位十六进制校验，导致API启动失败；自动回滚恢复了旧代码，但重启次数限制未清除，服务未恢复。续接发现后立即reset-failed恢复旧API/Worker，随后加入实际readReleaseId预检、回滚清除失败计数，以合法版本号重新发布成功。没有修改、恢复数据库，没有取消任务；必须如实保留此次发布缺陷。

本书独立生成故障（尚未修复）：2026-09-12 22:28:58在card:0失败，temporary/technical_failure/http-200/usage-known。DeepSeek v4 pro两次调用输入包5914字符，各输出计数10000tokens，约150/164秒。已知成功HTTP但无完整可提交结果；现有诊断不能区分空结果与输出截断，不应归因为输入过长。下一批核对资料整理任务的思考/可见输出预算及错误分类，用相称真实探针验证后再修复，不靠重复点击或盲目增大上下文解决。

文档发布：仅对服务器既有文档替换一处代码校验值并保留回滚副本，未上传此前被拒绝的完整内部路线文档。当前本地工单为本批完整记录。
