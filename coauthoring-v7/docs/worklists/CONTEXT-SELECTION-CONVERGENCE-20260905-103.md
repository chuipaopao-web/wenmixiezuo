# 第103批：卷资料整理无法收敛

状态：修复进行中，尚未发布。

## 变更合同

- 目标：解决指定用户景阳长歌第一卷在第101批上线后再次资料整理失败；完整发送包仍不超过15000字符，必要约束不静默删除，原任务可继续。
- 证据：2026-09-05北京时间22:43:44创建任务f209baae-e5e9-4e63-b6b7-e912055edb2c，22:51:06失败；生产22:36已为第101批。生产机内只读使用原存档返回复现第5/8页6次失败，完整包18797/15000；第4页及第5页多次卡在冗余reconsiderIds清单校验，覆盖核对未执行。资料选择1次、阅读24次均返回成功，不是Token耗尽。
- 范围：资料阅读器、必要的上下文恢复直接依赖、受影响测试及部署/交接记录。不启动整体重构，不改UI、供应商、模型额度、数据库结构或作者内容。
- 保留：owner/book与来源版本、作者原文、正式设定/规划、正文与结算、成功结果、调用记录、15000字符硬上限、当前视觉风格；未知调用不重发、已完成检查点复用。
- 修复：移除要求模型重复计算旧标记差集的门槛，程序准确计算移除编号后由Agent做覆盖核对；预算失败反馈确切超额及上次选择，核对不通过返回具体缺失编号，有限修正且不放宽真实约束。根据回归/真实虚构探针结果决定必要的直接改进，不以增加次数替代解决根因。
- 风险：仍使用原文片段回填，正式来源/权威/格式不改变；覆盖核对由同配置模型承担，不冒充独立复核。只修局部校验及反馈，不改核心工作流/迁移/计费，按局部API与模型调用门禁验证，不默认全量。
- 验收：无reconsiderIds/错误冗余清单仍能进入真实覆盖核对；重复误标可删除、真正否定条件不能丢；超限反馈可纠正到预算内；拒绝/未知/格式错误有界；同恢复请求防重与成功页复用；卷链章/规划使用同阅读器的受影响回归；类型检查/构建、真实配置模型的虚构八页密集材料探针。
- 发布：第101批生产组合只叠加本批文件，排除本地后台96—98批；相称门禁后备份、迁移一致性、连续30秒零在途和即时复核，逐服务切换，保留回滚，公网验证。不得为了窗口停止作者任务。
- 作者数据：本次诊断只在生产机内读取存档并输出页码/字符/错误，不导出作品或用作品做外部探针；真实作者任务恢复须明确区分运行结果与合成验收，不声称其卷已完成。

## 进度与证据

- [x] 实时生产诊断与存档只读重建；工作区仅既有未跟踪.claude，保持不动。
- [x] 修复与反例测试：阅读器22、上下文编译器16、创作流水线21，共59项通过；原文、版本、预算、否定边界、未知不重发、相同恢复请求复用均回归。
- [x] API类型检查与隔离构建通过。密集全真实模型探针9页/11次调用，最终3691/15000字符，11条明确约束全部原文保留，包含末页披露边界。强制错误后真实恢复探针另8次调用：15970字符超限经修正及覆盖核对降为3745；故意漏掉安全条件被拒绝后恢复为3741。材料全部虚构。证据在artifacts/diagnostics/r103-dense-probe和r103-repair-probe。
- [x] 完整产品差异仅阅读器及对应测试；原文回填方式、投影结构和15000上限不变。无新增依赖、迁移、UI或全量门禁。
- [ ] 安全上线、公网复验、交接和本地提交。

验证过程中的夹具修正：多页单元测试初始只有7页，扩大为至少8页；安全反例初始材料未超预算而走原文直通，增加虚构背景长度后触发核对并通过。未放宽断言，未把未触发路径算成通过。
