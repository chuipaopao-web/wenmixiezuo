# 第176批：设定续作资料整理溢出修复

故障证据：管理员书籍v7-book-d62cea9732c42c6d1d84d7215c239cb4最近四次续作均在模型调用前失败，5项failed、0调用；原始资料选择提示估算23665 tokens超过20000工位上限。按钮已创建任务，并非点击事件丢失。

目标：按完整事实分页供成员选择，再合并入选资料；超出设计包时再次选择，不裁断条件、不改原版本。保留4主题批次、作者要求、正式与草案身份、原有模型和主编复核；失败提供真实资料准备原因。核查重启仅处理未完成项，不覆盖19项已保存内容。

验证：超预算规模、末页必要事实、空页、非法ID、无法缩减、输入不变、真实编译预算与设定闭环；后台路线同步。现有服务器安全窗口发布，保留回滚；上线后在用户授权书籍上续作未完成任务并观察结果。无删除和迁移。

状态：实现及回归通过，准备部署。51项原有/分页回归通过；追加20×3000字的真实编译与生成闭环通过（替换原740字案例为两种规模，共52项）。API类型检查通过；没有以模拟调用证明真实模型结果，真实续作待上线核验。
