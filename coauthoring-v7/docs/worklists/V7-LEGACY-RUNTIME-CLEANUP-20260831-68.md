# V7 旧运行边界复核与清理

任务编号：V7-CLEANUP-68

本次唯一清单：`coauthoring-v7/docs/worklists/V7-LEGACY-RUNTIME-CLEANUP-20260831-68.md`

任务类型：旧实现清理、运行边界收敛、发布基线复核

当前问题：上一轮 V7-only 清理在当前工作树留下约 407 项未结算状态，工单和交接却写成已经完成；旧成员专业人设 `agent-team-v2` 及其模型提示、旧团队初始化、旧岗位提示和模型白名单依赖仍在当前源码中；本地清理后 V7 与生产运行包尚不是同一版本，无法可靠判断当前 BUG 属于哪套运行链。

复现步骤：

1. `git status --short` 可见约 407 项状态，`git diff --stat` 显示约 390 个文件、68,000 余行删除。
2. `ModelAdapterFactory`、`BookOnboardingService` 和 `TeamTemplateService` 仍引用 `contracts/agent-team-v2.ts`。
3. `agent-team-v2.ts` 仍保存成员专业身份、专长、工作方法和旧岗位映射。
4. 本地 `RELEASE_ID` 与生产健康接口返回的运行版本不同。

期望结果：当前源码只保留 V7 作者端、V7 独立后台、共享安全/账号/会员/用量/备份底座和 V7 创作运行所需实现；成员只保留拟人化身份、可用模型和任务分工，不保留个人专业人设或岗位人设；题材融合人设只在具体书籍任务运行时生成和使用；当前清理差异可解释、可测试、可提交、可部署。

允许修改：上一轮清理已涉及的根配置、API、Worker、V7 后端、测试、当前交接和部署文档；旧成员人设、旧团队模板、旧岗位提示、旧模型绑定、旧路由/Worker/检索的直接依赖和只验证旧行为的测试；本清单。

明确不改：作者端产品结构和页面交互；资料策划 Agent、方法语义检索、ContextCompiler 预算和工作流重构；生产数据库和服务器业务数据；作者账号、书籍、正文、确认规划、人物事实、任务、用量与消费记录；API Key、密码、会话秘密；已合并迁移和历史表名；Git 历史对象。

必须保留：V7 开书、设定、时光机、卷、链、章纲、正文、审查、定稿、人物与故事记忆；题材融合档案及任务期题材人设；146 项方法、156 项情节模式、36 项配方；TaskContract、ContextPack、PromptManifest、模型快照、用量记账、失败恢复；账号和书籍隔离、正文不可变版本、备份恢复和管理员审计。

新功能验收：

- 旧成员专业人设定义、构建函数和有效运行引用为零。
- V7 模型调用不再从成员姓名、旧岗位或旧专业身份生成系统提示。
- 成员身份、模型绑定和任务责任仍能由 V7 治理正常读取。
- 题材融合人设及其书级工作档案仍保留，且没有被本批误删。
- 旧 API、旧 Worker、旧本地向量运行时和旧产品前端没有重新进入启动、构建或路由。
- 当前功能台账和交接不再用错误的“已提交/已部署”状态描述未结算工作树。

原功能回归：V7 API 路由、Worker 心跳/正式化、作者端和后台构建、开书确认、题材融合、规划、创作、人物记忆、提示治理、账号/会员/用量和生产只读健康检查保持可用。

测试范围：先执行零引用和依赖闭包扫描、受影响单元/集成测试、六个 workspace 类型检查与构建、102 个迁移空库重复执行、V7-only 启动验证；本批会改变核心运行入口并结算上一轮大规模清理，满足全量 `npm run verify:full` 触发条件，最终执行全量门禁。真实模型调用和产品工作流改造留到下一批，不在本批消耗模型额度。

部署要求：本批先形成干净可复现提交；通过全量门禁、部署前在途任务安全检查和回滚包验证后，再统一发布 API、Worker 和静态资源。不得通过清理或暂停作者任务制造部署窗口；遇到生产恢复、不可逆数据操作或无法保证在途任务安全时停止。

design_review_id：DR-V7-CLEANUP-68（Prompt/模型运行来源和核心启动闭包变化）

## 用户决定

- REQ-01【进行中】成员是通用智能体，不存在成员个人专业人设。
- REQ-02【进行中】岗位只表示当前任务责任和权限，不构成专业人设。
- REQ-03【进行中】题材融合人设按书籍和当前任务临时装配，不永久绑定成员。
- REQ-04【进行中】删除 V7 不需要的旧代码、文件和配置数据，但不删除作者或生产业务数据。
- REQ-05【进行中】先完成运行边界清理，再实施资料策划/上下文重构，最后全量 BUG 验收。
- REQ-06【已确认】删除旧开书、旧团队、旧岗位提示运行闭包；接受取消旧团队初始化、编辑租约、旧提示编译和旧设定初始化语义。
- REQ-07【已确认】删除当前 V7 未使用的 Codex、opencodego 旧模型通道；保留 V7 当前模型治理与实际模型路由。

## 实现、验收与证据映射

| 编号 | 对应需求 | 内容 | 状态 |
| --- | --- | --- | --- |
| IMP-01 | REQ-01/02/06 | 追踪并删除 `agent-team-v2`、旧角色提示和旧团队初始化的运行闭包 | 已完成 |
| IMP-02 | REQ-03 | 证明 V7 题材档案和任务期题材人设不依赖旧成员人设 | 已完成 |
| IMP-03 | REQ-04 | 复核上一轮全部删除、保留和迁移差异，清理遗漏的旧入口/依赖/测试/当前文档 | 已完成 |
| IMP-04 | REQ-04 | 核对旧配置数据，仅处理非作者、非生产业务的废弃配置；必要历史迁移只保留兼容说明 | 已完成 |
| IMP-05 | REQ-05 | 建立可复现 V7-only 运行闭包和发布基线 | 已完成并部署 |
| IMP-06 | REQ-07 | 删除 Codex、opencodego 旧连接器、配置、测试和无效模型路由 | 已完成 |
| ACC-01 | REQ-01/02 | 搜索、类型检查和运行证据证明旧成员/岗位人设不再参与 V7 调用 | 已通过 |
| ACC-02 | REQ-03 | 题材融合档案、设定和至少一条提示清单集成测试通过 | 已通过 |
| ACC-03 | REQ-04 | V7 六个 workspace 构建、迁移、启动和全量门禁通过 | 已通过（本地＋生产） |
| ACC-04 | REQ-04 | 作者数据、不可变正文、账号/书隔离和密钥边界未改变 | 已通过 |
| ACC-05 | REQ-05 | Git 差异全部属于本清单或上一轮同一清理闭环，没有未解释文件 | 已通过并提交 |
| EVD-01 | ACC-01 | 引用闭包、依赖图和最终零引用扫描 | 已取得阶段证据 |
| EVD-02 | ACC-02 | 题材融合与提示治理受影响测试记录 | 已取得最终证据 |
| EVD-03 | ACC-03/04 | 类型检查、构建、迁移、启动、全量测试和安全回归记录 | 已取得本地证据 |
| EVD-04 | ACC-05 | `git diff --stat`、完整文件清单、提交与发布记录 | 已取得并完成发布 |

## 安全审查记录

- 已通过的最小变更：`ModelAdapterFactory` 不再读取 `agent-team-v2` 的成员姓名、专业身份、专长、工作方法或旧岗位映射生成系统提示；现存旧调用参数只作为无效兼容参数，后续随旧闭包删除。
- 2026-08-31 老板明确授权：删除上述旧开书／团队／岗位提示运行闭包，并删除当前 V7 未使用的 Codex、opencodego 旧模型通道；保留历史迁移和所有作者生产数据。
- 本次授权解除此前两项自动安全审查阻断。实施仍须先断开有效引用、保留 V7 当前模型注册表和路由，并用回归证据证明不触碰历史迁移及作者生产数据。
- 已删除 Codex 连接器/测试及 opencodego 配置、认证格式、路由、测试和启动变量；V7 方舟 Coding/Agent Plan 端点保持不变。
- 已删除 `agent-team-v2`、旧团队模板、旧岗位提示编译器、旧提示仓储和旧团队仓储；V7 建书不再创建 `agent_instances`、`editor_leases`、旧讨论任务或旧设定工作区。
- 自动安全审查继续保护两类未获单独授权的范围：一是把确定性测试模式和旧角色模型兼容档案整体重写为新公共模型结构；二是删除旧 ContextPack、续写导入、设定基线和作者稿件仓储。两类均未绕过、未删除。
- 管理员问题状态与反馈任务归属校验被识别为敏感数据边界；本批只把仪表盘失败数和用户操作只读任务投影接到 V7 表，问题中心写入/归属保持原兼容行为，等待单独隔离设计。

## 信息职责审计

| 节点 | 负责者 | 本批结论 |
| --- | --- | --- |
| 成员身份、模型绑定、上下岗、任务权限 | 系统 | 保留确定性 V7 治理；不得把姓名或固定专业偏好转成提示词 |
| 题材融合和当前任务创作气质 | 大模型 Agent | 保留现有书级题材工作档案；本批只保护边界，不重构算法 |
| 当前任务责任、输入、输出和失败条件 | 系统＋Agent | 系统装配任务合同，Agent完成语义工作；任务责任不是岗位人设 |
| 旧数据和引用能否安全删除 | 系统＋工程审计 | 只按运行引用、数据权威和兼容证据判断，不用名称猜测 |

## 复用审计

- 当前 V7：直接保留 `agent-governance`、`prompt-governance`、书级题材档案、V7 四类模型网关和当前作者/后台应用。
- 旧实现：`agent-team-v2`、旧 `role-prompts`、旧团队模板和旧运行入口只用于定位删除闭包，不复用其产品语义或专业人设。
- 共享底座：继续复用账号、会员、用量、数据库、备份、模型适配器的通用连接能力；把模型允许列表改为 V7 治理/运行配置来源，不复制新连接器。
- 外部依赖：本批不引入新依赖或公开源码。

## 设计审计

### 第一轮：边界和失败模式

- 删除成员专业人设不能同时删除成员身份、头像、模型差异、任务责任和失败恢复。
- 删除旧模型白名单来源前，必须由 V7 模型注册表或当前运行配置提供等价允许列表，不能放宽成任意模型。
- 旧团队数据表和已合并迁移属于历史事实，不改写；只删除当前运行初始化、读取和展示路径。
- 生产运行包仍旧于本地清理树，本批不能用线上成功证明本地新闭包可用。

### 第二轮：实现后复核

- V7 开书仍原子创建书籍、开书资料、规划状态、预算和故事圣经；旧团队、租约、提示编译和旧设定启动均为零。
- `agent-team-v2`、旧成员专业身份字段、Codex/opencodego 代码与配置零引用扫描无结果。
- V7 书名解析已从旧品牌流水线抽成独立输出合同；旧品牌流水线及其仓储已删除。
- V7 后台失败任务只读投影直接读取当前 V7 任务表；新增真实 V7 失败书名任务集成测试，证明旧 `tasks` 为空时仍能统计和展示。
- 历史迁移没有删除或改写；生产数据库和作者数据没有连接、修改或清理。

## 阶段验证记录

- 六个 workspace 与测试 TypeScript 检查：通过；API、Worker、作者端、后台端正式构建：通过。
- 根级完整测试：75 个文件、342 项通过；V7 后端 Vitest 82 项与 Node 原生 12 项通过；作者端 125 项、后台端 39 项通过（另有后台 3 项按既有条件跳过）。
- 受影响模型、开书、修订、书名测试：5 个文件、47 项通过；六个 V7 当前模型档案均可解析，Kimi K3 白名单遗漏已修复。
- V7 独立后台测试覆盖“旧 tasks 为 0、V7 失败任务为 1”的真实投影；问题中心写入和反馈任务归属仍保持旧兼容边界。
- 102 个历史迁移在临时空库完整执行并安全重复运行；迁移目录无 Git 修改，生产数据库和作者数据未连接、未修改。
- 静态发布包 `71bc037ed34b44654563` 共 9 个文件，清单哈希 `5e59e046b19e1fcd2a7d9ff808bb9b4831d60f26906e762a2c2eee57312c41ee`，组装和逐文件校验通过。
- 隔离启动冒烟：临时空库下 API、Worker、作者端、后台端、登录保护和健康检查通过；测试进程、端口、临时目录与启动记录均已清理。
- esbuild 从 `apps/api/src/main.ts` 生成运行依赖图：130 个 TypeScript 文件中主入口实际依赖 106 个，24 个未进入主入口。后者由独立迁移/备份入口、类型与数据安全测试支撑、以及被安全审查保护的 ContextPack/续写导入/设定基线兼容代码构成，已登记而非冒充删除；唯一仅剩合同名单引用的旧 `model-call-service.ts` 已继续删除。
- 最终阶段差异：422 个已跟踪文件变更，878 行新增、72,605 行删除；另有 18 个本批 V7 入口、台账、工单和安全测试文件待纳入提交。历史迁移与 `data/` 均无变更。
- 当前代码体量（不含依赖、构建产物、文档和迁移）：生产源码 234 个文件、64,991 行；测试源码 117 个文件、19,448 行；运维脚本 18 个文件、1,513 行，共 85,952 行。102 个历史迁移另计 6,972 行，按数据兼容要求全部保留。
- 2026-08-31 经老板确认把逻辑口径 440 项审计差异统一纳入提交；Git 的重命名识别将其中 1 组删除＋新增折算为 1 条 rename，因此提交摘要为 439 个文件、2,953 行新增、72,530 行删除。提交为 `899ed10 refactor(v7): remove retired runtime closure`，工作树随后清洁；102 个迁移文件零差异。
- 生产发布在 `/opt/wenmi-releases/wm-v7-20260831-001700-71bc037e` 隔离安装与构建；生产数据库副本上两次迁移均为 `applied: []`、版本 102、完整性 `ok`。部署前正式回滚副本为 `rollback-backup/wenmi-before.sqlite`，SHA-256 为 `04bc44e3f291def0c0a97320daf16cd616f02dd3ef68695633156ea9ff434c30`。
- 切换门禁覆盖旧顶层任务/模型调用及开书、设定、规划、人物、创作、正式化全部 V7 可执行表，连续 30 秒与停服后立即复核均为 0；没有取消、暂停或改写作者任务。第一次切换因旧 Worker 心跳仍在 15 秒有效期内而被版本门禁自动回滚，第二次因新静态目录缺少 Caddy 遍历权限返回 403 而自动回滚；两次均确认旧 API、Worker、静态入口恢复后才修正部署脚本并重新执行完整门禁。
- 最终生产发布为 `wm-v7-20260831-001700-71bc037e`，静态包为 `71bc037ed34b44654563`；API、Worker、Caddy 均 active，主域/后台域健康检查均为 `ok + worker ready`，作者端、后台端及新哈希资源均返回 200，Worker 数据库心跳发布号一致，部署后 journal warning 为零。预检临时数据库副本已删除并释放约 769MB；正式回滚备份、旧发布、102 个迁移和所有生产数据保留。

## 阶段结算

- 当前阶段：旧开书/团队/岗位提示闭包与 Codex/opencodego 通道已删除；全部本地门禁、提交、可回滚生产部署和上线后复核均已完成。
- 下一阶段：资料策划 Agent、任务期题材融合人设、方法语义检索、ContextCompiler 分层预算、任务详情与失败恢复另开工单实施；其后再做全工作流与后台商业化全量 BUG 验收。
