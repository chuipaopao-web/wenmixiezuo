# 第99批：各层资料包可靠取材与预算恢复

状态：已完成修复及安全发布，2026-09-05 19:44 CST 上线。真实作者失败任务未自动重跑，实际恢复效果待作者操作形成证据。

## 变更合同

- 目标：时光机、卷、链、章的资料不再因整份拼接、方法菜单或机械截短而阻塞；Agent 按任务分批阅读可信来源，系统校验引用与最终预算。
- 范围：创建/规划资料编译、相关提示合同、资料调用恢复与状态、直接依赖测试和交接。复用现有模型网关、版本快照、幂等请求及审计，不新增模型供应商、数据库迁移或权限。
- 不改：页面结构、后台重构、作者账号、已有作者任务及内容；不重跑作者实际任务，不修改正式设定、规划、不可变正文和结算。
- 保留：当前成员配置、人工选择/继续/停止入口、原文与历史版本、当前任务成功检查点；未知调用禁止重复发送；新整理版本不复用旧超限包。
- 实现：保留完整冻结原文；超限时由资料 Agent 分页选择有路径的原文片段，程序仅核验来源、计数和组装，不能自行判断故事语义。方法作为可选参考让位于事实。资料提炼保持衍生身份，不替换正式版本；身份建议不能推翻作者明确要求。
- AI预算：输入分页、输出长度、最大页数及每页有限补交；通过已有网关记录每次真实调用，稳定请求键复用成功页，未知停止。超限/引用不合法不向创作成员传递半成品。
- 验收：真实事故存档只在忽略目录重放；所有六类创作任务与规划路线/树路径预算、正式源/版本、原文引用、反向指令、超长目录、失败/未知/取消/恢复；对应类型检查/构建、真实模型小样探针、发布健康及核心入口冒烟。不得用模拟模型证明文学质量。
- 风险判断（design_review_id：R99-CONTEXT-EVIDENCE）：改变的是任务派生资料投影与局部模型读取，不改变正式来源权威、数据库语义、身份、账单或核心创作步骤。按局部模型调用变化跑受影响集成与单测，不默认全量；若实施改变受保护数据或核心工作流结构须先更新本段记录触发事实。
- 部署：从当前生产基线组合本批修复，排除本地第96—98批后台工作。连续30秒在途清零并即时复核后逐服务切换；保留备份与回滚，不能取消任务制造窗口。

## 已核实根因

生产失败包 099bf7d9 的最后输入重建为19434字符，卷上限12000；两次模型返回成功但最终资料组装失败，未进入卷创作。方法菜单去除后仍15894，不能只删菜单或放宽上限。当前设定轻量索引可只剩编辑说明；全书投影没有进一步语义取材；时光机在Agent取材前机械截断并阻断。

## 证据与待办

- [x] 代码与失败存档核实；当前工作区仅预存未跟踪 `.claude/`，不修改。
- [x] 实现及完整差异审查：局部资料传输/失败反馈与直接测试，未改正文、设定、结算或数据库迁移；遗留机械组包私有函数零有效引用后移除，公开旧快照诊断函数仅保留离线兼容。
- [x] 自动化验证：六类创作任务及三层规划预算、原文尾部硬约束、无效来源、有限补交、未知停止、冻结来源不改写；完整卷→链→章→正文→结算回归。新增真实模型调用检查点复用回归（包括末次返回损坏后复用先前选择和分页结果），失败成员状态回归。
- [x] 真实模型探针及人工核对：完全虚构的《雨港修桥记》，deepseek-v4-pro；卷5页得到4063/12000字符，章5页得到2871/6000字符。原文公开时机、无超能力、安全条件、当前阶段责任保留；另1次反向身份建议测试返回“第一场议事”，明确拒绝“最后一场”。仅证明这组真实模型资料任务，不冒充长期文学质量。
- [x] 隔离生产发布、线上验证、两级交接与发布号更新；修复提交 `5285319f`，发布元数据另行提交，不推送。

实现细节：完整冻结来源保持不变，只有派生调用视图使用`source-evidence-v1`原文节选，每条含原字段路径。分页最多24页，每页原文7000字符，含滚动证据和目录的阅读输入最多48000字符，每页最多一次格式/预算补交。系统只按模型返回的合法编号回填原文；必要源与必要设定类别不能缺席，已冻结选择在恢复时仍保留。执行包保持原12000/8000/6000预算；规划调用视图保持18000/14000/10000预算。方法菜单在创作资料超限时让位于事实，方法库与原有选择能力保留。规划树重复粘贴的来源改为引用。

限制及审批证据：自动审批拒绝把该作者具体私密小说资料再次送到外部模型，要求明确的数据发送授权。未绕过；改用纯合成资料做真实探针，原事故存档仅本地核查，未重新执行作者实际任务。任何模型仍可能取舍错误，不能声称零遗漏；证据不足/预算核验失败时保留原文与检查点并明确失败，不能截断硬事实或伪装完成。

构建：V7后端生产构建、API类型检查及隔离目录构建、Worker类型检查及隔离构建通过；默认API dist目录受本地权限限制，改在`.tmp/r99-api-dist`构建，未覆盖本地后台演示服务。全量验证无合同触发事实，未运行。

最后门禁：本地及服务器各根10组受影响测试89项通过、后端3组合同/资产测试37项通过。未把新增分页调用混同为重复资料选择，流水总数与不可重复执行分别核对。

## 部署与线上验证

- 发布 `wm-v7-20260905-193522-5285319f`，北京时间19:44:16开始切换；API/Worker均正常，`/health`为ok、worker=ready、canStartModelTasks=true。作者端与后台页面各HTTP200、各2个JS/CSS引用正常；auth/me与admin/overview未登录均401；数据库quick_check/外键、切换后错误日志检查通过。
- 发布包以 `cd7daedb2d1b0a95f2d661d53cb3ca238e0a3b15` 为生产基线，只覆盖修复 `5285319f30f610cb402c30887707107f1459f81e` 的14个代码/测试/清单文件。`R99_COMPOSITION.json`保留逐文件SHA；未携带本地第96—98批后台改动。包为 `artifacts/deploy/r99-5285319f-utf8-source.tar.gz`，SHA256 `c4b46f4f123ac19db08ffc027700de38f1a97397096383578e4c8bbda0010161`；564个归档文件的UTF-8名称及内容逐一校验。
- 服务器独立依赖安装、contracts/backend/API/Worker构建、126项针对测试、运行源码闭包与实际模块解析通过。106个迁移与生产校验和一致；生产机私有副本两次迁移no-op，隔离API健康，模型调用记录增量0。
- 新鲜完整备份 `20260905T113750Z-515204`。原规划调用自然结束，切换前连续30秒在途为0、每次停止服务前及停止后均复核；没有取消或改写作者任务。回滚点 `wm-v7-20260905-090100-cd7daedb` 保留。静态资源未发布，原版本 `c7402f64fd0a9818ef4f` 完整性验证通过。
- 首次暂存 `wm-v7-20260905-192740-5285319f` 在切换前被源码闭包门禁拦下：Windows tar解包损坏两个中文CMD文件名。改用Python直接读Git归档、按UTF-8重新打包并逐文件核对后，使用新暂存目录发布，未修改运行中的旧目录。随后隔离复制因持续写入耗时，诊断期间自然完成；两次尝试只向本次复制PID发信号的命令均在精确校验处停止，实际未发送信号。未改变生产心跳或用户任务。
- 发布脚本返回 `DEPLOYMENT_PASSED`。回执在生产发布根目录的 `stage-result.txt`、`backup-result.txt`、`preflight-result.txt`、`cutover-result.txt`、`postdeploy-result.txt`。本次为HTTP与运行验收，无UI改动，不冒充浏览器视觉验收或真实作者小说生成验收。
- 交付边界：作者原文、正式设定/规划、正文、结算和历史结果未改写。资料Agent仍可能遗漏语义，最多24页及必要证据预算仍有保护上限；不能保证任意长输入无条件成功。失败保留来源和检查点，不能伪装完成。
