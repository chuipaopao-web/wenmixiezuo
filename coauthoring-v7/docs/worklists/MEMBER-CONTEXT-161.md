# R161 成员职责收敛与静默更新

目标：成员页只管成员与模型，不再嵌入上下文或规则编辑器；按现有功能档案关联参与功能与最近执行记录，跳转唯一功能管理入口。

保留：成员身份、头像、模型绑定、准入和现有表单不变。实际输入组件继续用于功能管理页；历史记录不改。

静默发布：移除自动版本重载；代理对旧页面的版本探针返回空响应，防止已打开的旧脚本在本次发布后主动刷新。导航请求继续返回页面。先验证代理兼容，再原子切换静态资源，保留旧资源，不重启API/Worker。

验证：相关组件与类型构建、根与历史入口、探针响应、服务进程及资源校验。状态：实施中。
