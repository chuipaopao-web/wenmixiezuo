# R161 成员职责收敛与静默更新

目标：成员页只管成员与模型，不再嵌入上下文或规则编辑器；按现有功能档案关联参与功能与最近执行记录，跳转唯一功能管理入口。

保留：成员身份、头像、模型绑定、准入和现有表单不变。实际输入组件继续用于功能管理页；历史记录不改。

静默发布：移除自动版本重载；代理对旧页面的版本探针返回空响应，防止已打开的旧脚本在本次发布后主动刷新。导航请求继续返回页面。先验证代理兼容，再原子切换静态资源，保留旧资源，不重启API/Worker。

验证：9项相关组件检查、类型和后台构建通过；模型保存全局版本检查保留。生产旧探针204、导航200、静态资源哈希、健康及401通过，API/Worker进程不变。代码0f4fe6b，静态0f39feb36167722f6174，回滚28ed241da0929b5cd259；静态物料/opt/wenmi-releases/r161-map-0f4fe6b，代理回滚/opt/wenmi-releases/quiet-admin-161/Caddyfile.before。未进行生产模型配置写入或付费调用。状态：已发布。
