# R183：全书方向资料整理恢复

- 状态：已上线，API/Worker wm-v7-20260909-180500-499ff1ac；静态保持R182。
- 目标：修复管理员机甲书全书方向在资料整理阶段失败，使作者能继续生成实际方向后再讨论重构。
- 根因证据：原任务8次资料调用成功；末页反复超过18000字符，规则/facts/摘要与字段重复运输；重试未向资料阅读器传入恢复轮次。
- 范围：规划调用视图去除重复表示、空值；独有事实及条件、全局规则保留；冻结源不改。已完成调用幂等复用，失败页有限恢复。同步后台RB-22与路线、交接。
- 保留：作者开书、24项设定、原快照/版本、任务与计费审计、现有时光机交互、生成结果合同及模型配置。不实施新时光机设计，不自动生成或采用作者方向。
- 验证：针对重复字段、旧辅助条件、独有事实、全局约束、长资料分页与恢复、跨书/版本回归；实际冻结源只读重放及预算检查，必要合成模型探针；API构建与发布检查。
- 发布：后端兼容发布，静态资源保持R182，连续30秒零在途后切换API/Worker，健康/心跳/后台地图核查；失败回滚代码，不改生产作者数据。

## 实施与验证

- 499ff1ac：使用既有规则渲染器将旧条件/代价/例外一起转换成运输短句，按完全一致的原文或渲染结果去重；不以相似度猜测删除事实。全局规则仍是不可拆散的保留项，独有事实不删除。开书只删除空值。
- 59项资料/规划回归通过；最终渲染修正后26项资料回归再次通过、规划树2项和地图发布6项通过，API构建通过。测试计数含交叠，不相加冒充独立覆盖数。
- 服务器内原任务快照：原运输40306字符；第一版21596字符，真实资料选择仍在第4页超限，未发布该行为；最终17359字符/26来源，无需额外选材调用。重放一致、原快照未变、未启动作者方向任务。120条规则保留，旧非空条件仍由同一渲染器完整合入。
- 首次发布包版本号wm-v7-r183-499ff1ac不符合现有格式，API启动失败后自动回滚R177，健康与Worker恢复；修正为wm-v7-20260909-180500-499ff1ac，追加版本格式、源码闭包与模块解析复核。无迁移或作者数据更改。
- 自动审批拒绝完整作品快照导出和原始日志回传；改为服务器内只读验证与非内容统计，未绕过导出限制。
