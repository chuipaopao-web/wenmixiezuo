# 第150批：统一默认文字模型

用户最新要求所有默认首选改为DeepSeek V4 Pro，取消GLM首选。调整开书默认选择与文字岗位初始配置，并同步现有生产岗位默认；实际测速、准入结果、作者主动选择及历史任务快照保留。独立复核排除设计模型，节点不合格模型不借默认配置强行准入，图片继续使用图片模型。

仅执行默认选择、配置及构建验证，不再消耗真实模型测试额度。验证通过后按既有安全部署流程发布，记录实际结果。

实现：开书对已准入名单优先选择Pro但不改测速报告；通用九种文字用途默认Pro；初始六岗位默认Pro，运行名册优先尊重默认标记，排除设计模型后无默认成员时继续使用可用候补。镜像源同步。20项定向用例（含真实排名保留、手选、异模型排除、创建/规划默认）及API/兼容运行包构建通过。线上只读确认六个Pro绑定均compatible；发布后通过治理服务设置默认，保留前后配置审计。未调用模型。
