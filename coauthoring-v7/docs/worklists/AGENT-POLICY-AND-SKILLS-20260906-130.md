# 第130批：创作指令与固定成员模型治理

用户已授权优化指令并开始调整Agent成员。主代理直接实施，复用现有成员治理、版本化提示、执行名册和后台；不重写开书流程或迁移数据库。

## 本批交付边界

- 修正链、章纲、正文工位与自然正文Skill的通用固定节拍；区分事实硬边界和文学建议，保留作者选择的节奏目标。
- Skill可显式不加载；去掉默认重复的资料边界Skill，基础可信边界仍在编译器中。禁止重复别名、跨任务Skill；技术重试仍原样使用快照。
- 固定23位成员编号、岗位和历史记录，模型绑定继续可修改。统一后台与执行名册的模型兼容来源；所有文本模型进入候选展示，未接通岗位和已发现问题的组合不能直接返岗。保留正在使用的正常成员；不凭一次短探针宣布全岗位能力通过。
- 后台展示候选模型的兼容/待验证/暂停复测原因，说明修改只影响新任务；保留现有页面风格。
- 第一阶段完成共享配置与防错；完整的工位样本评测、准入记录编辑和自动健康熔断属于后续Agent批次，不把本批当成RB-48整体完成。

用户追加授权：全部成员使用女性化姓名和20—24岁成年女性古风头像；发型、五官、神态与服饰区分。生成6×4图集，每位当前成员有独立位置；共享身份表统一作者端/后台/新任务显示，保留旧编号和历史任务原记录。

核查扩展：原卷方案解析器也把4—8章、1万—2.8万字当硬门槛。本批同步改为章纲接口既有2—30章容量、连续区间与正数字数覆盖；这不是文学节奏门槛。默认成员停岗自动启用与默认唯一索引冲突一并修复。

## 保留与验证

作者输入、已确认方案、不可变正文、身份/账本/任务权威、旧任务与提示版本不变。只有相关提示编译、成员治理/名册、后台组件测试、对应类型构建及390/1440浏览器验证；模型参数和生成流程不变，不重复六模型全岗位调用。文学质量提升未做对照实测，不作已证明声明。

部署已授权：提交后新目录预检、版本冲突/源码闭包、备份和在途连续30秒零、逐服务切换、公网检查，保留R129回滚。提示新增版本需在副本检验，不覆盖旧版本。

## 状态

本地验收通过，待生产预检/发布。生产仍R129。

验证：提示编译/旧版本/重试与治理30项、后端相关53项、后台组件2项通过；12项身份/名册测试在新增头像后复核。API隔离输出构建、双Web类型与构建通过；本机原API dist权限拒绝，改为artifacts/rebuild/130/api-dist，不改文件权限。390/1440浏览器无横向溢出或脚本错误，23个头像位置互不重复；证据artifacts/rebuild/130。未新增真实模型调用或宣称文学质量提升已验证。下一步先完成本批，再按实际工位评测扩展候选准入，不先做RB-20。
