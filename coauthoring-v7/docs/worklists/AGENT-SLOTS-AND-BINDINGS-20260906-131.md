# 第131批：固定岗位成员底座与可替换模型

用户授权：六类文字岗位各9名固定成员，图片岗位1—2名；接入已合作的实际模型，未绑定位置保留，具体全书/卷等节点后续逐个评测。结合现有可用功能与独立新后端实施，规则不冻结旧人数或限制合理模型候选。

本批范围：7岗位、56固定身份位置（54文字+2图片），原23位身份保留；新增成员默认候选/未绑定。统一纯数据目录供当前前后端与rebuild使用，修复新后端姓名仍旧的问题。模型目录纳入已配置7种文字通道（含GLM5.2）与Seedream，支持后续扩展，不虚构第8/9种可用通道。后台可查看全部成员、岗位说明、绑定/解绑模型；新成员绑定存独立加法表并审计，不进入旧运行名册或自动接单，节点准入实测下一批开展。保留当前已验证运行成员及任务快照。

不在本批批量调用真实模型、切换所有执行器或引入LangGraph，不把绑定当评测通过。后续节点验证才开放新增组合的任务执行；共享书籍记忆/权限/账本继续复用，不按成员复制。新后端目录同步不等于PG生产切换。岗位职责去除重复派单和强制道歉等固定流程要求，安全、来源、版本与有界恢复保留。

验证：目录唯一性、岗位容量、图片/文字类型隔离、绑定与解绑/审计/配置冲突、启动不覆盖配置、旧名册/任务快照兼容；新旧后台/作者目录类型构建及390/1440浏览器；生产独立目录以运行账号预检，增量表安装、备份、30秒无任务、安全切换和回滚。不进行无关全量测试。

部署：持续授权内发布；新配置独立加法表旧代码忽略，回滚不需删成员配置或恢复作者库。新头像沿用20—24岁成年女性古风，33个新增身份独立图位。唯一状态见本文件；完成同步路线和HANDOFF。

状态：2026-09-06 22:28:30北京时间已上线，服务`wm-v7-20260906-222000-2ab0b0b9`、静态`2b59b1944341332fbb13`。绑定/解绑、13预留位置、56身份唯一性、角色类型与冲突/重启保护通过；新旧后端类型与Web构建通过；390/1440后台56头像无溢出与脚本错误。源码闭包已纳入共享JS目录；旧测试关于GLM返岗/允许仅一名交接者的预期已按现有暂停与异模型保护校正。未执行新的真实模型调用，新增成员仍待具体节点评测。

发布经过：提交cf2b4542独立构建、运行账号开书合成流程、源码闭包与四工作区模块解析通过。21:44备份空间不足曾阻止发布；用户本轮明确授权部署和清理后，8份旧离线预检副本经无人使用检查及解压SHA核验无损压缩，释放2,540,407,738字节。完整备份`20260906T141106Z-560173`于22:11完成；没有删除作者数据、正式备份或降低5GiB预留门槛。0108在副本迁移后应用生产，旧23位配置逐行不变，新增33位只写独立配置表。

首次切换暴露岗位提示内容更新但仍沿用@1的兼容遗漏，API启动失败并自动回退R130。提交2ab0b0b9使7岗位新增@2，保留历史内容和引用，补充已有@1数据库升级的针对性回归测试（该文件4项通过）。从现网复制的数据以wenmi账号完整启动通过，67份历史提示哈希不变、@2发布且再次初始化幂等，验证过程禁止模型调用。第二次切换时保留的两个旧哈希资源被严格静态清单拒绝，再次回退；修正发布脚本，静态验证移到停止服务之前，新包严格验证，保留资源逐一对照上一版哈希。没有以放过未知资源或删除旧缓存资源解决。

最终发布前在途任务连续30秒为零并立即复核，逐服务启动并验证当前Worker PID/版本心跳。API/Worker健康、作者/后台入口一致、受保护接口401、双站新头像哈希、390/1440公网首页无溢出和脚本错误通过。线上岗位7/成员56/在岗16/停岗7/候选20/未绑定13，外键与quick_check通过。R130回滚源保留并包含0108迁移文件，回滚保留新增配置与历史提示，不恢复作者库。证据为当前release根目录`full-startup-probe.json`、`migration-trial.json`、`slots-live.json`、`static-retained-check.json`、`public-checks.json`、`deployment-passed`及本地`artifacts/rebuild/131/live-browser.json`。后台路线同步为本批已发布、RB-48整体仍部分实现；完整Agent执行及PG迁移不在本批完成范围。
