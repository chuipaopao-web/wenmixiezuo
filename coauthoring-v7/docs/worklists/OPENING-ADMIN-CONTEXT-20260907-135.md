# 第135批：开书重做、修订与后台上下文入口

目标：换成员重做采用常显头像选择；核查作者调整意见到设计修订/异模型审查的真实路径。后台统一展示全部固定岗位成员，改善筛选与可发现性；开书流程能定位到生效提示配置与实际下发上下文，清理确认重复的旧展示入口。

保留：既有开书表单与绿色视觉、原始想法、候选和作者修订版本、任务恢复/确认防重、账户权限、模型准入及计费；不删除作者数据、历史快照、迁移或仍在执行的兼容代码。实际调用记录只读；配置沿用版本化草稿、预览、发布和回退，不创建第二套配置。

实施：先核查真实代码与当前后台数据，再在现有作者/后台页面和必要服务内局部调整。生产作者与rebuild副本同步。主代理直接实施；不做全量模型速度测试。

验证：受影响作者交互、后台成员/配置/调用筛选，必要修订输入与提示编译测试、对应类型/构建及390/1440浏览器。复用既有版本治理和权限测试。静态优先独立发布；若修订传递缺陷涉及服务，按安全部署等待在途连续30秒清零并立即复核，保留代码回滚，不恢复数据库。

状态：局部实现及本地验证通过，服务器预检/发布中。已确认修订实际由设计成员执行再审查；换成员为全新原始想法任务。后台实际56身份：7岗位，16在岗/7停岗/14待验证/19未绑定，不把全部身份当作全部可接单。

修复：作者自由意见优先完整保留；决定文本在既有8项×2000字符合同内无损分段，超界在写入前提示，不静默丢弃。编译同时传给修订与异模型审查，明确后续作者意见优先于被修改的旧想法。后台版本编辑保留结构字段，防止未保存切换丢改动；修复重复查询同一调用详情一直加载。清理固定三位旧策划名单，入口改到真实成员治理。

已验证：作者57项、开书服务22项、原提示治理4项、后台14项相关测试通过；作者/后台类型、核心/API构建通过。390/1440合成浏览器验证56成员筛选、共用规则编辑、只读随机样例、调用查询、头像重做及失败后资料保留，无页面异常/横向溢出。没有全量模型测速。构建原有大包提示保留。

发布准备：02:28完整备份20260906T182829Z-572408通过。此前空间不足时将两份9月1日/2日离线预检SQLite无损gzip、逐字节SHA校验后替代原未压缩副本，未删除在线数据、作品或正式备份。生产切换仍需连续30秒在途清零并立即复核。

用户中途明确：资料包/上下文管理以全站基础通用规则为主，管理员或其他用户的实际资料只随机抽取一条作只读样例，用于判断准确性、冗余和压缩需要，不做逐用户节点资料管理。实施采用基础通用配置默认入口、单条真实样例与字符占用；既有历史追溯降为高级排障，历史快照不写回。
