# 第94批：全产品逐页重构与接管顺序

## 变更合同

- 目标：将老板“从注册登录开始，全部前端逐页讨论开发、后端逐功能完成”的要求落实为项目内可持续接管的执行文档和开发顺序表。
- 基线：`5bfd079`，分支 `codex/light-coauthoring-v7`；现有未跟踪 `.claude/` 保持不动。
- 修改范围：新建 `docs/REBUILD_EXECUTION_PLAN.md`；修订主规格的阅读入口、目标和实施顺序引用；根 AGENTS、DEVELOPMENT_WORKFLOW、两份 HANDOFF 增加接管路由；本清单。共7份文档。
- 保留：原64章详细设计及推荐身份、当前UI视觉偏好、既有功能与作者资产的保护要求、生产发布记录、按风险验收与授权边界。
- 不做：产品代码、接口实现、数据库迁移、真实模型调用、依赖安装、推送或部署；不把未来页面方案或全站实施视为已获批准。
- 验收：覆盖当前作者入口、注册登录和账号服务、14个后台导航入口；每个目标单元写明讨论主题、前端结果、对应后端、依赖和失败验收；提供唯一状态表、下一步、单页执行合同与中断接管办法；明确旧R0—R7仅为阶段背景，不与新顺序竞争。
- 验证：直接文件链接、编号/依赖/覆盖、表格和规则一致性、完整差异与范围检查；按纯文档改动不运行应用测试和构建。
- 部署：本批不部署；未来重构另按具体批次记录开发和发布授权，不能沿用第90批小修发布授权。

## 结果与证据

- 通过：新增执行文档，RB-00至RB-61共62个单元，每项有讨论、前端结果、后端步骤、关键验收和规格章节；依赖无向后引用/循环，状态表与卡片一一对应。
- 通过：核对当前作者12种AuthorView、账号界面/路由、领域路由声明和后台14个导航入口；全部入口有重构去向。仅核对入口和相关实现片段，没有冒充全站逐按钮运行审计。
- 通过：唯一进度表分别记录设计、前端、后端、验收、上线及证据；当前均未启动产品重构。下一次产品讨论为RB-01注册页，RB-00只做本页必要准备；已获准范围自主实施，未讨论页面不自动编码。
- 通过：根AGENTS、开发流程、两份HANDOFF和主规格接入执行文档。主规格64章及V9全部63章映射保持完整；原R0—R7改为能力阶段背景，不再与逐页顺序竞争。
- 通过：直接文档文件链接、编号/锚点、依赖、表格列数、代码围栏和编码检查；完整差异审阅中修正了顺序表分隔列数和重复接管段落。
- 通过：规则场景核对——新接管从注册页讨论；“这样可以”不自动扩大为全站实施；一组已定且已授权页面可连续开发；未验证后端/模型/邮件/支付不标完成；公共模块变化回归受影响已验收页；旧权益和数据迁移有单一权威。
- 范围：仅合同列出的7份文档；已有 `.claude/` 不动。最终暂存范围和差异检查后本地提交，实际提交号由Git与交付回复记录。
- 未验证：各新页面设计、代码、后端、模型质量、真实浏览器、容量/迁移和商业运行效果；本批的验收案例是后续要求，未实际执行。
- 不适用：应用测试/构建、真实模型调用、生产数据操作、推送和部署。当前线上没有因本批发生变化。
- 交付状态：开发执行文档和顺序表完成；不代表62个产品单元已经讨论、实现或验收。本清单只记录第94批文档工作的证据，全产品进度仅维护在执行计划中。
