# 第92批：事实核查与结果责任

目标：落实老板要求，开发者独立判断、按事实纠正错误前提，以实际结果验收，不能附和或用道歉代替解决问题。
范围：根AGENTS、开发流程、开发流程Skill、对应规则场景及两级HANDOFF；不改产品代码、数据、模型配置、发布号，不推送、不部署。
保留：第91批相称流程与自主执行；用户目标和偏好决定权；权限、数据安全、证据真实性和生产底线。
实施依据：当前Skill已做流程精简，但事实/假设区分、反证和错误后修复责任仍不够具体；直接补充现有源文件，不另建审查机制。
验收：关键结论有证据；错误前提直接纠正并给方案；证据不足明确未知；合理偏好不被当作错误；验证不迁就实现；错误后修正和复验；没有新增固定报告、审批或全量门禁。
验证：相关链接、Skill结构、D13—D20场景的静态规则覆盖、完整差异；不运行产品测试、浏览器或模型调用，不冒充真实Agent行为评测。
基线：9217cd28；仅既存未跟踪 .claude/，不触碰。

## 结果

已完成规则补强。根AGENTS记录总开发者责任，开发流程第1节给出具体核查与纠错方法，Skill链接到该方法，避免再维护一套完整规则。

静态验证通过：7个范围内文档、Skill frontmatter、链接章节、13个本地Markdown链接、D13—D20共8个场景定义及Git差异检查。场景逐条核对了错误归因、证据不足、合理偏好、验收迁就实现、自己犯错后的处理和有依据时认可，规则覆盖明确；这不是8次实际Agent行为测试。

保留项核对：没有改变授权/安全门槛、既有相称测试矩阵或第91批自主执行边界，没有加入固定审批、报告和全量验证要求。没有产品代码或其他任务文件混入；两级HANDOFF只补规则状态。

未验证：实际Agent长期遵循效果；不承诺仅修改规则就能杜绝失误。
未执行：应用测试/构建、浏览器、真实模型、推送和部署。
提交：本批本地提交，最终提交号见完工回复；线上仍为第90批。
