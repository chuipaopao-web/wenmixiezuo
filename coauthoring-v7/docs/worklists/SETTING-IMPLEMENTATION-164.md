# R164 完整设定方案实施（连续三步）

授权：执行R163全部确认方案，按第一步检查后继续第二步、第三步，最后整体回归；每次发布同步后台当前功能说明、方案验收状态及路线。用户允许以新版为主处理测试数据。采用增量兼容读取，不主动删除账号、结算、原文或在途任务；不为省事清空数据库。

1. 目录/生成/UI：60项映射24主题、适用性完整分类、规则卡结构、取消600字质量门槛、单成员批次/独立复核/恢复、三阶段页面。状态：实施中。
2. 存储/注入：单一规则来源、全书/主题/对象层、全书卷链资料选择、章纲正文继承、来源和预算保护、缺资料处理。状态：核心路径已实现，整体验证中。
3. 变更/连续性：规则变更影响、确认与正文边界、状态和知情资料、完整链路回归与必要真实探针。状态：引用影响预览及正式版本保护已实现；语义变更分类与冲突闭环仍待补齐。

复用：当前唯一SQLite账号/任务/结算与API/Worker、既有单主设计及冻结后备名单、作者正式版本、React页面和统一后台。不得因本次任务整库迁移或恢复旧产品。数据库新增形状沿现有迁移机制；来源版本和多用户隔离保持。

验证：每步受影响测试/类型/构建和对应页面状态检查；三步涉及核心工作流及资料语义，最终按验收标准执行verify:full及来源/恢复/隔离检查。实际模型探针使用合成书籍和既有额度，控制样本；工程正确、模型表现、长篇效果分别记录，不宣称零崩坏。生产API/Worker按在途任务连续30秒零再逐服务发布，静态独立更新保留旧资源，不主动刷新管理员页面。

后台同步：RB-21确认方案保留；管理·字段必须描述本次真正上线行为，关联RB-22—29/32资料流程随改变更新。源校验仅在说明核对后更新，逐步记录验收项证据和未完成项，不把局部标整套完成。

2026-09-08进展：24主题及旧键映射、三类分类、规则卡同源展示/事实投影、取消600字拒收、全书约束不可遗漏、待修改期间仍读取上一正式设定、后续引用影响预览、章纲/正文/审查继承上级选材已实现。46项设定流程、23项证据阅读检查和22项创作全链路检查分别通过；后者验证13份上下文只启动5次常规资料选择，其余继承；不代表真实模型或百万字文学质量。修复旧快速模型白名单过滤统一成员名单、导致无后备可交接的问题；DeepSeek优先保留。21项既有页面检查通过，本地真实组件验证修改内容按钮和条件展示，无白屏。源码闭包漏登记及既有断言与当前迁移/模型默认值不一致已修正；根测试565、后端101、作者218、后台72分别通过，后台3项既有跳过；这些发生在最后局部审查修改前，不代替最终验证。尚未发布。

真实小样与修正：仅合成资料、套餐调用，无生产数据及现金回退，共6次。首轮DS设计15.9秒格式正确但把“主要”强化为“只能”、增加禁令及误标global，质量不通过。指令修订后DS两个主题11.0秒，6条topic规则保留主要/凭证/延迟。Kimi全量重写审查曾出现issues字符串、已修问题再次要求作者选择及遗漏禁令等问题，因此不能仅以格式通过验收。现单项规则卡审查改成冻结编号的replace/remove局部修改；系统拒绝重复编号、无依据、越界、两份结果和删空，保留未改规则。最后Kimi小样20.2秒修正主要/只能、误标层级与无依据禁令，仍残留一条对象与主题重复，记录为文学质量局限，不宣称零幻觉。新增6项规则检查与46项设定流程共52项通过（局部审查严格必填和旧主题视图调整后还需最终验证）。探针证据位于.local/r164-rule-probe-result*.json，不包含作者数据。

设定清单岗位已统一：新任务由策划编剧执行，角色合同允许setting_recommendation，历史主编任务仍按冻结身份继续；作者页面和后台AI-020至022同步。旧条目新增统一主题标识供目录识别，尚不等同语义合并。

2026-09-08 22:15最终工程检查：verify:full完整命令通过，根567/后端101/作者218/后台72通过（后台3项既有跳过），另有既有Node脚本检查；类型、源码闭包、功能说明来源校验、构建均通过。静态构建93a8860ad630c4dc7f18，仅本地，未发布。单条规则审查与设定流程52项、设定UI21项也单独通过。上述验证确认当前实现，不代表下列尚未完成的目标或长篇文学效果。

待闭环：语义变更分类与规划/正文冲突处置、新增对象关键依赖补充、旧主题合并的完整语义覆盖。后台说明区分本批已上线与完整方案部分实现，不能把本批工程通过写成R163全部完成。

发布执行：用户追问后明确先发布已通过验证的本批，不将剩余功能作为上线前提。初次切换遇到既有skill.data-boundary@2种子哈希冲突并自动回滚；原因是设计岗位新增任务导致共用触发列表改变，空库测试未覆盖该旧种子。修复为@3新版本及去重排序触发列表，保留@2不改；新增旧库升级/重复启动回归，29项针对性检查和后端101项及Node脚本通过。发布门禁追加真实资产记录的只读内存副本预检。原始生产库未被预检修改，回滚没有执行数据恢复。空间不足通过压缩保留三个旧预检副本解决，未删除用户原件或历史备份；新完整备份20260908T144508Z-629259已校验。

2026-09-08 23:07发布完成：代码2d1c6e4c，API/Worker wm-v7-20260908-225000-2d1c6e4c，静态93a8860ad630c4dc7f18。服务器独立构建、源码闭包、模块检查通过。预检最初误用需要业务表的summary，改为只比较三张资产表完整快照，确认74个旧版本不变、42个当前资产匹配、重复升级幂等；预检始终只写内存。连续30秒在途零并即时复核后切换，Worker首次心跳等待后健康通过。公网作者/后台根与历史入口、JS和头像哈希、保护接口401、任务服务就绪、后台导航200与旧探针204验证通过，旧哈希资源保留；本轮未进行管理员交互登录及新真实模型调用。路线1.58实际读取文件已与发布源字节一致。证据目录/opt/wenmi-releases/wm-v7-20260908-225000-2d1c6e4c，含deployment-passed、public-checks.json、seed-upgrade-preflight.json及seed-upgrade-precutover.json。上述待闭环部分继续实施，未删除作者数据。
