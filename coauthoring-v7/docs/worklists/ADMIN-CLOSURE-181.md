# R181 后台收尾与旧实现依赖核查

状态：执行中。

目标：核对82个路线单元的线上现状与重构剩余工作，修复混合状态和过时说明；确认旧代码退出边界，修复本次发现的验收遗漏。完成后再讨论时光机新设计。

范围：后台功能管理/开发路线、唯一路线原文、发布前文档契约检查及直接相关测试。保持作者页面、账号、账务、作品和任务；不执行数据清理，不将独立PG整站切换或未确认的未来功能纳入本次发布。

验证：按docs/ACCEPTANCE.md的后台、前端逻辑、发布与文档行执行；核对会员/后台/成员/提示资产已有接口回归，源码可达性，地图解析和权限，受影响组件/类型/构建及桌面手机预览。真实生产只读核查；没有真实模型或长期阅读验证不宣称通过。

部署：验证后静态原子发布，保留旧哈希资源和回滚，不重启API/Worker；同步路线原文。旧代码只有确认未引用且不属于有效兼容、迁移或回滚边界才删除。

## 核查发现

- 当前批次停在165，功能说明仍写自动重载，与161及179发布行为矛盾。
- 82单元中17个前/后端字段开发中；16个已发布单元中9个整体验收未通过。已上线基础能力和未来重构合同不能混为一个“开发中”。
- ManualOpeningForm仍供BookProfileDialogs和AI开书结果编辑调用，不属于可删除死代码。rebuild/apps是独立未切换工程；当前运行仍依赖apps/api、apps/worker、coauthoring-v7和legacy-opening共享包。
- 原源码闭包检查没有孤立生产源码，但迁移数量测试固定109；当前已有0110账务归档、0111创意偏好，实际111，导致验收误报。改为逐文件核对发布清单，继续保留历史迁移。
- 清理AgentGovernancePage单纯转发包装层，AssetAdminApp及UnifiedAgentGovernance测试直接使用统一组件。删除已明确被替代并describe.skip的旧分页面测试3项（并非失败测试）；统一成员治理3项现行交互测试继续执行。该清理不改变菜单、URL、API或规则。

## 实施与证据

82卡片已补齐当前交付/执行归属/剩余范围/退出条件；功能管理与路线共用DeliveryScope，折叠实现细节。部署、验收失败、回退的状态不混用。发布前检查四项说明缺失/重复；更新165批次、自动重载、成员规则入口、运营收入和设定预算的过时说明。

已取得：会员/管理员/成员/提示资产接口21项通过；地图权限与源码闭包15项通过；发布文档门禁5项通过。后台首次74项通过、旧3项跳过；清理后再验证。Web类型与静态构建通过；源码闭包及68项当前能力/130项历史退出记录检查通过。390/1440隔离浏览器实际组件检查，详情展开、功能切换、无横向溢出/页面错误通过，截图artifacts/rebuild/181；非生产登录页面，未补验所有历史功能的真实管理员操作，未取得改动前同视口截图。

仍不声称完成：RB-20全链变更影响、全站上下文压缩可视化、完整治理和独立PG切换；这些是各卡保留的后续业务范围，不以本批台账收尾代替实现。没有调用付费模型或启动作者任务。
