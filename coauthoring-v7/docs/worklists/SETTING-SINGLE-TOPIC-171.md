# R171 设定逐主题连续设计与展开修复

已授权：增加批次和合理Token用量优先保证准确性；条件移入规则详情；查看详情必须明显展开与收起。

实现范围：每个生成工作单元只含一个主题，取消尾项合并；同一主成员顺序连续设计、现有换员续做与独立复核保留。每次沿用已确认规则与当前草案的分开标记，超上下文预算时由资料选择调用挑完整事实，不机械截断。完成内容持久化，后续单元重新读取，旧版本与正式内容不覆盖。输出额度不盲目放大。

界面：收起时仅短预览，查看详情展开完整规则与操作；条件连同适用/涉及放入每主题唯一的折叠详情，代价和例外继续直接显示。

验证：相关后端编排/上下文/失败续做测试；作者页面与手机/桌面展开收起检查；类型、构建和后台说明同源验证。发布前按部署规则等待在途连续30秒为零，不中断作者任务；无迁移和作者数据清理。

状态：实现完成，待发布。47项设定集成检查通过（46项整套通过，最后租约夹具列名修正后定向复测1项通过）；12项合同/规则检查、26项作者组件检查、完整类型与测试类型通过。390/1440浏览器验证短预览、完整展开、再次收起、条件默认隐藏及键盘展开，页面无错误或横向溢出。资料接续、换员和未知调用保护沿用并已回归。未新增真实模型调用，不将工程检查宣称为百万字文学质量保证。
