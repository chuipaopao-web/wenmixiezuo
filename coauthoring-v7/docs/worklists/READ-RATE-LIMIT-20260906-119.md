# 第119批：正常页面读取被共享限流误伤

状态：已上线，2026-09-06北京时间11:11:24。用户报告时光机“请求较多”，授权修复；持续安全部署授权有效。

- 生产只读证据：近期18个429，同一访客分组107个200/8个404/18个429；限流涉及全书树、故事状态和正文进度。当前API为R104，静态R118，health200。未输出IP/令牌/作品正文。时光机初始加载7个并行只读请求，无任务时不会轮询；现有默认100/min/IP跨业务共享，快速浏览容易超额。
- 按已验证会话的用户ID区分读写桶：认证GET/HEAD读取600/min，认证写100/min；公共登录/注册继续IP限流10/5min、3/5min，即使带cookie也不改变；匿名100/min/IP，health独立桶。不得关闭限流或信任客户端传入身份。
- GPT-5.5仅修改请求策略与安全测试，主代理负责合同、生产组合、反例审查与安全部署。本批不改UI、模型、作品、数据库或任务状态。
- 验证真实Fastify hook顺序、认证归属、读写隔离、公共认证保护、限额与429/Retry-After；按生产R104源叠加最少文件构建验证，避免混入未发布后台/PG。API发布前在途任务连续30秒零并立即复核，保留R104回退，不取消任务制造窗口。

## 验收与实际发布

- GPT-5.5完成两文件实现与回归，主代理复查真实Fastify生命周期。主代理本地10项通过；按R104组合后的10项通过；服务器真实依赖中最终10项通过。编译后的独立Fastify探针验证GET/HEAD共享600、写100、health100、匿名100、伪造身份不提额；真实会话/跨账号隔离由集成测试覆盖，未以模拟身份冒充真实认证验收。
- 基线包SHA256 `af592061506bdb9190d51839e06329e72c590081eca748a9782b33e19fa821aa`。仅叠加请求策略、相应测试、3个运维脚本及其闭包资源登记；明确排除了未发布后台的REBUILD_EXECUTION_PLAN资源引用。编译差异仅request-policy.js及其map，不发布PG重构或后台96—98。
- 产品提交49f1fccd；冻结运行包58aa0e75。最终包SHA256 `24468b55c2934a30956335b946075f34a20871393e53d7a353c4a37392e7cd9c`；实际执行的修订部署脚本来自88637b8，SHA256 `5f04c63083ba6c214193bd261f7880b32258338cc3586289a5fd22f9060f15f0`，保存在生产版本根目录deploy-used.sh。包内早期部署脚本不能用于再次部署；应使用项目当前脚本或该deploy-used.sh。
- 预发布中发现版本标识格式、Windows换行、运维资源登记和闭包输出路径不匹配，均在隔离目录修正。首次切换遗漏Worker Requires依赖，第二次遗漏API初始化先登记release_runs的就绪条件，触发回退；原服务恢复后再修脚本。没有取消作者任务或改写作者资料；过程中Worker曾短暂不可用，不能声称完全无服务影响。最终脚本先等待API健康响应，再启动Worker，并检查当前release/PID的新鲜心跳；读探针已验证匹配及拒绝错误版本。
- 服务器运行源码闭包205、运维源码15、运维资源17、迁移107、静态8、构建输入24通过；@wenmi/contracts与@wenmi/v7-backend均解析到本批隔离版本。完整备份 `20260906T030629Z-540287` 校验完成；备份副本迁移运行两次均零变更，quick_check与外键检查通过。未恢复生产数据。
- 每次切换均连续至少30秒在途为零并立即复核。最终API PID541336、Worker PID541356；两者版本均为 `wm-v7-20260906-105500-58aa0e75`。静态仍 `4f6d09ded6793ff2f998`，作者和后台入口与该版本字节一致；公网health200、worker ready、canStartModelTasks true，未登录me/books401。上线42秒后仍健康，线上policy.js哈希与已验收包一致：`c3d1329d1c391fe84fc5b4d0e440aba981eea9054209f0c7ccf575195a4a11a5`。
- 原R104源码与静态保留供回退，不回滚作者数据。真实用户尚需刷新/重新读取失败的时光机卡片；没有代作者启动生成任务，也不宣称已验证该用户的真实书籍交互。

本地证据：artifacts/rebuild/119/root-policy-tests.log、composed-policy-tests.log、baseline.json；artifacts/deploy/r119-package.json。生产证据：本批版本根目录的policy-tests.log、compiled-probe.json、modules.log、migrations.json、backup.log、deployment-passed.txt与deploy-used.sh。发布准备失败与回退记录保留供审计。
