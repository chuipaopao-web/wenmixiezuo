# R189 工作流程入口可见性修复

用户看不到R188新增入口。线上已为R188，但资产横向标签最后一项在窄屏被隐藏；修复入口首位展示、标签换行。仅修改资产分类导航顺序与局部样式，保留页面、数据、编辑保护、既有入口及流程规范。使用开发流程和UI规范，不涉及长篇质量规范。验证320/390/768/1440宽度四个入口均在容器内且可点击、旧导航/目录搜索、类型构建。通过后静态原子发布，不重启API/Worker，作者资源保持一致。

状态：本地通过，准备静态发布。5项组件与类型检查通过；320/390/768/1440浏览器验证入口首位、全部按钮在容器内、点击打开和页面无横溢。发现并修复资产标题栏预留空搜索列导致768宽度溢出，仅限定资产页标题栏，不改其他模块。证据artifacts/rebuild/189。
