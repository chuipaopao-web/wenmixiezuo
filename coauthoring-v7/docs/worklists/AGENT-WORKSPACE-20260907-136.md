# 第136批：按岗位组织的成员管理工作区

目标：合并成员与提示上下文两处导航。岗位顺序展示固定成员，成员有独立详情链接，统一管理模型/状态、岗位共用规则、固定工位输入规则与Skill，查看该成员最近真实资料、运行人设及冻结版本。基础规则仍是共用版本，历史用户输入只读，不为每个用户或成员复制配置。

依据：新后端与当前生产共用agent-catalog身份、独立opening-runtime及任务/提示合同；PG后台治理尚未完整接入，不能通过改标题冒充整站PG切换。本批在现行适配器增加按成员/工位精确查询，沿用真实配置发布事务，页面按新领域关系重组。核查本地旧服务数据，不复制生产作者库到本地、不凭空造在岗状态。

保留：56固定身份、模型准入、计费/执行器、作者页面、共用草稿/预览/发布/回退、历史快照和权限。删除重复导航而非历史记录；旧地址进入统一管理。没有新数据库迁移、真实模型测速或永久数据清理。

验收：成员/工位筛选在数据库查询层生效；空记录不能串到其他成员。配置作用域明确，返回与刷新可定位成员；390/1440浏览器验证常显岗位成员、独立详情、工位选择、样例、编辑与旧入口兼容。运行受影响前后端测试及构建。通过后按持续授权安全发布，服务变更必须零任务30秒并复核。

状态：本地验收通过，待安全发布。

实现：统一“成员与上下文”入口，岗位排序、56身份卡片、可刷新/返回的成员独立链接；模型与状态保留原事务，岗位/工位/Skill编辑沿用共用草稿、预览、发布、回退。成员详情显示最近实际任务、临时题材人设、冻结版本、输入原文及字符占用；查询在两类仓储来源限制成员/工位后取最近记录，空记录不会借用他人数据。移除重复导航，旧链接进入共用配置；局部未保存内容阻止导航切换。

验证：12项相关后台测试、4项提示治理集成测试、后台类型、API和后台构建通过；390/1440浏览器验证成员详情、工位筛选、实际输入、编辑范围、未保存保护、深链接、空记录、旧入口、头像，未见横向溢出或页面异常。没有额外模型调用，也没有重跑全量测试。证据artifacts/rebuild/136。

本地故障：43170后台连接的是43196上的R96旧测试服务，返回22人；已备份本地SQLite并用当前API替换该本地服务，保留账户与数据。只读真实服务查询确认7岗位56成员，本地无模型凭据因此成员停岗是正常隔离状态，不复制生产数据或虚构在岗；补齐本地共享头像路径。未重置测试账户密码。
