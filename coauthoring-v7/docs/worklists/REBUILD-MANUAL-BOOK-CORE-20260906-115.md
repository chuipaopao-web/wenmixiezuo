# 第115批：手动建书原始输入与目录事务

状态：本批内部建书事务已实现、主代理验收通过，未发布。承接114批0ea089ee；按RB-08先完成内部建书数据闭环，GPT-5.5实现，主代理独立验收。

## 范围与合同

- 只修改rebuild后端、共享合同、加法迁移、测试及必要文档；现有首页、手动新建表单和内容页UI不变。不改旧工程、不部署、不接生产数据或模型/邮件。
- 提供内部createManualBookFromSession能力，输入兼容现有手动表单的openingPackage、可选openingIdea与idempotencyKey。共享运行时合同明确字段、类型与合理容量；不静默丢字段、截断、修饰或摘要作者输入。书架展示标题可trim，但原始字段、空格、换行、数组次序保留。未知字段拒绝；不接受owner、bookId、成员/candidate/task冒充手动来源。原始输入仅为作者创作意图，不是正文实际或AI审查通过结果。
- 新增0006迁移，保存一份不可变手动开书来源（版本1、来源类型、owner/book、完整结构化输入）及初始空章节目录的元数据/版本。目录不凭空创建章纲、正文或卷链，后续RB-09扩展条目。数据库以约束保证来源和目录属于同一本书及owner；普通app角色不得更新/删除原始来源。
- 书籍元数据、原始来源、空目录及必要审计在同一个已验证会话事务中提交；复用账号→会话→书籍锁顺序，不调用自带事务的旧create后再分开补写。可在bookshelf模块中增加受控编排，不引入旧运行依赖或跨模块私有访问。
- 幂等键绑定owner和完整输入；同键同输入并发/丢响应重试只建立一次，JSON对象键顺序不同不应误判，数组/文本不同必须冲突。与已有仅元数据创建同键不得冒充完整建书成功。记录原始首次结果身份，后续归档不能在重试时被恢复或覆盖。
- 提供内部本人手动来源与目录读取，跨owner、未知书及非手动来源统一安全不存在；无效/撤销/停用会话拒绝。审计不包含原始文本、密码或会话。读取仅取本书，不获取其他书材料。
- 本批不开放现有confirmOpeningBook完整成功HTTP路由，不把书架可见当信息/设定、正文编辑全链路完成。后续完整接口/前端联调、服务端草稿和人工体验保留待完成。

## 验证和交付

按权限与不可变作者输入的高风险范围执行独立rebuild verify:full，使用本地合成账号/随机PG；重点覆盖完整手动表单、原文Unicode/空格/换行原样往返、非法字段/超限拒绝、并发幂等及变化冲突、对象键顺序、跨owner/跨书约束、失效会话、归档后重试、审计/来源/目录故障整体回滚、app不能改删来源及迁移升级。主代理另库检查编译后服务与数据库证据，审查差异后记录并提交。只证明确定性保存和权限，不声明文学质量或真实创作闭环通过。

本地预览按实际需要更新；本批无HTTP/UI变化则无需重启现有预览。旧迁移0001—0005保持字节不变，旧数据不迁入。

## 实现与证据（2026-09-06）

- GPT-5.5完成内部createManualBookFromSession/readManualBookFromSession、严格OpeningPackage合同、0006来源/空目录表及复合归属约束。响应为book/source/chapterDirectory；原始来源版本1、manual_opening_package身份，目录版本1/0条目，不生成任何正文或规划。
- 主审发现并纠正了草稿schema错误强制可空字段必填、按owner/input_hash唯一约束误拦不同请求创建相同内容两项问题；最终按现有表单可空语义保留，幂等只绑定owner/key。哈希键排序固定code-unit顺序，防止运行环境排序差异。没有降低既有安全校验。
- 原始输入存储限额与模型资料包限额分开：openingIdea最多5000 Unicode字符；各字段/数组容量见共享schema；稳定JSON序列化后的完整来源最多256000 Unicode字符。超过就拒绝本次写入，不截断原文。本批无公开HTTP，未来路由必须显式设置相容bodyLimit并验证请求到界面的错误保留行为。
- 主代理在fresh隔离库wenmi_rebuild_test_review115fresh_b201326241运行verify:full：103源码边界、严格类型、20基础+50真实PG+222作者测试及双Web构建通过，共292项。现有作者构建包体积警告仍为非阻断项。
- 另一隔离库11组编译后服务/真实PG反例通过：0005→0006升级后22个合成账号、24条会话、180本书与213条书籍审计的完整行校验摘要不变；迁移重复执行无新增。6个并发同请求只创建一份；完整原始文字/空格/CRLF/Unicode往返；对象键顺序、数组变化冲突及不同请求主动建同内容；跨owner、metadata-only同键与反向冲突；归档后重试不恢复；非法身份、NUL/孤立surrogate/字段及总量超限；5000个emoji边界；从实际未改ManualOpeningForm提取默认值和校验函数，最少合法表单保存原样；真实来源/目录/审计触发器故障分别使全事务回滚且原键可重试；app不能改删原始来源且数据库拒绝跨owner关联；退出和停用账号拒绝读写。
- 本地wenmi_rebuild_dev只应用0006；43282/health为200，43280真实API代理书架未登录为401。未重启API/Worker，页面和旧迁移无改动，未接生产。
- 本地证据：artifacts/rebuild/115/root-verify-full.log、root-review.json/log、upgrade-before.json、local-preview.json、plan-parser.json。提交内PG测试可重跑核心机制，独立故障探针在.local/rebuild/review-manual115.mjs。

## 接管与剩余范围

用户已返回，夜间自动接续已暂停；本批验收后不另派新批次，等下一安排。RB-08整体仍开发中/未验证：公开接口、服务端草稿、创建后信息/设定/手写联调与完整作者体验未完成。下一建议先补齐新书资料读取与目录/不可变正文的最小闭环，再接通保留的表单，不能仅包装内部create就宣称整页完成。当前来源仅记录原始创作意图，没有文学或长期叙事质量验收。
