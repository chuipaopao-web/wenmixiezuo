# 第120批：新工程开书成员头像选择与状态精简

状态：局部UI已验收，未部署。用户确认调整，保留原成员选择功能，改为头像选择；团队设计开始后去除重复状态文字。只在独立rebuild工程实施和验收，不切换生产架构，不部署现网。

- 范围：rebuild作者端NewNovelPage及局部样式、受影响测试。设计成员选择保留当前候选过滤、默认自动安排、选择持久化与提交语义，头像和姓名可点击，选中可见并支持键盘。仅调整开书前的选择，不顺手改换成员重新设计入口。
- 工作中：当前执行成员头像、姓名与一条简洁状态；设计/审查保持真实阶段；成员分工移入折叠详情；输入详情及停止/失败/恢复等操作保留。缩减本页留白，不改共享导航、全局样式和其他页面。
- 明确保留：团队设计、自己设计、资料、设定、取名的现有产品功能、流程和UI，除本批已确认的两处局部调整。后续按兼容性复用、适配与真实验收，不重新设计这些功能。本批不实现新架构开书后端、模型或数据库迁移。
- GPT-5.5负责局部实现，主代理审查与验证。验证成员选择/自动安排/持久化/提交参数、工作及审查显示、原失败恢复；相关测试、类型/构建、手机与电脑浏览器截图。浏览器使用合成任务夹具，不能宣称新架构真实AI开书已接通。
- 交付：本地可预览界面、验收证据、正式文档原位更新。缺少后端接入如实标记，不把UI完成视作架构切换完成。

## 最终实现与验收

- GPT-5.5实现NewNovelPage.tsx、独立NewNovelPage.css和AuthorApp.test.tsx。主代理要求删除初版多余成员状态/强模型说明，恢复原details选择入口，撤销新增的过期成员自动回退，并限定CSS作用域后验收。原选择持久化、成员过滤与提交逻辑保留；未知phase沿用真实状态文本，不凭UI编造步骤。
- 成员卡为原生radio，头像/姓名可点击，选中绿色且带不影响读屏名称的“已选”标识。默认自动安排保留。工作中只显示当前成员、单条实际状态与原进度数据；团队详情和原始想法可展开。去除重复当前工位、常驻人员列表和重复的编号阶段轨道；阶段仍由实际任务状态呈现。
- 主代理独立执行AuthorApp.test.tsx共56项通过；author TypeScript构建检查通过、Vite生产构建通过、103文件边界检查通过。没有运行无关PG/后台/真实模型测试。
- 浏览器使用明确合成任务夹具，对照修改前同一源版本和候选版，在390及1440宽度分别检查选择/设计/审查，共12张截图；候选6个页面无水平溢出和pageerror。额外验证自动安排默认、离岗过滤、点击头像姓名、键盘ArrowRight、三字姓名完整、刷新保留与所选成员提交参数一致。恢复/重复提交/自己设计等受影响原行为由56项流程测试覆盖。
- 证据：artifacts/rebuild/120/author-tests.log、typecheck.log、build.log、baseline-browser.json、candidate-browser.json及同目录截图。基线从修改前Git源重新构建，避免用旧dist误作当前基线；新样式不影响受保护导航。
- 文档已记录五块功能保留迁移决定，纠正详细规格42.4与最新复用授权的冲突；RB-19整体仍未验证/未发布，不能把本次前端验收当开书后端已迁移。
