# 第93批：全软件重构开发文档

## 合同

- 目标：分段完整阅读老板本次提供的V9，核对当前代码与已有运行记录，形成面向长篇AI小说创作商业产品的详细重构开发评审稿。
- 基线：`5050d43e`，分支 `codex/light-coauthoring-v7`；已有未跟踪 `.claude/` 不动。
- 唯一正文：`docs/REBUILD_DEVELOPMENT_SPEC.md`。本清单仅记录文档编制和验证；不另建重复功能总览。
- 修改范围：新文档、本清单、根与V7 HANDOFF中的文档入口；必要的只读代码和官方技术/法规核查。
- 保留：老板已满意的视觉方向、有效现有用户能力、作者数据与当前生产系统；建议的交互与架构变化均清楚标为评审方案。
- 不做：产品编码、安装依赖、数据库/会员/任务变更、真实模型调用、推送、部署；不把附件或此前讨论直接认定为获准实施规格。
- 验收：覆盖V9所有63个编号章节的去向；产品与交互、卷链章职责、AI与上下文、版本/事实/时间、任务/计费/恢复、API/数据/架构、后台/商业/迁移/验收均有可实施定义；检查矛盾、示例边界、来源、Markdown结构和链接。
- 验证：文档检查与完整差异审查；不运行应用全量测试，不能声称设计已经实现、压测或文学验收通过。

## 结果与验证

- 通过：完整分五段阅读本次V9，实际2,224行；原63个编号章节逐项映射到新稿。
- 通过：交付 `docs/REBUILD_DEVELOPMENT_SPEC.md` 评审稿1.0，10部分、64章、1,809行、58,435字符（包含Markdown与代码名），包括现有能力去向、卷/链/章分工、作者交互、事实/时间/修订、Agent与任务、数据/API、商业、迁移和验收。
- 通过：开发/产品/UI/长篇质量Skill和相关参考已读；当前导航、SQLite初始化、Fastify入口、规划/创作合同、API内异步启动、定稿事务outbox与Worker等关键机制已核对。现有功能台账仅作为核查索引，未当作运行验收。
- 通过：官方核查PostgreSQL领取锁、RLS、PITR及正式发布的生成式AI/标识规定，引用在相应章节；供应商合同、实际资质与具体适用条件仍待核实。
- 通过：主稿64章顺序、V9-01至V9-63无遗漏重复、27处本地文件链接、10个目录锚点、表格列数、代码围栏、编码与占位文本检查；两份HANDOFF新增入口存在。
- 通过：分段审阅新增主稿完整Git差异，核对交接差异；修正终态重试语义、过渡双库计费、故事时间不能统一UTC、导出文件哈希、历史资料访问和对V9修缮条件的过度简化。补齐手动建书、全书完结、导入与多主笔细节。
- 范围：仅主稿、本清单、两份HANDOFF，共4份文档。已有 `.claude/` 不处理。最终暂存差异检查通过后本地提交，提交号在交付回复记录。
- 未验证：新架构、功能、页面、模型质量、成本、容量、合规落地和迁移运行效果。20类故障用例是新稿测试设计，不是本批已运行测试。
- 不适用：应用类型检查/构建/全量测试、真实模型调用、浏览器产品验收、推送和部署。本批没有产品代码、数据库、作者任务或生产变更。
- 交付状态：文档完成，整体重构方案待评审；不把文档交付视为实施授权，线上仍为第90批。
