# 第108批 独立重构基础工程

## 变更合同

- 用户目标与授权：立即开工；GPT-5.5在独立位置重写业务，避免与旧代码冲突。承接第107批架构和执行文档3.1.1阶段B。
- 唯一清单：本文件；状态：第108批阶段B独立基础验收通过，未发布；RB-00整体尚待阶段C验证。
- 允许修改：`rebuild/`内独立工程、依赖锁文件、启动与验证脚本、基础迁移、最小API/Worker、作者与管理员基础外壳；本清单、HANDOFF和执行文档的实际状态。运行文件仅在`.local/rebuild/`，证据在`artifacts/rebuild/`。
- 明确不改：旧业务代码、根依赖/锁文件/工作区/启动命令、旧数据库、生产配置和服务；本批不实现完整账户或创作流程，不进行数据迁移和生产部署。
- 必须保留：当前线上用户服务、账号权益、作者数据及所有正式版本；旧文件和其他任务改动。新工程不能导入、复制或包装旧业务实现。成熟第三方依赖允许使用。
- 实现：Node24/严格TypeScript、React/Vite独立作者与后台入口、Fastify5、PostgreSQL18/pg；数据库参数必须显式配置，限定本地重构库；参数化SQL、同连接事务、可重复且校验历史内容的迁移、脱敏日志和安全错误、自动依赖边界检查。
- 隔离：预定作者43280、后台43281、API43282、数据库54329端口；占用时先核查，不终止其他进程。独立package.json与lockfile；不读取根.env，不回落生产或SQLite。数据库连接不仅校验名称，还核对本地目标及重构环境标记。凭据只来自环境，不提交。
- 验收：独立安装/类型检查/构建/测试与边界检查；真实PostgreSQL空库迁移、重复执行与事务回滚、错误配置拒绝；基础入口真实运行及手机/桌面浏览器检查。不得用mock或SQLite冒充PostgreSQL证据。
- 回归：核对旧业务文件和根依赖无差异、旧生产未操作、新工程无旧业务运行依赖。独立加法工程不改变既有保护数据语义，本批不运行旧工程全量verify:full。
- 风险审查编号：ARCH-108（隔离配置、全新数据库基础事务）。只在新测试库验证；后续账号权限、任务恢复、计费、迁移另外建立对应合同。
- 分工：Astra管理合同、数据库准备和交付审查；GPT-5.5独占`rebuild/`代码。代理交付后主代理检查通过才结束本批。
- 部署要求：仅本地隔离开发与测试，不部署生产。阶段B通过不等于RB-00全完成；阶段C任务恢复基线随后实施。

## 实施与验证记录

- 开始：rebuild目录不存在；旧工作区仅发现预存未跟踪`.claude/`，不触碰。当前HEAD为23d97b8。
- PostgreSQL实际环境：从PostgreSQL官方Windows页链接的EDB二进制页面下载18.6-3 Windows x64 ZIP，解压到`.local/rebuild/runtime/pgsql/`；postgres --version及实际SQL均返回18.6。下载包SHA256为`59F8CE701C63C2ED623C665A5E51B3EF6F2E37CCF837B68FFEED0742D0AE6ABD`（本地记录值，不声称已对比发行方签名）；保留发行包许可证。
- 真实集群：项目内pgdata，SCRAM密码验证，仅监听127.0.0.1:54329，无系统服务安装和全局PATH变更。沙箱pg_ctl的受限令牌失败后，获准以隐藏本地进程启动，pg_isready与实际SQL连接通过；未连接生产。
- 已配置独立开发库与随机后缀测试库；app/migrator分角色，均非superuser且无CREATEDB/CREATEROLE/REPLICATION/BYPASSRLS。环境标记由bootstrap单独建立，业务角色只有读取标记权限。凭据仅在忽略的本地private目录供设置进程环境，未输出或提交。
- 依赖工具：隔离目录npm12.0.2来自官方registry，下载包SHA512与registry integrity核对通过；Node24.19.0使用机器已有运行时，不修改旧工程依赖。
- 初步审查已交GPT-5.5修正：禁止连接URL参数绕过目标限制；禁止迁移自行标记陌生数据库；迁移锁提前、核对缺失历史迁移；API/Worker启动应验证真实数据库，Worker必须保持实际运行；前端响应使用运行时合同校验。
- GPT-5.5交付：50个源文件/配置/锁文件/说明文件，均位于rebuild；独立作者/后台、编译后API/Worker、Zod合同、pg事务/迁移、安全配置/日志/错误、AST边界检查、数据库初始化SQL与README。未复制或导入旧业务实现。
- GPT-5.5验证：边界检查26个源码文件、类型检查、13项单元测试（含5项边界负向夹具）、两个Web构建通过；真实PostgreSQL重复迁移/事务回滚2项通过；npm audit --omit=optional报告0漏洞（不表示以后永久无漏洞）；Worker ONCE启动通过。
- 初始化脚本审查：修复psql在DO美元引号中不替换变量、inet转text带掩码、\\quit额外退出码被忽略的实际兼容问题；使用真实地址/端口、限定名称/角色、已有库拒绝和ON_ERROR_STOP失败退出。环境标记保持bootstrap所有，app/migrator只能读。GPT-5.5已实测新库成功、已有库和非法名称拒绝，并在脚本新建库执行PG测试。
- 主代理独立PG验收：第三个随机测试库中8项通过——并发首次迁移只应用一次、校验和篡改拒绝、缺失历史迁移拒绝、失败迁移DDL及记录回滚、参数化提交/业务异常回滚、错误标记拒绝、app角色读就绪检查、URL参数/片段绕过拒绝。证据在artifacts/rebuild/108/pg-review-results.json；最终编译文件与被验证版本哈希一致。
- 实际运行：开发库首次迁移通过；编译后的API监听43282，作者/后台Vite分别监听43280/43281；Worker编译后启动且持续运行，无任务执行器和真实模型调用。数据库保持专用54329。未操作旧生产进程或作者任务。
- 浏览器验收：Edge真实浏览器在390/1440宽度检查作者/后台，无非预期横向溢出和页面运行异常；两端模拟503与错误合同响应显示安全提示，点击重新检查恢复真实API状态。证据为artifacts/rebuild/108/browser-results.json及截图；作者手机标题孤字换行已缩短修正。此处只验收基础外壳，不表示产品业务界面定稿。
- 干净安装：代理在运行中的原目录尝试npm ci遇到Windows预览二进制占用，未停止其他进程；主代理另建.local/rebuild/clean-install-108，不携带node_modules或dist。独立副本npm ci --offline按锁文件成功安装169包，再执行verify：边界、类型、13项单元测试、两个Web构建通过。npm12默认阻止esbuild安装脚本，平台预编译包仍实际构建通过，未盲目开放所有安装脚本。副本50个来源文件与最终rebuild逐文件哈希一致，锁文件SHA256为CF8763A9FC91497264D2755BB46191CA74FAB54AB3136AFDCB98E195BD4BA0B5。
- 依赖隔离实测：fastify/pg/react/react-dom/zod/typescript/vite/vitest的解析路径均位于rebuild/node_modules；未借用旧根依赖。新工程使用自己的workspaces和锁文件。
- 最终审查：含可选依赖的完整npm audit报告0漏洞；锁文件所有外部包来自registry.npmjs.org，没有跨旧工程file依赖。53个提交文件限于rebuild的50个文件、HANDOFF、执行文档与本清单；无旧业务/根依赖/生产文件变化，暂存内容未包含本批生成的数据库凭据。SQL工作区与Git暂存字节一致，LF规则生效，避免换行影响迁移校验和。git diff --check通过。
- 文档解析：现有后台解析器只读检查通过，版本1.6、80个单元、85项来源，RB-00前端基础已实现、后端开发中、整体验收未验证/未发布；该检查使用旧后台解析器验证文档兼容，不是新工程运行依赖。最终作者390宽度标题及真实状态复查通过。
- 交接：阶段B通过；阶段C合成任务领取/检查点/租约/取消/重启与未知结果恢复尚未实现，RB-00整体仍未验证。账号、创作、模型调用、旧数据迁移、商用容量和生产发布均不在本批完成范围。
