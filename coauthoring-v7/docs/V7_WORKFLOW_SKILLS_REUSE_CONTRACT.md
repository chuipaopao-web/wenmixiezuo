# V7 开发工作流、产品决策 Skill 与代码复用规则任务清单

任务编号：`V7-RULES-20260823-02`

状态：完成

本次唯一清单：`coauthoring-v7/docs/V7_WORKFLOW_SKILLS_REUSE_CONTRACT.md`

## 变更合同

```text
任务类型：规则、Skill 与正式文档维护
历史问题：长开发任务曾缺少强制拆批、唯一清单定位、阶段结算和停止条件；当前已由开发流程、V7代码边界和产品决策Skill固定执行方式，旧版源码不再是当前代码库。
复现步骤：一张开发清单可以容纳多个独立目标并连续执行很久；任务恢复时可能依赖对话记忆检索清单；写新功能前没有强制留下 V6 和公开实现复用判断。
期望结果：增强现有开发工作流；新增产品决策 Skill；固定 V7 新代码库、复用优先、公开源码商业安全和最终替换 V6 的规则。
允许修改：.agents/skills/wenmi-development-workflow/SKILL.md、docs/DEVELOPMENT_WORKFLOW.md；新增 .agents/skills/wenmi-product-decision/SKILL.md；coauthoring-v7/AGENTS.md、HANDOFF.md、docs/DECISION_BOARD.md、docs/BACKEND_ASSET_BOUNDARY.md；本清单；直接相关 Skill 评测仅在现有结构确有承载方式时新增。
明确不改：产品功能代码、V6 代码、数据库、接口、部署配置、当前生产、UI/UX Skill、长篇质量 Skill 和其他既有文件。
必须保留：现有未提交修改；V7 产品设计自由；作者数据与商业安全底线；未获“开始开发”授权不编码、不部署；旧系统在 V7 完整验收和安全切换前继续保留。
新功能验收：开发批次只有一个主要用户结果和独立回滚单元；任务清单唯一定位并使用 REQ/IMP/ACC/EVD 映射；长任务逐批结算；写代码前执行 V7、V6、现有依赖/官方实现、合适公开源码的复用审计；新产品决策 Skill 只引导讨论、推荐、确认与沉淀。
原功能回归：开发工作流原有变更合同、范围守恒、按风险测试、差异审计和安全门禁仍保留；其他两个项目 Skill 零修改；不把 V6 产品结构或公开源码变成 V7 设计约束。
测试范围：Skill 结构快速校验；关键触发与排除场景检查；文档链接和状态检查；Git 修改文件列表、diff stat 与完整差异；不运行应用测试、构建或 verify:full。
部署要求：不部署、不改生产。
```

## 需求—实现—验收—证据映射

| 需求 | 实现 | 验收 | 证据 | 状态 |
|---|---|---|---|---|
| `REQ-01` 保留并增强开发工作流 | `IMP-01` 在 Skill 入口和唯一流程源中增加拆批、定位、复用与阶段结算 | `ACC-01` 长任务不能以一张巨型清单连续推进 | `EVD-01` Skill 快速校验通过；D01、D05、D06 行为案例已建立 | 通过 |
| `REQ-02` 解决清单检索不准 | `IMP-02` 固定任务编号、唯一清单路径及 `REQ/IMP/ACC/EVD` 映射 | `ACC-02` 恢复任务不依赖模糊搜索或聊天记忆 | `EVD-02` 流程源含唯一清单模板、编号映射和恢复规则 | 通过 |
| `REQ-03` 新增产品决策 Skill | `IMP-03` 新建 `wenmi-product-decision` | `ACC-03` 只处理产品讨论与老板确认，不取得开发权限 | `EVD-03` Skill 快速校验通过；P01—P06 路由案例已建立 | 通过 |
| `REQ-04` V7 新代码库与复用优先 | `IMP-04` 更新 V7 本地规则、资产边界、交接和决策看板 | `ACC-04` 写功能前必须检查既有 V7、V6、成熟依赖/官方实现和合适公开源码 | `EVD-04` 四份 V7 当前文档已同步并通过一致性检查 | 通过 |
| `REQ-05` 减少重复代码和 Token | `IMP-05` 固定复制、微调、依赖复用、最小新写的选择顺序 | `ACC-05` 不为省 Token 跳过语义、许可证、安全和测试核查 | `EVD-05` 开发流程与 V7 规则均含复用顺序和公开源码门禁 | 通过 |
| `REQ-06` 最终替换现版 | `IMP-06` 固定完整验收、兼容、迁移、回滚后再切换 V7 | `ACC-06` 讨论期不提前删除或影响 V6 | `EVD-06` V7 规则、交接、决策和资产边界均明确安全切换路径 | 通过 |

## 范围与验收清单

- [x] 既有未提交修改已识别并保留。
- [x] 开发工作流 Skill 与唯一流程源职责一致、没有复制两套详细规则。
- [x] 大任务拆分、唯一清单定位、阶段性结算和重复失败停止条件已建立。
- [x] 每个功能写代码前的复用审计及最小实现顺序已建立。
- [x] 公开源码具备许可证、来源版本、安全、维护性和商业使用门禁。
- [x] 产品决策 Skill 触发和排除范围清楚，不与现有三个 Skill 争夺职责。
- [x] V7 在 `coauthoring-v7/` 建立新代码库、不直接依赖 V6 产品层代码的规则已建立。
- [x] V7 最终替换当前版本但保留安全切换和回滚阶段的规则已建立。
- [x] 未修改产品代码、V6、数据库、接口、部署配置、UI/UX Skill 或长篇质量 Skill。
- [x] Skill 校验、文档核对和完整差异审计通过。
- [x] 未运行应用测试、未部署。

## 未验证项目

- 独立 Codex 前向行为评测：未验证。当前没有老板授权的子代理评测；本次只完成 Skill 结构校验、路由案例建立和规则一致性检查，不冒充独立行为证据。
- V7 功能运行、浏览器交互、构建与部署：不适用。本次未修改产品代码，也未获得 V7 功能开发授权。
