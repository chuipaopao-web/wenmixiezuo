# 小说平台开发协作规则

## 权威顺序

发生冲突时按以下顺序执行：

1. 老板最新明确决定；
2. 本文件与 `docs/PROJECT_CHARTER.md`；
3. `docs/DECISIONS.md` 中仍然生效的决定；
4. 当前版本的产品、架构、数据、记忆、API、路线图和验收文档；
5. `docs/CONSENSUS_LEDGER.md` 的24条完整共识；
6. `docs/FINAL_SOLUTION.md`；
7. `docs/SOURCE_REQUIREMENTS.md`；
8. 旧聊天和口头进度。

旧决定被新决定替代后，只保留追溯价值，不得继续驱动开发。

## 当前授权边界

- 老板已于2026-07-19明确授权长篇终局release连续开发，活动 `release_id` 为 `wm-longform-r1-20260719-003435-e4d7b8b7`。
- 当前Codex可以按冻结的八阶段施工合同创建源码、依赖、数据库迁移、测试、运行服务和验收证据；普通安全、可逆、零现金技术决定不再逐项请示。
- 只有实际付费、新密钥/登录授权、永久删除、重大架构变更、生产数据恢复或无法自行消除的外部阻塞才暂停升级老板。
- 小说平台必须位于 `D:\wenmixiezuo`，不得在 `D:\AI智囊团` 内实现，也不得把AI智囊团作为运行时依赖。
- 不得停止、重启或修改AI智囊团服务。

## 开工前必读

1. `AGENTS.md`
2. `docs/PROJECT_CHARTER.md`
3. `docs/DECISIONS.md`
4. `docs/PRODUCT.md`
5. `docs/ARCHITECTURE.md`
6. `docs/DATA_MODEL.md`
7. `docs/AGENT_SYSTEM.md`
8. `docs/MEMORY.md`
9. `docs/LONGFORM_QUALITY.md`
10. `docs/API.md`
11. `docs/DEVELOPMENT_ROADMAP.md`
12. `docs/ACCEPTANCE.md`
13. `docs/ULTRA_LONGFORM_CONTINUITY.md`
14. `KNOWLEDGE.md`
15. `TASKS.md`

只在需要核对细节或争议时读取 `docs/CONSENSUS_LEDGER.md` 对应条目，禁止把24条全文默认注入每次模型调用。
涉及记忆、切片、Wiki、检索、上下文或混合RAG的设计与实现还必须完整读取 `docs/HYBRID_RAG_DESIGN.md`。
涉及正文、设定、规划、事实、Wiki、声音证据或临时资料的切片、重切和索引快照时，还必须完整读取 `docs/CHUNKING_DESIGN.md`。
涉及架构、数据、记忆、RAG、上下文、Agent、生成、审校或长篇质量的重要方案，还必须完整读取 `docs/DESIGN_GOVERNANCE_AUDIT.md` 并沿用其 `design_review_id`、Skill快照和两轮审查合同。

## 开发原则

- 技术栈固定为 React、TypeScript、Vite、Node.js稳定LTS、Fastify、SQLite、LanceDB OSS TypeScript本地嵌入投影、REST与SSE。
- 采用本地模块化单体加独立Worker，不使用微服务、WebSocket或需要独立运维的向量/图数据库服务；按DEC-012使用LanceDB OSS TypeScript本地嵌入模式保存可重建向量投影，关系和Wiki继续由SQLite正式源派生，任何投影都不得成为正史源。
- 最终单书规模按500万规范化中文字符、1500章设计；模型上下文必须与全书总字数解耦，Agent长期陪伴不得依赖全量聊天或常驻模型进程。
- 服务只监听 `127.0.0.1`。
- 业务层只通过Repository访问数据；Worker不得直接修改核心业务表或正式正文。
- 所有核心记录从第一天携带 `owner_id` 和 `book_id`；跨书查询必须显式携带隔离键。
- 数据库迁移只能向前新增，已合并迁移不可修改。
- 正文使用不可变完整版本；任何修改产生新版本，禁止静默覆盖。
- 知识生命周期使用同一SQLite权威下的临时、候选、正史、派生四层；作者可见原始内容默认归档而不自动永久删除，只有可重建投影、缓存和已校验碎片可以按保留策略自动清理。
- 切片采用不可变原始资料、结构节点、父子检索块和原子事实；默认不复制文本重叠，使用邻接与有界扩展。任何重切片创建新快照并原子切换，不修改原始资料或正史。
- 超长篇旧阶段默认使用版本化阶段结算；实体、开放线程、规则、因果或冲突等关键信息触发时，按卷、故事弧、章节/场景逐级回查正史原文。摘要只是可重建投影，不能成为事实权威或固定章数遗忘规则。
- API Key只从环境变量读取，不得进入数据库、日志、聊天、上下文、备份、导出或Git。
- 普通删除为归档；永久删除必须显示影响且只对已归档书开放。用户输入 `YES`（大小写不敏感、忽略首尾空白）并再次点击“彻底删除”后才可提交；服务端仍必须校验归档状态、原子清理全书数据并写删除墓碑。

## Agent与模型规则

- 历史首版数据保留9岗位审计记录；活动长篇release按DEC-021固定为11名当前成员，并按DEC-026使用可版本化模型绑定：默认DeepSeek＋GLM双编剧，Kimi可替换一席，豆包不讨论剧情；正文使用事实/文学/体验三职责、三异模型点评，GLM写手时事实席用DeepSeek，详见 `docs/AGENT_SYSTEM.md`。
- DEC-027在11名创作成员之外增加一个本地工具型角色“小文秘书”；它不属于创作席、剧情席或点评席，不能以头像、提示词或本地模型冒充第12名创作Agent或异模型意见。
- DEC-038将面向老板的“系统”身份并入小文秘书：普通聊天中的受理、任务、附件、导航和故障说明统一显示为小文秘书；`system` 只能作为内部事件来源和历史兼容值，不能继续以独立名称或头像发言。点名成员和创作成员仍直接回复，不得由秘书统一代言。
- 岗位、Agent实例和模型配置必须解耦；同一模型可以承担多个岗位，但必须如实显示共同模型来源。
- 同模型的两个岗位不能冒充异模型独立复核。
- Agent状态必须由真实任务、心跳、模型调用和工具事件驱动，禁止根据口头回复伪造“工作中”。
- 任何模型调用都必须生成可追溯的上下文包、调用记录和预算记录。
- 不保存或展示模型内部思维链，只保存任务步骤、依据、检查结论和结果。
- 老板明确点名成员时必须直接路由给该成员；明确讨论剧情时，小文秘书只能保留原话、识别实体、准备最小资料并启动主编主持的双异模型编剧会话，不能代替创作成员给剧情结论或用摘要遮蔽老板原话。
- “小任务”按权限、风险、可逆性和创作影响判断，不按消息长度判断。确定性代码优先于本地小模型；本地小模型只能产出受Schema与来源校验的候选，不能直接修改正史、正式正文、重大规划或模型绑定。

## 长篇创作质量Skill门禁

- 最高约束是同时降低长篇系统性失败并保护创造性、人物生命力和最终输出质量；不得为了提高一致性、召回率、规则遵循或审校分数，把软倾向变成硬规则或把正文压平成机械执行结果。
- 设计、比较、修改或复盘记忆、正史、RAG、Wiki、上下文、分层规划、Agent协作、章节生成、审校、作者偏好和长篇质量验收前，必须完整读取并执行 `.agents/skills/wenmi-longform-quality/SKILL.md`。
- Skill必须在形成推荐方案前路由和读取，不能等方案写完后只做格式审查。重大方案建立唯一 `design_review_id`，记录适用Skill的名称、版本或文件哈希、读取的参考、已确认事实/推断/老板偏好/未知项、替代方案、两轮审查、证据等级和回滚条件；完整合同见 `docs/DESIGN_GOVERNANCE_AUDIT.md`。
- Skill是开发设计与验收约束，不是更高事实权威，也不默认进入产品岗位提示词或每次小说对话。Skill更新只触发受影响决定的定向复核，在新决定和当前规格生效前不得静默改变旧实施合同。
- 发生人物漂移、剧情失控、检索错误、文风同质、重复、迎合或跨章质量下降时，必须用该Skill从真实代码、数据、上下文包和调用证据建立故障基线。
- 普通事实问答、纯文案修改、低风险样式调整和明确机械修复不触发完整审查，避免分析瘫痪。
- 每项重要方案必须建立“痛点—症状—根因—机制—反例—指标—证据—停止条件”追踪链；禁止以“使用向量库、多Agent、长上下文或更强模型”替代根因分析。
- 区分开放讨论、创意推演、暂存试写和正式生产；前三种不进入正史且不得被完整生产链过度阻断，只有正式生产强制准备、审校和结算。任何模式仍遵守版权、安全、费用、数据隔离和不可逆操作门禁。
- 任何改变检索、上下文、规划、提示词、生成、审校或岗位经验的机制，都必须按影响范围相对旧流程或弱治理基线进行创造性/输出质量非劣效验证；局部可逆修改做针对性多样本对照，岗位默认或全链路改变做跨任务/纵向盲评。一致性提升不得抵消核心创作维度下降。
- 固定同义词及同源测试不能证明语义或混合RAG；连续5章只能证明短流程运行。长篇质量表述遵守 `docs/LONGFORM_QUALITY.md` 的E0—E4证据门禁。
- Skill发现重大规格缺口时先更新当前规格，再修改业务代码；安装Skill本身不得冒充产品运行时能力完成。

## 任务与文件规则

- 首版默认由当前Codex单独开发、测试和整理文档，不调用其他开发Agent，也不把代码任务派回AI智囊团。只有老板以后明确授权增加开发成员时，才允许改变这一规则。
- 小说创作产品中的Agent岗位属于产品运行时成员，不等于参与编写本项目代码的开发成员。
- 正式开发使用唯一 `release_id` 连续推进。
- 每项开发任务必须有：目标、不做什么、唯一负责人、允许与禁止文件、依赖、实现约束、验收标准、测试命令、停止条件、回滚方法和复核人。
- 同一文件同一时间只有一个写负责人。
- 数据库迁移、公共API、共享类型、核心编排和安全机制始终串行。
- 阶段通过不等于口头完成，必须保存测试、迁移、运行、恢复和文档证据。
- 开发中发现规格缺口时先更新文档；不得自行猜测重大产品决定。

## 质量门禁

每个阶段必须执行并保留证据：

- 类型检查；
- 自动测试；
- 构建；
- 迁移空库与升级验证；
- 本地运行验证；
- Repository契约测试；
- 与该阶段相关的故障恢复和跨书隔离测试；
- 与长篇质量相关的独立金标、对抗查询、消融、上下文位置、纵向缺陷和反迎合测试；
- 与旧流程或弱治理基线的创造性非劣效测试，包括原创性、合理惊喜、人物可辨识度、情绪力度、检索锚定和整体阅读质量；
- 文档和覆盖矩阵同步。

最终完成标准只以 `docs/ACCEPTANCE.md` 为准。执行者不能自行宣布整个产品完成。

## 文档变更规则

- `docs/SOURCE_REQUIREMENTS.md`、`docs/FINAL_SOLUTION.md` 和 `docs/CONSENSUS_LEDGER.md` 是来源快照，不直接篡改。
- 老板的新决定写入 `docs/DECISIONS.md`，并同步修改所有受影响的当前规格。
- 每次重大修改必须记录决定编号、旧规则、新规则、影响文件和生效版本。
- 文档只写清楚已经决定的内容；不得留下无法执行的占位承诺。
