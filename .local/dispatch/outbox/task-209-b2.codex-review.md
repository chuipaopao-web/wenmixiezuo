# R209-B2首次Codex验收：不通过，暂不合入

2026-09-13，隔离commit 9fde49ac，基准e4c867f6。独立复跑六文件39项测试通过（exit0，24.27秒），不代表失败路径合格。以下均限定B2合同；不要求重做已通过功能。

## 阻断问题与证据

1. **审核/管理写入非原子**。routes约274行先reviewRevision再recordReviewOpinion，报告也明确承认分开落库。Codex临时DB故障触发器让review审计INSERT失败：HTTP500，但revision已是reviewed。违反状态与审计同事务合同；同样核对创建、修改、退役、恢复，不只补review。
2. **发布成功与请求记录存在断裂窗口**。routes约398–404行service.publish先提交，再更新请求记录和审计。Codex让请求表UPDATE失败：HTTP500，active已指向新release、request.release_id=null；移除故障同键重试409，不能恢复正确结果。补偿只覆盖publish抛错，无法回滚已提交的发布。不接受“内部有事务所以只能拆开”的理由，应由应用层统一事务/仓储原子操作或可恢复协议解决。必须有故障注入断言。
3. **用途筛选漏掉全部创作参考**。admin仓储listSummaries只查`$.method.usageTree`。合成reference的facets.purposes包含“人物与关系”，全列表1条，按该用途筛选0条（HTTP200）。必须按类型匹配reference用途，并覆盖用途父/子路径定义，不能只测method。
4. **发布关系编辑未闭环**。UI约791–801行移除条目自动过滤相关边；替换revision时保留旧边版本，缺少显式修改关联版本的入口。任务要求关联变更明确处理，不能自动删边。需支持增补/更换/移除关系的明确操作和预览，替换关联条目后管理员能完成有效发布；保持未动关系不变。保留原类型，不能用类型断言代替参数验证。
5. **HTTP冻结清单分页缺失，冲突合同不一致**。GET releases/:id忽略query直接全量返回；未接B1分页/游标校验。报告的陈旧active400不符合任务409；修正相应错误映射及测试，不按现状放宽合同。seenAvailability目前可省略，不满足必传所见状态的防覆盖约束；需原子校验所见状态与版本。
6. **离开脏表单与视觉验收缺口**。DraftEditor仅beforeunload，取消直接onCancel、返回列表直接onBack，应用内离开会丢输入而无提示。补应用内路径。Codex打开10-mobile-390-list.png，图像为多段重复页头且横向内容截断，不能作为正常390页面通过的证据；可能是截图管线问题，不能仅据此断言产品布局坏。重新提供干净视口截图并目视核查，真实滚动/按钮可达与截图证据均需成立。

独立探针：`.local/dispatch/outbox/task-209-b2.codex-probe.mts`。在隔离根运行`node --import tsx D:/wenmixiezuo/.local/dispatch/outbox/task-209-b2.codex-probe.mts`，只临时合成库，无生产/模型。输出如上三例，exit0表示探针执行完成，不表示产品通过。

## 范围和报告纠正

- 新admin仓储和质量脚本不在原白名单；返修任务明确允许前者，本轮质量脚本仅真实计数适配可保留，不扩大质量门禁改动。不要通过改计数掩盖证据缺失。
- SQL/恢复/发布事务在HTTP路由中编排违反已定分层；移入有权限的应用服务，路由只认证/解析/映射。授权允许相关B1模块必要增量，不另建身份/DB。
- 总规格未带入主目录最新19.6退出规则，第20节不得用本批简报覆盖正式合同。补实施记录时保留最新规则；未见旧依赖退出清单，需补。
- “0123无需停机窗口特殊处理”不能替代DEPLOY：API/worker发布仍按在途与迁移安全要求，不由GLM自行上线。
- 目前不再扩大到全量测试/模型评测；先补上述反例，保留既有通过证据。未合入、未部署，B2仍未完成验收。
