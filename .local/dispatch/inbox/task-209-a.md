# R209-A：全题材创作库实施前核查（唯一当前任务）

状态：待老板手动触发；执行者GLM5.3。你是本批执行者，不是产品重新设计者。仅执行本文，不自动继续R209-B—F。

## 1. 基准及范围

项目：D:\wenmixiezuo。
业务与规格核查基准：a29fdb75707b98c762cbc05f08d2980bb986ce60。
后续仅更新派工/交接说明不要求回退HEAD。开始时记录当前HEAD与git status；若基准之后有业务代码变化，核对是否影响本批，冲突部分报告待复核，不覆盖或reset。

本批是只读审计与限定本地探针，不改产品代码，不建立库，不修改线上提示词。无需创建工作树；所有输出限定到下列新文件，产品目录保持原样。若探针需要安装、编译改写产品目录或变更源码，改为记录阻断与建议，不自行实施。

唯一可写路径：
- D:\wenmixiezuo\.local\dispatch\outbox\task-209-a.result.md
- D:\wenmixiezuo\.local\dispatch\outbox\task-209-a\ 下的inventory.json、coverage.md、probes/及证据日志。

不改任务书、总规格、HANDOFF、旧报告、任何业务/测试源码、锁文件或共享依赖。不得提交、推送、合入、SSH、部署、修改数据库或启动监听。不要调用模型API/外网、新增收费服务、读取.env或真实作品数据库。报告不含密钥或作者私人内容。可直接读取项目已存在的脱敏测试样例、代码与本地已装工具。

## 2. 阅读顺序

1. D:\wenmixiezuo\AGENTS.md与HANDOFF.md当前记录；本任务以当前授权的只读范围为准。
2. docs/REBUILD_EXECUTION_PLAN.md当前状态、docs/REBUILD_DEVELOPMENT_SPEC.md阅读导航及后端运行边界。
3. coauthoring-v7/docs/worklists/CREATIVE-LIBRARY-209.md：先1、8—11、14、18、19，再按核查需要读其余内容；第17节是待接入提示词，不表示已上线。
4. coauthoring-v7/docs/worklists/OPENING-APPEAL-208.md第13节真实验证记录及相关代码。
5. .local/dispatch/README.md；涉及某目录时读取适用的局部AGENTS。

不要把旧V7历史设计作为最新要求，也不要看到路径带v7就删除或跳过真实运行包。

## 3. 必须完成的核查

### A1 真实运行与新后端边界
从根package.json工作区、包导出、apps/api与apps/worker导入追到实际执行代码。覆盖开书compiler、设定总整理、资料短卡、故事线推荐、全书selectMethods，以及后台资产供应。
入口线索：rebuild/packages/backend/src/legacy-opening、rebuild/packages/agent-catalog、rebuild/packages/time-machine-core、coauthoring-v7/backend、apps/api/src/application/books、coauthoring-v7/admin-console。
输出入口→应用服务→运行包→存储/模型网关的简短表，每行附文件及行号。标明已挂载、兼容适配、仅存在未挂载、未核实。检查总规格第11/19节能否按该真实边界接入；发现差异只报告，不另造新架构。

### A2 旧资产全量清单
核对创意参考与方法真实定义、别名、层级及供应注册；按实体与版本去重统计，避免编译副本、跨层复用被算成不同方法。
inventory.json每项记录实际id、已有编号/名称、来源位置、用途/层级、是否运行可用及证据、重复候选/待核查原因。不能给未核实项编造ID或完成状态。
给出总数及口径、原条目去向建议：保留、可能合并（须语义依据）、缺信息、供应未接通。编号方案按法/参稳定编号执行，但本批不真正分配编号、不删改资产、不批量改分类。
coverage.md映射总规格用途分类、题材覆盖，明确没有内容或无法确认的项；不为了凑38类生成新卡。

### A3 R208目录外标签复现
已知合成日常样例曾输出标签“温暖”，新增coreAppeal/readingTone有内容，但完整解析失败。此前补交探针没有充分验证真实引擎纠错，不能先入为主认定根因。
查输出schema、标签目录、解析器、修复反馈、任务快照路径。使用合成fixture及可控假网关在本地验证：首次无效标签→引擎实际反馈→模型模拟纠正→最终解析。可以在probes目录写独立测试；必须导入实际运行模块，不复制一套解析逻辑伪造通过。若只能静态分析或已装依赖不能安全运行，明确未动态复现及原因。
至少区分：合法标签；readingTone="温暖"但tags有效；tags含目录外“温暖”；修复后合法；持续无效时终止/保留状态。给最小修复建议及精确文件范围，不改业务代码，不通过放宽校验掩盖错误。

### A4 检索基线与下一批建议
核实当前是否真有成员查询→工具回送→再设计循环，还是一次性全集注入/固定选择。逐阶段标明，不将全书方法调用推定为故事线已支持。
用现有真实方法建立小型标注查询集：编号、别名、口语需求、跨层方法、融合需求、否定约束、无适用方法。只标真实相关条目；若检索接口可本地纯内存运行则测召回，否则交基线样例与未运行原因。不得宣称已完成全题材语义评测。
列出B批可执行的准确文件建议与依赖；未证实目录先建议，不提前创建。执行计划服从现规格，不新增向量服务或切数据库。

## 4. 自检与交付

开始和结束各记录git status与HEAD，确认产品跟踪文件未改变；已有未跟踪.claude/.local不是本批创建的全部内容，不清理。
报告包含A1—A4逐项结论、实际数量/口径、文件行号、实际命令与输出摘要、运行测试/静态检查/未验证分别列出、失败日志位置、必要修复建议及越界项。模型质量本批未测必须明说；模拟网关通过不等于真实模型通过。
inventory引用路径应能找到；计数与coverage一致；同义合并建议附机制相同的证据，不按同名自动合并。检查本批无源码改动、无部署、无模型调用。
结束写task-209-a.result.md；回复老板“R209-A已完成核查，请交Codex验收”，附未完成项。如受阻也交已有结果，不无限尝试；同一失败最多追加两次有明确理由的尝试。不要自动接B批，不要求老板判断代码或自行设计技术路线。
