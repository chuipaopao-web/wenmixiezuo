# REBUILD-CLOSEOUT-01：整体重构连续执行

状态：2026-09-15总体路线保留，当前仅下方S1-A具体实施合同可执行。Codex已核对9d24ffdf代码入口；S0不继续返修，S2—S6不在本次派工内。没有自动派工通道。

## 当前执行边界（覆盖下方旧的整包接续要求）

### 9523942a复核通过：K3连续完成最终候选验证（2026-09-16，不直接切换生产）

Codex独立foundation+cutover76/76通过（1.32秒），接受legacy-opening四文件必要同步及v7-backend/opening-runtime本地链接修正，不需追认等待。现网已通过授权SSH只读核实：RELEASE_ID=wm-v7-20260914-151504-a7614958，apps指向同版source/apps，API/Worker active，磁盘5.7G可用（49G总、88%使用）。这只是当前快照，实际发布前仍须实时复核备份口径/版本/在途，不以5.7G直接推断备份足够。

不要再写一份服务器只读操作清单。现在以9523942a已提交源码连续完成以下工作，无需逐步等老板确认：
1. 在独立目录git archive解包共同候选，使用已指定Node24.19与配套npm，不绕过engines。解析所有运行时workspace依赖实际路径，必须落在该归档目录内；不得读主区或工作区旧dist。time-machine-core源码已有retryRunFailed，不把主区未提交文件复制进候选；当前开发node_modules仍指主区core，不能以开发目录测试冒充独立候选证据。
2. 完成共同候选构建、静态manifest验证及0126迁移重复执行；旧版回退包保留新增迁移文件，在已迁移隔离库验证旧API启动/核心读取。复用已有组装脚本，不新增平行部署实现；不改主区、不写生产数据库、不停生产服务。
3. 真实通道只做相称探针：优先复用已配置的Agent凭据与实际ModelAdapterFactory/ArkPlanModelAdapter，输入只用合成数据。先验证实际Messages路径及GLM Chat专用路径；最多每个待发布文字模型一次、合计8次小请求，单次可见输出上限256 tokens，推理余量按适配器合法下限记录，单请求90秒，总10分钟上限；遇鉴权/套餐/协议错误停止，不自动换通道或追加充值。调用统计区分已知/未知用量，不输出密钥、不存思维链。不得仅再跑独立Chat脚本冒充Messages已经验证。
4. 小探针通过后，复用现有s1a-http-real-probe的合成样本做至多一轮端到端验证（预算不得超过原任务已核定上限，启动前把准确调用数/token/时限写入原结果段；无明确已有预算则只完成步骤1—3，不自行扩大）。真实HTTP推荐→确认→方案→审查→通过才允许采用；不强行改verdict、不降低审核标准、不机械连跑。失败按具体节点报告，已完成产物保留，不能用模型未过审冒充成功采用。
5. 在原报告给出最终冻结提交/归档hash/全部依赖路径/构建运行时/迁移回退/通道与真实链路结论。其余7项数据库边界、闭包69项及其他verify:full未通过事项继续准确列出，不能改白名单或略掉。CTX跨题材不在本批，不再扩任务。此阶段只验证，不直接生产切换；通过后交Codex一次性核定实际发布，不逐步停下来问是否继续。

### 18f3c2a0复核通过：K3接手Agent Plan离线收尾与冻结（2026-09-16，不部署）

本批边界缺口关闭：Codex独立材料/选择/Agent配置24/24通过，边界恰剩原7项，未加白名单。旧回退树需补齐0126的结论保留；最终候选仍须在合规Node上验证，不重复仅S1-A的组装预演。

明确移交：此前“不得提交CodexAgent差异”仅对下列本次指定文件解除。K3现在可审查、修正、补测并提交这些已存在的Codex未提交Agent Plan改动，不用等Codex再冻结。其他脏文件（尤其HANDOFF、主区正文/规格、老板删除文件）继续保护。

接手文件：apps/api/src/infrastructure/models/{model-runtime-config.ts,model-adapter-factory.ts,ark-plan-model.ts,volcengine-ark-image-gateway.ts}；coauthoring-v7/backend/{agent-governance/agent-governance-registry.ts,agents/agent-roster.ts,character-memory/character-memory-runtime.ts,creation-runtime/creation-runtime.ts,planning-methods/planning-editorial-runtime.ts}；scripts/quality/{ctx01/run-phase1.mjs,agent-plan-connectivity.mjs}；tests/unit/agent-plan-cutover.test.ts；deploy/.env.production.example；docs/DEPLOY.md。允许修改三个直接相关foundation测试以及必要的同范围反例；不全库替换Coding字样。

具体待办（Codex已实跑定位，不重新大范围侦察）：
1. tests/foundation/model-runtime-config.test.ts 16项中10失败：默认岗位/公开名册切Agent、缺凭据状态、覆盖环境变量与退役ID测试仍按旧Coding语义。按新明确合同更新输入及期望：只Agent可运行，只有旧Coding不可发新请求，显式subscription缺凭据不得退夹具，无凭据且未显式启用仍可离线开发。有效Agent覆盖变量中的退役模型应拒绝；旧Coding配置不承担新模型覆盖。保留非法端点拒绝等安全反例。
2. tests/foundation/ark-plan-model.test.ts 46项中4失败：前3项GLM开书/审查夹具仍用coding端点；第474行起旧Agent通用“直出”矩阵包含novel_reviewer，与新专用Chat审查冲突。对已授权Agent的GLM开书/审查分别验证/api/plan下Chat URL、低推理、messages与inputContext同源、用量、截断分类和不输出思维链；其他任务仍验证Messages路径。不能仅改成返回{}或删断言使测试变绿。旧Coding实例不代表有效工厂路由，工厂拒绝旧冻结任务的测试保留。
3. tests/foundation/volcengine-ark-image-gateway.test.ts 6项中1失败：旧“专用凭据优先”与所有模型统一Agent新要求相反。验证双密钥时选Agent、仅专用密钥不可调用、失败不回退按量；不执行真实生图。
4. DEPLOY第129行后的普通岗位Coding/专用图片优先文字仍与新表格冲突，原位改为统一Agent；凭据只环境变量，绝不写入代码、报告或归档。CTX继续新证据目录，不能混算旧套餐证据。
5. 改动前记录指定文件的现有diff，保留功能语义；完成后原三套68项、新Agent配置4项及受影响S1定向套件、backend构建/API类型验证。失败逐项查原因，不统称旧债、不放宽门禁。其余7项数据库边界另行保留未通过。
6. 使用明确Node24.19路径 C:/Users/MSIK/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe；先输出版本，配套npm入口先定位。不得再用engine-strict=false掩盖不合规运行时。只此版本下冻结共同候选，记录提交与准确文件清单，按已有授权推送留存，不部署。

本次不需要新真实模型调用：已有八文字模型小探针仅证连通，Messages实际路径、长任务自然过审/HTTP采用及CTX尚未证明；冻结工程候选不冒称这些完成。通道收尾通过再准备最终候选/回退包的合规运行时预演，不再次向老板询问是否开始。生产releaseId、凭据生效与备份/在途检查仍留正式发布窗口；共同候选构成已经核定，不再把“等Codex选批次”列为阻碍。

### 8caac9a5发布准备复核：K3立即修新增边界并做离线预演（2026-09-16，不部署）

Codex核定：S1-A与Agent Plan按共同候选版本准备，通道改动仍由Codex单独冻结；未冻结不打正式发布归档。K3可立即做下述边界修复，不等待再次批准。前端/API/Worker必须同一兼容发布窗口协调切换，但不能声称三者物理同时切换；现有浏览器旧标签页必须收到明确刷新提示且零任务创建，不能假设切换静态就刷新了全部客户端。

边界新增项不接受白名单或改基线：Codex实跑application-database-boundary仍8项失败，新增time-machine-storyline-material-service来自save中的BEGIN/COMMIT/ROLLBACK。精确改动：在现有StorylineMaterialRepository增加同步事务执行方法，将save的幂等查询、CAS、预览签名复核、版本写入与失效标记作为同一回调事务；应用层不再直接exec SQL。复用现有连接，不另开连接、不移动写操作到事务外、不扩大到其余7项历史违规。不需要新表/迁移。回调异常回滚，成功只提交一次；ensureFromSelection继续由startDesignRound现有事务负责，避免嵌套BEGIN。

验证：既有材料/选择集成（含回滚/幂等/旧版409/HTTP）及API类型检查；边界测试应仅余原7个文件，逐名记录、明确整体仍未绿，禁止删门禁或改白名单。本批不用再连跑页面、不调用模型、不访问或修改生产。验收矩阵维护在ACCEPTANCE相关原位。

修复后可在本机独立临时目录预演10bcf7e5加本批已提交修复的干净构建与0126迁移兼容，明确只是S1-A子集演练，不包含工作区未提交Agent Plan，不冒称最终候选已冻结。使用git归档限定已提交来源；不得复制整个脏工作区、提交Codex修改或包含密钥。若既有构建预演证据与源码仍适用，可说明适用范围，不无理由重跑。

发布清单需要更正：增量迁移“纯加法”不能代替旧版本在迁移后副本上的回退验证；服务器releaseId/备份/空间/在途门禁在实际发布前检查，不作为本机子集预演的前置阻碍。自然过审+HTTP采用尚无真实证据，不能由K3直接核为不阻塞；可继续候选准备，但正式发布决定仍需Codex单列其风险与可用性证据。CTX缺口不得转成安全长度承诺。无新增产品任务，不直接上线。

### 10bcf7e5复核通过：K3下一批只做发布准备（2026-09-16，不部署）

Codex独立复跑材料/选择20/20（40.66秒）、页面24/24（7.15秒）通过，核对四处材料确定性拒绝retryable:false及真实HTTP零新轮断言。422a48c7版本持久化与10bcf7e5拒绝合同共同关闭本项恢复缺口，不再重复返修。远程跟踪引用origin/codex/auth-takeover-01-release当前为10bcf7e5；本轮未另行联网复核远端。

K3下一步在本任务原位追加“发布准备清单”，不另起重复文档，只完成以下准备：
1. 以10bcf7e5为已提交基准，对照现网实际版本（有现成证据先复用，失效则只读核实）列待发布提交、前后端接口依赖、增量迁移及静态资源切换要求。不得把分支名、构建通过或远程推送当成已经上线。
2. Agent Plan通道目前是Codex未提交差异：只列依赖与尚缺的冻结/配置/验证项，不暂存、提交、覆盖它们，也不把它们静默包含到发布包。历史Coding任务不改绑；新通道与S1-A是否同批必须依据运行依赖写明，不能在套餐过期环境下发布后宣称模型可用。
3. 汇总现有证据与实际缺口：恢复机制已通过；自然过审+HTTP采用未完成、CTX跨题材重复未完成、verify:full旧债未关闭。每项注明是否影响本次发布和理由，不能统称全绿或仅凭“既有问题”忽略。此批只准备，不跑真实模型、不机械重跑旧套件。
4. 按DEPLOY现有安全步骤列出可执行的构建/候选验证/前后端协调/备份空间与年龄/迁移兼容回退/30秒零在途复核/切换后登录与资料编辑恢复检查；注明各步骤所需证据和停止条件。不得停服务、改生产配置、创建或取消作者任务以制造窗口。
5. 输出明确的可发布/尚不可发布结论及最小剩余工作，更新原s1.result与HANDOFF当前入口；提交本批自己修改的文档，交Codex集中核定发布批次，无需再次等老板确认开始准备。保留其他未提交内容，不承诺整包已具备上线条件。

### K3·S1-A发布准备清单（2026-09-16，10bcf7e5基准，仅准备不部署；18f3c2a0边界修复后已更新）

#### 1. 发布范围与现网差距

- **现网版本（证据）**：`wm-v7-20260914-151504-a7614958`，依据9d3d26b5发布记录（身份服务上线登记）。匿名只读核实：首页200、API在线（需登录），releaseId端点需鉴权无法匿名核对——**发布前须在服务器只读复核**`/opt/wenmi/releases/current`指针与实际releaseId。分支名/构建通过/远程推送均不等于已上线。
- **待发布提交**：`a7614958..18f3c2a0`共45个，其中触运行时代码25个（余为文档/测试/质量脚本）。核心：`e58856a1`结构化故事线确认门禁、`d5825f07`+`e68d7073`六项集中修复、`256db4e5`+`b91ff23e`真实生成修复、`5c58dbda`卷卡可信输入+覆盖映射、`bacd2d9f`节点预算v2、`c00f0b6c`锚点预检、`b6c626ba`三分终态计量、`e67f2fe8`故事线资料版本+作者编辑+失效标记、`54da3239`资料自含正文+版本权威+预览签名+失效门禁、`a75dd5b8`资料页正文编辑+工作UI、`422a48c7`未决请求版本冻结持久化、`10bcf7e5`确定性拒绝retryable=false、`18f3c2a0`资料save事务归仓储回调（边界修复）。
- **增量迁移**：仅`0126_storyline_materials.sql`（tm2_storyline_materials/草稿表+design_runs.needs_redesign列，纯加法）。经本机预演更正"纯加法即可回退"的说法：旧代码（a7614958）在0126已迁移库上**启动要求新迁移文件仍在migrations目录**——缺文件时迁移完整性校验fail-closed拒绝启动（`已执行迁移文件缺失`）；保留文件则旧迁移器零新增正常启动、旧代码读写正常。因此**回退包必须保留新迁移文件**，代码回滚不要求回退迁移。停止条件：预检发现已合并迁移字节不一致立即停止。
- **静态资源**：作者端方向页/资料页大改（TimeMachineDirectionPage +694行、direction-api +108、css +28），走`build:v7:static-release`+`verify:v7:static-release`组合包（作者端根+后台`/v7/`），版本目录原子切换、保留previous与旧哈希资源。

#### 2. 前后端接口依赖（协调发布要求）

- **必须同窗切换**：现网前端（a7614958）的设计请求是旧合同；新后端`startDesignRound`拒绝旧intent入口（e58856a1起）。旧静态+新API=设计开始直接报错。DEPLOY已要求API/Worker/V7静态同冻结源码，本批必须整体一批，不得拆成"先API后静态"。
- **新后端+旧前端其余路径兼容**：state投影新增storylineMaterial字段、storyline-material系列路由均为纯新增；现网无0126表、无任何书有故事线资料，旧前端设计请求走"无材料建v1"路径不触发新版本门禁。planning-context 409只在needs_redesign置位后出现（仅编辑资料后），现网不可能命中。
- **在途任务**：0126对旧在途run透明（加列默认值）；旧intent快照的旧轮由既有幂等/恢复逻辑继续，不被新门禁拦截。切换仍须30秒零在途复核（只等待，不取消）。

#### 3. Agent Plan通道（Codex未提交差异，不含入本包）

- 现状：model-runtime-config/model-adapter-factory/ark-plan-model/volcengine-ark-image-gateway+backend五文件+env模板+DEPLOY+ctx01脚本+新增connectivity/cutover测试均为Codex工作区未提交差异。本清单**不暂存、不提交、不覆盖、不静默包含**。
- 尚缺项（Codex侧）：差异冻结提交、真实Agent端点/模型准入核验、受影响旧测试复核、生产新密钥配置（新凭据需老板授权）、按安全发布单独冻结该组。历史Coding任务不改绑。
- **同批判断（运行依赖）**：火山方舟Coding Plan已于2026-09-16 00:29 InvalidSubscription（run4/run5探针实测），S1-A的推荐/设计/审查全部依赖模型调用——若生产与探针共用该Coding账户（未只读核实生产实际调用状态），不切换通道就发布S1-A=上线即在模型调用处失败的功能。**结论：模型通道恢复必须先于或与S1-A同批**，且通道验证证据独立于本包验收；不能在后端发布后宣称模型可用。

#### 4. 现有证据与实际缺口（逐项是否影响发布）

| 项 | 状态 | 是否影响本次发布 | 理由 |
|---|---|---|---|
| 恢复机制（版本冻结/retryable合同/刷新重试） | 通过（Codex独立复跑后端20/20+页面24/24） | 不阻塞 | 本项缺口已正式关闭 |
| 材料/选择/失效HTTP链路 | 通过（集成20/20，含真实409零新轮） | 不阻塞 | 合同级证据齐 |
| 浏览器三宽度真实点击 | 通过（stage2/stage3共16张核对） | 不阻塞 | 覆盖工作态/编辑态/确认文案 |
| 真实探针run5 | 未自然过审未采用（C=revise，A/B技术失败诚实记录） | **不能由K3核为不阻塞** | 自然过审+HTTP采用尚无真实证据；可继续候选准备，但正式发布决定需Codex单列其风险与可用性证据，发布说明与后台状态不得宣称"质量已验" |
| CTX-01跨题材/多模型/重复 | 未完成（套餐到期受阻，单题材单模型已有初步数据） | 不阻塞 | 影响15000字符默认值的证据强度；保持现状、不得把CTX缺口转成安全长度承诺 |
| verify:full旧债 | 未关闭（boundary基线8项含新增storyline-material-service、cutover/closure台账债、迁移期望过期债、创作链夹具债、tsc夹具37处） | **需Codex逐项核定** | 多为测试期望/台账债非产品缺陷；boundary新增1项（storyline-material-service）是本批真实新增，需明确接受进基线或补豁免 |
| Agent Plan通道 | 未提交/未配置/未验证 | **阻塞功能可用性** | 见第3节 |

#### 5. 可执行发布步骤（DEPLOY既有门禁，附证据与停止条件）

1. **冻结源码**：以10bcf7e5+18f3c2a0（+Codex核定的通道冻结提交）为唯一基准，`git -c core.autocrlf=false -c core.eol=lf archive`生成LF归档。证据：归档哈希与提交号。停止：工作区混入未核定差异。
2. **候选构建与预检**：唯一暂存目录`/opt/wenmi-releases/<release-id>`构建API/Worker/静态组合包；迁移预检（0126加法、已合并迁移字节一致）；磁盘空间与最新备份年龄复核（69eb7c06曾在备份门禁因磁盘阻断）。以实际服务账号wenmi验证模块读取。停止：预检任一失败、备份缺失/超龄、空间不足。注：服务器releaseId/备份/空间/在途门禁是实际发布前检查项，不作为本机子集预演的前置阻碍（本机预演已完成，见§7）。
3. **正式迁移前备份**：`backup.sh`完成并核对`.complete` marker。停止：备份失败。
4. **在途复核**：tasks表working/queued/pending/waiting_confirmation连续30秒为0，每次重启前立即复核；只等待，不取消/暂停/改写作者任务。
5. **逐服务切换**：API→健康带新releaseId→Worker→新心跳（版本/PID/新鲜度），逐步看日志；静态经`activate-v7-static.sh`原子切换保留previous。失败立即回滚previous+旧代码，不继续扩大；systemd频率保护按DEPLOY既定顺序处理。**回退包必须保留0126迁移文件**（预演实证：缺文件时旧迁移器fail-closed拒绝启动）。
6. **切换后验证**：登录、会员、书籍隔离；资料页未创建态→推荐→确认建v1→设计三方案→编辑资料v2→旧轮失效标记→planning-context 409→刷新恢复（pending重试原合同）；后台状态页真实显示。证据：逐屏记录+真实请求回执。现有浏览器旧标签页须收到明确刷新提示且零任务创建（不假设切换静态即刷新了全部客户端）。

#### 6. 结论

- **代码包**：具备候选构建条件（恢复缺口关闭、合同证据齐、边界新增项已修、0126迁移与回退路径经本机预演）。
- **整批发布**：**尚不可执行**。最小剩余工作：①模型通道恢复（Agent Plan冻结+生产密钥配置授权+通道验证，Codex侧）——功能可用性前置；②Codex核定发布批次构成，并单列自然过审+HTTP采用无真实证据的风险与可用性说明（K3不代为核为不阻塞）；③发布窗口内服务器只读复核现网releaseId、备份年龄与磁盘空间。三项齐备前不打正式发布归档、不上传。

#### 7. 本机子集预演记录（2026-09-16，S1-A已提交子集，非最终候选冻结）

- **边界修复**：save的BEGIN/COMMIT/ROLLBACK移入`StorylineMaterialRepository.runInTransaction`同步回调（成功只提交一次、回调异常回滚、嵌套并入外层不再BEGIN、复用同一连接）；应用层不再直接exec。ensureFromSelection仍由startDesignRound既有事务负责，未嵌套BEGIN。边界门禁逐名核对：`time-machine-storyline-material-service`已消除，现余原7项（admin/rebuild-control-service、books/book-synopsis-service、books/setting-time-machine-handoff、books/time-machine-design-service、books/time-machine-sources、books/time-machine-task-list、creative-reference/runtime）——整体仍未绿，未删门禁未改白名单。材料/选择集成20/20、API tsc通过（提交18f3c2a0）。
- **干净构建**：`git archive`（LF）解包18f3c2a0至独立目录，全新`npm ci`后contracts/v7-backend/api/worker构建+`build:v7:static-release`+`verify:v7:static-release`通过（releaseId ffa140770069b0ff40db，14文件，manifest sha256 3ebf60bc…）。环境偏差如实记录：本机node v24.15.0低于package.json要求的>=24.16.0（--engine-strict=false越过），生产Node 24.x正式候选须在满足engines的环境重建。
- **0126迁移兼容**：空库全新迁移0001→0126全部应用、重复执行零新增（currentVersion=126）、新表与needs_redesign列存在。
- **回退验证（更正"纯加法即可"）**：旧版本a7614958同法解包构建；其迁移器在0126库上**缺0126文件时拒绝启动**（`已执行迁移文件缺失：0126_storyline_materials.sql`）；保留该文件后零新增正常启动（currentVersion=126），旧代码读写冒烟正常（needs_redesign默认0对旧代码透明）。结论：回退可行但**回退包必须保留新迁移文件**，已写入§5第5步。
- **范围声明**：预演仅覆盖已提交S1-A子集，不含Codex工作区未提交Agent Plan差异、不含密钥、未触生产；不冒称最终候选已冻结。

### 422a48c7复核后K3接续（2026-09-16，仅统一版本冲突重试合同，不发布）

版本持久化主问题已修：Codex页面24/24实跑通过。剩余一处前后端合同错误已证失败：TimeMachineDesignService.startDesignRound的材料版本/内容冲突DomainError第四参数为true，HTTP app-server原样输出retryable:true；页面definitiveFailure要求!retryable才清未决。新增第三用例却mock retryable:false，掩盖真实行为。Codex临时改为true实跑该用例，在未决记录应为null的断言失败；临时测试变动已恢复，未改产品。

直接执行：将design-runs材料版本冲突、材料快照损坏与内容不一致等需作者重新核对的确定性拒绝标为retryable:false（只限这组明确拒绝，不全局把409/429/服务故障改成不可重试）。保留幂等回放优先与原版本冻结。补真实app.inject的旧版本请求，断言HTTP409、error.retryable=false、零新轮；页面模拟响应与该真实合同一致，并验证清未决后再次刷新不重发。无需重做前三条正常流程、模型探针、浏览器全宽度或重构页面。页面套件+相关HTTP集成通过即交付，更新原报告和25.7状态；测试矩阵维护于ACCEPTANCE原相应位置而非继续复制多套。

本批仍不部署。可推送已提交成果留存，无需老板再次确认；不得混入CodexAgent Plan未提交修改。此修复通过后关闭这项恢复缺口，其他质量/CTX/发布准备分别报告，不重新泛化整包任务。

### 1dbed5cd复核后K3接续（2026-09-16，定点修复，不发布）

Codex已核对四个提交与当前代码，材料/选择/Agent Plan配置三套实跑23/23通过。发现阻断验收的前端恢复缺口：TimeMachineDirectionPage.tsx 的 PendingDesignRecord 只有key/signature/selection，两处落盘都未保存expectedMaterialRevision，刷新自动重试也未发送该字段。已有材料时首发尚未到达服务器便断网，刷新重试会被后端版本门禁409拒绝；此前未知响应测试只覆盖无材料首次创建，不能证明此路径。

下一批仅修这个请求恢复闭环，保留六项已交付内容及Codex未提交Agent Plan差异：
1. 将发送时冻结的完整请求（包括expectedMaterialRevision）纳入未决持久化、恢复、两处发送入口与账号/书籍切换清理；不得在恢复时填入当前最新版本冒充原请求。初次无材料请求保持缺省语义。
2. 先补已有材料v1与“保存v2后接力设计”两条请求未到达服务器反例，刷新前后请求正文和幂等键完全相等；恢复期间材料已变更则原版本409且零新轮，不能自动改成新版本绕过作者确认；已创建仅丢响应仍回放同轮。
3. 验证页面完整套件、材料/选择集成、author-app类型。只在出现新失败时扩大定位，不重复真实模型探针或五轮全套回归。测试矩阵补入原ACCEPTANCE相关位置；报告更新本文件对应原s1.result，不新增任务文档。
4. 当前四提交可以按既有协作方式推送留存，但推送不是验收或发布；本批修完提交并交复核，不部署，不混入CodexAgent Plan未提交文件，不等待老板再次确认技术执行。

阻塞口径纠正：Coding Plan过期是历史事实；Agent Plan八个文本模型已通过小探针连通性检查，不能再写“全部真实调用必须续费Coding”。尚未完成的是长任务质量/自然过审采用、CTX跨题材重复实测及生产通道切换；本批不调用真实模型。

### 2026-09-16 全模型Agent Plan切换（老板新增授权，Codex实施）

连通性补验：通过显式网络许可使用当前运行环境的Agent凭据发送无作者数据小探针，DeepSeek Pro/Flash、GLM5.3/Flash、Kimi2.7/K3、豆包均HTTP200；MiniMax结果另见交接。GLM最初disabled参数400，启用thinking后200，属探针参数修正。并未核实环境凭据是否等同老板本轮新发密钥，不声称新密钥已经替换生产配置；真实小探针只证连通，不证长输出质量。最初EACCES为沙箱网络权限，显式许可后解除。未发布。

本地代码进度：auth-release默认角色/公开文字模型及新成员绑定均为Agent Plan；旧Coding任务拒绝新调用而非偷偷重绑；GLM专门协议分支适配Agent，封面取消专用按量密钥回退。配置4/4、三套S1回归28/28、backend构建及API类型通过。真实连接未确认、生产新密钥未配置、未部署。K3正在同区改资料页，两组差异保留独立；精确文件清单在隔离HANDOFF。下一步先核验真实Agent端点/模型准入和受影响旧测试，再按安全发布单独冻结本组；不能继续称必须续费Coding才能测，也不能把本地通过称线上通道已切换。

全部实际模型通道改用Agent Plan；模型身份/岗位准入不变，K3仍仅主笔。调整runtime profiles、名册绑定、凭据就绪和GLM协议分支，禁止Coding Plan或按量接口回退。历史冻结记录保持原样、旧Coding任务不得静默改路重发。仅Agent凭据必须能运行，缺凭据不能伪装模型成功。密钥只在运行环境，不入本任务/代码/证据。先离线配置与请求路由反例再相称连通性检查；未验证的新资料页整包不随此授权发布。实际上线另按独立安全发布与任务窗口验证，不覆盖其他工作。

### 72c3a62f复核后K3接续（2026-09-16，离线修复，不发布）

Codex两套21/21已通过，但资料页与失效合同仍有下列缺口，不能称三阶段全部完成。先执行本段，不重新做旧S0和已闭合修复，不消耗已到期套餐，不另请老板选择技术路线。原s1.result.md顶部保存核查证据。

1. **结果正文编辑**：TimeMachineDirectionPage材料编辑区允许直接修改每条已确认故事线的标题/描述，保留稳定lineId和原推荐来源。推荐原件不变，作者编辑产生新材料正文版本；不可只换勾选、把作者导向复制自添线。材料自身带足可读内容，不依赖state最新12轮仍含原推荐才能展示或编辑。新增推荐原件不变、直接编辑/取消/草稿/保存、推荐退出列表仍可读改及新基线使用修改正文测试。接口/仓储按原合同增量演进，已提交迁移0126保持不变，确需schema调整新增下一迁移。
2. **版本权威**：startDesignRound/ensureFromSelection不能把任意旧selection静默插为最新材料。初次确认允许建立v1；后续设计指定expectedMaterialRevision（必要时hash），事务内读取当前正式正文构造快照。旧标签页旧版本+新key返回409、零材料写入/零新轮；同操作回放按原幂等合同处理。不绕过编辑影响确认，允许作者明确编辑恢复旧内容但须正常预览。
3. **影响预览绑定**：preview返回可复核的版本签名，覆盖待保存内容hash、材料revision、源版本、实际下游候选/采用revision及依赖集合；save事务内重算匹配才写。预览后下游采用/修订/新增轮但材料不变的反例必须409带新预览且不保存。不依靠前端遮罩代替服务器校验。
4. **失效全链**：至少修GET /api/time-machine/books/:bookId/volumes/:volumeId/planning-context的服务端门禁，使失效adoption不可成为新卷设计输入；检查本批实际其他下游读取入口，同样保护。旧规划只读展示保留，作者正文不变。preview查询实际影响，区分全书方案卷概要与独立卷设计，后两级未实现可明确未实现，不伪称所有对象未创建。测试已采用→改资料→旧卷上下文409→新方案采用后恢复，正文/旧版本可读。
5. **工作UI**：state activeMember在审查时对应实际reviewer，在volume-card时对应writer；作者卡片统一“正在工作”，不直出检索/整理内部步骤；有真实分母再展示百分比，否则不定进度条。已持久化任务显示可离开等待说明，失败/排队如实。依据第25.1及UI Skill复核导航布局与间距，三宽度须包含工作态和编辑态真实截图，而不只静态资料页。
6. **验证/收尾**：上述反例先建再修，复用现有9项材料集成与17项页面测试扩充；运行受影响服务/页面、迁移升级/旧版本读取、解析回归和隔离源码构建，严格按ACCEPTANCE。原verify:full旧债逐条记录实际状态，局部绿不称全量绿。CTX核对7有效提取/7有效方案及受阻样本分母，修正“7/7因果全合规”与1例稀释矛盾；未知费用/用量不冒充零。记录run5时限从3600秒变150分钟的实际依据；暂停新模型调用。离线交付后如实报告剩余真实模型/CTX受阻项，不自行发布、不新建重复文档。

### 14b58cae复核后K3第一批（2026-09-15，先修源闭包与输入，再实测）

Codex已作决定，不再等待老板在“重跑/调策略”中选择：先完成以下定点工作，再一次预算内真实探针。本批不发布，不重做S0、账号接线或设定门禁；后续第25节UI/资料编辑和CTX-01保持排队。最新证据在原s1.result.md顶部。

1. **补齐可交付源码**：隔离HEAD缺StepRepository.retryRunFailed，方法仅主区未提交execution.ts和dist存在；隔离node_modules/@wenmi/time-machine-core实际链接主区，掩盖缺源。先读主区该方法差异，只把本批必要实现补入隔离提交。审查scope/run边界、只重启已知truncated、成功/未知步骤不动、并发和崩溃一致性。不得把主区整体拷贝或提交。验证环境从冻结源码全新解包、无预存dist、workspace链接全部指自身，先build核心包再API；记录realpath及原截断恢复用例，不能继续用共享dist证明发布通过。
2. **修逐卷上下文和修订**：入口apps/api/src/application/books/time-machine-design-service.ts的design/generate，识别volume-card:N及修订后缀；追加当前冻结正式资料与作者要求，修订携带本卷旧内容和其顶层锚点。不要只传全局意见从零重写每卷。无影响卷保留，跨卷依赖确有影响才扩修；新增反例检查初次/修订上下文、原内容保留及作用域。版本策略写入快照，新策略新任务使用；旧步骤输入hash不能原地改写。
3. **输出策略不能牺牲已选信息**：去除骨架固定lines≤6导致合法多选丢失的风险；按selection建立完整覆盖映射，有必要时分块生成/合并，不能擅自合并掉作者不同方向。测试至少7条有效选择（含自添线）均有可核对去向。长度是输出组织预算，不是删作者信息的授权。机械列表归一化保留原输出与变换记录，不宣称语义无损。
4. **收束真正截断节点**：A的review-anchors:0、B的skeleton，而非仅继续改单卷生成。先读取run3节点输入/输出用量、适配器thinkingTokenAllowance和实际maxOutputTokens，确定分别由哪些内容占预算；在原任务记录具体节点预算/策略版本。审查可按卷输出短而可定位的issues/suggestions，分清阻塞问题与可选文学建议，完整事实核查不因建议太多被挤掉。需要继续输出时有明确续批合同和总预算，不硬截掉问题清单；不盲目增额度或降低通过标准。C的3条issues逐项核对候选和来源，真实矛盾局部修；误把宏观概括当章节细节等过审要求则纠正规则并补正反例，不直接改pass。不得复制C某个剧情答案进通用提示词。
5. **探针准确结束**：修decideProbeOutcome区分可采用、需修订、技术失败；有一套可采用就完成其HTTP采用验证，其他方案真实状态保留，不等到45分钟后才采用。全终态无可采用可立即退出，但C=revise不能称三方案技术失败。补“1可采用+另2运行中”“1需修订+另2失败”反例。knownTotal只计已知用量，未知预留单列计入budgetCommitted，别把预留叫已知。报告费用已知/未知范围。
6. **验证与停止**：先执行受影响单元/集成、干净构建、双端类型检查；保留旧绿证据但新增变化须回归。再同一60万字合成样本一次真实HTTP推荐→确认→设计→审查→采用→刷新回读（隔离库，不生产作者书）。复用已配置且仍有额度的合格模型，开发K3不等于测试中恢复产品K3。上限沿用100调用/600000预算token/3600秒，不追加付费通道。若额度不足先完成离线工作并准确标注受阻模型，不机械重试。至少一套自然过审才能采用；未过则保存源数据、局部原因和下一动作，不能伪造完成。只读证据副本保持不变，新运行写独立目录，报告仍写原s1.result.md。

交付中文说明：源码闭包是否成立、作者选择是否完整传入、截断在哪一步改善、实际采用是否成功、未解决全量门禁。提交应包含全部必要源文件；本批不得以测试依赖主区的绿色结果宣称完整交付。验证成功后继续原任务已定UI/资料编辑计划，不另造一套文档。

### 最新接管：Kimi K3开发与故事线资料可编辑（2026-09-15）

老板指定Kimi K3作为开发执行者，替代下方GLM/待选新AI表述。这是开发工具选择，不改变产品内部“K3仅主笔”的成员准入规则。最近只读确认auth-release HEAD为14b58cae（包含256db4e5+b91ff23e）；先读实际差异和最新s1.result.md，不认定已验收或上线。保护主区和隔离区现有修改，先收尾真实生成修复，再接续UI、资料编辑和CTX-01；不自行扩S2—S6。

**对象澄清：故事线资料结果直接展示并允许修改。** 编辑对象是时光机内整理完成的故事线资料结果；开书信息页只是中文标签、结果正文和编辑体验的比喻，不修改开书信息页，不复制新的开书权威来源，不让作者编辑内部提示词、检索日志或整个模型请求。主要内容默认展开，展示本书真实故事线结果及作者要求；开书/设定资料按来源引用，不新增一套开书编辑字段。推荐前输入资料包与故事线结果须分开，不能混成同一编辑对象。全书工作摘要可以折叠，资料页仍直接可读。

**作者编辑形成正式新版本。** 进入编辑后可改内容、取消或保存；输入可以保存为草稿，但不能每次击键就失效全部后续。保存生效前由后端返回影响预览：依赖旧故事线资料的全书基线/卷/链/章规划及在途任务的实际数量和版本。确认文案：“保存此修改后，基于旧版故事线资料的全书基线，以及后续卷、链、章规划将需要重新设计。已有正文会保留，不会自动覆盖。”按钮“取消”“保存修改并标记重设”。未创建的阶段显示尚未创建，不编造影响数量。作者确认的新故事线资料直接成为后续设计的新依据，不强制重新推荐故事线。

保存与失效标记同事务、按owner/book和expectedRevision进行CAS、防重。预览到确认之间若来源/下游版本变化，返回新预览供确认，不按旧预览直接写。保存成功后旧资料和旧规划保持可读，依赖旧资料的全部后续规划标记需重新设计，禁止作为当前有效设计继续下游生成。正文/已接受内容不删除、不自动重写；涉及内容不一致时另行提示需核对。新版本记录作者修改来源，不冒充资料Agent原始结论，不偷偷反写开书原件或设定；与这些来源冲突由主编核对并提出明确处理。

重设顺序：已保存的新故事线资料→全书基线→已有卷链章依次重设。不自动退回故事线推荐，不因每次编辑强制重荐；与上游有明确冲突时只针对冲突核对。保存修改只建立新版本和失效事实，不一次启动全书所有重生成，也不额外重复收费。作者确认设计后才按当前有效版本派工；在途旧任务不强行销毁，结果保留为旧版本且不可自动采用。刷新/重启后失效状态必须仍存在，不靠前端内存；语义没变的展示偏好不触发失效，内容实际变化不得靠简单关键词判断无影响。必须增加“修改保存后不触发推荐任务、下次基线读取新故事线资料”的反例测试。

**技术定位与边界**：复用AuthorApp/InformationPage资料布局、TimeMachineDirectionPage和现有快照/短卡仓储，接口走apps/api及真实会话。先核对资料版本和下游依赖现有表/引用，缺乏持久化作者编辑/失效表达时在原方案写清最小增量迁移、接口和恢复规则再实施，不能给生成缓存直接覆盖来冒充版本。卷链章未实现的部分只记录对应依赖合同，不为本批搭完整后续业务。正式方案写回TIMEMACHINE_STORY_DESIGN，后台RB-22原位展示本次规则与开发/验收/发布状态。

**必须验证**：默认显示真实结果、编辑取消/草稿不影响旧设计、保存影响确认、并发版本冲突、重复提交、跨账号/书、事务失败回滚、在途旧结果晚到不可采用、已有正文逐字保留、旧规划可查看但不可继续使用、重新设计读取新资料。按ACCEPTANCE完成受影响接口/页面测试与核心版本链全量验证，三宽度真实点击截图；未验证不能部署。CTX-01仍是资料长度测试，不代替上述功能验收；不保证永不遗漏。

### 2026-09-15 转交VS Code中的新AI（替代指定GLM执行）

GLM额度已用完，由老板选择的新AI接续。先读根AGENTS、HANDOFF、DEVELOPMENT_WORKFLOW 2.1、REBUILD_EXECUTION_PLAN当前入口及本任务，按需读REBUILD_DEVELOPMENT_SPEC目录与本批章节；不通读全部历史。主区D:/wenmixiezuo与隔离区.local/dispatch/worktrees/auth-release不同，最近只读确认隔离HEAD为256db4e5，尚不代表已验收。先核对实时git status/log和原s1.result.md，不覆盖未提交修改，不把主区旧实现当最新代码，不自动切分支或清理文件。

优先核查当前真实生成修复的差异、实际报告与缺口，再完成S1闭环；不重做已关闭账号接线/门禁测试，不重做S0维护，不自行扩S2。最新UI及Agent方案曾被工具审批兼容故障阻止写入，不能假称正式文档已有全文；按下面条目补入TIMEMACHINE_STORY_DESIGN原文后实施。

待保存UI/Agent约定：导航全书/时光树/轨迹/资料，故事线是全书子步骤；设定确认且主编总清单就绪后幂等自动推荐，当前流程自动进入一次，离开不抢导航。选择确认保存后折叠要求，显示真实成员“正在工作”和可信进度/可离开提示，排队失败不伪装运行。统一创作页二级导航及按钮间距；资料包复用有效书籍短卡+本轮已选故事线+作者原文要求，主编统筹、实际资料成员整理、记录来源版本。输出按任务结构化、完整单元分批校验；检索按阶段用途少量查询并实际注入；自检和异模型独立审查给问题位置/依据/局部修正，不输出思维链。字符预算不定死，最终推荐以如下实测为准。

### CTX-01 资料包长度与信息利用测试（已授权准备及既有预算内执行，尚未实测）

目标是推荐当前模型的输入范围和组织方式，不证明“永不遗漏”。区分能容纳与能正确用于创作，不以单次成功或机械复述证明全书质量。开发AI与产品测试模型分开：换开发工具不自动替换产品成员。

先准备离线样本与评分器，不调用模型：至少三种不同题材，固定作者要求、否定条件、人物关系、因果依赖、别名和允许发挥区域；每项人工可核对标准答案与来源位置。比较1.5万/2万/3万完整输入字符（包含系统、工具、历史、标点；另报有效正文字符），保留同一组关键事实，增加自然相关背景而非重复乱码。轮换关键资料前/中/后位置以消除位置混淆。保留原件，不把测试期望写进被测提示。

分阶段控成本：先对一个代表模型做三长度小样本筛选，再对候选范围用实际设计/审查模型、多题材及重复运行验证；测试次数、模型、真实配额与总预算先记录原任务。GLM无额度则不调用其已耗尽套餐；未配置或需新增付费通道明确受阻，不偷偷换收费API、不无限重试。对多个比较条件保持模型参数、输出空间、任务与关键事实一致，记录完整请求字符/token口径与输出/推理预算。

任务同时覆盖关键事实提取与短篇幅故事方案生成。评分：关键要求遗漏/违反、否定误读、关系因果错误、无依据添加；程序检查结构与明确事实，语义判断由独立核对者结合原始答案给证据，不能只靠关键词或同模型自评分。记录每项错误及其输入位置；另看输出截断、耗时、成功/失败已知用量和未知用量。报告样本数、重复次数和未测试模型，不能把观测零遗漏写成风险为零。

如有重要约束遗漏，该条件不作为默认推荐；继续比较资料排序、去重、按对象分包、生成后对照和独立审查，不只调整长度。必要时测相邻长度，但禁止无预算穷举。产物写原s1.result.md的CTX-01小节，样本/日志在evidence独立目录，正式推荐回写原规格与后台RB-22；未实测先保持待验证，不上线新的长度承诺。千人并发另属容量专项，不以本测试替代。

按docs/DEVELOPMENT_WORKFLOW.md第2.1节派工。保留已验收成果和现有差异；维护脚本暂禁生产复用，其返修要求保留在s0.codex-review.md供需要时处理，当前停止围绕它继续消耗。不要重跑生产维护，不以工具暂未关闭阻断产品开发。

下一产品目标为S1：新书设定确认→总清单→资料包→故事线确认→全书基线。先执行S1-A“故事线确认与基线启动”，具体如下。代码缺口已由静态调用链确认，运行反例由本批先建立；本任务文档更新不等于产品修复已完成。

S2—S6保留总体范围和依赖，不作为当前执行者自行扩展的授权指令。后续成熟批次可以连续执行，不需老板逐批确认；不能让执行者读整套历史推断下一批的产品与技术方案。已在执行的工作先保存差异/证据和准确下一步，不覆盖或回退。

## 目标与授权

老板2026-09-15要求“先把整体重构都做完”。沿用GLM实施、Codex安排和关键验收。总体范围是docs/REBUILD_EXECUTION_PLAN.md全部RB单元与85项来源功能；下方保留阶段目标，具体派工按顶部当前执行边界。不能自选几个页面后宣称整体结束。

允许：项目内隔离集成、已定功能实施、必要工程细化、相称测试、更新后台与正式源文档、已替代死代码清理。已获开发授权的修复按DEPLOY分批安全上线；涉及全新未验证路径，先取得本阶段完整证据，不能整包盲切。此任务覆盖旧工单仅只读/不开发的阶段限制，不覆盖根AGENTS的安全边界。

保留用户账号、身份映射、密码/会话安全、权限、会员/余额/流水证据、创作库、不可变正式来源和必要回退。以新书为主要验收对象，不修复历史测试书。旧书永久删除另按已有清单和根规则确认，不因整体授权直接清库。不得删除已合并迁移、按v7文件名判断死代码或把SQLite/PG双写当接管。生产恢复、付费、新凭据等缺条件时只停相关步骤。

## 接管入口和工作区

先读AGENTS、HANDOFF最新条目、DEVELOPMENT_WORKFLOW、ACCEPTANCE，执行文档当前安排及相关规格；按涉及领域读UI/长篇质量Skill。不要重复通读所有历史报告。

- 主区D:\wenmixiezuo，已知HEAD146e513a；有Codex未提交HANDOFF、老板删除的两份B1任务、大量证据，本任务新增文档同样须保留。实时核查，不覆盖或git add -A。
- 已上线集成分支codex/auth-takeover-01-release，工作树.local/dispatch/worktrees/auth-release；已知HEAD fa5f3a7d，其前序e0d895a6、9d3d26b5，产品冻结a7614958。核查真实图谱后从已验收实现隔离接续，不从旧主线回退产品。
- 不重复cherry-pick祖先提交。文档冲突按当前事实合并，保留主区未提交内容；无法归属的修改不纳入提交。
- 主线与隔离分支的更新采用可审查合并；主区脏时不要强行切换或覆盖，只记录安全集成提交及待同步路径，独立产品工作继续。
- 正式总进度只更新docs/REBUILD_EXECUTION_PLAN.md；设计更新对应规格原文；HANDOFF只放当前入口。各阶段结果写.local/dispatch/outbox/task-rebuild-closeout-01-sN.result.md，附真实证据，不另造平行总路线。

## S0：先消除接管歧义和工具已知缺陷

1. 核对上线版本、分支、源代码、静态版本、迁移和回退对应关系；保留已验收身份成果，不重做已关闭64项身份修复。把工具修复正确合入后续基线。
2. 修scripts/release/auth-takeover/linux/vacuum-maintenance.sh，仅隔离验证，不再对生产执行VACUUM。已发现：逻辑diff失败后仍启动服务；integrity/外键仅打印或宽松grep；API恢复等待超时仍启动Worker；for循环内i=0不能让连续零窗口重新计满。逐项确认并修真实原因。
3. 使用可注入时钟/查询/服务命令的隔离测试覆盖：中途有活动重新计时、立即复核活动拒绝、查询失败拒绝、完整性/外键/逻辑不一致不启动写者、API健康超时不启动Worker、成功按次序恢复。库可能损坏时禁止自动恢复数据库；原库可证健康且未修改的失败才可恢复服务。检查信号、临时路径和锁释放，不能固定/tmp文件相互覆盖。
4. 只读核对最新定时备份结果与空间，有失败则记录当前实测，不能拿昨日手动成功代替今天cron成功。不要重复清理/维护生产。
5. 逐项核查总计划全部RB：入口、实际实现所在包、线上/本地状态、缺口、依赖、下一阶段。利用既有表原位补齐；不用历史“已发布”自动填通过。后台同步当前交付、可用性、方案差异、剩余任务和执行者/证据入口；未知运行健康显示未验证。

S0成果保留；当前优先由负责人补齐S1实施方案，不再由执行者从粗路线自行开展全面侦察或设计。

## S1：先完成新书到全书基线

### S1-A 当前可实施合同：结构化故事线确认与基线启动

#### 30a6f053复核：当前执行真实生成失败的定点修复（优先于下方历史动作）

2026-09-15 Codex已读真实探针SQLite、日志、适配器及生成入口，并亲自查看1440故事线与390方案截图。本轮不再重做账号接线、持久化门禁及上一轮页面测试。继续由GLM在auth-release@30a6f053隔离实施，结果写原s1.result.md。本批不部署、不开发S2，不重构无关全量旧债。

**已确认事实，禁止反复推翻已知证据**：tm2_model_calls共13次，已知input+output合计130536，1次用量未知；50900只累计成功返回的调用。deepseek三次失败输出12000，GLM两次失败输出16000，不能只归因deepseek。time-machine-design-service.call固定8000、ark-plan-model追加thinkingTokenAllowance，deepseek当前追加4000，所以12000首先是本系统请求总额度，不是已证实的供应商能力上限。方案C实际model_id=kimi-k3且HTTP400；用户已明确K3只保留主笔。方案A最终parseCandidate报ID格式错误，卷分批回调目前只查数量/卷ID/部分归一化，不能称完整合同已验证。夹具通过不能推出产品无缺陷。

**实施范围与顺序**：

1. **先修探针计量与退出，不再空等**（scripts/quality/s1a-http-real-probe.mjs）：成功和抛错knownUsage都累计，未知用量明确标未知，不按0计；调用前为当前并发/在途请求预留其输出及推理预算，已知累计+预留到上限则阻止新调用，失败后归还/结算预留。保留最多100调用/600000 tokens及外部3600秒上限，不得擅自扩大。解释token门槛基于供应商上报与保守预留的能力边界，不能承诺对失实usage绝对控制。A/B/C全终态失败应立即结束并把每轮phase/error/状态写result；不等待“至少一个成功”耗完45分钟、不再stateRuns=null。输出仅脱敏诊断和用量，不保存思维链或密钥。使用模拟成功、带knownUsage失败、未知失败、预算临界及三失败终态验证，不调用真实模型测脚本。
2. **保留长度截断分类并改变恢复动作**：定位apps/api/src/infrastructure/models/ark-plan-model.ts的stop_reason分支、model-adapter错误类型、time-machine-model-gateway.ts映射与time-machine-design-service.ts call/structured。已知length/max_tokens不得再丢成temporary后用相同长请求盲重试。以兼容的机器可读原因传递，不能靠中文message正则分型；HTTP400单独保留脱敏错误码/参数线索供诊断，不混成截断。未知结果仍保持不自动重发。只修改本次所需分类，回归其他调用者兼容性，不全局上调max_tokens或切付费端点。
3. **卷卡有界生成**：保持同一60万字样本、全书与卷的已有语义，不通过缩小字数/删字段假造通过。当前makePlan卷卡每批两卷；新任务快照版本采用逐卷生成，沿用完整骨架紧凑视图和前卷交接，限制自然语言字段到合同已有预算，明确不输出章情节。截断后保留已完成卷，不重复全书；单卷仍过大时明确失败并给可恢复信息，不无限重试。骨架阶段给紧凑结构化输出要求，长度错误不得与临时网络错误共用盲重试。沿用现有步骤存储与防重机制，不新建任务平台。旧冻结快照/步骤哈希不可改写；新策略版本进入快照/节点合同，已有失败探针保留为证据，新测试用新书新轮。
4. **合同错误提前定位**：从probe-db.sqlite的已保存成功output离线重组A候选，确定ID错误的准确字段路径和值，先写失败反例再修；不要让模型反复猜。在每卷接受进度前检查其结构、ID和相关引用，最终仍做全候选校验。优先复用time-machine-core/contracts.ts现有校验；允许精确字段反馈的一次局部修复，不得放宽ID正则、随机删除引用或重写已接受其他卷来制造通过。已合法ID、编号映射及作者中文名称保持分离。
5. **落实既定成员范围**：定位time-machine-sources.ts的effective roster、member()/eligible及其统一名册策略。新书的设计/推荐/资料/审查岗位排除kimi-k3，保留主笔；使用原本使用GLM5.3、豆包或Kimi2.7等合格模型的成员本人，不把K3成员换标签冒充别人。已有候选快照保持真实记录。设计者与该方案独立审查者不得同底层模型；人员不足明确受阻，不自动用K3补位或伪装独立复核。隔离探针不能只fixture阶段手工排除，要证明实际生产选择函数生效。定位现有策略后定点修复，不复制第二套名册到测试脚本。

**验证和停止条件**：先离线重放本次三种失败（截断、非法ID、400）并验证分别处理；模拟两卷中第二卷失败重试不重做第一卷，换版本不复用错误缓存；测试K3排除、合法成员身份及审查异模型；探针预算反例通过。只运行上述直接影响套件与相关包typecheck/build，保留已通过无关项。工程通过后，使用已有服务器授权和隔离安全做同一60万字合成输入的一次真实HTTP探针；至少一个完整方案通过审查并HTTP采用，其他方案若失败也如实列出，不称三套全可用。无采用结果就不发布，不机械增加调用或换更短样本。用量分别列成功已知、失败已知、未知，耗时用started_at/completed_at计算，不凭sleep输出猜时间。全量13项失败清单保留待独立收束，不因本批通过抹掉。

报告纠正：截图1440实际是横向顶栏与约1180宽主体，不是窄列竖导航；不得为视觉模型误读另改UI。390截图只有方案进度和底部操作，不能证明正文详情所有内容都已查看。真实交互操作记录保留，但不声称Codex亲自点击或对全部页面验收。

#### d5e4b0d1复核（历史）：两项已关闭，发布前验证结果见上方

2026-09-15 Codex核查72bd24e1/4459b76b差异，并在auth-release独立运行s1a-fixes 7/7、TimeMachineDirectionPage 13/13通过。真实账号接线、三种持久化设定阻塞状态与就绪后建轮已补齐；以下历史六项/两项不再重复实施。当前仍未发布，不等于整个S1或整体重构完成。

执行者仍为GLM，基准为隔离分支codex/auth-takeover-01-release@d5e4b0d1。继续使用本任务、原s1.result.md，不新建任务或报告，不碰S0维护工具，不开发S2。按顺序完成：

1. **补真实交互证据，复用现有harness**：tests/browser/s1a-browser-harness.test.ts已能推进真实隔离HTTP链，不重造平台。仅复查390/800/1440下勾选、添加、确认、返回操作，使用浏览器正常点击/触控及键盘；不能以DOM element.click或强制点击代替可操作性通过。先核对遮挡、禁用、动画及工具连接，保留一次失败证据；若确定工具问题，换可用浏览器再验，不反复超时。取得故事线编辑和基线方案的截图并实际查看；截图不可得就保留视觉未验证，DOM无溢出不等于美观通过。已有35秒轮询、原键重试工程证据不重跑，除非因此修改相关逻辑。
2. **准确说明证据范围**：F-gate里没有推荐时会显式POST recommendation-runs，因此它证明持久化准入，不单独证明自动交接；自动交接依据无此回退的浏览器harness。BREAK_FLAG在onRequest销毁连接，证明请求未完成时的恢复；服务端已建轮但回执丢失的场景由已有页面测试单独证明，不混为同一次浏览器结果。补充实际日志/截图的存在路径；没有独立证据文件就明确是操作记录，不能编造链接。
3. **真实模型探针只做一个合成新书样本**：先读scripts/quality/time-machine-real-probe.mjs相关调用；它仍使用旧startDesignRound(intent,key)和直连service，不能原样运行或作为本轮HTTP合同证据。优先复用当前harness的注册、建书、设定持久化准备和HTTP请求，确定性准备阶段与真正模型阶段明确分开；推荐→结构化确认→基线→审查→至少一个方案HTTP采用必须用当前实现。通过现有授权服务器环境在隔离目录/独立数据库运行，不能指向生产库，不能修改现网服务/任务或把密钥复制进文档、Git、本机文件。调用前先交代现有配置的预算与硬停止方式；只用既有用量，不新增付费或循环重跑。不能取得安全隔离环境时记录具体缺失、完成其他步骤；缺本地Key不等于已经核实服务器也不可用。
4. **收束发布阻碍，不再全库扫描**：从现有verify:full日志提取仍失败的精确测试/脚本、断言和所属文件，只形成短清单写原报告。区分构建/账号隔离/核心运行阻断与既有台账、夹具及架构债，指出本批是否新增；不得改白名单、删断言或写“基线同失败即通过”。不在本批自行重构7处数据库边界，不反复跑十分钟全量。Codex据精确清单另定必要修复范围，未解决的全量门禁仍标未通过。
5. **交付与结束**：真实交互、模型调用和全量门禁分别写通过/失败/未验证；报告用户现在可做什么、是否上线、唯一下一步。沿用原s1.result.md顶部更新和HANDOFF当前条目；本批不部署。Codex确认发布条件后再交GLM执行前后端协调发布，不能把候选API合同先单独上线。顺手删除新测试文件EOF多余空行即可，不为格式问题重复运行测试。

复验命令已由Codex实际执行：在auth-release根运行`node node_modules/vitest/vitest.mjs run --configLoader native tests/integration/domain/s1a-fixes.test.ts`；在其coauthoring-v7/author-app运行`node ../../node_modules/vitest/vitest.mjs run TimeMachineDirectionPage.test.tsx`。未改对应逻辑则保留本次结果，不为报数再跑。

#### 3a84dc98复核（历史，两项已在d5e4b0d1关闭）

已核查d5825f07/e68d7073实际差异：最终intent重建快照、事务内读取器无客户端版本后门、仓储拆分、dirty恢复标记、HTTP采用链已落地，保留相关测试，不重复前轮六项。此次为代码和证据核查，Codex未独立跑套件，未批准上线。

1. **修真实账号接线**：TimeMachineDirectionPage.tsx读取localStorage['wenmi:session-owner']，全author-app源码搜索仅此读取和页面测试手工setItem，无业务写入方；ownerIdRef空则pendingStorageKey返回null，正常用户的sessionStorage持久化不成立。复用AuthorAccountBoundary.tsx已有AuthorAccountContext及其导出hook（account-api.ts的AuthorAccount.userId为已验证账号ID），或由其现有父组件传入账号ID。不要新建localStorage身份来源/补伪造写入。账号未加载时不提前标pendingRestored完成；账号/书籍变化须重置恢复、dirty和未决引用，禁止重发上个账号/书籍的请求。测试使用真实会话Provider与userId，不再setItem不存在的身份键。至少验证正常登录→未知响应→卸载重挂载保存/重试原键，以及账号切换不读旧记录。
2. **补持久化门禁证据**：s1a-fixes.test.ts的HTTP链仍在约137行spyOn timeMachinePrerequisite→ready:true，能证明HTTP采用与快照一致，但不能证明真实设定流程。保留该用例并另补原S1要求的实际数据库状态：批次未完成、条目未确认、最终总清单未完成分别409且零设计；确认/总清单完成后同一有效请求可创建。用既有setting部门fixture和实际service/HTTP推进（模型响应可fixture），不mock门禁函数或用手工pv字符串冒充持久化来源。已有版本变更/回滚测试不重复。

只修这两项后跑相关定向/页面验证，再完成隔离浏览器390/800/1440（正常会话、刷新、未知响应、编辑不被轮询覆盖），不等上线后才验证。真实模型探针仍未完成，按既有授权环境安排；缺条件明确记录。verify:full旧债保持未通过，不能说全量绿。不要再改维护工具、扫描全库或开发S2；原s1.result.md更新准确交付状态。

#### 90328d31集中核查后的当前动作（覆盖“已完成/进入发布”）

Codex已核查e58856a1差异、实际函数和部分原始失败日志；未独立运行测试。功能已有进展，当前未通过发布验收。只按下面条目修本批缺陷，保留已通过成果，不重做S0/身份/全项目盘点。继续在现有隔离分支实施，报告仍写原s1.result.md。

1. **快照一致性，首要修复**：startDesignRound先snapshotTimeMachine(...,'',...)再只赋base.intent，导致manifest中intent哈希及documents的intent正文仍为空。推荐校验可以使用只含上游签名的读操作，得到规范intent后须构建完整最终快照（仍在同一事务），不能只改单字段。用真实保存的snapshot断言intent/document/hash一致，再通过实际HTTP adoptions走一条生成→审查→采用；不能直接仓储adopt或mock来源绕过路由重建manifest。这是本批新增问题，不是旧基线债。
2. **真实事务门禁与回放**：路由在事务外requirePrepared并传version，startDesignRound信任传值，因此所谓“全在事务内”不成立；ready变为false时旧请求回放也被路由先拦住。把当前ready/version读取放到事务内的新轮创建分支（可注入读取函数，但不得接受客户端版本作为当前事实），已有同键规范请求在鉴权/归属核查后回放，不要求重新就绪。加真实持久化状态测试，不用字符串pv-*证明设定版本：含批次未完/未确认/总清单未完、相同请求重启回放、开始校验后状态改变、创建第二方案故障回滚。保留既有采用前上游版本复查。
3. **页面恢复只做一次，不能覆盖正在编辑**：恢复effect依赖每次refresh新建的restoredSelection/restoredRecommendation对象，每次轮询都会set全部作者输入。按bookId+roundKey标记已恢复，作者dirty后不得重灌；新推荐与旧设计来源不一致时不能把旧ID套到新推荐。添加重新设计页编辑自添/备注、触发两次后台refresh仍保留输入的测试。
4. **跨刷新防重**：designKey及签名目前仅useRef，卸载丢失。用当前会话与书籍隔离的sessionStorage保存未决请求完整selection+key，在发送前持久化；响应未知/刷新重试原请求原键，有确定成功回执再清除待发送记录，state中roundKey可核对已有结果。退出/切换账号不得读到另一账号草稿；不要引入跨用户全局缓存。网络失败后不必要求用户重新填字；作者明确修改后另建请求。浏览器验证模拟服务端已创建但响应丢失→刷新→重试，实际任务数仍一轮。
5. **严格解析**：authorNote传数字/对象现在默默转空串，不符合严格校验；拒绝非字符串，按合同保持中文错误提示，不静默丢作者输入。复核新增解析反例即可，不扩成全库校验。
6. **本批新增架构违规必须修**：storyline-selection.ts直接查db已增加application-database-boundary失败名单。提取为现有infrastructure/db/repositories下的相关查询（无新表），或复用仓储；纯解析/规范哈希留application，校验接收已读取行。不得通过加白名单或删除门禁掩盖；旧7个违规另记，不要求本批全部重构。逐条对比全量失败的测试名称、断言差异，不能只比较失败总数。保留基准提交准确值，不再stash造成untracked污染；只必要时在干净隔离基准复核。verify:full仍未通过则如实标阻碍，不批准直接发布。

修复后先跑新增反例与受影响定向套件，再按已选风险范围验证。不先重复十分钟全量找错误。浏览器三宽度与真实链路探针在发布前的隔离环境执行，不需要先上线；现有密钥环境不可用就记录具体缺失，不能申请泛化权限或将验证拖到生产切换之后。暂不发布、不扩S2。

**结果**：作者仍在现有故事线页勾选/添加后点一次确认；后端保存准确选择与来源版本，才建立基线设计任务。不能仅凭设定就绪直接绕过故事线，不因刷新/超时丢选择或重复开三套任务。不增加独立确认弹窗或新页面。

**基准**：使用现有auth-release隔离工作树，先核对9d24ffdf及之后是否有他人提交；保留54f249a6工具改动但不执行。主区e9800b9d是派工规则更新，请按该提交的本任务最新版本阅读，不盲合整套脏主区。允许本批隔离提交；完成后交Codex一次集中验收，本批先不生产发布（API请求合同变更需协调前后端发布）。不要重复S0、全部RB盘点或身份测试。

**已核实代码与缺口**（路径相对仓库根）：

| 入口 | 当前行为与本批动作 |
|---|---|
| apps/api/src/application/books/v7-setting-editorial-service.ts：timeMachinePrerequisite，约224行 | 已有设定批次/全部confirmed/总清单结果门禁。保留；用真实持久化状态验证，不先改正确实现。enqueueSettingHandoff在确认/最终整理成功处已有调用 |
| apps/api/src/application/books/setting-time-machine-handoff.ts：dispatchSettingHandoffs | 已有版本化pending交接及recommend-initial键。保留自动推荐；本批不重写调度器 |
| apps/api/src/http/time-machine-routes.ts：POST design-runs，约87行 | 当前只requirePrepared+字符串参数检查，即调用startDesignRound；未核对推荐是否成功及作者确认来源。增加结构化请求校验和业务冲突响应 |
| apps/api/src/application/books/time-machine-design-service.ts：startDesignRound/createRun，约58/71行 | 当前接受自由intent，snapshot在事务外生成；createRun用snapshot哈希防重。将确认来源校验、选择冻结与三方案创建纳入同一同步事务；保留原执行器/审查/用量机制 |
| apps/api/src/application/books/time-machine-sources.ts：TimeMachineSnapshot/snapshotTimeMachine | 当前仅记录intent文字及开书/设定manifest。新增可选selection快照元数据，设计轮保存经验证的结构化选择；不建第二套事实库 |
| coauthoring-v7/author-app/src/TimeMachineDirectionPage.tsx：startDesign，约191行 | 当前将勾选拼成自由文字，每点击生成Date.now键；改发结构化来源和选择，同一未决操作保持键，已保存选择可从state恢复 |
| coauthoring-v7/author-app/src/time-machine-direction-api.ts | 同步请求/响应类型；普通作者只收到必要选择信息，不回传完整snapshot/内部成员配置 |
| tests/integration/domain/s1-setting-baseline-gate.test.ts | 新增测试目前ready为mock且前后payload不同；补真实状态与同payload对照，保留现有拒绝断言 |

**请求与存储决定（已定，不让执行者重新选架构）**：

1. POST design-runs保留现有URL，Body改为 `{idempotencyKey, selection:{recommendationRunId, recommendationHash, preparationVersion, selectedLineIds, addedLines:[{title,description}], shape:'auto'|'single'|'multiple', ensemble:boolean, authorNote:string}}`。前端的shape实际值先按现有枚举做明确映射。服务端不给旧intent-only请求继续启动的后门，返回400并提示刷新页面；旧结果仍能读取，不回填/修旧书。
2. state的成功推荐投影增加服务端计算的recommendationHash（对实际推荐result_json规范摘要）和对应preparationVersion；不可用前端伪造这些来源。服务端确认推荐为本owner/book、kind=recommend、state=succeeded、有合法lines，开书/已确认设定manifest与当前一致，preparationVersion匹配；跨owner/book按现有404合同，不泄漏对象。
3. selectedLineIds去重且必须存在于该推荐；名称/description/role取服务端推荐，不能接受客户端改写既有推荐内容。自添线独立存title/description。允许全自添但总选择至少一条；未知ID拒绝，不能静默丢弃。shape/ensemble/authorNote严格校验，保留作者文字含义。
4. 选择ID≤30、自添线≤20、标题≤80字符、描述≤500、authorNote≤1000；最终供生成的intent沿现有格式构建且≤4000字符。超限明确提示精简、不截断；这是请求边界不是文学章数/故事线数建议。运行反例覆盖中文、空白和超限。
5. 经校验的selection连同来源ID/hash/version与规范requestHash写入每个设计run的snapshot_json（可选字段，旧快照仍可读）。所有A/B/C共享同一selection；已有snapshot列足够，本批不新增数据库表/迁移，不修改已合并迁移。与建立设计轮同事务，失败无半轮；不使用文字关键词反解析选择。
6. 幂等先按owner/book/round_key读取已有轮，比较已存规范requestHash：同键同请求返回原轮，响应丢失后不因后来配置变化新开任务；同键不同选择409。新键才校验当前来源并创建。旧轮无此元数据不能猜测匹配，不为旧书做迁移。上游改变后原轮可读，不允许旧请求当新请求创建或采用过期结果；保留现有采用时manifest校验。
7. 事务只有一个所有者：在startDesignRound内BEGIN IMMEDIATE后完成来源读取/版本检查/快照构建/createRun/COMMIT；路由不嵌套事务。不在事务中调用模型/网络；现有start(scope,'design',...)若可绕过确认需改为内部受控入口或拒绝无selection的新设计，更新直接调用测试，不留第二条漏校验路径。
8. 页面保留现有“确认并设计”动作。生成键在一次提交开始时冻结，网络结果未知重试同请求同键；作者明确修改选择后才新键。state仅投影设计轮selection必要字段，刷新可查看当次实际选择；不要把结果默认recommended列表重新当作者已选。来源过期保留用户自添/补充并提示重新核对，不自动替作者确认新推荐。

**阅读边界与保留项**：只读上表文件、相关同目录类型/既有fixtures、time-machine-design/schemes/handoff测试、TimeMachineDirectionPage测试，规格第23节相关确认/版本段及R209当前上下文限制。已通过身份/维护测试不读不跑；不改模型目录、15000预算、创作库内容、提示词文学规则、VP-01及卷链章页面。真实模型调用仍复用现有资料包/检索/审查；本批未发现质量缺陷不顺手改prompt。

**执行与验收顺序**：

1. 在现有测试建立失败反例：真实设定已就绪但无selection直接design-runs不得创建任务；以目前实现跑出缺口再修。复用v7-setting-editorial-department.test.ts已有建书/条目确认/最终整理fixture，允许模型响应为夹具但不能mock timeMachinePrerequisite。没有必要从UI重复生成24项设定。
2. 实现服务端结构化确认、事务、防重、最小state投影；再改前端发送与恢复。相关新测试保留在现有S1测试文件和现有页面测试，不另建测试平台。
3. 验证真实数据库状态组合：批次未完成、存在未确认项、总清单未完成均409且零design/模型调用；就绪但无/失败/跨书/过期推荐仍拒绝；有效推荐+作者选择成功创建一轮且三方案selection一致；同payload重试同ID，同键不同payload409；模拟部分创建失败回滚；重新启动app后同键仍防重；采用前上游变更拒绝。对照请求payload必须相同，仅状态不同。
4. 浏览器390/800/1440核验勾选、自添、确认、刷新、响应丢失重试、来源过期反馈；无需重做整站视觉。至少一条真实后端隔离新书推荐→确认→基线→审查→采用链，模型先用已有确定性fixture证明工程流程；真实模型只在工程通过后用一个合成书样本和现有预算做一次完整探针。外部失败保留检查点，不反复付费重跑，不能冒充文学通过。
5. 定向命令（仓库根）：`npx vitest run --configLoader native tests/integration/domain/s1-setting-baseline-gate.test.ts tests/integration/domain/time-machine-design.test.ts tests/integration/domain/time-machine-schemes.test.ts tests/unit/setting-time-machine-handoff.test.ts tests/integration/security/time-machine-routes.test.ts`；页面：`npm test -w @wenmi/v7-author-app -- TimeMachineDirectionPage.test.tsx`。变更了设定服务才加其部门套件，不为报总数重复跑50项。
6. 本批改变核心工作流与事务准入，按ACCEPTANCE在定向通过后执行一次`npm run verify:full`。仓库脚本存在已知构建依赖顺序风险：opening-runtime应在v7-backend之前，按已验收build-candidate的拓扑构建；若验证命令实际因此失败，集中修package.json对应构建/校验顺序并记录，禁止靠残留dist造绿或重新审计发布工具。不新增依赖。原始输出落证据文件，只报告摘要。
7. 在原S1结果文件`.local/dispatch/outbox/task-rebuild-closeout-01-s1.result.md`交付：实际新增行为、修复文件/提交、各证据类型、等待/总耗时、未知用量如实标注、未上线。更新执行文档对应RB-22和HANDOFF当前入口。若无其他修改，不将整套S1报完成，S1-A与其余真实链路分别说明。下一步由Codex核查差异并安排前后端协调发布，不自动开发S2。

该合同的运行风险是请求合同更新、来源过期及幂等语义变化，因此先完成隔离证据再发布；不是重写时光机。发现上述静态判断已被更新代码解决，给出实际符号与反例证据后跳过该已关闭项，不照文档重复开发。

依据docs/TIMEMACHINE_STORY_DESIGN.md第23节、现有开书及时光机实现、R209当前合同核查并补齐。保留已认可绿色视觉和现有功能，修流程而非新造同义入口。

- 设定全部所需项完成且作者确认，主编整理正式设定总清单后，才能启动资料整理与故事线推荐。后端持久化前置条件及版本校验是权威，不能仅隐藏按钮；页面访问不越级触发。
- 资料整理→校核→主编推荐→作者修改/新增/重新设计→确认故事线→全书基线，逐步真实状态可见。成功成果持久化；刷新、多标签、重复点击、重启、超时、迟到结果都不得重复任务、污染版本或重复扣量。
- 资料包简洁、结构化、有来源；全请求15000字符上限包括岗位/系统/工具/历史，不能截掉关键事实凑字数，不强行填满。基线复用已核对书籍包并加入本次确认的故事线版本。
- R209按用途/阶段少量检索，输出引用的方法编号与简短采用理由即可，不保存思维链。候选、作者要求、确定设定和正文事实有边界。
- Kimi K3仅主笔可出场，其他岗位选择已有合格成员及其真实姓名头像模型；单人设计的审查主编不得同底层模型。无合格审查人明确受阻，不冒充独立审查。
- 状态页、时光机、后台读取同任务事实；失败不假装运行；多方案各自成功/失败/恢复，保留成功项。模型结果未知先核对，不能盲目重发。
- 完成新建书完整真实后端走查及预算内合成创作真实模型探针，验证设定未确认不得推荐、确认后可推荐、确认故事线后可生成/采用基线。浏览器至少390/800/1440宽度，刷新和失败恢复。模型失败记录真实原因和耗时，不用mock冒充成功。

## S2—S4：按已有规格补齐创作链

S2：VP-01以时光机规格第24节为唯一计算方案（默认6—10卷、每卷不超过50万字，短书例外按原文，不机械额外绑定阶段）。落实卷开始/收束、转折与人物变化、跨卷主支线职责、预算、确认后编号、改动影响预览。卷A/链A1/全书章号/主线支线独立稳定ID与中文短语显示；排序与ID分离，规划确认不冒充正文事实。

S3：RB-23/24/25/32，卷设计链→链页面结合剧情设计人物→链分章及章纲。已有角色复用实体和状态，新角色仍为候选直到采用；别名/同名不能错误合并。链页面分章才能选择新增节奏余韵卡，余韵可0—3章；全书和卷粗节奏不强制余韵；原卡与链专用卡适用阶段不能混淆。每阶段检索、提示、输出、编辑、审查、采用、状态和后台一起闭环。

S4：RB-26/27/27.1/28/29，正文完整版本→审查→采用→有来源事实提取/校核/结算→检索与相关状态更新→下一章。用户正文安全保存与后续整理分开；关键事实失败阻断依赖生成，辅助投影延迟不无故阻塞。语义判断交Agent，程序管权限版本事务预算防重。不能把“正文含收束短语”当自动卷结束判断；由Agent结合上下文证据给建议及状态更新。冲正保留证据，不自动覆盖已接受正文。至少验证连续多章、修订、同名/时间视角/否定、故障重试及跨书隔离；真实文学样本与工程测试分别报告。

S2—S4每阶段开工在对应原规格/清单记录具体文件、接口、权威表、输出与验收；依据现有功能做必要工程设计。不要臆造固定三套方案、固定剧情节奏等新硬要求。

## S5—S6：全范围对照与旧实现退出

S5按S0查明的全部RB缺口处理剩余作者端、书架/编辑/导出、人物/Wiki/图谱/检索、后台分类与编辑管理、模型/提示资产治理、用量与商业、账号外部通道及存储/任务底座。已有规格定稿的继续实施；依赖邮件凭据、付费服务、重大产品选择等只标明具体阻碍及受影响功能，继续无关项，不能伪造完成或擅自开通奖励提现。

PG与剩余旧适配退出先核查已实现的新核心和实际调用边界，制定单一权威的分批切换合同、迁移一致性和回退证据；未经证据不直接切整库。不复制旧业务到新目录宣称重构，不为删名字破坏正在使用的正确实现。

S6每个已替换模块查启动、动态注册、调度、路由、读写、测试、恢复引用；确认无在用依赖后删废弃代码和专属无效测试，保留共享正确能力。生产旧代码目录按精确白名单及实时链接/进程引用检查清理；含作者数据的目录不按代码目录处理。主线、后台、正式规格和发布版本一致后交总体报告。

## 验证、发布、连续执行规则

- 按ACCEPTANCE风险矩阵选择测试，核心工作流/权限/数据语义与恢复变化做规定全量及真实服务验证。失败必须修复，禁止改预期凑通过。
- 阶段一个逻辑提交，列最终文件、通过/失败/未验证、真实模型/浏览器证据、实际发布及回退版本。下一批具体方案已准备且获授权时可继续，不要求老板逐批回复“好了”；Codex集中反馈具体问题。
- 发布严格DEPLOY：备份有效、空间门槛不降、API/Worker真实在途连续30秒为零且立即复核，不取消任务制造窗口；API健康后再Worker，前端单独发布。无法安全发布则保留候选，继续独立本地工作。只传已授权服务器，不传作者作品给新第三方。
- 总体“完成”只按执行文档10.5：所有入口有去向、关键真实创作/商业路径通过、已定范围无未处置阻断缺陷、部署与旧实现退出有证据。阻碍/待定/未验证不能打勾。不能承诺零BUG或把文档齐全当完成。
- 本任务可跨会话接续。会话结束前保存当前阶段、提交、失败证据与下一条可执行动作；恢复先看这些，不重复已关闭工作。
