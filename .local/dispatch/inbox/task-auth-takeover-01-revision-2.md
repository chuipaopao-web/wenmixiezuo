# AUTH-TAKEOVER-01 返工2：补齐最终提交校验与真实回退

继续原工作树codex/auth-takeover-01，基于c7f2fdd3；读原任务书和outbox/task-auth-takeover-01.codex-review-2.md。原保护/提交/生产禁令继续。不要重做已通过的密码覆盖修复和整个业务，不修改Codex审查证据。

1. 修R2-1/R2-2：所有异步密码工作结束后，在最终同步事务中重新验证账号active、期望凭据/安全版本及完整CAS条件；改密还必须重查发起会话属于同一账号、未过期、未撤销。身份变化时拒绝或有界重试。安全状态变化与会话签发/撤销/审计需有明确顺序和原子性，不能第二次await后沿用旧current。不要只给测试中的setUserStatus加一个字段而放任其他路径绕过；明确凭据升级、停用/恢复、登出与撤销对应哪种版本/会话屏障。
2. 增加确定性并发反例：停用发生于首次验证期间和升级hash期间；停用后恢复旧会话不得复活；改密hash期间发起会话退出/被撤销/过期；改密期间停用；审计或会话写入失败的事务一致性。对照Codex probe复现修复前失败、修复后通过，不用不稳定固定sleep代替明确阶段控制。
3. 修R2-4：已知/未知账号登录、当前密码验证、新密码派生都走支持参数校验和同一有界资源调度。共用已有域函数，避免identity-service另行裸调scrypt。保持不同错误的对外凭据响应与限流语义，记录并发上限、等待上限的实际测试证据。
4. 修R2-3：当前新代码复制一份不能叫回滚。基于已知稳定基准构建最小兼容回退目标（密码v1/v2读取、0125及新增安全语义兼容），不回退至固定v1密码验证。回退目标必须能绕过本次新身份实现的潜在缺陷，有独立来源commit/文件diff/hash清单，明确新增端点回退后行为。用打出的真实包在独立进程执行main/bootstrap、迁移与HTTP登录/登出/权限、混合凭据、旧会话、owner/权益完整性验证，再切回新包；不能导入本地src当作打包验证。Worker依赖、资源路径、迁移校验和幂等需有证据。若无法做到，保持发布阻断，不伪称已通过。
5. 更新原result.md：将旧的“回退旧dist即可”、缺省改密行为等错误历史描述显式标为已废止，当前状态只引用最新有效证据；完整接管/本地验证/PG未验证/生产未部署分别写清。不以8项测试通过代表全面回滚或全部重构完成。
6. 修复后跑受影响身份测试和本次新增反例，按原任务要求完成可执行的总体检查。此前确认同样存在的业务基线问题继续记录，不为绿色改创作规则。提交增量及完整结果报告后停止，等Codex验收，不自行合入/部署。
