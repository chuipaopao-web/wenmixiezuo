# B2第二次定向返修：事务隔离与应用服务授权

继续原B2隔离分支，基于7e1316f1增量提交。先读主目录`outbox/task-209-b2.codex-review-2.md`。原B2和首次返修白名单持续有效，不reset、不重复开发已通过的UI/筛选/关系/分页，不改主目录产品、不部署/推送/合入/续批。

只收尾三项：
1. 共享transactionDepth导致独立请求混用事务。选择可证明正确的事务边界：同步SQLite工作单元或正确的同请求归属+连接级隔离。单加深度/全局布尔/AsyncLocalStorage不够。新正式测试真实制造A未完成时B到达；B不得提前成功后被A回滚；A失败B成功与反向、两者成功各自持久化、并发读取不可见未提交内容。避免用串行await或sleep假装并发。
2. BEGIN失败后连接/事务状态恢复：第二连接持锁使开事务失败，释放后同实例可正常重试；后续故障仍整体回滚。不强行清零掩盖真实未结束事务。
3. 管理应用服务恢复授权端口与接口依赖：每个写入口都校验管理资格和非空actor，不能靠任意actor字符串或只靠HTTP。通过Repository/事务接口注入SQLite实现，不反向依赖基础设施具体类。未授权调用返回拒绝且无写入。

现有Codex并发探针仅为缺陷示例，正式测试须适配修复后的合法接口；如串行策略会等待B，使用并发启动+B完成观测+A释放屏障，不让测试自身死锁，不删除断言假通过。

复跑六文件42项+新反例、API/后台类型检查；若前端代码未变无需重复拍全套截图。结果继续写task-209-b2.result.md，保留旧记录，列根因/实现/断言/实际结果。任务书保留原位，完成提交后停止等Codex验收。
