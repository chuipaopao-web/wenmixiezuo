# AUTH-TAKEOVER-01：发布清单纠正与Linux隔离预验

2026-09-14，老板要求能由GLM完成的工作全部交GLM，节约Codex额度。本任务由GLM连续执行：纠正发布清单→跨平台准备→Linux隔离构建与测试→迁移/回退演练→交证据，不逐步等老板确认。Codex负责关键判定与最终切换验收。此前产品缺陷已关闭，不再重写功能。

## 接管与权限

沿原工作树codex/auth-takeover-01，当前d2555484，开始先记录实际HEAD/状态。读根AGENTS、HANDOFF、docs/DEPLOY.md、原任务与Codex最终本地验收记录。前批报告在outbox/task-auth-takeover-01-release-prep.result.md，发布清单同名目录release-checklist.md。

允许：隔离分支修改发布/构建/演练工具、直接相关测试与文档；用既有SSH凭据向已授权admin@wenmixiezuo.com上传本任务私有代码/发布物至/tmp/，在/opt/wenmi-releases/下新建本批唯一隔离候选目录（不要覆盖已存在目录），进行Linux预验。服务器仅使用已有运行时/资源，不安装系统软件或引入付费。受阻先诊断，完成独立部分并如实记录。

禁止：合入主工作区、正式发布/切换链接、改systemd/Caddy/生产配置、停止或重启生产API/Worker、改写/取消在途任务、迁移或恢复生产库、删除书籍/用户/旧发布包。生产SQLite和/opt/wenmi/data不得作为测试写入目标。只用本批隔离空库和合成数据；生产只读可查版本/表结构/迁移名及hash/匿名聚合，禁止输出账号秘密/作品或复制到外部。密钥和生产.env不打包、不打印、不进入日志。

## 必须先纠正（当前清单不予直接执行）

1. 删除“Worker不需重启”步骤。docs/DEPLOY.md明确wenmi-worker Requires=wenmi-api，停止API会连带停止Worker；正式发布预案必须在途连续至少30秒为零并立即复核，再按API→确认新release健康→Worker→核对新PID/版本/新鲜心跳顺序启动；回退同理。本批不执行这些生产动作。
2. 删除“回退更旧v1-only版本并重置用户密码”。唯一可用回退必须支持既有v1/v2数据且包含已应用迁移。用户不应为发布失败改密，不能降低散列/删除迁移或恢复数据规避。
3. “0125在生产SQLite幂等重入”改为隔离合成库按现网迁移版本演练，生产只读比对兼容前提。不在生产跑测试迁移；不可用空库成功代替旧审计记录/账号/权益迁移验证。
4. 纠正“9项基线失败Codex确认”表述：GLM有基线自测，Codex只独立跑过指定身份/回退套件，未独立核实这9项全部与发布无关。列每项真实命令、基准commit、失败断言、是否影响本次运行及理由，不能只凭旧commit也失败排除风险；影响本次发布且可在范围内修复的应修复，不能删测试/放宽检查。涉及独立创作规则则记录具体阻断，不扩展猜改。

## 连续实施

### A. Linux产物与依赖闭包

记录候选commit、当前生产release（只读）、Linux Node/npm实际版本、受影响包。基于lockfile和真实workspace声明构建候选与独立回退包，不照抄Windows绝对路径/junction/cmd。包含必要workspace模块、API/Worker运行产物、迁移、启动所需配置及公共资产；验证从本批候选目录即可加载，不偷用运行目录的可变dist。生产node_modules不原地npm ci或覆盖。新目录内使用已有缓存/允许的包安装，失败如实报告。

回退基于已有稳定基准加最小双格式/迁移兼容补丁，保存完整补丁、基准commit、hash、可重复Linux构建步骤。回退包自身必须在已迁移合成库启动并接受新旧散列。不能打包本次新身份代码本身冒充独立回退。

### B. 工具安全

目录/归属/重复执行/脏工作树/输出已存在保护要实际测试。--force不得隐含覆盖未知目录。只清理本任务创建且归属明确的临时资源，绝不递归删除共享或生产路径。空闲端口动态选择或严格检查，启动后确认为自己的子进程和release；仅时间戳不证明PID归属。正常及失败路径等待自己子进程退出，不能kill占用端口的未知进程。

### C. Linux独立预验

清除测试进程模型凭据，禁用外发邮件/真实模型/真实支付，仅合成账号、假模型网关、独立数据库；禁止把生产.env原样加载到预验进程导致连接生产数据或后台副作用。使用专属端口，不能绑定43111等现有服务端口。Worker若需测试必须与独立配置/库配套，不能启动消费生产任务的Worker。

执行相关身份/权限/失败审计/会话/迁移/入口/创作库C6阶段检索测试，保留原18项与适用既有套件。按最高相关风险做构建/验证，实际失败归因，不用Windows结果冒充Linux。

新包→兼容回退包→新包三阶段真实HTTP/新进程/迁移器演练：v1/v2登录、登录升级、错误失败留痕、改密/撤销、旧会话、admin/user隔离、owner/权益不变、重复迁移、回退端点行为。用有历史审计记录的迁移前合成库演练0125，比较行内容和约束，注入失败确认不留半迁移。记录产物hash与所测包对应关系；不是导入src代替发布包。

### D. 最终交付

更新本次发布清单及后台实际读取的本专项状态，区分“本地已验收、Linux预验、自测/独立、未生产切换、PG未实施”。不要重放已发布的C6公共卡片；保留creative-r209-c6运行层待随候选切换的事实。新功能流程不变。

报告：D:\wenmixiezuo\.local\dispatch\outbox\task-auth-takeover-01-linux-preflight.result.md；证据目录同名。必须有最终commit、上传物/远端候选和回退路径、manifest/hash、完整命令和退出码、测试/失败归因、当前生产未变的版本/服务状态证据、待正式切换精确步骤与剩余阻断。不得包含密钥、密码、cookie或作品。修订前批报告错误主张，不覆盖Codex审查原件。

达到交付或真实外部阻断后停止，告诉老板结果路径。没有本批Linux预验和关键验收，不执行生产切换。不要因“Codex来做”跳过GLM能完成的构建、测试、排错、文档或打包工作。
