# AUTH-TAKEOVER-01：登录、权限和路由接管，直接实施任务

状态：待老板在ZCode中触发GLM5.3。不是只读审计任务：按下列顺序完成本地开发、集成、测试、死代码清理和发布准备；普通工程决定自行解决，完成一批后继续下一批，不等老板逐批确认。

## 1. 目标及边界

老板最新要求：GLM直接做完新后端接管，并清理死代码，保护已工作的开书和设定。Codex本轮只提供任务书，不代替GLM开发。唯一产品规格：docs/REBUILD_DEVELOPMENT_SPEC.md“后续专项：新后端接管登录、权限与运行入口”；本任务是执行合同，覆盖其旧的“只记录、未派发”状态，不扩大为整站业务重写。

完成指新入口实际装配认证、会话、权限及现有业务路由，旧入口不再被运行代码依赖，相关本地流程和回归通过，清理可证明废弃的代码并提交可审阅成果。不能以改名v7-server、复制整文件或转调旧createV7Server冒充接管。缺外部凭据/迁移证据等情况如实列未完成，不伪造闭环。

本批可在隔离分支完整开发和提交；先交Codex验收，**本批不合入主工作区、不推送、不部署、不修改生产服务/配置/数据库、不清理服务器**。生产身份切换及删除线上旧文件必须在验证和明确切换清单之后实施。本地死代码清理是本批交付，不要求只出建议。

## 2. 工作区与必读

- 基准：be68d87816e5cee4b4644db52cd3ec5f21de09c2；开始时核对HEAD及工作区，若已有后续提交，记录差异，基于包含最新授权成果的提交工作，不退回覆盖新变更。
- 分支：codex/auth-takeover-01。工作树：D:\wenmixiezuo\.local\dispatch\worktrees\auth-takeover-01。只在项目内工作；若同名工作树已有内容，先检查并续做，不删除重建。
- 主工作区两个任务文件已有删除改动，禁止带入或恢复；禁止git reset --hard、git checkout --、git clean及全局格式化。
- 先读根AGENTS、HANDOFF、docs/DEVELOPMENT_WORKFLOW.md、docs/ACCEPTANCE.md、docs/REBUILD_EXECUTION_PLAN.md当前接管状态；读适用目录AGENTS及Skill。参考第111批账号合同及当前真实代码，不要求通读所有历史。
- 只使用既有本地运行时与隔离测试资源。不输出密钥、口令、会话、作品；不调用真实付费模型、不发送真实邮件。已有本地PG能用则复用测试设施，不修改系统数据库或安装/重配系统服务。

## 3. 已核实现状，禁止误判

2026-09-14本地apps/api/src/main.ts调用http/v7-server.ts的createV7Server；它装配AccountAuthService、request-policy、账号、后台、开书、设定、时光机等路由。

开书路由apps/api/src/http/v7-opening-agent-routes.ts、设定路由v7-setting-editorial-routes.ts已调用application/books服务，并使用@wenmi/v7-backend；不能因为命名含v7就认定未开发或废弃。root package.json也引用rebuild的legacy-opening、agent-catalog、time-machine-core。先以实际调用图判断，保留最近的开书/设定/创作库行为，不能退回早期rebuild页面覆盖线上新版。

rebuild/apps/api/src/create-server.ts、account-routes.ts及rebuild/packages/backend内账号服务已有实现；第111批只有本地账号基础验收，不等于公开注册、发信、生产映射全部完工。复用前实际跑测试、核对接口和数据语义。

C6公共库和后台静态已上线，运行层代码在基准提交内但尚未部署。保留chain_chapters、32张独立卡、余韵0—3章等成果；不得用C5生产源覆盖本地。时光机当前失败/运行状态异常属于另一任务，不顺手改生成、计费或任务状态来让测试通过。

## 4. 允许修改的范围

- 新装配与身份：apps/api/src/main.ts、apps/api/src/http/中server/bootstrap、account/auth路由与request身份适配；apps/api/src/infrastructure/security/；与身份接管必要的runtime-config、数据库身份仓储/增量迁移。
- 复用新后端：rebuild/apps/api/src/、rebuild/packages/backend/src/、rebuild/packages/contracts/src/中认证/会话/权限/装配及其必要适配。范围不是允许改里面所有业务。
- 业务路由只改认证上下文/服务注入接口及相应import，不能改开书、设定、时光机等创作规则、提示词、模型名单、生成顺序或事实存储。worker仅允许身份/启动配置所必需的适配，不能改任务状态机。
- 当前作者站与管理后台仅允许认证API、会话过期、权限跳转的必要兼容；保持页面样式、导航和产品行为，不新设计页面。
- 相关tests、rebuild/tests、局部测试；scripts/release/auth-takeover/下发布预演/兼容/检查脚本；package.json/lock/tsconfig仅必要依赖和构建接线，不升级无关依赖。
- 直接更新本任务结果、HANDOFF、两份重构主文档的本专项状态与后台实际读取的对应路线记录；不把整站标完成。
- 上述依赖的确切文件在P0建立实施白名单。发现必要遗漏，可在本目标内用证据补充白名单后继续；不得以此扩展到创作业务重写、数据删除或生产操作。

## 5. 连续执行的批次

### P0：短审计后立即进入实现

列出入口→路由→身份→权限→业务服务的实际调用图，记录文件/符号，不只列目录。核对新旧登录、注册、退出、会话、账号停用、密码与管理权限能力；列明现有作者/管理员接口及书籍、会员、用量、任务如何取owner身份。先跑相应基线测试，区分已存在失败和本批回归。

输出implementation-map.md（结果目录内），给出复用、适配、替换、删除决定与必要文件白名单，然后继续P1，不等待批准。优先既有新账号服务；存储实现以它的真实约束选择，不另造第三套身份库。不为删v7字样强行迁移全部创作数据。

### P1：新身份与权限接管

明确唯一身份权威和owner映射。业务身份只来自验证过的会话；普通请求不能设置admin、owner或其他书归属。所有已公开业务路由统一使用新身份上下文，不能只迁登录而其他路径继续从旧cookie、header或配置owner兜底取身份。

保留当前对作者已提供的登录/退出/注册及相关安全功能和前端响应合同；新后端缺失能力应补齐兼容，不删除接口或新增未经确认的登录门槛。确需产品决策或外部发信而无法完成的功能，明确阻断生产切换，继续完成独立工作；不伪造邮箱已验证或邮件已发送，不开放管理员自注册。

会话过期/撤销/停用/改密后失效、错误响应、Cookie和来源防护、登录限流均需实际验证。密码与会话格式以现有可靠实现复用，不自研密码学。历史散列仅验证兼容；不能清空账号、重建owner、重置密码或丢权益来简化。

若新身份PG、业务SQLite并存，清楚界定唯一权威及适配：身份迁移先建立稳定映射、核对后切换，不双写两套身份、不声称跨库普通事务原子。不可跨库原子操作必须有失败恢复和幂等方案。仅在合成数据演练迁移；已合并迁移文件不能重写。

### P2：新运行入口与业务联调

建立独立server/bootstrap和显式模块注册，main实际使用新入口；新入口不能反向调用旧createV7Server。保留健康/worker状态、错误封装、作者响应脱敏、CORS/来源校验、body限制、请求ID、依赖注入和现有API路径。业务模块可暂经有退出条件的适配器接入，但不能遗漏路由或形成循环依赖。

保持开书、设定、创作库、时光机、后台、书架、会员/用量接口行为。仅用合成账号与可控模型网关验证同一个新会话能走到当前开书/设定任务路径、查询和确认，以及普通用户无法访问管理员接口/其他用户书籍。假网关结果只证明业务接线，不当作真实模型质量。

### P3：死代码清理

先建立删除清单：每个路径、替代入口、静态引用、动态装配、package exports、构建/脚本/worker/测试/部署引用检查和删除理由。证明无有效用途后，在隔离分支git删除并重跑相关检查。不能只靠rg无命中下结论。

只删除本次已替代的旧server/认证/路由胶水和专属无用测试/配置。仍被业务、导出、部署或回滚需要的代码不算死代码；保留项写明真实原因和退出条件。禁止删除coauthoring-v7整个目录、数据库、作者内容、发布包、迁移历史、任务证据或两份重构主文档。禁止以重命名一切来追求零V7字符串。历史文档引用可保留，运行引用必须可解释。

### P4：测试、自查、修复与发布准备

按docs/ACCEPTANCE.md身份/权限/恢复高风险要求执行。核对root及rebuild的package.json实际脚本和本地运行时，不能直接混用两个工程的依赖。至少执行受影响工程verify:full、类型/构建及真实本地API/隔离PG验证；环境不具备时标未验证并准备可重复命令，不改标准、不跳过失败测试后报全通过。

报告证据必须覆盖：未登录/普通用户/管理员矩阵；跨用户跨书；伪造owner/admin与编码路由；登录/退出/撤销/禁用/过期/改密；重启后会话；限流与来源/Cookie防护；账号-书籍-权益映射；当前开书和设定接线；后台/创作库只读查询；空库和重复迁移；迁移部分失败/重试/切换回退；无重复身份写入/路由注册/任务消费；日志无秘密。浏览器验证当前前台和后台登录、失效返回，不能以HTTP200代替页面体验。

失败先定位修复，重跑相关验证；不反复跑无关全量。全部通过后再自查完整diff与删除清单，按逻辑提交。准备部署清单、迁移预演、逐服务切换与回滚步骤，明确生产在途门禁和遗留条件，但不要执行生产操作。

## 6. 交付与停止

报告绝对路径：D:\wenmixiezuo\.local\dispatch\outbox\task-auth-takeover-01.result.md；附证据目录task-auth-takeover-01/。每批完成后更新一份报告，记录分支/工作树/基准和最终commit、实际命令/退出码、改变的行为、文件清单、删除清单及证明、测试失败与修复、未验证与阻断项、发布/回滚入口。用中文写明“已开发”“已验证”“已部署”三个状态，禁止混写。

不能完成的必须写原因和最小下一步，继续可独立完成的批次。最终自查后停止，告诉老板“可交Codex验收”，不要自行领取时光机修复或其他任务。不能保证零BUG，也不能用文档篇幅代替真实运行证据。
