# B2最后事务收尾：COMMIT失败恢复

继续原隔离树，基于6d223a12增量；原白名单/禁止推送部署合入续批持续有效。先读主目录`outbox/task-209-b2.codex-review-3.md`。不改前端，不重做权限/用途/关系等已通过功能。

1. 修复runInTransaction的COMMIT异常未回滚、depth未复位。不能只在COMMIT后清零或用finally清零掩盖连接还在事务中；操作/提交异常都需回滚，回滚失败须阻止该连接继续被当健康连接使用。同步事务边界保留，不恢复async深度方案。
2. 添加真实提交失败反例，可使用Codex临时双连接探针相同方法；断言首次不成功、无首笔残留、释放锁后同实例重试成功且独立连接可读。仅测试BEGIN失败不够。
3. 核实六文件并跑出现的双try测试偶发失败：保存两个子进程真实stdout/stderr与退出码、定位失败阶段，必要时只修夹具同步/启动条件，不削弱至少一个成功及无重号断言。记录本次原因和复跑结果，不用一次单跑成功否认失败记录。

复跑B1+B2六文件及API类型检查；前端零改动则沿用原证据。报告逐条说明实际代码和测试，不写不存在的finally。更新原结果报告，保留历史记录，提交后停止等Codex验收。不得移动任务书。
