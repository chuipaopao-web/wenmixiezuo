# AUTH-TAKEOVER-01 发布准备：已验收成果集成，不重新开发

2026-09-14老板授权Codex安排下一步。执行者GLM5.3，老板手动触发；完成后交Codex验收与发布。本批是本地合入准备，不是生产上线授权：不推送、不合入主工作区、不连接/改变生产数据库或服务、不调用真实模型、不清理服务器。

## 现状与范围

读取根AGENTS、HANDOFF、docs/DEPLOY.md、原任务书和outbox/task-auth-takeover-01.codex-review-final.md。GLM分支codex/auth-takeover-01当前557574db6caf85f90839ffc712a369f267ce0227，主线当前cc98cb05b3a9fd5d07a982ed007d38d8f6e7b0bd（任务书提交还会前移；开工读取最新主线）。在原工作树继续，先确认干净，保护其他修改。

Codex已独立确认18项局部测试与21项本地新→回退→新演练通过，前三轮已关闭问题不重做。本批只处理发布工具、安全清理、主线集成与最终证据。产品身份代码除确凿集成回归的最小修复外不再变更；不扩展PG迁移、时光机修复、新页面或创作规则。

## 1. 主线集成与状态对齐

将最新codex/light-coauthoring-v7合并进GLM自己的隔离分支，允许在该分支解决文档冲突并提交；不在主工作区执行合并，不带入主工作区两个旧工单删除改动。冲突按实际时间和证据整理，保留所有独立审查及当前结论，不用ours/theirs批量覆盖。记录合并前后commit和差异。

C6创作库和后台已上线，运行层creative-r209-c6此前未上线；当前分支源应保留该运行层成果。列明本次将一并发布的入口/身份/0125/C6运行变化，C6公共卡片已发布不重复新增、不退回C5。不能用服务器旧源码覆盖本地。PG保持未实施/未验证。

## 2. 修复发布工具，保留可重复性

允许修改scripts/release/auth-takeover/、直接相关测试、任务结果和开发状态文档；避免产品行为调整。

- drill-rollback.mjs的process.exit会跳过finally：按Codex演练副本改用退出码并等待清理完成；成功与故障均停止自己创建的子进程，不杀占用端口的无关进程。启动前端口占用应报错；健康验证必须确认为本次子进程，不能连接到别人的服务当作通过。
- build-rollback-target.mjs不得在开始时无检查rmSync递归删除固定.tmp-rollback-src。使用项目内本批专用唯一目录，校验解析后的绝对路径和归属标记；已有工作树有修改时保留并拒绝覆盖。仅清理本次创建且已确认归属的临时资源，先用git worktree正规清理。输出目录不混入旧构建残留，使用新目录或可校验的原子准备；不覆盖已有回退包。
- 不把D盘绝对工具路径、cmd或Windows junction当作Linux生产可用工具链。区分本地构建脚本与Linux发布脚本：可保留明确Windows本地适配，但发布清单必须有平台正确的依赖、workspace模块、编译资产和迁移位置，不能仅复制API dist就称完整部署包。不得打包本地密钥、.env、数据库、作者资料、node_modules中的无关私有文件。
- 回退目标保持独立已知基准+双格式/0125兼容补丁，记录来源commit、完整补丁文件、hash、依赖、启动命令和回退后新增端点行为。以校验条件限制文本补丁，不默默套错版本。Windows实测不标Linux生产通过。

## 3. 最终验证与证据

工具改动后，先验证目录已存在/脏工作树/输出已存在/端口被占用/子进程失败均安全退出且不损坏其他文件和进程，再重跑本地21项打包回退演练。检查完成后子进程确实退出。

主线合并后按docs/ACCEPTANCE.md身份高风险矩阵执行适用的verify:full与API/Worker构建，以及身份、迁移、权限、会话、开书/设定接线、创作库运行层直接受影响测试。原有18项必须保留通过。此前主线失败应逐项对应基线证据和影响：不删测试、不放宽断言、不顺手改创作规则；未查清是否影响发布的项仍标阻断，不能全归为历史。

有环境限制的PG、Linux构建、真实生产验证分别标未验证；可准备精确命令但不冒充已执行。正常工程阻断自行排查，不让老板选择技术细节；真正缺凭据或涉及权益的新决策停止依赖步骤并继续独立部分。

## 4. 发布清单与交付

生成一份release-checklist.md放结果目录，包含：候选commit、变更/删除文件、兼容迁移与回退包、构建命令/版本、产物manifest与hash、测试命令/结果、基线问题、Linux预验项目、生产发布前只读检查、账号/owner/权益验证、原子切换/逐服务启动、Worker新鲜心跳、故障回退和上线后检查。API重启遵守在途自然归零门禁，禁止改任务制造窗口；不得使用已被自动审批拒绝的忽略快照脚本。

后台路线记录如实区分：身份本地已验证、发布准备、尚未上线；不得把PG和全站重构标完成。交付不自动删除旧发布包、数据库或任何用户书籍。

报告写D:\wenmixiezuo\.local\dispatch\outbox\task-auth-takeover-01-release-prep.result.md，证据目录task-auth-takeover-01-release-prep/；原auth结果保留。列出最终commit、可重复命令、是否可进入Linux预发布、剩余明确阻断项。提交隔离分支后停止，不合入主工作区/推送/上线。老板把完成消息发给Codex后，由Codex核查发布物及剩余门禁再安排合入发布。
