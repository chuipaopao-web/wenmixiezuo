# R209-B1限定返修

先读outbox/task-209-b1.codex-review.md及原B1任务书。只修审查列出的底座问题，仍在codex/dispatch-task-209-b1及原隔离树，基于b712325a继续；禁止reset原工作。不得接B2、路由/UI/生成流程、模型或生产。

沿用原白名单，额外允许为修正同实体映射在同一未发布0122迁移内增加alias/idempotency所需表和索引，补必要类型与测试。不新增迁移013x，不更改旧迁移；0122尚未合入/发布，可在本批修订。默认不增加依赖。

按审查1—7修代码及真正覆盖缺陷的测试。必须做到：用途/层级按目标revision的结构化字段过滤；退役仍读旧release；新revision为草稿且审核发布有证据/并发控制；显式同实体多别名同编号；创建幂等不受后续编辑影响；release关系/分页与manifest冻结一致；实际竞争和写入中途失败证据。

必须删除或纠正与规格相反的断言（如同实体映射断言两id不同），说明原测试为何错误；不能削弱真实失败断言。用完整JSON字段结构而非描述文本匹配分类。输入校验应覆盖legacy、assetKind一致性、非法版本/状态与歧义。

主树唯一额外授权：按git对象2a4aa5fb中的原文恢复.local/dispatch/inbox/task-209-b1.md（先核对该对象确有文件），保持本任务及旧归档，不再移走任务书。不修改主树产品代码、Codex审查文件。

运行原20项及新增回归、API类型检查，完整报告实际命令/退出码/未验证。可参考Codex内存探针，但不能仅断言当前错误存在就称修好。报告写outbox/task-209-b1.result.md，追加返修记录、结果commit、文件清单和审查编号对应证据。允许原隔离分支提交，不推送合入部署。完成停止等Codex验收。
