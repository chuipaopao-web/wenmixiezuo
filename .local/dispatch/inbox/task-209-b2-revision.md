# R209-B2定向返修

状态：待老板手动触发；执行者GLM5.3，Codex验收。继续原隔离树和分支，基于9fde49ac增量提交，不reset、不重做原B2、不改主目录产品、不推送/合入/部署/调模型/续批。

先读主目录`task-209-b2.md`、`outbox/task-209-b2.codex-review.md`与总规格最新19.6/20节。审查中的六组问题逐项修复并做反例测试；独立探针可参考，不能改它输出伪装通过，应将对应断言加入正式测试。

具体要求：
1. 统一应用服务管理写入的事务和审计。故障时状态/意见/审计全部回滚，发布manifest/active/请求结果/审计同事务，或给出经故障验证的完整恢复机制。优先扩展现有仓储原子接口解决嵌套BEGIN，不在路由用多段SQL补偿。允许必要B1服务/接口变化但保持旧语义和36项回归。
2. reference用途筛选正确，父/子用途匹配规则明确且与UI一致；真实HTTP测试method/reference各自命中、不命中、跨页和混合筛选。
3. 关系编辑支持明确增加、修改端点版本、移除，发布预览列出变化；移除卡片不能偷偷过滤边，先提示处理；替换revision有路径完成有效发布。测试带边的两版本发布，不只无关系45卡。
4. GET release条目用冻结游标分页（合法limit及nextCursor，拒绝跨release/畸形），前端需要完整发布基础时遍历固定release，不读活动指针各页漂移。陈旧版本/状态冲突409；所见状态/版本必传且原子校验。
5. 脏表单取消、返回、切页均保护输入；提交成功不再提示。重新验证1440/390，提供正常视口截图，逐张查看，不以DOM或actionability冒充截图质量。
6. 补原B2要求的旧依赖退出清单；正确记录安全部署未执行。保留原规格，在主目录最新19.6/20规则基础上最小补本批状态，不能以旧基准文档覆盖新要求。

白名单沿原B2，补明确允许`apps/api/src/infrastructure/db/repositories/creative-reference-admin-repository.ts`及相关应用服务；0123尚未合入生产可本分支调整，0122不改。质量脚本`verify-v7-capability-cutover.ts`仅本批真实能力计数适配可保留，其余门禁不放宽。禁止新增大架构/依赖，禁止旧库或作者数据清理。

复跑B1+B2测试、上述故障/关系/筛选/分页/状态冲突反例、相关组件/API测试、API和后台类型构建。修复后探针预期：review失败状态保持draft；发布失败active/请求/审计保持一致，同键能恢复/安全重试；用途筛选1条命中。为避免前一个已修阻断影响后一个测试，正式反例各用独立夹具。

结果继续写`.local/dispatch/outbox/task-209-b2.result.md`并保留前次记录，列每项修复文件与命令/退出码，截图证据放原outbox子目录；未验证明确说明。不得移动inbox。提交完成后停止等Codex验收。
