# AUTH-TAKEOVER-01 返工3：失败安全审计持久化

沿原工作树/分支，从c328dca3增量修改，不合入、不部署、不操作生产。先读outbox/task-auth-takeover-01.codex-review-3.md，原合同和保护范围继续有效。本批只修R3-1和直接相关测试，不重写身份架构或创作功能。

1. 已知账号错误密码login_failed、错误当前密码password_change_failed，目前写入事务后被ROLLBACK删除。调整事务结构确保错误请求拒绝且失败审计可靠持久化；每请求只计一次，必要的业务成功状态修改仍回滚。不要在finally统一COMMIT或吞掉审计异常。
2. 增加真实仓储/API反例：错误登录后审计恰好+1且无新会话/升级/last_login修改；错误改密后审计恰好+1且密码/版本/会话不变；正确登录/改密保持成功审计原子；审计写入失败时不提交成功身份变化、不冒充已留记录；输出或日志无秘密。使用项目完整迁移库，不只用放宽约束的假表。重跑之前13项并发/权限用例。
3. 给出可直接复跑的回退演练命令和已有包绝对路径、基准commit与manifest/hash。构建脚本不可无检查递归删除既有临时工作树或用户指定输出目录：发现已有内容应校验归属、干净状态并续做或拒绝覆盖。不要清理服务器或当前其他工作树。回退证据继续区分GLM自测和Codex尚未复验。
4. 更新原result.md当前提交与三状态、修复证据。失败审计未验证/完整PG未实现/未部署不得写成通过或整体完成。完成增量提交后停止交Codex验收，不再领取其他功能。
