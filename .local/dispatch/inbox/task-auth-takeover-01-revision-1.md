# AUTH-TAKEOVER-01 返工1：不得按当前版本上线

继续原工作树/分支codex/auth-takeover-01、提交281e1749，保护本地其他修改。先读原任务书和outbox/task-auth-takeover-01.codex-review.md；本次不是新目标，原开发/清理/保护范围继续有效。不部署、不合入、不碰生产数据。

1. 在原结果报告中把“完整接管已开发/已验证”更正为部分完成、Codex验收未通过，保留原证据与错误修正记录。
2. 修复审查报告的密码并发覆盖、升级last_login_at不落库问题；验证时必须使用期望凭据版本或旧散列CAS，异步hash后在同一安全写入边界内重新核查凭据/状态，再写升级、审计及签发会话。并发改密/停用/撤销不能被旧登录绕过；冲突时拒绝或有界重试，不无条件覆盖。补真正失败前可复现、修复后通过的回归。未知账号当前走v1而v2账号走更昂贵派生，不得称“常数时间”，核对枚举风险并合理修正。存储参数只接受支持的格式与有界参数，异常记录fail closed，不任意降级。
3. 修复回滚：准备兼容已应用0125及v1/v2混合账号的回滚构建。演练新注册、历史登录升级、退出再登录、旧会话、新旧混合记录和新进程启动；确认不会丢账号/owner/权益。禁止把“加列”当作无行为迁移，禁止靠降级散列或删迁移记录回滚。
4. 完成原目标，不能停在复制旧server改名。核对并复用现有rebuild身份/会话/权限能力，以明确依赖边界接入当前业务；补齐当前已对用户提供的注册、管理员、停用、会话等行为属于原任务范围，不要求老板重新选产品。可用适配器保持当前外部合同，但必须给出新身份域实际承担职责的调用证据；不能仅在测试中用rebuild verifyPassword就称接管。单一权威、owner映射、防双写是硬约束。
5. PG若不可达，先只读核查项目已有本地运行/测试脚本与配置，能用已有隔离设施则恢复测试运行，不新装/重配系统服务、不触碰生产。若仍需新权限或外部条件，具体记录命令和错误，继续完成不依赖真实数据库的实现与接口集成，保持该项未验证、整体验收未通过；不可把环境阻断自动变为改名方案完成。
6. 按原任务书P4补真实安全矩阵、迁移/回滚、作者与后台登录/失效和开书设定接线。路由注册验证用有权限请求结合预期业务响应或真实路由表，而非只看匿名401。没有实现的改密/撤销能力不得出现在已完成列表；缺口必须补齐或明确标未完成与原因。基线创作roster失败继续隔离记录，禁止为凑通过修改无关创作规则。
7. 接管真实完成后再核对死代码清理，不能先以删旧入口证明接管。输出实际新旧调用图、保留适配器和退出条件，记录测试命令/退出码/新增缺陷及修复。保留独立复现证据，不改Codex审查报告冒充通过。

普通工程问题自行处理，批次验证后继续；最终更新原result.md并列出回应每条审查意见的文件/测试/提交。完成后停止交Codex验收，不把未完成或未验证标通过。
