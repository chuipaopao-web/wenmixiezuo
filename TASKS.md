# 项目任务状态

## 当前状态

- 阶段：长篇终局release阶段0至8适用E2工程门禁全部通过；历史首版阶段0至8证据只读保留，E3/E4未伪造。
- 业务代码：老板已于2026-07-19明确授权按最终冻结方案连续完成八阶段开发、测试、验收、Git备份，并在真实完成后关机。
- 当前授权：只在 `D:\wenmixiezuo` 由当前Codex单独开发；不调用其他开发Agent，不修改、停止或重启 `D:\AI智囊团`。
- 当前正式开发版本：`wm-longform-r1-20260719-003435-e4d7b8b7`；历史基线为 `wm-v1-20260716-220959-d5dd704d`。

## 已确认的开工参数

- 产品显示名称：文秘写作。
- 本地Web/API端口：`43110/43111`，只监听 `127.0.0.1`。
- 数据根目录：`D:\wenmixiezuo\data`。
- 现金费用保护线：未获单次明确授权前为0。
- 第二物理位置备份：暂未配置。
- 远程Git：已配置私有仓库 `chuipaopao-web/wenmixiezuo`，使用SSH推送 `main`。

## 开发顺序

已严格按照 `docs/DEVELOPMENT_ROADMAP.md` 完成八个阶段。日后新增需求必须创建新的决定和release，不得静默改写本首版验收历史。

长篇终局增量的唯一施工顺序见 `docs/superpowers/plans/2026-07-19-final-longform-platform-implementation.md`；已按DEC-028激活并连续执行。

## 开发任务 LONGFORM-20260719-01：阶段1 release基线、安全入口与能力探针

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`。
- `design_review_id`：`DR-20260719-09`，沿用DR-20260719-01至08的冻结设计。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/01-security-capabilities.md`。
- 目标：修正开工前已发现的文档命名/授权冲突，激活release，冻结旧版回归，完成本机会话、HTTP防护、运行/硬件/本地模型资产能力探针和阶段1证据。
- 不做什么：不改变书籍业务语义，不下载模型，不产生现金费用，不永久删除，不恢复生产数据，不触碰 `D:\AI智囊团`。
- 唯一负责人/复核人：当前Codex分实现轮和证据复核轮完成，不调用其他开发Agent。
- 允许文件：活动release文档、根配置、`apps/api`安全/能力/HTTP入口、`apps/web`会话客户端与能力显示、相关测试和脚本。
- 禁止文件：0001至0009既有迁移、来源快照、API Key、AI智囊团目录和无关生产正文。
- 依赖：历史Schema 9/首版门禁、DEC-020至028、最终八阶段计划和当前锁文件。
- 实现约束：只监听127.0.0.1；每启动轮换会话；Host/Origin/Fetch Metadata/Cookie/SSE均有负面门禁；探针只读且不远程下载；未知硬件如实为unknown；所有写操作安全可逆。
- 验收标准：历史首版零退化；类型/测试/构建/空库及Schema 9升级迁移/重复迁移/运行/Repository与恢复检查通过；安全负面集通过；能力探针返回真实状态与降级原因；阶段证据与提交一致。
- 测试命令：`npm run typecheck`、`npm test`、`npm run build`、`npm run migrate`、`npm run verify`、阶段1安全与能力目标集、隔离空库/升级库迁移和本地三进程健康检查。
- 停止条件：跨书/权威/秘密/路径/会话失败无法修复，或需要付费、新密钥、永久删除、重大架构变更、生产恢复、外部阻塞。
- 回滚：Git revert阶段1代码提交并把活动程序策略切回首版；本阶段不创建新业务数据语义。

## 开发任务 LONGFORM-20260719-02：阶段2 Repository、Schema 10—11与知识生命周期

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/02-repository-lifecycle.md`。
- 目标：建立长篇新增应用层的Repository/事务边界，新增表达/标签治理、最小开书资料、临时/候选/正史/派生四层知识生命周期和现实时间/叙事时间/叙事认知三轴，并升级章节结算候选链。
- 不做什么：不改写0001—0009，不把投影当权威，不猜旧数据时间，不让未确认资料自动进正史，不调用真实付费模型，不触碰AI智囊团。
- 唯一负责人/复核人：当前Codex分TDD实现和门禁复核两轮完成；不调用其他开发Agent。
- 允许文件：0010—0011新增迁移、Repository/UnitOfWork、知识/开书/表达应用服务、契约、对应API、测试和当前release证据。
- 禁止文件：来源快照、既有迁移、API Key、其他项目目录、未授权生产恢复与永久删除路径。
- 依赖：阶段1安全入口通过、Schema 9、DEC-020/025、DATA_MODEL/HYBRID_RAG/CHUNKING的权威与生命周期合同。
- 实现约束：每个核心查询显式 `owner_id + book_id`；迁移只向前新增；权威写入与操作记录同事务；正文确认和知识结算失败解耦；旧数据只标 `temporal_completeness=partial`。
- 验收标准：应用边界静态测试、Repository契约、空库和9→11升级/重复/中断、两书同名实体隔离、D级/歧义/梦境/观点/冲突不自动提升、章节确认后结算可恢复、类型/全测/构建/运行通过。
- 测试命令：阶段2目标集、`npm run typecheck`、`npm test`、`npm run build`、隔离与现有数据迁移、`npm run verify`、三进程smoke。
- 停止条件：需要现金、密钥、永久删除、重大架构变化、生产恢复或无法消除的权威/跨书/迁移外部阻塞。
- 回滚：关闭新能力写路径并回到Schema 9旧读路径；新增表保留，禁止回退或改写已应用迁移。

## 开发任务 LONGFORM-20260719-03：阶段3切片、本地语义与投影快照

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/03-chunking-local-semantics.md`。
- 目标：实现不可变原始资料、结构节点、父子检索块、FTS5与LanceDB OSS本地向量投影、离线嵌入适配器、投影快照原子切换和失败回滚。
- 不做什么：不远程下载模型、不调用付费模型、不把向量投影当正史、不默认向量化聊天、不复制AI智囊团资产。
- 唯一负责人/复核人：当前Codex分切片正确性、投影故障和检索质量三轮完成；不调用其他开发Agent。
- 允许文件：0012新增迁移、切片/嵌入/投影Repository与Worker、离线资产配置、对应测试和release证据。
- 禁止文件：既有迁移、API Key、来源快照、未授权外部下载、其他项目和永久删除路径。
- 依赖：阶段2Schema 11与知识权威、DEC-012/017/018、CHUNKING_DESIGN和本机能力探针。
- 实现约束：原文不可变；UTF-8字节范围可回链；默认零复制重叠；所有块带owner/book/source/hash/策略/水位；新快照validated后才原子切换；依赖或资产缺失时明确降级FTS/结构检索。
- 验收标准：切片边界/父子/邻接/来源哈希100%校验，跨书0泄漏，构建失败保留上一快照，空库/升级/重复迁移、类型/全测/构建/运行/恢复全部通过。
- 测试命令：阶段3切片/资产/投影目标集、`npm run verify`、隔离与生产迁移、三进程smoke和投影故障注入。
- 停止条件：需要现金、密钥、许可证不清资产、独立数据库服务、永久删除、重大架构改变或无法消除的外部阻塞。
- 回滚：原子切回上一已验证投影快照；原始资料和正史不回滚、不修改。

## 开发任务 LONGFORM-20260719-04：阶段4四路检索与上下文编译

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/04-hybrid-retrieval-context.md`。
- 目标：实现结构化事实、FTS/BM25、本地向量和SQLite Wiki/关系四通道，H/E/I车道、同源证据聚类、三轴证据闭环、有界历史下钻与岗位Token上下文编译。
- 不做什么：不让向量投票覆盖正史，不把同源摘要当多份证据，不把无答案补成事实，不给所有岗位同一资料包，不调用付费模型。
- 唯一负责人/复核人：当前Codex分硬门禁、召回融合、上下文创造性三轮完成；不调用其他开发Agent。
- 允许文件：0013新增迁移、检索计划/通道/证据/下钻/预算/压缩Repository与服务、API预览、对应测试和release证据。
- 禁止文件：既有迁移、来源快照、API Key、其他项目、永久删除和无来源结论路径。
- 依赖：阶段3Schema 12、DEC-019、三轴知识、切片来源和活动投影水位。
- 实现约束：先锁owner/book/canon/time/viewpoint/entity；H不进入RRF；E/I分车道；派生副本按来源簇去重；事实输出必须解引用正式源；最多一次补充、三级下钻；先预留输出和20%安全边界。
- 验收标准：张三/天安城/宣战规则、同名、否定、冲突、无答案、错误实体、关系爆炸、陈旧投影、上下文溢出和压缩探针全部通过；跨书0、硬门禁100%、来源闭环100%。
- 测试命令：阶段4目标集、`npm run verify`、空库/升级/重复迁移、三进程smoke与故障恢复。
- 停止条件：需要现金、密钥、独立服务、永久删除、重大架构改变或无法消除的权威/跨书阻塞。
- 回滚：逐书检索策略与上下文快照指针回到上一ready版本；不修改正史或原始资料。

## 开发任务 LONGFORM-20260719-05：阶段5超长篇连续性与十一人团队

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/05-continuity-eleven-agent-team.md`。
- 目标：实现五级阶段结算、承诺/伏笔/因果、滚动规划、岗位连续性、11名女性创作成员、版本化岗位/模型绑定、双编剧独立跨度评估和小文秘书受控入口。
- 不做什么：不把全聊天做长期人格，不伪造Agent在线，不让同模型冒充异模型，不让豆包讨论剧情，不让小文秘书创作剧情或正史。
- 唯一负责人/复核人：当前Codex分连续性、身份接管、模型路由和创造性门禁四轮完成；不调用其他开发Agent。
- 允许文件：0014—0015新增迁移、连续性/团队/绑定/路由/提示合同服务、对应API/测试和release证据。
- 禁止文件：既有迁移、API Key、其他项目、伪状态、永久删除和付费调用。
- 依赖：阶段4Schema 13、DEC-014/015/021/026/027、11人岗位合同和模型能力探针。
- 实现约束：摘要只导航；关键实体/规则/开放线程触发回查；模型快照随任务冻结；双编剧先独立后综合；单活动写手；研究员按需零空转；所有状态由任务/心跳/调用驱动。
- 验收标准：500/1000/1500章触发逻辑、摘要失败回建、承诺不按距离遗忘、11人职责/女性身份、主副编接管、双编剧隔离、豆包拒绝剧情、写手/点评异模型和秘书点名/剧情升级全部通过。
- 测试命令：阶段5目标集、`npm run verify`、空库/升级/重复迁移、三进程smoke和接管/摘要故障注入。
- 停止条件：需要付费、密钥、重大架构变化、永久删除、生产恢复或无法构成合法模型组合。
- 回滚：团队/模型/连续性策略指针回上一ready版本；保留历史任务、调用、正文和正史。

## 开发任务 LONGFORM-20260719-06：阶段6正确创作闭环

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/06-creation-production-loop.md`。
- 目标：实现方案确认、表达基线、不可变工单、单活动写手、三异模型同稿点评、有限重写、正文确认和确认后正史结算。
- 不做什么：不让单模型自审，不在老板确认前选稿/抽事实/结算，不把AI腔比例伪装为作者身份概率，不让批量写作跨过逐章确认。
- 唯一负责人/复核人：当前Codex分状态机、模型证据、拒稿恢复和阶段门禁四轮完成；不调用其他开发Agent。
- 允许文件：0016新增迁移、创作/点评/确认应用服务、Repository、API、测试、当前规格和release证据。
- 禁止文件：既有迁移、API Key、其他项目、未确认正史、永久删除和付费调用。
- 依赖：阶段5Schema 15、DEC-021/026、模型绑定快照、写手租约、检索上下文和正文不可变版本。
- 实现约束：工单只带资料指针/哈希；每稿三席冻结且与写手异模型；所有点评同一全文；最多两轮定点重写；老板接受后才结算；拒稿与准备重写同事务。
- 验收标准：三席Schema/模型差异、GLM写手事实席替换、单章与逐章批次确认、拒稿零正史污染、坏报告阻断、写手纪元和相同哈希阻断、备份迁移恢复全部通过。
- 测试命令：阶段6目标集、`npm run verify`、正式15→16/重复迁移、隔离恢复迁移、完整性/外键检查和三进程smoke。
- 停止条件：需要付费、密钥、重大架构改变、永久删除、生产恢复或无法形成合法三异模型组合。
- 回滚：关闭Schema 16新写路径并回到旧只读兼容；迁移前备份已验证，新增表和历史证据不逆向删除。

## 开发任务 LONGFORM-20260719-07：阶段7最终工作台与可移植入口

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/07-experience-portability.md`。
- 目标：交付浅绿色可调工作台、窄双栏、开放聊天、真实成员状态/头像、任务二级页、规划树、章节与资料库、模型/字体/颜色设置、PWA与桌面入口。
- 不做什么：不伪造成员在线，不暴露密钥，不恢复“一章/三章/五章”按钮，不把预算和确认占据团队侧栏，不读取或复制AI智囊团运行资产。
- 唯一负责人/复核人：当前Codex；不调用其他开发Agent。
- 允许文件：Web应用、现有API读模型、PWA/桌面启动与使用文档、测试和release证据。
- 禁止文件：AI智囊团、既有迁移、API Key、正式正文静默覆盖、永久删除。
- 依赖：阶段6确认式创作API、11人团队/小文秘书真实状态、知识/规划/章节/任务Repository。
- 实现约束：中区优先；两侧可收窄；颜色和字号可恢复；所有列表来自真实API；移动端可达；键盘/无障碍/错误恢复完整。
- 验收标准：设计与功能目标集、浏览器交互/无障碍/移动端、类型/全测/构建/迁移/运行/恢复和桌面入口全部通过。
- 测试命令：Web组件与API契约目标集、`npm run verify`、浏览器smoke、PWA离线壳、三进程启动和入口恢复。
- 停止条件：需要付费、密钥、永久删除、重大架构变化、生产数据恢复或外部不可消除阻塞。
- 回滚：回退Web静态产物/设置版本和启动入口；不改动业务正史及正式正文。

## 开发任务 LONGFORM-20260719-08：阶段8规模、恢复与最终发布

- `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；`design_review_id`：`DR-20260719-09`。
- 状态：完成；证据见 `docs/releases/wm-longform-r1-20260719-003435-e4d7b8b7/stages/08-scale-recovery-release.md`。
- 目标：完成500万规范化字符/1500章容量回放、24小时逻辑时钟Worker耐久、故障注入、干净恢复、桌面入口、使用说明、验收矩阵、最终提交与远程备份。
- 不做什么：不把加速逻辑时钟冒充真实24小时墙钟，不伪造E3/E4文学证据，不调用付费模型，不写入密钥，不永久删除，不触碰AI智囊团。
- 唯一负责人/复核人：当前Codex；不调用其他开发Agent。
- 允许文件：容量/耐久/恢复评测脚本与测试、当前文档、release证据、桌面入口和Git元数据。
- 禁止文件：既有迁移、来源快照、API Key、其他项目、未授权生产恢复和永久删除。
- 依赖：阶段1至7全部代码和证据、Schema 18、三进程运行、可移植包和备份恢复链。
- 实现约束：容量与模型上下文解耦；所有故障均可重复；恢复只在隔离目录演练；最终判断逐项映射 `docs/ACCEPTANCE.md`。
- 验收标准：全部自动门禁、容量、逻辑耐久、隔离、恢复、启动、文档、Git提交和远程推送有真实证据；未知能力如实降级。
- 测试命令：`npm run verify`、`npm run acceptance`、容量/耐久脚本、空库/升级/重复迁移、三进程smoke、备份验证和隔离恢复。
- 停止条件：需要付费、密钥、永久删除、重大架构改变、生产恢复或无法消除的外部阻塞。
- 回滚：Git回退阶段8脚本/文档；运行数据不逆向迁移，发布前备份保留并只在隔离目录验证。
- 最终证据：97个测试文件/199项测试、三端类型/构建、500万字符/1500章/5书回放、1440逻辑周期耐久、正式Schema 18重复迁移、正式备份隔离恢复和桌面启动/停止通过；E3/E4保持未取得。

## 设计任务 DESIGN-20260719-01：长篇终局能力开工冻结

- `design_review_id`：`DR-20260719-01`。
- 预留 `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；该历史设计任务当时未激活，现已由DEC-028激活。
- 状态：E0设计冻结完成并提交；禁止描述为代码、Schema、模型或500万字能力已完成。
- 目标：一次性冻结剩余的数据、检索、Agent、上下文、状态机、UI、安全、可移植、迁移、恢复、评测和八阶段开发合同。
- 不做什么：不创建业务源码、不安装依赖、不下载模型、不新增/运行迁移、不修改 `data`、不启动服务、不触碰AI智囊团。
- 唯一负责人/复核人：当前Codex分两轮执行Skill前置设计和反例复核，不调用其他开发Agent。
- 允许文件：当前规格、决定、任务、知识、路线图、验收、release设计记录和实施计划。
- 禁止文件：`apps/**`、既有迁移、运行数据、来源快照、`D:\AI智囊团`。
- 依赖：DEC-011至019、项目 `wenmi-longform-quality` Skill、记忆/压缩/计划Skill和历史首版证据。
- 验收：设计缺口矩阵、冻结包、运行工作流、安全运维、评测协议、迁移/逐书切换、八阶段TDD计划和当前规格同步完整；文档链接、编码、Git差异和现有项目门禁通过。
- 测试命令：文档引用/编码检查、`npm run typecheck`、`npm test`、`npm run build`、`npm run migrate`、`npm run verify`；不运行会修改生产数据的操作。
- 停止条件：需要付费/密钥/永久删除/重大架构变更/生产恢复或无法消除的外部阻塞；本设计未触发。
- 回滚：`git revert` 本次纯文档提交；不会影响历史release、Schema和数据。
- 当前证据：设计提交 `95d23e0`；两份长篇Skill审计、文档引用、UTF-8和差异检查通过；允许本机回环后类型检查、63个测试文件/132项测试、API/Web/Worker构建全部通过；隔离空库应用0001至0009并到Schema 9，重复迁移 `applied: []`；提交后干净工作树发布验收3/3且审计 `failures: []`。受限执行层首次阻断自身回环连接的3项连锁失败已如实记录，未修改测试掩盖。

## 设计任务 DESIGN-20260719-02：十一人团队与三异模型点评定案

- `design_review_id`：`DR-20260719-02`。
- 预留 `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；当前不激活。
- 状态：E0历史设计冻结完成；固定模型条款已被DEC-026修订，业务代码、Schema、迁移、真实模型运行和E1—E4证据均未实现。
- 目标（当时决定）：固定11名女性成员、双异模型编剧、主编/副编与主笔/副笔接管，并建立三异模型全文点评；当时的GLM/Kimi/豆包固定名单已由DEC-026修订。
- 不做什么：不建立复杂动态点评路由，不让全员读取正文，不默认生成主笔/副笔双稿，不把三票多数当真相，不把AI腔风险伪装成AI作者概率，不把内容筛查伪装成法律/平台保证；不修改业务源码、依赖、迁移、运行数据或 `D:\AI智囊团`。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：项目规则、决定、产品/架构/数据/Agent/提示词/记忆/RAG/质量/连续性/API/工作流/评测/路线图/验收/覆盖/知识/任务、项目级Skill、审计记录和最终八阶段实施计划。
- 禁止文件：`apps/**`、既有迁移、生产数据、API Key、来源快照和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009、DEC-010、DEC-014、DEC-018至020、`wenmi-longform-quality` Skill、老板确认的可用模型集合和“不复杂、固定三名异模型点评”决定。
- 实现约束：本任务当时冻结的十一人、单活动写手、三职责点评和最多两轮仍保留；豆包剧情席、DeepSeek副笔和永远固定GLM事实席不再驱动开发，按DESIGN-20260719-06/DEC-026执行。
- 验收标准：DEC-021生效；当前规格、Skill和八阶段计划无相反规则；三席模型/版本独立性、严格Schema、AI腔证据/比例、政治/情色位置/策略版本、误报/漏报、创造性非劣效、取消/超时/缺席和三份全文Token均有E1—E4测试门禁；历史首版9岗位事实不被重写。
- 测试命令：项目Skill官方 `quick_validate.py`、`node .agents/skills/wenmi-longform-quality/scripts/validate-audit.mjs docs/AGENT_TEAM_REVIEW_AUDIT.md`、Markdown引用/冲突扫描、`git diff --check`、`npm.cmd run verify` 和 `npm.cmd run acceptance`；本任务不把既有首版回归通过称为新运行时能力。
- 停止条件：需要真实密钥、现金付费、永久删除、生产恢复、重大架构变更或无法消除的外部阻塞时停止；E0纯文档定案不触发。
- 回滚方法：对本次纯文档提交执行非破坏性 `git revert`；未来运行时通过团队模板、点评策略和模型快照指针回滚，不改历史正文、正史或调用记录。
- 复核人：当前Codex依据更新后的项目长篇质量Skill完成两轮反例审查；不声称人员独立，后续E3/E4必须使用冻结金标、盲评和真实模型证据。
- 当前证据：`docs/AGENT_TEAM_REVIEW_AUDIT.md` 通过项目Skill审计脚本；项目Skill前置元数据通过与官方 `quick_validate.py` 等价的字段/格式校验，官方脚本本机因自身缺少 `PyYAML` 依赖未能启动，未伪造通过；33个变更文件的UTF-8和本地Markdown引用检查通过；`git diff --check` 通过；允许本机回环后类型检查、63个测试文件/132项测试和API/Web/Worker构建通过；隔离临时空库应用0001至0009到Schema 9，重复迁移 `applied: []`。首次受限运行的3项失败已定位为执行层阻止 `127.0.0.1`，放开回环后最小失败集5/5通过；一次全量运行另捕获到任务成功与Worker空闲心跳间的既有短时竞态，独立复跑及随后全量回归通过，本次未越权修改业务代码。当前仍只允许称E0方案已定。

## 设计任务 DESIGN-20260719-03：岗位职责可见性与研究员保留

- `design_review_id`：`DR-20260719-03`；预留release不激活。
- 状态：E0设计同步完成，业务API、Schema和前端尚未实现。
- 目标：让成员栏直接显示一句话职责并提供可点击的公开岗位合同；判断并冻结研究员的必要性、触发条件和零空转边界。
- 不做什么：不在190像素侧栏堆完整职责，不暴露原始系统提示/隐藏规则/密钥/工具参数/思维链，不让研究员参与普通聊天、每章固定三评或自动写入正史。
- 唯一负责人：当前Codex依据 `wenmi-longform-quality` Skill完成替代、注意力、事实风险、Token和创造性复核，不调用其他开发Agent。
- 允许/禁止文件：只修改当前决定、产品/数据/Agent/提示/API/验收/覆盖/实施计划/知识/任务和审计；禁止 `apps/**`、测试、迁移、运行数据、来源快照与 `D:\AI智囊团`。
- 实现约束：卡片显示姓名/短岗位/一句话职责/真实状态；详情显示公开职责/边界/激活/交付物/模型/当前任务/最后贡献/证据；研究员只处理明确现实事实问题，未激活时零检索、零模型调用、零全文输入。
- 验收标准：鼠标、键盘、手机可打开详情；公开与内部字段隔离；研究任务绑定问题和来源预算；无来源明确未知；研究卡只进临时/候选；纯虚构设定归设定师；研究员不是第四点评票。
- 回滚：对本次纯文档提交执行 `git revert`；未来运行时回滚公开岗位模板/API策略，不改历史任务和正史。
- 当前证据：项目长篇质量Skill审计通过；15个变更文件的UTF-8和本地Markdown引用检查通过；冲突扫描与 `git diff --check` 通过；允许本机回环后类型检查、63个测试文件/132项测试和API/Web/Worker构建通过。提交后发布验收和远程同步仍需执行；当前只允许称E0方案已定。

## 设计任务 DESIGN-20260719-04：下一版本功能总表老板审阅包

- 预留 `release_id`：`wm-longform-r1-20260719-003435-e4d7b8b7`；当前不激活。
- 状态：老板审阅文档已生成，等待老板确认；不能描述为业务功能已经实现。
- 目标：把今天确定的产品流程、11人团队、模型、三评、正史/临时、混合RAG、切片、超长篇、资料库、UI、安全、恢复、E0—E4和八阶段施工范围压缩为一份小白可审阅、开发可追溯的开工入口。
- 不做什么：不新增产品决定，不简化掉来源、风险和未知项，不创建业务代码、依赖、迁移、运行数据或真实模型调用。
- 唯一负责人：当前Codex使用 `wenmi-longform-quality`、`context-compression` 和 `writing-plans` 三个Skill整理并自审，不调用其他开发Agent。
- 允许文件：`docs/OWNER_DEVELOPMENT_SCOPE_REVIEW.md`、项目知识/任务账本和必要入口引用；禁止 `apps/**`、测试、迁移、来源快照、运行数据与 `D:\AI智囊团`。
- 验收标准：区分当前已有与下一版本待开发；覆盖DEC-008至022和八阶段；模型/岗位/权威/费用无冲突；没有占位承诺；本地链接、UTF-8、差异、全量回归和提交后发布验收通过。
- 停止条件：汇总发现会改变产品方向的未决冲突时停止并交老板；能力探针、模型可用性和真实纵向质量作为开工后未知项列明，不在E0伪造答案。
- 回滚：纯文档提交可执行 `git revert`，不影响历史首版、Schema和数据。
- 当前证据：DEC-008至022覆盖、模型/岗位/权威/费用冲突扫描、4个变更文档UTF-8、本地Markdown引用、重复标题和 `git diff --check` 已通过；允许本机回环后，类型检查、63个测试文件/132项测试及API/Web/Worker生产构建通过；功能总表已提交为 `844f359`；干净工作树发布验收3/3和审计23项通过、失败0项。远程同步待最终证据提交后一并执行。

## 设计任务 DESIGN-20260719-05：规划成果前端可见与去AI味策略复核

- `design_review_id`：`DR-20260719-04`、`DR-20260719-05`；预留release不激活。
- 状态：E0文档正在同步；现有Web缺口已由代码核实，不能描述为前端已经实现。
- 目标：把设定框架、总纲、卷纲、章纲、章节列表和真实卷章树冻结为可直接验收的规划工作台；判断第三方去AI味Skill是否应成为产品依赖。
- 当前事实：历史首版 `App.tsx` 的规划成果页只渲染原始JSON，左栏把所有章节显示在硬编码“第一卷”下；已有Kimi审校设计包含AI文风风险、段落证据和创造性非劣效门禁，但下一release业务运行时尚未实现。
- 不做什么：不在本轮修改 `apps/**`、测试、迁移或运行数据；不安装第三方Skill/CLI/依赖；不把外部Skill名称、商店评分或安装量当小说质量证据；不触碰 `D:\AI智囊团`。
- 唯一负责人：当前Codex使用 `wenmi-longform-quality` 和 `find-skills` 完成代码事实、外部候选和创造性反例审查，不调用其他开发Agent。
- 实现约束：五页签、真实卷章树、计划章/正文章分离、来源/版本/候选状态、影响预览、1500章按卷加载与虚拟列表；设定框架和资料库引用同一事实但不混淆计划与正史。去AI味默认只检测和定点修订，外部规则须来源/许可证/安全/小说反例/盲评通过后再版本化吸收。
- 验收标准：产品、决定、API、角色提示、总表、实施计划和最终验收一致；不存在通用JSON代替正式UI、硬编码第一卷、外部Skill自动生效、AI风险分等于作者概率或风险分降低等于质量提高等相反规则。
- 测试命令：文档编码/引用、决定覆盖、冲突扫描、`git diff --check`、`npm.cmd run verify` 和提交后的 `npm.cmd run acceptance`。
- 停止/回滚：需要付费、密钥、第三方未审计可执行代码、重大架构变化或生产恢复时停止；本轮纯文档可用 `git revert` 回滚，不影响历史首版和数据。
- 当前证据：12个变更文档UTF-8、功能总表引用、24个唯一决定编号、冲突扫描和 `git diff --check` 通过；允许本机回环后，类型检查、63个测试文件/132项测试及API/Web/Worker生产构建通过；功能与策略文档提交为 `99989d7`；提交后发布验收3/3和审计23项通过、失败0项。远程同步待最终证据提交后一并执行。

## 设计任务 DESIGN-20260719-06：最小开书、自然规划与可配置模型修订

- `design_review_id`：`DR-20260719-06`、`DR-20260719-07`；预留release不激活。
- 状态：E0设计冻结完成；历史首版开书框、1/3/5章按钮、批次API和旧模型绑定仍在业务代码中，不能描述为已经实现。
- 目标：把开书缩为六项最小卡；进入书籍即自由聊天；由两个异模型编剧评估剧情章节跨度；正文逐章执行；默认DeepSeek＋GLM剧情、GLM副笔、豆包只点评，并允许安全版本化配置。
- 不做什么：不修改 `apps/**`、测试、迁移、运行数据或真实模型配置；不调用付费API；不把情绪图谱变成自动剧情引擎；不让运行中任务换模型；不触碰 `D:\AI智囊团`。
- 唯一负责人：当前Codex使用 `wenmi-longform-quality`、`systematic-debugging` 和 `writing-plans` 完成根因、反例和施工影响复核，不调用其他开发Agent。
- 允许/禁止文件：允许当前决定、项目规则、产品/数据/Agent/提示/API/工作流/质量/评测/路线图/验收/功能总表/实施计划/Skill/知识/任务和专项审计；禁止业务源码、既有迁移、运行数据、来源快照、API Key和AI智囊团文件。
- 实现约束：开书字段仅书名/题材/分类/目标读者/预计规模/初始表达基线；视角首个正式工单前确认；世界观/力量体系按需讨论；情绪图谱分计划/实际且只读分析；双编剧独立输出跨度；无批次按钮；默认DeepSeek＋GLM且Kimi可替换一席，豆包拒绝；Codex/GLM写手池；三个点评模型彼此且与写手不同，GLM写手由DeepSeek事实点评；配置只对未来任务生效。
- 验收标准：DEC-025/026和所有当前规格一致；专项审计通过；测试设计覆盖开书最小字段、自然聊天、跨度评估、批次按钮删除、逐章状态、豆包剧情拒绝、同模型拒绝、GLM写手替补、配置历史/回滚和运行任务快照稳定；E0不冒充运行时。
- 测试命令：Skill审计验证器、决定/冲突/编码/引用扫描、`git diff --check`、`npm.cmd run verify`、提交后的 `npm.cmd run acceptance`；现有首版测试通过只证明未破坏历史功能。
- 停止/回滚：需要付费、密钥、重大架构改变、永久删除、生产恢复或无法形成三模型独立组合时停止；本轮纯文档用 `git revert` 回滚，未来运行时用绑定修订/策略指针回滚，不改历史任务和正文。
- 当前证据：设计提交 `48a05ad`；`docs/ONBOARDING_MODEL_FLOW_AUDIT.md` 与历史团队审计均通过项目长篇Skill审计验证器；81个Markdown文件通过UTF-8检查，本地Markdown引用全部可解析，26个决定编号唯一，`git diff --check`通过。首次受限运行中，3项测试仅因执行层禁止测试进程访问自身临时 `127.0.0.1` 端口而出现 `EACCES`；未修改代码或测试，允许本机回环后使用完全相同的 `npm.cmd run verify`，类型检查、63个测试文件/132项测试和API/Web/Worker生产构建全部通过。提交后发布验收3/3通过，审计23项通过且 `failures: []`。以上只证明E0文档一致且未破坏历史首版，不证明新开书、模型配置和自然写作流程已实现。

## 设计任务 DESIGN-20260719-07：小文秘书、本地分层与透明剧情路由

- `design_review_id`：`DR-20260719-08`；预留release不激活。
- 状态：老板已确认E0开发方向，文档和施工合同已收口；历史首版仍没有小文秘书、本地通用工具模型或新路由运行时，不能描述为已实现。
- 目标：在11名创作成员之外定义一个作者可见的本地工具角色，以确定性代码→本地模型→订阅/套餐创作岗位→老板确认的分层处理小问题；保证点名直达、剧情讨论由主编和双异模型编剧直接参与、老板原话保真、四类受控记忆和可回滚工具经验。
- 不做什么：不修改业务源码、既有迁移、依赖、运行数据或真实模型配置；不下载本地模型、不调用真实/付费API、不激活release、不让秘书创作剧情/正文/正史，不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex使用 `wenmi-longform-quality`、`memory-systems` 和 `writing-plans` 完成设计、反例和施工复核；不调用其他开发Agent。
- 允许文件：项目规则、决定、项目Skill、产品/架构/数据/Agent/提示/记忆/RAG/工作流/API/质量/评测/路线图/验收/覆盖/功能总表/冻结记录/实施计划/知识/任务和专项审计；禁止 `apps/**`、测试、迁移、依赖锁、运行数据、来源快照、API Key和AI智囊团文件。
- 依赖：DEC-018至DEC-026、11人团队合同、四路RAG、四层知识生命周期和当前八阶段计划。
- 实现约束：小文秘书不写入创作Agent实例或独立模型计数；原始消息先持久化/哈希；路由优先级冻结；任务大小按风险而非长度；本地模型只产严格Schema候选；剧情会话有粘性；经验仅工具/路由/故障、带反例/版本/到期/回滚；禁远程运行时下载和现金fallback。
- 验收标准：DEC-027与当前规格一致；专项审计通过；点名、原话、剧情升级、高影响短句、低风险长资料、低置信、离线、坏Schema、跨书、经验污染和创造性非劣效均有E1—E4施工门禁；E0不冒充运行时。
- 测试命令：项目Skill审计器、Skill快速验证、决定/引用/编码/冲突扫描、`git diff --check`、`npm.cmd run verify`、提交后 `npm.cmd run acceptance`；现有首版测试只证明文档变更未破坏历史功能。
- 停止条件：需要付费、密钥、购买硬件、独立服务/技术栈重大改变、永久删除、生产恢复、模型许可证不明或无法消除外部阻塞时暂停询问；本轮未触发。
- 回滚方法：纯文档提交使用非破坏性 `git revert`；未来运行时按路由政策、经验修订和本地模型快照指针回滚到确定性＋现有岗位直达，不改历史消息、任务、正文或正史。
- 复核人：当前Codex依据三项Skill完成两轮自审；受单人开发约束不声称人员独立，后续E3/E4必须使用冻结路由金标、隐藏对抗集、盲评和真实纵向证据。
- 最终证据：核心设计提交 `316a1d8`；三份项目Skill审计、Markdown UTF-8/本地链接、27个决定唯一性、Skill哈希、冲突/占位和 `git diff --check` 均通过；官方Skill快速校验脚本因随附Python缺少其自身 `PyYAML` 依赖未能启动，未联网安装或伪造通过，等价前置元数据约束检查通过；允许本机回环后，类型检查、63个测试文件/132项测试和API/Web/Worker构建全部通过；隔离空库迁移应用0001—0009，重复迁移为 `applied: []`、Schema 9；核心提交后干净工作树发布验收3/3、全部审计项通过且 `failures: []`。这些证据只证明E0设计与历史首版回归通过，小文秘书、本地模型、11人团队和新路由尚未实现。

## 增量任务 UI-20260717-01：工作台界面重设计

- `release_id`：沿用首版不可变追踪键 `wm-v1-20260716-220959-d5dd704d`，作为已发布首版的可逆界面增量，不重写阶段0至8历史。
- 状态：完成；功能提交 `81b61f2` 后干净工作树终验全部通过，证据见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-17-ui-redesign.md`。
- 目标：实现智囊团式浅绿内容优先工作台、紧凑侧栏、九岗位原型头像、左侧真实任务中心，以及可持久化的底色和字体设置。
- 不做什么：不接入付费模型，不更换技术栈，不新增数据库迁移，不修改、停止或重启 `D:\AI智囊团`，不伪造成员状态。
- 唯一负责人：当前Codex。
- 允许文件：`apps/web` 工作台与静态头像、任务只读映射与取消客户端、`apps/api/src/application/tasks/task-service.ts`、相关测试和当前规格/使用文档。
- 禁止文件：`D:\AI智囊团` 内全部文件、既有已合并迁移、来源快照和运行数据。
- 依赖：现有任务状态机、工作区聚合API、九岗位实例、Phosphor Icons、Vite和原生CSS。
- 实现约束：任务章节和状态来自数据库；取消调用真实REST端点；头像使用文秘写作自有副本；主题和字体仅写本机 `localStorage`；左右栏压窄但不牺牲键盘、移动抽屉和对比度。
- 验收标准：九头像可见；当前任务位于左栏并显示章节/阶段；详情含任务书、检查点、岗位和ID；可取消状态可真实取消；四种底色和四档字体即时生效并持久化；桌面和窄屏无横向挤压。
- 测试命令：`npm run verify`、`npm run migrate`、`npm run acceptance`，并使用本机Chrome生成1600×1000与500×844截图进行人工视觉检查。
- 停止条件：若需要改动数据库结构、引入付费服务、永久删除数据或无法保持旧任务兼容，停止并升级给老板；本次均未触发。
- 回滚方法：对最终交付中记录的本次界面提交执行非破坏性的 `git revert`；任务字段只扩大响应且无迁移，回滚不改运行数据。
- 复核人：当前Codex在实现完成后执行独立验证轮，不调用其他开发Agent。
- 最终证据：类型检查通过；52个测试文件、98项测试通过；3项发布验收测试及全部审计检查通过；生产构建通过；Schema 7迁移结果为 `applied: []`；本机健康检查为 `ok`、9个岗位真实返回；桌面与窄屏截图位于被Git忽略的 `data/verification/ui-redesign`。

## 增量任务 UI-20260717-02：女性成员身份与更窄侧栏

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：完成；功能提交 `28c5077` 后，干净工作树发布验收全部通过，证据见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-17-agent-identities.md`。
- 目标：把职责长称改为九名女性成员加简短职业名称，采用智囊团式真实状态，九名成员全部直接显示，并将桌面左右栏继续收窄到约176像素和190像素。
- 不做什么：不改变9岗位数量、职责、权限、调度或模型绑定；不引入付费服务；不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex。
- 允许文件：Agent领域定义、Agent团队服务、新增向前迁移、Schema契约、Web成员栏与布局、相关测试、当前规格和本任务验收证据。
- 禁止文件：既有已合并迁移、来源快照、AI智囊团文件和运行数据。
- 依赖：稳定 `role_key`、现有Agent实例、任务状态机、Worker心跳、原型头像和CSS主题令牌。
- 实现约束：`displayName` 为姓名、`roleName` 为短岗位；完整职责独立保存；工作状态由真实任务和Worker派生；迁移只更新显示字段。
- 验收标准：九名女性成员及短岗位完整显示；职责长称和模型字符串不占成员栏；状态语义与智囊团一致；两侧栏继续缩窄；旧书数据升级后任务和Agent引用不变。
- 测试命令：目标回归、`npm run verify`、两次 `npm run migrate`、`npm run acceptance`，并进行桌面与窄屏视觉检查。
- 停止条件：出现任务引用破坏、迁移不可逆损坏、需要付费或需要修改AI智囊团时停止；当前均未触发。
- 回滚方法：界面和代码使用 `git revert`；已执行的显示字段迁移保持向前历史，如确需恢复旧显示名必须新增反向迁移或在老板授权后使用本次迁移前验证备份，绝不直接改写已执行迁移。
- 复核人：当前Codex独立复核，不调用其他开发Agent。
- 最终证据：52个测试文件、99项测试全部通过；生产构建成功；发布验收3/3且审计无失败；Schema 9重复迁移为 `applied: []`；本机健康检查为 `ok`，实际工作区返回9名女性成员和新职业名称；迁移前备份已通过数据库哈希验证；桌面与窄屏截图位于被Git忽略的 `data/verification/agent-identities`。

## 增量任务 UI-20260717-03：任务中心二级页面

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：完成；功能提交 `b892afe` 后，干净工作树发布验收全部通过，证据见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-17-task-center-page.md`。
- 目标：把预算和待确认从右栏迁入由左侧任务入口打开的二级任务页面，右栏只显示团队成员。
- 不做什么：不改变任务、预算或确认的数据结构与业务规则；不新增数据库迁移、依赖或付费服务；不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex。
- 允许文件：Web工作台组件和样式、相关体验测试、当前产品/使用/验收文档、任务账本和本增量验收证据。
- 禁止文件：既有已合并迁移、来源快照、AI智囊团文件和运行数据。
- 依赖：工作区聚合API、任务状态机、预算记录、重大确认接口、现有移动抽屉和主题令牌。
- 实现约束：任务入口打开中间二级页；右栏仅接收团队和Worker状态；确认与任务取消继续调用真实REST接口；不伪造预算、待确认或成员状态。
- 验收标准：初始右栏不出现预算和待确认；任务二级页完整显示当前任务、最近任务、预算与待确认；确认和取消操作保持有效；桌面与窄屏无横向溢出。
- 测试命令：目标体验测试、`npm run verify`、`npm run migrate`、`npm run acceptance`，并进行桌面与窄屏视觉检查。
- 停止条件：需要改变任务/预算/确认核心模型、付费、密钥、永久删除、生产数据恢复或修改AI智囊团时停止；当前均未触发。
- 回滚方法：对本增量提交执行非破坏性 `git revert`；本次无数据库迁移和运行数据写入。
- 复核人：当前Codex独立复核，不调用其他开发Agent。
- 最终证据：52个测试文件、100项测试全部通过；生产构建成功；发布验收3/3且审计无失败；Schema 9重复迁移为 `applied: []`；在线健康、数据库和Worker均正常；真实Chrome在1600×1000下确认右栏只含9名团队成员，在500×844下确认任务页单列且无横向溢出；功能提交已推送至私有仓库。

## 增量任务 MODEL-20260717-01：九岗位订阅与套餐模型接入

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：完成；功能提交 `e95a0a7` 后干净工作树发布验收全部通过，最终证据见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-17-subscription-models.md`。
- 目标：按老板最新决定完成九岗位模型分配、详细提示词、历史书籍不可变快照绑定、开放讨论与章节写作/异模型审校接线、脱敏状态展示和桌面环境装载。
- 不做什么：不调用按量付费API，不写密钥，不伪造Kimi 2.7，不伪造盲选或Agent状态，不修改/停止/重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：模型适配与配置、岗位提示词、Agent/讨论/章节应用服务、健康接口、设置页、桌面启动脚本、测试和当前规格/验收文档。
- 禁止文件：来源快照、已合并迁移、AI智囊团全部文件和生产正文。
- 依赖：本机Codex ChatGPT登录态、已有火山方舟Coding Plan/Agent Plan环境凭证、现有模型快照/调用/预算/任务状态机。
- 实现约束：Codex临时只读无工具；方舟URL严格白名单；现金回退关闭；密钥只读环境；历史快照不修改；主笔和审校真实异模型；审校JSON严格校验；模型错误脱敏和真实取消。
- 验收标准：九岗位映射与提示词完整；现有书籍新增九个绑定快照且旧快照不变；老板指定主笔记录为 `owner_specified`；讨论和章节流水线调用指定适配器；现金记录为0；设置页只显示脱敏模型状态；确定性回归不破坏。
- 测试命令：目标基础/集成测试、`npm run verify`、`npm run build`、空库与现有库迁移/重复迁移、`npm run acceptance`、本机真实Codex/方舟烟雾测试和恢复/隔离检查。
- 停止条件：需要实际付费、需要新密钥、永久删除、重大架构变更、生产数据恢复或真实通道发生无法自行消除的外部阻塞。
- 回滚方法：代码与文档使用非破坏性 `git revert`；环境变量可移除；新模型快照保留历史并通过后续快照切回，不直接改写或删除。
- 复核人：当前Codex完成独立本地代码复核和全量门禁，不调用其他开发Agent。
- 最终证据：59个测试文件、117项测试全部通过；API/Web/Worker生产构建成功；发布验收3/3且审计无失败；现有库和空库均完成Schema 9与重复迁移验证；真实Codex及四类方舟模型烟雾测试通过；桌面冷启动为 `subscription-plan`，5组模型、9个Agent和Worker状态正确；恢复副本已验证；现金回退关闭且源码无凭证。

## 增量任务 FLOW-20260717-01：开放对话与写作准备门禁纠错

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`，作为首版流程纠错增量，不改写阶段0至8历史。
- 状态：已完成；根因、实现、回归、全量门禁、迁移、实机运行、备份恢复、生产孤儿状态修复、干净工作树发布审计、Git提交与远程备份全部完成。
- 目标：让普通消息得到活动主编真实回复，让自然创作意图自动进入相关岗位讨论，并保证任何入口都只能在老板确认规划成果后启动主笔。
- 不做什么：不使用聊天自动污染长期记忆/正史，不调用按量付费API，不写入密钥，不永久删除用户数据，不修改/停止/重启 `D:\AI智囊团`，不伪造岗位意见、主编汇总、章纲或章末事实。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：聊天/讨论/规划成果/章节流水线/Worker应用服务、Web工作台、相关测试、当前规格、任务账本与本增量验收证据。
- 禁止文件：来源快照、既有已合并迁移、AI智囊团全部文件、生产正文和API密钥。
- 依赖：现有消息、任务、上下文包、模型调用、预算、讨论决定、成果版本、章节状态机和订阅/套餐模型适配器。
- 实现约束：普通回复与讨论均持久化任务并携带上下文包；最近聊天最多12条且不进入长期记忆；创作确认才生成规划成果；直接批次API与对话命令共用准备门禁；章前和章末数据必须有真实来源；取消且无正文的章节壳只能安全复用并恢复为未开始；开放回复不按整章最高推理强度运行。
- 验收标准：问候有真实主编回复；创作意图无需前缀即可讨论；相关岗位先回复、主编后汇总；未准备写作零章节零主笔任务；确认后生成可追溯创作方案/故事圣经/总纲/章纲；已取消第1章可以在资料齐备后安全重做；固定测试剧情不进入生产事实或结算。
- 测试命令：目标回归、`npm run typecheck`、`npm test`、`npm run build`、空库/升级/重复迁移、`npm run acceptance`、桌面实机开放对话与规划门禁烟雾、备份和恢复验证。
- 停止条件：需要实际付费、新密钥、永久删除、重大架构变更、生产数据恢复，或出现三轮仍无法消除的外部阻塞时暂停；当前均未触发。
- 回滚方法：代码和文档使用非破坏性 `git revert`；新增消息、任务、讨论和成果版本保留审计历史；不删除已取消任务或旧章纲。如需回退运行数据，只能使用本增量前已验证备份并另获老板授权。
- 复核人：当前Codex按代码复核技能和完整质量门禁独立复核，不调用其他开发Agent。
- 最终证据：功能提交 `a560272`；62个测试文件、126项测试全部通过；API/Web/Worker生产构建成功；发布验收3/3且审计 `failures: []`；现有库与空库Schema 9、重复迁移、完整性和外键检查通过；真实GPT-5.6 Sol开放回复成功；未准备“写一章”零章节零正文；最终备份 `backup-2026-07-16T23-35-45-708Z-26cedeb7` 已验证；远程 `origin/main` 已核对为 `a5602720da8375724b81c09ad113177e8b5ac9c4`。

## 增量任务 QUALITY-20260718-01：长篇创作质量审查Skill

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：完成；Skill、规格、审计、自动化验证与项目规则已安装，核心提交为 `a43ce49`，证据见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-18-longform-quality-skill.md`。
- 目标：把长篇小说的注意力分散、正史漂移、检索缺失、规划漂移、角色同质化、迎合作家和自证式测试转换为可执行的设计审查、反例与E0—E4证据门禁。
- 不做什么：本任务不实现运行时混合RAG，不宣称已经改善真实长篇文学质量，不调用按量付费API，不写密钥，不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex；按老板规则未调用其他开发Agent。
- 允许文件：项目级 `.agents/skills/wenmi-longform-quality`、Skill评测夹具、长篇质量当前规格与差距审计、受影响的当前产品/架构/记忆/Agent/验收规则、任务账本和增量验收证据。
- 禁止文件：来源快照、既有已合并迁移、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：Codex Skill Creator规范、项目现有Vitest门禁、Git和当前发布账本；不增加产品运行时依赖。
- 实现约束：结论必须使用“痛点→症状→根因→机制→反例→指标→证据→停止条件”能力链；作者观点不是默认事实；确定性测试不能冒充文学质量证据；只有E3＋E4才可在明确范围内报告长篇质量改善。
- 验收标准：Skill结构校验通过；审计产物验证器能接受完整报告并拒绝缺反例与过度承诺；触发、反触发和对抗案例齐备；当前规格如实区分现有FTS能力与目标混合RAG；全量类型、测试、构建、迁移/Repository/恢复、运行和发布验收通过。
- 测试命令：官方 `quick_validate.py`、审计验证器、Skill目标Vitest、`npm run verify`、迁移/Repository/恢复测试集、`npm run acceptance`、现有本地服务健康核验和 `git diff --check`。
- 停止条件：需要实际付费、新密钥、永久删除、重大架构变更、生产数据恢复或无法自行消除的外部阻塞时暂停；本任务均未触发。
- 回滚方法：先从 `AGENTS.md` 移除强制Skill引用，再对本增量提交执行非破坏性 `git revert`；本任务无迁移和生产数据改写。
- 复核人：当前Codex按代码复核与完成前验证规则执行独立复核；因老板禁止其他开发Agent，未委派复核。
- 最终证据：官方Skill结构校验通过；Skill验证器目标测试3/3；全量63个测试文件、129项测试通过；API/Web/Worker生产构建通过；迁移/Repository/恢复测试8个文件、13项通过；本地Web/API均返回200且API报告同一release、Schema 9和现金回退关闭；提交后发布验收3/3且审计 `failures: []`。

## 增量任务 QUALITY-20260718-02：创造性保护型长篇质量Skill

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：已完成（Skill、验证器和当前规格范围）；运行时业务实现与E3/E4真实创作证据不在本任务中冒充完成。
- 目标：把“不得以一致性、检索、规划、记忆或审校为代价削弱小说创造性和输出质量”落实为可执行、可验证、可回滚的Skill最高约束。
- 不做什么：不实现运行时业务功能，不修改数据库或迁移，不写生产正文，不调用付费API，不写密钥，不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：项目级 `wenmi-longform-quality` Skill及测试、DEC-010、长篇质量/产品/记忆/Agent/验收当前规格、覆盖矩阵、任务账本、实施计划和增量验收证据。
- 禁止文件：业务源码、既有迁移、来源快照、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009、Skill Creator规范、现有Node验证器、Vitest和E0—E4证据体系；不增加运行时依赖。
- 实现约束：四种创作模式、五级约束、九岗位差异化上下文、最小充分检索、先完整生成后软审校、经验版本化与创造性非劣效必须同时成立；安全和正史硬门禁保持不变。
- 验收标准：Skill结构校验通过；验证器拒绝缺创造性保护和明确过度约束的审计；对抗案例覆盖创造性塌缩路径；当前规格同步；类型、测试、构建、迁移/隔离/恢复、运行和发布验收通过。
- 测试命令：官方 `quick_validate.py`、审计验证器正反夹具、目标Vitest、`npm run verify`、迁移/Repository/恢复测试集、现有服务只读健康核验、`npm run acceptance` 和 `git diff --check`。
- 停止条件：需要实际付费、新密钥、永久删除、重大架构改变、生产数据恢复，或无法在不降低创造性的前提下形成可验证规则时暂停；当前未触发。
- 回滚方法：对本增量提交执行非破坏性 `git revert`；如新增结构检查导致机械化，先回退对应强制项并保留DEC-010原则，不改写历史。
- 复核人：当前Codex依据Skill Creator、代码复核和完成前验证规则独立复核，不委派其他Agent。
- 最终证据：`docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-18-creativity-preserving-skill.md`。官方Skill结构通过；目标验证器1个文件/6项通过；全量63个文件/132项通过并完成三端构建；迁移/Repository/恢复8个文件/13项通过；真实Web/API/Worker只读健康通过；核心提交后发布验收3/3且审计`failures: []`。

## 设计任务 DESIGN-20260718-01：稳定表达基线与动态场景技法

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：设计规格完成；运行时实现与真实创作证据尚未开始，不得冒充功能已完成。
- 目标：取消全书固定一至三种写作手法，把长期稳定的作品表达身份与按场景动态选择的具体技法分层，并保护主笔自由创作区。
- 不做什么：本任务不修改业务代码、迁移或生产数据，不调用真实模型，不引入付费服务，不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：老板决定登记册及当前产品、数据、Agent、岗位提示、记忆、长篇质量、验收、覆盖矩阵和任务账本文档。
- 禁止文件：来源快照、业务源码、既有迁移、运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009、DEC-010、项目级 `wenmi-longform-quality` Skill和老板本轮明确决定。
- 实现约束：建书只确认全书表达基线；场景记录叙事功能和目标读者效果；主笔按场景选择最少充分技法；内部技法库是抽象软工具箱，不绑定具体作品或作者，不按剧情标签机械套用。
- 验收标准：DEC-011生效；所有受影响的当前规格一致；明确区分规格E0和运行时证据；没有把技法卡写成强制模板或把动态技法冒充已实现。
- 测试命令：`git diff --check`，并检索当前规格中的“固定模板”“动态技法”“全书表达基线”和DEC-011覆盖关系。
- 停止条件：若需要改变正史权威、引入外部服务、现金付费、永久删除或修改生产数据，停止并提交重大决定；本任务未触发。
- 回滚方法：对本次文档提交执行非破坏性 `git revert`；本任务没有运行数据或Schema变化。
- 复核人：当前Codex依据长篇质量Skill执行独立反例复核，不委派其他开发Agent。
- 当前证据：老板确认文本、DEC-011、9份受影响当前规格、差距审计和任务账本已同步；`git diff --check` 通过。运行时场景契约、技法路由和盲评仍为后续独立任务。

## 设计任务 DESIGN-20260718-02：混合RAG数据库与技术选型

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：设计规格完成；运行时实现、迁移和E1—E4证据尚未开始，不得冒充混合RAG功能已完成。
- 目标：在本机零现金、Windows/Node、可恢复和长篇创造性约束下，确定权威数据库、向量索引、关系/Wiki、切片、嵌入、融合、同步和重建的最终技术架构。
- 不做什么：本任务不安装运行时依赖、不修改业务代码或数据库、不调用付费API、不写密钥、不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：`AGENTS.md`、老板决定登记册、混合RAG设计及当前架构/数据/记忆/长篇质量/验收/实施/覆盖/差距/任务文档。
- 禁止文件：来源快照、业务源码、既有迁移、生产数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009至DEC-012、当前Node 24 `node:sqlite` 实现、项目长篇质量Skill和记忆系统设计规则。
- 实现约束：SQLite与不可变正文保持唯一权威；LanceDB仅保存按书可重建向量投影；FTS5负责精确检索；关系/Wiki由SQLite派生；不增加独立服务或端口；正式/临时索引隔离；投影带正史水位；本地嵌入模型按独立金标选择并锁定快照。
- 验收标准：DEC-012生效；`docs/HYBRID_RAG_DESIGN.md`包含方案比较、切片、生命周期、反例、恢复和E0—E4；受影响当前规格一致；明确运行时尚未实现。
- 测试命令：混合RAG审计结构验证、`git diff --check`、规格关键词与冲突扫描、文档引用存在检查。
- 停止条件：若需要付费、密钥、独立数据库服务、生产恢复、永久删除或修改运行数据，停止并提交单独授权；本任务未触发。
- 回滚方法：对本次文档提交执行非破坏性 `git revert`；没有Schema、依赖或运行数据变化。
- 复核人：当前Codex依据长篇质量和记忆系统Skill独立复核，不委派其他Agent。
- 当前证据：官方SQLite、Node、LanceDB、Transformers.js、ONNX Runtime、Qwen、BGE与GBrain资料已核对；设计仍为E0，后续必须依次取得E1实现、E2恢复隔离、E3独立金标消融和E4真实创作证据。

## 设计任务 DESIGN-20260718-03：500万字与全书生命周期Agent陪伴

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：设计规格完成；运行时、迁移、满规模测试和真实1000章纵向证据尚未开始，不得冒充功能已完成。
- 目标：把单书300至500万字、1000章以上和九名成员从开书到完结持续陪伴转化为可实现、可验收、不会导致上下文过载或Agent空转的最终规格。
- 不做什么：本任务不修改业务代码、依赖、迁移或生产数据，不运行真实模型，不产生现金费用，不修改、停止或重启 `D:\AI智囊团`。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：老板决定登记册、项目章程、超长篇专项设计和当前产品/架构/数据/Agent/记忆/RAG/质量/API/实施/路线图/验收/知识/任务文档。
- 禁止文件：来源快照、业务源码、既有迁移、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009至DEC-013、项目长篇质量Skill、记忆系统与上下文压缩规则、老板本轮明确规模目标。
- 实现约束：SQLite和正文保持权威；使用五级增量连续性、承诺账本、滚动规划、岗位连续性日志、有界连续性包和分层质量窗口；原始聊天不默认长期化；按需Agent未激活零调用；容量、连续性和文学质量分开声明。
- 验收标准：DEC-014生效；目标按500万规范化中文字符、1500章留余量；所有当前规格一致；E0设计与E1—E4运行证据明确分开；不存在用200章外推1000章、用容量冒充文学质量或用档案伪造Agent在线的表述。
- 测试命令：`git diff --check`、文档引用存在检查、500万/1500章/连续性关键词覆盖和旧门槛冲突扫描；业务全量门禁仅验证文档修改未破坏现有项目，不作为新能力证据。
- 停止条件：需要业务代码、迁移、真实付费、密钥、独立数据库服务、生产恢复或永久删除时停止并取得新的明确授权；本任务未触发。
- 回滚方法：对本次文档提交执行非破坏性 `git revert`；本任务没有运行数据、Schema或依赖变化。
- 复核人：当前Codex依据长篇质量、记忆系统和上下文压缩规则独立复核，不委派其他Agent。

## 设计任务 DESIGN-20260718-04：阶段摘要优先与关键触发回查正史

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：设计规格完成；运行时、Schema、迁移、前端与E1—E4证据尚未开始，不得冒充已实现功能。
- 目标：把“旧阶段默认读总结，触发关键信息时回查前文正史”设计为可实现、可审计、上下文有界且不削弱创造性的正式规则。
- 不做什么：本任务不修改业务代码、依赖、迁移或运行数据，不调用真实模型，不产生现金费用，不修改、停止或重启 `D:\AI智囊团`，不把设计E0称为产品能力。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：项目规则、老板决定、超长篇/RAG/记忆/数据/Agent/产品/API/质量/实施/路线图/验收/知识/任务文档和项目级长篇质量Skill。
- 禁止文件：来源快照、业务源码、既有迁移、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009、DEC-010、DEC-012、DEC-014、项目长篇质量Skill、记忆系统与上下文压缩规则，以及老板确认的阶段总结/关键触发判断。
- 实现约束：阶段摘要只作路由投影；硬事实和开放线程不按距离衰减；触发按卷→故事弧→章节/场景→正史原文下钻；一次初始检索加一次有界补充周期、最大三级；岗位差异化；本地候选与模型注入Token分开审计；摘要失败从权威源重建。
- 验收标准：DEC-015生效；受影响当前规格一致；第500/1000/1500章早期正史回查、无关旧文不注入、摘要错误兜底、Token有界和创造性非劣效均有明确测试门槛；E0与E1—E4严格分开。
- 测试命令：`git diff --check`、文档引用存在检查、DEC-015关键词与冲突扫描、项目级Skill结构验证；业务全量门禁只验证文档/Skill修改没有破坏现有项目，不作为新能力证据。
- 停止条件：需要业务代码、迁移、真实付费、密钥、独立数据库服务、生产恢复或永久删除时停止并取得新的明确授权；本任务未触发。
- 回滚方法：对本次文档提交执行非破坏性 `git revert`；本任务没有运行数据、Schema或依赖变化。
- 复核人：当前Codex依据长篇质量、记忆系统和上下文压缩规则独立复核，不委派其他Agent。
- 当前证据：项目级Skill `quick_validate.py` 通过，长篇质量审计结构验证通过，`git diff --check` 通过；全量类型检查、63个测试文件/132项测试和三应用构建通过；发布验收3/3功能用例通过，提交前审计仅按预期报告工作树未提交；隔离空库迁移9个版本及重复迁移通过，隔离API健康检查返回Schema 9和确定性模型模式。本证据只证明规格一致和既有首版未被破坏，DEC-015运行时仍为E0。

## 设计任务 DESIGN-20260718-05：四层知识生命周期与保留清理

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：设计规格完成；Schema、迁移、Repository、Worker、前端、清理任务和恢复证据尚未开始，不得冒充已实现功能。
- 目标：把临时资料、待确认候选、老板确认正史和可重建投影的边界、提升、纠错、归档、清理与恢复规则锁定为一套不会互相污染的知识生命周期。
- 不做什么：本任务不修改业务代码、依赖、迁移或运行数据，不调用真实模型，不产生现金费用，不修改、停止或重启 `D:\AI智囊团`，不执行永久删除。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：项目规则、决定、架构、数据、记忆、混合RAG、产品、API、实施、验收、覆盖矩阵、知识和任务文档。
- 禁止文件：来源快照、业务源码、既有迁移、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-012至DEC-015、老板确认的“老板确认才进入正史、未确认资料留在临时/候选层”以及长篇质量与记忆系统规则。
- 实现约束：一个SQLite权威；四层是权限与生命周期而非四库；提升追加新版本；聊天不正式向量化；每章20个热稿件版本；任务临时向量索引7天宽限；作者原始资产不自动永久删除；陈旧投影不得静默使用。
- 验收标准：DEC-016生效且当前规格一致；提升原子性、跨层隔离、归档恢复、候选排除、宽限清理、重复清理、投影重建和严格永久删除均有明确运行时测试；E0与E1—E4严格分开。
- 测试命令：`git diff --check`、文档引用存在检查、DEC-016/四层/保留规则冲突扫描；业务全量门禁只验证文档修改没有破坏既有首版，不作为新能力证据。
- 停止条件：需要业务代码、迁移、真实付费、密钥、独立数据库服务、生产恢复或永久删除时停止并取得新的明确授权；本任务未触发。
- 回滚方法：对本次文档提交执行非破坏性 `git revert`；本任务没有运行数据、Schema或依赖变化。
- 复核人：当前Codex依据长篇质量和记忆系统规则独立复核，不委派其他Agent。
- 当前证据：`git diff --check` 通过；类型检查通过；63个测试文件/132项测试全部通过；API、Web和Worker构建通过。首次受限环境运行因本地回环连接被拒绝（`EACCES`）产生3项连锁失败，同一代码在允许 `127.0.0.1` 临时端口后原命令完整通过，确认是执行环境限制而非代码回归。本证据只证明文档修改未破坏既有首版，DEC-016运行时仍为E0。

## 设计任务 DESIGN-20260718-06：长篇资料切片最终定案

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- 状态：设计规格完成；运行时Schema、迁移、切片器、LanceDB、独立金标与真实创作评测尚未开始，不得冒充已实现或有效。
- 目标：把正文、设定、规划、事实、Wiki、人物声音、摘要、聊天/草稿和研究资料的切片、来源定位、叙事状态、增量重建、原子切换、查询扩展和创造性门禁一次设计完整。
- 不做什么：本任务不修改业务代码、依赖、迁移或运行数据，不下载模型，不产生现金费用，不修改、停止或重启 `D:\AI智囊团`，不执行永久删除。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：项目规则、决定、切片/RAG/架构/数据/记忆/产品/API/质量/实施/验收/覆盖矩阵/知识/任务文档和项目级Skill验证证据。
- 禁止文件：来源快照、业务源码、既有迁移、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-012、DEC-014至DEC-016、项目长篇质量Skill、记忆系统规则，以及老板要求全维度检查后直接定案的授权。
- 实现约束：不可变原文＋结构节点＋父子检索块＋原子事实；原始UTF-8字节范围精确回链；默认零复制重叠；嵌入文本与证据分离；特殊叙事模式分开；新切片快照探针通过后原子切换；所有参数版本化可回滚。
- 验收标准：DEC-017生效；专项设计通过Skill结构验证；当前规格一致；固定切片、纯段落/语义、零盲目重叠父子块和可选小重叠有独立对照；硬状态100%、跨书0、来源回链100%且创造性非劣效。
- 测试命令：`node .agents/skills/wenmi-longform-quality/scripts/validate-audit.mjs docs/CHUNKING_DESIGN.md`、Skill快速验证、`git diff --check`、引用/冲突扫描和既有项目全量门禁；E0文档验证不作为E1—E4能力证据。
- 停止条件：需要业务代码、迁移、真实付费、密钥、独立数据库服务、生产恢复或永久删除时停止并取得新的明确授权；本任务未触发。
- 回滚方法：对本次文档提交执行非破坏性 `git revert`；未来运行时按快照当前指针切回上一已验证策略，不回滚原始资料或正史。
- 复核人：当前Codex依据项目长篇质量Skill和记忆系统规则完成两轮审查，不委派其他Agent。
- 当前证据：专项审计器返回 `PASS: docs\CHUNKING_DESIGN.md`；Skill元数据、触发身份和审计入口检查通过；DEC-017引用/覆盖与被替代重叠规则冲突扫描通过；`git diff --check` 通过；类型检查通过；63个测试文件/132项测试全部通过；API、Web和Worker构建通过。本证据只证明E0规格完整且没有破坏既有首版，切片运行时和E1—E4证据仍不存在。

## 设计任务 DESIGN-20260718-07：Skill前置设计治理与既有方案复核

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- `design_review_id`：`DR-20260718-01`。
- 状态：设计规格与10项已知缺口修正完成；运行时Schema、Repository、压缩器、查询规划、满规模回放和E1—E4证据尚未实现，不得冒充产品已具备这些能力或不存在运行时Bug。
- 目标：让重要方案在推荐形成前执行适用Skill，并复核DEC-009至DEC-017的数据库、正史、RAG、切片、阶段摘要、长会话和500万字连续性设计，修复实现前可见的权威、时间、候选、压缩、下钻、Token、消歧和离线模型边界。
- 不做什么：本任务不修改业务代码、依赖、迁移或运行数据，不调用真实创作模型，不产生现金费用，不永久删除，不修改、停止或重启 `D:\AI智囊团`，不把E0设计称为运行时完成。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：项目规则、老板决定、设计治理审计及当前架构/数据/记忆/RAG/长篇质量/超长篇/实施/验收/覆盖/知识/任务与增量证据文档。
- 禁止文件：来源快照、业务源码、既有迁移、生产运行数据、API密钥和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009至DEC-017，项目 `wenmi-longform-quality`、`memory-systems` 和 `context-compression` Skill，老板本轮要求，以及SQLite/LanceDB/Transformers.js当前官方技术边界。
- 实现约束：保存Skill版本/哈希与两轮审查；硬规则/偏好分层；确认原文高于自动抽取；三轴时间；候选休眠；锚定式压缩与轮换探针；下钻唯一计数；先预留输出；同名不静默消歧；正常运行禁用远程模型加载。
- 验收标准：DEC-018生效；`docs/DESIGN_GOVERNANCE_AUDIT.md` 通过Skill审计器；受影响当前规格无相反规则；官方Skill结构、引用、差异格式、全量类型/测试/构建、迁移/隔离/恢复和发布验收通过；E0与E1—E4明确分开。
- 测试命令：官方 `quick_validate.py`、`validate-audit.mjs`、引用/冲突扫描、`git diff --check`、`npm.cmd run verify`、迁移/Repository/恢复目标集和 `npm.cmd run acceptance`；工作树干净审计在提交后复跑。
- 停止条件：需要业务代码、迁移、真实付费、密钥、独立数据库服务、生产恢复、永久删除或无法裁决的重大架构变化时停止；本任务未触发。
- 回滚方法：对本任务文档提交执行非破坏性 `git revert`；未来运行时分别回滚Skill影响决定、投影快照、软偏好/压缩/检索策略，不回滚正史、安全、版权和隔离门禁。
- 复核人：当前Codex依据三套Skill完成两轮审查；受“不调用其他开发Agent”约束，本次E0审查不声称人员独立，后续E3/E4必须使用独立金标、盲评和真实作者验收。
- 最终证据：核心提交 `47e6089`；项目审计器返回 `PASS: docs\DESIGN_GOVERNANCE_AUDIT.md`；官方Skill校验返回 `Skill is valid!`；Markdown引用、冲突/占位扫描与 `git diff --check` 通过；受限环境首次全量测试因临时回环 `EACCES` 产生3项连锁失败，同一代码在允许 `127.0.0.1` 后类型检查、63个测试文件/132项测试和API/Web/Worker构建全部通过；迁移/Repository/恢复目标集11个文件/19项通过；核心提交后发布验收3/3通过且审计 `failures: []`、工作树clean。完整记录见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-18-skill-first-design-governance.md`。

## 设计任务 DESIGN-20260718-08：四路混合检索编排最终定案

- `release_id`：沿用不可变追踪键 `wm-v1-20260716-220959-d5dd704d`。
- `design_review_id`：`DR-20260718-02`。
- 状态：E0设计与当前规格同步完成；运行时Schema、迁移、Repository、四路检索、融合校准和E1—E4证据尚未完成，不得冒充产品功能已实现。
- 目标：面向单书500万规范化中文字符、1500章，完整确定结构化事实、FTS/BM25、LanceDB向量和SQLite Wiki/关系的查询路由、权威边界、融合、证据闭环、岗位配方、预算、降级、恢复和验收。
- 不做什么：本任务不修改业务源码、依赖、迁移或运行数据，不调用真实/付费模型，不安装独立数据库服务，不永久删除，不修改、停止或重启 `D:\AI智囊团`，不把E0设计称为已实现或有效。
- 唯一负责人：当前Codex；不调用其他开发Agent。
- 允许文件：老板决定、四路检索专项设计及当前架构/数据/记忆/RAG/Agent/超长篇/产品/API/质量/实施/路线图/验收/覆盖/知识/任务和增量证据文档。
- 禁止文件：来源快照、业务源码、既有迁移、生产运行数据、API Key和 `D:\AI智囊团` 全部文件。
- 依赖：DEC-009至DEC-018，项目 `wenmi-longform-quality`、`memory-systems`、`context-compression` Skill，SQLite FTS5和LanceDB官方技术边界。
- 实现约束：查询计划先锁定书/正史/三轴时间/观点/实体；结构化/FTS/向量/关系各司其职；H/E/I三车道；同源证据簇；冲突不平均；关系有界；确定性结论回查权威源；九岗位差异化；本地候选与模型Token分计；E0参数版本化。
- 验收标准：DEC-019生效；`docs/HYBRID_RETRIEVAL_ORCHESTRATION.md` 通过Skill审计器；受影响当前规格一致；同源重复、错误实体种子、冲突、无答案、关系爆炸、投影落后和创造性退化都有停止/回滚与后续测试门禁；E0与E1—E4严格分开。
- 测试命令：专项审计器、Skill快速验证、Markdown引用/冲突扫描、`git diff --check`、`npm.cmd run verify`、迁移/Repository/恢复目标集和 `npm.cmd run acceptance`；工作树干净审计在提交后复跑。
- 停止条件：需要业务代码、迁移、真实付费、密钥、独立数据库服务、生产恢复、永久删除或无法裁决的重大架构变化时停止；本任务未触发。
- 回滚方法：对本任务文档提交执行非破坏性 `git revert`；未来运行时按不可变策略/索引快照回滚RRF、Top-K、关系预算、重排和灵感模板，不回滚正史、安全、版权和隔离门禁。
- 复核人：当前Codex依据三套Skill完成两轮审查；受“不调用其他开发Agent”约束，本次E0审查不声称人员独立，后续E3/E4必须使用独立金标、隐藏对抗集、盲评和真实作者验收。
- 当前证据：核心提交 `b10630c`；专项审计器返回 `PASS: docs\HYBRID_RETRIEVAL_ORCHESTRATION.md`；官方Skill校验返回 `Skill is valid!`；`git diff --check` 和规格扫描通过；相同代码在允许 `127.0.0.1` 回环后类型检查、63个测试文件/132项测试和API/Web/Worker构建全部通过；迁移/Repository/恢复目标集11个文件/19项通过；提交前发布功能验收3/3通过，提交后复跑3/3通过且审计 `failures: []`、工作树clean。完整记录见 `docs/releases/wm-v1-20260716-220959-d5dd704d/increments/2026-07-18-four-channel-retrieval-orchestration.md`。
